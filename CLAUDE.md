# SpaceFace — Claude entry point

This repo has **one** agent front door: [`AGENTS.md`](AGENTS.md). It applies to every agent brand
(Claude, Codex, Cursor, ZCode, …) so no brand gets different law. It routes to depth docs —
`docs/ORIENTATION.md` (repo map), `docs/AGENT_OPERATIONS.md` (working agreement),
`docs/AGENT_LESSONS.md` (owner preferences and verified facts) — by need, not by default.

@AGENTS.md
