# Ironback MTX Cycle 29 — REVISE

Cycle 29 is the minimum shared correction from Cycle 28 remaining chase-camera
defects. It keeps the one-piece closed beam, legal occupancies, blunt salvage-
barge proportions, manufactured plate courses, framed dark cab tub, real open
hopper hole, four rooted arm stations, long drill, four-tine grab, saw and
crusher identity, two matched open dorsal pulse houses, and exact-source
evidence machinery. It rebuilds chase-countable hopper process, deepens the
pulse pair as two named dorsal voids, and recasts dry refractory / matte oxide
/ gunmetal maps so ceramic is not blown beige. The candidate is not accepted
or promoted.

## Controller disposition

`REVISE`

One Blender 5.1 run. Occupancy was legal on the first write; there was no
second run:

- default chase: `17.45%` width (`279.3` px) — inside `8–22%`;
- abeam chase: `11.67%` width (`186.7` px) — inside `8–22%`;
- close chase: `44.32%` width (`709.1` px) — inside the derived `19.862–54.621%` band;
- no supported view crops.

Runtime display scale is `2.502` for the traced `41.28 WU` length. All three
supported views are legal and uncropped. Cycle 25–28 stills remain
byte-for-byte unchanged.

Original-resolution review of the exact-source stills:

- the hull remains one lofted barge with a dorsal cavity and a floor. Abeam
  centerline is dark well, not studio. Plate courses keep lip thickness and
  seam channels. At D=144 they still read more as value steps than a fully
  countable plate diagram; the silhouette is still a blunt barge;
- two pulse houses sit in larger deck pockets as gunmetal U walls with a
  deeper thruster floor. Close port crop is a real dark well (min luma `~9`,
  max `~65`). Starboard still takes more key light (max `~166`). At D=144 the
  pair remains small patches; a stranger glance can still miss two separate
  voids;
- the hopper keeps a real hole. Close and clay show a short fore ceramic
  apron, air, three raised Y-axis drums with hubs/rings, proud V-jaws, and an
  aft dark throat. Default hopper mean luma dropped (`55` to `41`) and ceramic
  peaks in that crop dropped (`216` to `179`); the three drums still compress
  toward one dark rectangle at D=144;
- the four arms keep rooted turntable/yoke/boom load paths and the same four
  tool silhouettes. Close can name all four. Default still shrinks some of
  that into corner clusters;
- cab remains a framed dark greenhouse over the tub;
- oxide paint is matte dielectric with no copper coat. Shoulder/aft thermals
  are dry refractory, separate from oxide, no longer blown (default thermal
  peak `210` to `164`; close thermal peak `216` to `181`). Pulse/tool metal is
  cooler gunmetal. Hopper and pulse pockets keep cavity AO; exposed plate
  stays open.

A re-render, camera change, or texture-noise pass cannot close the remaining
D=144 pulse-void naming or default drum compression. The controller owns
acceptance.

## Retained exact-source evidence

- LOD0: `7071B3CC45E4B4FE49A66ACD602B460F3446DCC9F81CD9C41A371C3B289FA3C2`
  (`42,302` triangles, `8` draws, hull `3,700`)
- LOD1: `C78A798876B1EDB9189FDF45E1DDEF10140EBA844F75A46574672CBFDE1837F8`
  (`39,542` triangles, `8` draws, hull `3,572`)
- LOD2: `B2C1448A04731AFBE07C92FED69C36D9A5159BCCD06A9D88117DCDF909AA5534`
  (`28,606` triangles, `8` draws, hull `3,412`)
- Identity and occupancy: `cycles/cycle_29/EVIDENCE_IDENTITY.json`
- Required stills: `cycles/cycle_29/*.png`

No live, release, package, manifest, runtime, texture-size, or program-map path
changed in this cycle. Cycle 18–28 evidence were not modified.
