<!-- LIFETIME: HISTORICAL -->
# INACTIVE LEGACY PROMPT

This root prompt is retired and MUST NOT be executed, resumed, or treated as standing policy.

It previously contained an unbounded autonomous loop and an impossible “no substantive faults remain”
stopping condition. Those instructions caused agents to spend production budgets on recursive review,
validation-harness repair, acceptance paperwork, and repeated proof attempts instead of building the
game.

Current authority:

1. `AGENTS.md`
2. `CANONICAL_BUILD_MAP.md`
3. `design/program/INFERENCE_LANES.md` for `INFERENCE N ...`
4. the exact user request and live production owners

The historical text is preserved at
`docs/historical/GOAL_PROMPT_2026-08-12.txt` for archaeology only.
