MAKE IT BETTER

You are running the Fun Convergence Loop for SpaceFace. Read, in this order, and then start:
  design/program/FUN_CONVERGENCE_LOOP.md      (the loop, the roles, the stop conditions)
  design/FEEL_CONTRACT.md                     (the bars, in player units, with today's numbers)
  design/VISION.md                            (what the game is supposed to feel like)
  CANONICAL_BUILD_MAP.md section 13C and 15   (the packets; use their IDs, never invent new ones)

Scope: the words after MAKE IT BETTER, if any. Default scope is combat and flight feel in the
Crucible bench. Adventure feel work waits until the Crucible bench passes (Crucible first).

Do this, and do not stop after one step:
1. PLAY the bench on fixed seeds, headless for numbers and headed for frame strips at the shipping
   camera at normal speed.
2. MEASURE every bar the bench reaches plus the fun metrics (verbs per minute, consequences per
   action, time to first consequence, moments per minute, nothing-happened seconds, deaths by
   cause, knock budget on the player).
3. JUDGE with a vision-capable critic that did not make the change, ten questions, each answered
   with the frame that proves it. Take its ONE fundamental.
4. NAME THE FUNDAMENTAL in one line: when X happens, rule R in file F does Y, which breaks vision
   sentence S; changing R should move bar B from n to m.
5. FIX THE GUTS. Numbers, rules, listeners, clamps removed, systems connected. Never add content to
   hide a feel defect, never add drag or clamp given momentum, never give NPCs hidden gyros, never
   use camera shake or particles as the fix, never change a test or a golden to pass. Ship a test
   whose assertion message quotes the vision sentence.
6. COMPARE on the same seeds. Keep only if the bars moved toward target, nothing regressed, the
   critic pass count held, and npm run check:baseline is green. Ties revert.
7. REPORT one page in plain words: WHAT I FOUND, WHAT I CHANGED, WHAT YOU WILL FEEL, THE NUMBERS,
   THE FRAMES, NEXT. Commit the change and the report; push the current branch by name.
8. Take the next lowest bar. Stop only for a stop condition in the loop document, and say which.

Rules that never bend: fixed seeds; one hypothesis per cycle; the critic can see; a number in
player units before and after; the owner is never asked to adjudicate technical risk; finish the
whole cycle before replying.
