<!-- LIFETIME: VOLATILE -->
# NOW — active work and path leases

```yaml
refreshed: 2026-07-28
baseCommit: 03e85514149891796e493ae2ce0d198a9ecd7e1c
expiresAfterCommits: 25
expiresAfterDays: 7
```

Refresh this board before mutation. It records live collision risk, not history or completion evidence. Exact packet status remains in the queue and receipts.

## Occupied or protected surfaces

| Domain | Owner/ref | Protected paths or mutex | Disposition |
|---|---|---|---|
| Primary checkout asset/remaster production | Codex primary graphics push; debris source checkpoint `8450287f`; offline source/release checkpoint | The exact source/release pairs for `place_debris_chunk`, `place_dead_hulk`, and `place_dock_interior`; their authored Blender sources, bounded rebuild/release tools, focused tests/evidence, exact manifest rows, Shipworks dock wiring, and `REMASTER_HANDOFF_dock_hulk_debris.md` | **FOCUSED GREEN / OFFLINE SOURCE+RELEASE CHECKPOINT INTEGRATED / BLENDER + ASSET-MANIFEST MUTEXES RELEASED.** All three deterministic Blender sources and exact KTX2/Meshopt releases are published. The modern and compatibility Shipworks routes use the reviewed neutral H-04 bay; cached mounts resynchronize, yaw-neutral alignment is stable, and the fallback plus settled-authored composition gate reports 0/18,000 dock intersections. The lane holds neither `browser-gpu`, `performance-evidence`, nor `validation-broker`. It is formally blocked at G5 live Browser/Electron presentation, G6 live LOD/residency/performance, and the independent G7 art verdict behind PQ-034's shared acceptance ownership. |
| Starter Kestrel material-truth remediation | Codex primary graphics correction; exact-base `0f1e6001`; V6 generation `50B71F1D` | `assets/ships/kestrel_borrowed_time_v4/`, the exact live/source/release `kestrel{,_lod1,_lod2}.glb` family, their exact manifest/authoring rows and build/finalize checks, `src/render/ships/kestrelHero.js`, Kestrel-focused tests/evidence, and the canonical `docs/visual-assets/` material/shape guidance | **OFFLINE SOURCE+RELEASE CANDIDATE CHECKPOINT INTEGRATED / BLENDER + ASSET-MANIFEST MUTEXES RELEASED.** The same promoted Kestrel is rebuilt in place as an isolated candidate: 66 failed hero objects retired, 553 manufactured component objects added, every visible component material-billed, `DIE LAUGHING` identity authored, full source descendants render-visible in the packed blend, exact nine sockets and baseline collision retained, and all three LODs hash-bound as KTX2/Meshopt releases. Focused tests are 3/3 plus 22/22 inherited Kestrel contracts. The live/source/release manifest family remains unchanged. Offline Blender evidence supports keep for the named remediation, but runtime G5/G6, Browser/Electron G6 presentation, formal independent G7 art review, and promotion remain open behind PQ-034; this row holds no shared mutex. |
| User-directed portrait and surface improvement | Codex primary task; texture-audit checkpoint `48f7c9ff`, Gatling correction `6fba3543`, Ashline V2 surface checkpoint `22a1bb82` | `assets/ships/m4_ashline_v2/`, Ashline V2 finalizer/promotion tooling, live/release Ashline Dart/Lode/Rig pairs, and only their exact manifest/runtime rows; old `assets/ships/m4_ashline/` is read-only comparison input | **OFFLINE CHECKPOINT INTEGRATED / ASSET-MANIFEST LANE RELEASED.** Gatling's hard texture-role failures are repaired. V2 Dart/Lode/Rig now have distinct deterministic service histories, 39/39 bound source images with zero texture-audit findings, reviewed offline contacts, and structurally valid Meshopt/KTX2 candidates. They remain deliberately unwired: the live Dart/Lode/Rig are still the superseded family. The `browser-gpu` mutex remains owned by PQ-034, so promotion and Browser/Electron/LOD-transition/dense-combat acceptance remain on hold until that lease is released. |
| Modular-hull texture-role correction | Codex primary task; exact-base `2ca2d94d` | The ten `assets/ships/parts/hulls/hull_*.glb` sources, their exact ten `assets/ships/release/parts/hulls/hull_*.glb` pairs, corresponding rows in `assets/ships/parts/parts_manifest.json` and `assets/ships/release/release_manifest.json`, `tools/art/repair_modular_hull_texture_roles.mjs`, shared derived-map helpers/tests, and the exact modular-hull source/export wiring used to prevent regression | **FOCUSED GREEN / OFFLINE SOURCE+RELEASE CHECKPOINT INTEGRATED / ASSET-MANIFEST AND BLENDER MUTEXES RELEASED.** The ten canonical GLBs moved from 34 errors/27 warnings to 0/0, and all ten source/release pairs are hash-bound KTX2/Meshopt assets; the focused contract matrix is 44/44 green. This is intentionally a canonical-GLB repair path: Blender promotion now fails closed until material and runtime metadata parity is proven. Geometry, sockets, scale, collision, names, and runtime selection remain frozen. Fresh V2 CPU contact sheets and Browser/Electron/LOD acceptance are still open; the lane holds neither `browser-gpu` nor `validation-broker`, which remain blocked behind PQ-034. |
| Spacepunk markings and emissive atlas | Codex primary task; exact-base `ede3b1cd` | `assets/concept/factions/spacepunk_markings_motif_study_v1.png`, its exact concept-index row, `assets/ships/foundry/spacepunk_markings_v1/`, `tools/art/build_spacepunk_markings_atlas.mjs`, and focused atlas tests | **FOCUSED GREEN / SOURCE CHECKPOINT INTEGRATED / WRITER RELEASED; NO MUTEX HELD.** The project-original generated study remains reference-only. The production package is conventionally redrawn: 32 deterministic story-grounded paint/emissive cells, exact serial/dock text, 13 intentional emissive cells, stable UV metadata, mip gutters, and 64px survival checks; focused atlas tests are 3/3 and the concept index is 235/235. It is not runtime-wired. GLB/KTX2 integration, residency, bloom/alpha behavior, and close/flight/dense-scene acceptance remain a separate packet after an exact ship family is selected and the relevant leases are free. |
| Primary checkout intro/menu/HUD | integrated product baseline at `6271ca29` and `dcbe4a20`; writer released | `index.html`, `src/main.js`, `src/ui/screens/mainMenu.js`, `src/ui/screens/newGame.js`, `src/ui/hud.js`, `src/ui/uiRoot.js`, `styles/intro.css`, `styles/menu.css`, `styles/ui.css`, and the two HUD evidence images | **INTEGRATED / NO ACTIVE WRITER.** The cinematic intro/menu and compact non-diegetic flight fascia coexist with the current Massline targeting/status behavior. Performance work may optimize their implementation but may not restore the prior UI, remove information, or lower the authored presentation. |
| `place_debris_chunk`, `place_dead_hulk`, `place_dock_interior` | source-only visual checkpoint `427d9897`; debris rebuild `8450287f`; offline source/release checkpoint | Exact source/release pairs plus the handoff, under the primary asset/remaster row above | **BLOCKED AT G5/G6/G7 AFTER OFFLINE KEEP.** Debris replaces the soft twin-pod read with one ruptured pressure module; hulk replaces iter219 with one continuous carrier/drill-tender rupture; dock replaces iter280 with an open-front H-04 service bay. Source GLBs, exact release pairs, texture roles, structural contracts, and offline matched reviews are green. The untouched military/grit dock variants are deliberately unrouted until they pass the neutral bay's composition gate. Current Browser/Electron, live LOD/performance, and independent art evidence remain unclaimed. |
| `place_asteroid_rock_a/b/c` | visual production lane (not yet started) | Those source GLBs, release artifacts, `parts_manifest.json` rows | Untouched since 2026-07-11. Still awaiting re-authoring. |
| Committed candidate refs | named local/remote branches and registered worktrees | Their branch tips and worktree metadata | These are not active writers. Preserve the refs as reviewable candidates; do not merge or rebase them merely for a checkpoint. **Both leading PQ-018 branches would revert all six place GLBs and delete the authoring textures retained at `329acfe8` — never merge them wholesale.** |
| Performance plan sources | admitted from `7f7d030b`; reconciled to the synchronized product baseline in this refresh | `design/PERFORMANCE_MODERNIZATION_EXECUTION_PLAN.md`, `design/PERFORMANCE_OPTIMIZATION_CONSTELLATION.md` | Source plan/research are preserved. The execution plan now explicitly protects the integrated intro/menu/HUD, current gameplay semantics, and source-only remaster checkpoint from performance rollback or premature promotion. Live execution authority remains the PQ-034–PQ-044 queue/packet set; source-plan presence does not grant a lease. |
| Performance modernization admission | originally integrated at `be840c03`; carried through synchronized tip `df31ee8c` | The reviewed control-plane admission through PQ-044 | **INTEGRATED AND RELEASED.** Admission itself holds no implementation path or mutex. PQ-034 claim/lease state is recorded separately below; its refreshed exact-base evidence now binds the synchronized tree. |
| `sf-perf-modernization-20260726` | unaccepted foundation candidate | PERF-00 pure harness files plus PERF-03/PERF-05/PERF-06 candidate paths | **PROTECTED READ/REVIEW INPUT — no packet authority.** Never merge or commit wholesale. Selectively port only after the owning packet is admitted and claimed. |
| `sf-perf01a` / `codex/perf-01a-background-lifecycle` | protected lifecycle candidate | `electron/main.cjs`, candidate preload, `package.json`, lifecycle checks, `src/core/loop.js`, `src/main.js`, lifecycle tests | **PROTECTED DIRTY CANDIDATE — do not duplicate, merge, rebase, clean, or remove.** PERF-01/PQ-035 must review and selectively port after PQ-034; known teardown/input-owner gaps remain unaccepted. |
| PQ-034 / PERF-00 | `claude/perf00-20260727`; exact entry source baseline `df31ee8c`, with evidence first recorded by control-plane commit `f85b6410`; strict pre-record fingerprint `e3c1a60b66acdd0cef8c93ddf1dbc85bec9343af32659b98071255bf837e8ae8` (`changedFileCount: 0`) | `performance-evidence`, `validation-broker`, `browser-gpu`; exact PQ-034 write set only | **CLAIMED / LEASES HELD / ENTRY REFRESHED.** Default `npm run check:baseline` at the entry source baseline is 9/9 green in 68.088 s (`parallel:3+1x4`, allowance 7, 21.912 s headroom). Intel Graphics driver `32.0.101.8132`, 1920×1080 at nominal 59 Hz, Balanced power plan. Entry census: 18 Chrome and 8 Node processes; no Blender or Electron. Subsequent commits before packet source mutation are control-plane-only evidence updates, so the source baseline remains `df31ee8c`; implementation may resume from the clean branch tip. Clean L4 captures remain prohibited until broker preflight proves the environment uncontended. PQ-035–PQ-040 have focused implementation commits on this branch but retain their broker acceptance gaps; PQ-041 is claimed below; PQ-042 remains unclaimed and PQ-043/PQ-044 remain deferred. Dependency-ready is not claim-ready. |
| PQ-040 / PERF-06 | `claude/perf00-20260727`; implementation commit `8db096b7` | Dirty-range implementation paths and PQ-040 receipt | **FOCUSED IMPLEMENTATION COMMITTED / LEASE RELEASED.** Combat sprites and trail-streak matrix/color/opacity publish scene-owned component ranges; native broker performance acceptance remains pending. Ribbon, authored-instance, ship-auxiliary, unrelated VFX, assets, shaders, package dependencies, and runtime acceptance manifests were untouched. |
| PQ-041 / PERF-07 | `claude/perf00-20260727`; implementation commit `03e85514` | Electron runtime/shell/package implementation paths and PQ-041 receipt | **FOCUSED IMPLEMENTATION COMMITTED / PACKAGE, ELECTRON-SHELL, AND BOUNDED LIFECYCLE LEASES RELEASED.** Exact Electron 43.2, explicit deferred-runtime provisioning, stale-package rejection, a hardened sandbox/navigation/permission boundary, production-only package allowlisting, and isolated New Game teardown are committed. The exact unpacked-package command remains blocked on this Windows account's symbolic-link privilege; native Browser/Electron broker, packaged-startup, hardware-GPU, controller/audio/display, and performance acceptance remain pending behind PQ-034. The protected `sf-perf01a` candidate remains read-only. |
| `sf-ctl-a` / `sf-ctl-b` / `sf-ctl-c` / `sf-ctl-d` worktrees | controller run 2026-07-26 — **leases released** | — | All four lanes are merged and released. The worktrees are intentionally **left in place**: each holds a `node_modules` **Windows junction** to the primary checkout, and `git worktree remove` would follow the junction and destroy the primary's dependencies. Remove the junction first if these are ever cleaned up. `sf-ctl-a` holds a preserved, deliberately unintegrated PQ-018 candidate. |

## Current program facts needed for dispatch

| Item | Current fact | Consequence |
|---|---|---|
| `npm run check` (recorded 2026-07-27 at `7f7d030b`) | **Was dead on arrival and had been for 333 commits.** `package.json` defined a `precheck` npm **lifecycle** script; npm ran it automatically before `check`, `check:sim:v3` inside it was red, and the chain exited 1 having executed **zero** of its ~97 links — while looking like an ordinary `check` failure. **Repaired:** the hook is deleted and its three gates (`check:m1:tether-mass`, `check:sim:v3`, `check:sim:v3:compare`) are now the first links of `check` itself, with `check:save-schema` ahead of them. 101 links, all visible. | Earlier claims that "the chain dies around link 79 on `check:sim:compare`" were wrong; nothing downstream had ever run. Do not reintroduce a `pre*`/`post*` lifecycle script — put the link where people can see it. |
| 47-A telemetry goldens (recorded 2026-07-27) | Both were stale, deterministically, since `850c80f3` (2026-07-14). **Re-recorded with evidence**, not reflexively: the `850c80f3` tree was exported with `git archive` and re-run, reproducing both prior hashes exactly, and full 720-tick snapshots were diffed field by field. 21060 fields moved; 21033 are the new `$.economy` market-regime model. **Zero entity pos/vel/rot/angVel fields changed on either flight controller** — the physics contract is bit-identical. The nine trace-count changes are one new `shield.collapse` cue caused by `f277c5e7`'s `DAMAGE_MODEL.subsystemShare 0.75 → 0.35`. Full rationale is in the `notes` of both expected files. | `check:sim` and `check:sim:v3` are green. Anyone re-recording these again must produce the same class of evidence; the files say so. |
| `check:sim:compare` / `check:sim:v3:compare` | **Structurally cannot fail on a stale golden.** `scripts/sf-sim.mjs:716` returns ok whenever the two runs agree with *each other*, explicitly tolerating `expectedHash` and `expectedTraceCount` diffs against the expected envelope. That is why both stayed green through 333 commits of golden drift. | They are determinism checks wearing a correctness check's name. Only `check:sim` / `check:sim:v3` gate the goldens. **Open handoff:** `sf-sim.mjs` is not owned by this lane; the tolerance should be a named `--allow-stale-expected` opt-in, not the default. |
| `check:massline` (updated 2026-07-27 at `cf1bc145`) | **GREEN, 23/23.** The three reds are fixed. Its runner previously executed 23 children fail-fast and so named only the first red — it had **three** at `7f7d030b`, and everyone was chasing one. `scripts/check-massline-aggregate.mjs` is rebuilt on `check-baseline.mjs`'s shape: it runs every child past a failure, reports the full set, and schedules longest-first (the 36 s long pole was declared last). Serial cost **62 s**, not the ~130 s previously recorded; 36.2 s of that is `check:47a:physical-branches` alone. | Treat "the aggregate named one failure" as a lower bound in any *older* receipt. Current runs report the whole set. |
| `check:baseline` (latest synchronized run 2026-07-27 at `df31ee8c`) | The fast gate. Nine links: the four sim gates, `check:save-schema`, `check:flight:v3`, `check:m1:tether-mass`, `check:massline`, `check-ui-screen-imports`. Runs every link even after one fails, reports per-link timings, and exits red on any failure *or* on exceeding its enforced 90 s budget. The exact PERF-00 owner base is **9/9 green in 68.088 s** with 21.912 s headroom under the default shared allowance (`parallel:3+1x4`, allowance 7). An earlier forced `SPACEFACE_CHECK_JOBS=2` run was also 9/9 green but correctly failed the wall budget at 137.810 s; do not use that forced result as entry evidence. Excludes `check:flight:clean` (~6 min) by design. | The before-and-after gate for every lane. `scripts/check-baseline.mjs`, routed in `AGENTS.md` §9. This is the recorded PQ-034 entry gate; rerun it at packet exit and require the green set to remain a superset. |
| Check-runner concurrency and timeouts (2026-07-27, `cf1bc145`) — **read before building any measurement harness** | Two defects, both found by measurement, both likely to recur in new runners. (1) **Timeouts on `shell: true` children were never enforced at all.** `child.kill()` kills `cmd.exe` only; the grandchild outlives it and holds the stdio pipes, so the runner waits on `close` forever — a 180 s budget once reported **361 820 ms** and exit `null`. Fixed with `taskkill /pid /t /f` plus a grace window. (2) **Nested pools multiplied.** `check:massline` is a link of `check:baseline`, and both became parallel pools, so widths multiplied to 16 concurrent heavy sims. Runners now share one allowance via `SPACEFACE_CHECK_JOBS` and **split** it rather than re-spending it; `defaultAllowance()` keys off `round(availableParallelism()/2)` (physical cores under SMT), which is where the measured knee sits. | Any new process-spawning runner must reuse `taskkill /t` teardown and honour `SPACEFACE_CHECK_JOBS`, or it re-creates both bugs. Contention data for PERF-00: past ~2 concurrent heavy sims per lane, per-process time inflates faster than the pool drains — `check:sim` measured **8 227 ms → 14 942 ms** at x4, and the massline aggregate is *slower* at x4 (63 774 ms) than at x2 (47 867 ms). A perf sample taken while anything else runs is not a measurement. |
| Default player route | **Playable.** `npm run check:visual-stability` exits 0 at `aca82bb0` — 360 frames, 16 ships, `failureCount: 0`, no page errors. A previous entry here claiming `place_debris_chunk` "stalls playable flight" was measured and is **false**. | Do not treat the route as blocked. Route-acceptance work may proceed. |
| `check:assets:live` | The prior `75 !== 77` failure named the superseded debris/hulk release artifacts. Their exact source/release pairs now pass `check:opening-place-remaster`, including hash/byte parity, material parity, complete role-correct KTX2 payloads, and Meshopt geometry. | The browser-based live probe was deliberately not rerun while PQ-034 holds the shared acceptance lanes, so current headed status is **unclaimed**, not red or green. Re-run it through the validation broker after the lower gates when that lease is released. |
| `check:art` | Red at **link 1 of 23** — `test/asset-pipeline-gate-wiring.test.mjs:27`, `check:ci reaches the asset-pipeline contract through check:art exactly once (0 !== 1)`. The chain never reaches any GLB probe. **Correction, 2026-07-27: this is NOT a `package.json` wiring change and no such change can fix it.** The two assertions are mutually unsatisfiable. `asset-pipeline-gate-wiring.test.mjs:26-28` substring-counts `npm run check:art` inside `scripts['check:ci']`, while `scripts/check-m1-tether-mass-grounding.mjs:32` asserts `scripts['check:ci']` is *exactly* `npm run check:ci:report`. A delegating script can never literally contain its delegate's links. | Do not attribute `check:art` redness to assets, and do not attempt a `package.json` fix — it will just flip which gate is red. The defect is in the test: it should walk the aggregate expansion the way `check-gate-reachability.mjs` and `check-ci-report.mjs` already do, instead of substring-matching a one-line delegation. **Handoff: `test/asset-pipeline-gate-wiring.test.mjs`, unowned.** |
| `check:phase0-slice-contract` (recorded 2026-07-27) | Red, unrelated to the check chain: `Unclassified Math.random site: src/audio/bandBeds.js:33`. That line is `const random = typeof options.random === 'function' ? options.random : Math.random;` — an injectable default, i.e. exactly the pattern the classifier exists to bless. The file is unchanged since `850c80f3` and the tree is clean, so this predates the current chunk. | Escalated, not inherited. Owner is the audio/determinism seam, not the truth lane. Either classify the site or teach the classifier about injected-default RNG. |
| PQ-007 | Focused-green correction integrated; current Browser/Electron route acceptance remains open | Do not restore the rejected pursuit-slot mechanics from historical prose. Treat route evidence as a separate acceptance task. |
| PQ-017 | World Site kernel integrated; writer lease released | PQ-018 may consume the integrated manifest/kernel/runtime contract after re-reading current symbols. |
| PQ-018 | **Phases 0-3 integrated** at `aef540d3`; queue state `focused_green`. The Cathedral is a placed Ceres World Site with seven components, a physical black-box payload, and five evidence pages. | **Phase 4 only** remains: Browser/Electron route acceptance, game-camera visual review, matched Ceres performance, and the independent art verdict on the frozen source. The Ceres coordinate reservation (local `(300,2700)`, global `(-11988,10892)`, 76.632 WU clearance) is **owned by PQ-018; PQ-020 consumes it and may not relocate it.** Dependents no longer wait on parent integration: PQ-020 and PQ-021 bind the integrated `PQ-018.world-site` leaf receipt as an evidence dependency (blob-bound in the queue), and Phase 4 route/perf verification is relocated into PQ-020's matched Ceres route. |
| PQ-019 | **Leaf PQ-019A implemented on master** at `272e63b4` + `b66f2255`, receipt prose recorded at `d7a3dcbc`; queue state `planned` with `hasPartialLanding: true`. Tethys launcher/catcher/fence embodied; idempotent schedule receipts; real capsule contact; custody filtering; `check:pq019a:facility-embodiment` 12/12. | Open for PQ-019A acceptance: player-visible schedule cue, normal-route Browser/Electron presentation, game-camera visual acceptance, admission/residency/draw/GPU + representative performance, accepted receipt file, W03/W04/W05 parent-alias cleanup. PQ-019B/C unstarted and gated on A's accepted receipt. |
| PQ-020 | **Topology slice + continuation proofs integrated** at `9b0c1c28` + `ece08405..f98367c1` (receipt `PQ-020-ceres-topology-REPORT.md`, candidate `6c08de8b`); queue state `planned` with the PQ-018 dependency bound to the `PQ-018.world-site` leaf receipt. Natural jobs 3/3 held-out seeds; `dense_asteroid` proven as the production-pocket condition; save/re-entry idempotence; exact dual-frame agreement; routing honesty recorded. | Remaining is the headed set only: Cathedral-keyed matched route receipt (relocated PQ-018 Phase 4 — p95/p99/hitches, admission/residency, close/default/far/motion/LOD, draw/program), Browser/Electron, accessibility route review, perceptual pocket distinctness, human art verdict. All blocked on the PQ-034 lease; do not rebuild the deterministic layer. |
| PQ-021 | **Phases 0-4 (headless) integrated** at `b266515a` + merge `7b29a6fb` (receipts `PQ-021-ledger-wiring-REPORT.md`, `PQ-021-phase4-REPORT.md`). Natural earning proven at the ordinary operation API with fabrication failing closed; cold Continue byte-identical; two-host parity with flat resource counters; both live routes proven headless; the 1920px-figure-in-700px-column defect fixed panel-scoped and mutation-guarded; pq021-ledger-route broker cell built + registered, deliberately unrun. | Remaining is headed-only: Browser broker run, Electron parity, independent legibility review, physical controller pass — all behind the PQ-034 lease. Do not rebuild the headless layer. |
| PQ-022.exterior-relay-collar | **Structurally delivered at focused_green** (merge `7b29a6fb`, receipt `PQ-022-exterior-relay-collar-REPORT.md`): `place_claim_outpost_relay` accepted as-is, zero asset mutations, 9/9 admission/envelope/LOD/cleanup proofs, live exterior-route admission PASS. Reservation: relay reads as generic grey primitives at game camera (authored look) — re-authoring needs report is advisory in the receipt. | PQ-024 stays blocked until this leaf upgrades to `route_accepted` (headed Browser/Electron + independent visual verdict + matched performance under the PQ-034 lease) and the receipt blob binds into `PQ-024.evidenceDependencies`. |
| PQ-023 | **One leaf integrated** at `cdcbac32` (propulsion family); queue state `planned`. Still an **umbrella** — six cue families remain. | The branch's relaxation of the idle-sleep perf invariant was refused; `check-vfx-frame-sleep.mjs` is byte-identical to master. Consequence: no always-on idle nozzle glow. No visual or performance acceptance exists for this leaf. |
| Uncovered invariant, now closed | `operation-unreachable` (`worldSiteKernel.js:243`) throws at boot via `_ensureWorldSiteRecords`, and nothing asserted it. | `test/world-site-operation-reachability.test.mjs` (5 cases) is wired into the pq017 world-site fast gate. Do not relax `reachableOperations` without it. |
| PQ-022–PQ-025 | Corridor remainder | Use their active packets and entry gates. Do not dispatch an umbrella row when a prerequisite leaf packet is required. PQ-024 waits on the `PQ-022.exterior-relay-collar` leaf receipt; PQ-025 additionally waits on the `PQ-022.gold-corridor-required-assets` and `PQ-023.gold-corridor-required-cues` milestone receipts. |

## Preserved candidates, not active authority

- Wreck Cathedral source asset and evidence are committed and reusable; current runtime contracts decide promotion.
- Narrative, Lark, and VP-220 candidates remain on named recovery refs. They are not merged, accepted, or leased merely because they exist.
- Historical planning handoffs under `docs/handoffs/chatgpt-portfolio-20260723/` are reference material. Active packets supersede their dispatch prose while retaining grounded technical findings.

## Refresh procedure

Before claiming a packet:

1. re-run `git status --short` and `git worktree list`;
2. inspect open branches/PRs touching the packet's write surfaces;
3. compare this file's `baseCommit` to HEAD and refresh when the expiry is exceeded or any listed owner changes;
4. record the packet lease and exact paths before implementation;
5. remove or update the lease immediately when work is integrated, abandoned, or moved.

Do not append closeout narratives here. Put exact-revision evidence in a receipt and rely on Git history for the rest.
