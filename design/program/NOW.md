<!-- LIFETIME: VOLATILE -->
# NOW — threads changing the shared checkout

```yaml
refreshed: 2026-09-19
baseCommit: a4429eb5a63c0c71907175c8aea0279c4e04d176
expiresAfterCommits: 10
expiresAfterDays: 2
```

This is a short collaboration board, not a roadmap, backlog, completion ledger, or reason to stop
working. Product status and remaining tasks live in
[`02_REMAINING_WORK.md`](./02_REMAINING_WORK.md) and
[`roadmap/program-queue.json`](./roadmap/program-queue.json).

## Rules

1. Add one row immediately before the first mutation. Reading, research, review, and tests reserve no
   file and need no row.
2. Name the exact task, thread label, current state, and files being changed now. Do not claim a
   subsystem, lane, tool, GPU, or future phase.
3. A row protects the exact dirty hunk from being overwritten. It does not block the task, packet, or
   other files. Work on disjoint hunks or another returned task while arranging an explicit handoff.
3a. **A row is a claim, not evidence — check liveness before yielding to it.** Run
   `node scripts/check-now-liveness.mjs`. A row whose claimed files are untouched for 90 minutes is
   **stale by definition**: the writer is dead or done. Adopt the work (evaluate the dirty diff,
   finish or land it, receipt it) and delete the row — do not route around it, do not wait, do not
   ask. Dirty files alone are never proof of a live writer in this chronically dirty tree, and
   "row exists + files dirty" is the claim verifying itself. Collisions here are cheap and
   recoverable; work stalled behind a ghost is invisible and permanent — yielding to a stale row
   is the failure mode, not the safe choice.
4. Reread a shared file before every patch. Release the row as soon as mutation stops.
5. Use `PUBLISHING` only for the brief stage/commit/push window. Stage only the task's exact files,
   verify the staged names, publish with `git commit -- <paths>` (a pathspec publish cannot race a
   snapshot), then remove the row. A staged deletion of a file that exists in `HEAD` and on disk
   means the shared index is stale, not that the file is gone: `git reset -- <paths>`, then publish.
6. End every task with `RESULT: DONE` or `RESULT: NOT DONE` using the template in
   [`02_REMAINING_WORK.md`](./02_REMAINING_WORK.md). Delete stale rows; Git and receipts own history.
7. Do not create a worktree by default. Existing worktrees are recovery obligations recorded in
   [`04_WORKTREE_AND_INTEGRATION.md`](./04_WORKTREE_AND_INTEGRATION.md), not current ownership.

## Active mutation windows

| Task | Thread | State | Exact paths being changed now | Next terminal action |
|---|---|---|---|---|
| Four small tuneups (all verified live 2026-09-20; the footprint 18-frame job is inference-2x unit 2, yielded): lawful-kill heat skip, post-load encounter replan, feel B2/B3 camera envelopes, release-builder unnamed node | tuneups-zcode | RESULT: DONE | released; job 1 `39cfb9168` lawSecurity prices its own dead even mid-enforcement (clearlyHostile early-out no longer swallows factionLawful victims — a WANTED player killing an engaging patrol now mints a lawful_kill receipt instead of free kills; check-gameplay-core green with its kill leg re-pointed to the real law seam, law-kill-witness 13/13 + pq019 listener 25/25); job 3 `31da7ccf2` `_onSaveLoaded` delivers the authored entry breath + planner (PQ-209.00's named ≤600 s post-load dead-air residual; new test/encounter-save-loaded-replan.test.mjs 3/3, save-resume-confidence + director family green); job 4 `3f6ccaa72` attributed-not-fixed: B2 radius + B3 crossing are an authored-envelope conflict (B3's five clauses proven jointly infeasible — crossing needs a 1.76× camera open or 0.57× starter slowdown; ratio₃ = e/(1.35k) dies at any k ≥ 1.04; ships.js handling measured inert on flightV3; drive profile shared, stock thruster authored neutral PQ-176.01, assist governor is the owner §12 ruling) — proof + three re-author options in `receipts/FEEL-B2-B3-FEASIBILITY-2026-09-20.md`; job 5 `5060ee716` the release builder names what it creates (meshopt weld/join minted unnamed child nodes; fin_crystalline carried a source-side duplicate name) — place_cold_locker/ash_pin/tally_post + fin_crystalline now package, render-package coverage 257/257, SG-04 + authored-place-runtime green, the three places left check-sector-prewarm's failing archetype list | DONE: 4 commits, one yield released by inference-2x; golden attribution — sim/sim-v3 were red before this lane (foreign dirty-lane drift, hash `fe0070c1…` with my files reverted) and the deliberate post-load replan adds its own deterministic delta, flagged for the clean-tree re-record batch per §10d |
| INFERENCE 2x (unit 1: claim beacon world presence — spawn/despawn the proven route-buoy entity on deploy/expiry; unit 2: footprint board churn — signature-guard the 18-frame full re-render) | inference-2x | RESULT: DONE | released; unit 1 `2dc949cff` + cold-review fixes `0787e8c50` (src/systems/beacons.js + test/seam-beacons.test.mjs 5/5 — deploy spawns the renderer's route-nav buoy as a transient story prop, expiry despawns it; review fix: buoy drops AFT of the hull, not centered in it — fixed-collider depenetration shove — plus masslineTetherable latch field; record stays the deterministic sim brain, 47a untouched), unit 2 `21b2973d8` + cold-review fixes `688bf4a5a` (src/ui/screens/footprint.js + test/footprint-board-churn.test.mjs 8/8 — header rewritten every cadence pass, hang/board/record/edges rebuild only when the receipt signature moves; review fixes: credits+bribe gate sampled, ace returnsBigger sampled, onShow re-queues edge draw after resize-while-closed; ui grammar/hierarchy/lifecycle 104/104). check:baseline 12/15 with the 3 reds (sim/sim-v3/save-schema) the documented foreign dirty-lane drift. Both units inference-recorded | DONE: two production units committed + reviewed + fixed; row released |
| Capital bosses packet 09 integration (three authored capital scores: overlay install, tacticalAI hand-merge composing Enemy Mind + Nemesis gates, missions spawner rewrite, registry/save/presentation wiring) | bosses-integrator | MUTATING | `src/systems/tacticalAI.js`, `src/systems/missions.js`, `src/core/registry.js`, `src/save/saveSystem.js`, `src/data/encounters/capital-boss.js`, `src/data/encounters/capital-boss/*`, `src/ai/capitalBossOrders.js`, `src/combat/capitalBossGeometry.js`, `src/combat/capitalBossScore.js`, `src/missions/capitalBossSpawn.js`, `src/presentation/capitalBossPresentation.js`, `src/systems/capitalBossEncounters.js`, new save round-trip test, pin/manifest files it forces, `design/program/NOW.md` row | package tests 83/83 + overlay applied + tacticalAI merged; remaining: missions spawner rewrite, registry register, save/rebind path, overlay mount, boss/mission/combat suites + check:baseline, pathspec commits, release row |
| Endings packet integration (SpaceFace-Endings overlay: written history-dependent finales, DECISION-MODEL eligibility) | endings-integrator | RESULT: DONE | released; installer applied 9 files against exact packet baselines (commit `217350bf7`: 5 endings modules replaced, 4 new — value/lifeLedger/finaleContent/finaleRuntime — plus story.js adapter; dossArchive.js deliberately not copied; endingDefs.js CRLF checkout smudge normalized to the baseline blob first). Pins re-pointed to the new authored semantics, each new contract asserted (commit `59860c0c2`: story-endings 22/22, pq-032-01 6/6, m5 6/6, campaign47a-live, embodied-save; ending-choice A-E/epilogue/replay-chains untouched green). Delivery suite reproduced: 100/100 tests, 18/18 verify checks, fixture byte-identity. Save: new state.story fields ride the missions-owner story bag (saveSystem._serializeMissions), embodied-save round-trip green, no src/save edit. UI consumers verified: all prior exports retained, no shim needed; report-only residual: missionLog.js "Build sector power" card prose still describes the retired universal wealth/branch-rep/stake gate (the facts it displays are still emitted). Foreign reds during gates (not this lane): sim/sim-v3 golden drift (documented), save-schema capitalBoss + bounded-autosave epoch + pq146 capture reds from the live bosses/onboarding dirty files, pq-032-00 spine red inside the dirty missions.js spawn path | DONE: overlay applied, pins re-pointed, gates run with foreign reds attributed |
| PQ-209.00 encounter supply hours 5-9 (parked-player combat-beat starvation) | devin-desktop | RESULT: DONE | released; `src/systems/encounterDirector.js` + `test/encounter-proximity-relocate.test.mjs` committed `447235dbc` (ceres throughline ambush exempt from starvation relocation — waits on plain defer, never fizzle-drops the one-shot; adopted-actor harassers return before mercyStrikes/emits so no false repeating disengage toast). The measured fix itself landed earlier in `d18284845`/`993927b1d`/`446c6e5b7` (relocate-to-player + sector starvation clock + engaged-only combat_busy/stalemate release); this unit ran the battery and hardened review findings. Evidence: baseline hunter s8008 h5-9 spawned 0,0,0,0,0 (100% telegraph fizzle) → canonical rerun `.devshots/actual-game/hunter-seed8008-10h.json` spawned 10,10,11,8,6, telegraph:spawned 1:1, stale 0; post-fix verify set `.devshots/actual-game-verify/` hunter-4242 5,7,7,5,6 and hunter-8008 7,4,3,6,7 — leaf bar met on both seeds twice over. Queue row flipped done; receipt `design/program/roadmap/receipts/PQ-209.00-REPORT.md`. Residuals named for follow-up leaves: tension-recovery veto can hold combat supply while hull<50%/fatigue pinned; `_onSaveLoaded` never replans (≤600 s dead air). check:baseline 13/15 — sim/sim-v3 reds are the documented foreign dirty-src golden drift | DONE: spawned non-zero h5-9 both hunter seeds; leaf queue-closed |
| PQ-206.00 Dreadnought 2v2 ~12s resolution investigation (what kills the capital: found — nothing, the bench's ttk conflates the pilot's death with a capital kill; capitals end the cell at full hull; verdict authored-vs-bug + fixed-seed pin) | pq206-glm | RESULT: DONE | released; `test/pq206-00-dreadnought-wing-cell.test.mjs` (new, 10/10, `node --test --test-force-exit …` — rapier boot holds WASM handles after dispose), `design/program/roadmap/receipts/PQ-206.00-REPORT.md` (new). Commit `c68a4dd3a`. Finding: cell resolves by PILOT DEATH (bench ttkSeconds counts any death; player:death pushes into duel.deaths); capitals end every seed at full hull 11040/armor 4048, pilot dealt 369-611 vs 30,928 EHP, corsair wingmate killed at 5s having dealt zero, 100% of pilot damage is capital turret fire (weapon origin). Verdict: authored gank (tier-0 starter vs 2× level-8 capitals, ~23-28 dps landed of ~2,080 paper), no stat bug; `src/combat/runtime.js`/`src/data/missions.js` inspected, net-unchanged. Instrument defect named for the bench lane (ttkSeconds conflation). combat-ecology-roles 3/5 — the 2 reds are the documented pre-existing ghost-range pins, byte-identical files at HEAD, owned by leaf .03. check:baseline 12/15, reds foreign (sim/sim-v3 golden drift from dirty src lanes, massline contention timeouts; zero src files touched here). NOTE for boot lanes: node factory table lags manifest (stationServices, difficultyDirector) — test injects them pq-141-style | DONE: pilot-death mechanism pinned on seeds 4242+8008; verdict authored; receipt + test committed |
| PQ-208.00 progression verb drift guard (mechanical consumer verification for declared verb keys, fail-closed; leaf PQ-208.01 key wiring NOT this unit) | pq208-glm | RESULT: DONE | released; `scripts/check-progression-verb-audit.mjs` (consumer-evidence guard: {file,symbol} verified live each run, unwired keys only behind {declaredOnly: leaf}, fail-closed; 4 landing-lane keys now mechanically pinned to their landed consumers), `test/pq208-progression-verb-drift-guard.test.mjs` (new, 7/7 incl. planted-drift negatives), `design/program/PROGRESSION_VERTICAL_AUDIT.md` regenerated (additive). Commit `a28a13b4f` (+receipt commit). Receipt `design/program/roadmap/receipts/PQ-208.00-REPORT.md`. `src/data/modules.js` net-unchanged (drift planted→FAIL→reverted byte-identical; corrections are leaf .01's). check:baseline 12/15, all 3 reds proven foreign (massline-heads identical red under HEAD audit script; 47a green solo; sim goldens drift with src/ dirty from live lanes) | DONE: audit exit 0 (16 verb keys verified / 2 declared-only tracked by PQ-208.01); guard exits 1 on planted unwired key |
| PQ-033.02 min-spec floors and soak (adopted from devin-desktop, stale; convoy cap-release fix verified on master `5617c1cd5`) | glm-5.3-pq033-02 | RESULT: DONE (save-growth unit; FLOORS REMAIN RED — a fresh 2h soak pair on the min-spec GPU is the next measure) | released; `53996cd03` far-actor row budget (FAR_ROW_BUDGET=128 + enforceFarRowBudget in tickFarActors, durable jobId/worldRecordId rows protected) + `test/pq-033-02-far-row-budget.test.mjs` 3/3 + receipt addendum with full attribution tables. Attribution: the second save-growth source is a composite of BOUNDED ramps (chronicler stories cap 96/256, tensionDirector ring ~65 KB ceiling, economy cycle-blend tails ~26 KB steady-state, one-time market materialization +68 KB) plus ONE unbounded component — far-actor rows never expire in a fixed-cluster session. m2 world 31/31, convoy-cap 2/2, soak contract 13/13, check:baseline green (2×47a timeout re-ran solo green). Session diagnostic `scratch-save-attribution.mjs` left intent-to-add (uncommitted, reproducible per receipt). Next measure for the floors: boot cook 15.5/24.1 s vs 10, steady p95/p99 116.8/133.5 ms vs 33.3, heap residency — then a fresh soak pair.
| Onboarding vertical: thesis-first first hour | zai-coding-plan/glm-5.3 | RESULT: NOT DONE (route shipped + committed 614bec27d; stranger full-hour metrics not met — far-field body demotion defeats scripted thrust after swing recoil, see receipt) | released; `src/systems/onboarding.js` (thesis-first BEATS: tether→raid→claimed→drills→seam/dock/choice, raid+claimed beats, milestones, rescue/raid restage recovery, DOM guards), `scripts/check-first-hour.mjs`, `scripts/check-professional-first-hour-one-voice.mjs`, `scripts/lib/bench/playthroughStrangerPilot.mjs` (new — stranger archetype module), 4 beat-FSM test drivers. Attach <60s PROVEN (0.53s, seed 4242). Instrument integrations for the instrument lane (NOT committed, contested): pilot stranger re-export, ledger TARGETED_TYPES += 'firsthour:milestone', harness stranger entry + onboarding system + production flag seed — see receipt `design/program/roadmap/receipts/onboarding/2026-09-19-thesis-first-first-hour.md` | receipt written; NEXT: fix far-field body sleep for scripted input, then fixed-seed metrics twice |
| Arranger v1 integration + economy resource-work publisher in world owner | arranger-integrator | RESULT: DONE | released; `5617c1cd5` Arranger v1 (installer preflight refused the divergent working tree, so the delivery's documented `patches/world-arranger.patch` merge path was used; patch(preimage)==delivered file verified by hash; world.js carried hunks from the field-regrowth / membership-hysteresis / PQ-033.02 lanes landed deliberately with their paired files — fieldDepletion.js, membership test update, PQ-033.02's intent-to-add test). `ad60fed4a` economy resource-work publisher (persisted sector-wide seq, admission/clearance/cap checks, no-subscriber-tolerant free-regrow fallback, settle/cancel/uncertain rules) + `test/economy-resource-work-publisher.test.mjs` 7/7 against the real economy subscriber. arrangementVersion save round-trip proven through the real saveSystem envelope path (v1 new game / missing→v0 / explicit-0→v0 / unknown rejected) — no src/save/ edit needed. World suites: same 7 pre-existing failures before and after; baseline red set unchanged. | full playthrough battery deferred to the integrator after all packets land; new-campaign-only rollout (no v0→v1 migration) |

| Production VFX campaign (eight effect classes; integration lead + nine family lanes) | vfx-integration-lead | MUTATING | LEAD SEAMS: `src/render/vfx.js`, `src/render/vfxProfiles.js`, `src/render/weapons/presenter.js`, `src/render/combat/transientVfxMaterials.js`, `src/render/energy/energyMaterials.js`, `src/render/forceLanguage/sweptSurfaceBatch.js`, `src/render/dynamicBufferRanges.js`, `src/render/vfxAccessibility.js`, `src/render/bloom.js`, `src/render/renderer.js`. LANE SETS (disjoint, one owner each): `src/render/thruster/**` except contrailTrail/pathSampler (jets) · `thruster/ribbon/contrailTrail.js`+`thruster/systems/pathSampler.js`+`engineTrailSurfaces.js`+`velocityLanguage.js` (history) · `src/render/weapons/**` except presenter/shieldContacts + `combat/persistentBeams.js` + `forceLanguage/weaponDischargePool.js` (weapons) · `combat/{phasedExplosions,arcadeStructuralFx,causalStructuralBurst,structuredBurstGeometry,instancedSpritePool}.js`+`weapons/contactMarks.js`+`particleShards.js` (impacts) · `forceLanguage/{fieldForcePresentation,effectLifecycle,catalog}.js`+`weapons/shieldContacts.js`+`momentumSinkVfx.js` (fields) · `combat/densityVolumeData.js` (gas) · `src/render/vfx/quarksSystem.js` (debris) · `masslinePresentation.js`+`masslineReleaseArc.js`+`combat/tetherWebFx.js` (massline) · `spaceBackground.js`+`deepField*.js`+`parallaxLayers.js`+`paintedPlanets.js` (background) | integrate each lane by pathspec commit, then check:vfx-force-language + check:shader-compile + check:vfx-sleep + check:thruster:propulsion-family + one check:baseline + AFTER runtime witness |
| Finish-game fleet | grok-controller 01a08d61 | MUTATING | `design/program/NOW.md`, `design/program/roadmap/program-queue.json` | 164 walker not run (GPU); 164.00b node pin; 158/160/166/194 live; skip 141.02 |
| INFERENCE units 56-60 (map destination = flight waypoint; map context memory; one objective wording; rebind-aware labels; crucible refit controller focus) + adoption of the unclaimed finished INF-055 zoom-anchor hunk | zcode-inf-56-60 | MUTATING | `src/ui/screens/localmap.js` (adopting the unclaimed INF-055 hunk, then disjoint 056/057/058 hunks), `src/ui/navigationWaypoint.js`, `src/ui/screens/starmap.js`, `src/ui/screens/missionLog.js`, `src/ui/hud.js`, `src/ui/controlPrompts.js`, `src/ui/bindings.js`, `src/ui/screens/crucible.js`, `src/ui/screens/crucibleDraft.js`, `src/ui/input.js`, `src/ui/screenManager.js`, `test/localmap-zoom-anchor.test.mjs` + new focused tests, `design/program/NOW.md` | NOT touching `src/systems/missions.js` (bosses-integrator live) — 058 is consumer-side only |
| PQ-210 fleet wave-1 (units .06 quiet-machine, .07 motion ask-once, .01 frame budget propulsion half) | pq210-fleet-zcode | RESULT: DONE | released; `.06` all 12 harness/docs files (`64474cf99`, opencode deepseek-v4.1-flash + agy accept), `.07` reconciled over the INF-007 race — the ask is now a real screen on the default route and the gate actually fires on real defaults (`bbdd28157`, cursor-agent grok-4.6-xhigh + agy review + controller reconciliation, all three suites green), `.01` propulsion half (`ce4d24365`, devin swe-2-max; probe: base callback 38.5ms/sim 16.7 → 22.3/8.9). Receipts `design/program/roadmap/receipts/PQ-210.0{1,6,7}-REPORT.md`. RESIDUAL on branch `pq210-frame`: `.01`'s hud.js half (`fbb182073`+`7f21014a4`, transition-only DOM writes + the removeAttribute cache-poisoning correctness fix, 5/5 tests, held behind the zcode-inf-56-60 hud lane — cherry-pick when it releases). Queue close-out left to the finish-game fleet lane | DONE: three units landed; hud half parked on branch with receipt |
The legacy extraction, live Shipworks guidance, Market quantity controls and public career route are
committed after controller review. UI performance and place acceptance continue in this campaign.
PQ-184.01, PQ-184.03, PQ-187.00, PQ-177.07, PQ-144.01, PQ-190.00 and PQ-184.02 are closed in the
canonical queue. The duplicate incomplete checkout was removed after its DROP disposition was
committed and pushed.

The three native art rows (ordinary working tug, held furniture construction repair, yard tug body
re-author) are deleted: their writers stopped on provider limits, the dirty work was adopted and
finished, and it is committed and pushed. Ordinary life is done on the route — the tug moves a real
load through the combat attachment service under Rapier, and a five-minute HUD-hidden capture at the
Ceres reference pocket shows all six quiet behaviours (`scripts/capture-ordinary-life.mjs`).

| Failing-test repair (entity-mesh-visibility + pq-030-02 seed 30000) | muse-fix-tests | RESULT: DONE | released; test-only pin refreshes, `src/` untouched, foreign hunks preserved | both files 17/17, pq-030 family 12/12, check:baseline 15/15 |
| PQ-207.00 berth mechanic ledger cost (people-who-remember residuals `.00`) | devin-desktop | RESULT: DONE | released; `src/story/mechanicVoice.js` (rap line asks presence instead of building a page), `src/systems/shipLedger.js` (additive read-only presence probe `shipLedgerHasFactOutside` + per-family early exit — write-set crossing recorded in the receipt), `test/pq-207-00-berth-mechanic-cost.test.mjs` (new, 9/9). Commits `492bb4a9d` + `9f5af58aa`. Receipt `design/program/roadmap/receipts/PQ-207.00-REPORT.md` | DONE: 3745 us -> 19 us per berth card on a scarred hull with a 240-row trade ledger (5335 -> 20 us per arrival); `buildShipLedger` pages byte-identical vs `492bb4a9d^`; check:baseline 11/15 with all 4 reds proven foreign (identical with this change reverted). Queue close-out left to the fleet lane, which owns `program-queue.json`. Follow-up found, not fixed: `src/ui/screens/footprint.js:961` rebuilds a ledger page on the same 18-frame cadence while the board is open |

## Start another task

Use the copy-ready prompts in [`AGENT_TASK_PROMPTS.md`](./AGENT_TASK_PROMPTS.md), or run:

```text
node scripts/program-dispatch.mjs --next
node scripts/program-dispatch.mjs --ready
```

Choose the highest-priority result you want, add its short mutation row only when editing begins, and
finish it. If one exact hunk is protected, continue the task's disjoint work or choose the next queue
row; never report the whole program blocked.
