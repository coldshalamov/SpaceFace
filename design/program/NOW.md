<!-- LIFETIME: VOLATILE -->
# NOW — active work and path leases

```yaml
refreshed: 2026-08-05
baseCommit: e0b807bfa4354d5a87b63c9f5b364c6f65ec3ef2
expiresAfterCommits: 25
expiresAfterDays: 7
```

Refresh this board before mutation. It records live collision risk, not history or completion evidence. Exact packet status remains in the queue and receipts.

## Occupied or protected surfaces

| Domain | Owner/ref | Protected paths or mutex | Disposition |
|---|---|---|---|
| Primary checkout asset/remaster production | Codex primary graphics push; debris source checkpoint `8450287f`; offline source/release checkpoint | The exact source/release pairs for `place_debris_chunk`, `place_dead_hulk`, and `place_dock_interior`; their authored Blender sources, bounded rebuild/release tools, focused tests/evidence, exact manifest rows, Shipworks dock wiring, and `REMASTER_HANDOFF_dock_hulk_debris.md` | **FOCUSED GREEN / OFFLINE SOURCE+RELEASE CHECKPOINT INTEGRATED / BLENDER + ASSET-MANIFEST MUTEXES RELEASED.** All three deterministic Blender sources and exact KTX2/Meshopt releases are published. The modern and compatibility Shipworks routes use the reviewed neutral H-04 bay; cached mounts resynchronize, yaw-neutral alignment is stable, and the fallback plus settled-authored composition gate reports 0/18,000 dock intersections. The lane holds neither `browser-gpu`, `performance-evidence`, nor `validation-broker`. G5 live Browser/Electron presentation, G6 live LOD/residency/performance, and the independent G7 art verdict remain open; claim them as explicit dispatch units instead of inheriting a permanent PERF lease. |
| Starter Kestrel material-truth remediation | Codex primary graphics correction; V6 checkpoint `a23d09b9`; stencil refinement `339878fc` | `assets/ships/kestrel_borrowed_time_v4/`, its deterministic build/finalize scripts, exact candidate source/release family, Kestrel-focused tests/evidence, and only the Kestrel material-truth guidance/provenance records | **OFFLINE STENCIL CHECKPOINT INTEGRATED / BLENDER + ASSET-MANIFEST RELEASED.** The same existing Kestrel candidate now carries an original, one-sided `DIE LAUGHING` punk/protest stencil conforming 0.3 mm above its shoulder armor, with seven paint-loss gaps and a 34-quad LOD0 overspray/wear layer. The 182/114/114 marking-triangle contract, exact sockets, collision, identity, source/release hashes, KTX2 roles/mips and Meshopt parity are green; live/source/release manifest families remain unchanged. Runtime G5/G6, Browser/Electron, performance, promotion and the formal independent G7 art verdict remain open but are no longer blocked by a standing PQ-034 lease. This row holds no shared mutex. |
| Visual-asset census and material-truth workflow | Codex primary graphics program; integrated at `ed65e645` | Machine-readable and human-readable visual-asset catalog outputs, dated branch/worktree archaeology, `.grok/skills/spaceface-blender-material-truth/`, discovery wiring in the existing SpaceFace Blender skill, and focused catalog/skill validation | **FOCUSED GREEN / CATALOG + SKILL INTEGRATED / WRITER RELEASED.** The 82-row exact release census, branch/worktree archaeology, repeatable fiction-to-material workflow and top-five execution plan are committed. The top five are Kestrel marking, Ashline V2 Dart/Lode/Rig and the claim-outpost relay; each remaining asset has a named G1/G2 defect and fiction/dev material bill, but requires its own fresh exact-path lease before mutation. The stopped Lark branch is selective-donor-only, the old Grok worktree has no unique Kestrel deliverable, and alternate-ship donor candidates are preserved without wholesale merges. This row holds no shared mutex. |
| `PQ-022.relay-reauthor` | implementation checkpoint `e69af0ef`; H1/review evidence checkpoint `780b77b3`; writers released 2026-08-04 | No path or mutex held; canonical source/release relay GLBs, both manifests, deterministic Blender source/tooling, offline evidence, exact retained route captures, and causal visual receipt are committed | **IMPLEMENTED + FUNCTIONAL H1 + EXACT-FINAL CAUSAL REVIEW ACCEPTED / ALL MUTEXES RELEASED.** The relay reads as an asteroid-gripping recovery/communications installation with visible load paths, stepped core, formed vessels, transfer trusses, receiver, mast, service hardware, and localized ownership cues. Frozen identity, envelope, seven sockets, placement scale, collision proxy, cleanup, and runtime role remain intact. One Browser and one source-Electron launch passed close/default/far LOD0/1/2 presentation, and causal review binds exact release `85b8d74e...67a8` to G1/G2/G4 KEEP. No performance or promotion claim is inherited. |
| `PQ-022.relay-reauthor-h1` | exact unit accepted at `780b77b3`; released 2026-08-04 | No path or mutex held | **FUNCTIONAL H1 PASS / RELEASED.** Browser claim `25068-5f75ea9c6a14de2404bb4bf3` and Electron claim `45252-4720537cb0b09b0f6d1ff` each passed once at fixed seed 47 with no retry. All six original-resolution captures, exact hashes, consumed claim ledgers, one-launch counts, and parity records are retained under `evidence/h1/row7-pq022-asset-leaves/relay-reauthor/`. No performance, causal art, or physical-hardware claim is made. |
| `PQ-022.relay-reauthor-review` | exact unit accepted from candidate `780b77b3`; released 2026-08-04 | No path or mutex held | **PASS REVIEW / KEEP G1, G2, G4 / RELEASED.** Six exact-final route captures causally close the gray cylinder-and-box defect with a gripping collar, formed vessels, rooted transfer/recovery/communications hardware, separated material zones, and ownership signals. LOD2's screen-space detail boundary is explicit; exact release `85b8d74e...67a8` is route-visually accepted. No performance or promotion claim is made. |
| `PQ-022.refinery-reauthor` | candidate checkpoint `228f28d7`; guarded live promotion and focused checks completed 2026-08-04 | No path or mutex held; deterministic Blender source/generator, candidate/review/validator bundle, canonical source/release, and both manifest rows are committed or ready for the closing receipt commit | **IMPLEMENTED + EXACT-SOURCE G1/G2/G4/EMISSIVE KEEP / BLENDER + ASSET-MANIFEST RELEASED.** Candidate `49d6a50f...38ed` replaces the flat drum-and-wall row with a fabricated feed→crusher/transfer→differentiated separation→thermal/recovery→slurry/Chalk storage→dock/control chain. The one guarded transaction published source `9faf59a6...5d88`, Blend `5af14fcf...f6c`, and 15-KTX2/66-Meshopt release `3b673f76...7c0`; Foundry, Khronos, Blender export, admission, disposable-root release, corridor-assets, asset-receipts, and reachability are green. Zero Browser/Electron launches were spent. Targeted route H1, route-causal review, and matched performance remain separate units. |
| `PQ-022.billboard-buoy-reauthor` | implementation `dab9199a`; status reconciliation `e0b807bf`; writer released 2026-08-05 | No path or mutex held | **INTEGRATED + FOCUSED-GREEN / RELEASED.** The neutral shared billboard, dedicated 24-light Candle Fleet memorial, and neutral navigation buoy source/Blend/release families, manifests, ordinary runtime registration, and exact-source review are committed. The live identity split is 3/3 green and atlas integrity passes all 280 nodes. Browser/Electron presentation, causal route review, and matched performance remain separate unclaimed units. Receipt: `PQ-022-billboard-buoy-reauthor-REPORT.md`. |
| User-directed portrait and surface improvement | Codex primary graphics task; texture-audit checkpoint `48f7c9ff`; Ashline V2 surface checkpoint `22a1bb82`; Rig builder `a633fe65`; exact-source visual evidence `28b5af4c`; family epoch `fd30adcc` | `assets/ships/m4_ashline_v2/` except the read-only third-party reference tree, its deterministic build/render/finalize tooling, and focused offline evidence/tests | **FOCUSED GREEN / OFFLINE ASHLINE V2 RIG G1-G4 CHECKPOINT INTEGRATED / WRITER + BLENDER + ASSET-MANIFEST MUTEXES RELEASED.** Rig's existing silhouette, dimensions, sockets, collision, identity and family role remain frozen, while its boom, jaw, tether drum, paired drives, refractory liners, hardface wear surfaces, cable steel and recessed ignition cues now form one fiction/material-bound manufactured assembly rather than a primitive blockout. The exact source/candidate mirror is `E46AAFCB…B7B892` (3,610,796 bytes); 11 exact-source Rig artifacts are bound into the 29-artifact family epoch `D40B2BE6…A44E`. This is deliberately not a live promotion or a whole-ship G7 pass: inherited donor-hull surfacing, Reaver/Corsair differentiation, Browser/Electron, normal-flight/dense-combat/LOD, residency/performance, live manifests/selectors and the independent human art verdict remain open as separately claimable acceptance work. Old live Ashline, canonical manifests/runtime selectors and Quaternius reference assets remain read-only. |
| Modular-hull texture-role correction | Codex primary task; exact-base `2ca2d94d` | The ten `assets/ships/parts/hulls/hull_*.glb` sources, their exact ten `assets/ships/release/parts/hulls/hull_*.glb` pairs, corresponding rows in `assets/ships/parts/parts_manifest.json` and `assets/ships/release/release_manifest.json`, `tools/art/repair_modular_hull_texture_roles.mjs`, shared derived-map helpers/tests, and the exact modular-hull source/export wiring used to prevent regression | **FOCUSED GREEN / OFFLINE SOURCE+RELEASE CHECKPOINT INTEGRATED / ASSET-MANIFEST AND BLENDER MUTEXES RELEASED.** The ten canonical GLBs moved from 34 errors/27 warnings to 0/0, and all ten source/release pairs are hash-bound KTX2/Meshopt assets; the focused contract matrix is 44/44 green. This is intentionally a canonical-GLB repair path: Blender promotion now fails closed until material and runtime metadata parity is proven. Geometry, sockets, scale, collision, names, and runtime selection remain frozen. Fresh V2 CPU contact sheets and Browser/Electron/LOD acceptance are still open; the lane holds neither `browser-gpu` nor `validation-broker`, and those mutexes are acquired only by an exact acceptance unit. |
| Spacepunk markings and emissive atlas | Codex primary task; exact-base `ede3b1cd` | `assets/concept/factions/spacepunk_markings_motif_study_v1.png`, its exact concept-index row, `assets/ships/foundry/spacepunk_markings_v1/`, `tools/art/build_spacepunk_markings_atlas.mjs`, and focused atlas tests | **FOCUSED GREEN / SOURCE CHECKPOINT INTEGRATED / WRITER RELEASED; NO MUTEX HELD.** The project-original generated study remains reference-only. The production package is conventionally redrawn: 32 deterministic story-grounded paint/emissive cells, exact serial/dock text, 13 intentional emissive cells, stable UV metadata, mip gutters, and 64px survival checks; focused atlas tests are 3/3 and the concept index is 235/235. It is not runtime-wired. GLB/KTX2 integration, residency, bloom/alpha behavior, and close/flight/dense-scene acceptance remain a separate packet after an exact ship family is selected and the relevant leases are free. |
| Primary checkout intro/menu/HUD | integrated product baseline at `6271ca29` and `dcbe4a20`; writer released | `index.html`, `src/main.js`, `src/ui/screens/mainMenu.js`, `src/ui/screens/newGame.js`, `src/ui/hud.js`, `src/ui/uiRoot.js`, `styles/intro.css`, `styles/menu.css`, `styles/ui.css`, and the two HUD evidence images | **INTEGRATED / NO ACTIVE WRITER.** The cinematic intro/menu and compact non-diegetic flight fascia coexist with the current Massline targeting/status behavior. Performance work may optimize their implementation but may not restore the prior UI, remove information, or lower the authored presentation. |
| `place_debris_chunk`, `place_dead_hulk`, `place_dock_interior` | source-only visual checkpoint `427d9897`; debris rebuild `8450287f`; offline source/release checkpoint | Exact source/release pairs plus the handoff, under the primary asset/remaster row above | **BLOCKED AT G5/G6/G7 AFTER OFFLINE KEEP.** Debris replaces the soft twin-pod read with one ruptured pressure module; hulk replaces iter219 with one continuous carrier/drill-tender rupture; dock replaces iter280 with an open-front H-04 service bay. Source GLBs, exact release pairs, texture roles, structural contracts, and offline matched reviews are green. The untouched military/grit dock variants are deliberately unrouted until they pass the neutral bay's composition gate. Current Browser/Electron, live LOD/performance, and independent art evidence remain unclaimed. |
| `place_asteroid_rock_a/b/c` | provenance repaired for rock_a (receipt `PQ-022-ceres-geology-rock-a-REPORT.md`); visual re-authoring decision owned by the art census lane | Those source GLBs, release artifacts, `parts_manifest.json` rows | **Correction 2026-07-28: the previous "untouched since 2026-07-11" claim was false** — four art-lane commits (`d2df9994`/`af287fc6`/`d17cc678`/`ede16953`, 07-24/25) overwrote the three live sources with unaccepted WIP iterations while touching no manifest. rock_a's recorded 1.97MB source was proven true (three lineage proofs) and restored; `check:graphics:asset-receipts` is GREEN again and the dev/release two-different-rocks split is closed. rock_b/rock_c still carry the identical live-source-overwrite defect (off-corridor; same one-line restore available; WIP iterations recoverable at `ede16953`). Visual re-authoring remains awaited and is the census lane's call. |
| Committed candidate refs | named local/remote branches and registered worktrees | Their branch tips and worktree metadata | These are not active writers. Preserve the refs as reviewable candidates; do not merge or rebase them merely for a checkpoint. **Both leading PQ-018 branches would revert all six place GLBs and delete the authoring textures retained at `329acfe8` — never merge them wholesale.** |
| Performance plan sources | admitted from `7f7d030b`; reconciled to the synchronized product baseline in this refresh | `design/PERFORMANCE_MODERNIZATION_EXECUTION_PLAN.md`, `design/PERFORMANCE_OPTIMIZATION_CONSTELLATION.md` | Source plan/research are preserved. The execution plan now explicitly protects the integrated intro/menu/HUD, current gameplay semantics, and source-only remaster checkpoint from performance rollback or premature promotion. Live execution authority remains the PQ-034–PQ-044 queue/packet set; source-plan presence does not grant a lease. |
| Performance modernization admission | originally integrated at `be840c03`; carried through synchronized tip `df31ee8c` | The reviewed control-plane admission through PQ-044 | **INTEGRATED AND RELEASED.** Admission itself holds no implementation path or mutex. PQ-034 claim/lease state is recorded separately below; its refreshed exact-base evidence now binds the synchronized tree. |
| `sf-perf-modernization-20260726` | unaccepted foundation candidate | PERF-00 pure harness files plus PERF-03/PERF-05/PERF-06 candidate paths | **PROTECTED READ/REVIEW INPUT — no packet authority.** Never merge or commit wholesale. Selectively port only after the owning packet is admitted and claimed. |
| `sf-perf01a` / `codex/perf-01a-background-lifecycle` | protected lifecycle candidate reviewed for PQ-035 | `electron/main.cjs`, candidate preload, `package.json`, lifecycle checks, `src/core/loop.js`, `src/main.js`, lifecycle tests | **PROTECTED DIRTY CANDIDATE — preserve in place; no merge, rebase, clean, remove, or overwrite.** Its ten-commit/dirty contents were classified selectively; accepted concepts were ported and repaired on master, rejected hunks remain only on the protected branch, and PQ-035 is now terminal. The worktree holds no lease or future acceptance authority. |
| PQ-034 / PERF-00 native closure | Accepted source `4f602802`; Browser `22380-df81be7b607f4276302e6ac8`; Electron `32560-97a1a4a4e9dade4d4ce87d91` | No mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-01.** Same clean source digest `8948e0ad…`, distinct runtime candidates/raw traces, 25/25 valid windows per runtime, real Intel D3D11, quiet activity, empty error arrays, context recovery, restoration, and owned cleanup all pass. Browser's eight-PID owned tree reaches zero; enabled overhead is `0.772627%`. The integrator causal review accepts harness authority/validity, claims no optimization gain, and grants no absolute-budget waiver. Receipt: `PQ-034-performance-harness-REPORT.md`. |
| PQ-019 H3 matched performance | Accepted candidate `51c5967a`; exact unit `PQ-019.h3-performance`; claim base `8febcb42` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-01.** Browser claim `34148-f81823309088525213ab63c2`, digest `5fa313e2…`, passed all six matched windows on real Intel ANGLE D3D11/default quality with zero page issues and clean teardown. Loaded pursuit median p95 was `16.8 ms` versus normal `33.4 ms`; median frames above 32 ms were `6` versus `51`, so the live feature fits the matched envelope. The separate absolute target remains honestly open and unwaived: normal pair 3 contained two >50 ms frames and normal median p95 misses the `17.5 ms` sampling envelope. Receipt: `PQ-019-h3-performance-REPORT.md`. |
| PQ-020 H3 matched performance | Accepted candidate `f7464211`; exact unit `PQ-020.h3-performance`; claim base `dc210503` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-02.** Browser claim `27012-047cbbea6c18528dea331f95`, digest `103b715d…`, passed all six 300-interval windows on real Intel ANGLE/D3D11/default quality with zero raw hitches, zero page issues, and clean teardown. Floor median p50/p95/p99 was `16.7/16.8/17.1 ms`; Cathedral target was `16.7/16.8/16.9 ms`. Exact topology pruning retains one indexed 86,661-triangle/eight-role color batch, classifies only 1,882 exposed-alloy triangles as genuinely open, prepasses 84,779 closed triangles with a position-only shader, and reuses one packed ORM sample per role without changing source/release assets, pixels, LOD, defaults, or content. Receipt: `PQ-020-h3-performance-REPORT.md`. |
| PQ-023 H3 matched performance | Accepted candidate `93c76f65`; exact unit `PQ-023.h3-performance`; claim base `e996a837` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-02.** Browser claim `25092-dc1e123131f87f9b970e8185`, digest `606f7607…`, passed all six matched windows on real Intel ANGLE/D3D11/default quality with exact pose/route continuity, zero target >32/>50/backlog/product hitches, and clean teardown. Floor/target median p50/p95/p99 were `16.7/16.8/17.0 ms` and `16.7/16.8/17.3 ms`; correlated GPU median p95 was `9.017811/10.403488 ms`. Ambient-mixed dense peak medians were `254 particles / 96 sprites / 60 streaks`; one beam/six explosions stayed exact per target run and all critical cues survived. Three prior failed claims are retained as fully characterized harness evidence. No production/asset/H1/H2 path changed. Receipt: `PQ-023-h3-performance-REPORT.md`. |
| PQ-023 station-side operation VFX | Integrated at `48e0bc74`; exact unit `PQ-023.station-side-event-vfx` | No path or mutex held | **FOCUSED-GREEN + RELEASED 2026-08-04 / ZERO HEADED RUNS.** The four existing seeded station operations now reach one fixed six-record pooled renderer with distinct non-color silhouettes, 12 Hz active cadence/idle sleep, 1,500-unit culling, dedupe, expiry, sector/save cleanup, real-patrol retirement, and reduced-motion/flash behavior. Focused tests are 6/6; station contract and VFX sleep checks pass, with 0.01711 ms idle mean. Receipt: `PQ-023-station-side-event-vfx-REPORT.md`. |
| PQ-024 H3 matched performance | Accepted candidate `2509a168`; exact unit `PQ-024.h3-performance`; claim base `02d2c877` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-02.** Browser claim `33596-be4c6688b2df89754fd8285d`, digest `293e6966…`, passed all six matched windows on real Intel ANGLE/D3D11/default quality with exact route/pose/settings continuity and clean teardown. Floor/target median p50/p95/p99 were both `16.7/16.8/17.2 ms`; product hitches, >50 ms frames, backlog shedding, renderer admission, and page issues were zero. Correlated GPU median p95 was `11.112655/11.740571 ms`, a `+0.627916 ms` matched delta inside the `+0.8 ms` envelope; both separately reported absolute profiles also pass. Four prior failed claims remain causal harness evidence. H1 was retained; no relay visual-quality or historical optimization claim is made. Receipt: `PQ-024-h3-performance-REPORT.md`. |
| PQ-035 native lifecycle acceptance | Accepted source `f3046007`; Browser `10372-4aa9e5f78322240b4566e2bd`; source Electron `12340-3eefb1bf37636736c1d67ead` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-04.** Both one-use cells share source digest `bbd92995…db10f01`, fixed seed `35035`, route/regression digests, stable route signature, hardware Intel ANGLE/D3D11, zero runtime errors, zero hidden GPU submissions, bounded repeated transitions, foreground-equivalent cadence, and clean owned teardown. The accepted harness rejects and records foreground-interrupted windows instead of averaging them. Protected `sf-perf01a` remains untouched. Physical workstation suspend/lock and packaged startup were not synthesized; those exact host/package boundaries remain with `PQ-041.native-acceptance`. Receipt: `PQ-035-lifecycle-REPORT.md`. |
| PQ-036 / PERF-02 scheduler acceptance | Accepted at `391e8658`; retained PERF-00 Browser `22380-df81be7b607f4276302e6ac8` and Electron `32560-97a1a4a4e9dade4d4ce87d91`; current lifecycle pair `10372-4aa9e5f78322240b4566e2bd` / `12340-3eefb1bf37636736c1d67ead` | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-08-04.** The PERF-00 pair shares source digest `8948e0ad…c9913e4` and contains owner-complete scheduler observations across 25/25 valid windows per runtime. The current lifecycle pair covers the only later scheduler-owner diagnostics change. Deterministic owner tests cover injected UI/admission classification; no duplicate headed run, optimization delta, physical-host, or packaged-startup claim was made. Receipt: `PQ-036-scheduler-seam-REPORT.md`. |
| PQ-039 / PERF-05 hot-query acceptance | Accepted at `9b50f317`; deterministic owner evidence | No path or mutex held | **INTEGRATED + FOCUSED-GREEN + RELEASED 2026-08-04.** Full-scan shadow parity holds through stable order, boundary/tie cases, stale identity, destroy, and same-tick spawn churn (`3` checks / `0` mismatches). The exact 100→500 entity curve retains the same selected ID and nearby candidate visits at `1→1`; bounded scratch and lifecycle diagnostics are exposed. No Browser/Electron, CPU-time, FPS, GPU, compositor, or resource magnitude is claimed. Receipt: `PQ-039-hot-query-REPORT.md`. |
| PQ-025 performance owner facts | accepted implementation candidate `4df37347`; receipt `PQ-025-performance-owner-facts-REPORT.md` | No mutex held | **FOCUSED-GREEN + RELEASED 2026-07-31.** Full-window bounded p50/p99/raw intervals, calibrated missed-vsync, residency, and completed-frame draw/triangle owner facts are live and fail closed on overflow/window mismatch. The 180-sample overlay ring is diagnostic only. No manifest, qualification, Browser/GPU claim, or live performance conclusion was spent. |
| PQ-040 / PERF-06 | `claude/perf00-20260727`; implementation commit `8db096b7` | Dirty-range implementation paths and PQ-040 receipt | **FOCUSED IMPLEMENTATION COMMITTED / LEASE RELEASED.** Combat sprites and trail-streak matrix/color/opacity publish scene-owned component ranges; native broker performance acceptance remains pending. Ribbon, authored-instance, ship-auxiliary, unrelated VFX, assets, shaders, package dependencies, and runtime acceptance manifests were untouched. |
| PQ-041 / PERF-07 | source-Electron H1 accepted at `c8b4fa2c`; receipt `PQ-041-electron-smoke-h1-capture-REPORT.md` | No mutex held; row-8 pass plus three continuation failures retained | **H1 FUNCTIONAL PASS / RELEASED 2026-07-31.** One fixed-seed source-Electron launch proved canonical menu → New Game → authored flight → public Helios waypoint → physical `[ E ]` dock → Station → Ledger on Intel ANGLE/D3D11 with clean owned teardown. Exact package, paired Browser/Electron performance, controller, and human visual gates remain open. Protected `sf-perf01a` remains untouched. |
| Program-recovery harness wave | Codex primary integrator; exact PQ-019A/PQ-019C/PQ-020/PQ-041 repairs | No mutex held | **FOCUSED-GREEN / ALL FOUR HARNESSES RELEASED 2026-07-31.** Each retained H1 fingerprint has a seconds-scale regression and exact receipt. No headed, GPU, performance, or human claim was made by the repair wave. |
| Program-recovery route-cell wave | Codex primary integrator; exact `PQ-023.cues-harness-repair` and `PQ-024.survey-route-harness` complete | No path or mutex held | **BOTH FOCUSED-GREEN + RELEASED 2026-07-31 / HEADLESS ONLY.** PQ-023 now moves inside authored Cathedral range before awaiting admission in both hosts; retained valid evidence was not rerun. PQ-024 now has a registered fixed-seed one-use public Browser route through real output, relay, save/Continue, and reentry. Neither cell was launched. No `browser-gpu`, `validation-broker`, `performance-evidence`, or human mutex is held. |
| PQ-007 control-route recovery | Codex primary integrator; exact prompt-truth and broker-route harness repairs complete | No path or mutex held | **BOTH REPAIRS FOCUSED-GREEN + RELEASED 2026-07-31 / HEADLESS ONLY.** Pilot/Helm no longer teach rejected pursuit; both fixture modes remain; separate Browser/Electron broker cells now exist. `PQ-007.route-acceptance` is unblocked, but neither runtime claim has been spent. |
| PQ-007 broker-route harness | Codex primary integrator; exact unit `PQ-007.route-harness-repair` complete | No path or mutex held | **FOCUSED-GREEN + RELEASED 2026-07-31 / HEADLESS ONLY.** Both fixture modes remain intact; separate one-use Browser and Electron actor cells now cover visible New Game, useful hostile acquisition by `G`, native relative pointer drawing/clutch/reversal, HUD truth, pursuit absence, release, and keyboard resumption. Neither claim was spent. |
| PQ-007 route acceptance | Codex primary integrator; exact unit `PQ-007.route-acceptance` complete | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-07-31.** Distinct fixed-seed Browser and Electron claims passed once each on candidate `c1b190e2`; public keyboard/pointer route, useful `G` acquisition, draw/clutch/reversal, visible HUD/route, pursuit absence, release, and keyboard recovery are proven. No performance or physical-controller claim was made. |
| PQ-019 capsule H1 continuation | Codex primary integrator; exact unit `PQ-019.capsule-h1-capture` complete | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-07-31.** Candidate `3590acf9`, Browser claim `7584-0f021eaf85838a532440c0a8`, digest `530315c2…`: the live authored capsule is centered and judgeable at applied runtime zooms `45/66/108`, with no page errors. The accepted `--capsule-only` route did not recapture retained facility/cue evidence. H2 now keeps this capsule while revising only facility identity. |
| PQ-019 surface-heist H1 continuation | Codex primary integrator; exact unit `PQ-019.surface-heist-h1-capture` complete | No path or mutex held | **ROUTE-ACCEPTED + RELEASED 2026-07-31.** Candidate `f30a35d0`, Browser claim `16760-d2392f32208ac30dca5602a5`, digest `df8e0618…`: fence, confiscation, production-combat destruction, and both reduced-stake attempts passed on real Intel ANGLE D3D11 with zero page errors. Retained DOM-abandon/lawful-observe evidence was not rerun. H2 keeps all five Browser outcomes; performance and Electron route parity remain separate. |
| PQ-020 Ceres H1 continuation | Exact unit `PQ-020.ceres-h1-capture` complete on candidate `04514d0b` | No mutex held | **FUNCTIONAL H1 ACCEPTED + RELEASED 2026-08-01.** Browser claim `26052-3223fa474a1b497e1638943b` and the gated source-Electron continuation each passed 21/21 frames at seed `47` on Intel ANGLE/D3D11 with zero issues. Both endpoint directions, all four natural pocket arrivals, Cathedral admission/framing, F5/Continue restoration, repeat selections, exact semantic parity, and clean Electron teardown are proven. Receipt `PQ-020-ceres-h1-capture-REPORT.md`; H2 and H3 remain separate. |
| PQ-023 Cathedral cue H1 continuation | Exact unit `PQ-023.cues-h1-capture` complete; Browser candidate `f373e8a7`; Electron repair `e2bb0165` | No path or mutex held | **FUNCTIONAL H1 ACCEPTED + RELEASED 2026-07-31.** Browser claim `40008-bf8ebe348500b65aa41197e6`, digest `3b31ee47…`, passed `87/87` frames and `15/15` motion segments on real Intel D3D11 with zero issues and all named Cathedral/cleanup predicates green. After a duplicate-navigation harness repair, distinct Electron parity matched all four transition/caption/accessibility semantics with zero issues and clean owned teardown. H2 now keeps Cathedral accessibility while routing only combat readability into repair; H3 remains separate. |
| PQ-023 combat-readability H1 continuation | Codex primary integrator; exact unit `PQ-023.combat-readability-h1`; candidate `b626a8b3` | No path or mutex held | **FUNCTIONAL H1 ACCEPTED + RELEASED 2026-08-01.** Browser claim `20336-b461d7ba80289197646bd11d`, digest `f50250e3…`, passed the bounded 22-capture/five-motion/three-sheet route on real Intel D3D11 with zero issues and clean pools. Source Electron matched the exact five-cell semantic projection, emitted zero issues, and closed cleanly. Retained Cathedral, unrelated weapon, ordinary, and capital cells were not rerun. Evidence: `evidence/h1/row6-pq023-combat-readability/`; receipt: `PQ-023-combat-readability-h1-REPORT.md`. Causal review is next. |
| PQ-023 combat-readability causal review | Codex primary integrator; exact unit `PQ-023.combat-readability-review`; candidate `b626a8b3` | No path or mutex held | **PASS REVIEW / RELEASED 2026-08-01 — KEEP FLAK, REVISE ONLY FULL-MOTION SMALL DESTRUCTION.** Original-resolution stills/motion prove the repaired flak's compact radial cloud differs from autocannon's incidence fan. Normal small destruction remains a minute speck/two-fragment event and is less legible than the kept reduced fallback. Dense composition, cleanup, Cathedral, larger hierarchy, and Browser/Electron parity remain KEEP. Receipt: `PQ-023-combat-readability-review-REPORT.md`; an exact small-only repair/capture/re-review chain is now queued. |
| PQ-023 small-destruction salience repair | Codex primary integrator; exact unit `PQ-023.small-destruction-salience-repair`; entry `e1821165` | No path or mutex held | **FOCUSED GREEN + RELEASED 2026-08-01.** The new contract reproduced 22/23 because the normal core lived only `0.09 s`; the localized full-motion ignition repair passes 23/23 with a `0.14 s` core, larger hot footprint, three biased fragments, and one offset lobe. Reduced mode remains pruned to its accepted pair; no ordinary ring was introduced. Presentation, lifecycle, dense-budget, and baseline gates pass. Receipt: `PQ-023-small-destruction-salience-repair-REPORT.md`; small-only H1 is next. |
| PQ-023 small-destruction salience H1 | Codex primary integrator; exact unit `PQ-023.small-destruction-salience-h1`; candidate `dfecee8f` | No path or mutex held | **FUNCTIONAL H1 ACCEPTED + RELEASED 2026-08-01.** Browser claim `27440-9da9c44669eff50157e38fbd`, digest `fbabadf8...`, passed the bounded 14-capture/three-motion/two-sheet route on real Intel D3D11 with zero issues and clean pools. The repaired opening measured three sprites, three fragments, `1.46016` hot size, and `3.51` envelope without an ordinary ring. Source Electron matched the exact three-cell projection and closed cleanly. Evidence: `evidence/h1/row6-pq023-small-destruction-salience/`; receipt: `PQ-023-small-destruction-salience-h1-REPORT.md`. Causal review is next. |
| PQ-023 small-destruction causal review | Codex primary integrator; exact unit `PQ-023.small-destruction-salience-review`; evidence commit `7f3c769d` | No path or mutex held | **PASS REVIEW / RELEASED 2026-08-01 — KEEP.** Matched original-resolution openings and declared motion windows show the repaired event now lands as a bright sustained core plus three-point asymmetric breakup at the ordinary camera, remains the smallest hierarchy member, and borrows no ordinary ring. Reduced fallback, dense composition, cleanup, flak/Cathedral/larger hierarchy, and Browser/Electron parity remain KEEP. Receipt: `PQ-023-small-destruction-salience-review-REPORT.md`. H3 is next. |
| PQ-024 survey H1 route pair | Exact unit `PQ-024.survey-h1-capture` complete; candidate `c10f8921` | No path or mutex held | **FUNCTIONAL H1 ACCEPTED + RELEASED 2026-07-31.** Browser claim `31656-aad66eda6f14d0177d92cd8d`, digest `61fa7c4d…`, passed the full public Survey → Core → real output → one relay → F5/Continue → same-site re-entry route on real Intel D3D11 with zero issues. The gated Electron run matched normalized semantics and closed cleanly. H2 later found a presentation-only contradiction in the Core frame; production, relay, Continue, re-entry, and H3 evidence remain valid and are retained. |
| `sf-ctl-a` / `sf-ctl-b` / `sf-ctl-c` / `sf-ctl-d` worktrees | controller run 2026-07-26 — **leases released** | — | All four lanes are merged and released. The worktrees are intentionally **left in place**: each holds a `node_modules` **Windows junction** to the primary checkout, and `git worktree remove` would follow the junction and destroy the primary's dependencies. Remove the junction first if these are ever cleaned up. `sf-ctl-a` holds a preserved, deliberately unintegrated PQ-018 candidate. |
| PQ-024 / survey-claim | integrated to master at `07d2ba53`; H1 candidate `c10f8921`; relay visual evidence `780b77b3`; H3 candidate `2509a168` — **leases released** | — | **INTEGRATED / FUNCTIONAL H1 + MATCHED H3 ACCEPTED / H2 COMPLETE WITH TARGETED REVISE.** Atomic Core state, real-output consequence, exactly-one relay, cold Continue, same-site parity, final relay art, and matched performance are proven. Only the Core-commit presentation contradicts itself on screen; an exact repair/H1/review continuation owns that frame. Final promotion also waits on `PQ-022.promote-relay` receipt binding. |
| `PQ-024.h2-verdict` | exact review from survey candidate `c10f8921` plus relay evidence `780b77b3`; released 2026-08-04 | No path or mutex held | **REVIEW COMPLETE / REVISE COMMITTED TRANSITION; KEEP ALL DOWNSTREAM CELLS.** Both hosts show `NO CLAIM`, red `ASSAY 2/3`, and a stale occupied placement preview beside the permanent-site receipt. Production consequence, one relay, save/re-entry, final relay art, and H3 remain KEEP. `evidence-review` is released; no human-review blocker remains. |
| `PQ-024.committed-transition-repair` | implementation commit `7ad32c94`; exact unit released 2026-08-04 | No path or mutex held | **FOCUSED GREEN + RELEASED.** The first-site survey-commit listener now resolves the new site and forces the existing next-frame HUD/inspector owners. The public actor exits Build through shipped `Escape` and fails closed until committed owner state, `ANCHORED`, durable assay copy, the site overview, awaiting-first-output consequence, and no occupied-placement error agree. Focused regressions passed 30/30 in about 0.45 s; no Browser/Electron launch was spent. Receipt: `PQ-024-committed-transition-repair-REPORT.md`. |
| `PQ-024.committed-transition-h1` | unclaimed; focused harness corrections `355483eb` and `91202dea`; released 2026-08-05 | No path or mutex held; retained two-attempt reconciliation remains evidence | **READY / UNPROVEN / RELEASED.** Two Browser attempts exposed and then focused-green repaired two harness-only control errors; zero Electron launches and no accepted target screenshot exist. The production transition repair remains focused-green, and accepted Survey, production, relay, Continue, re-entry, and H3 cells remain valid. A future exact capture may be freshly claimed if it is still worth doing; this stale row does not reserve `browser-gpu` or `validation-broker`. |
| Retained worktree census | 25 registered worktrees at refresh | Worktree metadata only; no cleanup lease | **PRESERVE.** Twenty-three have zero tracked changes. `sf-perf-modernization-20260726` has 19 tracked changes and `sf-perf01a` has 9; both are protected foreign candidates and were not inspected beyond status counts. Most clean worktrees are fully merged or old/superseded checkpoints; four clean refs retain unique commits, including the ten-commit PERF candidate. This recovery does not remove worktrees or branches. |

## Current program facts needed for dispatch

| Item | Current fact | Consequence |
|---|---|---|
| **NEXT WORK (read this row first)** | Coordination refreshed 2026-08-05. The only active writer is the `claude-graphics` modern-parity lane above. `PQ-022.billboard-buoy-reauthor` is integrated/released; `PQ-024.committed-transition-h1` is unfinished but unclaimed and reserves nothing. | Re-run the live dispatcher before selecting work, preserve Claude's exact dirty paths, and claim only a disjoint outcome. No pre-2026-08-05 claim on this board is a blocker. |
| H1 headed batch status | **ALL FUNCTIONAL H1 ROWS ACCEPTED.** Original PASS evidence remains valid for rows 1, 2, and 7; rows 3, 4, 5, 6, 8, and PQ-024 row 9 are accepted candidate-bound continuations. | Do not rerun accepted H1 rows as a ritual. H2 judgment and H3 matched performance remain distinct. |
| `npm run check` (recorded 2026-07-27 at `7f7d030b`) | **Was dead on arrival and had been for 333 commits.** `package.json` defined a `precheck` npm **lifecycle** script; npm ran it automatically before `check`, `check:sim:v3` inside it was red, and the chain exited 1 having executed **zero** of its ~97 links — while looking like an ordinary `check` failure. **Repaired:** the hook is deleted and its three gates (`check:m1:tether-mass`, `check:sim:v3`, `check:sim:v3:compare`) are now the first links of `check` itself, with `check:save-schema` ahead of them. 101 links, all visible. | Earlier claims that "the chain dies around link 79 on `check:sim:compare`" were wrong; nothing downstream had ever run. Do not reintroduce a `pre*`/`post*` lifecycle script — put the link where people can see it. |
| 47-A telemetry goldens (recorded 2026-07-27) | Both were stale, deterministically, since `850c80f3` (2026-07-14). **Re-recorded with evidence**, not reflexively: the `850c80f3` tree was exported with `git archive` and re-run, reproducing both prior hashes exactly, and full 720-tick snapshots were diffed field by field. 21060 fields moved; 21033 are the new `$.economy` market-regime model. **Zero entity pos/vel/rot/angVel fields changed on either flight controller** — the physics contract is bit-identical. The nine trace-count changes are one new `shield.collapse` cue caused by `f277c5e7`'s `DAMAGE_MODEL.subsystemShare 0.75 → 0.35`. Full rationale is in the `notes` of both expected files. | `check:sim` and `check:sim:v3` are green. Anyone re-recording these again must produce the same class of evidence; the files say so. |
| `check:sim:compare` / `check:sim:v3:compare` | **Structurally cannot fail on a stale golden.** `scripts/sf-sim.mjs:716` returns ok whenever the two runs agree with *each other*, explicitly tolerating `expectedHash` and `expectedTraceCount` diffs against the expected envelope. That is why both stayed green through 333 commits of golden drift. | They are determinism checks wearing a correctness check's name. Only `check:sim` / `check:sim:v3` gate the goldens. **Open handoff:** `sf-sim.mjs` is not owned by this lane; the tolerance should be a named `--allow-stale-expected` opt-in, not the default. |
| `check:massline` (updated 2026-07-27 at `cf1bc145`) | **GREEN, 23/23.** The three reds are fixed. Its runner previously executed 23 children fail-fast and so named only the first red — it had **three** at `7f7d030b`, and everyone was chasing one. `scripts/check-massline-aggregate.mjs` is rebuilt on `check-baseline.mjs`'s shape: it runs every child past a failure, reports the full set, and schedules longest-first (the 36 s long pole was declared last). Serial cost **62 s**, not the ~130 s previously recorded; 36.2 s of that is `check:47a:physical-branches` alone. | Treat "the aggregate named one failure" as a lower bound in any *older* receipt. Current runs report the whole set. |
| `check:baseline` (latest PERF-00 exit run 2026-08-01 at `e4d9afdf`; code-identical acceptance source `4f602802`) | The fast gate. Ten links: the prior four sim gates, save schema, flight V3, M1 tether/mass, Massline, UI screen imports, and the added PQ-020 Ceres topology contract. It runs every link after a failure, reports per-link timings, and exits red on any failure or on exceeding its enforced 90 s budget. The PERF-00 exit is **10/10 green in 44.307 s**. Excludes `check:flight:clean` (~6 min) by design. | The before-and-after gate for every lane. `scripts/check-baseline.mjs`, routed in `AGENTS.md` §9. PERF-00 preserved and extended the entry set; downstream packets must retain that superset. |
| Check-runner concurrency and timeouts (2026-07-27, `cf1bc145`) — **read before building any measurement harness** | Two defects, both found by measurement, both likely to recur in new runners. (1) **Timeouts on `shell: true` children were never enforced at all.** `child.kill()` kills `cmd.exe` only; the grandchild outlives it and holds the stdio pipes, so the runner waits on `close` forever — a 180 s budget once reported **361 820 ms** and exit `null`. Fixed with `taskkill /pid /t /f` plus a grace window. (2) **Nested pools multiplied.** `check:massline` is a link of `check:baseline`, and both became parallel pools, so widths multiplied to 16 concurrent heavy sims. Runners now share one allowance via `SPACEFACE_CHECK_JOBS` and **split** it rather than re-spending it; `defaultAllowance()` keys off `round(availableParallelism()/2)` (physical cores under SMT), which is where the measured knee sits. | Any new process-spawning runner must reuse `taskkill /t` teardown and honour `SPACEFACE_CHECK_JOBS`, or it re-creates both bugs. Contention data for PERF-00: past ~2 concurrent heavy sims per lane, per-process time inflates faster than the pool drains — `check:sim` measured **8 227 ms → 14 942 ms** at x4, and the massline aggregate is *slower* at x4 (63 774 ms) than at x2 (47 867 ms). A perf sample taken while anything else runs is not a measurement. |
| Default player route | **Playable.** `npm run check:visual-stability` exits 0 at `aca82bb0` — 360 frames, 16 ships, `failureCount: 0`, no page errors. A previous entry here claiming `place_debris_chunk` "stalls playable flight" was measured and is **false**. | Do not treat the route as blocked. Route-acceptance work may proceed. |
| `check:assets:live` | H1 row 1 passed on real Intel/ANGLE D3D11: 77/77 declared GLB parts, 2/2 whole ships, 14/14 presented ships authored, no visible fallback, no page errors. | Functional headed admission is green. Its contended millisecond fields are not performance evidence; H3 still owns matched timings. |
| `check:art` | Red at **link 1 of 23** — `test/asset-pipeline-gate-wiring.test.mjs:27`, `check:ci reaches the asset-pipeline contract through check:art exactly once (0 !== 1)`. The chain never reaches any GLB probe. **Correction, 2026-07-27: this is NOT a `package.json` wiring change and no such change can fix it.** The two assertions are mutually unsatisfiable. `asset-pipeline-gate-wiring.test.mjs:26-28` substring-counts `npm run check:art` inside `scripts['check:ci']`, while `scripts/check-m1-tether-mass-grounding.mjs:32` asserts `scripts['check:ci']` is *exactly* `npm run check:ci:report`. A delegating script can never literally contain its delegate's links. | Do not attribute `check:art` redness to assets, and do not attempt a `package.json` fix — it will just flip which gate is red. The defect is in the test: it should walk the aggregate expansion the way `check-gate-reachability.mjs` and `check-ci-report.mjs` already do, instead of substring-matching a one-line delegation. **Handoff: `test/asset-pipeline-gate-wiring.test.mjs`, unowned.** |
| `check:phase0-slice-contract` (recorded 2026-07-27) | Red, unrelated to the check chain: `Unclassified Math.random site: src/audio/bandBeds.js:33`. That line is `const random = typeof options.random === 'function' ? options.random : Math.random;` — an injectable default, i.e. exactly the pattern the classifier exists to bless. The file is unchanged since `850c80f3` and the tree is clean, so this predates the current chunk. | Escalated, not inherited. Owner is the audio/determinism seam, not the truth lane. Either classify the site or teach the classifier about injected-default RNG. |
| PQ-007 | Focused-green correction plus Browser/Electron public-route acceptance integrated at `249c5e38` | Do not restore the rejected pursuit-slot mechanics from historical prose. The remaining controller-specific gap is not part of this route-accepted keyboard/pointer claim. |
| PQ-017 | World Site kernel integrated; writer lease released | PQ-018 may consume the integrated manifest/kernel/runtime contract after re-reading current symbols. |
| PQ-018 | **Phases 0-3 and public Browser/Electron route integrated/accepted; exact-source art review complete with `REVISE`.** The Cathedral is a placed World Site with seven components, physical payload, five evidence pages, and proven monumental presence. Whole-asset review of source `f335935f…` / release `dc5510f…` found P1 slab/bar construction, weak fracture interfaces, and clay-dark material allocation. | `PQ-018.cathedral-reauthor` → targeted H1 → causal review now owns visual closure while preserving the two-half envelope, fly-through, sockets, markers, coordinate and useful source. Matched Ceres H3 remains folded to PQ-020. Receipt `PQ-018-cathedral-art-verdict-REPORT.md`. |
| PQ-019 | **A/B/C, both harness repairs, both H1 captures, H2 review, and matched H3 performance complete.** The five Browser outcomes, physical capsule, and non-regressing matched feature load are accepted. H2 revises facility readability: launcher inherits the relay defect; catcher/fence remain too similar. Receipts `PQ-019-h2-facility-route-REPORT.md` and `PQ-019-h3-performance-REPORT.md`. | `PQ-019.receiver-facility-reauthor` owns only catcher/fence form/material identity. After the separate relay review, targeted facility H1 and causal review close the four-role art question. Electron route coverage and W03/W04/W05 promotion remain downstream; the absolute performance target remains open and unwaived. |
| PQ-020 | **Topology, route-harness repairs, functional H1, H2 review, and H3 matched performance are complete.** Candidate `04514d0b` proves the full Browser/Electron route; candidate `f7464211` closes the Cathedral target performance cell with six hitch-free windows at the 16.7 ms class. H2 keeps Cathedral presence but revises first-three-pocket flight presentation: the maps are distinct, while arrival stills are player-ship-dominant or edge-cropped. Receipts `PQ-020-h2-pocket-cathedral-REPORT.md` and `PQ-020-h3-performance-REPORT.md`. | The refinery re-author implementation is complete with exact-source KEEP; its separate targeted H1/review chain still owns route presentation. After that causal review, `PQ-020.pocket-presentation-recapture` and review own only target-visible refinery/outpost/beacon proof. Promotion remains downstream; old failures are diagnostics only. |
| PQ-021 | **CHECKED OFF / `INTEGRATED` + `ROUTE_ACCEPTED` at `61f2ffe7` 2026-08-01.** One bounded projector/panel is reachable through station and Codex; five physically earned Cathedral pages survive Continue with exact Browser/Electron host parity, accepted media/provenance/focus/accessibility, and shipped controller-semantic reachability. Final receipt `PQ-021-h2-legibility-controller-REPORT.md`. | No remaining dispatch unit. No physical-device or matched-performance claim was manufactured, and neither is required to keep the completed Ledger packet open. |
| PQ-022.exterior-relay-collar | **STRUCTURAL REPAIR IMPLEMENTED AT `e69af0ef`; REVISED FUNCTIONAL H1 + EXACT-FINAL CAUSAL REVIEW ACCEPTED AT `780b77b3`.** The canonical source/release replaces the plain-cylinder/box read with a manufactured rock-gripping recovery/communications relay while preserving exact identity, envelope, sockets, placement, collision, cleanup, and runtime binding. One Browser and one source-Electron route passed close/default/far LOD framing; causal review binds exact release `85b8d74e...67a8` to G1/G2/G4 KEEP. | Relay art closure is complete. Retain its exact evidence until corridor H3 and the separate PQ-024 receipt-promotion/binding unit are dependency-ready; do not spend another relay route launch. Receipts `PQ-022-relay-reauthor-h1-REPORT.md` and `PQ-022-relay-reauthor-review-REPORT.md`. |
| PQ-022 corridor stations/furniture/traffic | **H2 REVIEW FOLLOW-UP: KEEP 8 / REVISE 2 / REVERT 0 AT THE OFFLINE IMPLEMENTATION BOUNDARY.** Trade hub, military, mining, jump ring, all three Helios traffic bodies, and the exact-source revised refinery are KEEP. Billboard and nav buoy remain visibly under-authored; original disposition receipt `PQ-022-corridor-assets-h2-disposition-REPORT.md`, refinery implementation receipt `PQ-022-refinery-reauthor-REPORT.md`. | Claim `PQ-022.billboard-buoy-reauthor` next. The refinery targeted H1/review and the billboard/buoy implementation/H1/review—not a wholesale Row 7 rerun—must close before corridor H3 and milestone promotion. |
| PQ-023 | **GOLD-CORRIDOR CUE MILESTONE ACCEPTED + RELEASED.** Implementation, Browser/source-Electron H1, solo-integrator H2 and causal reviews, repaired/accepted flak and small destruction, accessibility semantics, and matched H3 performance are complete. Candidate `93c76f65` passed the six-window headed Browser cell; promotion commit `d13fd041` binds receipt blob `599a0f44…` into PQ-025. | No gold-corridor cue phase remains. Preserve the accepted receipt/evidence chain; unrelated future PQ-023 umbrella expansion is outside this recovery wave. |
| Inherited reds newly attributed 2026-07-28 | `check:one-voice` (stale source-string assertion against `registry.js`, scripts/check-one-voice.mjs:231; 10 behavioural sections pass) and `check:audio-identity` (three uncovered ids: `sfx_mining_seam_reward`, `sfx_vector_mine`, `sfx_rcs_disrupt`) — both A/B-proven pre-existing at their lanes' bases; repair chips filed. `check:graphics:asset-receipts` was in this class and is now GREEN (rock_a provenance repair). | Do not attribute either red to current corridor lanes; both have precise causes recorded. |
| Uncovered invariant, now closed | `operation-unreachable` (`worldSiteKernel.js:243`) throws at boot via `_ensureWorldSiteRecords`, and nothing asserted it. | `test/world-site-operation-reachability.test.mjs` (5 cases) is wired into the pq017 world-site fast gate. Do not relax `reachableOperations` without it. |
| PQ-022–PQ-025 | Corridor remainder | Use their active packets and entry gates. Do not dispatch an umbrella row when a prerequisite leaf packet is required. Only `PQ-024.promote` waits on the exact `PQ-022.promote-relay` receipt/blob; the active committed-transition repair/review chain does not. PQ-025 additionally waits on the `PQ-022.gold-corridor-required-assets` and `PQ-023.gold-corridor-required-cues` milestone receipts. |

## Preserved candidates, not active authority

- Wreck Cathedral source asset and evidence are committed and reusable; current runtime contracts decide promotion.
- Narrative, Lark, and VP-220 candidates remain on named recovery refs. They are not merged, accepted, or leased merely because they exist.
- Historical planning handoffs under `docs/handoffs/chatgpt-portfolio-20260723/` are reference material. Active packets supersede their dispatch prose while retaining grounded technical findings.

## Refresh procedure

Before claiming a packet:

1. re-run `git status --short` and `git worktree list`;
2. inspect open branches/PRs touching the packet's write surfaces;
3. compare this file's `baseCommit` to HEAD and refresh when the expiry is exceeded or any listed owner changes;
4. record the packet lease and exact paths before implementation;
5. remove or update the lease immediately when work is integrated, abandoned, or moved.

Do not append closeout narratives here. Put exact-revision evidence in a receipt and rely on Git history for the rest.
