# Why this exists

Cloud inference is abundant; local review attention is scarce. The bank converts surplus Jules calls into narrowly scoped evidence-producing work while keeping merge authority local. Each task is small enough to reject cheaply, useful enough to merge independently, and explicit enough that `NO_CHANGE` remains a valid high-quality result.
