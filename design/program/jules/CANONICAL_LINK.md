# Canonical route note

This task bank is intended to be entered from `CANONICAL_BUILD_MAP.md` and does not supersede it. The bank is candidate cloud work only; admitted PQ work, INFERENCE production, and acceptance remain under the existing canonical program authority.

Canonical-map integration line to retain when this branch is merged:

`Jules / cloud agents / spend cloud requests` → `design/program/jules/README.md` → `scripts/jules-dispatch.mjs`.
