From 15f06a7317cef0267c4f48144bdb405e06b9eaae Mon Sep 17 00:00:00 2001
From: Devin AI <158243242+devin-ai-integration[bot]@users.noreply.github.com>
Date: Sat, 26 Sep 2026 02:32:19 +0000
Subject: [PATCH 5/5] Demo ledger: log baseline sim/pq020 reds landed by the
 2026-09-25 release merge

Co-Authored-By: Robin Gattis <93robingattis@gmail.com>
---
 design/program/DEMO_READINESS_2026-09-20.md | 1 +
 1 file changed, 1 insertion(+)

diff --git a/design/program/DEMO_READINESS_2026-09-20.md b/design/program/DEMO_READINESS_2026-09-20.md
index 7d158d942..32f029d5d 100644
--- a/design/program/DEMO_READINESS_2026-09-20.md
+++ b/design/program/DEMO_READINESS_2026-09-20.md
@@ -230,3 +230,4 @@ which before closing it.
 | D52 | Ceres pocket (`sector_ceres_belt`) offers only 2 distinct combat shapes — below the E6 repetition bar | E6's budget ("the same encounter shape cannot be the only combat offer for three hours; at least three shape ids in hours 0–3 and 5–9") measured across a 60-day `planEncounters` walk on `sector_ceres_belt` (tier 1, security 0.72, zones `mining_belt/refinery_approach/ambush_lane/derelict_field`): only `patrol_scan` + `bounty_hunter` are plannable — every other `deck:'combat'` shape is filtered by `minSectorTier ≥ 2`, `maxSecurity < 0.72`, zone-type mismatch, or `externalOnly`/`anchorPoiId` gating, and `bounty_hunter`'s `bountyOnly` is a fire-time gate so the runtime diet can be thinner still. The deliberate starter-pocket design (`sectors.js` "Tier-2 sectors restore the full combat band") leaves Ceres's combat diet one authored loss from a mono-offer. The demo route `sector_helios_prime` passes at exactly 3 (`opening_hauler_raid`, `patrol_scan`, `bounty_hunter`); frontier sectors pass at 9–12. Pinned by the new `test/e6-encounter-shape-budget.test.mjs` | Hours 1–10 in Ceres — same two combat beats repeating for a player who stays in the starter pocket | open — fix is authored: one or two more Ceres-plannable combat shapes (`deck:'combat'`, `zoneTypes ⊆ {mining_belt, refinery_approach, ambush_lane, derelict_field}`, no `minSectorTier > 1`, no `maxSecurity < 0.72`, not `externalOnly`, not `bountyOnly`-gated) would lift Ceres over the bar; the fixture covers the letter on helios_prime/nyx_march/io_reach today |
 | D53 | `src/systems/world.js` `_stripSectorFullExtras` → `dropDressingSector` (sector first materialized REDUCED as a neighbour, then entered FULL by gate) | The hero landmark is simply not there: the Skerris Throne (Sker) and the Resonant Cathedral (Vesta) are neither live entities nor dressing rows when you arrive, and their scanner/discovery POIs are gone too. Every POI of any sector reached from a neighbour is erased the instant the neighbour materializes REDUCED, and FULL entry never re-spawns them | Adventure travel; any landmark or scanner beat outside the start sector | open — ready patch `.devshots/landmark-diag/proposed-poi-residency.patch` (a `keep` predicate on `dropDressingSector` retaining POI rows, plus `_promotePoiRowsToLive` on FULL entry); verified with a loader redirect (Throne returns live, no new test failures). Not applied 2026-09-25 only because `world.js` carried another lane's live hunks; it spawns entities, so run sim-compare when it lands. Repro: `node .devshots/landmark-diag/probe-landmarks.mjs` (capture order Helios→Ceres→Tethys→Vesta→Pallas→Sker, seed 4242) |
 | D54 | Hero landmarks at the shipping camera (`scripts/capture-sector-identity.mjs --sectors=six --anchor=landmark`, seed 4242; probe `.devshots/landmark-diag/`) | Standing beside a named wonder you see a prop the size of your own ship, or nothing: the Candle Fleet (`place_memorial_array`, 14.8 m), the Resonant Cathedral (`place_maintenance_gantry`, 20.1 m, lore says twin spires and an arch) and the Skerris Throne (`place_dead_hulk`, lore says a fortress of welded hulls) all draw at or below the player hull (26.5 WU); the Quiessence's seventeen freighters spawn as `fx` with no place model, so only a 5 px buoy shows; the Tethys Anvil planet is ~2,200 WU off-frame from anywhere outside its pull, so its well has no visible presence at its edge; and under the untracked place-scale work (`modelTruthPlaceDrawScale` / `modelTruthMounts.js`) the Wreck Cathedral draws at ~18,300 WU, 30× its authored 609 WU | PQ-153.02 still review; any "go see it" beat | open — art/scale call: proposed targets Candle Fleet ~90–110 WU, Resonant Cathedral ~100–120, Skerris Throne ~140–160 (`_spawnPOIs` passes only `visualRadius` today; it must pass `placeScale`/`placeTargetRadius`), an intact dark-freighter place model for the Quiessence band, and the ×30 Cathedral fixed inside the place-scale lane before it lands |
+| D55 | `check:baseline` at `d4359bd2c` (2026-09-25 release merge) — `sim`, `sim-v3`, `sim-v3-compare`, `pq020-ceres-topology` red on a clean worktree | The 47-A authoritative telemetry hash drifted from its expected envelope (`sim`/`sim-v3` run + `--reload-at` compare all diverge at the reload/60-tick boundary), and the Ceres pocket-topology receipt no longer matches its digest — the release that landed the living-adventure lanes moved the sim/world without re-deriving these goldens. Reproduced identically on detached `origin/master` (5/16 red incl. `ui-control-labels`) so it is upstream of any lane work | The seeded-47A determinism receipt — the hook the demo's "same seed, same fight" claim stands on — currently can't prove itself | open — repro: `npm run check:baseline` in a clean `d4359bd2c` worktree (needs node_modules). Fix is re-deriving the 47-A expected envelope/topology digest against the released sim (or reverting whichever released hunk was unintentional) — per law do NOT re-record `test/*.expected.json` just to silence; a sitting in the 47-A/adventure-lanes files handles it |
-- 
2.34.1

