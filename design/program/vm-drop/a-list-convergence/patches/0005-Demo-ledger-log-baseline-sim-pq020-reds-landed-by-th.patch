From bc89361e940c1818bac89e54c3741732cb486593 Mon Sep 17 00:00:00 2001
From: Devin AI <158243242+devin-ai-integration[bot]@users.noreply.github.com>
Date: Sat, 26 Sep 2026 02:32:19 +0000
Subject: [PATCH 5/5] Demo ledger: log baseline sim/pq020 reds landed by the
 2026-09-25 release merge

Co-Authored-By: Robin Gattis <93robingattis@gmail.com>
---
 design/program/DEMO_READINESS_2026-09-20.md | 1 +
 1 file changed, 1 insertion(+)

diff --git a/design/program/DEMO_READINESS_2026-09-20.md b/design/program/DEMO_READINESS_2026-09-20.md
index bdf6667aa..2634b1695 100644
--- a/design/program/DEMO_READINESS_2026-09-20.md
+++ b/design/program/DEMO_READINESS_2026-09-20.md
@@ -226,4 +226,5 @@ which before closing it.
 | D46 | 2026-09-25 `check:all` sweep — foreign red classes new since the 2026-09-23 receipt set | Full triage of all 46 ids 2026-09-25 late: every id re-run on the dirty main tree AND in a throwaway clean-HEAD worktree at `b9b0199ad`; each remaining red re-run once before being called real. **Green at HEAD and on the tree — 10** (fixed by the 57 commits since the sweep): depth-program-k1-ui-runtime, time-effects, sg05-runtime, sg06-tether-resilience, save-load-slot-trust, station-departure, recommended-next, localmap-routes, sg08-mix-profile, map-information-depth. **Contention flakes — 3** (pass quiet on both trees; the sweep reds were load artifacts): check-sg06-ai, check-sg05-branch-policies, check-sg06-production-ports. **(a) dirty-WIP artifact — 8** (red on the dirty tree, green at clean HEAD): check-bundle, check-perf-packets, check-src-reachability, check-authored-place-runtime, check-camera-shake-scope, check-sg08-golden-trace, check-47a-counterplay, check-title-continue-runtime. **WIP-resolved — 5** (red at clean HEAD, green on the tree because the uncommitted WIP *is* the fix; land with their owners): depth-program-authored-salvage (weapons `range 240` vs fixture `520` — dirty `src/data/weapons.js`), depth-program-k1 (k1-runtime production-fitting/ports), check-depth-program-k1-behavior (faction_understory combat fire — dirty `combatDoctrine`/`factionDoctrines`), check-47a-tactics + check-47a-live-branch (surrender_evidence — uncommitted 47-A work). **(b) fixed this sitting, commit `ec7ce0306`**: check-market-nav — stale check thresholds re-pinned to live data (Economy Pulse `ff6a0cae9` food basePrice 40→73; INF-085 `0efc5e5cf` intel label "scan · Nm old") — green now; check-sg08-render-vfx — partially (stale `trailSampleProcedural` token re-pinned to the landed `e0b3b820c` shard-sliver shader), still red on "fleet overflow ships should show procedural streak mesh" → VFX lane. **(c) owned elsewhere — 14**: HUD/ORRERY lane: check-type-floor (275 sub-12px declarations, all 13 ORRERY-refactor files), check-ui-budgets (baseline:stale + PQ-184 grammar debt; re-capture after the WIP settles), check-station-tabs (deterministic waitForResponse timeout at HEAD), check-mission-cargo-loading (buy-affordance label vs `orrery/marketLayouts.js`), check-first-15-runtime (sweep assertion names the HUD tracker). asset-pipeline lane: check-graphics-asset-receipts, check-kestrel-wholeship, check-parts-manifest, check-sg04-release-assets, check-station-archetype-glb-load (167 ok / 3 generator mismatches), check-shipworks-dock-composition (`place_dock_interior.glb` extension drift) — re-verified this sitting. depth/encounter lanes: depth-program-encounter-loader-test + -2 (committed `015-opening-hauler-raid` sits inside the append-only migration-era prefix; the seeded 60-schedule golden hash also drifts — goldens, not re-recordable per law). 47-A lane: check-47a-live-cold-open (Kessler comms assertion, red on both trees). **Environment, unresolved — 4**: probe-flight-visual (GPU browser capture cannot finish on this host; attempted twice again), check-mission-handoff (`page.goto` stall ×2, never reaches an assertion — needs a quiet host), check-bar-mission-readiness (timeout stage unstable across runs — needs a quiet host), check-assets-live (publish-ratchet: requires HEAD == origin/master; HEAD holds unpushed commits — resolves on push). D48 re-verified once this sitting: `node --test test/bounded-autosave.test.mjs` red with exactly the ledgered signature (6.2 ms-floor siblings, blocking spikes 424.8/72/63/22.5/17 ms inside `encode_part_dispatch`) — claim holds. | Demo build health — each red hides regressions in its own area | open — classification complete; 24 ids still red at clean HEAD, each named to its owning lane above; none is a mechanical D46 fix. Do not re-record receipts/goldens to silence |
 | D50 | Dormant-rock materialization can still spawn a scaled collider over a live hull on three paths sibling to the fixed A6 fling (`_tickAsteroidFieldInteractions` now promotes at collider skin; `_growFieldSeam` gained hull clearance + the authored collider scale) | Same defect class as the seed-4242 player fling (player ejected to a ~200k-unit orbit at ~3 km/s, `wave-a6` `fail_latch`): a dormant field rock becomes a live body while already inside a hull, and positional depenetration is unclamped — velocity clamps (`MAX_CONTACT_DV`, player give) do not cover positional writeback in `sg02DynamicBodyOwner`. Remaining overlap-capable admits: `src/systems/traffic.js` (~5019) promotes a barge's dormant rock at `dist < 40` to rock CENTER — radius-blind, so a barge parking on a big gas-rock (collider ~33) can materialize the ball over itself; `src/render/renderer.js` cook/prewarm promotes (~7160/7207/8840) batch-promote nearest dormant rocks by record radius — an NPC hull already overlapping the record eats a spawn-overlap; `src/core/physics.js` projectile sweep (~850) tests records at `rec.radius` while the promoted body carries the scaled collider, so bystanders under the record disc can be overlapped. Defense-in-depth gap behind all of them: no admission-time overlap check or per-tick positional-correction bound in the Rapier owner — any new path that admits a collider already-overlapped reproduces the yeet | Flight anywhere near dormant rocks — a mining barge or the player yeeted off the sector mid-demo | open — repro for the traffic row: park an NPC barge within 40 of a dormant `ast_gas_cloud` (R 14–30) record's center and let `_step*OreCarrier` promote; the spawned ball starts intersecting the barge. Cheapest coverage: extend `test/asteroid-field.test.mjs` collider-skin pattern to the traffic promote, or add an admission-time overlap guard in `promoteAsteroidFieldRock` |
 | D49 | `test/pq146-escape-routes.test.mjs`, `test/pq146-evidence.test.mjs`, `test/pq146-projectiles.test.mjs`, `test/pq146-save-roundtrip.test.mjs`, `test/pq146-save-episodes.test.mjs`, `test/pq146-clothesline-route.test.mjs`, `test/pq146-narrative.test.mjs`, `test/pq146-field-routes.test.mjs`, `test/pq146-flight-evidence.test.mjs` — 9 stunt-detection tests red at clean HEAD | The stunt-proof fixtures (Kickstart, Needle Thread, Clothesline, Rapier shove, trajectory observer, save roundtrip) assert launch/retention speeds and intercepts tuned to the OLD propulsion ceilings; after `e8d10fed7e` the scripted launches no longer cross the stunt gates (the needle gate is `1.25*life.cruise`), so the proofs read receipts 0 where 1 is expected. Pinned by bisect in a throwaway clean worktree (`git bisect run node --test test/pq146-escape-routes.test.mjs`, 2026-09-25): first bad commit `e8d10fed7e` (2026-09-16 "Restore fast cruise ceilings" — deliberate MOTION_CHANGED, motion goldens re-recorded but these nine fixtures were not re-derived); identical failure-name set at HEAD `45d844376` and on the dirty tree, and the later `703917d86` threat prefilter is exonerated (red at its parent too). Found by the PQ-210.01 lane while gating its own perf unit | Stunt receipts (the score language) silently stop firing in the regimes the proofs encode | open — repro: `node --test test/pq146-escape-routes.test.mjs` at clean HEAD (3 of 4 red, same names every run). Fix is fixture re-derivation against the restored ceilings (recompute each scripted launch's expected speed/intercept from the eight profiles' restored combatSpeed/precisionSpeed), NOT re-recording motion goldens and NOT weakening the needle gate; a sitting in `src/combat/stunt*` or the propulsion fixtures handles this as part of its unit |
+| D51 | `check:baseline` at `d4359bd2c` (2026-09-25 release merge) — `sim`, `sim-v3`, `sim-v3-compare`, `pq020-ceres-topology` red on a clean worktree | The 47-A authoritative telemetry hash drifted from its expected envelope (`sim`/`sim-v3` run + `--reload-at` compare all diverge at the reload/60-tick boundary), and the Ceres pocket-topology receipt no longer matches its digest — the release that landed the living-adventure lanes moved the sim/world without re-deriving these goldens. Reproduced identically on detached `origin/master` (5/16 red incl. `ui-control-labels`) so it is upstream of any lane work | The seeded-47A determinism receipt — the hook the demo's "same seed, same fight" claim stands on — currently can't prove itself | open — repro: `npm run check:baseline` in a clean `d4359bd2c` worktree (needs node_modules). Fix is re-deriving the 47-A expected envelope/topology digest against the released sim (or reverting whichever released hunk was unintentional) — per law do NOT re-record `test/*.expected.json` just to silence; a sitting in the 47-A/adventure-lanes files handles it |
 
-- 
2.34.1

