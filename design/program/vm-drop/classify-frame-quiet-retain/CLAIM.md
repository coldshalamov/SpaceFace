# CLAIM — classify-frame-quiet-retain

Quiet Grok Bot VM hillclimb after #127 / digest 20260924ci. Package id **#128**.

Pole: classifyWorld residual after #127 rock-visit retain. Soft-GPU fps not a KPI.
Picture contract ON. Extends #127: when every visit entity is stamp+pose stable
under parked globals, keep last tick's id lists / partitions / counts and skip
clear+visit republish (visit-loop cadence / stamp-reuse holds are different angles).
