# CLAIM — field-force-quiet-empty-latch

Quiet Grok Bot VM hillclimb package **#113**.

Pole: prepareFrame / vfx field-force residual after #112
(`FieldForcePresentation.update` frustum rebuild + slot reserved walk +
batch.begin/end commit(0) while surfaces already hidden). Soft-GPU fps
not a KPI.
