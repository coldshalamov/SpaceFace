# CLAIM — asteroid-query-callers

Claimed by Quiet Grok Bot VM (Code Work).

- Job: `asteroid-query-callers`
- Kind: patch (portable CPU)
- Pole: `queryAsteroidField` call-site leftovers after integer cell keys (over-wide rock disc + dead decode-runway scan)
- Scratch: `vm-work/asteroid-query-callers`
- Measured against master tip `35e519ebd`

Do not start a second copy.
