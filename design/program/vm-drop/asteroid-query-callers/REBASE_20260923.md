# Rebase — 2026-09-23 against master `568d1358e`

Master renamed `TABLE_PROMOTE_HORIZON_SECONDS` → `TABLE_DECODE_RUNWAY_SECONDS` and
rewrote the shared-scan comment block in `appendNearbyLedgerRows`. The original
patch no longer applies.

## New patch

`patches/0001-perf-world-tighten-rock-query-discs-drop-dead-decode-runway-rock-scan.patch`
(format-patch from scratch apply on `568d1358e`).

## Re-verify (quiet VM)

| Signal | Result |
|---|---|
| Offline disc microbench 4000×800 rocks | **217.5 → 23.84 ms (~9.12×)** |
| Focused tests | 25/26 pass; sole fail is pre-existing on bare master (`quiet Ceres combat list… got 53`) |
| Picture contract | unchanged |

Old patch kept under `patches/archive/` for history.
