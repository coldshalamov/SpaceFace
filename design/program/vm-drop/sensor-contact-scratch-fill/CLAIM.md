# CLAIM — sensor-contact-scratch-fill

Quiet `ai.stack` / `liveFramesFor` → `entityContacts` residual: ephemeral
observers allocated `{...base, confidence, threat, hostile}` per contact.
Fill retained per-member `contactRecords` instead (PerceptionMemory still
Object.assigns into durable records).

Primary KPI: portable microbench ≥1.5× (soft-GPU fps not claimed).
