**cpu-profile-flight-refresh:** Fresh quiet soft-GPU V8 CPU profile on master tip `35e519ebd` (2026-09-23 ~00:09–00:11 EDT). Settled held-thrust **60937 ms** (`--ms=60000`). Idle **57.3%** (was 60.4% @ `0612d2b9f`); long tasks **18**. Soft-GPU still owns `bufferData`/`isProgram`/residency uploads. Portable top self: `radar.draw` 398.9 ms, `classifyWorld` 189.3, `registry.step` 170.2, `queryFarActors` 111.6, `syncEntityViews` 69.2, `prepareFrame` 63.9, `hud.frame` 61.5. Report-only; no game code. Artifacts under `artifacts/settled-60s/`. See also hitch-hillclimb-fresh-20260923.
