# DONE — IMPORT_DIGEST (report job)

## Summary

Refresh vs master **`37f50a70d`** (unchanged after fetch). Quiet CPU re-rank cite:
`hillclimb-20260924b` settled-45s held-thrust (Picture ON, soft-GPU ignored).

**Shipped this pass:** none — no new ≥~1.5× portable cut clear after accounting for
pending digest rows.

**Scour misses (evidence):**
- Physics sleep residual / S1-idle Rapier expansion: JS kinematics stand-in **~1.41×**
  (under bar); richer capture+give+refresh stand-in **~0.97×** (sleep checks eat the
  gain). Current `mayRapierIslandSleep` only admits S2–S4, which `entityNeedsPhysics`
  already drops from dynamics — sleep rarely fires on live dynamicRecords.
- classifyWorld visit-loop cadence still under bar (prior).
- spatial-hash surface @600 not retried (hold).
- flight-propulsion-scratch hold (integrated miss).
- 11 live rocks still pinned — no legal cut.
- same-material hull *draw* batch not pursued (draw/GPU not the portable pole after
  pending imports; soft-GPU fps ignored).
- Admission residual without dummy prewarm: prior hitch-opening-admission miss stands.

**Still pending strong imports (import-first):** #31 optic, #32 emergent-hot-spatial
(~4×), #33 share-unchanged-ship-materials (~1.96×), #34 hud-credits-pulse-no-reflow
(hitch), #35 prune-evidence-cadence, #36 projectile-surface-distance-first,
#37 classify-closed-form-index, #38 classify-signature-prune-membership,
#39 registry-step-dispatch (~1.74–2.88×), plus #1 far-actor, #17 asteroid-query,
#13 prepare-pitch, #15 submit-scratch, #12 massline.

## Next poles

1. **Import portable pending** (#31–#39 + #1 + #17 + #13 + #15 + #12) — covers every
   top portable self row on the 45s re-rank.
2. Far-AI residual *after* #1 (tickFarActors / aiPorts contact base) — re-measure only
   post-import.
3. HUD leftover *after* #34 (hud.frame ~41 ms) — re-measure only post-import.
4. Presentation ledger / journal residual *after* #1+#17 shrink query cost inside
   appendNearbyLedgerRows.
5. Remaining registry owners after #39 (47 quiet Ceres names) — only with fresh
   post-import profile; no extra content-gate candidate cleared ≥1.5× this pass.
6. Soft-GPU fps is not a KPI.
