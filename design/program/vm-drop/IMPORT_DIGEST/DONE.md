# DONE — IMPORT_DIGEST (report job)

## Summary

Refresh vs master **`568d1358e`**. Quiet CPU re-rank cite from 2026-09-24b held-thrust; soft-GPU ignored.

**New this pass:** #35 `prune-evidence-cadence` (~11× aggregate / ~7× worst; overturns prior weak miss) + #36 `projectile-surface-distance-first` (~1.87×).

**Still pending strong imports:** #31 optic, #32 emergent-hot-spatial (~4×), #33 share-unchanged-ship-materials (~1.96×), #34 hud-credits-pulse-no-reflow (hitch), #35 prune-evidence-cadence, #36 projectile-surface-distance-first, #17 asteroid-query-callers (~9×), #1 far-actor-cell-key (~2×), #13 prepare-pitch (~2.3×), #15 submit-scratch, #12 massline.

**Holds:** flight-propulsion-scratch (integrated miss).

**Misses this pass:** spatial-hash surface sample @600 WU (regresses vs Map walk); registry.step / selectClassifyEntities closed-form still under bar without a stronger structural cut.

## Next poles

Import portable pending → `registry.step` / `selectClassifyEntities` residual (closed-form catch-up ≥1.5× or skip) → remaining 11 live rocks still pinned → same-material hull batch only if draw remains the pole. Soft-GPU fps is not a KPI.
