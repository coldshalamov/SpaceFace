# DONE — IMPORT_DIGEST (report job)

## Summary

Refresh vs master **`568d1358e`**. Prior radar/classify/HUD/threat/closure rows remain on master.

**New this pass:** #32 `emergent-hot-spatial` — when `emergent.hot`, radius probes use warm spatial hash; portable crowded microbench **~4.0×**; 5/5 emergent tests.

**Still pending strong imports:** #31 optic, #17 asteroid-query-callers (~9×), #1 far-actor-cell-key (~2×), #13 prepare-pitch (~2.3×), #15 submit-scratch, #12 massline, **#32 emergent-hot-spatial (~4×)**.

**Holds:** flight-propulsion-scratch (integrated miss).

## Next poles

Import portable pending → remaining 11 live rocks still pinned (8 geology / 2 collision / 1 activity; contracts block cut) → fewer program keys / share unchanged ship materials (no dummy prewarm) → optional quiet CPU profile re-rank. Soft-GPU fps is not a KPI.
