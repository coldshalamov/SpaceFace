# CLAIM — energy-quiet-relevant-skip

Quiet Grok Bot VM hillclimb package **#104**.

Pole: prepareFrame / vfx energy residual after #103
(`_energyPlumeRelevant` probe cost when already quiet-hidden).

After the #103 hide latch, consecutive idle ticks still paid full
`_energyPlumeRelevant` (player `_engineDriveFor` + trail-candidate drive
walk). Cheap maybe-awake while `_energyQuietHidden` clears **~4.3×**
median (floor ≥3.1× across rebenches). Soft-GPU fps not a KPI. Picture ON.
