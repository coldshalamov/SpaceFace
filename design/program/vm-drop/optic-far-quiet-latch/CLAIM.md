# CLAIM — optic-far-quiet-latch

Quiet `tickOpticFieldRocks` still paid `queryAsteroidField` over the authored
prefetch disc plus an entityList optic shelve scan every tick when no optic
interest was nearby. Quiet-latch after empty-interest probe; wake on
asteroidField.version / membership / player move / 0.5 s rescan.

Primary KPI: portable microbench ≥1.5× (soft-GPU fps not claimed).
