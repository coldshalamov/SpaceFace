# CLAIM — energy-quiet-hide-latch

Quiet Grok Bot VM hillclimb package **#103**.

Pole: prepareFrame / vfx energy residual after #102
(`_hideEnergyPlumes` / plasma+retro+fleet.reset on consecutive quiet ticks).

Quiet settled flight re-ran full plasma/retro/fleet.reset every idle frame
(and cleared the #100 family asleep latch). Latch after first hide clears
**~11.8×** median (floor ≥10.3×). Soft-GPU fps not a KPI. Picture ON.
