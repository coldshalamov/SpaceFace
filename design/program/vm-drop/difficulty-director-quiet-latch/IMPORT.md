# IMPORT — difficulty-director-quiet-latch (#162)

## What

Quiet-latch settled `difficultyDirector` per-tick pacing eval (scores / credit
trend / pin watch) when mults are at stance targets and damage books + flee
holds are empty (~3.0× median / ≥2.43× floor on bare master).

## Apply

```
git am --ignore-space-change design/program/vm-drop/difficulty-director-quiet-latch/patches/*.patch
```

Base: master tip `97c88f92b` (at package time).

## Claims

Portable registry.step residual after #161 — not soft-GPU fps. Fresh ungated
subsystem (not packCombat / stampNear / lifetime / classify / trust-sleep /
bandRadio / traffic / law / factionPresence / aiEncounter rediscovery).

## Owner-GPU

No headed verification required for this CPU latch. Picture contract ON
(latched = no pacing eval / pin tick / credit sample until wake or 0.5 s rescan).
