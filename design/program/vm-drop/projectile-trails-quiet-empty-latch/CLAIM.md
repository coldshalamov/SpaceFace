# CLAIM — projectile-trails-quiet-empty-latch

Quiet Grok Bot VM hillclimb post-#121. Package #122.

Pole: prepareFrame / VFX residual after #121 persistent-beams callsite.
Cut: quiet-latch empty projectile-trails relevant path (indexedTypeScan +
cache check + resetProjectileTrailDiag) after first empty observe; wake on
entityIndexVersion or `_projectileCacheDirty`.

Soft-GPU fps not a KPI. Picture contract ON.
