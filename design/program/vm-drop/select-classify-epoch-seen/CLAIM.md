# CLAIM — select-classify-epoch-seen

Quiet `selectClassifyEntities` cleared a `Set` of near-disc ids every
incremental classify. Dense `Uint32Array` id-epoch marks bump a generation
instead (presentationQueries pattern). Target: classifyWorld /
selectClassify residual after #37+#38+#45+#48.
