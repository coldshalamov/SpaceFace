# CLAIM — optic-field-resident

Lane A membership: optic lattices off quiet `entityList`.

- Owns: `src/world/asteroidField.js` (promote optic hull/radius, demote, tick),
  `src/systems/world.js` (`_ensureOpticStructures`, tick hook),
  `test/optic-field.test.mjs`
- Stay off: renderer, presentationRunner, dressing leftovers, farActorTable grid
