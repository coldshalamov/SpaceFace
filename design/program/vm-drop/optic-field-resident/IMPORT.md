# IMPORT — optic-field-resident

## What it is

Ceres Prism Gallery optic lattices stamp into `asteroidField` on FULL residency
(not `entityList`). `tickOpticFieldRocks` promotes inside the authored decode
disc and shelves past exit (far-actor membership shape). Quiet Ceres census:
entityList **96→54**, live rocks **53→11**. Portable walk microbench **~1.64×**.

## How to apply

```bash
git fetch origin
git checkout -B import/optic-field-resident origin/master
git am design/program/vm-drop/optic-field-resident/patches/*.patch
node --test test/optic-field.test.mjs test/asteroid-field.test.mjs \
  test/ceres-table-authority-census.test.mjs test/decode-runway-residency.test.mjs \
  test/far-actors.test.mjs
```

## Apply order

Clean on bare `origin/master` @ `568d1358e`. Independent of pending query /
far-actor / pitch packages. Complements table-authority Lane A.

## Picture

Untouched. Gallery still collides/prisms when the player enters the decode disc.
