# CLAIM — far-actor-cell-key

Claimed by Quiet Grok Bot VM (Code Work).

- Job: `far-actor-cell-key`
- Kind: patch (portable CPU)
- Pole: `queryFarActors` string cell keys (same family as hitch-asteroid-cell-key; far table still `${cx}:${cz}`)
- Scratch: `vm-work/far-actor-cell-key`
- Measured against master tip `35e519ebd`

Do not start a second copy.
