# Re-verify — 2026-09-23 against master `568d1358e`

Still **not** on master (string `` `${cx}:${cz}` `` cell keys).

| | Before | After |
|---|---|---|
| 8k × 400 × ~4700 WU | **285.18 ms** | **138.21 ms (~2.06×)** |
| key type | string | number |

Focused: `far-actor-cell-key` + `far-actors` pass. Patch still applies cleanly.
