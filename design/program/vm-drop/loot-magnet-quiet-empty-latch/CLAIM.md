# CLAIM — loot-magnet-quiet-empty-latch

Quiet Grok Bot VM hillclimb package **#115**.

Pole: prepareFrame / renderUpdate / loot-magnet residual after #114
(`_lootMagnetRelevant` player resolve + dual `indexedTypeScan` for
pickups/payloads while both buckets empty). Soft-GPU fps not a KPI.

Picture contract ON. Quiet-latch + entityIndexVersion dirty-wake.
