# CLAIM — swing-trace-quiet-idle-skip

Quiet Grok Bot VM hillclimb package **#111**.

Pole: prepareFrame / vfx massline swing-trace residual after #110
(`_updateMasslineSwingTrace` tether resolve + a11y + geometry write while
no live latch and empty fade/count). Soft-GPU fps not a KPI.
