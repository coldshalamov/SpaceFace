# Wreck-piece textures — DONE

RESULT: **DELIVERED**. Seven untextured aftermath sources rebuilt with real PBR
materials (LOD0/1/2, role-merged meshes) into this outbox folder. Names:
`wreck_ore_freighter_hopper`, `deb_ore_freighter_hopper_lid`, `wreck_liner_bow`,
`wreck_liner_boatbay`, `deb_liner_hull_panel`, `aft_armor_slab`, `frag_grating_sheet`.
Blender 4.5.14 LTS; maps under `maps/`; one chase still each under `stills/`;
sizes and SHA-256 in `IMPORT.md` / `build-report.json`. Incubator pack and
manifests untouched. No PR; outbox only on `vm-drop`.

