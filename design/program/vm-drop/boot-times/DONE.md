Cold boot → first control (firstPlayableFrameAt via New Game → Launch) finished on the idle Grok Bot VM against master `0fd64234a71147fd16f47f7a5f988d30fe758e3f`. Three cold runs (fresh Chromium context each): **22.64 s**, **21.69 s**, **20.97 s** wall. Longest stage every run: **`loading:entering-flight` → `first-playable`** (~7.6–8.2 s). Env: **software GPU** (ANGLE SwiftShader / Subzero), 8-core Xeon guest, headless Playwright. Full stage tables in `raw-runs.json` / `report.md`. Report only — live loader untouched. No blockers.
