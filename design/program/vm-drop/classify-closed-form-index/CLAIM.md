# CLAIM — classify-closed-form-index

Lane: selectClassifyEntities / classifyWorld closed-form catch-up residual.

- Owns: `src/core/coreSystem.js` (`closedFormMovers` entity-index lane) +
  `src/world/activityRuntime.js` (`selectClassifyEntities` catch-up walk) +
  focused activity-runtime test
- Stay off: spatial-hash surface sample @600 WU, flight-propulsion-scratch,
  dummy prewarm, picture defaults, pinFacts rebuild (separate package)
