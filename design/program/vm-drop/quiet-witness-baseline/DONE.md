**quiet-witness-baseline (partial):** three `npm run probe:runtime-witness` runs and `npm run check:baseline` on idle soft-GPU VM against master `3ecf3de72608008a54d544c58a35b70b1a574e90` — all blocked by missing `src/data/emergentPrimitives.js` (import in `weapons.js` since `01cf5d26d`). Headline: witness **EXIT 1** ×3 (`TimeoutError` 90s + http.404 emergentPrimitives); phase table / fps / drawCalls / long-tasks / cold first-draw **n/a** (not invented); baseline **4/16 green**, EXIT 1; GPU tier **software** (SwiftShader Subzero via standalone WebGL / `detectGpu` SOFTWARE_RE — in-game detectGpu never reached). Artifacts under `design/program/vm-drop/quiet-witness-baseline/`. Master break recorded honestly; no game code fixed in this report job.
