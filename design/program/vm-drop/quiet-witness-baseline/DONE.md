**quiet-witness-baseline (complete):** three `npm run probe:runtime-witness` runs + `npm run check:baseline` on idle soft-GPU VM against master `0612d2b9fc994557dd35cb00d0df21b791722c23` (emergentPrimitives.js present). Headline: run1 hitching / run2–3 presenting; **drawCalls 103/103/114**; **long-tasks 60/53/58**; sim ~2.9/4.9 ms avg/p95; GPU-resource wall **prepareOpeningGpuResources 14.9/15.2/8.2 s**; cold **firstPresentAdmission 1110 ms** (run1); cold **drawPreparedFrame 225/11/122 ms**; GPU tier **software** (ANGLE Mesa llvmpipe — soft-GPU, not SwiftShader this pass). Baseline **12/16 green**, EXIT 1 (save-schema + sim/sim-compare/sim-v3 hash drifts already on master; expected JSON untouched). Artifacts under `design/program/vm-drop/quiet-witness-baseline/`. Completes the prior empty report on this same folder.
