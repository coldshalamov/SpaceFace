# CLAIM — registry-step-dispatch

Lane: registry.step dispatcher residual (quiet idle combat-island owners).

- Owns: `src/core/catchupPolicy.js` (content wake mask + scratch queue),
  `src/runtime/authoritativeSystemManifest.js` (CONTENT_GATE + calendar moves),
  `src/core/registry.js` (indexed update loops), `test/sim-clock-catchup.test.mjs`
- Stay off: flight-propulsion-scratch, dummy prewarm, picture defaults, soft-GPU fps KPI,
  visit-loop classify cadence, spatial-hash surface @600 WU
