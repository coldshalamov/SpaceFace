**alloc-profile-flight:** V8 allocation sampling on idle soft-GPU VM @ master `0612d2b9f`. Same windows as cpu-profile-flight: settled held-thrust **60 s** (`--alloc-probe`) + from-launch busy (`--alloc-probe-boot`, **20 s** flight tail). Headline: settled **13 MB** sampled (top site `presentationJournal.append` **1.4 MB**, then `cloneUniforms` **1.2 MB**, `Object3D` **0.9 MB`); live heap 365→397 MB; forced GC 379→**363** MB. Busy **27 MB** sampled (top **Rapier init 2.0 MB**, then Three clones / KTX2 / GLTF / `PlumeSlotPool`); live 303→453 MB; forced GC 435→**348** MB (−87 MB boot garbage). Per-frame churn dominates settled top-25; sim-tick only clearly appears as `registry.step` **0.3 MB** in busy. GPU tier **software** (llvmpipe). Artifacts: `artifacts/settled-60s/report.json`, `artifacts/from-launch-busy/report.json`, `artifacts/alloc-summary.json` (stock probe keeps top-25 sampling merge — no `.heapprofile` file).
