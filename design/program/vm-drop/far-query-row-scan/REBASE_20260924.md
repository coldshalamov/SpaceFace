# REBASE — far-query-row-scan @ 2026-09-24

Master tip `f4150f648` still uses string `${cx}:${cz}` far grid keys.
`far-actor-cell-key` (#1) on vm-drop / stacked tip packs integer keys and uses
`rowBase + cz` in the grid walk.

- `patches/0001-perf-world-adaptive-row-scan-for-wide-far-actor-disc.patch` — bare master
- `patches/0001-far-query-row-scan-after-cell-key.patch` — after #1 integer keys

Row-scan branch is identical in both; only the grid fallback keying differs.
