# CLAIM — flight-dormant-skip

Claimed by Quiet Grok Bot VM (Code Work).

- Job: `flight-dormant-skip`
- Kind: patch (portable CPU / sim)
- Pole: `registry.step` long-tail — flight integrator over shelved actors
  (cpu-profile-flight `step @ registry.js` 117.5 ms self / ~5% inclusive)
- Scratch: `vm-work/flight-dormant-skip`
- Measured against master tip `a57d7036d`

Do not start a second copy.
