**cpu-profile-flight:** V8 CPU profiles on idle soft-GPU VM @ master `0612d2b9f`. Settled held-thrust **60920 ms** (`--ms=60000`) + busiest from-launch **50629 ms** (`--from-launch --flight-ms=20000`). Headline: settled **60.4% idle**; busy owner `presentationRunner.frame` **19.7%** inclusive → bloom/`drawPreparedFrame` (soft-GPU); mid-flight admission still pollutes via `isProgram`/`checkProgramsReady` ~6%; portable hot loops **radar.draw**, **queryAsteroidField**, **classifyWorld**, **syncEntityViews**/`prepareFrame`, sim `stepSimulation` ~5% inclusive. From-launch dominated by soft-GPU `isProgram` across New Game + authored-visuals + entering-flight (Launch→flight 18.8 s). Artifacts: `artifacts/settled-60s/profile.cpuprofile`, `artifacts/from-launch-busy/profile.cpuprofile`, top-40 tables in `report.md`. GPU tier **software** (llvmpipe).
