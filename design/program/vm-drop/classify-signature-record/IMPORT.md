# IMPORT — classify-signature-record

Independent. Prefer after #37 `classify-closed-form-index` + #38
`classify-signature-prune-membership` so the residual this cuts is the
per-visit template-string stamp signature left once prune/catch-up are gone.
