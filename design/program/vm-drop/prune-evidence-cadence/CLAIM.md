# CLAIM — prune-evidence-cadence

Lane: stunt evidence journal prune cadence on the grammar hot path.

- Owns: `src/combat/stuntEvidence.js` (`pruneEvidence`, `PRUNE_EVIDENCE_CADENCE`) + focused test
- Stay off: closeFieldIntervals semantics, flight-propulsion-scratch, dummy prewarm, picture defaults
