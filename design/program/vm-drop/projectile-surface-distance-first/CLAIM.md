# CLAIM — projectile-surface-distance-first

Lane: projectile evidence surfaceHistory walk — range before materialSurface.

- Owns: `src/combat/stuntProjectileEvidence.js` (`sampleProjectileEvidence` surface loop) + focused test
- Stay off: materialSurface cache (prior miss), spatial-hash 600 WU (regresses at this radius), picture/prewarm
