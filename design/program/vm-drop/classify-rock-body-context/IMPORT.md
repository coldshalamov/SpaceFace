# IMPORT — classify-rock-body-context

Independent. Prefer after #45 `classify-signature-record` so the residual this
cuts is visit-body context assembly left once stamp-signature alloc is gone.
Does not rehash stamp-reuse / inert / visit-loop cadence (prior under-bar).
