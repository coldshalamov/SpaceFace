# CLAIM — share-unchanged-ship-materials

Lane: flight-template material share for new-ship hitch (PERF backlog #6).

- Owns: `src/render/partsLibrary.js` (`cloneFlightInstanceMaterials` + flight-template probes/bench)
- Stay off: dummy prewarm, bloom/shadows/picture defaults, geology rock membership, flight-propulsion-scratch
