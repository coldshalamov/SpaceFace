# IMPORT — composition-threat-prefilter

Independent. Prefer after #46 `snapshot-fence-yaw-quat-cache` so the residual
this cuts is the chase composition / framing hostility walk left once fence
pack yaw work is gone. Stacks under prepareFrame leftovers after #13+#44+#46.
