# Performance Modernization Progress

Read this file first after every compaction. Append one line after each packet commit; do not rediscover completed packets.

- 2026-07-28 | PERF-00–04 | DONE through `75238d15` | equivalence, lifecycle, scheduler seam, offline compiler, and dense PresentationWorld committed | next: PERF-05
- 2026-07-28 | PERF-05 / PQ-039 | DONE at `bfb189b4` | deterministic batched NPC hostile queries committed and pushed; broker acceptance pending | next: PERF-06
- 2026-07-28 | PERF-06 / PQ-040 | DONE at `8db096b7` | scene-owned dirty GPU ranges for combat sprites and trail streaks committed and pushed; broker acceptance pending | next: PERF-07
- 2026-07-28 | PERF-07 / PQ-041 | DONE at `03e85514` | Electron 43.2 runtime, hardened shell, explicit provisioning, and production package allowlist committed and pushed; native broker/exact packaged-startup acceptance pending | next: PERF-08
- 2026-07-28 | PERF-C01 | DONE at `97dbdf01` | HLOD canopy/shadow policy now refreshes on LOD or hierarchy transitions instead of recursively traversing unchanged visible roots every frame; focused render tests, bundle gate, and baseline 10/10 green; no live speed claim | next: rank offline continuation packets while PERF-08 awaits clean broker evidence
- 2026-07-28 | PERF-C02 | DONE at `d64157e8` | render-only craft pitch now scans the maintained `entityIndex.shipLike` domain instead of every authoritative entity while preserving offscreen/re-entry continuity and exact cues; focused 10/10 tests, bundle gate, and baseline 10/10 green; no live speed claim | next: rank the next evidence-independent frame-work packet
- 2026-07-28 | PERF-C03 | DONE at `79f70038` | initialized field geometry now sleeps without uploads when inactive, reuses retained Set/Color/Quaternion scratch, and publishes only instance families written by the active field kind; focused field/VFX 42/42, bundle gate, and baseline 10/10 green; no live speed claim | next: integrate the active-shadow prepass packet and ranked parallel continuation work
- 2026-07-28 | PERF-C04 | DONE at `44adb826` (docs `cc2196d0`) | shadow follow, key-light/target matrix preparation, and the asteroid shadow-camera union now gate on the renderer's actual `shadowMap.enabled` rather than the user setting, so a zero-receiver frame does no shadow-camera work and the light/target world matrices update exactly once before `shadow.updateMatrices`; renderer/shadow 67/67, bundle, baseline 10/10 | next: HUD roster and projection packets
- 2026-07-28 | PERF-C05 | DONE at `1476fb13` | the contacts roster hashed `Math.round(distance)` into its rebuild guard, so in flight the guard never fired and `innerHTML` teardown plus a full row rebuild ran ~5 Hz whenever a contact was in range; now reconciled by entity id with per-field writes. Review diffed a canonical serialization of the rendered strip across 5 states: 354 lines each, byte-identical | next: projection guard
- 2026-07-28 | PERF-C06 | DONE at `9da33ed5` | `worldToScreen` refreshed `camera.matrixWorldInverse` per call (a decompose plus a 4x4 invert) for the ~7 points the flight HUD projects each frame; now guarded on a self-validating decomposed-transform cache (position/quaternion/scale + parent + pivot + `matrixWorldNeedsUpdate` + both auto-update modes) with an optional out-param. Deliberately NOT a frame-lifecycle flag: `worldToScreen` is called from input paths outside the render frame | next: boot backdrop
- 2026-07-28 | PERF-C07 | DONE at `c2f9eeb6` | `SpaceBackground` tiered itself in its constructor from an absent `state.render.gpu`, guessed `mid`, and the renderer re-tiered it afterwards, so every machine whose true tier is not `mid` built the whole procedural backdrop twice at boot and discarded the first. Two lines hoisted. NOT byte-identical on software GPUs, where planets go 512 to 256 — and 256 is the authored low-tier value, so this restores authored behavior a stale `planetCache` was suppressing | next: audio churn
- 2026-07-28 | PERF-C08 | DONE at `b345d195` | `_applySettings` rewrote all seven bus gains every rendered frame and the brake/tether beds re-rammed unchanged targets: 28 AudioParam writes plus 7 `cancelScheduledValues` per idle frame, of which 26 are now eliminated. The bus gate stands down only after the 50 ms linear glide closes, because a linear ramp re-anchors on `param.value` and is not memoryless, unlike `setTargetAtTime` | next: gate registration
- 2026-07-28 | PERF-C04a | DONE at `54304d5c` + `3fc2cc29` | all eight packet tests existed but were referenced by no npm script, so every guard ran only by hand; added `check:perf-packets` and chained it into the CI matrix (confirmed reachable, not among the 270 orphans). Also rewrote the shadow test, whose zero-receiver assertion read a hardcoded harness field neither function under test touches — `assert.equal(true, true)`. It now derives `shadowMap.enabled` through the real `_syncShadowMapEnabled` and covers the enable/disable/enable stale-follow transition; removing the production gate fails 3 of 4 | next: PERF-C09 post-process clears, PERF-C10 thruster buffer usage
- 2026-07-28 | PERF-C04 | DONE at `44adb826` | shadow follow, key-light/target matrix preparation, and the asteroid shadow-camera union are gated on the renderer's actual `shadowMap.enabled` rather than the user setting, so a zero-receiver frame does no shadow-camera work, and the light/target world matrices now update exactly once before `shadow.updateMatrices`; focused renderer/shadow 67/67, bundle gate, and baseline 10/10 green; no live speed claim | next: HUD presenter scheduling (DOM-only presenters currently step up to 4x per rendered frame) and per-frame screen-projection allocation
- 2026-07-29 | PERF-I01 (deterministic instrumentation, Phase 1 + Phase 2 headline) | DONE | §3 inventory written (`PERF_INSTRUMENTATION_INVENTORY.md`); Tier-1 counter substrate `src/core/perfCounters.js` + WebGL context instrumentation `src/render/glInstrumentation.js`, both OFF by default and install-on-enable; `check:perf-counters` added and chained into `check` ahead of `check:gate-reachability` (absent from the 270-orphan list, i.e. reachable); 12/12 focused tests, all three load-bearing guards mutation-proven (removing each fails exactly its own test, restore clean). **Headline measurement, `scripts/probe-shader-compile-timeline.mjs`: post-boot shader compiles in idle flight = 0 over 301 frames, reproduced twice. Under scripted stimulus: 10 and 4 across two runs — variance that is itself the argument for the deterministic scenario harness the brief specifies in §5.** `gl.linkProgram` capture attributes them to two distinct classes: a DRAW-TIME cache miss inside `WebGLRenderer.render` -> `renderBufferDirect` -> `setProgram` (a sprite program, byte-identical cacheKey to one already compiled at boot -> program cache thrash), and `prepareMaterial` under `traverse`, i.e. `precompilePipelines` running after the boot boundary (canopy). Captured headless on SwiftShader; counts are Tier-1 valid, no timing claimed | next: Phase 3 volume counters + the deterministic scenario harness on the existing `deps.requestFrame` scheduler seam
- 2026-07-29 | PERF-I02 (instrumentation seam wired + cross-validated) | DONE | GL seam installs at `renderer.js` renderer construction (before anything renders, so the boot ramp is captured); frame boundary in `presentationRunner.frame()`; counters exposed as `perfRuntime.tier1` on the un-debug-gated `__SPACEFACE_PERF__` handle for browser/Electron parity. **Cross-validated end to end on a real WebGL2 context: production seam 91 shader links / 3 post-boot, independent page-level wrapper 91 / 3 — exact agreement.** Fixed a defect that would have made every production count wrong in the direction of good news: `totals` accumulated in `endFrame()`, so anything recorded outside a frame pair was wiped by `beginFrame()` — and the live capture shows **70 of 91 links (77%) are off-frame** (the boot ramp links before the first presentation frame; `compileAsync` resolves between frames). Totals now accumulate at record time; `offFrame` is reported separately because an off-frame link is an async compile while an in-frame link is a draw-time stall. Added `UNSOURCED_FIELDS`: family H (DOM/layout) is in the schema with no producer wired, and its `0` must not be read as healthy. Post-boot over 616 frames: renderTargetAllocations 0, renderTargetResizes 0 (both zero-budgets met); bufferPartial:bufferFull = 32587:105, so `dynamicBufferRanges` is doing its job; ~37 draw calls/frame, consistent with the earlier falsification of the draw-call theory. Gates: baseline 10/10, sim/replay/sim:compare/render-hotpath/bundle/src-reachability/gate-reachability all exit 0, perf-packets 39/39, perf-counters 17/17, check:shader-compile green with instrumentation OFF. Five mutation proofs total | next: family H producer, then the §5 deterministic scenario harness on `deps.requestFrame`
