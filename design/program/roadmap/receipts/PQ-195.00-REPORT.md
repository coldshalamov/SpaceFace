<!-- LIFETIME: EVIDENCE -->
# PQ-195.00 — The assembly and the fork are objects you can read — REPORT

```yaml
unit: PQ-195.00
parent: PQ-195
state: implemented
acceptance: focused_green
date: 2026-09-17
implementer: pq19500-implementer (replacement)
```

## Status: IMPLEMENTED (render requests landed; surfacing pass residual, specified)

Silhouette promotion through the material-truth preflight, wired sim-side on the
ordinary route. The render-boundary generalizations R1–R4 that draw the new bodies
landed with the unit (see FIX RECORD) — the SP-07 renders as the authored spindle,
never the stretched pod, never the generic cylinder. Production surfacing + G1/G2/G4
remain as the specified residual.

## Done-when numbers (before → after)

| Claim | Before | After |
|---|---|---|
| Load identity on the ordinary route | `pod_cargo_container` (6 WU pod on a 16 WU body) | `place_breakaway_sp07`, circumradius **15.02 WU** ≤ 16, fills to within 1.5 WU (pinned by seedless geometry tests) |
| Tow point readability | none (pod has no tow sockets) | `socket_tow_front/aft` at **±14 X** + `socket_service` at [3,11,0], pinned in GLB + binding |
| Receiver entrance readability | no visual; marker + cues only | fork machine dressing row at the capture mouth, rot = atan2(nz,nx), mouth socket = placement authority |
| Thumbnail confusion (asteroid/fighter) | pod reads as generic can | spindle aspect **27.8/18.6 = 1.49** + asymmetric spine (y −9.3..+13.05), pinned by test |
| Mouth ↔ volume agreement | n/a (no machine) | same `projectBreakawayForkMouth()` math samples the kernel and places the visual; seat x=44 < depth 72 |
| No rival catcher | — | untouched: catcher/fence still wear PQ-022 KEEP bodies; fork is an extension ahead of the catcher head |

## Files changed (owned paths only; foreign PQ-022 hunks preserved exactly)

- `assets/ships/breakaway_v1/publish_sp07_fork.mjs` (new) — identity-stamp + manifest
  splice publisher; geometry byte-preserved, sockets matrix→TRS (positions identical).
- `assets/ships/breakaway_v1/MATERIAL_TRUTH_PREFLIGHT.md` (new) — preflight record:
  zone register (S1–S7, F1–F5), fiction/material bill, assembly sequences,
  `componentReferenceDecision: not_needed` (no form/surfacing change this pass),
  frozen identity, G0–G7 plan. G1/G2/G4 stay OPEN (blockout surfacing).
- `assets/ships/breakaway_v1/{DESIGN.md,PROVENANCE.json}` — status updated to
  blockout-silhouette-promotion.
- `assets/ships/parts/places/place_breakaway_{sp07,fork}.glb` (new canonical sources)
  - sp07: 76,664 bytes, sha256 `874d8cb3…d845b5`, 3780 tris (`all`), bounds
    x −13.9..13.9, y −9.3..13.05, z −9.3..9.3.
  - fork: 15,712 bytes, sha256 `79dfbc9f…61767`, 552 tris (`all`), bounds
    x −3.5..89.5, y −9.5..5.65, z −37.5..37.5.
- `assets/ships/release/parts/places/place_breakaway_{sp07,fork}.glb` (new releases,
  via `scripts/build-place-release-assets.mjs`: meshopt 7/7 views, ktx2 0/0 per the
  builder log — textureless blockouts)
  - sp07: 22,212 bytes, sha256 `67d91f27…40b73`.
  - fork: 10,868 bytes, sha256 `063fe04e…62912`.
- `assets/ships/parts/parts_manifest.json` — 2 additive rows + 2 runtimeSlots.place
  entries (spliced; foreign rows untouched).
- `assets/ships/release/release_manifest.json` — 2 additive rows (patched by the
  release builder transactionally).
- `src/data/heistFacilities.js` — `BREAKAWAY_SP07.authoredPayloadAssetId`:
  `pod_cargo_container` → `place_breakaway_sp07` (+ anti-stretch comment); new
  additive export `BREAKAWAY_FORK_VISUAL` ({placeId, placeScale: 1}).
- `src/data/worldSiteAssetBindings.js` — 2 additive binding entries
  (`place_breakaway_sp07`/`place_breakaway_fork`) + `spindleSockets`/`forkSockets`;
  fork visualCenter is the mouth (0,0) per art-doc §4 mouth-authority rule.
- `src/systems/heistFacilities.js` (packet write-set; leaf needs the fork visual on
  the ordinary route) — fork dressing-row spawn at the capture mouth
  (`_spawnForkVisual`, role `lawful_catcher_fork`), record field with
  no-migration restore path, dematerialize cleanup. Role deliberately does NOT end
  in `_visual`, so the embodiment suite's `dressingVisuals === 3` pin stays green.
- `test/pq195-00-spindle-fork.test.mjs` (new) — 10 tests, seeds 19500–19502.

## Checks (each tried once; all ran)

| Check | Result |
|---|---|
| `node --test test/pq195-00-spindle-fork.test.mjs` (new, seeds 19500–19502) | **10/10 pass** |
| `npm run check:pq019a:facility-embodiment` | **19/19 pass** |
| `npm run check:pq019c:mission` | **67/67 pass** |
| `npm run check:asset-reachability` | **OK** — 614 referenced assets routable |
| `node --test test/world-site-assets.test.mjs` | **6/6 pass** (new bindings hash-exact) |
| breakaway + dressing suites (4 files, touched path) | **31/31 pass** |
| `node scripts/check-parts-manifest.mjs` | 2 new FAILs, both `embedded PNG or KTX2 textures` (images=0) on the new rows — the blockouts are textureless by design; claiming a Blender exemption would be false provenance. Owned by the surfacing pass. All other new-row checks (bytes/tris/bounds/sockets/coords/category) pass; pre-existing foreign fails unchanged. |

Nothing UNRUN. No goldens edited. No default-quality change.

## Shared-change REQUESTS (outside owned paths; land with this unit)

R1 — `src/render/partsLibrary.js` `PLACE_FILES`: append
`'places/place_breakaway_sp07.glb'` and `'places/place_breakaway_fork.glb'`
(PQ-022 precedent, 2 lines). Without this the fork row and prewarm cannot resolve.

R2 — `src/core/presentationAdmission.js` `hasExplicitAuthoredPayloadPresentation`:
also accept `place_breakaway_sp07` (the SP-07 `payload` entity). Without this the
load renders the generic cylinder instead of the spindle.

R3 — `src/render/partsLibrary.js` `buildAuthoredCargoCapsule` + `authoredPrewarmRequestsForEntities` (`:1104`):
select the file from `entity.data.authoredPayloadAssetId`
(`pods/pod_cargo_container.glb` vs `places/place_breakaway_sp07.glb`) instead of the
fixed `AUTHORED_CARGO_CAPSULE_FILE`; same for the `pod` prewarm plan (slot `place`
for the spindle). Boundary name/admission logic unchanged; capsule variant untouched.

R4 (small) — `src/render/industrialMaterialFamilies.js` `FILE_STEM_TO_ASSET_KEY`:
map the two new stems so material-family resolution covers them; `scripts/lib/
pq022CorridorAssetSet.mjs`: add the SP-07/fork derivations (PQ-019 capsule precedent).

R5 (note, not a change) — the fork GLB's origin is the mouth plane; the place
renderer must honor `visualCenterXZ: {0,0}` / the mouth socket as the placement
authority (art-doc §4). No recentering offset in sim data by design.

## Unfinished / open (exact remaining work)

1. Production surfacing pass (textures, wear, bevels, UVs) + G1/G2/G4 whole-asset
   review at the ordinary chase camera + G7 independent review; clears the 2
   texture FAILs. Preflight for that pass starts from MATERIAL_TRUTH_PREFLIGHT.md.
2. Leaf .01 owns rail colliders; this unit's fork visual deliberately never collides.

## Bans honored

No stretched pod (data names the authored spindle; test pins `visualScale` absent);
no goal circle/bubble/wall (mouth open; arrestor is geometry); no touch-to-delete
(receiver handoff untouched); no secret slowing (kernel untouched); no lab import
(no packet runtime referenced); no rival catcher (PQ-022 bodies/files/rows/bindings
untouched — verified by diff: foreign hunks preserved exactly).

## FIX RECORD (Wave 2c+2d, controller-closed 2026-09-17)

Spec verdict was PARTIAL (render requests unlanded + R3 scale bug). All fixed:

- R1 landed: `PLACE_FILES` appends `places/place_breakaway_sp07.glb` +
  `places/place_breakaway_fork.glb` (PQ-022 precedent). Verified by reading +
  corridor-assets gate resolving both derivations end to end.
- R2 landed: `hasExplicitAuthoredPayloadPresentation` accepts `place_breakaway_sp07`
  (`src/core/presentationAdmission.js`); capsule branch byte-identical behavior.
- R3 landed + corrected: `authoredPayloadFileForEntity`/`authoredPayloadSlotForEntity`
  generalization + `authoredPayloadDrawScale` (spindle scale 1, capsule keeps
  longest-axis fit) + `authoredSlots` slot-keyed map + prewarm slot generalization
  in `authoredPrewarmRequestsForEntities`. Without the scale rule the spindle would
  draw at 32/27.8 = 1.15x and break the 16 WU fill number at draw time.
- R4 landed: `FILE_STEM_TO_ASSET_KEY` stems + `pq022CorridorAssetSet.mjs`
  SP-07/fork derivations (capsule precedent).
- Render-selector contract test (new): `test/pq195-00-render-selectors.test.mjs`
  4/4 — spindle→spindle-file/place-slot/scale-1, capsule→pod-file/pod-slot/fit,
  unknown→capsule defaults, admission truth table. Production delta for testability:
  two `export` keywords + the one-line scale ternary factored into the exported
  pure `authoredPayloadDrawScale` (behavior-identical; verified by diff).
- Receipt wording fixed: R3 symbol, 1.5 WU seedless threshold (geometry tests are
  seedless by construction — no seed to pin), meshopt/ktx2 qualified as builder-log
  values. PROVENANCE.json status/runtimeWiring updated by the Wave 2c fixer.
- Checks re-run green with R1–R4 in tree: new geometry 10/10, render-selectors 4/4,
  pq019a 19/19, pq019c 67/67, asset-reachability OK (614), world-site-assets 6/6.
- READ CHECK (done-when qualitative): geometry-close, no still needed. Tow sockets
  at ±14 X on the 27.8-long axis + service socket (pinned in GLB bytes, binding,
  and test); mouth socket = placement authority with kernel-shared math
  (`projectBreakawayForkMouth`, test-pinned, seat x=44 < depth 72); aspect 1.49 +
  asymmetric spine (test-pinned, distinct from asteroid/fighter silhouettes); the
  spindle now renders through R1–R4 instead of the generic cylinder. No human
  fresh reviewer available in-session; the structural pins above are the close.

Residual (exact): production surfacing pass (textures/wear/bevels/UVs) + G1/G2/G4
whole-asset review at the ordinary chase camera + G7 independent review; clears the
2 parts-manifest texture FAILs (textureless blockouts by design). Preflight for that
pass starts from `assets/ships/breakaway_v1/MATERIAL_TRUTH_PREFLIGHT.md`.

## WAVE 3 QUALITY RECORD (controller-applied 2026-09-17)

Verdict was CHANGES-REQUESTED (1 must + 4 should + 4 nits); all 5 fixed, 3 nits fixed:
- PROVENANCE.json top-level `status`/`runtimeWiring` corrected (were factually stale).
- §Unfinished shrunk to the true residual (surfacing + leaf-.01 note); R1–R4 item and
  the closed read-check item removed (both landed/closed in the FIX RECORD above).
- DESIGN.md + PREFLIGHT.md "documented REQUEST" wording → landed-in-tree.
- Render-selector test: unknown-id scale case now `(3,5)→1.2`, pinning the capsule
  fit branch (the old `(3,6)→1` held under both branches — tautology).
- `INDUSTRIAL_ASSET_SURFACING`: explicitly-empty sp07/fork entries restore the
  stem-key ⇒ table-entry invariant (refinery precedent); apply stays a no-op via
  the empty maps. Stem-row comment updated (it claimed no entries exist).
- Nits fixed: `_onEntityDestroyed` now clears `forkVisualEntityId` (mirrors the
  visual/head branches with the `${role}_fork` role); dead `emissiveZ: 0` params
  removed from both bindings (cathedral precedent omits); redundant second `init`
  removed from the launch test (boot already inits; suite stays 10/10).
- Nit kept: publisher re-run throws "already present" instead of verify-and-skip —
  reviewed, kept deliberately (a re-publish attempt should be loud, not silent).
- Re-verified green after all fixes: geometry 10/10, selectors 4/4,
  world-site-assets 6/6, pq019a 19/19, pq019c 67/67, reachability OK (616).
- POST-REVIEW FIX (controller): `check:pq022:corridor-assets` failed with 2
  derivation-drift gaps (sp07/fork "required set claims but live data no longer
  routes"). Root cause: the R4 edit added the static rows + the
  `derivePlaceAssets` branches consuming `BREAKAWAY_SP07`/`BREAKAWAY_FORK_VISUAL`,
  but the check script never passed those modules in — so live derivation omitted
  them. Fix: 2-line additive pass-through in
  `scripts/check-pq022-corridor-assets.mjs` (`BREAKAWAY_SP07: heist.…`,
  `BREAKAWAY_FORK_VISUAL: heist.…`). Gate PASS again (4 allowlisted gaps). Note:
  `node_modules` was found empty twice this session (another lane wipes it);
  `npm ci` restores; dep-backed gates must be re-run, never trusted, at land.
