<!-- LIFETIME: RECEIPT -->
# PQ-164.00 — Every screen on a pad

```text
NOT DONE  PQ-164.00 — a live-manifest pad walker exists but has not been run.
                      The headed Chromium walk is serialized behind PQ-194.01 GPU.

WHAT I FOUND     Gamepad UI verbs already exist. There was no check that walked
                 every registered screen plus the station.

WHAT I CHANGED   A synthetic-pad walker was written. It was not executed. The
                 npm script is wired. No production input code was changed.

WHAT YOU WILL FEEL   Nothing new in the hands yet. The pad walk has not been
                 proven on the live screens.

THE NUMBERS      bar | before | after | target | seed
                 check:gamepad:screens | missing | script present, not run | green | 16400
                 screens pad=yes | unmeasured | unmeasured | all listed | 16400
                 npm script | absent | present | present | —
                 settings-profile | OK | OK twice | green | —
                 controls-discoverability | OK | OK twice | green | —

UNPROVEN         Every listed screen pad=yes on a live Chromium walk. The walker
                 launches a browser and was not run while the UI-stage capture
                 holds the GPU.
```

## Node pin (164.00b, no Chromium)

Controller salvage after Clinepass weekly cap. `node --test test/pq-164-00-gamepad-screens.test.mjs` drives shipped `createGamepad` + `createUiInput`.

| Check | Run 1 | Run 2 |
|---|---|---|
| A/Cross → accept, B/Circle → cancel | pass | pass |
| SCREEN_MODULES ≥ 20; shared modal pad route | pass | pass |
| accept confirms on every registered screen | pass | pass |
| cancel pops; locked gameOver traps B | pass | pass |
| dpad emits ui:navigate | pass | pass |
| `check:settings-profile` | OK | OK |
| `check:controls-discoverability` | OK | OK |
| `check:gamepad:screens` (Chromium) | not run | not run |


## Files

- `scripts/check-gamepad-screens.mjs` (new)
- `package.json` — `check:gamepad:screens` only (foreign `three.quarks` line preserved)
- this receipt
