<!-- LIFETIME: RECEIPT -->
# PQ-164.00 — Every screen on a pad

```text
DONE  PQ-164.00 — every registered screen and the station take D-pad, A and B
                  on seed 16400 (26/26 twice).

WHAT I FOUND     Pad verbs already existed, but kit word lists hid extra
                 buttons from the D-pad (only the Tab-stop counted), the walk
                 waited on a 3D boot that never showed, and Drill popped itself
                 unless a rock was pending.

WHAT I CHANGED   The pad now treats those extra buttons as reachable, the same
                 way arrow keys already did. A screen walk drives A / B / D-pad
                 on the live catalog in a real document, without booting the
                 3D picture. Drill stays open on a pending rock so Leave is a
                 real pad target.

WHAT YOU WILL FEEL   On a pad, Settings, Pause, the title and the station rail
                 move between every word, not just the first. B still backs out
                 (locked death/crucible sheets stay put, as designed). The HUD
                 is unchanged.

THE NUMBERS      bar | before | after | target | seed
                 screens pad=yes | walk timed out on window.SF | 26/26 twice | every listed | 16400
                 kit words pad-reachable | 1 Tab-stop | 2/2 D-pad down | all words | 16400
                 station tabs | unproven | shipworks→industry→shipworks | cycle | 16400
                 drill sheet | open-failed route | dpad/accept/back yes | mounted | 16400
                 node pad pin | 5/5 | 7/7 twice | green | 16400

THE FEEL         Menus on a pad now feel like they own the whole sheet, not a
                 single highlighted button.

NEXT             PQ-164.01 glyphs and remap (already done in this tree)
```

Seed `16400`. Live catalog is 26 screens (the registered set, including the station and the dev sandbox). The packet's "27" was the old count; the walk uses what actually registers.

Locked sheets that trap B by design: game over, crucible draft, crucible refit, crucible results.

Station B is undock: D-pad and A work on the hub, LB/RB cycle the rail, B leaves.

Chromium walk (twice, both green, after Drill stayed mounted):

```text
26/26 screens pad=yes seed=16400
SCREEN drill pad=yes dpad=yes accept=yes back=yes
SCREEN station pad=yes dpad=yes accept=yes back=yes tabs=shipworks->industry->shipworks
ok gamepad screens: every listed screen accepts pad dpad/accept/back (26/26 seed=16400)
```

One page-error was observed and not gated: `Cannot read properties of undefined (reading 'toFixed')`. It did not fail the pad walk.

## Checks (twice where it prints the bar)

| Check | Run 1 | Run 2 |
|---|---|---|
| `check:gamepad:screens` | 26/26 pad=yes | 26/26 pad=yes |
| `node --test test/pq-164-00-gamepad-screens.test.mjs` | 7/7 | 7/7 |
| `check:settings-profile` | OK | — |
| `check:controls-discoverability` | OK | — |

## What changed

- Pad focus includes kit words and tabs that use roving tabindex, so D-pad is the arrow-key analog.
- The pad is polled while a modal is open, even if the pause request has not landed yet.
- The walk uses the shipped gamepad and UI input on the live screen catalog in a real browser document, without waiting for a 3D `window.SF` boot.
- Drill is given a pending rock so `onShow` does not pop it; Leave / D-pad / A / B run on the mounted sheet.

Trackpad pointer/wheel wiring already in the UI input file was left as the PQ-164.02 live seam.
