<!-- LIFETIME: RECEIPT -->
# PQ-032.00 — Beats 1–3 become set pieces

```text
STATUS PARTIAL. Beat 1 settles on its headline verb on the live
                route, proven from the live emitter. Beat 2 settles
                on its headline verb (pull) by either posted method.
                Beat 3 does NOT settle on "tow": one massline:throw
                on the slag core, measured 1471 WU from the berth,
                still pays sling_in. The dock door was tightened;
                the throw door is sector-wide and was never touched.
                The prior STATUS DONE rested on a fixture berth.

WHAT I FOUND     1. The story latch-dock gate is live FORMULA on a
                 DEAD fixture. The test spawned its own berth at
                 radius 40 / dockRadius 80 — geometry the game never
                 emits (world.js:1463-1464 emits only 42/90, 34/72,
                 26/60) — and the receipt printed the resulting
                 111 / 120 WU as the live gate. Both dest stations
                 are size M, so the live gate is 103 WU (pod r7) and
                 112 WU (core r16).

                 2. Every passing input was placed, not earned. The
                 pod and the core were assigned cargo.pos = berth.pos
                 — 0 WU from the station CENTRE, i.e. inside a 34 WU
                 hull. No solver produces that pose. B1's rock was
                 likewise teleported onto the tower's centre.

                 3. "It does not pay as sling_in at 700 WU" is false.
                 _tryPhysicalDockComplete returns false for a short
                 story tow, but _onPhysicalThrow (missions.js:4482-89)
                 pays sling_in on `currentSectorId === destSectorId`
                 alone — no distance test at all — and
                 _onPhysicalReleaseRated (4445-53) pays it at 700 WU.
                 Neither carries the storyDock guard.

                 4. B2's second posted method, corridor_pull, settles
                 on a reel once the two escorts are dead, with no
                 berth in the world at all. That IS "pull", so the
                 beat is honest — but "means bringing it to the dest
                 dock" overstates it.

WHAT I CHANGED   Test only. missions.js was not touched. The berth is
                 now the REAL station the live world spawns in the
                 dest sector. B1's rock is released at 80 wu/s and
                 flown one fixed step at a time until the live
                 masslineImpacts observer detects contact. The pod
                 and core are reeled to a tight carry and flown in on
                 a live state.player.tether mirror, and the arrival
                 pose is asserted legal before the turn-in. A
                 mid-tow waypoint inside the leftover 700 WU berth
                 radius proves the story gate tightened something.

WHAT YOU WILL FEEL   Swinging a mass into the dead tower clears
                 Honest Work — the rock has to travel and land on the
                 tower, and calling your shot does nothing. Pulling
                 the pod clears First Blood. The long tow is the one
                 that still lies: you can throw the core anywhere in
                 Ceres Belt and the beat pays as if you brought it
                 home.

THE NUMBERS      bar | before | after | target
                 whip settles B1 | claimed | proven live | the swing IS the verb
                 B1 contact pose | 0 WU (tower centre) | 32 WU = hulls touching | reachable
                 B1 travel to contact | teleport | 21 ticks ballistic | it must fly
                 throw release settles B1 | no | no | contact only
                 story latch-dock gate | 111 / 120 WU fixture | 103 / 112 WU live | live geometry
                 dest berth | radius 40 / dock 80 invented | radius 34 / dock 72 live | the real station
                 leftover slack | 24 WU | 24 WU | unchanged
                 PHYSICAL_BERTH_WU | 700 | 700 | authored / sling_in unchanged
                 pod in the field pays | n/a | 890 WU: no | fail
                 pod inside old 700 WU pays | untested | 421 WU: no | THE tightening
                 pod towed to the dock pays | 0 WU (in the hull) | 81 WU on a 19 WU line | yes
                 core in the field pays | n/a | 1471 WU: no | fail
                 core inside old 700 WU pays | untested | 434 WU: no | THE tightening
                 core towed to the dock pays | 0 WU (in the hull) | 90 WU on a 28 WU line | yes
                 B3 one throw in-sector pays | unstated | 1471 WU: YES | should fail
                 branches added | 0 | 0 | none
                 endings added | 0 | 0 | none
                 scenario | pq-032-00-set-pieces | seed 3200 | headless print

THE FRAMES       Headless proof. No headed GPU strip.

HONESTY GAPS     The tow is scripted kinematics. This harness runs no
                 physics and no tetherGameplay, so the reel and the
                 run home are stepped by hand. That buys an arrival
                 POSE and a latch the live route can produce; it does
                 not prove the constraint solver drags the mass.
                 A headed or full-registry run is still owed.
                 sling_in IS a posted method for beat 3
                 (["tow_in","sling_in"]) — the method is designed,
                 its sector-wide range is what is not.
                 Default DEFAULT_MASK.wreck is still 0. Only mission
                 wrecks get the explicit mask. _playerLatchedTo still
                 falls back to latchedTargetId after release; the
                 rewritten test no longer leans on that fallback.
                 Leftover authored / sling_in still use 700 WU.
                 B2 board tag is still leftover campaign47a:b2:elroy.
                 Do not mark the queue.

NEXT             Close the B3 throw door before claiming beats 1–3
                 settle on their headline verbs: _onPhysicalThrow and
                 _onPhysicalReleaseRated need the same storyDock
                 guard _tryPhysicalDockComplete already has, or
                 sling_in needs a berth test instead of a sector
                 test. That is a missions.js edit and out of scope
                 for this review. PQ-032.01 moves the ending gate.
                 Do not mark the queue from this receipt.
```

## Files

- `test/pq-032-00-set-pieces.test.mjs` — rewritten: live berth, ballistic B1 contact, towed B2/B3, mid-tow discriminator
- `design/program/roadmap/receipts/PQ-032.00-REPORT.md` — this receipt

Read but not edited: `src/systems/missions.js` (story latch-dock / whip settle),
`src/systems/masslineImpacts.js` (`COLLIDABLE_TYPES` includes `wreck`), `src/systems/world.js`,
`src/data/sectors.js`.

Same-fixture carriers, left alone: `test/contract-47a-b2-investigation.test.mjs`,
`test/contract-47a-b3-consequence.test.mjs`, `test/story-campaign47a-live.test.mjs` all spawn the
same invented `dockRadius: 80` berth. All three still pass. They inherit finding 1.

Not edited: `program-queue.json`, `entity.js`, `physics.js`, `weapons.js`, `NOW.md`, FRONTEND screens.

## What this review changed

2026-09-09 honesty review of the landed leftover settle. No production code moved.

- The dest berth is the live station: `world` is on the harness, the player flies to the dest sector,
  and `world.update` spawns `station_expanse` / `station_ceres` at radius 34 / dockRadius 72.
- B1 proves travel: the rock is cut loose at 80 wu/s and stepped until `masslineImpacts` fires. It
  contacts at 32 WU, exactly the combined radii — the pose a solver would rest at.
- B2/B3 prove a pose, not an assignment: reeled to a tight carry, flown in, and asserted clear of the
  station hull, clear of the towing ship, and inside the live docking range (physics.js:757).
- A mid-tow waypoint at ~420 WU — inside the leftover 700 WU `PHYSICAL_BERTH_WU`, outside the story
  gate — is the only assertion that proves the tightening did anything. Without it the far-side
  refusal at 890 / 1471 WU would pass identically under the old rule.
- A second test pins both dest stations at authored size `M`, so a resize fails here instead of
  silently moving the 103 / 112 WU figures.

## Proof

```text
PQ-032.00 whip contact settled B1: yes
PQ-032.00 wrecking_ball rock flew 21 ticks to contact at 32 WU (hulls touch at 32 WU)
PQ-032.00 rescue_under_fire live berth station_expanse radius 34 dockRadius 72 — story latch-dock gate 103 WU
PQ-032.00 rescue_under_fire life_pod sits 890 WU from the berth at turn-in — paid: no
PQ-032.00 rescue_under_fire life_pod inside the leftover 700 WU berth at 421 WU — paid: no
PQ-032.00 rescue_under_fire reeled 1416 + towed 811 ticks — life_pod parks 81 WU out on a 19 WU line, ship 62 WU (dock range 120 WU)
PQ-032.00 rescue_under_fire life_pod towed to 81 WU (gate 103 WU) — paid: yes
PQ-032.00 tow_recovery live berth station_ceres radius 34 dockRadius 72 — story latch-dock gate 112 WU
PQ-032.00 tow_recovery slag_core sits 1471 WU from the berth at turn-in — paid: no
PQ-032.00 tow_recovery slag_core inside the leftover 700 WU berth at 434 WU — paid: no
PQ-032.00 tow_recovery reeled 1216 + towed 811 ticks — slag_core parks 90 WU out on a 28 WU line, ship 62 WU (dock range 120 WU)
PQ-032.00 tow_recovery slag_core towed to 90 WU (gate 112 WU) — paid: yes
```

`node --test test/pq-032-00-set-pieces.test.mjs` — 3 pass, seed 3200.

Still green, unedited: `contract-47a-b2-investigation`, `contract-47a-b3-consequence`,
`story-campaign47a-live` — 3 pass.

### The B3 sling_in leak, measured

Same live-world harness, same seed. B1 and B2 settled normally; the core was never towed:

```text
B3 core lies 1471 WU from station_ceres (story latch-dock gate 112 WU, leftover berth 700 WU)
B3 one massline:throw at 1471 WU settled the beat? true -> beat 4
methods: ["wrecking_ball","stage_tow","sling_in"]
```

This is NOT pinned as a green assertion anywhere — blessing a leak as expected behaviour is how
anti-vision tests get written. It is recorded here so the next iterate can close it. Reproduction is
the spine harness in `test/pq-032-00-set-pieces.test.mjs` with the B3 tow replaced by a single
`massline:throw` on the slag core while standing in `sector_ceres_belt`.
