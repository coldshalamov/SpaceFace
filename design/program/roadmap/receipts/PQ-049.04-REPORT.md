<!-- LIFETIME: RECEIPT -->
# PQ-049.04 — Express traffic to Massline express-liner mapping

**Verdict:** IMPLEMENTED. The existing Helios `express` traffic role resolves the civic liner
through the live presentation seam; simulation, custody, and route authority untouched.

## What shipped

- `src/render/partsLibrary.js`: `WHOLE_SHIP_FILE_BY_TRAFFIC_ROLE.express =
  'wholeships/massline_express_liner_v1.glb'` and
  `WHOLE_SHIP_ASSET_ID_BY_TRAFFIC_ROLE.express = 'SF_WHOLESHIP_MASSLINE_EXPRESS_LINER_V1'`
  (committed; survives the PR #163 merge).
- Release payload on disk: `assets/ships/release/parts/wholeships/massline_express_liner_v1.glb`
  plus LOD1/LOD2, and `assets/ships/release/render-packages/massline-express-liner-v1{,-lod1,-lod2}`.
- `traffic.js` express role unchanged — `docks: true, trades: true, express: true`, the single
  scheduled high-security express slot, `_stampExpressRoute` itinerary. No sim edits.

## Evidence

- `node --test test/pq048-passenger-liner-service.test.mjs test/massline-express-liner-runtime.test.mjs`
  — 7/7 green. The runtime test resolves a live `trafficRole:'express'` / `ship_mule` record to
  `massline_express_liner_v1.glb` + `SF_WHOLESHIP_MASSLINE_EXPRESS_LINER_V1`, pins that the player
  Mule and courier Lark keep their accepted bodies, and proves the release GLB resolves a
  render-package pilot (no source-route fallback).
- `npm run check:baseline` — 15/16 green. Sole red is `check:massline` aggregation: four heavy
  47a children TIMED OUT under host contention (no assertion reported). Solo re-run reproduced
  22/26 green with the same four environment timeouts; `check:mining:bulk-guidance` (a foreign
  collision repaired earlier this session) is green.
- Remaining asset/traffic checks (`check:traffic`, `check:asset-status`,
  `check:asset-reachability`, `check:assets:live`) were still in flight at receipt time on a
  saturated host; the mapping seam and release payloads are verified on disk and by the focused
  tests above.

## Still owed for `done`

- Natural Browser and Electron express spawn captures (label, route, dock context) — PQ-049's
  acceptance requires the live-route pictures; this machine could not produce a non-starved run.
- The four timed-out 47a children re-verified on a quiet host.
