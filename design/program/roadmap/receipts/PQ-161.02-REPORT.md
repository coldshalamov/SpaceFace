<!-- LIFETIME: RECEIPT -->
# PQ-161.02 — Force palette: receipt

```text
DONE  PQ-161.02 — rope, wells, repulsors, impulses, and shields now have five hues that stay distinct under deuteranopia, protanopia, and tritanopia, and the brightness order does not flip.

WHAT I FOUND     The live force colours used the same cyan for the hitch and the well. The other three hues swapped brightness under red-weak and blue-weak vision. A label would not have fixed that.

WHAT I CHANGED   Authored a five-hue force palette with a locked brightness stack (well darkest, punch brightest) and a headless contrast check on seed 16120. Identity is the hue and the brightness step, not a caption.

WHAT YOU WILL FEEL   A hitch, a well, a plow, a punch, and a shield no longer share a colour in the contract. The world still paints the old cyan and amber until the dirty render files take this palette. That wiring was out of this leaf.

THE NUMBERS      bar | before | after | target
                 distinct hexes | 4 (rope = wells) | 5 | 5
                 min ΔE deuteranopia | 0 | 23.46 | ≥ 20
                 min ΔE protanopia | 0 | 26.58 | ≥ 20
                 min ΔE tritanopia | 0 | 22.60 | ≥ 20
                 brightness order under 3 CVD | 0 / 3 (flips) | 3 / 3 wells < rope < shields < repulsors < impulses | 3 / 3
                 void contrast (worst CVD) | n/a (collision) | 3.11 | ≥ 3
                 trichromat min hue | 0° | 28.4° | ≥ 25°
                 headless suite | missing | 4 / 4 | green
                 seed | — | 16120 | 16120

THE FRAMES       Headless contrast simulation. No headed Chromium. No stills.

NEXT             PQ-161.03 — teaching overlay in the Range and the stroke preview only. Queue not marked from this receipt. No commit.
```

## Hues

| Channel | Hex | Name | Verb |
|---|---|---|---|
| wells | `#036F59` | teal sink | pull |
| rope | `#ED4EA3` | magenta line | hitch |
| shields | `#83AEFA` | sky barrier | hold |
| repulsors | `#FFB98B` | peach push | shove |
| impulses | `#FFE956` | gold flash | punch |

Brightness order (darkest → brightest), preserved under all three simulations: wells < rope < shields < repulsors < impulses.

## Contrast under CVD (Machado 2009, seed 16120)

| Sim | min ΔE | worst pair | void contrast | order |
|---|---|---|---|---|
| deuteranopia | 23.46 | rope–wells | 3.11 | preserved |
| protanopia | 26.58 | rope–shields | 3.54 | preserved |
| tritanopia | 22.60 | repulsors–impulses | 3.38 | preserved |

## Checks

| Check | Result |
|---|---|
| `node --test test/pq-161-02-force-palette.test.mjs` | 4 pass |
| `node scripts/check-faction-kit.mjs` | OK (14 kits, 16 faction claims; force claims are a separate export) |
| Entry `npm run check:baseline` | RED before first edit: sim / sim-v3 47-A hash drift + 90s budget. Foreign. Not this leaf. |
| `npm run check:colour-tokens` | RED on `styles/ui.css` literals. Foreign. Not this write set. |

## Stay-off

Not edited: `src/ui/screens/range.js`, `src/render/renderer.js`, `src/render/vfx.js`, `program-queue.json`. No commit. No push.

## Tradeoff

Magenta hitch and teal well leave the old shared cyan. Peach plow stays warm; gold punch stays the brightest flash. The world will look wrong until the dirty VFX files read this contract — that is the bargain for not stomping live render work.

## 2026-09-10 salvage (Lane Q)

```text
DONE  PQ-161.02 — five force hues still clear ΔE 20 under deuteranopia, protanopia and tritanopia when measured with minPairDeltaE; telegraph kinds map to those hues, never to a faction id.

WHAT I FOUND     minPairDeltaE and TELEGRAPH_FORCE_CHANNELS were already in palettes.js and unused by the palette test.

WHAT I CHANGED   Extended the existing force-palette proof to call minPairDeltaE on every force pair and every telegraph-force pair, and pinned that telegraph kinds cannot resolve to a faction.

WHAT YOU WILL FEEL   A hitch, a blast and a punch still disagree on hue. The halo can now name the force without stealing the faction coat. Faction paint versus force hue is a second channel, not a 20-ΔE promise — Reach red next to a well can still collapse under protanopia.

THE NUMBERS      bar | before | after | target   (seed 16120)
                 minPairDeltaE force-vs-force | same hues, uncalled | 22.60 (repulsors–impulses, tritanopia) | ≥ 20
                 telegraph-force pairs (none/deuteranope/protanope) | untested | min 34.96 (impulses–repulsors, deuteranopia) | ≥ 20
                 telegraph kind → force channel | table existed | engine_flare/weapon_charge→impulses, wake_mines→repulsors, attach_spool→rope | no faction id
                 faction-primary vs telegraph-force minPairDeltaE | unmeasured | 8.05 (faction_verge_layers vs rope, deuteranopia) | two channels, not the force floor

FILES            test/pq-161-02-force-palette.test.mjs
                 design/program/roadmap/receipts/PQ-161.02-REPORT.md

CHECKS           node --test test/pq-161-01-telegraphs.test.mjs test/pq-161-02-force-palette.test.mjs | 11 pass, 0 fail
                 npm run check:baseline (entry) | 12/15 green; RED pq020-ceres-topology, sim, sim-v3 (pre-existing)
                 npm run check:baseline (exit) | 12/15 green; same three reds, same 47-A hashes; list not longer

UNPROVEN         World VFX still paints the live stand-ins (rope=wells cyan). No headed still. Faction-vs-force ΔE 8.05 is measured and is below 20; identity stays a data-faction attribute, not a recolour of the force table. palettes.js itself was not rewritten in this salvage.
```

## 2026-09-10 laneB finish

The lane's 11 leaf tests pass together (`node --test test/pq-161-01-telegraphs.test.mjs test/pq-161-02-force-palette.test.mjs` — 11 pass, 0 fail). The halo now reads this palette on the default route: each leftover telegraph cue carries `data-force-channel` plus an inline border colour taken verbatim from `FORCE_PALETTE` (`engine_flare`/`weapon_charge` → impulses, `wake_mines` → repulsors, `attach_spool` → rope), and the pq-161-01 test pins the channel, the faction attribute staying a separate channel, and the cue colour clearing on expiry. `minPairDeltaE` is exported and called by the palette test for every force pair under all three CVD simulations. World VFX still paints the old stand-ins — that wiring stays with the render owners, unchanged from the salvage verdict above.

## 2026-09-10 seed alignment (leaf order PQ-161.02)

```text
DONE  PQ-161.02 — the force-palette contrast proof now runs on the scenario seed the leaf names, 16102, and is still green under all three colour-vision simulations with the brightness order preserved.

WHAT I FOUND     The shipped constant and test used seed 16120; the leaf packet measures seed 16102. A transposition, not a colour problem — the contrast numbers never depended on the seed.

WHAT I CHANGED   Aligned FORCE_PALETTE_SEED and the four test seed assertions to 16102. No hue, verb or brightness rank moved.

WHAT YOU WILL FEEL   Nothing changes on screen — rope, wells, repulsors, impulses and shields keep the same five hues. A reviewer running the leaf's seed gets the same green result instead of a seed-guard error.

THE NUMBERS      bar | before | after | target   (seed 16102)
                 deuteranopia min ΔE | 23.46 (seed 16120) | 23.46 | ≥ 20
                 protanopia min ΔE | 26.58 | 26.58 | ≥ 20
                 tritanopia min ΔE | 22.60 | 22.60 | ≥ 20
                 brightness order under 3 CVD | 3 / 3 preserved | 3 / 3 preserved | 3 / 3
                 trichromat min hue | 28.4° | 28.4° | ≥ 25°
                 void contrast (worst CVD) | 3.11 | 3.11 | ≥ 3
                 headless suite | 6 / 6 twice | 6 / 6 twice | green

CHECKS           node --test test/pq-161-02-force-palette.test.mjs | 6 pass, 0 fail (run twice)
                 npm run check:colour-tokens | RED — styles/ui.css:104-105 duplicate --accent/--sf-foe; foreign, not this write set
                 npm run check:baseline (exit) | 11/15 green; RED vfx-techniques (foreign src/render/uiStage.js soft-card), pq020-ceres-topology, sim, sim-v3 (pre-existing); no new red from this seed-only edit

UNPROVEN         World VFX still paints the live stand-ins (rope = wells cyan). No headed still; contract + simulation only.
```
