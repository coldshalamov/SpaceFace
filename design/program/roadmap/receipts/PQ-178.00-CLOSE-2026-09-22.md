<!-- LIFETIME: RECEIPT -->
# PQ-178.00 — Close 2026-09-22

The beat standard, template, and 47-A opener sheet were already in the tree
(`PQ-178.00-REPORT.md`). The queue still said ready. This close keeps that
sheet and makes the validator catch a dialogue tree or a stick-lock that the
sheet does not admit.

A reply list, option list, branch list, or dialogue `nodes` entry fails even
when `forbidden.dialogueTree` and `forbidden.choiceMenu` are false. A
`takesStick`, `takesControl`, `lockStick`, or `lockPlayer` fails the same way
at the top of the sheet or buried inside it.
The live corpus stays five passing sheets. The 47-A opener still names the
leftover actors and the leftover hitch and cut. It does not collapse to
mine-and-dock.

Proved: `test/pq-178-00-beat-standard.test.mjs` 5/5 and
`test/pq-178-01-beats.test.mjs` 4/4. `node scripts/check-beat-standard.mjs`
PASS, 5 sheets.

Queue: PQ-178.00 ready → done.
