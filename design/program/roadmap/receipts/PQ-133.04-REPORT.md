<!-- LIFETIME: RECEIPT -->
# PQ-133.04 — Surface receipt + Ricochet Foundry vertical slice (Phase 4)

```text
STATUS DONE

WHAT I FOUND     The 2026-09-14 bounded candidate (6746fafd) carried the
                 Foundry content but five shared-owner gaps: live combat
                 accepted absent or forged surface receipts and reflected
                 absolute velocity (R1/R2), the Foundry room was an empty
                 idle room with a direct-shot bypass (R3), there was no
                 public route, no Mirrorjaw machinery and no causal
                 presentation (R4), and the wave-ten swap left the planner
                 snapshot, capital_broadside coverage and arena-parity
                 tests red (R5). Baseline fingerprint: foundry test 8 pass
                 / 5 fail (R1 x2, R2, R3 x2); wave tests 3 failing;
                 crucible-run red on 1 inherited failing suite.

WHAT I CHANGED   Nothing in this note. The R1-R5 landings already on
                 master (e9eb6f4e0, 2d3df2960, b1eaaa816, a74b6ba63,
                 closed in 7359a9947) satisfy the bounded receipt's blocker
                 requests. This file is the leaf receipt the queue points
                 at, and it explicitly SUPERSEDES the 2026-09-14 bounded-
                 candidate receipt design/program/roadmap/receipts/
                 PQ-133.04.md, which is left untouched.

                 R1/R2 physics receipt + bounded ricochet (e9eb6f4e0):
                 physics.js / surfaceContact.js issue trusted surface-
                 contact receipts carrying contact and relative velocity;
                 attackHit.js / surfaceReflection.js reject absent, forged,
                 cross-body or stale receipts; sg02DynamicBodyOwner.js and
                 physicsAuthority.js apply the bounce as an authoritative
                 continuation command, surface-relative. Gate: shared-owner
                 R1 x2 and R2 pass in the foundry tests.

                 R3 authored Foundry room (2d3df2960): survivalArena.js +
                 arenaModuleLibrary.js install three reflective bank
                 plates, furnace gate posts and boss-room decor from wave
                 one; shutters and plates move through the physics
                 membrane, never pose-written; authoredRoomFrame is
                 published as camera-law input on survivalArena:installed.
                 Gate: shared-owner R3 x2 pass (arena plate helper refuses
                 an ordinary direct shot; authored geometry installs).

                 R5 content disposition (b1eaaa816): the authored wave-ten
                 split stands - Foundry (helios_core) fields the committed
                 ram mirrorjaw_foreman; the six other arenas keep
                 dreadnought_boss and capital_broadside. One planner
                 snapshot rebase with the reason recorded in the test
                 file; the schema test re-authored around an explicit
                 AUTHORED_WAVE_TEN_BOSS map. No golden edited. Gate: waves
                 25/25 after the baseline 22/3.

                 R4 route, boss machinery, causal presentation
                 (a74b6ba63): Mirrorjaw Foreman with a mirror prow,
                 directional armor split, rear machinery and three
                 verified solution families; the public block ruleset
                 (ten-wave Foundry ending in victory, never a refit bench)
                 on the ordinary Crucible route with the public zero-power
                 mirror_demonstrator kit (Pulse direct / Bank / Smart);
                 ricochet continuation resolves to authored cue, caption
                 and audio through the presentation arbiter. Gate:
                 r4-boss + r4-presentation + r4-route 22/22; the route
                 test walks loadout -> arena_intro -> ... -> victory.

WHAT YOU WILL FEEL   Crucible offers a Ricochet Foundry block: ten waves
                 in an authored reflective room where your Pulse shots
                 bank off plates and moving shutters, wave ten is the
                 Mirrorjaw Foreman whose mirror prow must be out-thought
                 three ways, and every ricochet kill explains itself on
                 screen with cue, caption and audio. Adventure stays
                 untouched.

THE NUMBERS      foundry focused (foundry + foundry-room) | 27 pass / 0 fail | 8 pass / 5 fail | green
                 r4 boss/presentation/route | 22 pass / 0 fail | absent at baseline | green
                 wave schema + planner | 25 pass / 0 fail | 3 failing | green
                 arena suites (arena, swarm-arena, survival-run) | 59 pass / 0 fail | green at baseline | green
                 check:attack-spec | 72 pass / 0 fail | green at baseline | green
                 check:sim | pass, exit 0 | green at baseline | green
                 check:combat | grammar 8/8 + SG-03 save/reload | green at baseline | green
                 check:crucible:run | 14/14 suites green | 1 inherited failing suite | green, red -1
                 check:playable | 16/16 pass | green at baseline | green
                 Foundry wave-10 p95 | 6.66 ms ladder (round 1: 86.82 ms, optimizer engaged), 3.58 ms fresh probe | 16.7 ms target | pass
                 stages | physics done | arena, content, route done | 4/4 done

NEXT             Still unproven: visual acceptance (no headed capture was
                 taken; owner capture ruling 2026-09-16), aimed human play
                 feel (the evidence here is deterministic tests plus the
                 playable gate, not an aimed ten-wave human run),
                 Electron/Sandbox/Lab parity (the perf probe is a sim-side
                 CPU upper bound, not a render-side frame time at play
                 size), and the wave-30 arc (the block is ten waves;
                 thirty-wave Foundry is PQ-133.07). Material: the block's
                 true perf peak is wave six, the swarmer flood (p95 up to
                 ~32 ms observed) - the standing candidate for the
                 performance program. Next leaf: PQ-133.05 chain /
                 payload / bridge.
```
