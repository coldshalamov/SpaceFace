<!-- LIFETIME: RECEIPT -->
# PQ-178.03 — The story ledger

```text
DONE / DONE  PQ-178.03 — At the berth the mechanic retells the campaign from the receipts, in order.
WHAT I FOUND     The berth line ignored the campaign receipts the game already writes, so a real run had nothing to read back.
WHAT I CHANGED   The berth reader now turns those receipts into one "I was X, then Y, so I Z" sentence, and a second sentence when the line was hitched or cut.
WHAT YOU WILL FEEL   Dock after any of the campaign and the mechanic tells you what you did, in the order it happened, including a filed ending. A new ship with no receipts still gets silence. You will not hear thrown-mass versus a cut unless that settlement receipt kept the method; the live mission receipt still does not store it.
THE NUMBERS      bar | before | after | target
                 campaign clauses from sidecar receipts, seed 17803 | 0 | 7 | the acts in order
                 clauses dropped on that run | 7 | 0 | 0
                 empty berth invented a campaign | 0 | 0 | 0
THE FEEL         You can retell the run from the berth sentence alone.
NEXT             PQ-152.01
```

Seed 17803. The seven clauses are: sampling the variance, docking the sample, knocking the variance tower, pulling the pods under fire, towing the slag core, taking the traders' intro, finished the traders' proving runs.

Headless. `node --test test/pq-178-03-ledger.test.mjs` and `test/pq-150-01-mechanic.test.mjs`. `check:bar:narrative`, `check-codex-narrative`, and `check:mission-receipts` passed. `check:baseline` is red on save-schema and the 47-A sim tape in this dirty tree; this reader is not on that tape.
