DONE PQ-193.02 — Reverse and brake now fire a short pair of bow jets from the same cyan sheet family as the main drive, with no leftover needle trail, and a stranger can name braking from a HUD-hidden chase still.

DONE WHEN (verbatim): A stranger can tell, HUD hidden, that the player is braking.

WHAT I FOUND — Reverse already had a volumetric path, but that construction cannot make a crease (it reads as smoke), leftover needles were gated only on paper, and the first-flight VFX freeze hid every live jet until the opening hold released.

WHAT I CHANGED — Drove reverse from the same swept-sheet and forge mouth as the main drive, stubby at the bow, blocked needle sprites on the production player, and lit that path from reverse demand even if the impulse RCS table was not ready.

WHAT YOU WILL FEEL — Holding brake lights two short cyan jets out the nose, same family as the long wake behind you, not two hairlines. The first seconds of a new game can still hold all drive light frozen while the opening GPU picture settles — after that, braking is obvious.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Construction | isotropic volume (soft shoulders) | swept ribbon sheets + forge mouth | same family as the main drive | 19302 |
| Recipe length / exit radius | 4.8 / 0.18 hairline, then 3.2 / 1.25 volume | 4.6 / 1.28 stubby jet | stubby, not a filament | 19302 |
| Exit diameter vs lit length at full demand | ~0.075 then ~0.74 volume | 0.53 | ≥ 0.5, not hairline | 19302 |
| Held-brake frames with both bow jets live | unmeasured, then 120/120 headless | 2/2 live on the chase still | both live while reverse demand | 19302 |
| Needle sprites on production player | leftover trail still callable | 0 | 0 | 19302 |
| Stranger recognizes braking, HUD hidden | unproven; 0 qualifying stills | yes — two cyan bow jets opposite the wake | recognizable braking | 19302 |

THE FEEL — It plays better: brake is a jet you can read at chase distance, not two needles and not a fog blob.

NEXT — PQ-141.02 every actor has a sentence (highest live `--ready` row; this unit stays closed).

FILES

- `src/render/thruster/recipes/plasmaStreamRecipe.js` — stubby ribbon-family retro recipe; Hitch freeze untouched.
- `src/render/thruster/systems/playerRetroVolume.js` — shipped retro update + ribbon/forge owner; needle gate.
- `src/render/vfx.js` — production reverse uses PlayerRetroJets; 141.02 NPC-job hunks and foreign thruster quality-tier hunks untouched.
- `test/pq-193-02-reverse-jets.test.mjs` — imports shipped update.

CHECKS

| Command | Round 1 | Round 2 |
|---|---|---|
| `node --test test/pq-193-02-reverse-jets.test.mjs` | PASS 3/3 | PASS 3/3 |
| `node --test test/pq-193-live-picture-arrival.test.mjs` | PASS 7/7 (arrival file) | — |
| `npm run check:vfx-techniques` | PASS 10/10 | PASS 10/10 |

UNPROVEN — First ~20 s of New Game still freezes drive VFX behind the opening GPU hold (pre-existing). After that hold, the HUD-hidden chase still names braking. Hitch/Kestrel assets untouched.
