DONE PQ-193.12 — The fat-cylinder station stand-in can no longer appear on the default route; stations load authored bodies only.

DONE WHEN (verbatim): Every station the player can reach on the default route loads its authored body. The fat cylinder plus hoops fallback is a defect, not a style. Diagnose load failure; do not pretty the primitive.

WHAT I FOUND — Station archetype resolution was partial over STATION_TYPES; an unresolved archetype fell through to a procedural fat-cylinder-plus-hoops body that then published as if it were a station.

WHAT I CHANGED — Station archetype resolution is total over STATION_TYPES, and an unresolved archetype now fails closed (no body published) instead of publishing the procedural stand-in. A leaf test enumerates every station type and asserts an authored body path.

WHAT YOU WILL FEEL — No station is ever a grey barrel with rings. If an authored body ever failed to load you would see nothing there rather than a toy, and the test would go red first.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| STATION_TYPES with an authored body path | partial | all | total | enumeration |
| Procedural fat-cylinder publish path | reachable | removed | 0 | code |

THE FEEL — Places look built, not placeholder.

NEXT — `--next`.

CHECKS

| Command | Result |
|---|---|
| `node --test test/pq193-12-station-authored-body.test.mjs` | PASS (re-run 2026-09-12 on `824c773bb`) |
| `npm run check:baseline` | 11/15; the four reds are the pre-existing tree-wide drifts, not this leaf |

EVIDENCE — Commit `dcc0c14de` (2026-09-11) "Load authored station bodies only; never publish the fat-cylinder stand-in." `src/render/partsLibrary.js` (+71/−11) and the leaf test.

UNPROVEN — `check:playable` / `check:assets:live` headed boot (GPU route) after this commit; the enumeration test closes the code-path bar.
