<!-- LIFETIME: RECEIPT -->
# PQ-149.00 — Session rhythm

```text
STATUS DONE

WHAT I FOUND     Directors could mint combat any time a window was open.
                 Work, aftermath, and quiet had no shared gate. Station
                 cosmetics and combat launches used the same pump.

WHAT I CHANGED   One eight-phase clock now lives on the encounter director.
                 Combat shapes defer with reason `rhythm` in work,
                 aftermath, and quiet. Civilian life may still run.
                 Violence is the combat window. No phase sits longer than
                 twelve minutes. The station pump holds budgeted launches
                 in those same phases and still lets a repair drone crawl.
                 The tactical director stays in RESPITE and will not
                 reinforce. Telemetry prints `rhythm:phase`.

WHAT YOU WILL FEEL   A quiet dock stays quiet. A fight is allowed to
                 finish, then the field lets go. Work and aftermath do
                 not spawn a second fight on top of the first.

THE NUMBERS      bar | before | after | target
                 combat in quiet / aftermath / work | legal | blocked (`rhythm`) | defer
                 combat in violence | legal | legal | allow
                 civilian in quiet | unmeasured | allowed | life still runs
                 phase cap | none | 12 min | no dwell > 12 min
                 compressed clock (seed 14900) | — | travel → curiosity → opportunity → tension → violence → aftermath → quiet → work | all eight
                 station quiet pump | patrol + drone | patrol deferred; repair_drone fires | cosmetics may run
                 tactical reinforce in quiet/aftermath | possible | RESPITE, no request_reinforcement | hold
                 scenario | test/pq-149-00-session-rhythm.test.mjs | seed 14900 | headless

THE FRAMES       Headless compressed clock. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-149.01 is escalation
                 seeds. Do not mark the queue from this receipt.
```

## Files

- `src/systems/encounterDirector.js` — eight-phase clock; combat defers with `'rhythm'`
- `src/ai/director.js` — quiet/aftermath holds tactical RESPITE
- `src/systems/stationSideEventDirector.js` — budgeted launches defer; cosmetics may run
- `src/systems/telemetry.js` — `rhythm:phase`
- `test/pq-149-00-session-rhythm.test.mjs` — seed 14900

Shared directors also carry PQ-149.01 escalation seeds; that leaf has its own receipt.
