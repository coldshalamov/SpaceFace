<!-- LIFETIME: RECEIPT -->
# PQ-028.01 — Throw something into it

```text
STATUS DONE

WHAT I FOUND     The Ceres ring already flings off-axis hulls. Seed
                 2801 already puts a Wasp through the tube sideways,
                 out at more than 3× its cruise, and into a rock
                 hard enough that B6 terrain crumple fires. The
                 anvil is an asteroid, not a radiation hazard.

WHAT I CHANGED   Nothing. The live writer and the named Rapier proof
                 were already on the tree. Do not retune a passing
                 sling.

WHAT YOU WILL FEEL   Chuck a light into the Ceres ring off-axis. It
                 leaves as a long-range projectile and dies on the
                 rock. This is still a local toy, not Atlas travel.

THE NUMBERS      bar | before | after | target
                 Wasp exit / cruise | already 3.11× (327 / 105) | 3.11× | ≥ 3.00×
                 B6 terrain crumple | slam damage 400, control tumble | same | terrain, impactDamage > 0
                 pre-solve closing | 327 WU/s | 327 | > crumple 30
                 hull | 150 → 0, died | same | light crumples, not an HP aura
                 anvil | asteroid, hazard none | same | never radiation
                 alignmentDot at entry | ~0 | same | < 0.85
                 scenario | test/pq-028-01-sling-throw.test.mjs | seed 2801 | Rapier

THE FRAMES       Headless Rapier real-path is enough. No headed GPU strip.
                 Exit tick 52, slam tick 56, 57 ticks total. Backend
                 rapier-dynamic, flight v3, production profile.

NEXT             Integrator updates the queue. Leftover: PQ-028.02
                 the player-built sling (mod_throughline_sling at a
                 claim, NPC traffic). Do not mark the queue from this
                 receipt.
```

## Files

- `src/systems/travelLanes.js` — already live (`_flingSlingBody`, `throwPerp`)
- `test/pq-028-01-sling-throw.test.mjs` — already tracked

Not edited this pass. `program-queue.json` and `NOW.md` untouched.
`design/program/roadmap/receipts/PQ-028.00-REPORT.md` left untracked.
