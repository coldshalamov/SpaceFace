<!-- LIFETIME: HISTORICAL -->
# PQ-139.03 — impulse hits are directional, implementation receipt

- **candidate:** `86db59183740ccd9a00945b68fe63ed02b353e85`
- **integrated as:** `250aee07`
- **integration state:** implemented; normal-speed shipping-camera capture remains before `done`

## Player-visible result

Concussion slugs keep a signed shock sheet and spall back toward the shooter. Rail hits retain an
axial penetration cut even with the shipping weapon presenter mounted. Impulse charges emit a
bounded record for each real shove and draw a sheet/ribbon/cone along each victim's direction,
rather than inventing one averaged axis. The old generic spherical `combat.explosion.small` visual
cue is retired for charges only; its audio cue remains.

Reduced-motion keeps the causal axes with fewer, shorter, dimmer primitives. The existing bilateral
collision presentation is unchanged.

## Verification

- Controller: `node --test test/vfx-impulse-cone.test.mjs` — 7/7.
- Controller: `npm run check:vfx-techniques` — PASS, 12 entries / 11 files.
- Gemini 3.8 Flash High: PASS.
- Grok 4.6: PASS, session `01a06d1c-2be3-7593-ba2b-2cd9b662e542`.

This receipt deliberately stops at `implemented`: the branch worktree had no shipping assets, so a
normal-speed chase-camera capture of concussion, presenter-live rail, and a multi-victim charge is
still the direct visual acceptance step.
