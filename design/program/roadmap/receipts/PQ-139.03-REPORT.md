<!-- LIFETIME: HISTORICAL -->
# PQ-139.03 — impulse hits are directional, implementation receipt

- **candidate:** `86db59183740ccd9a00945b68fe63ed02b353e85`
- **integrated as:** `250aee07`
- **integration state:** implemented; normal-speed shipping-camera capture remains before `done`

## Player-visible result

Concussion slugs keep a signed shock sheet and spall back toward the shooter. Rail hits retain an
axial penetration cut even with the shipping weapon presenter mounted. Impulse charges emit a
bounded record for each real shove and draw a sheet/ribbon/cone along each victim's direction,
rather than inventing one averaged axis. The old generic spherical `combat.explosion.small` visual
cue is retired for charges only; its audio cue remains.

Reduced-motion keeps the causal axes with fewer, shorter, dimmer primitives. The existing bilateral
collision presentation is unchanged.

## Verification

- Controller: `node --test test/vfx-impulse-cone.test.mjs` — 7/7.
- Controller: `npm run check:vfx-techniques` — PASS, 12 entries / 11 files.
- Gemini 3.8 Flash High: PASS.
- Grok 4.6: PASS, session `01a06d1c-2be3-7593-ba2b-2cd9b662e542`.

## THE FRAMES

`design/program/roadmap/receipts/fun-loop/manifests/crucible/3f560ea5-dirty-8f437213/shove_light-physics_toolkit-s4242/`
— **`normalSpeed: false`, 0.561 of real time against the 0.60 floor**, 124 frames spanning
sim 32.93 s → 50.27 s, 22 player shots fired, `hudTextVerified: true` at both the start and the end
of the run, `webglRenderer` "ANGLE (Intel, Intel(R) Graphics (0x00007D45) Direct3D11 …)" — a real
GPU, not a software rasteriser. Source identity: gitHead `3f560ea5`, gitTree `25a2d44f`, production
dirty, diff hash `8f437213…`. Seed 4242, `physics_toolkit`, the real Crucible click-through, shipping
chase camera, default quality, no overlays. It is under the floor because the machine was carrying
three other lanes' headed browsers: the manifest's `realtimeStages` read
`{settled 0.893, afterRecorder 0.575, beforeScreencast 0.495, duringScreencast 0.561}` — the game was
already at 0.495 with the debugger attached and **nothing encoding**, so the photographer is not the
cost. Under `CANONICAL_BUILD_MAP.md` §1.3 law 2 a slow strip still carries an **appearance** claim,
which is what this unit is; **no timing claim is made from it.**

**What a viewer sees, and the honest verdict: the directional primitive is not resolvable at the
shipping camera.** The strip's one player-involved consequence cluster is at sim 36.3–37.5 s
(frames 26–36). At 4× magnification of `frame_031.jpg` (sim 36.87 s) the struck hostile sits inside a
**round, radially symmetric orange bloom with a bright core** — a phased explosion, not a sheet, a
ribbon or a cone. On `frame_045.jpg` (sim 38.67 s), mid-burst, what is legible on the target is a
**flat teal shield disc** (the shield-hit fresnel, `SPR_FRESNEL` in `src/render/vfx.js`) plus the
tracer streak from the muzzle. Hostile hulls measure roughly 40–110 px across a 1280×720 frame and
retention runs at 4–8 fps, so a cue that is a few dozen pixels wide and lasts a fraction of a second
cannot be told apart from a sphere in these pictures. That is a statement about what this strip can
prove, not a claim that the code draws a sphere: the code's cone/sheet/ribbon families are pinned by
`test/vfx-impulse-cone.test.mjs`.

**And the half of this unit that actually retires the sphere is not reachable on this route at all.**
The `combat.explosion.small` cue was retired *for impulse charges only*, and the Crucible cannot carry
a charge: `physics_toolkit` (`src/data/combatLabSetups.js`) grants three weapons and no cargo, and
nothing on the Crucible door grants `cmdty_impulse_charge` — the same gap PQ-137.09 filed as a
shared-change request in its §7.1. So no Crucible tape, however fast, can photograph the multi-victim
charge case.

**What the remaining acceptance needs**, precisely: (a) a capture at a closer camera or at a higher
retained cadence around the impact tick — today `MOMENT_EVENTS` in
`scripts/lib/bench/frameStripCapture.mjs` carries `projectile:hit`, but `condenseMoments` merged every
one of this run's 22 hits into a louder neighbour, so the 8 fps burst never landed on a hit; and
(b) for the charge case, either the Crucible starter stock PQ-137.09 asked for, or an adventure-route
driver in the strip harness (there is none — `driveRealCrucibleRoute` is the only route it can fly).

This receipt deliberately stops at `implemented`: the branch worktree had no shipping assets, so a
normal-speed chase-camera capture of concussion, presenter-live rail, and a multi-victim charge is
still the direct visual acceptance step.
