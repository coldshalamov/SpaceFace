<!-- LIFETIME: RECEIPT -->
# PQ-031.02 — NPC counterplay

```text
DONE  PQ-031.02 — an ace cuts a player bridle on her own strike, and an Anchor Controller ignores the same throw because 420 kg does not notice 16 kg

WHAT I FOUND     Aces already cut on offensive doctrine phases. The specialist already carries cut_line. Heavies "ignored" the throw by refusing the second latch. That was a flag, not mass.

WHAT I CHANGED   I took the admission flag off. A heavy takes the rope. The clothesline ΔV is now the coupled share, so the heavy keeps the helm and the light is yanked. Yara No-Cut still cuts on interceptor strike. I did not give anyone a gyro, a module, or hit-point armor.

WHAT YOU WILL FEEL   Bridle two lights and an ace on a strike pass cuts the line. Bridle a light to an Anchor Controller and the heavy barely moves while the light loses the stick. The specialist still carries the cutter as a doctrine verb, not a fitted toy.

THE NUMBERS      bar | before | after | target
                 ace cut | leftover specialist_cut only | ace_cut on live interceptor strike, seed 31002 | one cut
                 heavy admission | leftover heavy_endpoint_resists, no rope | rope active, seed 31002 | one ignore
                 heavy helm | leftover never formed | no tumble on 420 kg | ignore by mass
                 light helm | leftover n/a | light tumbles | yanked
                 Rapier ignore | leftover n/a | heavy 6.46 wu/s vs light 79.54, ratio 0.081, seed 31002 | mass ratio 16/420 = 0.038
                 specialist cutter | leftover cut_line, no module | same | specialist carries a line-cutter
                 scenario | pq-031-02-npc-counterplay | seed 31002 | headless

CHECKS           npm run check:baseline | entry 12/15, exit 12/15, same three reds
                 npm run check:massline | PASS 26/26 including check:massline:heads
                 node --test test/pq-031-02-npc-counterplay.test.mjs | PASS ace_cut + ignore ratio 0.081

THE FRAMES       Headless. No headed GPU strip. A blind reviewer naming a threat from silhouette is not this leaf's mechanical half.

UNPROVEN         Silhouette legibility of ace vs heavy from the shipping camera. I did not capture a still or a strip. The current law takes a looked-at still over a stored strip; that is a controller vision pass. tumbleStates still reads the process COMBAT_FLAGS map.
```

## Files

- `src/systems/tetherGameplay.js` — removed the NPC-heavy admission flag; catch ΔV is the coupled share; ace cut leftover kept
- `test/pq-031-02-npc-counterplay.test.mjs` — seed 31002 ace cut + physics ignore
- `test/massline-twin-bridle-head.test.mjs` — leftover ignore now forms the rope so check:massline:heads stays honest

Not edited: `modules.js`, `masslineSnares.js`, `masslineThreats.js`, `program-queue.json`, `NOW.md`.
