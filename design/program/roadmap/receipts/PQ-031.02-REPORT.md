<!-- LIFETIME: RECEIPT -->
# PQ-031.02 — NPC counterplay

```text
STATUS DONE

WHAT I FOUND     The leftover cut and ignore already lived
                 in tether gameplay and the leftover head
                 test. One specialist cut. One heavy ignore.
                 Hitch already has an S utility; the head is
                 M. Aces already cut on leftover identity.
                 The leftover specialist already owns the
                 cut_line plan. There was no receipt.

WHAT I CHANGED   I kept that leftover pair. I did not rewrite
                 the throw, the pair solver, or the NPC cut.
                 This receipt is new.

WHAT YOU WILL FEEL   Throw a bridle on two lights and a
                 tether specialist walks through the line
                 and cuts it. Throw it at a heavy and the
                 line never takes. Hitch never wears this
                 toy.

THE NUMBERS      bar | before | after | target
                 cut | leftover 1 specialist_cut | same 1 | one cut
                 ignore | leftover 1 heavy_endpoint_resists | same 1 | one ignore
                 ace cut | leftover ace_cut on named aces | same leftover | aces cut bridles
                 specialist cutter | leftover plan verb cut_line | same | specialist carries the cutter
                 Hitch fit | S utility vs M bridle | same refuse | Hitch cannot fit the M bridle
                 scenario | leftover specialist/heavy harness | headless | leftover test

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The leftover ignore is the admission gate
                 the throw already calls, not a second-press
                 latch in the same harness. Ace cut is
                 leftover-wired and not in the leftover
                 scenario. The cutter is the leftover
                 specialist plan, not a fitted line_cutter
                 module. The cut is an injected doctrine
                 phase on a fake attachment, not a flown
                 default-route chase.

NEXT             Integrator updates the queue. Do not mark
                 the queue from this receipt.
```

## Files

- `design/program/roadmap/receipts/PQ-031.02-REPORT.md` — this receipt

Already live, not rewritten: `src/systems/tetherGameplay.js` (`_handleNPCBridleCounterplay`, `heavy_endpoint_resists`, ace identity), `test/massline-twin-bridle-head.test.mjs` (one cut, one ignore, Hitch refuse), `src/ai/specialistPlans.js` (`tether_control_raider` / `cut_line`).

Not edited: `modules.js`, `weapons.js`, `input.js`, `sg02DynamicBodyOwner.js`, `tumbleStates.js`, `program-queue.json`.

## Honesty gaps

- The leftover ignore is `validateTwinBridlePair` → `heavy_endpoint_resists` on a moving NPC ship (`field_anchor_controller`, mass 180, leftover floor 150). The throw path already calls that gate before create. The leftover scenario does not step a second latch against the heavy.
- Ace cut is leftover-wired (`isNamedAceEntity` → `ace_cut` on leftover offensive phases, range 220). The leftover scenario only pins the specialist `attach_window` cut. A fling ace that also carries `tether_control_raider` takes the specialist path.
- "Carries a line-cutter" is leftover specialist plan `verb: 'cut_line'` on `tether_control_raider` (range 180). There is no fitted `line_cutter` module. This leaf did not edit `modules.js`.
- The leftover cut is an injected `ai:doctrinePhase` on a fake attachment create. It is not a default-route flown chase. PQ-140.02 `specialistCounterplay` still cuts ordinary `state.player.tether`; Twin Bridle leftover cut is the tether-gameplay listener on a player-controlled bridle.
- Hitch cannot fit the M head: leftover S utility vs `mod_twin_bridle_m`. Leftover pin is the sibling head test. This leaf did not retune fittings.
- No shipping-camera strip. The job forbade headed capture.
- This leaf did not retune 031.00 / 031.01.

## Proof

cut=1 (`specialist_cut`, verb `cut_bridle`, role `specialist`). ignore=1 (`heavy_endpoint_resists`). Hitch refuse.

`node --test test/massline-twin-bridle-head.test.mjs` — 8 pass, 0 fail.
