<!-- LIFETIME: RECEIPT -->
# PQ-148.00 — Pods are bodies

```text
STATUS DONE

WHAT I FOUND     Jettison already left a colliding persistent payload.
                 The named proof already shoves that pod into a Wasp
                 on Rapier and round-trips it through save serialize.

WHAT I CHANGED   Nothing in the live cargo path. This receipt records
                 the proof that was already on the tree.

WHAT YOU WILL FEEL   Dump ore and it is a crate with mass. Shove it
                 and a light hull moves. Continue and the crate is
                 still there. It is not a pickup with a timer.

THE NUMBERS      bar | before | after | target
                 body | colliding persistent payload | same | not a TTL pickup
                 save persist | _serializeEntities keeps the pod | same | Continue
                 ore shotgun | ≥ 30% Wasp cruise (seed 14802) | same | ≥ 30%
                 scenario | test/pq-148-00-pods-are-bodies.test.mjs | seed 14802 | Rapier

THE FRAMES       Headless Rapier shotgun. No headed GPU strip.

NEXT             Integrator updates the queue. Leftovers: .01 volatile,
                 .02 smuggling, .03 named cargo (tests already exist —
                 characterize before inventing). Do not mark the queue
                 from this receipt.
```

## Files

- `test/pq-148-00-pods-are-bodies.test.mjs` — named proof

Already live: `lootShards.js`, `cargo.js`, `jettisonImpulse.js`.

Not edited: `lawSecurity.js`, `program-queue.json`.
