<!-- LIFETIME: RECEIPT -->
# PQ-148.01 — Volatile cargo classes

```text
STATUS DONE

WHAT I FOUND     Jettison already stamps three real hold lines onto
                 colliding pods. Slam, contact, and throw already do
                 three different things. One catalog, one loot path.

WHAT I CHANGED   Nothing in the live cargo path. This receipt records
                 the proof that was already on the tree.

WHAT YOU WILL FEEL   Dump fuel cells and a hard slam shoves nearby
                 hulls. Dump ice volatiles and a scrape bites the
                 hull that touched it, not a distant one. Dump
                 platinoid and the crate barely leaves your stern
                 and leans into a well. Amber, green, violet lamps
                 and three silhouettes. Customs cones and named
                 owners are not this leaf.

THE NUMBERS      bar | before | after | target
                 classes | 3 live (fuel / volatiles / platinoid) | same | 3 distinct
                 explosive slam Δv (seed 14811) | 38.671 WU/s | same | nearby hull moves
                 radial impulse | 2455.7 | same | published blast, radius 80
                 corrosive hull tick (seed 14812) | 5.980 | same | contact bite
                 far hull | 0.000 | same | not an aura
                 superdense throw (seed 14813) | 30.000 vs iron 60.000 | same | cannot throw far
                 field pull | 188.657 vs iron 75.463 | same | pulls on wells
                 lamps | amber / green / violet | same | readable by lamp
                 scenario | test/pq-148-01-volatile-classes.test.mjs | seeds 14811–14813 | Rapier

THE FRAMES       Headless Rapier slam / contact / well. No headed GPU strip.

NEXT             Integrator updates the queue. Leftovers: .02 smuggling,
                 .03 named cargo (tests already exist — characterize
                 before inventing). Do not mark the queue from this receipt.
```

## Files

Not edited this turn. Already live and proved:

- `src/data/commodityVolatileClasses.js` — three classes, three lamps, three silhouettes
- `src/systems/lootShards.js` — slam impulse, contact hull tick, field pull, throw scale
- `src/systems/cargo.js` — jettison uses the throw scale (not edited)
- `test/pq-148-01-volatile-classes.test.mjs` — named proof

Not edited: `fragileCargo.js`, `commodities.js`, `lawSecurity.js`, `heat.js`,
`program-queue.json`, `NOW.md`.
