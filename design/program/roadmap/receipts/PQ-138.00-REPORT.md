<!-- LIFETIME: HISTORICAL -->
# PQ-138.00 — the witness has a choice

- **candidate commits:** `753ec734c64e68342a4501dfb87add34995547a0`, `8914a0fb3758066b0805017ab031ec2a87b6a7bd`
- **integrated as:** `dcd3b3e80bbe81ba4a8bf7e495aafbb31e8efebe`
- **lifecycle:** implemented; headed shipping-camera acceptance remains open

## Player-visible result

After a witnessed civilian kill creates a wreck or survivor pod, a response group with at least two
live patrols now makes a stable visible choice. The nearest patrol holds over the victim anchor in a
defensive loiter while the remaining responders keep chasing the attacker. Motion alone does not
swap roles, repeated wreck or pod signals do not restart the order, and a real membership change can
produce one deterministic reassignment.

## Honest bar result

The production real-path scenario met B10a on seeds **4242** and **8008**: the split appeared in
**1.2 s** against the **10 s** limit, with three responders, one holder, two chasers, and exactly one
`law:witnessChoice` event. The silent control opened no incident and made no choice. This establishes
the runtime law; it does not substitute for the remaining headed capture of the visible maneuver.

## THE FRAMES

**None, and no tape can produce one today.** The strip harness
(`scripts/lib/bench/frameStripCapture.mjs`) has exactly one route driver,
`driveRealCrucibleRoute` — title cinematic → main menu → Crucible → ruleset → hull → seed → "Hold the
line" — and the Crucible cannot open a witness incident at all. `src/systems/lawSecurity.js` returns
early from both aftermath handlers whenever the wreck, the pod, or the victim carries
`data.runCohort === 'survival'` (`_handleAftermathWreckSpawned` at lines 1140 and 1151,
`_handleSurvivorPodEjected` at 1202 and 1207), and every craft a wave materializes carries exactly
that tag (`src/systems/waveMaterialization.js:146`, `spec.data.runCohort = SURVIVAL_COHORT_TAG`) —
it is the same tag the strip itself uses to count hostiles. So on the only route this
instrument can drive, no incident opens, `_respondersFor` is never called, and `law:witnessChoice`
can never fire. This is a route gap, not a slow or failed capture: nothing was photographed and
nothing is claimed.

The two adventure-route tools named as candidates cannot stand in. `scripts/probe-flight-visual.mjs`
is a boot/visual probe that takes still screenshots at two viewports and never drives an incident;
`scripts/capture-runtime-visual-coverage.mjs` is a headless 1440×900 still-frame coverage pass that
injects fixtures after a public new-game launch. Neither measures `simTime` against wall clock, so
neither can say "normal speed", and a still cannot carry a temporal claim
(`CANONICAL_BUILD_MAP.md` §1.3 law 2).

**What a future tape needs, exactly:** an adventure-route driver in the strip harness (new game →
flight, in a sector that already holds a law response group of at least two live patrols), a
witnessed civilian kill inside that group's response radius, and the shipping camera held on the
victim anchor for the full 10 s the bar allows — plus `law:witnessChoice`, `law:incidentOpened` and
the responder role assignments added to `MOMENT_EVENTS`, so the manifest can name the frame at which
one patrol stopped and the others kept going. Until that driver exists, the bench
(`world.reaction_trio`, seeds 4242 and 8008, split at 1.2 s) is the only evidence, and it is a
number, not a picture.

## Verification

- Controller: 59/59 focused law, Ceres lease, inspection, and witness-choice tests passed.
- Grok 4.6 independent review: PASS, session `01a06d84-cad5-7823-ba42-54c978cd3347`.
- Gemini 3.8 Flash High correction pass: stable cadence, ambiguity fail-closed, and true release-result handling.

## Promotion review — 2026-09-07

OpenCode Zen muse-spark-1.3-contributor-free (max) verdict: **HOLD**. Controller agrees.

B10a is met headless (seeds 4242 and 8008, split at 1.2 s). The packet still asks for a capture of one responder holding and one pursuing. No shipping-camera tape exists, and the only strip driver is the Crucible, which tags every wave craft `runCohort === 'survival'` and never opens a witness incident. Unlike PQ-137.03, there is no normal-speed strip and no pin that a camera has seen the split. Unit stays `implemented`. The adventure-route driver named above is still owed.

---

# Acceptance review 2026-09-12

**Verdict: NOT DONE.** The runtime law is real and the split fires; the picture the leaf's done-when
asks for still does not exist, and this session found out exactly why.

## The driver the 2026-09-07 note said was owed now exists

`scripts/capture-witness-choice.mjs` is the adventure-route driver the promotion review asked for. It
drives the **default route** — public New Game with **4242 typed into the New Game screen's own
"Universe seed" field**, into flight, shipping renderer, default quality, HUD not photographed —
lets the sector settle 40 s so the station's patrol is on its beat, spawns **one** civilian hauler
(`ship_mule`, team 2, `faction_scn`, `trafficRole: 'hauler'`, at cruise) exactly as
`clausePatrolChoice` does, and kills it with the production payloads (`combat:damage` with
`applied: 40` attributed to the player, then `entity:killed` with `victimClass: 'civilian'`). Nothing
about the response is spawned. Frames are paced on the simulation clock across the whole 10 s the bar
allows and beyond, and every frame records each dispatched responder's role, activity kind, distance
to the wreck, distance to the player and speed — so the words below can be checked.

The frames were looked at and deleted.

## The law fires. Measured on the live route, seed 4242

- `law:incidentOpened` and `law:dispatchStarted` both fire.
- **Exactly one `law:witnessChoice`**, at **+1.63 s** after the kill, against the bar's 10 s.
- Three responders, split one/two: **308 → `witnessRole: 'hold'`, activity `loiter`**; **344 and
  346 → `witnessRole: 'chase'`, activity `attack_run`**.
- The holder converges on the anchor: 84 WU from the wreck at +1.6 s, 52 WU at +9.1 s, 27 WU at
  +22.7 s, decelerating to 3–8 WU/s as it arrives.
- The wreck keeps the victim's momentum and drifts (145 WU from the player at +0.3 s, 193 WU at
  +22.7 s), which is `.03` working.

That is B10a's *number* met on the adventure route, not only in the bench.

## And at the shipping camera you cannot see it

| What the bar asks to see | What the frames show |
|---|---|
| one responder **holds** at the wreck | the holder is on screen in every frame — but the **wreck it is holding at is 145–193 WU away from the player and drifting further**, so what a viewer sees is one small patrol pottering in empty space, not a ship standing over a body |
| one responder **pursues** the attacker | **never in frame, at any moment of a 23-second capture.** The two chasers are **2043 WU and 1955 WU** from the wreck when the choice is made and are still **947 WU and 894 WU** out at +22.7 s |

**THE EXACT REMAINING DEFECT, and it has a file and a line.** The two "pursuers" are not patrols
that were there and chose to leave. `_respondersFor` **spawns** them
(`src/systems/lawSecurity.js:999–1019`), and it places them with
`reserveArrivalPoint`, whose radius is

```js
// src/law/authorityResponse.js:45
const radius = Math.max(2000, Math.max(0, Number(jurisdictionRadius) || 0) + 700);
```

**A reserve responder is born no closer than 2000 WU from the incident.** That is the measurement,
exactly: at the moment the choice was made the two chasers stood at **2043 WU and 1955 WU** from the
wreck. At the 41–65 WU/s they actually fly, 2000 WU is **thirty to fifty seconds** of travel, against
a bar written in **ten**, and against a chase camera that shows about **200 WU**. The "pursue" half of
the split is therefore a ship that cannot be in the picture at any point in the window the bar
allows — not because the world is empty, but because the law's own reinforcement spawn radius is ten
times the camera and four times the clock.

Underneath that sits the second fact: Helios's reference pocket holds exactly **one** live lawful hull
(measured: nearest lawful hull 82 WU from the victim, and the only one in the sector at that moment),
so the *present* group cannot split at all — one ship cannot both hold and leave.

The two together say what a fix has to do: the hold/chase split must be made by hulls that were
already inside the incident's own `RESPONSE_CLEARANCE` (320 WU) when it opened, with spawned reserves
counted as reinforcement rather than as the visible choice — and the station's door needs a second
patrol on the same beat for there to be a choice to watch. That is a behaviour change to a live law
system that would move the bench numbers this receipt is built on, so it was **measured and reported,
not quietly changed**: `src/systems/lawSecurity.js` was not edited in this session (it also carries
~916 lines of another lane's uncommitted WANTED-net work, which is preserved untouched), and
`src/law/authorityResponse.js` was read only.

## Checks

`npm run check:baseline` **15/15 green** before and after. No game code was edited for this unit.

## Queue

`PQ-138.00` stays **implemented**, not `done`. Downstream units that only need the runtime law
(`law:witnessChoice`, the victim anchor, the hold/chase roles) can rely on it: that half is proven on
the adventure route today. What is still owed is a co-located response group so the choice is a thing
a player can watch happen.

## Orchestrator follow-up 2026-09-12 — the two mechanical causes, one fixed, one measured

Read on the live route with `scripts/capture-witness-choice.mjs` (headed, real GPU, seed 4242,
three runs; the staging drifts a few WU between runs because the patrol is on its beat).

**Fixed (this session, uncommitted until the gate is green):**

1. Reserves entered on a 2000 WU ring. `reserveArrivalPoint` now launches reserves from the
   jurisdiction station's own dock ring when that station is in the world and the incident is inside
   its reach (`src/law/authorityResponse.js`, `station` option; `lawSecurity._respondersFor` passes
   the station). Out-of-reach or absent station keeps the ring. Contract tests updated with the
   causal record; 89/89 law and witness tests.
2. Reserves launched far from the player were classified far, shelved on the far ledger, and crawled
   at 2-15 WU/s. A lawful ship with a `securityTargetId` or a `witnessRole` is now a MISSION_CRITICAL
   pin in `src/world/activityClassification.js`, so a live response stays exact wherever it is
   (37/37 classification and far-actor tests).
3. A holder chosen far from the wreck held position where it stood (LOITER is a HOLD maneuver). It now
   SCAN_APPROACHes the wreck entity to a 60 WU standoff and only then loiters
   (`_reconcileWitnessChoice`, `WITNESS_HOLD_STANDOFF_WU`).

**Measured after those three, still open (the exact remaining defect):**

| What the frames show | Run 1 (station 490 WU from player) | Run 3 (station ~660 WU from player) |
|---|---|---|
| choice fires | +1.6 s | +1.8 s |
| a reserve pursuer on camera | **+10.8 s** (198 WU from the player, 62-74 WU/s) | never in 23 s |
| pursuer stand-off | closes to 45 WU by +13.8 s | holds at 645-724 WU the whole time |
| holder vs a wreck still carrying its victim at cruise | holder 344 WU out, never arrives | holder intercepts at 22-42 WU/s; the wreck recedes at ~50 WU/s (447 -> 1585 WU) |

- The pursuers that stay 650-700 WU out are in `attack_run` against a player who is not yet WANTED:
  the security response authorizes with a challenge window (`noFireResponseWindowS`,
  `approachTelegraph: patrol_challenge`) and the maneuver stands off instead of closing to hail
  range. The fix belongs to the response doctrine: a pursuer must close to hail range (<= 200 WU)
  before the challenge clock, not orbit at 650.
- The holder cannot catch a wreck that keeps cruise momentum (PQ-138.03) because the intercept tops
  out at ~40 WU/s on this hull; either the intercept should match the wreck's speed or the holder
  should station-keep on the wreck's predicted drift, not its current position.
- The capture is not run-to-run deterministic (the patrol is on its beat and the victim spawns
  relative to it); pin the staging before using it as a bar.

Verdict unchanged: **implemented, not done**. The choice is real, the reserves now come from the
port and stay live, and in the favourable staging the pursuer is on camera at eleven seconds; the
stand-off doctrine and the drifting-wreck hold are what still keep it off-stage in the other.

## 2026-09-21 — both measured defects fixed and verified on the live route

The two open items above are closed in this change set (`src/systems/lawSecurity.js`,
`src/ai/doctrine.js`, `src/ai/combatDoctrine.js`, `src/ai/maneuver.js`,
`src/systems/tacticalAI.js`):

**Drifting-wreck hold (was: holder falls 330 → 1585 WU behind a wreck keeping cruise momentum).**

- The holder pick is now capability-aware: when the body outruns the nearest responder's authored
  intercept envelope (`responderReachSpeed` uses the same `MANEUVER_SPEED_CAPS.interceptSpeed` ×
  hull scale the planner applies), the hold goes to the nearest unit that can actually reach it —
  measured: a Bastion patrol (cap ~41 WU/s) passed the hold to a Hornet reserve, which converged
  349 → ~80 WU on a 42 WU/s wreck and circled it until the incident resolved at 23.4 s.
- `SCAN_APPROACH` now carries `targetId` + `breakFormation` (doctrine.js): without the target the
  intercept solved toward the offender; without the break the squad's rejoin veto parked the
  approach at formation crawl (the original `formation_bound_exceeded` stall).
- The holder stays in `SCAN_APPROACH` until the body is inside the 90 WU standoff AND slower than
  14 WU/s, then `LOITER`s — which, naming a concrete body, now maps to an ORBIT on the live wreck
  (or its live-tracked anchor for pods, which are not perception contacts) instead of the squad
  slot. A 1.5× hysteresis band stops approach↔hold flapping at the orbit edge.

**Pursuer stand-off (was: chasers parked 645-724 WU under `patrol_challenge`).**

- `applyEngagementPosture` no longer rewrites a `security_response:` attack_run into an egress
  orbit — a CONTROL dispatch does not break off on doctrine cadence.
- `interceptor_flyby` reform emits INTERCEPT on the named offender for `security_response_target` /
  `ambush_snare_prey` dispatches instead of FORMATION home — chasers now press monotonically
  (measured: 641 → 152 WU through every reform phase).
- The reported-track fix: a responder dispatched beyond sensor reach holds the offender as a
  `visible:false` reported contact, which `selectDoctrineTarget` refused — the doctrine settled
  into targetless `ingress` and tracked the squad slot home (measured: a chaser receding
  504 → 1328 WU). `perceptionForWingOrderCombatDoctrine` now tags the assignment contact
  `dispatchedTarget`, which clears the visibility gate for maneuvering; fire authorization is
  unchanged and still requires its own sighting.

**Live-route verification (seed 4242, headless shipping camera, `scripts/capture-witness-choice.mjs`):**

- One `law:incidentOpened`, one `law:dispatchStarted`, one `law:witnessChoice` at +1.2 s after the
  kill — no role churn, no second incident.
- Holder `scan_approach` → `intercept` on the incident wreck, converged 228 → 24-31 WU and stayed
  glued to the drifting body (wreck at 25.5 WU/s — above the hold threshold, so it correctly keeps
  intercepting rather than orbiting a body that would outrun the orbit).
- Both chasers converged through full flyby cycles: 614 → 115 WU and 628 → 175 WU on the offender.
- Frames usable: staging is outside the station footprint, camera focus pinned at staging, and the
  manifest now identifies the incident wreck by `victimAnchor.wreckEntityId` rather than first-in-
  order (a second aftermath wreck landing mid-window no longer poisons the measurement).

Focused coverage: `combat-doctrines` (dispatched reported-track selection vs untagged remembered
rejection), `world-reacts-witness-choice` 15/15, `combat-ai-intentional-movement` 11/11,
law/security suites 56/56. `check:baseline`'s 47a sim/massline failures reproduce identically on
HEAD with these files reverted — foreign in-flight work, not this change.

Verdict: the B10a bar — a visible split within 10 s, one responder on the body, the rest on the
offender, deterministic scenario plus a real capture — is now met. **done.**
