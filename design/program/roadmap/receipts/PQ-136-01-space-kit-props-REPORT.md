<!-- LIFETIME: HISTORICAL -->
# PQ-136.01 — Everyday space kit placement receipt

- **packetId:** PQ-136
- **leafId:** PQ-136.01
- **candidateCommit:** (the commit carrying this receipt)
- **disposition:** PASS (all loader-legal props placed; the measurement corrected the premise)
- **acceptance:** focused_green

## The measured truth (supersedes the admission's "30 unused")

The pack is 62 files / **46 identities**: 16 previously-selected props exist as production release
bodies (the "32 referenced" counted their production twins), and the remaining **30 are source-only
identities with no release body and no identity extras** — they fail the fail-closed authored
loader, the same class as the wreck pack. Nothing was stamped or forced through (the wreck-pack
mistake was not repeated); they are recorded refused and routed to the promotion leaf (PQ-136.00's
re-shaped pipeline job covers both packs).

## What changed

The 16 loader-legal props now actually appear in ordinary sectors through the EXISTING
world-dressing spawn path (no second path): station props by stations, lane props by gates, work
props by asteroid fields; core sectors station+lane, belt station+work, fringe all three. Bounded
4–6 extra non-solid dressing objects per sector, admitted and released with the sector (no
permanent residency growth). Deterministic private RNG stream so Save/Continue and other dressing
do not shift. Variety: without-replacement selection with an asserted floor of 10 distinct props
across eight seeds — the test fails if the picker collapses to the old three decorations.
Ceres/Tethys still skip the two IDs an earlier still-panel ruling forbids.

## What passed (controller-verified)

`test/pq136-everyday-space-kit-placement.test.mjs` 4/4 (incl. the guard that the thirty stay
unstamped and unrouted); mutation: removing one routing row turns three tests red;
`check:asset-reachability` green at 276 (the placed bodies were already retail-counted — the
placement, not the routing, is the new player value); both 47-A goldens unchanged; LF endings.

## Residual

The 30 source-only identities need the canonical release-pipeline promotion (shared with the wreck
pack's 44) before they can ever reach a camera. One pipeline job covers both.
