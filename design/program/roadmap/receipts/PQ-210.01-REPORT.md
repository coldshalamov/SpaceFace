# PQ-210.01 — A fight fits the frame: 60 fps with ten hostiles — REPORT

RESULT: PARTIAL — propulsion half implemented + integrated; HUD half implemented on branch,
integration HELD behind the live `zcode-inf-56-60` lane that owns `src/ui/hud.js`.

## What landed (master)

`src/core/flight/propulsionCatalog.js` (branch commit `3ac1df595`):

- Per-entity memo (`MERGED_PROFILE_CACHE` WeakMap) for the partial-authored merge in
  `resolvePropulsionProfile`, keyed by entity with reference comparison of `authored`/`base` — a
  fitting rewrite or role/mass re-inference invalidates cleanly (`inferProfile` returns the frozen
  shared catalog profiles, so base references are stable and the memo actually hits).
- `normalizeProfile` identity fast path: an object stamped with the current schema version and a
  valid family/id/label/solverSpeedLimit returns by identity instead of re-spreading every tick.
- Module-level `DRIVE_FAMILY_SET` replaces a per-call `Object.values().includes`.
- Effect: downstream WeakMap caches (player feel, cruise) keyed on profile identity hit for
  merged-profile NPCs for the first time; three spread objects per entity per tick stop.

## What is ready but held (branch `pq210-frame`)

`src/ui/hud.js` (`fbb182073`): transition-only DOM writes for the lock ring (edge trackers +
`clearAttr`, which also fixes a real pre-existing correctness bug — `removeAttribute` bypassed
`setAttr`'s cache, so re-applying the pre-clear value was silently skipped) and the settle-motion
fast path (`settleMotion.motionReduce` short-circuits `prefersReducedMotion`'s per-gauge
classList/matchMedia fallback — one fresh MediaQueryList per gauge per frame). First-use hint
writes go through the cached `setText`/`setHidden` helpers. `test/pq-210-01-frame-budget.test.mjs`
(`7f21014a4`, 5/5 green) pins both contracts. Integration: as soon as the `zcode-inf-56-60` row
releases `src/ui/hud.js`, cherry-pick `fbb182073` + `7f21014a4`.

## Measured (controller-run probe:smooth-flight:crucible, this machine, 20 s window)

| state | fps mean | callback ms (sim) | frames over 33 ms | hitch callbacks | game speed |
|---|---|---|---|---|---|
| base `833b22f65` | 14.8 | 38.5 (16.7) | 87.6 % | 91 of 304 | 63.2 % |
| +propulsion (A1) | 32.6 | 22.3 (8.9) | 32.7 % | 4 of 683 | 88.9 % |
| +hud (B3, quiet machine) | 41.8 | 16.9 (6.1) | 17.5 % | 1 of 843 | 91.2 % |
| A re-run (A3, quiet machine) | 45.3 | 15.7 (5.3) | 10.9 % | 2 of 911 | 89.7 % |

Base → A is mechanism-aligned (sim callback halved by the allocation memo) and far outside
run noise. A↔B delta is within machine noise on this host (an early B "regression" at 24.1/17.9 fps
was reproduced as host contamination: an identical-code A re-run read 44.2 ms callback in the same
noisy window). Honest reading: the HUD half is neutral-to-positive on clean readings, carries an
independent correctness fix, and is kept. The unit's absolute exit bar (60 fps at the busy reading
on the OWNER'S iGPU) is NOT claimable from this host; the relative callback reduction is the
evidence this machine can produce. Queue brief's remaining suspects for follow-up: the 22
always-on point lights (`src/render/weapons/weaponLights.js` — owned by the live VFX campaign's
weapons lane; controller-forbidden here) and wave-spawn sim frames (`survivalArena.js` — foreign
dirty lane). The CPU profile named `getAttribute` chains in HUD kit internals and per-frame HUD
attribute churn as the top owners alongside sim allocation.

## Provenance

Delegated: Devin CLI `swe-2-max` (`--permission-mode smart`, required for shell tools —
`accept-edits` rejects command execution and cannot run probes), isolated controller worktree
(`.worktrees/pq210-frame`, base `833b22f65`). Devin produced both implementations with careful
semantic verification (WeakMap key safety, `normalizeProfile` field coverage, motion-truth-source
equivalence) but its per-change probe plan died when the permission layer rejected its
`git stash` call; the controller took over measurement, committed the candidate in three
reviewed commits, and ran the six-probe attribution matrix. A lingering Devin child process
applied the four helper `export` keywords and committed the focused test (`7f21014a4`) — reviewed
and accepted by the controller. The unit did not self-certify: the controller reproduced every
check and owns the probe gate (workers were forbidden GPU probes for contention).

## Integration

`3ac1df595` cherry-picked to master (with `-x`). `fbb182073` + `7f21014a4` remain on branch
`pq210-frame` for immediate cherry-pick when `src/ui/hud.js` frees. Queue close-out left to the
finish-game fleet lane (`program-queue.json`).
