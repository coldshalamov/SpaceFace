<!-- LIFETIME: RECEIPT -->
# PQ-169.01 — Ghosts

```text
STATUS DONE

WHAT I FOUND     A shared Crucible run already wrote a pose tape, hashed
                 it, and played it back as a see-through hull. Two
                 machines that held the same tape already got the same
                 number. Nothing new was missing on seed 16910.

WHAT I CHANGED   Nothing in the live Ghost path. This receipt records
                 the proof that was already on the tree. The named
                 proof is test/pq-169-01-ghosts.test.mjs.

WHAT YOU WILL FEEL   Finish a Crucible run and the next time you pick
                 that seed, Ghost offers to race the last recorded hull.
                 It is translucent and does not shoot, ram, or write
                 campaign money. There is still no online share and no
                 Steam board.

THE NUMBERS      bar | before | after | target
                 same tape JSON, two storages, seed 16910 | 476914635 | same | identical uint32
                 settle + reload + second storage JSON | hash 476914635, 3 frames | same | persists
                 arm that hash, pose at tick 6 | x=6 z=0 r=0.5 | same | sample hit
                 pose between ticks 0 and 6 | x=3 r=0.25 | same | interpolates
                 change one sample | n/a | hash differs | different
                 live survival start with queued hash | tape hash 476914635 | same | arms
                 campaign credits / weapons / Rapier on ghost | n/a | none | not a combatant
                 hull opacity | 0.32 | 0.32 | translucent
                 scenario | test/pq-169-01-ghosts.test.mjs | seed 16910 | headless

THE FRAMES       Not a camera leaf. The claim is a shared hash and pose
                 playback. Headed Chromium skipped.

NEXT             Integrator updates the queue. Leftover: .02 weekly
                 mutators are already on the tree — characterize before
                 inventing. .03 hangar feed is already closed as not
                 needed. Do not mark the queue from this receipt.
```

## Files

- `test/pq-169-01-ghosts.test.mjs` — named proof

Already live: `src/systems/survivalRecords.js` (`ghostHash`, pose tape, `ghosts.byHash`),
`src/systems/survivalRun.js` (record + arm on survival start), `src/systems/survivalMutators.js`
(queued ghost hash), `src/render/crucibleGhost.js` (translucent hull), `src/ui/screens/crucible.js`
(Ghost door).

Prior hyphen receipt: `design/program/roadmap/receipts/PQ-169-01-REPORT.md`.

Not edited: survival owners, `program-queue.json`, `NOW.md`.
