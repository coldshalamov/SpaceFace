DRAFT — PQ-033.02 report. Numbers marked [TBD] fill from the browser/electron soak evidence
envelopes and `npm run check:pq033:min-spec` output when the runs complete. Do not ship this
file with [TBD] markers still in place.

2026-09-19 addendum (devin-desktop): the soak's cycle-42 death is fixed at the root — a cap-refused
convoy record left a phantom npcJobs entry behind, so the save grew ~+4 KB per save/load roundtrip
until localStorage refused the quick save at 5 MB. Measured and verified below. A first full
acceptance run then reached cycle 151 (vs 42) and died on a different seam: the hull left the berth
facing station structure, so full thrust was zero motion and the driver called it stuck — the driver
now turns (public KeyD) before failing, and a second run is in flight. The acceptance soaks
themselves are still the open item: the browser run is in the frozen worktree
`.devin-tmp/pq033-soak-2026-09-19` (soak-candidate commit `4673b7522` (runs 2/3 base) and `c79a5b6e6` (run 4: known-good base
`94f38d342` + both fixes); the Electron host is prepared at `56949dfc1` (the same game code plus two
harness-only fixes: CSP-safe polling handle parity and a bounded tick-freeze retry, `9d7b61180`).), and the Electron host has
never produced evidence. The world.js fix and its test are in the working tree but NOT published to
master: `src/systems/world.js` carries three other lanes' uncommitted hunks (field regrowth — which
also needs the uncommitted `src/systems/fieldDepletion.js` —, membership hysteresis, scan compass),
and this repo publishes partial work with pathspecs only, so the file rides its next legitimate
commit (same rule PQ-022 followed on 2026-09-17). The driver fix IS published (`10935f3f8`).

---

DONE  PQ-033.02 — The named minimum-spec GPU now has written floors, a two-hour public-route
soak on each host proves the game holds them, and the save/load round-trip is exercised end
to end at scale.

WHAT I FOUND     The release matrix named a target — 60 fps median, ≤1 gameplay hitch per
minute over 50 ms, bounded transition stalls, 8 GB footprint — but nothing measured it: no
spec named the GPU, no harness ran the loop, and no check recomputed the floors from raw
evidence. The soak then exposed real defects the floors exist to catch: cold-cache first-draw
stalls (multi-second `bloomScene` bricks while authored textures/programs uploaded inside
presented frames), a shadow-depth admission path that could never work (a standalone
`shadowMap.render` with no live render state), an autopilot limit cycle that orbited the dock
berth at ~42 WU/s forever, and a save/load path that grew a duplicate generation of persistent
actors across cycles.

WHAT I CHANGED   `build/min-spec.json` names the floor hardware (Intel Core Ultra 7 155U
integrated graphics, Iris Xe class or better) and the floors themselves; `minSpecFloors.mjs`
recomputes every floor from raw soak evidence and `check:pq033:min-spec` fails closed on
missing or forged evidence. The browser and Electron soaks drive the public route — new game,
authored flight, public input, map waypoint, undock, F5 save, F9 load, economy restore,
redock, market trade — for ≥200 cycles and ≥120 minutes each, hitch-classified by the game's
own attribution (external-scheduling noise is recorded but excluded from the gameplay floor).

The fixes the soak earned:
- The convoy job bag is bounded by the record ledger. `_captureSectorDurableRecords` refuses a
  first-time convoy record once 24 are alive in the sector and drops the hull's phantom
  `worldRecordId` — but the hull's npcJobs entry, keyed on that record id, stayed in
  `state.npcJobs.byId` forever. `npcJobsRuntime.deserialize()` restores every job VIRTUAL and
  `_tryRelink()` binds it by worldRecordId against a rematerialized hull, so a job whose record was
  never persisted can never re-link: the bag grew ~+4 jobs and ~+4 KB per save/load roundtrip until
  localStorage's 5 MB quota refused the quick save at soak cycle 42. The refusal now releases the job
  in the same breath as the phantom id; the hull itself stays live ambience under the ordinary
  traffic stepper (`traffic.js` keeps npcJobsRuntime as the movement writer only while
  `data.jobId` exists).
- GPU admission is sealed against first-draw work: shader compilation and texture/geometry
  residency are chained behind one refcounted pending latch per subject; bloom hides
  never-compiled drawables and routes them through admission instead of letting them link
  inside a presented frame; shadow-depth variants warm via a real render of a staged
  casters+key-light scene into a scratch target.
- The dock-berth autopilot carries a sustained envelope brake: inside the berth stage with
  speed above the gate, counter-thrust holds until the hull is under the legal docking speed.
- Berth-stage autopilot guidance holds the manifest corridor lane: generic obstacle avoidance
  inside it steered the hull across a spar/ring primitive and the compound proxy expelled the
  ship back out of the dock envelope at ~46 WU/s (`dock:range` flickered true then false).
- The lane lock now requires geometric lane membership — a hull knocked off the corridor wedge
  keeps the mouth aim and exits through the ring gap instead of grinding structure — and
  lane-locked runs pace behind traffic squarely ahead instead of ramming it.
- A contact-held ship parked just outside the dock gate no longer waits forever for the berth:
  the capture lane's heading gate exempted only "nearly stationary" hulls under 8 wu/s, and a
  hull wedged on the station proxy reads a residual ~8 wu/s of solver noise — so the assist
  refused to pull it in and the dock prompt never came (soak cycle 503). The exemption now
  covers anything at or below the authored berth dock speed; a ship that slow inside the
  capture lane is docking traffic by definition.
- Save/load restores one generation of persistent actors; repeated cycles no longer duplicate
  entities or grow quota.
- Convoy world-records are bounded per sector at capture (the rematerialize/top-up ratchet
  minted ~8 fresh convoy identities per round trip — ~1 MB of save growth by cycle 94), and a
  recovery-backup write that hits the storage quota now sacrifices the backup instead of
  aborting the save: a lost backup beats a lost save.
- The soak driver itself hardened against observed state-dependent traps: search rows that only
  name-match 'Helios Station' include courseDisabled rumor/goal targets that resolve to no
  course (the click emits nothing — the prior cycle-12/22 failure signature); a restore that
  lands inside the dock envelope makes the station's primary action 'RETURN TO SHIP', not
  'Set Waypoint' — and the re-approach loop now nudges the throttle for in-envelope stalls
  instead of re-plotting a course that cannot exist; and a berth expulsion or early arrival
  still gets the bounded re-approach loop like a player would fly.

WHAT YOU WILL FEEL   First flight after a cold boot no longer bricks mid-frame while shaders
link and textures upload. Autopilot actually parks at the dock instead of orbiting it
forever. Long sessions stay stable — saves don't fatten and the world doesn't accumulate
ghost actors.

THE NUMBERS      floor | target | browser | electron
median frame | ≤16.7 ms | [TBD] | [TBD]
gameplay hitches >50 ms | ≤1/min | [TBD] | [TBD]
max transition hitch | ≤2000 ms | [TBD] | [TBD]
boot to menu | ≤10 s | [TBD] | [TBD]
heap growth | ≤30 MB/30 min | [TBD] | [TBD]
save/load cycles | ≥200 | [TBD] | [TBD]
soak duration | ≥120 min | [TBD] | [TBD]

MEASURED 2026-09-19 — the save-growth blocker (fast public-route loop: New Game → 26-30 × F5/F9,
one headed browser on the named min-spec GPU, `scratch-save-growth.mjs`). Provenance: this is a
session diagnostic (console output), kept because it isolates the mechanism; the re-checkable
artifact is the acceptance soak's own per-cycle `saveBytes` in its `evidence.json`, which lands with
the run below.
| quantity | before the fix | after the fix |
|---|---|---|
| quick-save payload, cycle 0 → cycle 24-30 | 287.5 KB → 404.7 KB (+117 KB) | 266-289 KB, flat (oscillates with live traffic churn) |
| `npcJobs` payload, cycle 0 → 24-30 | 6.9 KB → 102.3 KB | 1.0-7.6 KB |
| jobs with no ledger record (orphans) | 0 → 48 | 0 at every cycle |
| ledger alive convoy records | 24 (cap) | 24 (cap) |
The prior run's death signature (`saveErrors: [{slot:"quick", reason:"quota"}]`, `storageBytes`
5,227,939 at cycle 42) is what this closes.

Residual orphan sources (independent review, not fixed here — both need a long unsaved session):
`gcExpiredRecentMemory` can drop a RECENT record whose hull was captured before its job was assigned
(>180 s with no save/demote), and the 48-record per-sector bound can evict a RECENT record in the same
window. Neither is reachable on the soak route (it saves every cycle); the cheap hardening is to mark
a hull's record permanent at `assign()` time.

Residual behavior question (independent review + two live samples, 2026-09-19): a cap-refused hull
loses its job, so its movement falls to the ordinary traffic stepper. Measured over 10 s windows:
haulers/patrols/prospectors keep flying (4/5 and 8/12 still-present refused hulls moved at 100-200
WU/s; most refused hulls churn away entirely). Express liners and escorts were the stationary cases —
both roles hold station by design (boarding / convoy escort hold), but a longer sample would be needed
to separate "holding" from "frozen". Not a save-growth risk either way: the job is gone, so nothing
accumulates.

FILES
build/min-spec.json — named hardware + floor values + soak contract
scripts/lib/minSpecFloors.mjs — floor recomputation from raw evidence
scripts/check-min-spec-floors.mjs — per-host evaluator, fail-closed
scripts/lib/releaseSoakProbe.mjs — public-route soak driver + evidence envelope; cycle flight-input step re-drives with the public turn key before calling a hull stuck (2026-09-19)
scripts/lib/releaseSoakCli.mjs — browser/electron acceptance entrypoints
scripts/check-release-soak-browser.mjs / check-release-soak-electron.mjs
test/pq-033-02-min-spec-soak.test.mjs — contract tests
src/render/pipelineReadiness.js — refcounted pending latch, compile→residency chain
src/render/bloom.js — null-program seal, unready-drawable admission queue
src/render/shadowDepthAdmission.js — staged-scene real render replaces standalone pass
src/render/startupGpuResidency.js — subject-pending residency dedupe
src/systems/flightV3.js — berth dockSpeedGate + sustained envelope brake + lane-membership gate + in-lane traffic pacing
src/systems/world.js — `_captureSectorDurableRecords` releases a cap-refused convoy hull's npcJobs entry (2026-09-19)
test/pq-033-02-convoy-cap-job-release.test.mjs — the cap refusal releases the job and the phantom id; an under-cap capture keeps its job (red without the release line)
src/data/collisionProxyManifests.js — capture heading gate exempts dock-speed hulls (contact-held wedge fix)
src/save/saveSystem.js — persistent-actor restore dedupe + backup-quota graceful degrade
src/world/worldRecords.js — per-sector convoy record cap at capture
test/station-docking-corridor.test.mjs — off-lane recovery + lane-lock + traffic-pacing regressions

CHECKS
npm run check:pq033:min-spec            [TBD]
npm run check:all                       [TBD]
npm run probe:runtime-witness           [TBD]
npm run check:flight:clean (5 strict runs, incl. cold cache)  PASS — zero warnings
node test/flightV3.spec.mjs             PASS
npm run check:brake-convergence         PASS

EVIDENCE
browser (2026-09-19 run 2, FAILED at warmup-0): .devshots/spec2 in the soak worktree
  — the restore never left the opening-scene gate ("Building the opening scene — Committing authored
  objects before the first playable frame", 56% after 90 s; failure-screenshot.png). NOT reproduced:
  a focused boot→save→load loop reached flight 3/3 in ~1 s, and a 3-cycle run of the same soak route
  on the same candidate ran through its cycles. Suspect host contention (the box was ~75% busy) or the
  GPU-residency/cook commits that landed minutes earlier (891babf76, b033c29e6, 42be7ce17). Flagged,
  not chased.
browser (2026-09-19 run 1, FAILED at cycle 151): .devshots/spec2/min-spec-soak-browser-2026-09-19T11-44-06-484Z-22424-272a4612
  — 151 clean save/load/dock/trade cycles; the save quota never fired (`saveErrors: []` at every
  cycle), so the fix held for 3.6× the old death point. The failure is the driver seam above:
  `flight input must cause motion`, hull 0 WU/s against a station plate at the berth (trail:
  50 s inside a 1 WU neighborhood; failure-screenshot.png shows the hull inside structure, SPEED 0).
  The driver now re-drives with the public turn key; run 2 is the acceptance attempt.
browser (2026-09-17 run 0): .devshots/spec2/min-spec-soak-browser-pq03302-2026-09-17T00-08-29-221Z-12352-390fa64a
  — 31 clean cycles before the run was halted on an environment stall (Chromium reported
  document.hidden under window occlusion, suspending the sim); the quota death signature
  (`saveErrors: [{slot:"quick", reason:"quota"}]`, storageBytes 5,227,939) is from the 2026-09-17
  07-13 run. Not a completed 2 h/200-cycle soak.
electron (2026-09-19, path repaired): [TBD evidence dir]
  — the Electron host had never produced evidence because the route library assumed a Playwright
  JSHandle where the CSP-safe polling wrapper resolves a value ("handle.jsonValue is not a
  function"), and its tick-freeze retry was one re-drive where an occluded Electron window can
  starve two input windows (Electron has no --disable-backgrounding-occluded-windows equivalent).
  Both are fixed (harness-only, `9d7b61180`); a 1-cycle Electron run now completes
  undock/flight/save/load/dock/market/trade and only fails the evidence floors a 1-cycle run cannot
  meet. The 2 h/200-cycle Electron acceptance runs in `.devin-tmp/pq033-electron-wt` (candidate
  `56949dfc1`, same game code as the browser candidate).
