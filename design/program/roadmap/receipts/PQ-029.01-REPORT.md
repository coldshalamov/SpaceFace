<!-- LIFETIME: RECEIPT -->
# PQ-029.01 — Elastic whip as stored energy

```text
STATUS DONE

WHAT I FOUND     The whip already snapped. A stored stretch on seed
                 29010 left a mass-16 wasp at 1.22× its cruise. The
                 Range had no word for it — only a scenario JSON.

WHAT I CHANGED   The spring still owns the snap. Stretch now publishes
                 stored energy and a readable strain glow. Load past
                 the whip's stretch edge asks the line to break. The
                 Range rail now ends with Stretch stores, release snaps.

WHAT YOU WILL FEEL   Latch a wasp, burn away, stay on the line. The
                 cable goes hot as it stores the stretch. Let go of
                 the burn and the return yanks the light end. Stretch
                 past the load and the line dies empty. Open the Range
                 (F4) and the last MASSLINE word teaches that.

THE NUMBERS      bar | before | after | target
                 snap / Wasp cruise (seed 29010) | 128.4 / 105 = 1.22× | same | ≥ 0.40×
                 stored energy (live snap) | unpublished | 54768 | > 0
                 strain glow (live snap) | n/a | 0.92 | readable
                 Range rung | missing | whip_snap, index 8 | teaches
                 scripted teach | n/a | 1.48 s, verdict clear | ≤ 60 s
                 tractor_throw index | 7 | 7 | do not reorder
                 physics owners | snap untouched | energy + glow + edge break | no retune
                 scenario | test/pq-029-01-elastic-whip.test.mjs | seed 29010 | Rapier + Range

THE FRAMES       Headless Rapier snap + Range rung tick. No headed GPU strip.

NEXT             Integrator updates the queue. Leftovers: .02
                 coupler, .03 NPC users. Do not mark the queue from this
                 receipt.
```

## Files

- `src/systems/tetherGameplay.js` — stored energy, strain glow, whip load read
- `src/core/sg02DynamicBodyOwner.js` — publish stored energy; whip breaks at the load edge
- `src/ui/screens/range.js` — one MASSLINE rung after tractor_throw
- `test/pq-029-01-elastic-whip.test.mjs`

Not edited: `modules.js`, `program-queue.json`, `NOW.md`.

## 2026-09-10 Lane N

```text
DONE  PQ-029.01 — A stretched whip stores ½ k s², the glow reads that store, and a player cut spends it so a mass-16 wasp leaves at 0.60× Wasp cruise.

WHAT I FOUND     The live spring already yanked. Seed 29010’s attached return left the wasp at 1.22× cruise, but the cut threw the leftover energy away. That is apply, not store-and-release.

WHAT I CHANGED   Remaining stored energy is now spent as a closing impulse along the line when the player cuts. Load-break still dumps it empty. The glow keys off that same stored quantity. A slack cut fires nothing.

WHAT YOU WILL FEEL   Latch a wasp, burn away until the cable goes hot, then cut. The light end is yanked by the energy you stored, not by a gun table. Stretch past the load and the line dies with no snap. The Range still teaches the return stroke; that screen was not in this lane’s write set.

THE NUMBERS      bar | before | after | target
                 player-cut snap / Wasp cruise (seed 29010) | attached yank 128.4 / 105 = 1.22×, cut spent 0 | 63.0 / 105 = 0.60× (Δv 44.8 from 21386 spent) | ≥ 0.40×
                 stored energy at cut | published, not spent | 21386 → 0 | spent on release
                 strain glow at that store | stretch-linear  | 0.57 | readable ≥ 0.35
                 slack cut wasp speed | unmeasured | 0.00 | not a gun
                 Range whip_snap | 1.48 s clear, glow 0.52 | 1.48 s clear, glow 0.52 | ≤ 60 s
                 heavy 2400-mass hull | shrugs | same | no light-hostile clear
                 attached return still live | 128.4 | 128.4 (massline-elastic-whip-snap) | do not delete the spring
                 scenario | test/pq-029-01-elastic-whip.test.mjs | seed 29010 | Rapier + Range

THE FRAMES       Headless Rapier cut-spend + Range rung. No headed GPU strip.
```

### Files this pass

- `src/core/sg02DynamicBodyOwner.js` — spend remaining whip PE on `tether_cut` / `cut`
- `src/systems/tetherGameplay.js` — glow from stored energy; `tether:whipSnap` before the cut
- `src/data/modules.js` — whip comment only; magnetRange lines untouched
- `test/pq-029-01-elastic-whip.test.mjs`

### Checks

- `node --test test/pq-029-01-elastic-whip.test.mjs` — PASS 5/5
- `node --test test/massline-elastic-whip-snap.test.mjs` — PASS (attached return 1.22×)
- `npm run check:massline` — PASS 26/26
- `npm run check:baseline` ENTRY 12/15, EXIT 12/15; same three reds (pq020-ceres-topology, sim, sim-v3); 47-A hashes unchanged from entry

### UNPROVEN

- Headed shipping-camera glow. tether.load already carries the whip glow into HUD/VFX; this pass did not capture a GPU strip.
- Range.js still yanks with the live spring while latched. That drill exists and clears; the store-and-release proof is the Rapier cut.
