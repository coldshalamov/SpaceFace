# PQ-129.05 — Next-contact hull warm rejection

Status: done by measured invalidation; no production mutation.

PQ-129.02's result-bearing Intel D3D11 flight classified 269 hitches at 97.8% named coverage.
Compose and admission each owned zero hitches. PQ-129.03 repeated that result at 97.5% named
coverage, and PQ-129.04 consequently rejected speculative compose slicing.

This leaf would move authored hull preparation onto the approach runway. With zero live
compose/admission hitches, that would warm work which the measured route did not identify as a
player-facing brick. The later shader census did observe physical BatchedMesh keys at first contact,
but their previously attempted broad warmup worsened same-machine flight; they are not evidence of
authored hull compose/admission cost.

No changes were made to `partsLibrary.js`, `renderer.js`, `tabletopPolicy.js`, population, visual
quality, or the approach-runway policy. Later combat evidence may admit a narrower leaf only if the
classifier names compose or admission as an owner.

Routing consequence: evaluate `PQ-129.08` against the same classifier. The measured implementation
pole remains the main HDR scene render/presentation path, not speculative contact preparation.
