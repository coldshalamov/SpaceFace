PARTIAL PQ-193.04 — Four of the five 47-A objects publish packaged bodies; the Kessler beacon was publishing a blocked toy and now publishes a reviewed one, and the hole that let it through is closed.

DONE WHEN (verbatim): "Spindle, rescue capsule, Kessler beacon, Bourse wreck, and the ordinary TOW can. Model first if hitch still owns the 47-A wiring file; hook when that row is idle. Upgrade the existing object. No parallel prop."

WHAT I FOUND — `f580852a9` (2026-09-11) already pointed all five mission slots at packaged bodies on disk, so `WORLD_VISUAL_CENSUS.md` rows 20-24 saying "**No model file.**" were stale. Chase stills of what those slots now publish (legal chase, 144 WU / abeam / 58 WU, off the shipped GLBs, with the meshopt release bodies decoded and rendered as a cross-check):

| Slot | Body it publishes | At 58 WU |
|---|---|---|
| evidence spindle | `pods/pod_cargo_container.glb` | Real hardware: ribbed container, corner castings, orange and cyan markings. |
| ordinary TOW can | `pods/pod_cargo_container.glb` | Same. Same body as the spindle, so the mission's hero can has no tell. |
| Bourse wreck | `places/place_aftermath_wreck_liner_bow.glb` | A modelled bow instead of boxes and ribs. Untextured blockout language, but it reads as a broken ship. |
| civilian / rescue capsule | `places/place_habitat_pod.glb` | A faceted white capsule with a flat navy decal. Packaged, not finished. |
| **Kessler handoff beacon** | **`places/place_sensor_mast.glb`** | **A lattice tower on a flat mustard slab with flat navy boxes. A toy.** |

`place_sensor_mast` is one of the sixteen `OCCUPATIONAL_YARD_PLACE_IDS` that the 2026-08-18 still panel checkpointed as toy / open-cage / LEGO. `src/data/occupationalYardDressing.js` keeps it out of `PLACE_FILES` for exactly that reason, and `test/unused-model-live-wire.test.mjs` pins it: "must stay out of PLACE_FILES until a still panel clears toy/open-cage." But `visualOverrides.packagedPropForEntity` also accepts a literal file path, and that route never consults `PLACE_FILES`. Naming the file walked straight past the checkpoint, drew a blocked body on the default route with `hideImmediately`, and no check said a word.

WHAT I CHANGED — Pointed the Kessler handoff beacon at `places/place_lane_beacon.glb`: admitted to the live place selector, already spawning in Helios today, and chase-reviewed under PQ-193.03. Then closed the route: the yard checkpoint now also guards the packaged-prop table, and every file it names must exist in the release parts tree. Rewrote the five census rows to say what each slot actually publishes and how good it is.

WHAT YOU WILL FEEL — The covert handoff spot in the opening mission is a lane beacon instead of a yellow-slab lattice tower. Nothing else in the scene moves.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| 47-A slots publishing a packaged body | 5 of 5 | 5 of 5 | 5 | — |
| ...publishing a **checkpointed toy** | 1 (Kessler beacon) | 0 | 0 | legal chase |
| ...publishing a body the place selector admits | 0 | 1 | — | — |
| Packaged-prop file paths guarded against the yard checkpoint | 0 of 5 | 5 of 5 | 5 | — |
| Census rows still claiming "No model file" | 5 | 0 | 0 | — |
| `unused-model-live-wire` | 7/7 | 8/8 | all | 47 |
| `check:playable` | 16/16 | 16/16 | 16/16 | — |
| `check:baseline` | 15/15 | 15/15 | 15/15 | — |

THE FEEL — One fewer object in the opening mission looks like it came out of a different, cheaper game.

NEXT — leave the queue row open. Two of the five are packaged but not finished, and the receipt says so rather than closing over them: the rescue capsule is a faceted white blockout and the Bourse wreck is untextured blockout language. The spindle and the ordinary tow can are the same body, so the object the whole mission is about is indistinguishable from freight — a tell (a seal, a ledger tag, a different door) is the obvious next leaf. `place_sensor_mast` and its fifteen siblings still need the still panel that clears them.

CHECKS

| Command | Result |
|---|---|
| `node --test test/unused-model-live-wire.test.mjs` | PASS 8/8 |
| `npm run check:playable` | PASS 16/16 |
| `npm run check:baseline` | 15/15 green |

STILL QUOTE — Legal chase stills of `pod_cargo_container`, `place_habitat_pod`, `place_sensor_mast` and `place_aftermath_wreck_liner_bow`, looked at and deleted. The sensor mast's flat mustard base slab and flat navy instrument boxes are the 2026-08-18 panel's "toy / open-cage" verdict exactly; the cargo container is the only one of the four that reads as manufactured equipment.

OPTION TAKEN — Re-point one slot at an already-admitted, already-reviewed body, and guard the table. Rejected: authoring a new beacon (the brief says upgrade the existing object, no parallel prop); reverting to the code-built mast + hoops (a blocked toy is worse, but so is going back to primitives); clearing `place_sensor_mast` off the checkpoint on my own say-so, which is the still panel's call.

UNPROVEN — A headed New Game still with the Kessler beacon in frame; the 47-A handoff zone sits 282 WU off the opening spawn and the probe does not fly there. Mutation-checked instead: restoring `place_sensor_mast` in the table fails the new guard. `src/render/visualOverrides.js` carries another writer's uncommitted work and was not opened.

2026-09-12 (later, b2c346b93) — the onboarding rescue rock named on the world-picture list is no longer a white icosphere. Both rescue rock ids were missing from the common-rock alias table, so the PBR surface library, UV transform and vertex colours all opted out; on top of that, a rock built inside the opening's 4 s map race kept the bare white material under the final cache key, and the first-flight residency hold parked both rocks in the build queue until +20.3 s while the tutorial line pointed at them. All three fixed; headed probe (seed 4242, Intel) shows both rocks meshed with `SF_CommonRock_GeologicalPBR_v4` at T=1 s. The three open leaves above (capsule, hulk, hero can tell) are unchanged.
