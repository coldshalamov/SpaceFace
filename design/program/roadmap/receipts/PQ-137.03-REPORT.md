<!-- LIFETIME: HISTORICAL -->
# PQ-137.03 — nimble fighting flight, integrated result

- **implementation commits:** `3d094176`, `1901ad5e`
- **integration state:** implemented; B2 is met, while the governed above-cap camera clause in B3 is still honestly red
- **route:** production `flightV3` plus `rapier-dynamic`, fixed seed

## Player-visible result

The entire drive catalogue now reaches fighting speed and reverses quickly enough to feel nimble,
and it turns inside one visible screen depth. Every profile has a separate `travelCeiling`, so the
combat rescale does not erase earned cross-map speed.

| Bar reading | Before | After | Target |
|---|---:|---:|---:|
| rest to cruise | 3.05 s | **0.67 s** | <= 1.5 s |
| 180-degree reversal | 4.48 s | **1.58 s** | <= 3.0 s |
| turn radius at cruise | 8.86 screen depths | **0.95** | <= 1.0 |
| screen crossing at cruise | 0.585 s | **1.374 s** | >= 1.2 s |

The above-cap camera readings remain **1.01x / 1.40x**, below the governed opening required by B3.
That is why this row is `implemented`, not `done`; this receipt does not convert an honest camera
failure into a pass. The separate weave-over-cap exploit also remains product work.

## Verification

The code and focused tests landed before this recovery turn. The current real-path B8 run measured
cruise at 94.9999 WU/s and independently derived a 1.2105 s visible-depth crossing from the current
115 WU depth. The known `sim-v3` hash change is the causal motion change from this leaf and has not
been blindly rerecorded.
