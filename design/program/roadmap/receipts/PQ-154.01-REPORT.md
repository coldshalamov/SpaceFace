<!-- LIFETIME: RECEIPT -->
# PQ-154.01 — Wreck ecology

```text
STATUS DONE

WHAT I FOUND     Aftermath wrecks were markers. An old field did not grow
                 a crew. Scavengers lived in encounter scripts, not on
                 the wreck.

WHAT I CHANGED   A wreck field ages on the 10-minute sim day. After one
                 day it spends a budget of two: a scavenger and either a
                 squatter or a trap. Four days later they decay. Spent
                 budget does not refill. The Ceres Abandoned Driller is
                 already a day old.

WHAT YOU WILL FEEL   Come back to a day-old wreck and someone is on it.
                 Kill them and they do not come back. Wait four days and
                 the field is empty again.

THE NUMBERS      bar | before | after | target
                 fresh field | 0 inhabitants | 0 | not instant
                 age ≥ 1 day (seed 15410) | empty | ≥ 2, scavenger + second | ≥ 2
                 budget | n/a | 2 spent, no refill | decays, not a farm
                 four-day field | n/a | 0, decayed | stays empty
                 Ceres derelict | salvage marker | ≥ 2 on enter | on the route
                 clock | n/a | WRECK_ECOLOGY_DAY_S = 600 | sim time, not wall
                 scenario | test/pq-154-01-wreck-ecology.test.mjs | seed 15410 | headless

THE FRAMES       Headless spawn/decay. No headed GPU strip.

NEXT             Integrator updates the queue. Leftover: .02 your own
                 wreck. Do not mark the queue from this receipt.
```

## Files

- `src/systems/aftermathWrecks.js` — ecology budget, seed, decay
- `test/pq-154-01-wreck-ecology.test.mjs`

Already live: `uniqueWrecks.js`, `salvage.js`, `survivorPod.js`.

Not edited: `src/render/`, `hullFracture.js`, `program-queue.json`.
