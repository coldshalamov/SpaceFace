DONE  PQ-186.04 — Four master reds characterized and greened for a stated cause. Hostile kills
still erupt scrap + a credit chip. An explicitly marked non-lawful facility can witness. The
authored cargo pod is present in source, release, and the runtime pod slot with manifest numbers
that match the live GLBs.

WHAT I FOUND
1. Hostile shard burst (2 tests). Live `loot:drop` already fires `source: kill_burst` with scrap
   commodities plus an AC-01 `credit_chip` (no commodityId). The assertion required *every* item to
   carry commodityId, so the whole burst counted as zero after chips landed on the same drop.
   Not a missing player-facing fountain.
2. Explicit law witness. `lawWitnessesNear` walked only `forEachLivingWorldActor`, which never
   yields dressing FX. Heist/facility markers are `type: 'fx'` with `data.lawWitness`.
   `forEachExplicitWitnessMarker` already existed in livingWorldViews and was not wired.
3. Capsule tris 4552 !== 3976. Source manifest and live GLB already agree at 4552 / 263268 bytes;
   the test pinned a historical 3976 and an old release sha. Reconciled by measuring the live GLBs
   through `glbMetrics` / `fileRecord` instead of hand-editing 3976 → 4552.

WHAT I CHANGED
- `test/pq019-heat-incident-listener.test.mjs` — `shardDropsOf` accepts a kill_burst that mixes
  commodity scrap and credit chips; still requires at least one commodity line.
- `src/systems/lawSecurity.js` — `lawWitnessesNear` also walks `forEachExplicitWitnessMarker`.
  Preserved the unrelated 4-line ambient `takeNearWorkSlice` hunk already dirty in this file.
- `src/world/livingWorldViews.js` — restored the living-world JSDoc onto `forEachLivingWorldActor`.
- `test/pq019-authored-cargo-capsule.test.mjs` — source.tris/bytes and release shas must match the
  live GLBs via the graphics receipt helpers.

WHAT YOU WILL FEEL   Kill a hostile and scrap still flies off the hull with a credit chip in the
burst. A heist catcher marked as a witness can actually see the theft. The cargo pod you already
fly is the one in source and release.

THE NUMBERS      bar | evidence
hostile kill_burst | seed 19019 encounter wasp: loot:drop items include cmdty_scrap_metal + credit_chip 83; test 2/2 green
non-lawful marker witness | type fx + lawWitness true → 1 witness, stableId lawful_catcher, lawful false
capsule presence | source.file pods/pod_cargo_container.glb; tris === measured GLB; bytes === disk; release sha === disk; pod slot listed
cause recorded | AC-01 chips; living-world scan skip of fx; remesh 3976 → live 4552 already in committed manifests

FILES
test/pq019-heat-incident-listener.test.mjs — kill_burst item shape
src/systems/lawSecurity.js — explicit witness walk (disjoint from ambient-slice hunk)
src/world/livingWorldViews.js — JSDoc
test/pq019-authored-cargo-capsule.test.mjs — live GLB/manifest agreement

CHECKS
node --test test/pq019-heat-incident-listener.test.mjs test/pq019-law-incident-intake.test.mjs
  test/pq019-authored-cargo-capsule.test.mjs   PASS  all four reds green (full files 48/48 in the
  combined 186.04 + related run; the four named assertions specifically green)

LEFT OUT / UNPROVEN
- Manifest rows were already 4552; no GLB rewrite. The pin is now live agreement, not a historical
  triangle count.
- lootShards.js / heat.js / the two asset manifests did not need edits.

RESULT: DONE
