<!-- LIFETIME: RECEIPT -->
# PQ-177.01 — Charts with a forecast cone

```text
STATUS DONE
  Inspector cone paint on leftover Iron Ore at leftover Ceres:
  last-ten-minute sparkline, predictPriceCurve band, regimeLabel
  word, fresh/stale from existing market-memory age, contemplated
  sale (qty × station pay). Player can tell history from forecast.
  Hist points are {t, mid} only — modelled vs observed was not
  invented. Headless only.

STATUS NOT DONE
  Invented 30% profit playtest / income-uplift score. Forecast
  quality stays a calibration/decision question, not a painted
  profit promise.

WHAT I FOUND     predictPriceCurve and regimeLabel already lived on
                 the cycle. The Orbital inspector chart was history
                 only. Quote age already lived on market memory.
                 Hist does not mark modelled vs observed.

WHAT I CHANGED   The leftover inspector now paints the last ten
                 minutes and the leftover forecast band from the
                 leftover live Ceres Iron Ore record. Sale line is
                 qty × leftover station pay. 177.02 chain HTML stayed.

WHAT YOU WILL FEEL   Open Iron Ore at Ceres. The sparkline is the
                 past. The band is the forecast. The word names the
                 regime. The quote says fresh or stale if you have
                 already seen it. The sale line is the contemplated
                 lot, not a promised thirty percent.

THE NUMBERS      bar | before | after | target
                 cone | history polyline only | predictPriceCurve band + key | not equal to hist
                 regime | buried in driver sentence | leftover regimeLabel word | live cycle
                 quote age | console intel only | fresh/stale when memory exists | no invented clock
                 sale | console total only | qty × station pay on inspector | no invented fees
                 30% playtest | none | none | do not invent
                 scenario | test/pq-177-01-cone.test.mjs | seed 17701 | headless

THE FRAMES       Headless inspector HTML. No headed GPU strip.

NEXT             Integrator does not mark the queue from this receipt.
                 Calibration/decision tests remain a later honesty
                 pass, not an income-uplift score.
```

## Files

- `src/ui/views/marketPresentation.js` — leftover cone, key, sale line
- `src/ui/station/screens/market.js` — leftover Ceres Iron Ore hist window + predictPriceCurve wiring
- `styles/orbital.css` — leftover cone / sale line
- `test/pq-177-01-cone.test.mjs`
- `design/program/roadmap/receipts/PQ-177.01-REPORT.md`

Not edited: `economy.js`, `economyCycles.js` (read only), `priceForecast.js` (sector words only), `program-queue.json`.
