<!-- LIFETIME: EVIDENCE -->
# PQ-195.02 — The instrument says what the machine is doing — REPORT

```yaml
unit: PQ-195.02
parent: PQ-195
state: integrated
acceptance: focused_green
date: 2026-09-19
implementer: opencode-go/deepseek-v4.1-flash (worker), controller-integrated
reviewer: opencode-go/glm-5.3 (independent, VERDICT: ACCEPT)
integratedCommit: 55daa6e74e1814ca1a14c561520dfe4af1307b7d
candidateCommit: 05f77f47a801e9ca74a8b99879c92d096043aaa2 (pq195-third-shift)
```

## Status: INTEGRATED

The capture fork's mechanical truth is bound into the contextual EDGE surface: one
transient entry in the existing `.sf-leftcontext` column while the SP-07 load is the
player's situation. Identity, `NN / 100 WU/s`, mouth alignment, and the kernel phase
— in the §5 interface vocabulary, never raw FSM ids, never colour alone.

## Done-when numbers

| Claim | Evidence |
|---|---|
| Player aborts too-fast on words, not colour | `phase:'outside'` at 155 WU/s → `Approach at 100 WU/s or less` + `Slow the assembly before contact` BEFORE any contact; colour only reinforces |
| No fourth permanent HUD anchor | mounts inside the existing `.sf-leftcontext`; detaches the whole subtree when not live (column collapses to `:empty`); re-reveal never stacks; verified by test + reviewer grep (no lab card in `src/`) |
| Lab load card not installed | instrument is a new read model + DOM controller; no standalone card imported |
| Truth the machine knows | reads `state.heistFacilities.schedule`, `capture.phase/entryCount`, the real load body, and `projectBreakawayForkMouth` geometry — same projection the facility and colliders use |
| Live gate | null unless flight + not docked + heist sector + launched capture-fork variant + live load + (tethered | acquired | ≤400 WU of mouth); five negative gates tested |
| Accessibility | `role=status` + `aria-live=polite` + composed aria-label; reduced-motion/flash classes honored; no animation; forced-colors fallback |

## Files changed

- `src/ui/forkInstrument.js` (new, 372 lines) — pure read model
  (`classifyForkInstrument`, `forkInstrumentModel`, `forkReceiverWorld`) + DOM
  controller (`createForkInstrument`) with scoped `<style>` (survivalHud pattern,
  kit tokens only).
- `src/ui/hud.js` (+11 lines) — create, mount into `leftContext`, update on the
  10 Hz slow clock, destroy in teardown.
- `test/pq195-02-fork-instrument.test.mjs` (new, 289 lines) — 6 tests.

## Controller fix during review

Worker read `tether` off the player entity; the production mirror is
`state.player.tether` (tetherGameplay writes it, hud.js:4713 reads it the same
way). Fixed in `forkInstrument.js` and the test's synthetic state corrected to
match production. Without the fix, "Load coupled" could never show and the
instrument would unmount while towing beyond 400 WU.

## Checks (controller-reproduced; reviewer independently reran)

| Check | Result |
|---|---|
| `node --test test/pq195-02-fork-instrument.test.mjs` | 6/6 (worktree + master) |
| `node --test test/pq195-01-fork-colliders.test.mjs` | 4/4 |
| `npm run check:pq019c:mission` | 67/67 |
| `npm run check:ui:control-labels` | OK (19 actions) |
| `npm run check:player-facing-labels` | OK |
| `npm run check:ui-a11y` | exit 0 (worker) |
| `npm run check:baseline` | 15/15 |

## Reviewer notes (non-blocking)

- §5's mouth-acquisition phrase "Rails have the assembly" could replace 'In the
  fork bay' in a future copy pass — same voice, no rule broken.
- Sector gate is lenient on falsy `currentSectorId` (unreachable while a live
  capture-fork schedule exists).
- Injected `<style>` persists after destroy (same as `sf-crun-css` kit pattern).
- tooFast/aligned epsilon of +1e-9 is physically unreachable vs kernel bounds.

## Strings added (player-facing)

`CATCHER FORK`; `SP-07 flywheel assembly`; `Assembly in transit`; `Load coupled`;
`Arresting load`; `Verifying custody`; `Approach at 100 WU/s or less`;
`Come through the open end`; `Bring the assembly around again`;
`Lined up with the open end`; `Off the mouth centre-line`; `In the fork bay`;
`Slow the assembly before contact`; `Line the assembly up with the mouth`;
`Recover it and try the approach again`; `Bring it through the open end under 100 WU/s`;
numeric `NN / 100 WU/s`.
