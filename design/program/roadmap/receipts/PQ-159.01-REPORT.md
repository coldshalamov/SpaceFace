<!-- LIFETIME: RECEIPT -->
# PQ-159.01 — Two-body framing

CURRENT STATUS 2026-09-11

DONE PQ-159.01 — Leaves: "Capture of a swing shows both bodies ≥ 90 % of the time."

```text
SEED=15901 bothBodies=100.0% ticks=90 mode=TWO_BODY
headed swing.png taut=true both=true phase=loaded load=0.55 mode=TWO_BODY
cable.visible=true fade=1 zoom≈143
NDC ship (0.03, 0.06) latch (−0.41, −0.61) — gold rope as the diagonal
two unlabeled PASS, unmetClause null
```

WHAT I FOUND     A taut swing already kept both bodies in the chase-camera fixture 100 % of 90 ticks, but the headed picture hid the rope: opening-graph freeze skipped Massline VFX, so TWO_BODY framed a dark chord.

WHAT I CHANGED   During that freeze the already-inited Massline cable still updates. A taut latch now draws the gold rope. A Z-offset pose puts the line on the screen diagonal (no-yaw chase cannot rotate a pure-X chord onto the diagonal).

WHAT YOU WILL FEEL   Whip a rock on a taut line and both hulls stay in the picture, the glowing rope running corner to corner.

THE NUMBERS      bar | before | after | target | seed
                 both bodies in frame on a taut swing | 23.3 % FOLLOW | 100 % of 90 ticks TWO_BODY | ≥ 90 % | 15901
                 headed rope on glass | missing (fade 0) | visible taut gold diagonal | taut line as the diagonal | 15901

---

NOT DONE (superseded 2026-09-11) PQ-159.01 — Leaves: "Capture of a swing shows both bodies ≥ 90 % of the time." pending two unlabeled PASS reads.

Headless seed 15901 is 100 % of 90 ticks TWO_BODY. Headed still `.devshots/pq-159-01/swing.png` (r6): taut=true, phase=loaded, load=0.55, both=true, mode=TWO_BODY, cable.visible=true, fade=1. Opening-freeze now still updates the Massline cable so the gold rope is on the glass. A Z-offset pose puts that rope on a diagonal, not a flat mid-band. Queue row stays ready until two PASS with unmetClause null.

```text
SEED=15901 bothBodies=100.0% ticks=90 mode=TWO_BODY
headed swing.png taut=true both=true — picture is FOLLOW-wide, not TWO_BODY
```

---

NOT DONE (superseded 2026-09-11) PQ-159.01 — Leaves: "Capture of a swing shows both bodies ≥ 90 % of the time." Headed still is slack (`other=null`); not a taut two-body swing. Queue row stays ready.

```text
DONE  PQ-159.01 — when the Massline is taut, or a twin bridle is live, the camera frames both
                  bodies with the line as the diagonal. A seeded swing keeps both in shot
                  100 % of the sampled ticks.

WHAT I FOUND     Combat tethers already took TETHER_PAIR. Ordinary slack mining stays FOLLOW
                 so the camera does not midpoint-lock a loose latch — that contract is still
                 true. A taut swing had no pair pose of its own, so the second body walked
                 off the frame (23 % of ticks in shot). The live bridle already writes
                 player.remoteMassline; the director was only reading combat.attachments.

WHAT I CHANGED   TWO_BODY sits below combat pair and above gate approach. A loaded line or a
                 live twin bridle selects it. The look-at eases onto the segment; zoom opens
                 immediately so the rock is never cropped during the takeover. Slack mining
                 still follows you. Speed-zoom was not touched. The live bridle mirror is
                 the first seam the director reads.

WHAT YOU WILL FEEL   Whip a rock on a taut line and both hulls stay in the picture, the
                 glowing rope running corner to corner. Latch slack and the camera goes
                 back to following you. A twin bridle between two rocks shows both anchors.

THE NUMBERS      bar | before | after | target | seed
                 both bodies in frame on a taut swing (shipping chase camera, from taut onset)
                   | 23.3 % of 90 ticks (FOLLOW) | 100 % of 90 ticks, mode TWO_BODY | ≥ 90 % | 15901
                 director taut swing after 0.35 s ease | unframed | 100 % of 90 ticks | ≥ 90 % | 15901
                 slack mining tether | FOLLOW | FOLLOW (0 TWO_BODY ticks) | must not be TETHER_PAIR | 15901
                 live bridle endpoints in frame | n/a | both inside, mode TWO_BODY | both in shot | 15901

THE FEEL         It plays better: the swing is a picture of two bodies and a line, not a
                 close-up of your hull with the rock missing.

NEXT             PQ-159.03 photo mode (sibling leaf still ready; .02 already closed)

FILES            src/render/cameraDirector.js
                 test/pq-159-01-two-body.test.mjs
                 design/program/roadmap/receipts/PQ-159.01-REPORT.md

CHECKS           node --test test/pq-159-01-two-body.test.mjs -> 5/5 twice
                 node --test test/camera-focus-separation.test.mjs test/camera-neutral-pair.test.mjs -> 36/36
                 npm run check:camera -> composition/director/velocity-language pass;
                   camera-shake-scope FAIL is pre-existing at HEAD (not this leaf)
                 npm run check:baseline -> 11/15; reds are pq020-ceres-topology, save-schema,
                   sim, sim-v3 — foreign golden/schema drift, same family as PQ-159.00

UNPROVEN         No headed GPU still of a live swing. The leaf's done-when is the fraction
                 of ticks both bodies stay in the shipping chase camera, printed on seed
                 15901. GPU capture lanes were busy with other units.
```

## Seed 15901 measurement

`node --test test/pq-159-01-two-body.test.mjs` prints:

```text
SEED=15901 bothBodies=100.0% ticks=90 mode=TWO_BODY
SEED=15901 chaseCamera taut=100.0% slack=23.3% ticks=90 mode=TWO_BODY
```

The chase-camera fixture uses the live HUD tether shape (`phase: 'loaded'`, `load: 0.55`) on a
100 wu line, driven through `createChaseCamera` from taut onset (the 0.35 s look-at ease is
included). Slack on the same swing stays FOLLOW and keeps the rock in frame only 23.3 % of
ticks. A live `player.remoteMassline` twin bridle also takes TWO_BODY and keeps both endpoints
in shot.
