<!-- LIFETIME: RECEIPT -->
# PQ-159.01 — Two-body framing

```text
NOT DONE  PQ-159.01 — Leaves: "Capture of a swing shows both bodies ≥ 90 % of the time."

Seed 15901: geometric TWO_BODY fixture keeps both bodies in frame 100% of 90 ticks. No headed GPU swing capture. Queue row stays ready.

DONE (geometric ticks only; swing capture unmet — not a close)  PQ-159.01 — when the Massline is taut, or a twin bridle is live, the camera frames both
                  bodies with the line as the diagonal. A seeded swing keeps both in shot
                  100 % of the sampled ticks.

WHAT I FOUND     Combat tethers already took TETHER_PAIR. Ordinary mining stays FOLLOW so the
                 camera does not midpoint-lock a slack latch — that contract is still true.
                 A taut swing and a world-to-world bridle had no pair pose of their own, so
                 the second body walked off the frame.

WHAT I CHANGED   The live camera director now has a TWO_BODY mode, below combat pair and
                 above gate approach. A taut line (loaded/overload, or a high load/ratio)
                 or a live twin bridle selects it. frameTwoBodyLine puts the look-at on the
                 segment's bounding box and zooms until both radii sit inside the 10 % safe
                 frame. Mining that is not taut still stays FOLLOW. Speed-zoom was not
                 touched.

WHAT YOU WILL FEEL   Whip a rock on a taut line and both hulls stay in the picture, the
                 glowing rope running corner to corner. Latch slack and the camera goes
                 back to following you. A twin bridle between two rocks shows both anchors.

THE NUMBERS      bar | before | after | target | seed
                 both bodies in frame on a taut swing | unframed (FOLLOW) | 100 % of 90
                   ticks, mode TWO_BODY | ≥ 90 % | 15901
                 slack mining tether | FOLLOW | FOLLOW (unchanged) | must not be TETHER_PAIR | 15901
                 live bridle endpoints in frame | n/a | both inside | both in shot | 15901

FILES            src/render/cameraDirector.js
                 test/pq-159-01-two-body.test.mjs (new)
                 design/program/roadmap/receipts/PQ-159.01-REPORT.md (new)

CHECKS           node --test test/pq-159-01-two-body.test.mjs -> 3/3 twice
                 node --test test/camera-focus-separation.test.mjs -> 34/34
                 node --test test/pq-159-00-impact-kick.test.mjs -> 7/7 (kick not regressed)

UNPROVEN         No headed GPU still of a live swing: the leaf's done-when is the
                 deterministic fixture on seed 15901, which the packet names as acceptable
                 evidence. GPU capture lanes were busy.
```

## Seed 15901 measurement

`node --test test/pq-159-01-two-body.test.mjs` prints:

```text
SEED=15901 bothBodies=100.0% ticks=90 mode=TWO_BODY
```

The fixture is a taut `phase: 'loaded'` line between a hull and a rock, swung through 90 ticks
after the 0.35 s pair ease. The live director (`createCameraDirector` + `frameTwoBodyLine`)
stays in TWO_BODY for every sampled tick and keeps both bodies inside NDC ±1. A slack mining
latch still fails `isTautOrBridle` and does not enter TETHER_PAIR or FOCUS_PAIR.
