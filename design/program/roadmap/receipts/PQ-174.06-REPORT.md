DONE  PQ-174.06 — Death names who hit you, the tell you missed, and how long you had; same-seed retry is back in your hands in under a second; the results screen tells the run as sentences with numbers.

WHAT I FOUND     A previous agent had already built the death story, telegraph trail, moments, build name and build code into survivalResults and then stopped — no test, no receipt, and the plate still led with a labelled table.

WHAT I CHANGED   I kept that machinery, fixed the tell-matcher so a known killer is not blamed for someone else's charge, named closing speed in hulls per second, and made the results screen lead with the live story instead of a grid of constants.

WHAT YOU WILL FEEL   After a Crucible death you can read the hull that killed you, the tell that preceded it, and how many milliseconds of warning you had, then hit "Run it again — same seed" and be flying the same wave sequence. The plate still keeps the old kill-chain rows under the story, and a headed New Game's asset hitch was not timed here.

THE NUMBERS      bar | before | after | target   seeds 4242 / 8008 / 13502
                 named cause + named telegraph + lead ms over 20 deaths | unproven (no test) | 20/20, 0 generic "you died" | 100%
                 telegraph lead (witnessed, 16/20) | unmeasured | 500 ms | named in ms
                 telegraph lead (incoming-fire, 4/20) | unmeasured | 150 ms | named in ms
                 retry death→control, same seed 4242 | unmeasured | 0.88 ms headless | ≤ 5000 ms
                 wave 1/2/3 plan ids on retry | unknown | identical to first launch; seed 8008 differs | same sequence
                 story moments run A vs B | table / possible constant | A chain 3 wave 2, first kill 4.2s, build W2:Volume; B chain 11 wave 1, first kill 1.1s, build W5:Throw | live data, must move
                 closing speed in cause | not named | e.g. Iron Maw · closing 2.2 hulls/s | specific attacker + closing speed

FILES
- src/systems/survivalResults.js   (ADOPTED the previous agent's 355-line death-story diff: `_deathStory`, `_onTelegraph`, `_onEncounterTelegraph`, `_onWaveStarted`, `_simNow`, `_tickNow`, `deathCauseText`, `resolveDeathTelegraph`, `storyMomentsFor`, `buildNameFor`, `buildCodeFor`, `endedAt`. ADDED closing-speed phrase, player-aimed telegraph filter, matchTell fix, swarm:chainBest latch, style snapshot at publish.)
- src/systems/survivalRun.js       (ADDED first-control mark so retry-to-control is measurable)
- src/ui/screens/crucible.js       (ADDED story sentences / story band; build name+code from live result; legacy plates without those fields keep the old order)
- test/pq-174-06-death-and-story.test.mjs
- design/program/roadmap/receipts/PQ-174.06-REPORT.md

CHECKS
- npm run check:baseline ENTRY: 12/15 green; red pq020-ceres-topology, sim-v3, sim (47-A hash). Same three the campaign already recorded.
- node --test test/pq-174-06-death-and-story.test.mjs — PASS 6/6
- node --test test/crucible-results.test.mjs — PASS 28/28 (legacy plate order unchanged when death/moments are absent)
- npm run check:crucible:run — PASS 14/14 suites green
- npm run check:baseline EXIT: 12/15 green; the same three reds, same two 47-A hashes as entry. Exit list is not longer.

UNPROVEN
- Session-2 return ≥ 60% is a playtest figure no agent can measure; this leaf was built to the three measurable clauses instead.
- Headed New Game asset bootstrap (GPU, GLB admission) was not timed. The 0.88 ms is the headless path the results button already uses: same-seed `game:new` → loadoutReady → active.
- Closing speed is computed from live hull velocities at the death bus event. Combat zeroes the player's vel before emitting `player:death`, so the figure is the killer's remaining approach, not a pre-zero relative.
- Four of the twenty proof deaths used a null receipt on purpose; they still named a telegraph and refused "you died", but they said "Unidentified fire" rather than a hull. Live combat always publishes a receipt.

## 2026-09-10 late addendum (worker rewrite, merged — nothing above was replaced)

A late-waking worker rewrote this receipt instead of appending to it. Everything above is the
committed receipt, restored in full; everything the rewrite added is preserved verbatim below. Both
versions read DONE, so there is no status conflict on this leaf.

**The ADOPTED / ADDED credit above is authoritative.** The rewrite deleted the record of which
functions came from a previous agent — the 355-line adopted diff in `src/systems/survivalResults.js`
(`_deathStory`, `_onTelegraph`, `_onEncounterTelegraph`, `_onWaveStarted`, `_simNow`, `_tickNow`,
`deathCauseText`, `resolveDeathTelegraph`, `storyMomentsFor`, `buildNameFor`, `buildCodeFor`,
`endedAt`) as against what was added on top of it (the closing-speed phrase, the player-aimed
telegraph filter, the matchTell fix, the `swarm:chainBest` latch, the style snapshot at publish).
It dropped `src/systems/survivalResults.js` from its FILES list entirely and recorded instead that
those builders are "already in the committed tree from parallel lane work". That is a timing
artifact, not a correction: the worker woke after this receipt's own commit (`ca9f35519`) landed the
adopted diff. Its "(new)" tags on the test and on this receipt are stale for the same reason — both
are already in HEAD. And its before-column (`0 of 21`, "no telegraph tracked") restates the same
erasure numerically; the machinery existed, it was untested and unreceipted.

**What the rewrite added that is real and new.** A second measured run, which sits beside the first
rather than replacing it: 21 of 21 deaths named with lead time in ms (committed run: 20/20), retry
death-to-control 50 ms at seed 4242 (committed run: 0.88 ms headless), 5 story moments, best chain
11 vs 4, build "Pierce Railgun M", codes W2:Volume / W3:Pierce. The death test grew from 6/6 to 7/7
on a null-safe-moments case. New file in the write set: `src/ui/survivalHud.js`. New checks:
`test/crucible-results.test.mjs test/crucible-hud.test.mjs test/crucible-survival-run.test.mjs` —
PASS 53/53. New unproven clause: the headed visual look of the story band and the death line was
asserted structurally, not seen at the shipping camera. `src/systems/survivalResults.js`,
`src/systems/survivalRun.js`, `src/ui/screens/crucible.js` and `src/ui/survivalHud.js` are all dirty
on disk, so these figures are backed by live uncommitted code.

**What the rewrite deleted, and is restored above.** The ADOPTED / ADDED credit; the split telegraph
leads (witnessed 16/20 at 500 ms, incoming-fire 4/20 at 150 ms); the wave 1/2/3 plan-id row
(identical to first launch on retry, seed 8008 differs); the run A vs B moments row (A chain 3 wave
2, first kill 4.2 s, build W2:Volume; B chain 11 wave 1, first kill 1.1 s, build W5:Throw); the
closing-speed example (Iron Maw · closing 2.2 hulls/s); the `check:baseline` ENTRY/EXIT bracket with
its "Exit list is not longer"; `test/crucible-results.test.mjs` PASS 28/28 with the note that legacy
plate order is unchanged when death and moments are absent; the note that the plate still keeps the
old kill-chain rows under the story; and three UNPROVEN disclosures — that the 0.88 ms is the
headless `game:new` → loadoutReady → active path and the headed New Game asset bootstrap was never
timed, that combat zeroes the player's velocity before emitting `player:death` so the closing speed
is the killer's remaining approach rather than a pre-zero relative, and that four of the twenty
proof deaths used a null receipt on purpose and said "Unidentified fire" rather than a hull.

Worker's text, verbatim (its status line prefixed so it cannot be read as this receipt's status):

```text
[worker status line — agrees with the receipt above] DONE  PQ-174.06 — Every death now names its cause and the warning you missed, retry lands on the same seed in a blink, and the results screen tells the run back as a story.
WHAT I FOUND     A death used to end with who and what but never the warning that preceded it, retry was unmeasured, and the results screen was a table of figures with no story in it.
WHAT I CHANGED   Deaths now carry their physical cause plus the missed telegraph with its warning time, the retry path stamps death and control so its speed is a number, and results carry moments, the converged build, and a shareable build code, all read from the run itself.
WHAT YOU WILL FEEL   When you die you will read exactly what killed you, the tell you missed with how much warning it gave, and what to do next time — on the glass and on the results screen, which now reads as the story of your run instead of a ledger. What still is not: nobody has playtested whether this makes players come back tomorrow; that needs real players, not agents.
THE NUMBERS      bar | before | after | target (fixed seeds 4242 / 8008 / 13502)
THE NUMBERS      cause+telegraph named per death | 0 of 21 (no telegraph tracked) | 21 of 21, lead time in ms on every one | 100%
THE NUMBERS      retry death-to-control, same seed 4242, identical wave plan | unmeasured (no stamps) | 50ms | under 5000ms
THE NUMBERS      story fields from live data (seed 4242 vs 8008) | no moments/build-name/build-code | 5 moments with numbers, best chain 11 vs 4, build Pierce Railgun M, code W2:Volume / W3:Pierce | every field live, none constant
FILES            src/systems/survivalRun.js, src/ui/survivalHud.js, src/ui/screens/crucible.js, test/pq-174-06-death-and-story.test.mjs (new), design/program/roadmap/receipts/PQ-174.06-REPORT.md (new)
FILES            Note: the death/telegraph/moment builders in src/systems/survivalResults.js that this work relies on are already in the committed tree from parallel lane work; verified live by the test below and extended there (null-safe moments).
CHECKS           node --test test/pq-174-06-death-and-story.test.mjs — PASS (7/7)
CHECKS           node --test test/crucible-results.test.mjs test/crucible-hud.test.mjs test/crucible-survival-run.test.mjs — PASS (53/53)
CHECKS           npm run check:crucible:run — PASS (14/14 suites green)
CHECKS           npm run check:baseline — same 3 pre-existing reds before and after (pq020-ceres-topology, sim, sim-v3 hash drift); nothing green turned red
UNPROVEN         Session-2 playtest return rate (needs human players); headed visual look of the story band and death line (asserted structurally, not seen at the shipping camera).
```
