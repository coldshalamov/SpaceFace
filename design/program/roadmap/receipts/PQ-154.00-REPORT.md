<!-- LIFETIME: RECEIPT -->
# PQ-154.00 — Authored fracture seams

```text
STATUS DONE

WHAT I FOUND     A wrecking-ball slam on a medium already splits along one
                 authored seam. Two wreck pieces inherit the hull's motion
                 and mass. This is not a general fracture solver.

WHAT I CHANGED   Nothing in the live fracture path. Seed 15400 already
                 prints two pieces. This receipt records that proof.

WHAT YOU WILL FEEL   Hit a medium hull hard enough with the wrecking ball
                 and it comes apart in two flying pieces, not one flat
                 wreck. Scavengers on old wrecks, and your own death wreck,
                 are not this leaf.

THE NUMBERS      bar | before | after | target
                 pieces (seed 15400) | 2 | 2 | ≥ 2
                 seam | medium_starboard_outrigger | same on replay | one authored seam
                 closing | 32 wu/s | 32 | ≥ 30
                 hull class | medium (mass 45) | medium | medium
                 threshold | 30 wu/s | 30 | 30
                 sub-threshold | 1 wreck, 0 splits | same | no split below 30
                 scenario | test/pq-154-00-fracture-seams.test.mjs | seed 15400 | headless

THE FRAMES       Headless scenario is enough. No headed GPU strip. No
                 render edit. Frame budget was not the bar this leaf moved.

NEXT             Integrator updates the queue. Leftovers: .01 wreck ecology,
                 .02 your own wreck. Do not mark the queue from this receipt.
```

## Files

Not edited this turn. Already live and proved:

- `src/data/hullFractureSeams.js` — three to five authored seams per class
- `src/systems/hullFracture.js` — pick one seam, spawn two pieces, inherit vel/mass
- `src/systems/collisionConsequences.js` — notes a pending slam from closing speed
- `src/systems/mining.js` — live piece-spawn on `entity:killed` (foreign dirty hunk left untouched)
- `test/pq-154-00-fracture-seams.test.mjs` — seed 15400

Not edited: `src/render/`, `aftermathWrecks.js`, `uniqueWrecks.js`, `salvage.js`,
`survivorPod.js`, `program-queue.json`, `NOW.md`.
