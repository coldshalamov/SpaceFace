<!-- LIFETIME: RECEIPT -->
# PQ-152.01/.02/.03 + PQ-171.01 — Integration closeout 2026-09-22

The four leaf receipts (`PQ-152.01-REPORT.md`, `PQ-152.02-REPORT.md`,
`PQ-152.03-REPORT.md`, `PQ-171.01-REPORT.md`) each recorded DONE with the work
landed and proved, but the queue was never flipped — the authors left the mark
to the integrator per receipt convention. This closeout re-verified every claim
on the current tree and repaired what had drifted underneath them.

## What had drifted, and the adjudication

1. **Stale authored-count pins (10 → 11).** INF-082 (`4fc8d0fae`) legitimately
   added an eleventh authored set piece (`heavy_tow`, the Frame Coupler proving
   job) and updated the PQ-152.01 test to eleven — but the PQ-152.02 and
   PQ-152.03 catalog assertions still pinned `AUTHORED_SET_PIECES.length === 10`.
   Updated both to 11 with the citing comment, matching the .01 convention and
   the validator's own expectation.

2. **Stale zero-pins on `OFFER_MIX`.** The mid-game economy heartbeat
   (`b65ad1f40`) regenerated the mix so civilian hubs post physical work by
   workload share (trade_hub tow 0→2, military demolition 0→2). The PQ-152.00
   re-close (`6c2cc6215`) already adjudicated: heartbeat weights stand; the
   zeros were initial-implementation caution, never the done-when. The .01 test
   still carried the old zero pins — updated to the heartbeat values with the
   same citing comment, matching the re-close.

3. **Dead clause on a dead event.** `f524ade41` (owner ruling 2026-09-21)
   dropped the beam overheat state machine and the `mining:overheated` event —
   but `vent_discipline` still keyed on it, and `check-mission-conditions`
   correctly refused a term nothing emits. The forbid clause was removed
   (17 terms now); `vent_rhythm` still prices the live vent rhythm. Unknown
   clause rows filter out on load, so old saves drop it silently.

4. **Brief truncation could cut the clause text.** All three clause-stamping
   sites in `missions.js` truncated the *combined* `base + suffix` line at
   `CONDITION_BRIEF_MAX`, so a long base brief pushed the term text off the end
   — exactly what the legibility gate exists to catch (it failed on
   `asp_heavy_tow_0` and `mo_station_sker_0_3`). The stampers now share
   `withClauseBriefSuffix`: on overflow the base yields and the term text is
   never cut. `heavy_tow`'s authored brief was also shortened to fit inside the
   cap while keeping the required "frame coupler / Tractor Systems / shipworks"
   names.

## Proved this session

- `test/pq-152-00/01/02/03` + `inf-082-proving-tow` + `world-reacts-failure-mutates`
  + `pq-171-01-repetition-meter`: **36/36 green** (seeds 15200/15210/15220/15230/17101).
- `node scripts/check-mission-conditions.mjs`: **OK — 17 physics terms, 10 observed
  events, 7 per-tick predicates**.
- `npm run check:mission-receipts`: OK.
- `npm run check:content:repetition`: PASS — 170 scheduled beats, 16 distinct
  grammar shapes, hottest 3.40/h under the 4/h budget, seed 17101.
- `npm run check:playable`: **16/16** (boot, launch, hull, world, controls,
  save/continue round-trip, clean, shader, assets, authored hulls).
- `npm run check:sim:long` (PQ-171.01's named packet check): deterministic run,
  36120 ticks, hash produced.
- `npm run check:baseline`: 12/16 — the four reds are foreign dirty-tree work,
  not this lane: `save-schema` (drift is `$.settings.video.shadows` +
  `shadowsDefaultVersion`, a settings default change in flight), and the 47-A
  tape/telemetry goldens red from concurrent combat/economy edits (mining.js,
  weapons.js, attackSpec.js, cruise.js dirty). Bisected: `sim` fails identically
  with this lane's files restored to HEAD. Same inherited reds the PQ-152.00
  re-close recorded.

## Queue

PQ-152.01, PQ-152.02, PQ-152.03, PQ-171.01: ready → done.
