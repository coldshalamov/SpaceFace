<!-- LIFETIME: RECEIPT -->
# PQ-049.02 — Massline express-liner source → candidate → release promotion

**Verdict:** IMPLEMENTED. The sanctioned family transaction ran to completion on the current
finalize script (contractVersion 2): source LODs → release candidates → live `parts/wholeships`,
then an incremental SG-04 release build re-encoded all three LODs and rewrote their
`release_manifest.json` rows with exact hashes. The asset is a promoted technical candidate —
not wired and not visually accepted, per the leaf's own scope.

## What ran

- `node assets/ships/massline_express_liner_v1/scripts/finalize_massline_express_liner_v1.mjs`
  — copied `source/wholeships/*lod{0,1,2}.glb` to `release_candidates/wholeships/`, stamped
  SG-04 scene identity (`contractVersion: 2`, `wiringStatus: live_express_candidate`, role
  `civic_pressure_drum_liner`), published into `assets/ships/parts/wholeships/`, and wrote
  `evidence/finalize_report.json` (`status: technical_candidate`). The previous live copies
  carried `contractVersion: 1`; they are now byte-identical to the v2 candidates.
- `node scripts/build-sg04-release-assets.mjs --no-clean --only
  wholeship_massline_express_liner_v1{,_lod1,_lod2}` — incremental sanctioned release build:
  lod0 12667384→5277236 B, lod1 11984252→5835040 B, lod2 5122612→2184064 B (KTX2 + meshopt),
  `release_manifest.json` rewritten for the three ids (268 assets total).

## Exact hashes (evidence/finalize_report.json source, release_manifest.json release)

| LOD | source sha256 (parts) | release sha256 | release bytes |
|---|---|---|---|
| 0 | `aaa58f10265d08e2…` | `75bbdc56046f1360…` | 5,277,236 |
| 1 | `9daeca0eab934297…` | `f9d7b5c036b05da9…` | 5,835,040 |
| 2 | `2622d382fd4b838a…` | `ea77fb49bc33fe54…` | 2,184,064 |

Manifest records existed already (`parts_manifest.json` part row, three
`release_manifest.json` rows) and are now hash-exact against the new payload.

## Evidence

- `npm run check:asset-status` — green: 168 parts tracked, 2 blocked, 0 ambiguous.
- `npm run check:asset-reachability` — green: 623 runtime assets routable.
- `npm run check:assets:live` — **not runnable in this tree state, by design**: the probe
  requires `HEAD == origin/master` and a globally clean worktree before it will produce live
  evidence (provenance gate at `probe-authored-assets-live.mjs:88-93`). This tree carries
  concurrent-lane edits, so the probe exits before measuring anything. Not a product defect.
- `npm run check:baseline` — the only in-tree reds are `check:sim`/`check:sim:v3` hash drift
  caused by *uncommitted foreign* route/economy work; the identical golden check was re-run in
  a clean HEAD worktree and passed, so committed state is green.

## Still owed for `done`

- `check:assets:live` evidence on a clean, pushed candidate (provenance gate).
- `check:baseline` green in-tree after the foreign economy/route lane lands and re-pins its goldens.
- Wiring, spawn captures, and acceptance remain the declared scope of `.03` (render package)
  and `.05` (natural-route acceptance), not this leaf.
