DONE  PQ-170.01 — A station now visibly grows a named module because of the freight YOU supplied
it (market sells plus your own relay convoys), and a stocked Trade Relay is a depot Concord's patrols
depend on: keep it stocked and a real Concord patrol rotation holds your depot lane; let it run dry,
go cold or get raided and the rotation is withdrawn. Both survive Continue.

WHAT I FOUND     Nothing in the game let a station change because of the player. Stations were
spawned from static data on every sector entry; player trade only moved prices and standing; relay
convoys sold at a market and vanished into credits. Patrol presence was a static role mix per
sector (traffic) plus law responses — there was no infrastructure the player could build that a
faction's patrols read. The write set could not touch traffic or the hub UI, but two existing
public seams were enough: the encounter director's authored-request API (the same one traffic uses
for the depot pirate watch) and the news surface that already feeds the dock-arrival card.

WHAT I CHANGED
- `src/data/sectors.js` — `STATION_GROWTH_LADDERS`: an authored three-rung growth ladder per station
  type (trade hub, refinery, mining, fab, military, black market, research), each rung a named
  module with a short tag, a cumulative-throughput threshold (150 / 450 / 1000u), a relay fee cut,
  a faction power bonus and its own dock line. Separate constant, never on the station record.
- `src/systems/claims.js` — owns the durable ledger `state.claims.stationGrowth` (lazy: absent until
  the first counted unit, so untraded saves and goldens keep their exact shape). Feeds: the SELL
  side of `economy:tradeCompleted`, and relay `convoy_sold`. Crossing a rung stamps the module on
  the live station (name tag `Ceres Refinery · BULK INTAKE`, `data.stationGrowth`), re-stamped on
  every sector entry and on load, publishes the line in the station faction's voice through
  `news:publish` (ticker, toast, dock-arrival card — one voice, no second toast), writes a receipt
  on the relay that fed it, and cuts the relay sale fee at that station. Depot dependency lives on
  the relay body as `depotSupport`: stocked + active ⇒ supported; supported ⇒ a `patrol_beat`
  rotation is requested from the director on a fixed simTime cadence (reused = still live, so a
  post-load wipe or a spawn-cap denial self-heals), relieved 30 s after each 120 s beat, posted
  only while the player is inside the world's near-residency radius of the lane anchor (otherwise
  the far-actor table would virtualize the hulls two ticks after spawn). Dry for 90 s, cold, raided
  or decommissioned ⇒ support lapses, the live rotation is stood down through the director's
  `abort`, and Concord says so. A rotation counts (and earns standing) only after it held the lane
  for 60 s, so a post/leave/return loop cannot farm Concord rep. Sealed Crucible runs count nothing.
- `src/systems/factions.js` — war power reads the claims ledger (a grown station adds the rung's
  authored power bonus to its faction; each provisioning depot adds Concord power) and two new rep
  facts route through `applyRep` only: `station_growth` (+8 with the station's faction per module)
  and `depot_provisioning` (+2 with Concord per completed rotation).
- `src/data/conflictReactions.js` — pure authored copy: station growth in each of the eight station
  factions' registers, and Concord's posted/withdrawn/cold/raided/decommissioned depot lines.
- `test/pq-170-01-station-growth-depot-patrol.test.mjs` (new, 8 tests): ladder data; sells grow a
  station (buys and sealed runs do not); relay convoys grow it and the fee cut lands on the next
  sale; growth save/load with re-stamp; factions power + rep through the sole writer; the 60 s
  credit floor (5 s on the lane counts nothing, an aborted beat never counts); a real
  headless sim (world + claims + encounterDirector + save) posts real Concord hulls at the depot
  lane, relieves them, withdraws them on dry stores, and re-posts after Continue; flying off ends
  the beat unseen and nothing re-posts until the player is back; a cold relay loses the rotation
  at once.

WHAT YOU WILL FEEL   Sell ore at the same station a couple of times and it changes: its name on
your contacts, target panel and local map grows a tag (BULK INTAKE, BONDED ANNEX, PATROL HANGAR…),
the ticker and the dock card say the station built it on your freight, in that station's own voice,
and your relay convoys to it keep more of every sale. Stock a Trade Relay and a Concord patrol pair
or trio takes up station on your depot lane — they are there because you keep the depot fed. Pull
the freight out, or let the site go cold, and Concord tells you the rotation is withdrawn and the
hulls leave. Save, quit, Continue: the module is still on the station, the depot is still supported,
and the rotation is back on the lane within seconds.

THE NUMBERS      bar | evidence (seed 17001)
station gains a module from player throughput | 160u sold at Ceres Refinery → rung 1 `growth_bulk_intake`; 120u → nothing; buys/sealed runs → nothing
relay convoys count + fee cut | 300u relay input → 5 convoys, station rung 1 on the 3rd; fee 0.20 → 0.15 on later sales, revenue recomputed
patrols depend on the depot | stocked relay → `depot-patrol:claim_1:1`, 2 Concord `patrol_lawman` hulls 327 WU from the depot on the lane; far player → `player_far`, nothing posts
support lapse degrades presence | withdraw stores → supported 85 s later, lapsed at 90 s, rotation aborted `depot_support_lapsed`, 0 live 30 s on; cold site → lapsed at once
no rep farming | a beat resolved by flying off (hulls virtualized) held < 60 s → not counted, no `claim:depotPatrolCompleted`; only a beat held ≥ 60 s earns the +2
save round-trip | growth ledger + re-stamp exact after deserialize; Continue restores support, live id cleared, rotation `:5` re-posted with 3 hulls
deck cost | one beat = 15 civilian pressure per ≈150 s cycle vs ≥0.35/s refill (<30% of the slowest deck)
untraded shape | `stationGrowth` absent from serialize until the first counted unit; sim/sim-v3 fingerprints byte-identical before/after

FILES
src/data/sectors.js — STATION_GROWTH_LADDERS, stationGrowthLadderFor, stationGrowthRungFor
src/systems/claims.js — stationGrowth ledger, stamp, news, relay fee cut, depotSupport + rotation
src/systems/factions.js — growth/depot power, station_growth + depot_provisioning rep
src/data/conflictReactions.js — stationGrowthReaction, depotPatrolLine, DEPOT_PATROL_LINES
test/pq-170-01-station-growth-depot-patrol.test.mjs — 8 headless tests
design/program/roadmap/active/PQ-170.md — leaf .01 checked

CHECKS
node --test test/pq-170-01-station-growth-depot-patrol.test.mjs   PASS 8/8 (real director, real hulls)
node --test test/claim-*.test.mjs pq-145-00-depot-traffic automation-depot-intake
  faction-war-fronts story-ecology-reactions pq024-asteroid-claim-manifest   PASS 82/82
  (pq-028-02-claim-sling is RED with `exit speed 174.00 must be ≥ 2× cruise 340.00` — identical
  against a clean `git archive HEAD` tree; foreign dirty travelLanes.js/physics.js, not this leaf)
npm run check:faction-standings   PASS
npm run check:save-schema         PASS (version 14, 288 paths — ledger is lazy, schema unchanged)
npm run check:claims-guidance     PASS      npm run check:claim-base   PASS
npm run check:save-import-safety  PASS 16/16
node scripts/check-data.mjs 17/17  node scripts/check-data-refs.mjs OK
npm run check:sim / check:sim:v3  RED before and after with byte-identical fingerprints
  (440814e7… vs expected 22499c93…; 79c17e19… vs f18d2929…) — pre-existing foreign drift, no new drift
npm run check:baseline            ENTRY red: sim, sim-v3 (fingerprints recorded above), massline →
  check:47a:civilian-priority ("all 47-A actors should remain bound 4 !== 0"); all three foreign.
  EXIT 12/15 green, the same three reds, nothing added (sim fingerprints byte-identical).
node scripts/check-program-docs.mjs  PASS (2 warnings: NOW.md header age/commit staleness, pre-existing)
Committed tree, not the working tree (git archive of ba49bc3a3 with src/test/scripts): leaf 8/8,
  claim-travel-infrastructure 8/8, pq-145 depot-traffic 2/2, claim-defense 8/8, faction-war-fronts
  5/5, story-ecology 6/6; claim-specializations 25/26 where the one red (`site reuses a shipped
  place asset: place_asteroid_rock_a`) is the archive lacking assets/ and fails identically at
  HEAD~2 (5afa9de8c) — 26/26 on the working tree.
Foreign uncommitted claims.js hunk (entityIndex statics/asteroids, +19/−4) preserved byte-for-byte
  (verified against a pre-edit snapshot) and absent from both commits.

LEFT OUT / UNPROVEN
- No new mesh is bolted onto the station: render and world are outside the write set, so the
  physical add-on is a shared-change request. The module reads on the station's name tag on every
  in-world surface, on the ticker/toast/dock card, on the relay's Base ledger, and mechanically.
- The hub tabs read static services; the module does not add a service tab (same reason).
- `postEndingReplayChains.js` is untouched: `mission:completed` carries no station or quantity, so
  replay-chain deliveries cannot feed growth without a missions.js change.
- The rotation flies the Concord flag in every sector: the lawful `patrol_beat` shape is authored
  Concord and the request seam does not carry a faction. Copy is written as Concord reach.
- Headless only. Nobody watched the lawmen on a GPU route.

RESULT: DONE
