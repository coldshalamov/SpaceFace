<!-- LIFETIME: RECEIPT -->
# PQ-031.01 — Coupled-pair physics

```text
DONE  PQ-031.01 — a bridled pair orbits as one system and lawn-darts a rock in 5 of 5 seeds, shares helm loss, and breaks at the leftover load rating

WHAT I FOUND     The leftover Rapier fixture already spun two 16 kg lights into a rock in 5 of 5 seeds with 410–577 deg of relative spin. Shared helm loss already lived on any taut attachment. The leftover orbit create did not pass the catalog break block, so it never asked the rating.

WHAT I CHANGED   I kept that leftover spring. The named proof now hands the catalog break into the solver. A hard swing requests a break at 9660 N against the leftover 9200 N rating. A taut bridle still only publishes a share; it never writes velocity.

WHAT YOU WILL FEEL   Two lights on the rope turn together and hit the rock as one body. Hit one and the other feels it. Yank hard enough and the line parts. Nothing damps them into cooperating.

THE NUMBERS      bar | before | after | target
                 terrain hits | leftover 5/5, seeds 0–4 | 5/5, same seeds | ≥ 4 of 5
                 spin | leftover 410–577 deg | same | ≥ 90 deg in ≥ 4
                 dist swing | leftover 4.1–28.4 WU | same, inside 60 WU stretch | bounded
                 helm share ΔV | leftover 20 on equal 16 kg, 40 ΔV | 20.000, seed 31001 | coupled inertia
                 load break | leftover rating unused in orbit fixture | breakRequested, tension 9660, rating 9200, seed 31001 | break by rating
                 velocity writes | leftover none | same | none
                 scenario | pq-031-01-coupled-pair | seeds 0–4 and 31001 | headless Rapier

CHECKS           npm run check:baseline | entry 12/15, exit 12/15, same three reds
                 npm run check:massline | PASS 26/26
                 node --test test/pq-031-01-coupled-pair.test.mjs | PASS 5/5 hits, share 20, break 9660/9200

THE FRAMES       Headless Rapier pair. No headed GPU strip. "Reads as one system in the camera" is the 410–577 deg spin, not a looked-at still.

UNPROVEN         I did not look at the shipping camera. I did not run the default-route player throw into a live rock field. Hit points were not touched.
```

## Files

- `src/systems/tetherGameplay.js` — leftover share path kept; catch ΔV now uses the same coupled share
- `test/pq-031-01-coupled-pair.test.mjs` — seeds 0–4 orbit/impact plus seed 31001 share and load-break
- leftover `test/massline-twin-bridle-orbit.test.mjs` still prints the same 5/5

Not edited: `sg02DynamicBodyOwner.js` (leftover load cap and tension rating already there), `tumbleStates.js`, `modules.js`, `NOW.md`.
