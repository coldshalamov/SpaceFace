<!-- LIFETIME: RECEIPT -->
# PQ-031.01 — Coupled-pair physics

```text
STATUS DONE

WHAT I FOUND     The leftover pair already lived in the bridle
                 spring, the Rapier owner, and helm share.
                 Five leftover seeds already lawn-darted a
                 rock as one spinning system. Line swing
                 already stayed inside leftover stretch.
                 Hitch already has an S utility; the head is
                 M. Pair admission already refused two
                 heavies and loops. There was no receipt.

WHAT I CHANGED   I kept that leftover pair. I did not rewrite
                 the throw, the NPC cut, or the solver. The
                 leftover orbit test now also pins the leftover
                 load bound. This receipt is new.

WHAT YOU WILL FEEL   Throw the bridle and two lights become
                 one bolas. They spin and lawn-dart a rock.
                 A taut hit on one hull yanks the other. The
                 line dumps load at its rating, not forever.
                 Hitch never wears this toy.

THE NUMBERS      bar | before | after | target
                 terrain hit | leftover 5/5 seeds | same 5/5 | ≥ 4 of 5
                 spin as one system | leftover 5/5 ≥ 90° | same 5/5 | ≥ 4 of 5
                 shared helm | leftover taut share mu/m_p | same | partner gets an intent
                 line load | leftover maxTension 9200, overshoot 1.05 | same | break by rating
                 length swing | leftover max 28.4 on rest 80 | same ≤ 60 | inside leftover stretch
                 Hitch fit | S utility vs M bridle | same refuse | do not put the head on Hitch
                 pair admission | leftover two-heavy / cycle refuse | same | no station-planet cheese
                 scenario | massline-twin-bridle-orbit | headless Rapier | leftover test

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The orbit is two idle-control lights and a
                 planted rock, not a default-route pirate
                 chase. Shared helm leftover is proven on a
                 taut attachment fixture, not this orbit
                 seed. Hitch leftover is the sibling head
                 test; this leaf did not retune fittings.
                 The same leftover files already hold 031.02
                 specialist cut / heavy ignore. This leaf
                 did not rewrite those. The leaf packet was
                 not in the tree.

NEXT             Integrator updates the queue. PQ-031.02 is
                 NPC counterplay. Do not mark the queue
                 from this receipt.
```

## Files

- `test/massline-twin-bridle-orbit.test.mjs` — leftover 5-seed Rapier orbit; leftover load bound now asserted
- `design/program/roadmap/receipts/PQ-031.01-REPORT.md` — this receipt

Already live, not rewritten: `src/systems/tetherGameplay.js` (pair admission, `_shareHelmLoss`), `src/core/sg02DynamicBodyOwner.js` (bridle tension cap), `src/systems/tumbleStates.js` (`tether_share`), `test/massline-twin-bridle-head.test.mjs` (Hitch refuse), `test/chain-tether-share.test.mjs` (shared inertia / helm).

Not edited: `modules.js`, `input.js`, `threatHalo.js`, `hud.js`, `spatialHash.js`, `program-queue.json`, `NOW.md`.

## Honesty gaps

- The leaf packet `.devshots/packets/PQ-031.01.md` was not in the tree. Authority is `PQ-031.md` leaf `.01`.
- The named proof is a Rapier lab pair: idle newtonian controls, rest 80, COM drift 42, planted rock at x=220 r=18, 480 ticks. It is not two flown default-route pirates.
- "Reads as one system in the camera" is leftover `spinDeg` (≥ 90° in 5/5). There is no shipping-camera strip. The job forbade headed capture.
- Shared helm loss and coupled inertia are leftover PQ-137.09 (`_shareHelmLoss` publishes `tether_share`; slack transmits nothing). Leftover proof is `test/chain-tether-share.test.mjs` on a taut attachment fixture whose `defId` is `tether_standard`. The share loop is def-agnostic. This leaf did not retune it and did not re-run that fixture as a Twin Bridle row.
- Bounded line load is leftover `attachment_twin_bridle` `maxTension` 9200 plus leftover `TWIN_BRIDLE_TENSION_OVERSHOOT = 1.05` in the Rapier owner. The orbit test now pins leftover length swing ≤ rest × leftover `maxStretchRatio` 0.75 (60). It does not re-prove a rating-break cut.
- Hitch cannot fit the M head: leftover S utility vs `mod_twin_bridle_m`. Leftover pin is `test/massline-twin-bridle-head.test.mjs`. This leaf did not edit `modules.js`.
- Leftover pair admission (`validateTwinBridlePair`) still refuses two heavy anchors, NPC heavies, and attachment cycles. That pin stays in the leftover head test. This leaf did not retune it.
- The same leftover files already hold 031.02 specialist cut / heavy ignore. This leaf did not rewrite those.
- `tumbleStates` still maps `tether_share` through the process `COMBAT_FLAGS` map. Production already has that map ON. This leaf did not rewrite tumble.

## Proof

| Seed | Hit | Spin deg | minRock | distSwing |
|---|---|---|---|---|
| 0 | yes | 577.2 | −2.2 | 4.1 |
| 1 | yes | 476.0 | −3.2 | 28.4 |
| 2 | yes | 577.2 | −2.1 | 4.1 |
| 3 | yes | 410.4 | −3.5 | 4.1 |
| 4 | yes | 507.2 | −2.7 | 12.4 |

Hits 5/5. Spin ≥ 90° 5/5. Length swing ≤ leftover stretch 60 in 5/5. Backend `rapier-dynamic`.

`node --test test/massline-twin-bridle-orbit.test.mjs` — 1 pass.

Leftover Hitch pin, not rewritten: `node --test --test-name-pattern "Hitch cannot fit the Twin Bridle" test/massline-twin-bridle-head.test.mjs` — 1 pass.
