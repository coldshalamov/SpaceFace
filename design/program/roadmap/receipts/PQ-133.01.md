<!-- PROGRAM_EVIDENCE_RECEIPT
packetId: PQ-133
leafId: PQ-133.01
acceptance: proven
disposition: PASS
candidateCommit: 78760ab2eb5f34de92cfd880a2e66969a0584d17
-->
# Receipt — PQ-133.01 Combat Lab extension

```yaml
packet: PQ-133.01
candidateCommit: 78760ab2eb5f34de92cfd880a2e66969a0584d17
lifecycleClaim: implemented
acceptanceClaim: proven
disposition: PASS
seed: 1864401122
browserRoute: PASS
electronRoute: PASS
```

## Player outcome

The ordinary game root now reaches Sandbox → Combat Lab in both Browser and Electron. A player can
select Hornet, Physics Toolkit, Wasp Flight, Lagrange Crucible, seed `1864401122`, and wave 1; launch
through the real `game:new` path; reopen usable pause/step/speed/refill/invulnerability/clear controls
and authoritative telemetry; and relaunch the same seed through that same path.

The route witness verifies the active owned ship, not just the form: all three selected weapons are
fitted through the live ships owner. Six Lab-owned Wasps materialize through spawn budget at the
authored Lagrange center `(-500, 800)`, and the pre-simulation spawn receipt is byte-equal after
Relaunch same seed. Both launches pass the exact opening-submission admission gate.

## Defects closed at acceptance

- Cached Lab controls survived hide/show but retained disposed state; cached reopen and second reopen
  now refresh usable controls.
- A fresh Lab silently rejected every Physics Toolkit weapon at the research gate. The Lab now walks
  the canonical tech/economy writers, begins its ephemeral run before arena entry can autosave, and
  fits the selected hardware through `ships.fitModule`.
- A second loading transition retained prior opening-admission state; the per-run latch resets.
- Opening validation depended on ambient cached programs that the driver could legally evict; the
  exact opening compile subjects now define the required set.
- An evicted render-package source task could strand concurrent callers on a stale record; all stale
  observers now retry without deleting a newer task.
- Lagrange and Cinder Lab centers had no matching authored zones. Both centers are now explicit; the
  Ceres structural pin records the intentional zone-count change from 5 to 6.
- The long Sandbox STAGE shrank beneath its contents and the LIVE TOOLS apron painted over Quick
  Setup cards. The full Sandbox instrument now owns vertical scroll and its flex regions cannot
  collapse through each other; the route asserts zero stage/apron content overlap.
- The cold seed input collapsed to a token-sized field; it now keeps a readable 14-character floor.

## Verification

- `node --test` over Lab setup/same-seed/build-code/screen/controls plus asset disposal and opening
  admission: **123/123 pass**.
- `npm run check:crucible:lab-route`: **PASS** in Browser, including fitted build, six owned enemies,
  authored arena position, same-seed relaunch, zero layout overlap, and zero page/request errors.
- `npm run check:crucible:lab-route:electron`: **PASS** with the same assertions in an isolated
  Electron profile.
- `npm run check:playable`: **16/16 pass**, including save/Continue, visible authored hull, controls,
  shaders, and served assets.
- `npm run check:baseline`: 13/14 initially; the only red was the intentionally stale PQ-020 Ceres
  structural digest after the new Cinder approach zone. Its focused six-test suite and live checker
  pass after re-pinning exactly `authored.zones: 5 → 6`; the other 13 baseline gates were already
  green, including both sim goldens, save schema, render packages, opening mesh defer, flight, and
  the Massline aggregate.
- Current evidence epoch:
  `design/program/roadmap/evidence/pq133-01-combat-lab/EPOCH.json`.

The in-app Browser plugin could not attach because its local kernel assets were missing, so rendered
QA used the repository's Playwright Browser/Electron harness. Both current captures were inspected at
original resolution; no framework overlay, blank page, section overlap, or relevant console error is
present.

## Remaining boundary

This receipt proves Phase 1 reachability, deterministic setup/relaunch, controls, telemetry, build
code contracts, and the supported Wasp materialization claim. It does not claim that a headless agent
won a dogfight, that the Lab is fun, that every catalog combination was manually flown, or that any
later `PQ-133.02`–`.13` leaf is accepted. The parent `PQ-133` therefore remains open.
