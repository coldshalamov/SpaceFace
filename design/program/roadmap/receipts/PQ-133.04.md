<!-- LIFETIME: RECEIPT -->
# PQ-133.04 — bounded Foundry implementation candidate

Date: 2026-09-14. **FAIL / blocked on shared owners; not an implemented or integrated leaf.**
Controller review remains required. This is a usable partial code candidate plus executable
acceptance failures, not permission to advance PQ-133.05 or to restamp the historical build map.

## Identity and ownership

- Checkout: `C:\Users\93rob\Documents\GitHub\SpaceFace`, existing `master`; no worktree.
- Entry/base: `3bd28815e924a85cf3b952284b7fa6934ef4c487`, matching the controller observation.
- Implementation result: `6746fafd50d86cbe5c14080b1d8abfccd0098392` (four files).
- This receipt is a separate documentation-only follow-up; its commit is identified in the handoff.
- Before mutation: cwd/branch/HEAD, full porcelain status, empty staged-name list, NOW contents,
  `node scripts/check-now-liveness.mjs`, and exact allowed-path status/diffs were inspected.
  The heavily dirty checkout was retained. Live ownership rows did not reserve these allowed files.
- All six authorized production paths were clean at entry. Only the three named below were changed.
  No reset, clean, restore, checkout, stash, broad add, golden edit, package-script edit, or push.
- NOW, queue, build_map, other tests, foreign dirty/untracked paths and all shared-owner sources
  were left untouched. Both staging and commit names were checked with exact pathspecs.
- Live reconciliation: PQ-133.03 is implemented; PQ-133.04 is ready / acceptance unproven.
  Historical `.04 [DONE]` is not evidence. Lifecycle and queue have not been changed.

Complete candidate changed-file list:

| Path | Change |
| --- | --- |
| `src/combat/attackPropagation.js` | Refuse malformed bounce counts, non-finite/fractional/inconsistent shared proc budgets and invalid generations before spending or mutating continuation state. |
| `src/data/enemies.js` | Add real `mirrorjaw_foreman`: existing Bastion hull, committed-brawler doctrine, concussion/Pulse weapons, slow turn, finite damageable defenses, zero bounty and no authored loot. |
| `src/data/survivalWaves.js` | Foundry/Helios wave ten uses Mirrorjaw with the existing six staggered swarmers; truthful recipe question and derived boss-circuit identity. No cap, count, cadence, reward or planner-owner change. |
| `test/pq-133-04-foundry.test.mjs` | Thirteen deterministic/adversarial tests, including five deliberately unsatisfied shared-owner acceptance assertions. No skip/todo converts missing behavior into a pass. |
| `design/program/roadmap/receipts/PQ-133.04.md` | This bounded handoff and exact outstanding ownership requests. |

Unchanged authorized files: `src/core/physicsAuthority.js`, `src/data/survivalArenas.js`.
`src/systems/arenas/ricochetFoundry.js` remains absent. Foundry already means `helios_core`;
`combatLabSetups` owns its identity, `survivalArena` owns wave installs, and `swarmArena` owns
Swarm terrain. An unregistered duplicate controller, side-effectful data module, or unused physics
API would not connect this slice.

## What the candidate actually does

The malformed-budget reproducer failed before the guard and passes after it. Valid AttackSpec,
lineage, continuation, Smart Bank selection and proc costs remain on existing owners. No projectile
respawn or alternative damage/physics implementation was added.

The ten existing Foundry recipes remain pure, frozen, repeatable and bounded through `planWave`,
`spawnBudget`, `waveMaterialization` and `survivalWave`. Waves one through nine are reused,
not claimed as newly authored machinery. Wave ten now materializes an actual Mirrorjaw, not an
unknown-id fallback to a wasp. Its escorts use the ordinary seven-body total at ticks 0/90/180.
In the focused test it dies after **297 canonical Pulse damage hits at level 4**, emits one kill,
releases its spawn reservation and emits no campaign credit or loot drop. This exercises
`combat.onHit`; it is not a claim that 297 player-aimed projectiles hit in a live fight.
The ten-wave cleanup test uses scripted lethal damage and the normal destruction-event budget
release, so it proves owner bookkeeping, not player survivability or physical machinery cleanup.

Campaign snapshot and fitting-reference checks pass in the boss test. Browser run-wallet collection
also leaves campaign credits at 5000. Persistent save/inventory/fitting code was not changed.
The state remains `mode: flight` with orthogonal `run`; explicit phase owners were not replaced.

## Required shared-owner work — exact material blockers

These requests are NOT an expanded write authorization. Coordinate ownership before editing.

### R1 — physics receipt issuance and fail-closed combat (CRU-025/026)

Owners: `src/core/physics.js`, `src/core/surfaceContact.js`,
`src/combat/attackHit.js`, `src/combat/surfaceReflection.js`.

- Physics emits projectile-hit payloads at `physics.js:660/698` without this surface receipt.
- `attackHit.js:108` (`receiptForContact`) manufactures a branded receipt when the supplied
  receipt is absent or forged. Thus the kernel's WeakSet rejection does not protect live combat.
- `surfaceReflection.js:80` accepts a valid brand belonging to another projectile; body/surface,
  freshness and spec/lineage association are not enforced at this seam.
- Required change: issue trusted contact data from the actual sweep/solver, bind it to the actual
  contact/body/tick, reject missing/forged/mismatched/stale input in combat, and remove fabrication
  from combat and toy helpers. Preserve current same-body lifetime semantics.
- Reproduce: focused tests named `shared-owner R1`. Absent receipt authorizes a bounce;
  a receipt for `bolt` also authorizes `unrelated-bolt`.

`physics.js` is foreign dirty. The other named files being clean does not put them in this lane.

### R2 — relative contact velocity and sole-writer continuation (CRU-025/026/029)

Owners: R1 owners plus `src/core/sg02DynamicBodyOwner.js` and the existing physics command membrane.

- `surfaceContact.js:86/113` stores projectile velocity only; no surface velocity at the
  contact point or relative velocity. The moving-plate test expects surface (2,0), relative (10,0)
  and outgoing (-8,0), but surface velocity is undefined.
- `surfaceReflection.js:101/110` reflects absolute velocity and directly writes entity motion;
  `attackHit.js:52/53` also nudges position. These are not a continuation command consumed by physics.
- Required change: measured linear/angular surface velocity at the contact point, bounded
  surface-relative reflection, and same authoritative-body continuation applied by the physics
  owner. Truthful shutter/loose-plate movement must be integrated with the actual body owner.
- Extending only the allowed `physicsAuthority.js` would leave an unused API: no permitted
  consumer can be wired. Do not add dead interfaces as an implementation claim.

### R3 — authored room and actual material surfaces (CRU-028/029)

Owners: `src/systems/survivalArena.js`, `src/systems/swarmArena.js`,
`src/data/arenaModuleLibrary.js`, `src/data/combatLabSetups.js`, plus R1/R2 physical owners
and the existing camera/framing consumer after its controller resolves exact ownership.

- `survivalArena.js:246` returns an empty Foundry idle room; the first two waves install no
  reflective Foundry geometry. Later Helios phases install wells/repulsors/damping/cover/mines,
  not the promised moving shutters/furnace/loose reflective plate.
- `arenaModuleLibrary.js:301` manufactures a ray-based bank without AttackSpec eligibility.
  `survivalArena.js:894-900` uses that helper and directly writes velocity. An ordinary direct
  shot bounces in the focused helper test. Shutters use predictive line deletion instead of
  authoritative contact/movement.
- Required change: materialize authored plate/furnace/absorbent/gate/loose-plate geometry once
  through the existing install/materialization/field owners, retain slot caps and owned teardown,
  and drive room/camera law from the shared truth. Remove the direct/enemy-shot bypass.
- Reproduce: both `shared-owner R3` tests. This cannot be fixed by adding catalog descriptions.

### R4 — normal route, boss machinery and causal presentation (CRU-027/031 + shared invariants)

Owners: `src/ui/crucibleLaunch.js`, `src/systems/survivalRun.js`,
`src/systems/survivalWavePlanner.js`, `src/systems/weapons.js`,
`src/combat/subsystems.js`, R1/R3 owners, `src/systems/presentationOrchestrator.js`,
`src/systems/presentationAdapters.js`, `src/render/vfx.js`, `src/audio/audioSystem.js`.

- Ordinary Crucible defaults to Swarm, whose planner bypasses these authored recipes. The scored
  selection reaches Helios but is a thirty-wave Gauntlet: wave ten leads to refit, not ten-wave victory.
  The observed Ricochet Runner carries autocannon/concussion/Bank Shot/trap, not the requested
  same Pulse Laser direct/Bank/Smart demonstration. That public route still needs to be resolved.
- Mirrorjaw is killable and uses existing real AI/weapons, but has no reflective frontal ram
  surface, external rear machinery, breakable subsystem transitions or three verified solution
  families. Catalog metadata cannot implement those missing combat/surface mechanisms.
- The toy route emits `combat:bankShot` directly to VFX; compiled contact continuation does not
  publish equivalent receipt-backed causal presentation through the semantic arbiter.
  Reuse existing VFX/audio/caption arbitration, reduced-motion settings and slot budgets.
- The real route failed CLEAN in `vfx.js:11796`, `_updateProductionRcs`, reading an undefined
  player's `pos` during the route. File itself is clean and untouched; this is not attributed to
  this patch merely because other renderer sources are dirty.
- `survivalRun.js`, `subsystems.js` and `audioSystem.js` are foreign dirty. Resolve the
  actual shared owner before any fix; do not treat this request as a claim over those files.

### R5 — deliberate content delta versus existing acceptance fixtures

Owners: controller/product review and `test/crucible-wave-schema.test.mjs`,
`test/crucible-wave-planner.test.mjs`. Do not re-record a golden to obtain green.

Replacing Iron Maw with the specified committed ram changes the wave-ten snapshot and violates
the previous requirement that all arenas have identical enemy content. It also removes Foundry's
only `capital_broadside` encounter. That tactical-vocabulary loss needs a product disposition
or a reviewed composition solution before the no-quality-cut gate can pass. This candidate
does not silently add a second boss or alter other waves to appease the snapshot.

Existing later-act template reuse and the shared boss circuit also inherit Mirrorjaw's identity;
no .07/.10 feature work is claimed. Review that reuse alongside the bounded ten-wave route.
The unrelated pre-existing `crucible-results` starter expectation remains unresolved.

## Validation and evidence

Commands ran in the shared checkout; results therefore bind this candidate PLUS its preserved
foreign dirt, not a clean-room commit checkout. No unchanged failing acceptance cell was retried.
Tests were rerun only after a material candidate/harness correction.

| Exact command | Result |
| --- | --- |
| `node scripts/check-now-liveness.mjs` | Entry ownership/liveness inspected; no owned-path overlap. |
| `npm run check:baseline` (before editing) | PASS 15/15, about 45.2 s. No spawn EPERM reproduced. |
| `node --test test/pq-133-04-foundry.test.mjs` | FAIL: 8 pass / 5 fail / 0 skipped. Five failures are R1/R2/R3 above. |
| `npm run check:attack-spec` | PASS 71 before and after the propagation change. |
| `npm run check:crucible:run` (entry) | FAIL 1/14 suites: results starter mismatch, actual ricochet_runner / expected physics_toolkit. |
| `npm run check:crucible:run` (candidate) | FAIL 3/14 suites: schema 13/15 (doctrine coverage + arena equality), planner 9/10 (wave-ten snapshot), results 27/28 (same inherited starter mismatch). Other 11 suites pass. |
| `node --test test/crucible-arena.test.mjs test/crucible-swarm-arena.test.mjs test/crucible-survival-run.test.mjs` | PASS 58/58; controller baseline retained. |
| `npm run check:combat` | PASS 8 combat grammar checks and SG-03 save/reload. |
| `npm run check:save-schema` | PASS version 14, 285 paths. |
| `npm run check:sim` | PASS 20 repeats, 720 ticks, seed 47, reload tick 600, existing telemetry unchanged. |
| `npm run check:sim:compare` | PASS original expected telemetry and reload comparison. |
| `npm run check:crucible:route -- --gauntlet` | FAIL 12/13: CLEAN VFX exception; route details below. |
| `npm run check:playable` | PASS 16/16 including real Browser New Game, controls, quick-save/Continue, clean/shader/assets/hull checks. |
| Scoped `git diff --check` and staged `git diff --cached --check` | PASS on candidate files. |
| Whole-checkout `git diff --check` | FAIL on widespread inherited foreign whitespace, e.g. .cursor/.grok/docs; not repaired or rerun. |

Simulation SHA256: `371585f229c9795d513b009da5afa9942533d52156a0b02e74469932e3df1524`,
identical to its baseline. No golden was edited.

Browser evidence is the canonical runner's stdout, summarized durably here:
headless Chromium, 1440x900, seed 4242, `http://127.0.0.1:8440/`,
main menu -> Crucible -> scored Gauntlet -> flight, helios_core, wave 1.
Six wasps, spawn budget 6/24; HUD says WAVE 1 / 30. Scripted kills reach draft
(70 XP / 90 score); six chips yield 12 run credits and leave Adventure credits at 5000.
Three offers; Autocannon S fitted. Death produces an attributed result, and restart reproduces
`helios_core_w01_intro_mass:w1:a0:1092`. The VFX CLEAN stack begins:

```text
TypeError: Cannot read properties of undefined (reading 'pos')
  at Object._updateProductionRcs (.../src/render/vfx.js:11796:23)
  at Object._updateEnergyPlume (.../src/render/vfx.js:11702:10)
  at Object._updateEnergy (.../src/render/vfx.js:11097:12)
  at Object.update (.../src/render/vfx.js:10474:23)
```

This is one ordinary public-route diagnostic, not an aimed ten-wave playthrough. It is not
rerun to corroborate the same CLEAN failure. The playable Browser check separately passes;
its diagnostic warnings include GPU bloom submission stalls, not a Foundry p95 sample.
Both canonical runners own and close their Browser/server resources and use ephemeral profiles;
no player-profile save route was used.

**DEFERRED:** Foundry p95 <=16.7 ms. No candidate-bound Foundry performance trace was captured;
there is no measured p95 or performance evidence path to cite. This receipt and the test source
are the durable diagnostic evidence paths, not substitutes for a trace.
**NEEDS HUMAN / shared-owner completion:** representative play-size screenshots, visual/collision
agreement, reduced-motion/audio causality, aimed Pulse direct/Bank/Smart play, ten-wave completion,
and Electron/Sandbox/Lab parity. The game-playtest skill's visual gate is unsatisfied: the canonical
route runner intentionally captures no screenshots. No visual acceptance is claimed.
The systematic-debugging skill guided reproduction-first changes; it did not widen ownership.

## Disposition by requirement

| Requirement | Status |
| --- | --- |
| CRU-025 contact receipt | FAIL live authority/relative-velocity gaps, R1/R2. |
| CRU-026 bounded ricochet | Partial kernel PASS; live physics/material path FAIL, R1/R2/R3. |
| CRU-027 Smart Bank | Deterministic selector PASS; public Pulse route/causality unproven, R4. |
| CRU-028 Foundry greybox | FAIL, R3. |
| CRU-029 moving machinery | FAIL, R2/R3. |
| CRU-030 ten waves | Deterministic plans/materialization/cleanup PASS; full physical block/route/tuning FAIL, R3/R4/R5. |
| CRU-031 Mirrorjaw | Killable canonical recipe PASS; reflective ram/machinery/solutions FAIL, R4. |

Next action: controller review this **partial FAIL candidate**, resolve R1–R5 ownership/content,
and explicitly authorize the needed shared-owner edits before continuation. Do not mark the
leaf implemented, accepted or integrated from these focused passes.
