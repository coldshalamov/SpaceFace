<!-- LIFETIME: RECEIPT -->
# PQ-028.02 — The player-built sling

```text
STATUS DONE

WHAT I FOUND     The claim Throughline is already the Ceres 96×48
                 cylinder. Claims fabricates it from
                 mod_throughline_sling, offers hauler/trader hooks,
                 and traffic assigns one ambient hull to that route.
                 Seed 2802 already puts a collides:true Mule through
                 the claim tube at more than 2× its cruise. The
                 existing test stamps the hauler; a no-stamp session
                 still gets the same hook and the same exit.

WHAT I CHANGED   Nothing. The live writers and the named Rapier
                 proof were already on the tree. Do not retune a
                 passing sling. Do not invent a second travel
                 system.

WHAT YOU WILL FEEL   Build the Throughline on a commissioned
                 refinery claim. After alignment, a hauler or
                 trader starts running that ring. Ride it or
                 watch them leave faster than cruise. Still a
                 local toy with a schedule, not Atlas travel.

THE NUMBERS      bar | before | after | target
                 Mule exit / cruise | already 2.13× (181 / 85) | 2.13× | ≥ 2.00×
                 hook assigned (no stamp) | 1 ambient hauler | same | ≥ 1 NPC route
                 hook id | throughline:claim_pq028_02 | same | claim ring
                 ring | 96×48 cylinder from from→to | same | Ceres .00/.01 untouched
                 entered / exited | true / true, tick 36 | same | aligned tube
                 scenario | test/pq-028-02-claim-sling.test.mjs | seed 2802 | Rapier
                 hook session | claims.travelInfrastructureHooks + traffic._applyClaimTravelHooks | seed 2802 | 1 assigned

THE FRAMES       Headless Rapier real-path is enough. No headed
                 GPU strip. Backend rapier-dynamic, flight v3,
                 production profile.

NEXT             Integrator updates the queue. Family leftover:
                 none after this leaf. Do not mark the queue from
                 this receipt.
```

## Files

- `src/systems/travelLanes.js` — already live (`buildClaimSlingRing`, `_applyClaimSlingRings`)
- `src/systems/claims.js` — already live (`_newTravelInfrastructure`, `travelInfrastructureHooks`)
- `src/systems/traffic.js` — already live (`_applyClaimTravelHooks`, `_stepClaimTravelRoute`)
- `test/pq-028-02-claim-sling.test.mjs` — already tracked (stamped physics)
- `test/claim-travel-infrastructure.test.mjs` — already tracked (module build + hook assign)

Not edited this pass. `program-queue.json` and `NOW.md` untouched.
`design/program/roadmap/receipts/PQ-028.00-REPORT.md` left untracked.
