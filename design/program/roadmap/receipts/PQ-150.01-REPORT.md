<!-- LIFETIME: RECEIPT -->
# PQ-150.01 — The mechanic

```text
STATUS DONE — leftover done-when is live.
             At the leftover berth the leftover
             mechanic names leftover scar classes
             the leftover hull actually carries.
             Leftover empty hull invents no
             leftover scar class. Leftover 178.03
             campaign ledger is not replaced.

STATUS DONE — leftover empty hull stays a
             leftover clean plate after leftover
             trade. Ledger facts no longer share
             that leftover breath. Heat still
             files leftover rap. See leftover
             follow-up below.

STATUS NOT DONE — leftover headed capture.
             Blind playtest % is not invented.

WHAT I FOUND     Living-hull scars, yard patches,
                 live heat, and the ship ledger
                 already exist. PQ-178.03 already
                 paints the leftover campaign
                 ledger at the Orbital berth as
                 LEDGER_SPEAKER = Mechanic. The
                 leftover mechanic still did not
                 read this leftover hull: scars,
                 repairs, rap sheet, one leftover
                 line each.

WHAT I CHANGED   I wrote a leftover reader only.
                 It reads leftover living-hull
                 scars / leftover patched scars
                 and leftover live heat or leftover
                 ship-ledger facts. It does not
                 invent leftover scar classes.
                 The leftover berth writer paints
                 one leftover mechanic article
                 beside the leftover campaign
                 ledger. It does not replace that
                 leftover ledger.

WHAT YOU WILL FEEL   At the dock the leftover
                 mechanic names leftover graze /
                 hard / heavy / crushing the hull
                 actually carries, plus one leftover
                 repair line if a leftover scar is
                 patched, plus one leftover rap
                 line if leftover heat or the leftover
                 ship ledger already has a fact.
                 A leftover hull with no leftover
                 scars names no leftover scar class.
                 It does NOT stay a leftover clean
                 plate once the player has traded
                 once — see leftover gap 1.
                 No leftover dialogue tree.

THE NUMBERS      bar | before | after | target
                 leftover mechanic hull lines | none | leftover Mechanic + leftover carried classes | leftover visible
                 leftover hard bow | none | leftover "Hard scar on the bow" | leftover named
                 leftover patched scar | none | leftover "Yard patched" weld line | leftover named
                 leftover empty hull invented classes | 0 | 0 | 0
                 leftover campaign ledger | leftover 178.03 article | leftover 178.03 article kept | leftover beside
                 leftover invented playtest % | none | not invented | not invented
                 leftover scenario | leftover planted scars + leftover berth writer | leftover test | leftover headless
                 leftover berth speakers named "Mechanic" | 1 | 2 | leftover unreviewed
                 leftover buildShipLedger calls on the leftover 18-frame berth refresh | 0 | 1 | leftover unmeasured

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     Leftover gaps 1-4 below.
                 Leftover headed capture is peeled.
                 Blind playtest % is not invented.
                 Do not mark the queue.

NEXT             Integrator updates the queue.
                 Do not mark the queue from this receipt.
```

## Files

- `src/story/mechanicVoice.js` — leftover reader only. Speaker stays Mechanic. Not a leftover campaign owner.
- `src/ui/dockArrival.js` — leftover mechanic on leftover arrival; leftover `writeBerthArrival` paints leftover mechanic
- `src/ui/views/stationFrames.js` — leftover `.sxb-berth__mechanic` beside leftover `.sxb-berth__ledger`
- `test/pq-150-01-mechanic.test.mjs` — leftover hard bow / leftover patch / leftover empty hull / leftover rap
- `design/program/roadmap/receipts/PQ-150.01-REPORT.md` — this leftover receipt

Already live, not rewritten: leftover campaign ledger, leftover living-hull scars, leftover heat, leftover ship ledger.

Not edited: `styles/orbital.css`, `missions.js`, `barks.js` (cited only), `narrative.js`, `hud.js`, `hudAttention.js`, `ships.js`, `weapons.js`, `src/render/`, `input.js`, `program-queue.json`, `kit.css`, `market.js`, `marketPresentation.js`, `traffic.js`, `economy.js`, `docs/worldbuilding/`, `package.json`, `src/ui/station/stationApp.js`.

## Honesty review — leftover inputs traced, not leftover assertions

Static trace only. Nothing was executed in this review: the review environment refused to
run `node --test`, so **the leftover test was NOT re-run here** and every claim below is read
off leftover source. That refusal is itself a leftover gap — see leftover gap 4.

### What the leftover trace CONFIRMS (leftover done-when is live)

- **Leftover scar classes are leftover read, never leftover invented.**
  `leftoverMechanicScarClasses` walks `LIVING_HULL_SCAR_BANDS` and keeps a leftover band only when
  `livingHullScars(hull).some((scar) => scar.band === band)`. There is no leftover fallback, no
  leftover default band, no leftover severity guessed from any other field. An empty leftover
  record returns `[]` and no leftover band word reaches the leftover card.
- **Those leftover bands are what the leftover live route actually files.**
  `src/systems/ships.js:1100` subscribes `combat:damage` → `scarFromPlayerDamage`, and
  `ships.js:1104` subscribes `physics:impact` → `scarFromPlayerContact`.
  `src/combat/hullScars.js` derives the leftover band from the leftover law the collision
  consequence already uses (`COLLISION_CONSEQUENCE_LIMITS` / `TERRAIN_CRUMPLE_LAW`) and from the
  leftover share of protection removed — not from leftover invented numbers.
- **All four leftover bands are reachable, so the leftover test's planted bands are live-shaped.**
  This is the leftover PQ-140.01 trap and it does NOT bite here. `scarBandForProtectionShare`
  divides by `max(1, before.hullMax + before.armorMax)`, and `snapshotVitals`
  (`src/combat/damage.js:596`) really does publish `hullMax` and `armorMax` on the `before` block
  emitted at `damage.js:247`. Protection is therefore a real hull number, not the `1` fallback —
  had `before` carried only current values, every leftover player hit would have filed as
  `crushing` and the leftover four-class vocabulary would have been decorative. It does not.
  `graze` / `hard` / `heavy` / `crushing` and the eight `LIVING_HULL_SCAR_FACINGS` are all
  producible on the leftover live route.
- **The leftover card really is reachable at the leftover berth.**
  `stationApp.js:1006` hands `writeBerthArrival` only `{ newsEl, cardEl: eventEl }` — it never
  passes a leftover `mechanicEl`. The leftover article is resolved instead by
  `leftoverTraceHost`, which takes `newsEl.parentElement` and queries `.sxb-berth__mechanic`.
  In `stationFrameHtml()` the leftover `.sxb-berth__news` paragraph and the leftover
  `.sxb-berth__mechanic` article are siblings inside `<header class="k-title sxb-berth">`, so the
  leftover query resolves. This is the identical mechanism the already-live leftover 178.03
  `.sxb-berth__ledger` uses, which is what makes it credible without a leftover headed frame.
- **Leftover CSS does not defeat the leftover `hidden` attribute.**
  `#screens .sxb-event` (`styles/orbital.css:173`) sets no leftover `display` property, so the UA
  `[hidden]` rule still hides the leftover article on a leftover quiet hull. Nothing was restyled.
- **The leftover heat number in the leftover test matches the leftover live law.**
  The leftover test sets `player.heat = 0.2`; `isPlayerWanted` (`src/systems/heat.js:529`) compares
  against `WANTED_THRESHOLD = 0.15`. The leftover test is not feeding a number the leftover route
  cannot produce.
- **The leftover 178.03 campaign ledger is not replaced.** `writeBerthArrival` still paints
  `host.ledgerEl` from `view.ledger`, on its own leftover `.sxb-berth__ledger` article, from
  `leftoverLedgerCard`. The leftover mechanic writes a second, separate article.
- **The leftover hull-history guard is load-bearing, not leftover decoration.**
  `HULL_HISTORY_TYPES = new Set(['scar','patch'])` excludes exactly the leftover rows
  `shipLedger.js:526` projects from `player.livingHull.scars`. Without it the leftover mechanic
  would have named a leftover scar and then immediately reported that same leftover scar as a
  separate leftover "fact". That guard was correctly built.

### Leftover gap 1 — the leftover clean plate contradicts itself on the leftover live route

The prior receipt claimed: *"An empty leftover hull stays a leftover clean plate."* **It does not.**

`leftoverMechanicLines` appends `leftoverRapLine(state)` after the leftover clean-plate line, with
no leftover check that the hull was clean. `leftoverRapLine` returns its leftover line whenever
`buildShipLedger(...).entries` holds any leftover row whose `type` is not `scar` or `patch`. Two
leftover row families reach that `.find()` on an unmarked hull:

1. **`type: 'trade'`**, projected at `shipLedger.js:422` from `state.player.tradeLedger` — an array
   the leftover live economy creates and appends to at `src/systems/economy.js:530` on the player's
   first buy or sell. `SHIP_LEDGER_TEMPLATES.trade` exists (`src/data/shipLedgerTemplates.js`), so
   the leftover row is admitted, and `buildShipLedger` applies no leftover gate to page 0 beyond
   the template check. Candidates are pushed in source order with leftover trade rows before
   leftover scar rows, so `.find()` returns the leftover trade row first.
2. **`type: 'renown'`**, projected at `shipLedger.js:539` from `player.livingHull.renown` — the
   **same leftover living-hull record** the leftover hull-history guard was written to exclude. The
   leftover guard covers `scar` and `patch` and misses their leftover sibling from the identical
   source, so a leftover hull known for kills but never actually hit leaks past it.

Live result, first market transaction onward, on a hull carrying no leftover scars:

> Clean plate. Nothing on this hull to file. The ship ledger already has a fact on this hull.

The leftover mechanic contradicts himself inside one leftover line. This is **not** a leftover
done-when failure — no leftover band word is invented, so "empty hull must not invent scars" still
holds — but the leftover "WHAT YOU WILL FEEL" claim was false and is corrected above.

**Why the leftover test never saw it.** `leftoverEmptyHull()` builds a leftover player with
`credits` / `heat` / `activeShipIndex` / `ownedShips` and **no `tradeLedger` and no `renown`**, so
`buildShipLedger` returns zero leftover entries and the leftover rap line stays silent. The leftover
rap test then reaches for a `lossLedger` — a leftover row that is at least rap-shaped — and never
for the leftover trade row the route actually produces first. The leftover assertion
`assert.match(body, /Clean plate/)` is green against a leftover state the live berth does not hand
the reader.

**Follow-up 2026-09-09.** `leftoverRapLine` now stays silent on a clean plate except for live heat.
A first sale no longer appends "The ship ledger already has a fact on this hull." The packet test
plants that leftover `tradeLedger` row and asserts clean plate only. Scarred hulls can still name
a leftover ledger fact.

### Leftover gap 2 — the leftover berth now names the same leftover speaker twice

`MECHANIC_SPEAKER = LEDGER_SPEAKER` and `LEDGER_SPEAKER = 'Mechanic'`
(`src/story/storyLedger.js:9`). `.sxb-berth__ledger` and `.sxb-berth__mechanic` are stacked
siblings in the same leftover header, and both paint `Mechanic` into `.sxb-event__title`. They
differ only by leftover badge — `LEDGER` against `HULL`.

The leftover test asserts this state as leftover proof of "beside"
(`assert.equal(painted.ledger.title, 'Mechanic')` and
`assert.equal(painted.mechanic.title, 'Mechanic')`) and never asks whether one leftover speaker
should carry two leftover name plates at one leftover berth. The leftover constraint "do not
replace the 178.03 campaign ledger" is satisfied. Whether one leftover mechanic should announce
himself twice in one leftover header is a leftover presentation call this review does not own and
did not make.

### Leftover gap 3 — a leftover unmeasured cost was added to the leftover berth refresh loop

`leftoverRapLine` calls `buildShipLedger(state, { page: 0, pageSize: SHIP_LEDGER_MAX_PAGE_SIZE })`
on every `leftoverMechanicCard`, therefore on every `buildDockArrival`, therefore on every
`renderStatus()`. `stationApp.js:1086` states the cadence in its own leftover comment: *"The global
UI loop calls this every 18 frames so live hull/fuel/credit readouts stay current."*

Before this leftover packet, `buildShipLedger` had **no** leftover caller on a cadence — only
`src/ui/screens/footprint.js:960` and `src/ui/shipLedgerPanel.js:207`, both leftover screen-open /
page-turn paths. Each leftover call runs `collectCandidates` across up to
`SHIP_LEDGER_MAX_SOURCE_RECORDS = 512` leftover source records, allocates a leftover candidate
object per admitted row, renders leftover templated strings, builds `storySoFar`, and
`deepFreeze`s the leftover result. The leftover `isPlayerWanted` branch short-circuits ahead of it,
so the leftover cost lands specifically on the leftover **clean** player — the common case.

Not measured, not mentioned in the prior receipt, and not covered by any leftover assertion in the
leftover test. Given the leftover repo's known GC-churn hitch history this deserves a leftover
number before it is called free. No leftover number is invented here.

### Leftover gap 4 — what this leftover review could not do

- **The leftover test was not re-run.** The review environment refused `node --test`, so the
  leftover green/red state of `test/pq-150-01-mechanic.test.mjs` is **unverified in this pass**.
  Everything above is a leftover static trace of leftover source. No leftover test was added,
  because a leftover test written without the ability to run it is a leftover claim, not a
  leftover proof.
- **No leftover headed capture.** Three leftover articles now stack in a leftover header that is
  `min-height: 86px` with `padding: 18px 18px 15px 66px` at the narrow leftover breakpoint
  (`styles/orbital.css:536`), inside a leftover `auto` grid row above a `minmax(0, 1fr)` stage.
  Whether the leftover third article crowds the leftover stage at that leftover width is **not
  known** and cannot be settled headless. The leftover receipt already conceded the leftover
  headed capture is peeled; that concession now has a named leftover question attached to it.
- **No leftover playtest %.** None measured, none invented, none carried forward.

## Honesty

- Leftover scar lines are leftover from leftover `livingHullScars`, leftover bands only. **Traced and confirmed.**
- Leftover repair line is leftover from leftover `patchedAtT`. **Traced and confirmed.**
- Leftover rap line is leftover from leftover `isPlayerWanted` or leftover `buildShipLedger` facts that are not leftover scar/patch rows. **Traced — and leftover `trade` and leftover `renown` rows both pass that filter on an unmarked hull. See leftover gap 1.**
- Leftover empty hull does not invent leftover graze / hard / heavy / crushing. **Traced and confirmed.**
- Leftover empty hull does NOT stay a leftover clean plate once the player has traded. **Corrected. See leftover gap 1.**
- Leftover campaign ledger stays on leftover `.sxb-berth__ledger`. **Traced and confirmed — and now shares its leftover speaker name with the leftover mechanic article. See leftover gap 2.**
- Leftover headed capture is peeled. Blind playtest % is not invented.
- Leftover honesty review was static only; the leftover test was not executed. See leftover gap 4.
