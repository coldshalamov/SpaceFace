<!-- LIFETIME: RECEIPT -->
# PQ-158.04 — Voice for the barks (receipt)

```text
DONE  PQ-158.04 — every radio line now has a directed voice, a subtitle, and a house
                  a stranger can name from the sound alone.

WHAT I FOUND     The 271 lines were catalogued, but the recordings were radio hash
                 with no pitch. Eight houses shared one squelch. The mechanic at
                 the berth had words and no voice.

WHAT I CHANGED   Gave each house a real fundamental (Vael 80 through Choir 170),
                 ran every line through an agent-run voice pipeline, and let the
                 mechanic speak on dock the same way. Subtitles are the spoken
                 line. Sim luck is untouched.

WHAT YOU WILL FEEL   Concord sits low and dry. Reach is thin and fast. Quiet is
                     slow and dark. Vael is under the floor. Docking, the mechanic
                     talks over the clunk. Every bark that plays also captions.

THE NUMBERS      bar | before | after | target | seed
                 corpus through pipeline | 271 catalog rows, hash WAVs | 271/271 captioned + 271/271 pitched WAVs | 271 | 15804
                 stranger names eight registers | 0/8 from bark WAVs (f0=0) | 8/8 unlabeled clips + 8/8 player-heard bark WAVs | 8/8 | 15804
                 mechanic at berth | text only | dock speaks bark_mechanic + caption | same pipeline | 15804
                 captions === spoken line | 271/271 | 271/271 | all rows | 15804
                 sim RNG | untouched | untouched | untouched | 15804
                 leaf tests | 9/9 callsign-only | 13/13 twice | pass twice | 15804

THE FEEL         Radio now has people in it, not one shared hiss.

NEXT             Queue row was already marked done by the integrator. --next still owns the live list.
```

Seed 15804. Audio stays presentation. A human with headphones was not in the room. The stranger here is the pitch tracker on the eight unlabeled callsign clips and on the eight player-heard bark recordings — that is the player-unit listen, not a label table.

Foreign synth, sample-generator, massline, mix, and settings hunks were not touched.

## Files

- `src/audio/barkVoice.js` — registers, pipeline, pitched utterance renderer, stranger namer
- `scripts/generate-bark-voice.mjs` — agent-run writer/checker
- `assets/audio/voice/` — 271 line WAVs, 9 register samples, 8 unlabeled clips
- `src/audio/audioSystem.js` — live bark play + captions; mechanic on dock
- `test/pq-158-04-bark-voice.test.mjs`

## Checks

- `node --test test/pq-158-04-bark-voice.test.mjs` — 13/13 twice (seed 15804)
- `node scripts/generate-bark-voice.mjs --check` — 9 register WAVs + 8 unlabeled clips byte-identical
- `node scripts/generate-bark-voice.mjs --all --check` — 271/271 line WAVs byte-identical
