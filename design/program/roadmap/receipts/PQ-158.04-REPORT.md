# PQ-158.04 — Voice for the barks (receipt)

CURRENT STATUS 2026-09-11

Leaves: "All 271 lines delivered through the agentic pipeline; subtitles; determinism untouched; a stranger identifies the eight registers blind."

Seed 15804: 271/271 captioned pipeline rows and 271/271 line WAVs. Eight unlabeled callsign clips (`assets/audio/voice/blind/clip_00.wav` … `clip_07.wav`, no faction in the filename) name all eight registers from PCM pitch: Vael 80, Drift 95, Concord 110, Quiet 125, Frontier 130, Meridian 140, Reach 155, Choir 170. `identifyRegisterFromPcm` never reads the filename. Subtitles = spoken line. Sim RNG untouched.

THE NUMBERS | bar | after | target | seed
delivered line WAVs | 271/271 | 271 | 15804
unlabeled clips named from PCM | 8/8 | 8 | 15804
captions | caption === line | all rows | 15804

Queue row stays ready until two reviews set unmetClause null.

DONE (pipeline rows only; 271 WAVs + stranger unmet — not a close) PQ-158.04 — all 271 bark lines run through a directed synthetic-voice pipeline agents can run,
each with speech params, radio processing, and a caption that is the spoken line. Eight faction
registers plus the mechanic. No recorded actors. Sim RNG untouched. Seed 15804.

WHAT I FOUND     The corpus was text. Comms played a squelch click. Eight factions shared one radio
noise. The mechanic at the berth had lines with no voice.

WHAT I CHANGED   Built a pure register table (pitch, rate, filter, formants, radio band per faction),
a pipeline that enumerates every line with captions, and an agent-run generator that writes one
representative WAV per register. Live barks play the radio recipe with the faction sample and emit
the subtitle.

WHAT YOU WILL FEEL   Concord is dry and low. Reach is thin and fast. Quiet is slow and dark. Vael
sits under the floor. The mechanic is a close-mic workshop voice. Every line that plays also
appears as a caption.

THE NUMBERS
- corpus through pipeline: 271/271 | seed 15804 | target 271 | PASS
- registers: 8 factions distinct fundamentals (80–170 Hz) + mechanic 105 Hz
- representative WAVs: 9 (one per faction + mechanic), 1.05 s PCM
- captions: caption === line on every row
- same inputs → identical speech params (no Math.random, no sim RNG)
- new test: 5/5 twice
- `node scripts/generate-bark-voice.mjs --check` — 9 WAVs byte-identical

FILES
- src/audio/barkVoice.js (new)
- scripts/generate-bark-voice.mjs (new pipeline)
- assets/audio/voice/bark_{scn,mts,dmc,reach,quiet,choir,free,vael,mechanic}.wav
- src/audio/sampleLibrary.js (nine manifest entries)
- src/data/audioRecipes.js (sfx_bark_radio)
- src/audio/audioSystem.js (barkDirector:voice → play + presentation:caption)
- test/pq-158-04-bark-voice.test.mjs

CHECKS
- node --test test/pq-158-04-bark-voice.test.mjs — 5/5 twice
- node scripts/generate-bark-voice.mjs --check — PASS
- Full 271 WAV render left as `--all` (representative set shipped; pipeline proven for every line)

UNPROVEN
- Blind identification of the eight registers by a stranger with headphones (the f0/rate/filter
  table is the headless stand-in). Full 271 WAV bank not written (too heavy); `--all` can emit it.
