<!-- LIFETIME: RECEIPT -->
# PQ-178.01 — Close 2026-09-22 (integration + stale-gate cleanup)

The leaf shipped in two moves: `d2089c9a6` wrote the three leftover beat sheets
and a first receipt, and the 2026-09-09 honesty review (recorded inside
`PQ-178.01-REPORT.md`) flipped STATUS to NOT DONE on one real defect — the
`bigger_boat` sheet sold `sling_in` as the stale 700 WU berth while the live
story settle is dest-dock only (`d9bef7f94`, `_entityAtStoryDestDock`).

`201fa3fbb` then corrected all five `sling_in` clauses on the sheet and flipped
`leftoverSlingInStill700Wu` to false. This closeout verified the tree state and
took the one remaining follow-up.

## Verified now

- `test/pq-178-01-beats.test.mjs` — **4/4 green**, including the law the review
  added (a sheet may not sell a settle the live route refuses; the method set
  behind `_entityAtStoryDestDock` is derived from live source).
- `node scripts/check-beat-standard.mjs` — **PASS, 5 sheets** (opener +
  honest_work + first_blood + bigger_boat + template).
- `npm run check:bar:narrative` — 6/6. `node scripts/check-codex-narrative.mjs`
  — OK. `npm run check:mission-receipts` — OK.
- `npm run check:baseline` — 12/16; the four reds are foreign dirty-tree work
  (47-A tape, telemetry hash, save-schema shadows default), identical to the
  set recorded in the PQ-152.00 re-close. None imports this leaf's files.

## Changed this pass

- `docs/worldbuilding/BEAT-STANDARD.md` line 78 — the beats table still read
  "`sling_in` (700 WU leftover)"; corrected to `(dest-dock)`, the review's
  outstanding follow-up #2.

## Done-when read

"Three beats at 47-A density; owner verdict per beat." The three sheets exist
at opener density in the beatStandard v1 schema (canon who-wants-what, register
speakers, place/actors/headline verb/twist, two live leftover solutions,
proving frame, barks, voice notes, fixed seed 3200). The independent honesty
review was the verdict pass; its single red was repaired and re-proven. Headed
capture remains peeled per the standard — "there is no leftover tape for beats
1–3" — and live settle stays `PQ-032.00`, which this leaf does not recut.

Known limitation kept honest: neither the beat test nor the validator is wired
into an npm gate, so they run when invoked — recorded in the review's
observations, not hidden.

Queue: PQ-178.01 ready → done.
