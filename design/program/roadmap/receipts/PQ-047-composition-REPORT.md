<!-- LIFETIME: HISTORICAL -->
# PQ-047 — living freight incident 5x — composition receipt

```yaml
packet: PQ-047
resultCommits: pre-campaign packet commits (8f6c7e0c..0eb09fdf) + c676c536 (unit A telegraph) + 1b6478e5 (units B+C gaps) on master
date: 2026-08-10
workers: codex-root (original packet era, session stopped; work adopted), grok-4.5 + codex (gap closure), opus (composition review), fable controller (adoption, integration, closure)
verdict: CLOSE-PACKET (composition review, seeded end-to-end drive on the real production stack)
```

## Composition proof (one seeded incident, all five units)

Telegraph published for a concrete manifest carrier (A) → NPC drive-disable opens civilian
Massline recovery, team preserved (C) → disable spills exactly one conserved pod; player theft
through the real pickup seam books one law-owned incident; kill produces the second spill with
carrier at zero and the conservation sum intact (B) → exactly one durable wreck marker carrying
the encounter-minted manifestId, killer, sector; salvage pool is hull scrap, never re-issued
cargo (D) → exactly one freight loss priced once through the reserve-before-emit ledger, one news
headline (E). Duplicate disable/kill/close events: one spill, one identity, one loss. 104/104
across the five unit suites at 1b6478e5; baseline 11/11.

## Key questions answered

- lootShards vs custody spiller are mutually exclusive by construction and the gate discriminates
  (a manifest civilian without custody stamps still drops its payload).
- Single writers clean across economy pressure (64-bounded reserve-before-emit ledger), law/heat
  (receipt-consumed), movement (tacticalAI sole writer), persistence (custody adopts unit C's flag).
- All six player verbs reachable through real systems: defend, steal, disable, tow, ignore, ram
  (outward ram is attributed; player-as-target immunity is the owner-call non-goal).

## Follow-ups recorded (latent, non-blocking)

1. Multi-hauler predation convoys: custody stamps land on every hauler but death-spill routes only
   the selected target — a second hauler's cargo would vanish. No authored encounter produces one
   today (`329-curtain-convoy.js` ships size [1,1]).
2. `carrierDestructionPending` is write-only (no reachable trigger; give it a consumer or remove
   it when next touched).

## Residuals

Mid-incident Continue in the fully composed state (open custody + live marker in one save) is
proven pairwise, not in one run. Ordinary-route HEADED story review remains available to the owner
whenever wanted; the headless composition drive is the packet's wave-5 evidence.
