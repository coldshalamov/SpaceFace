<!-- LIFETIME: RECEIPT -->
# PQ-147.03 — Cluster and detonate

```text
STATUS DONE

DONE PQ-147.03 — Plant a well on a primed light and the pile cooks: three other hulls take the blast, two crates fly, a rock takes the slam, and more than one helm is lost. That is twelve receipts on every seed, nine of them already on the board the tick the moment detector calls it.

WHAT I FOUND     Leftover already had the well withhold the park-speed term from a primed light, a classifier that only reads bus receipts, and a live moment detector. There was no receipt. The first proof pass batched detonations before tumbles, so earlier collisions looked like chain secondaries.

WHAT I CHANGED   Adopted the leftover as written. Did not retune the well and did not rewrite the classifier. The proof now feeds receipts in tick order and pins the live detector, not a scripted explode-all.

WHAT YOU WILL FEEL   Cook a light, drop a well, and the clump does not sit there — it slams. Cargo leaves the pile. A rock takes a hit. Other ships lose the helm. One well is the action; the primed light is the ammunition. Nothing in the ring burns hull for standing in it.

THE NUMBERS      bar | before | after | target
                 Seeds with ≥ 3 secondaries | leftover unproven | 5/5 | 4/5
                 Receipts (tick order), seeds 14703–14743 | unproven | 12, 12, 12, 12, 12 | ≥ 3
                 Live detector first publish | unproven | 9, 9, 9, 9, 9 | rates the pile
                 Live detector final count | unproven | 12, 12, 12, 12, 12 | ≥ 3
                 Well equilibrium (unmarked) | 45 WU/s | 45 WU/s | 30–60 band
                 Fields that deal HP / type radiation | 0 | 0 | 0

THE FRAMES       Not taken (headed GPU forbidden this lane). Headless five-seed real path is the proof.

NEXT             PQ-147 leaves are done. Queue state left for the integrator.
```

## Seed table

One action: plant the well. The light is primed in setup. `FIELD_FLAGS.enabled = true`.

| seed | receipts | live first | live final | kinds | pass |
|---|---|---|---|---|---|
| 14703 | 12 | 9 | 12 | other_body_hit, cargo_thrown, terrain_slam, second_tumble | YES |
| 14713 | 12 | 9 | 12 | other_body_hit, cargo_thrown, terrain_slam, second_tumble | YES |
| 14723 | 12 | 9 | 12 | other_body_hit, cargo_thrown, terrain_slam, second_tumble | YES |
| 14733 | 12 | 9 | 12 | other_body_hit, cargo_thrown, terrain_slam, second_tumble | YES |
| 14743 | 12 | 9 | 12 | other_body_hit, cargo_thrown, terrain_slam, second_tumble | YES |

5/5 seeds ≥ 3. The bar asked for 4/5. Detonation counts moved with the seed (23 / 57 / 25 / 9 / 33); the secondary count did not. That is a tight pile saturating unique kind+id rows, not an explode-all script.

## Hypothesis (one)

A primed light that skips the well's 45 WU/s park term arrives as a slam. The 137.09 charge then writes receipts. Counting those receipts — never a forced boom — should clear three secondaries on four of five seeds.

## Proof

| Claim | Scenario | Seed | Result |
|---|---|---|---|
| Primed light skips well convergence | leftover seam | — | `wellUsesVelocityTerm({ primed: true })` is false; unmarked band still 45 WU/s |
| Classifier ignores the detonator and manual triggers | unit | — | first primed tumble skipped; player skipped; 5 receipts from the sample blast |
| Well + primed light ≥ 3 secondaries | `test/pq-147-03-cluster-detonate.test.mjs`, rapier-dynamic | 14703–14743 | 5/5 at 12 receipts; live detector published every seed |
| Moment detector rates it | `fields:clusterDetonate` | all five | first publish 9; running total 12 |
| Not explode-all | same | all five | detonations vary; no scripted boom |

## Honesty gaps

Closed this leaf: the proof no longer feeds every detonation before every tumble. Pre-chain collision tumbles are not credited as chain secondaries. The live detector is required, not only a post-hoc replay.

Left on purpose: leftover still counts the charge plate and the well emitter as `other_body_hit` (ids 9 and 10 on this cast). Those are real blast receipts. Without them each seed still has three hull hits, two crates, and a rock. The classifier was not rewritten.

Left on purpose: setup primes the light; the one player action is the well. Ships are parked so the well does the work. The live event fires once at the first rated tick (9); later helm-losses raise the running count to 12. Both clear the bar. The well was not retuned to make 5/5.

## Forbidden paths

Not edited: `src/ui/screens/range.js`, `src/ui/powerRail.js`, `src/ai/`, `src/render/vfx.js`, `src/render/renderer.js`, `src/systems/input.js`, `design/program/roadmap/program-queue.json`, `design/program/NOW.md`. No headed capture. No commit.

## Checks

| Check | Result |
|---|---|
| `node --test test/pq-147-03-cluster-detonate.test.mjs` | 3 pass; 5/5 seeds ≥ 3; live detector published on every seed |

STATUS DONE
