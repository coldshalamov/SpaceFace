<!-- LIFETIME: RECEIPT -->
# PQ-032.01 — The gate moves and widens

```text
STATUS DONE

WHAT I FOUND     Leftover ending eligibility already lived
                 in eligibility.js. The shared gate asked
                 for B7, 100k cr, 50 branch rep, and an
                 empire stake (capital / claim / outpost).
                 A combat-only B7 fixture with money and
                 standing still died on empire_stake. Five
                 endings. No second writer.

WHAT I CHANGED   I kept that leftover writer. Combat stake
                 is one named ace beaten (aceMemory). Tow
                 class and one field read leftover owners
                 (fittings / discovery / spine B2–B3). No
                 new ending. No branch menu. 25h soak
                 peeled.

WHAT YOU WILL FEEL   A fighter who beat an ace can file an
                 ending without a capital, a claim, or an
                 outpost. A builder who planted a claim or
                 outpost still can. Same five endings.
                 Same line to Ashfall.

THE NUMBERS      bar | before | after | target
                 combat-only A | leftover empire_stake | THE CLEAN UNIFORM | combat-only reaches an ending
                 builder B | leftover THE CLEAN UNIFORM via empire | THE SAME SILENCE via claim/outpost | builder reaches an ending
                 empire wall | required | empire or ace | combat stake alternative
                 tow class | leftover unnamed | ≥ medium (spool / coupler / spine B3) | heavy-verb tier
                 field | leftover unnamed | one (discovery / deploy / spine B2) | one field
                 ace | leftover unread | combat stake | one ace beaten
                 100k cr | leftover required | leftover required | honesty: still there
                 50 rep | leftover required | leftover required | honesty: still there
                 endings | leftover 5 | leftover 5 | no new endings
                 branches | leftover 0 | leftover 0 | no new branch menu
                 25h soak | packet asked | peeled | headless fixtures
                 scenario | pq-032-01-ending-gate | seed 3201 | headless print

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     Leftover 100k cr and 50 branch rep still
                 gate eligibility and the live offer.
                 Sandbox still uses those leftover money
                 and standing checks only. Builder does
                 not need an ace. Spine B7 still counts
                 as the field and the long tow when the
                 fit bag is empty. ships.js is dirty and
                 off limits. No 25h telemetry.

NEXT             Integrator updates the queue. PQ-032.02
                 makes each ending's climax a toy. Do not
                 mark the queue from this receipt.
```

## Files

- `src/story/endings/eligibility.js` — combat stake alternative; heavy-verb reads
- `src/story/endings/index.js` — re-export `TOW_CLASS_MIN`
- `test/pq-032-01-ending-gate.test.mjs` — combat-only and builder fixtures
- `design/program/roadmap/receipts/PQ-032.01-REPORT.md` — this receipt

Already live, not rewritten: `src/story/endings/endingDefs.js` (five endings, leftover 100k / 50), `src/systems/story.js` (`_endgameGateMet` still leftover 100k + 50), `src/systems/aceMemory.js` (defeated records), `src/data/modules.js` (spool / coupler), `src/systems/world.js` (`fieldsDepleted`).

Not edited: `endingDefs.js`, `src/systems/ships.js`, `src/systems/missions.js`, `src/data/missions.js`, `src/render/`, `assets/`, `docs/visual-assets/`, `index.html`, `build_map.md`, `program-queue.json`, FRONTEND screens, `src/systems/input.js`, Program 1 clock files.

## Honesty gaps

- Leftover `ENDGAME_NET_WORTH_CR` (100,000 cr) and `ENDGAME_REP_MIN` (50) still sit in `evaluateSharedGate` and in sandbox eligibility. `story.js` `_endgameGateMet` still uses those leftover numbers for the live offer. This leaf did not invent a second offer writer.
- A combat-only run that lacks leftover money or standing still cannot file. The widen is empire versus ace, not a wipe of the leftover credit / rep wall.
- Sandbox is still not an ending. It still skips empire / combat stake and the heavy-verb codes.
- Builder path uses leftover empire stake (claim / outpost). It does not require an ace. Ace beaten is the combat-stake alternative, not a third hard gate on both routes.
- Tow class ≥ medium is a leftover capability read: industrial spool (`tetherSpoolMult` 3), capital spool (6), or frame coupler. When the fit bag is empty, spine `beatIndex >= 3` (bigger_boat long tow) still counts. There is no leftover named `towClass` writer; `ships.js` is dirty and off limits.
- One field reads leftover `world.discovery.*.fieldsDepleted`, player `fields.deployed`, or spine `beatIndex >= 2` (first_blood). Field deploy runtime stays transient.
- Combat stake reads leftover `state.aceMemory[id].defeated`. This leaf does not write ace memory.
- Five leftover endings. No new branch menu. Auto-dispatch and A/B board rows stay leftover.
- 25-hour telemetry peeled. Headless fixtures are the landing bar. No shipping-camera strip.
- Do not mark the queue from this receipt.

## Proof

```text
PQ-032.01 combat-only A THE CLEAN UNIFORM
PQ-032.01 builder B THE SAME SILENCE
```

`node --test test/pq-032-01-ending-gate.test.mjs` — 4 pass, 0 fail, seed 3201.

Also green: leftover `node test/story-endings.test.mjs`.
