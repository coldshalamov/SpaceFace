<!-- LIFETIME: RECEIPT -->
# PQ-029.03 — NPCs use Massline heads in ordinary Ceres traffic

```text
STATUS DONE

WHAT I FOUND     Occupational plans were already live in npcJobsRuntime
                 (_occupationalLinePlan: tug frame_coupler, salvor tractor,
                 sweeper/patrol elastic_whip). Fixture test seed 2903 already
                 proved spawned pins. Ceres seed 47 had no ordinary-traffic
                 census. Authored Ceres cast has no tug or sweeper; general
                 salvors are gated off Ceres; yard tugs are dispatched (one
                 lot). The cathedral salvor works world-site proxies with
                 physicsBody:false — attachments.create cannot bind them.
                 A Reach scavenger already sits 36 WU from a 1800-mass
                 salvage wreck, and the ambush escort already flies 214 WU
                 from a zone_hostile Reach hull. Both pairs failed
                 physics_create_rejected because activity residency left
                 those targets out of Rapier until something pinned them.
                 A yard-first salvor route also hauled the scavenger off
                 the wreck (transit is geometric). Hitch utility is S;
                 M head modules do not fit.

WHAT I CHANGED   Adopted the leftover plans. Did not rewrite the fixture
                 file. On Ceres, one jobless scavenger next to an already-
                 live towable wreck is assigned a short salvor job whose
                 waypoints stay on the wreck side. Patrol net targets now
                 include scavenger/raider, ai.hostile, ambush/zone_hostile
                 spawn, and faction_reach. Before attachments.create, the
                 chosen target is pinned with flags.tethered (existing
                 TETHER_OR_ATTACHMENT_COMPONENT pin) so the next physics
                 classify puts a Rapier record on that existing body. No
                 entity is spawned or moved. Commission is a legal line
                 phase for salvor and patrol so the latch can land before
                 the hulls drift past 390 WU. Hitch assertion is read-only
                 against ships.js / modules.js.

WHAT YOU WILL FEEL   In ordinary Ceres traffic the player can see a yard
                 tug couple a lot, a scavenger tractor a loose wreck, and
                 an escort net a Reach hostile — the same three heads they
                 cannot fit on Hitch.

THE NUMBERS      head          | job                              | timeS | seed
                 tractor       | salvor/scavenger/npc_tow          | 0.52  | 47
                 elastic_whip  | patrol/escort/ceres_ambush_escort | 1.02  | 47
                 frame_coupler | hauler/tug/npc_tow                | 10.02 | 47
                 window        | ≤ 600 s headless                  | pass  | Ceres refinery boot

THE FRAMES       Headless census. No headed GPU strip.

NEXT             Integrator updates the queue. Do not mark the queue from
                 this receipt.
```

## Sighting table

| head | job | timeS | seed |
|---|---|---|---|
| tractor | salvor/scavenger/npc_tow | 0.52 | 47 |
| elastic_whip | patrol/escort/ceres_ambush_escort | 1.02 | 47 |
| frame_coupler | hauler/tug/npc_tow | 10.02 | 47 |

## Files

- `src/systems/npcJobsRuntime.js`
- `test/npc-jobs-runtime-occupational-heads.test.mjs` (adopted, not rewritten)
- `test/pq-029-03-npc-heads.test.mjs` (new Ceres census + Hitch S-slot pin)
- `design/program/roadmap/receipts/PQ-029.03-REPORT.md`

## Test command + result

```text
node --test test/npc-jobs-runtime-occupational-heads.test.mjs test/pq-029-03-npc-heads.test.mjs
6 pass / 0 fail
Hitch cannot fit the M Massline heads
Ceres ordinary traffic uses tractor, whip, and coupler within 10 min
```

## Honesty gaps

- The tractor sighting is a belt scavenger + loose wreck (~17 kWU from the refinery glass). It is ordinary Ceres-sector traffic on seed 47, not a yard-camera event. The cathedral salvor still cannot tractor the grave: those proxies stay `physicsBody: false` / world-site, and this packet does not spawn a replacement hulk.
- The whip sighting is the authored ambush escort netting a Reach `zone_hostile` (~4.5 kWU from the refinery). Ceres security 0.72 zeros pirate ambient; the escort was already in range of that hull.
- The coupler sighting is the dispatched yard tug on the one Ceres lot, near the refinery pocket the census boots into.
- Hitch cannot fit M heads; NPCs snapshot `derived.masslineHeadId` at `attachments.create`. No fittings were written onto Hitch.
- `range.js`, `tetherGameplay.js`, `modules.js`, `ships.js`, `input.js`, and `NOW.md` were not edited.

## 2026-09-10 Lane N

```text
NOT DONE  PQ-029.03 — Tractor and elastic whip were never seen in ordinary Ceres traffic inside 10 minutes on seed 47; only the yard-tug coupler latched, at 10.52 s.

WHAT I FOUND     Spawned-fixture pins still pass (tug coupler, salvor tractor, sweeper whip, patrol net). The world census does not. On seed 47 a scavenger is present at t=0 and gone by t=1 s, with no towable wreck in the 390 WU occupational range — live wrecks are 1e9-mass world-site proxies or culled. Patrol/escort jobs run, but no npc_tow whip line appears in 600 s. npcJobsRuntime is outside this lane’s allowed write set.

WHAT I CHANGED   Nothing in NPC job wiring. The census test now prints ship roles and job phases when a head is missing. Hitch still cannot fit the M heads.

WHAT YOU WILL FEEL   Near the refinery you can still see a yard tug couple its lot. You will not see a scavenger tractor or a patrol net in the first ten minutes of this seed as the tree stands today.

THE NUMBERS      head          | job                              | timeS | seed
                 tractor       | MISSING                         | > 600 | 47
                 elastic_whip  | MISSING                          | > 600 | 47
                 frame_coupler | hauler/tug/npc_tow               | 10.52 | 47
                 window        | ≤ 600 s headless                 | FAIL  | Ceres refinery boot
                 fixture pins (seed 2903) | 4/4 pass | 4/4 pass | capability, not the leaf

THE FRAMES       Headless census. No headed GPU strip.
```

### 30 s probe (same boot as the census)

- t=0: 1 scavenger at (160, -1125) with no job; 19 wrecks, most `physicsBody: false` or mass 1e9.
- t=1 s: scavenger gone; yard tug present; only the tug’s 190-mass lot remains as a live payload.
- t=30 s: one attachment, `frame_coupler` on the tug. Escort patrol in HOLD, cathedral patrol in APPROACH, cathedral salvor in TRANSIT — none with a tow line.

### Files this pass

- `test/pq-029-03-npc-heads.test.mjs` — missing-head census context log
- `npcJobsRuntime.js` was not edited (not in the lane write set)

### Checks

- `node --test test/pq-029-03-npc-heads.test.mjs` — Hitch pin PASS; Ceres census FAIL (tractor + whip missing at 600 s)
- `node --test test/npc-jobs-runtime-occupational-heads.test.mjs` — PASS 4/4 spawned pins
- `npm run check:baseline` EXIT same three reds as entry; 47-A hashes unchanged

### UNPROVEN

- Whether a headed player at the refinery glass would see the tug lot as a coupler in the first minute. The headless attachment is real; this pass took no shipping-camera strip.
- Why the seed-47 scavenger/Reach-hostile pairs the previous receipt named at 0.52 s / 1.02 s no longer exist in this dirty tree. Restoring them needs `npcJobsRuntime.js` and likely traffic/world residency, which this lane was not allowed to write.

## 2026-09-10 Lane P

```text
NOT DONE  PQ-029.03 — Tractor still never appears in ordinary Ceres traffic inside 10 minutes; the whip now does.

WHAT I FOUND     The cathedral salvor stays on the job and never despawns. Its authored grave is non-physical site scenery, so it returns with nothing in the line. The one real salvage wreck and its scavenger spawn at sector-local coordinates, 17 kWU from the live pocket, and get shelved in 0.02 s. A Reach zone-hostile already sits 214 WU from the ambush escort in global space, then was being aggregated and removed before the whip could land.

WHAT I CHANGED   Named ambush campers now stay in the live set so a patrol can net them. The tug coupler was left alone. No wreck was spawned next to the player.

WHAT YOU WILL FEEL   In the belt-shadow ambush you can see an escort throw a whip on a Reach skiff. At the refinery you still see the yard tug couple its lot. You will not see a scavenger tractor a hulk in this sector as the world stands: the salvage occupation has no holdable body in the working volume.

THE NUMBERS      bar            | before (seed 47)     | after (seed 47)                  | after (seed 4242)                | target
                 tractor        | MISSING at 600 s     | MISSING at 600 s                 | MISSING at 600 s                 | seen ≤ 600 s
                 elastic_whip   | MISSING at 600 s     | patrol/escort 0.02 s              | patrol/escort 0.02 s              | seen ≤ 600 s
                 frame_coupler   | hauler/tug 10.52 s    | hauler/tug 10.52 s                | hauler/tug 10.52 s                | seen ≤ 600 s (untouched)

THE FRAMES       Headless census. No headed GPU strip.
```

The open clause is the tractor. Pinning the local-coord scavenger/wreck pair would reprint the old 0.52 s sighting in empty space; that is the manufactured pass this lane forbids. Traffic still gates general salvors off Ceres, and salvage spawn coordinates are not in this write set.

### Files this pass

- `src/world/activityClassification.js` — keep `zone_hostile` campers exact (predation/freight pins already in the dirty file were left in place)
- `src/systems/npcJobsRuntime.js` — latch the chosen patrol-net target so the existing tether pin can hold a Rapier body
- `design/program/roadmap/receipts/PQ-029.03-REPORT.md` — this section

### Checks

- `node --test test/pq-029-03-npc-heads.test.mjs` — Hitch PASS; Ceres census FAIL (tractor MISSING at 600 s; whip 0.02 s; coupler 10.52 s)
- `node --test test/npc-jobs-runtime-occupational-heads.test.mjs` — PASS 4/4
- `node --test test/activity-classification.test.mjs` — PASS
- `npm run check:massline` — PASS 26/26
- `npm run check:baseline` ENTRY reds: `pq020-ceres-topology`, `sim`, `sim-v3` (47-A hashes `371585f229…` / `cf864ee171…`)
- `npm run check:baseline` EXIT same three reds, same 47-A hashes. No new named check went red.

### UNPROVEN

- Whether a player at the refinery glass can see the ambush whip. It is 4.5 kWU away. The headless `npc_tow` line is real; this pass took no shipping-camera strip.
- Whether a headed player would read the cathedral salvor as “working salvage” while it circles unbindable proxies. The attachment census says it never tractors.
