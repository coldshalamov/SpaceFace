# PQ-205.00 — Bomb-bay route acceptance and look/feel — REPORT

RESULT: DONE

PLAYER RESULT: gravity reads as a crater you can fly into and tar as a broken vein-cloud, not
two floor diagrams, while the eight payloads still share one telegraph mesh; first drop is cooked
with the rest of the shaders.

## What shipped

1. `src/render/bombPresentation.js` — the telegraph is now a lit StandardMaterial batch with
   volume: drift collars and wakes, standing warning vanes on the live blast radius, a broken
   crater lip / inner well / in-falling sheets for the neutron slug, and gapped veins plus mounds
   for tar. Camera world-matrix is updated before frustum cull so a lookAt does not drop a field.
   One mesh, no extra lights, sprites, shock rings, or post process.
2. `src/render/precompile.js` — the existing cook retains a tiny `bomb-telegraph` owner with the
   live material recipe so the first drop is not a new shader key.
3. `test/fixtures/bomb-choreography/presentation.html` — combat-tilt camera (50° FOV, 60° tilt,
   zoom 58 / 144 / 330), reduced motion/flash, occupancy split into batch-fill vs frame.
4. `scripts/probe-bomb-bay.mjs` — isolated-save look walk + 0/6/24 hitch numbers. Stills stay in
   `.devshots/pq-205-00/` and are not committed.

## Look

Inspected on the production mesh at the live camera recipe. Gravity and tar are distinct at
combat zoom and at wide; minimum zoom looks down the crater throat; reduced motion keeps the
danger boundary; reduced flash dims the layer without shrinking reach. Warning is vanes, not a
HUD ring. Drift is a small collar and wake, not a floor icon.

## Occupancy (14-CPU host, fixture camera pulled back so 24 bombs stay in frustum)

| bombs | vertices | batch fill p50 / p95 | frame p50 / p95 | first fill hitch |
|---|---|---|---|---|
| 0 | 0 | 0.0 / 0.0 ms | 0.2 / 2.1 ms | 0.4 ms |
| 6 mixed | 480 | 0.1 / 0.2 ms | 1.2 / 7.2 ms | 0.2 ms |
| 24 stress | 2052 | 0.4 / 0.6 ms | 1.9 / 8.2 ms | 0.8 ms |

Frame times include the unbatched procedural bomb bodies. The new batch itself stays under 1 ms
fill at the world cap. Host: 14 CPUs, load1 0.

## Route

All eight definitions travel the same safe → armed → committed → burst (or field) path in
`test/bombs.test.mjs` / `test/bomb-choreography.test.mjs` (36/36 with presentation). Digit9 /
Comma / R remain the default bay verbs. Headless Chromium did not expose `window.SF` within 150 s on this worktree pass (fixture WebGL
is fine). `npm run check:playable` is also red on BOOT: `uiRoot.js` imports
`./capitalBossOverlayMount.js`, which is not in this worktree (404). CLEAN and SHADER passed; we
did not touch `src/ui/`. That missing overlay is a foreign bosses-integrator hole, not this leaf.

## Checks

- `node --test test/bombs.test.mjs test/bomb-choreography.test.mjs test/bomb-presentation.test.mjs` — 36/36
- `node --test test/authored-precompile-residency.test.mjs` — retains `bomb-telegraph`
- `node scripts/probe-bomb-bay.mjs --headless --fixture-only` — look + occupancy above
- `npm run check:playable` — red BOOT from a missing `capitalBossOverlayMount.js` this branch does
  not contain (404, foreign). Not caused by this leaf.

## Not this leaf

Audio catalog (`.01`), NPC drop + shootable proxy (`.02`), rack economy (`.03`). No second
ordnance sim, HUD, or scan loop.
