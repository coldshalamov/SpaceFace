# PQ-145.00 — 2026-09-10 worker handoff

NOT DONE PQ-145.00 — The freight and pirate simulation works and survives Continue, but the drawn map route is missing and the shipping-camera route could not reach flight.

WHAT I FOUND — A plain claim emitted `claim:claimed` without a freight consumer; only completed Throughlines had a civilian service hook, and that hook intentionally excluded NPC jobs.

WHAT I CHANGED — Connected claims to one recurring supply hauler through the existing traffic, NPC-job, world-record, and encounter owners; saved the service timetable, prevented duplicate unloads, and preserved depot service through Ceres legacy-traffic cleanup.

WHAT YOU WILL FEEL — Claiming a body dispatches a cargo-carrying hauler from the nearest station, followed by an empty return berth job and the next supply run. A pirate squad watches the route and springs its existing ambush when approached; this behavior was measured in the focused simulation, not visually accepted in the game.

THE NUMBERS

| Bar | Before | After | Target | Seed |
| --- | --- | --- | --- | --- |
| Rookery Prospect inbound hauler jobs, 2 sim seconds after claim | 0 | 1 | >=1 | 14500 |
| Jobs after JSON save envelope loaded into a fresh simulation through the real save owner | not sampled | 1, same world-record identity and service leg | >=1 | 14500 |
| Return hauler job, uncontested 100-second service run | not sampled | 1 | >=1 | 14500 |
| Completed supply/return legs; supplies delivered | not sampled | 2 legs; 16 units | real advancing jobs | 14500 |
| Pirate watch after 17 sim seconds near the claim | not sampled | 6 live pirate ships; encounter phase `conflict` | real pirates watching the route | 14500 |
| Existing Chart Careers route text | not sampled | present | present | 14500 |
| Drawn claim route on the map | not sampled | absent | present | 14500 |

FILES

- `src/systems/traffic.js`
- `test/pq-145-00-depot-traffic.test.mjs`
- `design/program/roadmap/receipts/PQ-145.00-REPORT.md`
- Coordination only: updated this worker's existing row in `design/program/NOW.md`.

Re-read `claims.js`, `siteLogistics.js`, `traffic.js`, and `travelLanes.js`. `siteLogistics` is local cell-network math, not a traffic producer. No changes to claims, site logistics, travel lanes, missions, ships, HUD, or Atlas. The PQ-138.05 Ceres `record && !entity` materialization guard remains intact. No commit or push; the controller owns publication.

CHECKS

- `npm run check:baseline` entry: **FAIL**, 11/15 green. `pq020-ceres-topology`, `sim-v3`, `sim`, and `massline` red; Massline's physical-branches child timed out at 151738 ms without an assertion. Baseline wall time 152227 ms exceeded its 90000-ms budget.
- `npm run check:baseline` exit: **FAIL**, 11/15 green, same reds. Both actual 47-A hashes are unchanged from entry (`cf864ee1…` V3 and `371585f2…` legacy); same Ceres topology failures; physical-branches timed out at 150477 ms. No newly red baseline check. Log: `.devshots/delegate-20260910/scratch/pq145-baseline-exit.log`.
- `npm run check:save-schema`: **PASS**, version 14, 282 paths.
- `node --test test/pq-145-00-depot-traffic.test.mjs`: **PASS twice**, 2/2 each run. Identical measured counts; logs `pq145-test-1.log` and `pq145-test-2.log` in the scratch directory.
- `npm run check:atlas-integrity`: **PASS**, 292 nodes and 71 edges.
- `node --test test/ceres-activity-traffic-cast.test.mjs`: **FAIL**, 11/16. The same five failures reproduce using entry traffic blob `9a41da7be` through the scratch ESM loader, without changing the checkout: three stale no-freight expectations for the authored Throughline hauler and two pre-existing count expectations (9 versus 8; 2 versus 1). Logs `pq145-ceres-regression.log` and `pq145-ceres-before.log`.
- `npm run check:playable`: **FAIL**. Menu reached at 53.9 seconds; New Game remained at `gpu-resources`, progress 0.9, until the 90000-ms flight timeout. Pilot/hull/world/controls could not run; clean/shader/assets checks passed. This failed before a claim could be placed, and the depot dispatcher returns immediately with no claims. Log `pq145-playable.log`.
- `npm run check:all:smoke`: running when this initial handoff was written; final result will be appended.

UNPROVEN / OPEN

- The drawn-route clause needs the map owner: `src/ui/galaxyMap.js` builds/draws `travelRoute` only for Throughline markers. That file is outside this leaf's write set. Careers lists the actual job's depot/station text, but that is not claimed as a drawn route. No fabricated sling or Atlas rebuild was used to disguise the missing consumer.
- No shipping-camera capture, cold browser title-Continue proof, or before/after frame-time witness: the existing playable route did not get past GPU-resource preparation. The tests use actual Ceres authored positions and public claim/save APIs with a focused simulation list; they do not exercise renderer/flight/physics acceptance. The fixture places the player at the claim to observe the watch, and separately omits the pirate director to measure an uncontested full supply/return cycle. It does not assign jobs directly or write NPC transforms.
- Post-destruction replacement cadence is implemented through world tombstones but not separately measured.

Controller next action: assign the small map-consumer change to the map owner, then close the normal-camera and title-Continue route gates once the shared launch route reaches flight. This worker does not self-assign acceptance.

FINAL CHECK APPEND

`check:all:smoke` is **NOT PASS / interrupted**. Vision assertions, UI imports, 47-A compare, and claim/base checks passed. `47a-live-cold-open` timed out waiting for the browser at 15 seconds; `first-15-runtime` timed out at 30 seconds; `market-first-loop` timed out waiting for the main menu at 15 seconds. The run was stopped during `flight-clean` after these repeated startup failures, with its exact process tree terminated. Remaining smoke commands were not run. Per-command logs, initially emitted by the existing runner to its default scratch location, were moved to `.devshots/delegate-20260910/scratch/pq145-smoke-details/`; the run output is `pq145-smoke.log` beside that directory.

Both final requested test runs passed 2/2 with the same 1 in-session job, 1 after Continue, 1 return job, 6 pirates, 2 completed uncontested legs, and 16 delivered units. The drawn-map-route measurement remained false. `git diff --check` passed for the owned source, test, and report. The mutation row is released; no agent or check launched by this worker remains running. Overall leaf status remains **NOT DONE**, for the open map and visual-route clauses above.

Diagnostic correction: the smoke runner classifies `47a-compare` as **FAIL / stale_golden**, despite its process exit code 0 and equal baseline/candidate hashes. It reports the same stale legacy hash plus audio-cue expected count 2 versus actual 3. Thus the completed smoke prefix has **3 passed, 4 failed**, followed by interrupted `flight-clean`; the earlier sentence calling this smoke comparison a pass refers only to the process exit and is superseded here. Process cleanup evidence is limited to ended tracked execution sessions and the explicitly terminated smoke process tree; no machine-wide process audit was performed.
