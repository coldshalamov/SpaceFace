DONE PQ-193.11 — The four Helios lane marks hold at chase camera, and the two red live-wire cases were a stale harness, not a dropped POI.

DONE WHEN (verbatim): "The four Helios marks wired 2026-09-09 (tally, claim, ash pin, whistle) reviewed at chase camera. Cube foot/deck is a fail. Pin and locker stay the 2026-08-18 unanimous pair."

WHAT I FOUND — Two cases in `test/unused-model-live-wire.test.mjs` were red ("Helios must keep the place_lane_pin POI", "Ceres survey cache must wear debris again"). Neither POI was dropped. `b4b5b1476` ("Fix the player-facing bugs the sweep actually proved") added `poiMustStayLiveActor` to `src/systems/world.js`: a POI that is not a combat or interaction actor no longer becomes an entity, it becomes a presenter dressing row (`insertDressingRow`, `src/world/dressingTable.js`). Both lists are drawn — `collectJournalPresentationEntities` collects them together and `renderer._syncWorldPresentationTableMeshes` poses them together — but the test's `spawnSector()` helper scanned `state.entityList` only, so a quiet landmark on screen was reported MISSING. `SECTOR_ANCHORS` still carries `poi_helios_lane_pin` at (215, -70) and `poi_survey` with `place_debris_chunk`; the assertions on the anchor rows themselves were green the whole time.

WHAT I CHANGED — Taught the test helper to read both surfaces the presenter reads, with the causing commit named in the code. No production data or geometry changed for that repair. Then rendered legal chase stills (`play_chase` 144 WU / abeam / `play_chase_close` 58 WU) of all six lane-furniture bodies from the shipped source GLBs and ran a cross-section census of every foot, deck, sill, base, flange, boot, shim, token and collar member.

WHAT YOU WILL FEEL — Nothing new in the picture: the marks were already on screen. The check now tells the truth about them, so the next person who breaks a lane mark finds out.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| `unused-model-live-wire` | 5/7 | 7/7 | 7/7 | 47 |
| Helios POIs seen by the scan | 11 (entities only) | 11 + dressing rows incl. `place_lane_pin` | every spawned POI | 47 |
| Ceres POIs seen by the scan | 16 (entities only) | 16 + dressing rows incl. `place_debris_chunk` | every spawned POI | 47 |
| Box (4-sided) foot/deck members, tally + claim + ash + whistle | — | **0 of 42 measured** | 0 | legal chase |
| Cross-section sides on those members | — | 6, 8, 10, 12, 14, 24 | never 4 | legal chase |
| Triangles: tally / claim / ash / whistle | — | 1196 / 1208 / 1100 / 1528 | > 200 | — |
| Release bytes | pinned SHA-256 x6 | unchanged | unchanged | — |
| `check:baseline` | 15/15 | 15/15 | 15/15 | — |

THE FEEL — At 58 WU the tally post reads as a weigh station: a grate deck on six-sided pads, a mast with a hanging ledger banner, a tong yoke, a boom and a lit head. The claim mark reads as a driven spike with a crushed antenna hoop, a paint nozzle and a bolted plate. The ash pin reads as a memorial: a ten-sided base, a spar of tokens, a name plate in a cage. The whistle reads best of the four — a strapped drum with a warped lid, a crank and a basket lamp. None of them is a primitive with a label on it.

CUBE TEST — The 2026-09-09 pass replaced the cube foot/deck/token/lantern members with prisms and discs, and the census confirms it held: `tally_foot_0..3`, `tally_sill_0..3`, `tally_shim`, `ash_foot_0..3`, `ash_shim` are six-sided prisms; `tally_deck_rail_n/s/e/w` and `tally_deck_crumple` are 8- and 12-sided bars; `ash_base` and `ash_token_0..2` are ten-sided; `claim_flange` and `claim_weld_collar` are fourteen-sided; `whistle_boot` and `whistle_boot_sole` are ten-sided. Not one four-sided cross-section among them. PASS.

The 12-triangle members that remain on these bodies are wires, chains, antenna segments, basket gathers and bite tabs — thin members where a box IS the shape. `place_lane_pin` still carries 12-triangle feet, and `place_cold_locker` 52 box members; both are left exactly as they are, per the brief's "Pin and locker stay the 2026-08-18 unanimous pair."

NEXT — leave the queue row; do not `--integrate`.

CHECKS

| Command | Result |
|---|---|
| `node --test test/unused-model-live-wire.test.mjs` | PASS 7/7 |
| `npm run check:baseline` | 15/15 green |

STILL QUOTE — Legal chase stills of all six bodies at 144 WU, abeam and 58 WU, looked at and deleted. At 144 WU every mark is a small dark silhouette against the sky, which is the correct read for lane furniture; the identity plates on the claim mark and the ash pin are the brightest thing on either body at 58 WU and are the weakest surface of the four.

OPTION TAKEN — Repair the harness and cite the commit that moved the contract, rather than re-adding a second marker entity beside the dressing row (which world.js explicitly refuses: "never create a second marker entity beside that owner"). Rejected: editing the assertions, re-spawning POIs as entities, re-authoring geometry that already passes the named test.

UNPROVEN — A headed New Game still with a lane mark in frame at the live chase camera (the marks sit 200-1800 WU from the spawn point and the player must fly to them). `furniture_identity_plate` reads near-white at 58 WU on two of the four bodies; that material is shared across the whole place library and is not this leaf's to retune.
