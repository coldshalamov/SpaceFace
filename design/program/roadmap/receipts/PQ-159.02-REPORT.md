<!-- LIFETIME: RECEIPT -->
# PQ-159.02 — The beat

```text
DONE  PQ-159.02 — a rated moment now gets a 150 ms time dip, a camera hold, and a stinger;
                  death gets a cam. The beat fires only from the moment detector.
                  Headless sim bodies do not move.

WHAT I FOUND     bulletTime already rates stunt:trickDetected into moment:holyShit and
                 already plays a slow-mo pulse plus a stinger cue. feel.js answered that
                 event with a 60 ms kiss and no camera hold. Death punched FOV and a red
                 wash, but did not hold the wreck. Collisions already have their own
                 shorter hitstop — they must not steal this beat.

WHAT I CHANGED   feel.js now resolves the beat only on moment:holyShit (150 ms dip on the
                 existing feel:hit-stop time-effects source, so it min-wins with bullet
                 time and pause). The chase camera grew hold() and deathCam(): the hold
                 freezes look-at for 150 ms; death holds longer and eases out. Collisions
                 still do not call hold() or the stinger. Sim positions and simTime are
                 untouched — presentation only.

WHAT YOU WILL FEEL   Land a named stunt and the world hesitates for a beat, the camera
                 holds the picture, and the bass drops. Die, and the camera stays on the
                 wreck instead of cutting away. A scrape still ticks; it does not get this
                 beat.

THE NUMBERS      bar | before | after | target | seed
                 rated-moment dip | 60 ms kiss | 150 ms, timeScale 0.12 | 150 ms | 15902
                 camera hold on moment:holyShit | none | holdRemaining > 0, frame stays
                   put while the hull is moved | 150 ms hold | 15902
                 stinger | detector cue only | feel also emits moment.stinger | fires |
                   15902
                 collision → beat | n/a | hold 0, no stinger | detector only | 15902
                 death cam | none | deathCam true, hold > 0.5 s | death gets a cam | 15902
                 headless entity hash | n/a | unchanged after the beat | unchanged | 15902

FILES            src/render/feel.js
                 src/render/camera.js
                 test/pq-159-02-beat.test.mjs (rewritten onto shipped feel/camera)
                 design/program/roadmap/receipts/PQ-159.02-REPORT.md (new)

CHECKS           node --test test/pq-159-02-beat.test.mjs -> 5/5 twice
                 node --test test/pq-159-00-impact-kick.test.mjs -> 7/7

UNPROVEN         The stinger is proven as a bus cue, not a heard sample. No headed death
                 still. GPU capture lanes were busy.
```

## Seed 15902 measurement

`node --test test/pq-159-02-beat.test.mjs` prints:

```text
SEED=15902 dipMs=150 stinger=moment.stinger nonMoment=null
SEED=15902 headlessHash=1:0:0:0:0:100 unchanged=1 timeScale=0.12
```

`resolveMomentBeat('physics:impact')` and unrated `stunt:trickDetected` return null. The live
feel host wired to `createChaseCamera` dips `state.timeScale` to 0.12, arms `hold()`, and
emits `audio:cue moment.stinger` only on `moment:holyShit`. Entity positions and `simTime`
are identical before and after the beat.
