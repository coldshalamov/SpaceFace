<!-- LIFETIME: RECEIPT -->
# PQ-027.03 — Timed hangar jam

```text
STATUS DONE

DONE PQ-027.03 — A wreck in the Ceres hangar mouth holds a full-burn
                 fighter inside for twenty seconds. The door is mass.
                 After the hold the bay can open again.

WHAT I FOUND     Ceres already had a scheduled aperture. A stuffed hull
                 jams the mouth. An inward cone and a pinch sheet move
                 mass. The open door lets the same fighter out.

WHAT I CHANGED   Nothing in the live machinery. This leaf adopted that
                 leftover and re-ran the named seed-2703 Rapier hold.
                 The receipt now names the frozen clock and the
                 occupancy hold.

WHAT YOU WILL FEEL   Jam a wreck in the hangar mouth and the next
                 fighter stays inside. The door is mass, not a spawn
                 lock. The wreck keeps the jam while it sits. Yank it,
                 wait out the twenty seconds from the jam, and the bay
                 can launch.

THE NUMBERS      bar | before | after | target
                 jammed hold (seed 2703) | leftover 20 s, along −72.19, max −47.98 | same | ≥ 20 s inside
                 open-bay launch | leftover crossed at 2.07 s | same | open door does not hold
                 hold field | leftover cone strength 720 | same | writes force
                 pinch field | leftover sheet strength 820 | same | writes force
                 hazard type | leftover debris_current | same | never radiation
                 hull drain | none; world only chews hull on type radiation | same | never an HP aura
                 scenario | seed 2703 | Rapier | test/environmental-aperture-jam.test.mjs
                 named leaf | contract only | same | no second world

THE FRAMES       Headless Rapier is enough. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-027 leaves are done.
                 Do not mark the queue from this receipt.
```

## Files

- `design/program/roadmap/receipts/PQ-027.03-REPORT.md` — this receipt, numbers from this session
- Already live, not rewritten: `src/data/environmentalMachinery.js`, `src/systems/environmentalMachinery.js`, `src/systems/terrainAnchors.js`, `src/systems/world.js`, `src/data/hazardLanguage.js`, `test/environmental-aperture-jam.test.mjs`, `test/pq-027-03-hangar-jam.test.mjs`

Not edited: `src/systems/input.js`, `src/data/modules.js`, `src/data/fields.js`, `src/data/sectors.js`, `program-queue.json`. Earlier leftover in the same files (027.00 machines, 027.01 reef, 027.02 weather) was not rewritten.

## Honesty gaps

- The aperture already held. This leaf did not retune the cone, sheet, cycle, plug, or mouth.
- Earlier leaves in the same leftover files were left alone. No 027.00–.02 retune. No new radiation belt.
- Weather leftover still names `veil_radiation_belt` / `vesta_radiation_belt` and role `radiation_belt`. Their hazard type is `nebula`. No `WEATHER_VOLUMES` row is typed `radiation`.
- Leftover authored `type: 'radiation'` zones still exist on Veil and Vesta in `sectors.js`. Those still chew hull. This leaf did not invent a weather radiation belt and did not retune those leftover sector rows.
- The Rapier hold freezes `simTime` at 1 s (open window). Twenty seconds is 1200 physics ticks under occupancy. The wreck stays seated, so occupancy keeps the jam even if that clock had run.
- After-hold open / release is `aperturePhase` plus field unregister. There is no second Rapier launch after twenty seconds of a running sim clock.
- The stuffed hull keeps the jam while it sits. Yanking it does not open the bay at once; the cone still holds until twenty seconds from the jam start.
- The hold cone writes force (kernel inward accel > 40; live along went from −48 to −72.19, max −47.98). The fighter is a Wasp with forced full-burn thrust every tick, not a spawn flag and not a live encounter spawn.
- A jammed occupied door does not stack a plug. The stuffed hull is the door. An empty locked door still gets a static asteroid plug.
- The named proof does not boot `world`, so it cannot watch the hull-drain path. That path only chews hull when a zone type is `radiation`. The aperture zone and the hazard-language boundary are `debris_current`.
- Parent table asked for a shipping-camera capture. The leaf packet and this job forbade headed capture. Proof is headless Rapier seed 2703.
- Browser play has fields on. The named proof forces the same flag so Node can see the shove.
- The named leaf `test/pq-027-03-hangar-jam.test.mjs` is the contract pin. The live Rapier hold is `test/environmental-aperture-jam.test.mjs`.
- The leaf packet `.devshots/packets/PQ-027.03.md` was not in the tree. Authority is `PQ-027.md` leaf `.03`.

## Proof

| Bay | Crossed at | Along | Max along | Exit |
|---|---|---|---|---|
| open (no wreck) | 2.07 s | past exit | past exit | 38 |
| jammed (wreck in mouth) | none in 20 s | −72.19 | −47.98 | 38 |

Hold cone kind `cone` strength 720. Pinch sheet kind `sheet` strength 820. Hazard type `debris_current`.

`node --test test/pq-027-03-hangar-jam.test.mjs test/environmental-aperture-jam.test.mjs` — 4 pass, backend `rapier-dynamic`.

## 2026-09-09 — player-started jam (Lane I)

```text
DONE  PQ-027.03 — The player rams a wreck into the Ceres hangar mouth. The jam holds a full-burn wasp inside for twenty seconds. Closing speaks remaining time at force zero before the bite. The open door still launches.
WHAT I FOUND     A pre-placed static wreck already held a wasp for 20 s. Nothing showed the player shoving the hull in, and closing did not telegraph a zero-strength door.
WHAT I CHANGED   Closing now registers the door volume at strength zero so you can read the window. The named Rapier proof has the player ram the wreck. I did not retune the cone or the 20 s hold.
WHAT YOU WILL FEEL   Stuff a hull in the Ceres hangar mouth and the next fighter stays inside. Stand in the mouth as it closes and you get the remaining seconds with no shove yet. Yank the wreck and the cone still holds until twenty seconds from the jam. What still is not: I did not look at a shipping-camera still, and this proof freezes the sim clock at the open window the same way the leftover did.
THE NUMBERS      seed 2703, rapier-dynamic, profile production
                 bar | before | after | target
                 player ram | leftover pre-placed static wreck | playerHit=true, wreck seated along 18 → in mouth | player started it
                 jammed hold | leftover 20 s, along −72.19, max −47.98 | 20 s, along −67.85, max −30.86 | ≥ 20 s inside
                 open-bay launch | leftover 2.07 s | 1.43 s | open door does not hold
                 remainingS at jam | leftover phase only | 20.00 s | readable
                 closing telegraph | leftover no zero-strength register | hold cone strength 0, remainingS > 0 | window you can plan
                 hazard type | leftover debris_current | same | never radiation
CHECKS           npm run check:baseline — FAIL entry 12/15 and exit 12/15. Same three reds: pq020-ceres-topology, sim-v3, sim. Nothing this unit added.
                 node --test test/pq-027-02-weather.test.mjs test/pq-027-03-timed-access.test.mjs — PASS 6/6
                 node --test test/pq-027-03-hangar-jam.test.mjs test/environmental-aperture-jam.test.mjs test/environmental-weather.test.mjs — PASS 8/8
                 node scripts/check-hazard-language.mjs — PASS
UNPROVEN         I did not take a shipping-camera still. I did not fly Ceres in a headed session. simTime is frozen at 1 s during the 20 s (1200 Rapier ticks); the leftover used that same freeze. The wreck ends at along −83.72, not still in the mouth — occupant is false at the end; the hold cone is what parks the wasp. After-hold open on a running clock is still aperturePhase, not a second Rapier launch.
```

FILES            `src/data/environmentalMachinery.js`, `src/data/hazardLanguage.js`, `src/systems/environmentalMachinery.js`, `test/pq-027-03-timed-access.test.mjs`, this receipt.
