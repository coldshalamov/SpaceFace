<!-- LIFETIME: RECEIPT -->
# PQ-026.00 — Momentum sink as the bungee

```text
STATUS DONE

WHAT I FOUND     The weapon and status existed as a four-second frame damper on the *target*.
                 Planting on a rock did nothing to the player. There was no stored-and-returned
                 momentum. Before: after a rock hit the player still left at 1.00× cruise if they
                 burned away — no snap, no 2×. Existing damper tests were already green (11/11).

WHAT I CHANGED   Plant on a rock, burn away, release. Weapons.js stores receding speed against
                 the rock's frame and queues one additive snap impulse back through the plant
                 at 2.04× that speed. A second trigger releases; the four-second window auto-
                 releases. Light ships stay damper targets. No speed-cap write, no cruise nerf.
                 Shared VFX carriers were not edited.

WHAT YOU WILL FEEL   You shoot a rock, burn away, and let go. The line snaps you back through
                 the pursuer faster than you left — earned speed stays yours.

THE NUMBERS      bar | before | after | target
                 plant→tension→release exit / cruise | 1.00× (no snap) | 2.040× (214.2 / 105) | ≥ 2.00×
                 second-trigger exit / cruise | none | 2.040× | ≥ 2.00×
                 Rapier snap exit / cruise | n/a | 2.040× | ≥ 2.00×
                 scenario | test/momentum-sink.test.mjs | seed 26029 | headless

THE FRAMES       Headless scenario is enough for this leaf. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-026.01 gravity mark is a later leaf.
```

## Files

- `src/data/combatDefs.js` — `MOMENTUM_SINK_BUNGEE` tuning; status comment.
- `src/data/weapons.js` — weapon copy: rock plant is the bungee.
- `src/systems/weapons.js` — plant / tension / release; impulse through the physics membrane.
- `src/render/momentumSinkVfx.js` — planner-only optional tension length. No new pool.
- `test/momentum-sink.test.mjs` — 2× cruise scenario, second trigger, Rapier snap.

Not edited: `src/render/vfx.js`, arcade structural FX, instanced sprite pool, density films,
transient materials, structured burst geometry, `fields.js`, queue, `package.json`.

## Checks

| Check | Result |
|---|---|
| Existing damper + presentation tests (before) | 11 pass |
| `node --check` on write-set JS | pass |
| `npm run check:massline:momentum-sink` | 14 pass, 0 fail |
| Printed exit | `exitSpeed=214.200 cruise=105 ratio=2.040` |

STATUS DONE
