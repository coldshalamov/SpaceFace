<!-- LIFETIME: RECEIPT -->
# PQ-177.02 — Supply-chain glyphs on the Orbital market

```text
STATUS DONE on leftover inspector paint.
                 NOT DONE on leftover invented blind %.
                 Headless only.

WHAT I FOUND     Iron Ore already carried leftover
                 producedBy / consumedBy in
                 commodities.js (mining →
                 refinery, trade_hub). Ceres Refinery
                 is type refinery in sectors.js.
                 The leftover Orbital inspector named
                 price and demand and never the chain
                 or whether this dock buys.

WHAT I CHANGED   The selected-row inspector now prints
                 leftover producedBy → leftover
                 consumedBy in leftover cleaned role
                 words, plus leftover why-this-station
                 from the live dock type (entity
                 stationTypeId, else the sector
                 record, else the catalog). Orbital
                 CSS only. No new commodities. Queue
                 unmarked.

WHAT YOU WILL FEEL   Open Market at Ceres, select Iron
                 Ore. Under the quote you see
                 "Mining → Refinery and Trade Hub"
                 and "This dock buys it." A mining
                 berth says it produces it. A dock
                 that is neither says it does not
                 buy it.

THE NUMBERS      bar | before | after | target
                 inspector chain | none | Mining → Refinery and Trade Hub | producedBy → consumedBy from COMMODITIES
                 this-dock at Ceres | silent | This dock buys it. | live type refinery is a leftover consumer
                 invented blind % | forbidden | not invented | do not invent a leftover playtest %
                 headed capture | peeled | not taken | headless only

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     No Mine / Refine map existed, so the
                 leftover words are leftover cleaned
                 role ids (Mining, Refinery, Trade Hub),
                 not leftover invented verbs. Hover on
                 a station glyph was not added; the
                 leftover selected inspector is the
                 leftover surface. Blind playtest %
                 was not invented.

NEXT             Later leaves own charts, twists, and
                 the black market. Do not mark the
                 queue from this receipt.
```

## Files

- `src/ui/views/marketPresentation.js` — leftover chain sentence + leftover this-dock line on the selected inspector
- `src/ui/station/screens/market.js` — leftover live dock type from `state.ui.dockedStationId` / leftover station entity / leftover sector record
- `styles/orbital.css` — leftover inspector chain line
- `test/pq-177-02-glyphs.test.mjs` — leftover Iron Ore at leftover Ceres, leftover consume role, leftover this-dock buys
- `design/program/roadmap/receipts/PQ-177.02-REPORT.md` — this receipt

Not edited: `src/data/commodities.js`, `src/systems/economy.js`, `src/systems/missions.js`, `src/ui/hud.js`, `src/data/ships.js`, `src/render/`, `styles/kit.css`, `design/program/NOW.md`, `program-queue.json`.

## Proof

```text
PQ-177.02 Iron Ore chain: Mining → Refinery and Trade Hub
PQ-177.02 Ceres (refinery): This dock buys it.
```

`node --test test/pq-177-02-glyphs.test.mjs` — 4 pass, 0 fail.
