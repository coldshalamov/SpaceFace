<!-- LIFETIME: RECEIPT -->
# PQ-152.03 — Clauses that twist

```text
STATUS DONE

WHAT I FOUND     Seed 15230 boards could carry physics terms, but none of
                 them fired mid-run and changed the job. A broken term
                 either ate the premium or ended in Mission FAILED. The
                 escort never turned. The lot never cooked. The buyer
                 never showed a badge. PQ-138.04 only mutated three
                 old failure reasons.

WHAT I CHANGED   Five mid-run twist clauses joined the catalog. They
                 stamp from their own seed stream, not the physics-term
                 pool. When they fire the contract mutates through the
                 existing PQ-138.04 successor seam. The escort becomes
                 a hunt. Cooked cargo becomes a recovery. A sting
                 berth reroutes. A waking wreck becomes a hunt. Bait
                 pods become a field-clear. Helios stays clean.

WHAT YOU WILL FEEL   The convoy shows its real colours and the job
                 becomes a hunt. The lot cooks and you are sent back
                 for what remains. The drop is a sting and the home
                 berth is still open. You bump a dead hull and it sits
                 up. You close on the pods and the field goes loud.
                 Nothing dies in a red box.

THE NUMBERS      bar | before | after | target
                 twist clause ids | 0 | 5 | ≥5
                 escort_turns on seed 15230 | 0 | 15 | live
                 cargo_volatile on seed 15230 | 0 | 18 | live
                 buyer_is_the_law on seed 15230 | 0 | 12 | live
                 wreck_wakes on seed 15230 | 0 | 15 | live
                 pods_are_bait on seed 15230 | 0 | 27 | live
                 Helios twist hits | n/a | 0 | stay off 47-A
                 each twist successor | none | live, no Mission FAILED toast | mutate not fail
                 authored catalog rows | 10 | 10 | stay 10
                 capital_boss type | own type | own type | stay own type
                 scenario | test/pq-152-03-twist-clauses.test.mjs | seed 15230 | headless

THE FRAMES       Headless board + mutate-not-fail proofs. No headed GPU strip.

NEXT             Integrator updates the queue. Do not mark the queue from this
                 receipt.
```

## Five clause ids

| Id | Fires when | Mutates into |
|---|---|---|
| `escort_turns` | the convoy flips hostile | `bounty_hunt` (turned) |
| `cargo_volatile` | the lot slams / cooks | `salvage_retrieval` (cooked) |
| `buyer_is_the_law` | you close on the drop | `cargo_delivery` home reroute (sting) |
| `wreck_wakes` | you hit the salvage hull | `bounty_hunt` (wakes) |
| `pods_are_bait` | you close on the pods | `patrol_clear` (bait) |

## Mutate-not-fail proof

Each twist still emits one `mission:failed`, then posts and accepts a live successor on the PQ-138.04 seam. The Mission FAILED toast does not fire. The follow-up toast names the new job. Seed 15230 boards carry all five; Helios carries none.

## Edited vs proved

Edited: `src/data/missionConditions.js`, `src/data/missions.js`, `src/systems/missions.js`, `test/pq-152-03-twist-clauses.test.mjs`, this receipt.

Proved: seed 15230 boards and five mutate-not-fail scenarios via `test/pq-152-03-twist-clauses.test.mjs` (8/8). PQ-152.00 still green (5/5). PQ-152.01 still green (3/3). PQ-152.02 still green (3/3). PQ-138.04 still green (7/7). `check-mission-conditions` green (18 terms). AUTHORED_SET_PIECES still 10. Capital boss stays its own type. heist stays last.

Not edited: `program-queue.json`, `329-curtain-convoy.js`, `ships.js`, `modules.js`, `weapons.js`, `input.js`. No commit.
