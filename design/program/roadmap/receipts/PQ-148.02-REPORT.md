<!-- LIFETIME: RECEIPT -->
# PQ-148.02 — Smuggling physics

```text
STATUS DONE

WHAT I FOUND     The scan was already a cone, not a hold roll. A cruise
                 kick already beats dwell and lands in a catch net. A
                 parked crate already raises heat through the bust event.

WHAT I CHANGED   Nothing in the live cargo or law path. This receipt
                 records the proof that was already on the tree.

WHAT YOU WILL FEEL   Kick a crate through the cone at speed and it
                 slips. Park it in the beam and the law notices. Hide
                 it behind a hauler and the beam misses.

THE NUMBERS      bar | before | after | target
                 drop-kick (seed 14821) | entered 1, scanned 0, caught 1, transit 0.400 s, cruise 95.0 | same | transit < 0.70 s dwell, net catch, heat 0
                 scanned pod (seed 14822) | heat 0.000 → 0.160, found, dwell 0.700 s | same | heat +0.16 via bust
                 hauler blind (seed 14823) | scanned 0, heat 0 | same | hull occludes the line
                 cone | heading + 0.55 rad + 90 wu, not a sphere | same | physics, not a roll
                 heat writer | heat.js on contraband:scanned | same | no second writer
                 scenario | test/pq-148-02-smuggling-physics.test.mjs | seeds 14821 / 14822 | Rapier headless

THE FRAMES       Headless Rapier drop-kick and parked-pod dwell. No headed
                 GPU strip.

NEXT             Integrator updates the queue. Leftover: .03 named cargo
                 (owner, origin, destination; a spilled pod's owner reacts).
                 Do not mark the queue from this receipt.
```

## Files

- `test/pq-148-02-smuggling-physics.test.mjs` — named proof

Already live: `lawSecurity.js` (cone + dwell), `lootShards.js` (catch net),
`jettisonImpulse.js` (cruise kick), `heat.js` (0.16 bust).

Not edited: write set, `program-queue.json`, `NOW.md`.
