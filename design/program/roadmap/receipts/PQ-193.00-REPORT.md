DONE PQ-193.00 — every lockable hull on the default route publishes a packaged complete body; pending hulls are un-targetable empty substrate until that same body arrives.

DONE WHEN (verbatim): default-route targeting never sits on empty space for those hulls.

WHAT I FOUND — The inherited dirty changes already required and allowlisted all 13 roster hulls plus the Massline liner, but an explicitly required ship with no allowed whole-body selection still requested seven modular slots and could fall through to modular assembly. The prior worker's only open item was the live-route proof: a headless-browser launch stalled at the gpu-resources stage under software GL.

WHAT I CHANGED — This adoption kept the implementation as found (allowlist, required-hull routing, missing-selection fallthrough guard, and the extended tests) and added the missing live evidence: the default route was flown on the isolated Electron launch (real GPU, seed 47 New Game), every lockable ship was inventoried, and a still-pending patrol Hornet was approached through the public `world.relocatePlayerInSector` seam so its decode could be watched end to end.

WHAT YOU WILL FEEL — Ships you can lock are complete manufactured bodies — Hornet patrol, Atlas and Mule traffic, the reaver wing, the yard tug — and a hull that is still decoding simply is not a target yet: no bracket on blank space, no modular kit pretending to be the ship while the real one arrives.

THE NUMBERS

| Bar | Before this worker | After | Target | Seed |
|---|---|---|---|---|
| Required, allowlisted roster/liner bodies | 14/14 in inherited diff | 14/14 | 14/14 | deterministic catalog |
| Required ship with no live selection: requested modular slots | 7 | 0 | 0 | deterministic entity fixture |
| Default route lockable ships publishing a blank (Electron, seed 47, t≈118 s) | unmeasured | 0 of 7 lockable | 0 | 47 |
| Pending hull publishes then becomes lockable (patrol Hornet id 312) | unmeasured | pending→ready→authored, `hornet_production_v1.glb`, 10/13 visible meshes | yes | 47 |
| Focused checks | 29/29 | 49/49 twice; arrival contract 7/7 | all pass | fixtures |

LIVE ROUTE — `publish-watch-electron.mjs` (scratch): seed-47 New Game on isolated Electron, 15 ships at sim t≈118 s; the 7 lockable ships all carried `assetState: authored` with packaged whole-ship slots (`kestrel.glb`, `atlas_production_v1.glb` ×2, `mule_production_v1.glb`, `ashline_rig.glb` ×2, `yard_tug.glb`) or had already published and streamed their mesh out beyond draw range; the 8 still-pending ships (patrols, tug, salvors at ~950–14 000 wu) were not lockable — empty substrate by design until the body arrives. `arrival-proof-electron.mjs` relocated beside pending patrol Hornet 312: un-lockable while pending, published `wholeships/hornet_production_v1.glb` at sim t≈38 s with 10 visible meshes in scene, lockable only after publication; chase screenshot attached. Target cycling correctly finds zero targets in a no-hostile opening (the cycle is hostile-only) — the per-ship admission/lockable data above is the contract evidence.

The earlier browser-route stall (LAUNCH timeout at `gpu-resources`, software GL, concurrent fleet load) reproduced once here and is the first-flight cook's in-flight territory — it is not this leaf's code. On the Electron route the same candidate reaches flight immediately. The `[render] opening submission post-submit validation failed` console error observed on launch is pre-existing (already present in committed evidence `design/program/roadmap/evidence/h1/row7-pq022-asset-leaves/*/report.json`) and belongs to the same opening-submission work.

FILES

- `src/render/partsLibrary.js` — required-hull sets (`REQUIRED_WHOLE_SHIP_DEF_IDS`, traffic roles, asset refs), `requiresProductionWholeShipForEntity` rewrite, `liveWholeShipSelection` gate on every selection path, the 12 packaged-live allowlist additions, recovery-tug and smuggler/pirate maps, the missing-selection `return {}` preload guard, the missing-record throw, and the `requiredWholeShip` propagation line in `wrapShipWithAuthoredParts`. **Committed by patch staging only these hunks** — the same file's dirty first-flight cook machinery (first-flight entity sets, upgrade-queue holds/pumps/settle, shared-material-role stamps, liveSectorGpuAdmission) is preserved uncommitted for its owner.
- `test/live-ship-visual-package-coverage.test.mjs` — roster/liner package-content coverage (payload identity, size, SHA-256, body primitives).
- `test/live-whole-ship-admission.test.mjs` — publication cases, missing-selection, missing-body and delayed-body regressions.
- `design/program/roadmap/receipts/PQ-193.00-REPORT.md` — this receipt.

CHECKS

| Command | Result |
|---|---|
| `node --test test/live-ship-visual-package-coverage.test.mjs test/live-whole-ship-admission.test.mjs test/fleet-player-wholeship-routing.test.mjs` | PASS 49/49 (this worker) |
| `npm run check:live-whole-ship-admission` | PASS 42/42 |
| `node --test test/pq-193-live-picture-arrival.test.mjs` (untracked fleet harness; not committed by this leaf) | PASS 7/7 |
| Electron playable+capture, seed 47 | flight reached; PILOT/HULL/WORLD/CONTROLS/AUTHORED HULL pass; CLEAN flag is the pre-existing opening-submission console error above |
| `npm run check:baseline` | entry reds `pq020-ceres-topology`, `sim`, `sim-v3` are pre-existing foreign golden drifts (unchanged fingerprints per prior receipt); this leaf adds no sim change |

UNPROVEN — Electron parity on a second machine and the matched draw-call/p95 pair stay open for the packet's performance row; nothing here is a stop condition for the leaf's own done-when.

Evidence (scratch, not product source): `.devshots/delegate-20260910/scratch/pq193-00/` — `route.json`/`route.png`, `publish-watch.json`, `arrival-proof.json`/`arrival-proof.png`, `publish-watch-electron.mjs`, `arrival-proof-electron.mjs`, `split-193-patch.mjs`, `pq193-only.patch`.

Candidate: branch `master`, shared dirty checkout; only the PQ-193.00 hunks of `src/render/partsLibrary.js` were staged and committed; the preserved first-flight cook diff remains in the working tree for its owner.
