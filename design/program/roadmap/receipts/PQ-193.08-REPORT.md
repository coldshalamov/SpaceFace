DONE PQ-193.08 — The volatiles tanker and the inspection cutter spawn on Helios as rare extras, and they read as ships.

DONE WHEN (verbatim): Chase-camera stills first. If they still read as missing-hull kits, enclose, then let the existing tanker and customs traffic roles roll. Tug is already live. Do not spawn a toy.

WHAT I FOUND — Both GLBs were on disk but read as open kit frames (missing hull enclosure), and the tanker/customs traffic roles were mix-zero so they never rolled.

WHAT I CHANGED — Rebuilt both hulls through the NPC work-fleet Blender builder with enclosed hulls (source, parts, release GLBs regenerated), and wired them as mix-zero Helios extras that the start-sector traffic always seeds once each, rather than ambient rolls. Freight causality knows the tanker as a volatiles carrier.

WHAT YOU WILL FEEL — A tanker and a customs cutter you have not seen before pass on the Helios lanes; they are closed hulls, not frames.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Helios start-sector traffic includes tanker + cutter | 0 / 0 | 1 / 1 | each once | test seed |
| Ambient roll weight | mix-zero (never) | mix-zero extras, seeded explicitly | not a toy roll | mix table |
| Hull enclosure | open kit frames | enclosed builder hulls | reads as a ship | rebuilt GLBs |

THE FEEL — Two more working boats with jobs; the lane feels populated by trades, not by clones.

NEXT — `--next`.

CHECKS

| Command | Result |
|---|---|
| `node --test test/unused-model-live-wire.test.mjs` | the tanker/cutter cases PASS (re-run 2026-09-12 on `824c773bb`); two unrelated cases in the same file (`Helios lane furniture admits the repaired corridor family`, `restored Ceres/Tethys landmarks do not wear the toy yard kit`) are red from a separate live-place-selector change and are tracked as their own defect |
| `npm run check:baseline` | 11/15; the four reds are the pre-existing tree-wide drifts, not this leaf |

EVIDENCE — Commit `37b083a72` (2026-09-11) "Let Helios show a volatiles tanker and inspection cutter as rare extras." 12 files.

UNPROVEN — Headed chase still of the rebuilt hulls in a live spawn.
