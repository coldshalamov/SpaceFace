<!-- LIFETIME: RECEIPT -->
# PQ-148.03 — Cargo with a name

```text
STATUS DONE

WHAT I FOUND     The named proof was already on the tree. A spilled
                 pod already carries origin/destination/owner. After
                 freight:cargoSpilled the owner demands restitution,
                 and both the bark and the comms ledger say her name.
                 Player dump of their own hold is a null reaction.

WHAT I CHANGED   Nothing in the live cargo path. No second identity
                 catalog. This receipt records the proof that was
                 already committed.

WHAT YOU WILL FEEL   Break a named hauler and Mira Bluepack is on
                 the radio: she wants the crate back. The log says
                 the same name. Dump your own ore and nobody posts
                 a bounty on you.

THE NUMBERS      bar | before | after | target
                 identity stamp | owner/origin/dest on pod.data | same | named pod
                 owner reaction | restitution | same | owner reacts
                 bark cites owner | "Mira Bluepack demands restitution for the spilled cargo." | same | name in bark
                 ledger cites owner | "Mira Bluepack — restitution after the spill." | same | name + verb
                 self-jettison | no bark, no bounty | same | not a self-bounty
                 scenario | test/pq-148-03-cargo-name.test.mjs | seed 14803 | headless, 2/2 pass

THE FRAMES       Headless spill + jettison. No headed GPU strip.

NEXT             Last 148 leaf. No leftover. Integrator updates the
                 queue. Do not mark the queue from this receipt.
```

## Files

- `design/program/roadmap/receipts/PQ-148.03-REPORT.md` — this receipt

Already live (proved, not edited): `src/data/cargoIdentity.js`, `src/systems/lootShards.js` (`stampCargoIdentity`), `src/systems/cargo.js` (player jettison stamps `ownerId`), `src/systems/barkDirector.js` (`cargoSpillBarkText` / `cargoSpillLedgerText` on `freight:cargoSpilled`).

Named proof: `test/pq-148-03-cargo-name.test.mjs`.

Not edited: `lawSecurity.js`, `heat.js`, `range.js`, `tetherGameplay.js`, `aftermathWrecks.js`, `survivorPod.js`, `input.js`, `proofSixtySeconds.js`, `program-queue.json`, `NOW.md`.
