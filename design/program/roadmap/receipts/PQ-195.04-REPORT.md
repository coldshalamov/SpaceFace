<!-- LIFETIME: EVIDENCE -->
# PQ-195.04 — One visible local consequence — REPORT

```yaml
unit: PQ-195.04
parent: PQ-195
state: integrated
acceptance: focused_green
date: 2026-09-19
implementer: opencode-go/deepseek-v4.1-flash (worker), controller-integrated
reviewer: controller self-review (opencode provider quota exhausted; GLM/Kimi dispatches stalled)
integratedCommit: a7b1e4f36e2128805672a6e22b8c90a7050540c7
candidateCommit: ffe5ba07b + d1d0b3d1c (pq195-third-shift)
```

## Status: INTEGRATED

Berth Three is down a flywheel. Its stalled hauler waits beside the lawful
catcher until a committed fork handoff activates it — good return resumes the
haul circuit, damaged return (< 0.6) runs the bounded repair shuttle, and a
fenced/destroyed/expired/abandoned load leaves the legitimate operation waiting.

## Done-when numbers

| Claim | Evidence |
|---|---|
| Three distinct sim states | `resumed` (full berth↔yard circuit), `reduced` (shorter/slower repair shuttle), `stalled` (no berth job) — real npcJobs phases/intents, not cosmetic flags |
| Bound to the receipt | Activation consumes `heist:receiverCommitted` only when `facilityId === 'lawful_catcher'`, `payloadStableId === SP-07`, finite `condition01`; `receiptId` recorded on the job payload |
| Save/load survives | Consequence state IS the serialized `state.npcJobs` record; the hull is transient, re-materialized by heistFacilities and re-linked by stable per-seed `worldRecordId` |
| Never plays over a lost load | Fence commits carry no `condition01`; destroyed/expired/abandoned never reach a commit — four non-activation paths all proven |
| Idempotent | Replay of the same receipt and a second different receipt both return the existing record untouched; one job id, one tier, original binding kept |

## Files changed

- `src/data/heistFacilities.js` — `BREAKAWAY_BERTH` authored site (parked spot,
  two service circuits, worker identity), `BREAKAWAY_BERTH_RESUME_MIN_CONDITION01 = 0.6`,
  `berthServiceTier()`. Deliberately NOT a `PQ019_FACILITIES` member.
- `src/systems/heistFacilities.js` — `berthWorkerRecordId(seed)` shared join key;
  `_materializeBerthWorker` spawn/adopt on sector materialize; cleanup on
  dematerialize/destroy; transient `berth.workerEntityId` only.
- `src/systems/npcJobsRuntime.js` — `_onBerthHandoff` seam on
  `heist:receiverCommitted`, `_activateBerthWorker` (assign-or-virtual-create),
  `berthStatus()` read-only projection.
- `test/pq195-04-local-consequence.test.mjs` — 11 proofs through real seams.
- `test/pq019-facility-embodiment.test.mjs` — repaired stale owned-entity counts
  (3 heads + .01's 3 colliders = 6 idle, 7 after launch). Was red since .01.

## Checks (controller-reproduced)

| Check | Result |
|---|---|
| `node --test test/pq195-04-local-consequence.test.mjs` | 11/11 (worktree + master) |
| `node --test test/pq019-facility-embodiment.test.mjs` | 6/6 (repaired) |
| `node --test test/pq195-01..03` | 19/19 |
| `npm run check:pq019c:mission` | 67/67 |
| `npm run check:baseline` | 15/15 |

## Reviewer notes

- No independent cross-model review: the opencode-go provider was exhausted
  mid-review (glm-5.3 session stalled twice; kimi-k3 and glm-5.3-flash produced
  zero events). Controller self-review substituted: full diff read, all checks
  independently reproduced, pre-existing reds verified by stash isolation.
- Known unrelated red: `test/fresh-run-reset-owners.test.mjs` expects `{byId:{}}`
  while npcJobsRuntime.newGame ships `{byId:{}, siteCouriers:{}}` — predates this
  branch on master; foreign, left alone.
- Ambient effect: the parked worker hull now exists in Tethys on every
  materialize; takes no spawn-budget slots, `passive` AI, proven harmless.
