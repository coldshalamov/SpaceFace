<!-- LIFETIME: RECEIPT -->
# PQ-024 survey-to-relay H1 capture receipt

## Dispatch identity

- Exact unit: `PQ-024.survey-h1-capture`
- Disposition: **DONE / FUNCTIONAL H1 ACCEPTED**
- Candidate commit: `c10f8921`
- Browser claim: `31656-aad66eda6f14d0177d92cd8d`
- Candidate digest: `61fa7c4ddaa125620b96af9fc2fae3027f8c7439935631e01a1a920215ee604b`
- Fixed seed: `24024`

## Accepted evidence

The registered Browser cell passed the complete public survey-to-relay route on real Intel
ANGLE/D3D11 with zero page issues. It bought the required materials at Helios, selected and reached
an asteroid through the unified map, latched Massline across the 60 Hz input owner, revealed a
formation, committed the Core, installed an extractor, observed a real positive output receipt,
materialized exactly one relay, quick-saved, cold-continued, and re-entered the same producing site.

Only after Browser PASS, the distinct Electron wrapper continued the canonical first window through
the same public actor. Its normalized cargo, Survey, Core, extractor, production, relay, Continue,
and re-entry projection exactly equals Browser. It also used real Intel ANGLE/D3D11, emitted zero
page issues, and closed the owned runtime/profile.

The complete review bundle is committed under
[`row9-pq024-survey-claim/`](../evidence/h1/row9-pq024-survey-claim/). Browser receipt SHA-256 is
`12a83694a6058b650983bd07ab221053554f443501f8e8d1449204a58d37e53a`; Electron receipt SHA-256 is
`c536da270a8831f6889448136b5b37c85f8f5426869a9420b1bc1dabc0677fcc`.

## Causal repair boundary

The accepted claim followed the retained fingerprint `1c7548ce...`: after cold Continue, the active
restored waypoint overlaid the rematerialized asteroid contact with an old entity identity and higher
map hit priority. Candidate `c10f8921` uses the shipped Route and Mission lens controls to remove that
overlay, waits for its click target to disappear, then pointer-selects the current exact site asteroid.
The seconds-scale manifest regression pins that public sequence. Earlier failed claims remain retained
in the active packet and are not promoted as passing evidence.

## Verification

- Broker Browser terminal result: **PASS**.
- `node scripts/check-pq024-asteroid-claim-electron.mjs`: **PASS**.
- Browser/Electron semantic projections: **equal**.
- Browser and Electron page issues: **0 / 0**.
- Browser and Electron GPU: real Intel ANGLE/D3D11.
- Required focused gates: `npm run check:pq024:survey-claim`,
  `node --test test/asteroid-sites.test.mjs`, and `npm run check:sim:compare`.
- Control-plane gates: `node scripts/check-program-docs.mjs` and
  `node --test test/program-control-tools.test.mjs`.

## Residual ownership

- `PQ-024.h2-verdict`: blocked on the named SpaceFace human gameplay/visual reviewer and the
  independent `PQ-022.relay-human-verdict` owner.
- `PQ-024.h3-performance`: blocked on `PQ-034.native-closure` and a quiet-machine matched-performance
  window.
- `PQ-024.promote`: depends on relay promotion plus both H2 and H3.

No human judgment, physical-controller, screen-reader, performance, relay `route_accepted`, or parent
promotion claim is made here. The `browser-gpu` and `validation-broker` mutexes are released.
