DONE PQ-193.07 — The Helios Arclight flies as a rare heavy hauler with its own body and its own traffic role.

DONE WHEN (verbatim): Promote and route the existing Arclight. Player can find it. Do not steal the Span or Atlas slot. Package before wiring.

WHAT I FOUND — The Arclight existed as a foundry source (`assets/ships/m4_hero_hauler/`) but had no packaged body, no render package, and no traffic role, so nothing in the live world could ever spawn it.

WHAT I CHANGED — Packaged `helios_arclight.glb` (parts + release + a render package with a pilots entry), gave it its own partsLibrary identity, and added a rare heavy-hauler traffic role on Helios that always seeds one Arclight in the start sector. Span cargo haulers and the Atlas keep their own slots.

WHAT YOU WILL FEEL — Somewhere on the Helios lanes a long heavy hauler that is not a Span goes by. It is rare; you will not see three of them.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Arclight packaged bodies | 0 | parts + release + render package | packaged before wiring | manifests |
| Helios start-sector traffic includes one Arclight | never | always 1 | ≥ 1 rare | test seed |
| Span / Atlas slots | own bodies | own bodies (unchanged) | untouched | maps |

THE FEEL — A distinct heavy silhouette on the lane; it is a new thing to look at, not a re-skin.

NEXT — `--next`.

CHECKS

| Command | Result |
|---|---|
| `node --test test/pq-193-07-arclight-hauler.test.mjs` | PASS 2/2 (re-run 2026-09-12 on `824c773bb`) |
| `npm run check:baseline` | 11/15; reds `pq020-ceres-topology`, `save-schema`, `sim`, `sim-v3` are the pre-existing tree-wide drifts being repaired separately (goldens red since the 2026-09-08 fresh root; not this leaf) |

EVIDENCE — Commit `ef827d714` (2026-09-11) "Let the Helios Arclight fly as a rare heavy hauler." 11 files: manifests, GLBs, render package, `src/systems/traffic.js`, `src/render/partsLibrary.js`, the leaf test.

UNPROVEN — A headed chase-camera still of a live Arclight spawn; the packaged render package and the traffic-role test close the reachability bar.
