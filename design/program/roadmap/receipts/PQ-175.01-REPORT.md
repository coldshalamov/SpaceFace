<!-- LIFETIME: RECEIPT -->
# PQ-175.01 — Props are toys

```text
DONE  PQ-175.01 — each of the four swarm rooms now has three props you can
                 use to kill or get away, and the live Survival installer
                 registers them so they move mass in the room.

WHAT I FOUND     The rooms already authored three toys and planArenaInstall
                 kept install.toys. playArenaToyScenario was catalog
                 kinematics only. survivalArena._onWavePlanned installed
                 fields, mines, and cover, and never the furniture.

WHAT I CHANGED   The live installer now keeps the authored toys, emits them
                 on survivalArena:installed, and exposes them on
                 diagnostics. Crushers slam in update. Currents that are
                 already the room cone stay on the two field slots. Shutters
                 cut shots. Plates bank shots. Relays still occupy the two
                 storm slots. No third field. Never a radiation aura.

WHAT YOU WILL FEEL   Hide behind the shutter and the shot dies on the bar.
                 Bank off the plate and the bolt comes back from a new
                 angle. Ride the sluice or the well into the jaws while
                 they cycle; wait, and the same shove is quiet. Relays
                 still have to be the bridge. The rooms do not chew hull
                 by standing in a glow.

THE NUMBERS      bar | before | after | target
                 live toys registered | 0 | 3 per law room on seed 17510 | install event + diagnostics
                 field slots | 2 | 2 | still two
                 lagrange toys used | 0 | ridge shutter, saddle plate, basin crusher | 3
                 cinder toys used | 0 | sluice current, lane shutter, mouth crusher | 3
                 cryo toys used | 0 | frost shutter, ice plate, shock crusher | 3
                 storm toys used | 0 | two relays, grid shutter | 3
                 kill or escape | none | 2 kills + 1 escape per room | kill or escape
                 live crusher surge | n/a | slam ≥ 30 wu/s | crumple speed
                 live crusher calm | n/a | 0 wu/s from rest | schedule is the trick
                 hazard type | none | debris / debris_current | never radiation
                 scenario | seed 17510 | four rooms, live installer + catalog | headless

THE FRAMES       Headless proof. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-175.02 is draft verbs.
                 Do not mark the queue from this receipt.
```

## Files

- `src/systems/survivalArena.js` — live toy install, diagnostics, update step
- `src/data/arenaModuleLibrary.js` — toy catalog, verbs, seed-17510 scenario
- `src/systems/lagrangeCrucible.js` — ridge shutter, saddle plate, basin crusher
- `src/systems/cinderSluiceArena.js` — sluice current, lane shutter, mouth crusher
- `src/systems/cryoDriftArena.js` — frost shutter, ice plate, shock crusher
- `src/systems/stormLatticeArena.js` — two relays, grid shutter
- `test/pq-175-01-props-are-toys.test.mjs` — catalog + live installer pin

Not edited: `survivalDraft.js`, `survivalMutators.js`, `survivalWaves.js`, `program-queue.json`.

---

# PQ-175.01 — continuation: live physical proof per toy kind (seed 17501)

```text
DONE  PQ-175.01 (continuation) — each of the five authored toy kinds is
      installed by the live law room on seed 17501 and changes an actual
      shot, trajectory, kill, escape or carried mass through the shared
      physics/field kernel, with a paired negative control per kind.

WHAT I FOUND     The uncommitted arenaModuleLibrary.js hunk already held the
                 toy contract (kinds, verbs, hazards, live proof helpers) but
                 no test installed each kind on the fixed seed; the only seed
                 shipped was 17510 and the current/relay kinds had no
                 fail-closed physical assertion.

WHAT I CHANGED   Added a seed-17501 test that boots the real Survival room
                 per arena, registers the authored toys through the live
                 installer, and asserts each kind's physical effect through
                 the existing field kernel, surface-contact reflect, crusher
                 step and storm conductivity graph — each with a control that
                 must NOT fire; no production code was rewritten.

WHAT YOU WILL FEEL   A shot dies on the authored shutter bar and a parallel
                 one passes. A bolt banks off the Cryo plate and comes back
                 from the other side while an off-axis bolt flies on. The
                 Cinder crusher slams a drifting hull past the crumple speed
                 in surge and is dead quiet in calm; the sluice carries a
                 hull down the wedge; the Storm relay walks the current
                 graph. None of it is a glow.

THE NUMBERS      bar | before | after | target | seed
                 live toys per arena | (catalog only) | 3/3/3/3 | >= 3 | 17501
                 shutter crossing/parallel | unmeasured | dead / alive (all 4) | dead / alive | 17501
                 plate bank n?v | unmeasured | +90.000 vs -90 in | sign flip | 17501
                 crusher surge / calm | unmeasured | 1776.856 / 0.000 wu/s | >= 30 / 0 | 17501
                 current carried / outsider | unmeasured | 139.279 / 0.000 wu/s | > 0 / 0 | 17501
                 relay live / dead hops | unmeasured | 7 / 0 | >= 1 / 0 | 17501
                 field slots | 2 | 2 | <= 2 | 17501

FILES            `test/pq-175-01-arena-toys.test.mjs` (new) —
                 seed-17501 live proofs; `src/data/arenaModuleLibrary.js` —
                 adopted the uncommitted toy contract, unchanged; this receipt.

CHECKS           npm run check:baseline          FAIL (pre-existing 11/15
                   green: pq020-ceres-topology, sim, sim-v3, massline
                   47-a timeout — same set at entry)
                 npm run check:crucible:arc      PASS 15/15
                 npm run check:crucible:run      FAIL 13/14 suites — only
                   test/crucible-results.test.mjs, identical fingerprint at
                   entry (crucibleStarterIdForSetup null -> 'ricochet_runner',
                   expected 'physics_toolkit'; foreign starter-kit change)
                 node --test test/pq-175-01-arena-toys.test.mjs  PASS 6/6
                   (run twice, identical)
                 node --test test/pq-175-01-props-are-toys.test.mjs
                   test/survival-arena-bank-feedback.test.mjs  PASS 15/15

UNPROVEN         Headed GPU/route capture; entity position integration is
                 owned by the movement/physics systems, so the current proof
                 measures the field-kernel velocity it writes, not travelled
                 distance on the shipping route. Relay conduction is proven
                 through the shared storm graph, not a live bolt chain.
```
