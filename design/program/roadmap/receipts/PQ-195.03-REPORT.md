<!-- LIFETIME: EVIDENCE -->
# PQ-195.03 — Two destinations for one body — REPORT

```yaml
unit: PQ-195.03
parent: PQ-195
state: integrated
acceptance: focused_green
date: 2026-09-19
implementer: opencode-go/deepseek-v4.1-flash (worker), controller-integrated
reviewer: opencode-go/glm-5.3 (independent, VERDICT: ACCEPT)
integratedCommit: 3eb061cefc0f1caf0ed6b6b177c89d9f10dddf32
candidateCommit: e9bd80468721c0bd2699565e37928220490ce950 (pq195-third-shift)
```

## Status: INTEGRATED

The Quiet fence is a real second destination for the same SP-07 assembly: one
accepted contract, one physical object, one receiver family, one arbiter — two
policies. A possessed load's `fence_contact` settles `fenced_success` at the
fence's own flat terms; the lawful fork still pays base + condition bonus.

## Done-when numbers

| Claim | Evidence |
|---|---|
| Both routes complete from one accepted contract | Lawful: `capture_settled` → `lawful_arrival_observed` → complete (played through real Rapier). Illicit: latch + real `physics:impact` at the fence head → `fenced_success` → complete |
| Never paid twice by relabelling | `record.settled` gate refuses re-arbitration both directions (proved by direct `onFacilityCandidate` probes post-settle); exactly one `economy:grantCredits` per run |
| Latching never raises WANTED | `lawSecurity.reportIncident` spied at the public seam: 0 reports, 0 heat; `reportsTheft:false` for the variant |
| Fence's own terms | `fencePayoutCr: 1200` (premium over 960 lawful base, under the 1320 tier-3 board ceiling), flat — no Concord condition grade (`deliveredCondition` not recorded for fence handoffs) |
| Capsule Run untouched | `PQ019C_TERMINAL_SETTLEMENT` byte-identical; capsule `fencePayoutCr: 0`; a live capsule fence run still pays `payoutCr` end-to-end |

## Files changed

- `src/data/heistMission.js` — `fencePayoutCr: 1200` tuning candidate; settlement
  remap `fenced_success → complete/'fenced'`; `fenced` cue line; Quiet clause in
  the offer description; policy field (0 for capsule).
- `src/systems/heistFacilities.js` — facility-specific contact custody: the
  catcher head still refuses fork-variant touches while the fence stays a contact
  receiver for every payload; `payloadStableId` no longer hardcoded to the
  capsule; `_forkCustodyProof`/`prepareReceiverHandoff`/`commitReceiverHandoff`
  are fork-facility-scoped; `condition01` only for fork deliveries.
- `src/systems/missions.js` — outcome-aware payout: `fenced_success` pays flat
  `fencePayoutCr` when > 0; all other outcomes keep the base+quality fold-in.
- `test/pq195-03-two-destinations.test.mjs` — new, 9 tests (a–f + offer copy).
- `src/missions/heistMissionRuntime.js` — NOT changed: `settledOutcome` already
  records the fenced outcome for .04.

## Integration note

`src/systems/missions.js` carried protected foreign work (an in-progress
career-history feature importing exports absent at HEAD). The foreign hunk was
preserved to a patch file, reverted for the commit, and re-applied immediately —
the leaf landed complete and the foreign diff is byte-identical in the tree.

## Checks (controller-reproduced; reviewer independently reran)

| Check | Result |
|---|---|
| `node --test test/pq195-03-two-destinations.test.mjs` | 9/9 (worktree + master) |
| `node --test test/pq195-01-*.mjs test/pq195-02-*.mjs` | 10/10 |
| `npm run check:pq019c:mission` | 67/67 |
| `npm run check:baseline` | 15/15 |
| Worker's extra sweep (fork/physics/gate/route/save/refusal/pq195-00) | 85/85 |

## Reviewer notes (non-blocking)

- If a future variant sets both `fencePayoutCr > 0` and `qualityBonusFraction > 0`,
  the flat fence override silently wins for `fenced_success` — intended today.
- No dedicated fence-side BW-01 refusal test for breakaway; the shared gate path
  covers it structurally (capsule refusal test exists).
- Test shortens `launchWindowS` on the posted offer — harness convenience; accept
  path is the shipped intent.
