<!-- LIFETIME: RECEIPT -->
# PQ-138.05 — The evidence ledger at one site

```text
STATUS DONE

WHAT I FOUND     The leftover Ceres checklist already
                 lived in the leftover ledger test. One
                 player-caused freight kill at the
                 Refinery Approach already left five
                 traces in world state. There was no
                 receipt.

WHAT I CHANGED   I kept that leftover checklist. I did
                 not rewrite aftermath, traffic, rumor,
                 or the market. This receipt is new.

WHAT YOU WILL FEEL   Kill a loaded hull on the Ceres
                 approach. The wreck stays. The route
                 thins. Iron moves. A rumor names the
                 yard. The station shows a patch.

THE NUMBERS      bar | before | after | target
                 wreck persists | leftover aft marker + live wreck | same 1 | one wreck
                 traffic thins | leftover 2 haulers → 1 | same 1 | witness remains
                 price moves | leftover economy:applyTradePressure | same 1 | price or mid moves
                 rumor appears | leftover pirateRumor + UI card | same 1 | a rumor
                 structure patch | leftover station patched receipt | same 1 | a patch
                 checklist | leftover 5/5 true | same 5/5 | all five traces
                 scenario | leftover Ceres kill harness | 8 s | headless

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The leftover incident is an injected
                 combat:damage + entity:killed pair, not
                 a flown default-route gun kill. Traffic
                 thinning is the loaded hull gone and the
                 witness marked disrupted, not a sector
                 population census. Price is leftover
                 trade pressure on iron, not a visible
                 ticker. The rumor is leftover pirate
                 rumor state plus a UI card, not a
                 station bark. The patch is a leftover
                 station data receipt, not a GLB scar.

NEXT             Integrator updates the queue. Do not
                 mark the queue from this receipt.
```

## Files

- `design/program/roadmap/receipts/PQ-138.05-REPORT.md` — this receipt

Already live, not rewritten: `src/systems/aftermathWrecks.js`, `src/systems/traffic.js`, `src/systems/pirateRumor.js`, `src/systems/economy.js`, `test/pq-138-05-evidence-ledger.test.mjs`.

Not edited: `lawSecurity.js`, `program-queue.json`, `src/render/`, `assets/`.

## Honesty gaps

- The leftover kill is an injected `combat:damage` + `entity:killed` pair on a planted Ceres freight hull. It is not a Rapier-flown default-route gun kill.
- "Traffic visibly thins" is leftover hauler count 2 → 1 plus `routeDisrupted` on the witness. It is not a headed strip of empty lane.
- "A price moves" is leftover `economy:applyTradePressure` on Ceres iron. The leftover mid/buy quote changes. There is no station chart in this proof.
- "A rumor appears" is leftover `pirateRumor` zone headline plus a UI card body. It is not a mechanic line or band-radio bark.
- "A structure shows a patch" is leftover `station.data.structurePatch` / patched receipt, not a docked visual scar.
- No shipping-camera strip. The campaign forbade headed capture.
- This leaf did not retune leftover 138.00–.04.

## Proof

```text
PQ-138.05 Ceres evidence ledger
| trace | ok | evidence id |
| wreck persists | true | aft_* |
| traffic thins | true | route:station_ceres:* |
| price moves | true | station_ceres:cmdty_ore_iron:* |
| rumor appears | true | sector_ceres_belt:zone_ceres_refinery |
| structure patch | true | aft_patch:* |
```

`node --test test/pq-138-05-evidence-ledger.test.mjs` — 1 pass, 0 fail.
