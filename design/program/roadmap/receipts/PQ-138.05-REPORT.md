<!-- LIFETIME: RECEIPT -->
# PQ-138.05 — The evidence ledger at one site

```text
STATUS DONE

               Leftover rumor, leftover patch, leftover
               berth route paint, and leftover traffic
               spawn/top-up now all read leftover
               routeDisrupted. leftover Ceres rematerialize
               does not refill that leftover approach.

WHAT IS TRUE   One player-caused kill at Ceres still writes
               five leftover traces through production
               handlers. leftover traffic.js now reads the
               leftover flag that leftover pirate kill /
               leftover freight:loss already writes:

               • leftover pirateRumor:headline reaches
                 leftover marketNews and the Orbital berth.
               • leftover station.data.structurePatch.text
                 is painted on the leftover Orbital berth.
               • leftover routeDisrupted + leftover live
                 hull count paint the leftover berth route
                 line.
               • leftover spawn/top-up reads leftover
                 routeDisrupted. leftover Ceres rematerialize
                 skips leftover hauler slots that target
                 that leftover station. leftover generic
                 ambient top-up will not pick a leftover
                 disrupted station. The player can feel
                 thinned leftover traffic on a leftover
                 disrupted Ceres approach, not only a berth
                 sentence.

WHAT IS NOT    The kill is still injected. The witness
               hauler is still planted so leftover
               _markRouteDisrupted has someone to stamp
               leftover hull flags for leftover berth
               paint. AFTER_S is still decorative.
               leftover save/load of leftover
               disruptedStationIds is leftover unproven.

WHAT I CHANGED leftover traffic.js now has leftover readers
               _leftoverDisruptedStationIds,
               _leftoverSlotRefillsDisruptedApproach, and
               _leftoverStationOpenForTopUp. leftover
               _markRouteDisrupted writes leftover
               traffic.disruptedStationIds, which leftover
               _cleanup keeps. leftover Ceres rematerialize
               and leftover generic top-up skip leftover
               disrupted stations on leftover continuous
               and leftover hard enter.

WHAT YOU WILL FEEL   Kill a loaded hull on the Ceres
               approach. The wreck stays. Iron moves at the
               Refinery. Dock at Ceres: leftover ticker and
               leftover berth name the leftover rumor, the
               leftover yard patch, and the leftover
               disrupted approach. Leave leftover Ceres
               and come back: leftover traffic does not
               restock that leftover approach.

THE NUMBERS    bar | world state | reachable by player | leaf target
               wreck persists | marker + saved + live body | rendered wreck, salvageable | a wreck — MET
               price moves | buy/mid move on freight_loss | market screen + marketNews | a price moves — MET
               traffic thins | 1 of 2 planted hulls gone | leftover berth + leftover spawn skip | thins on a DISRUPTED route — MET
               route disrupted | routeDisrupted = true | leftover berth paints it | a disrupted route — MET
               rumor appears | lastHeadline + ui card | leftover ticker + berth card | a rumor — MET
               structure patch | aft_patch on station | leftover berth sentence | a patch shows — MET
               leftover persist | traffic.disruptedStationIds | leftover hard Ceres enter stays thin | persist across leftover _cleanup — MET
               checklist | 5/5 world state | 5/5 visible | five traces the player can see
               scenario | injected kill harness | headless paint + leftover top-up + leftover hard enter | headless
```

## Files

- `src/systems/traffic.js` — leftover routeDisrupted reader + leftover disruptedStationIds persist
- `test/pq-138-05-evidence-ledger.test.mjs` — leftover plant + leftover hard-enter stay-thin row
- `design/program/roadmap/receipts/PQ-138.05-REPORT.md` — this receipt

Already landed, not edited: `pirateRumor.js`, `marketNews.js`, `dockArrival.js`,
`stationFrames.js`, `orbital.css`.

Not edited: `missions.js`, `world.js`, `activityClassification.js`, `lawSecurity.js`,
`ships.js`, `weapons.js`, `modules.js`, `input.js`, `src/render/`, `styles/ui.css`,
`NOW.md`, `program-queue.json`, `package.json`, `stationApp.js`, `styles/kit.css`.

## Honesty gaps

- **The kill is injected.** Same leftover `entity:killed` shape as `combat.js:585`.
  No gun, physics, AI, or law system runs.
- **The witness is planted.** leftover `_markRouteDisrupted` still needs a second
  leftover hauler bound for `station_ceres`. The live 3-hull mix does not guarantee that.
- **The 8 s is decorative.** All leftover writes land inside the two emits.
- **leftover rumor is in-session.** leftover `pirateRumor` does not serialize
  `lastHeadline` / leftover `ui.pirateRumor`. A save/load can drop the leftover card
  until another leftover surface event.
- **leftover route copy is presentation.** leftover `routeDisrupted` is a boolean;
  the leftover sentence is leftover dockArrival copy over leftover hull count.
- **leftover hard enter still wipes leftover freighters.** leftover `_cleanup()`
  still clears leftover `traffic.freighters`. leftover `traffic.disruptedStationIds`
  is the leftover persist the leftover rematerialize now reads. leftover save/load
  of that leftover bag is leftover unproven here.
- No shipping-camera strip. The campaign forbade headed capture.

## Proof

```text
PQ-138.05 Ceres evidence ledger
| trace | ok | evidence id |
| wreck persists | true | aft_13waj40 |
| traffic thins | true | route:station_ceres:21 |
| price moves | true | station_ceres:cmdty_ore_iron:fl_be57371d |
| rumor appears | true | sector_ceres_belt:zone_ceres_refinery |
| structure patch | true | aft_patch:aft_13waj40 |
| rumor paints | true | pirateRumor:sector_ceres_belt:zone_ceres_refinery |
| patch paints | true | aft_patch:aft_13waj40 |
| route paints | true | route:station_ceres:21 |
| approach stays thin | true | route:station_ceres:21 |
```

`node --test test/pq-138-05-evidence-ledger.test.mjs` — 3 pass, 0 fail.

Leftover planted leftover `routeDisrupted` true on leftover a leftover Ceres approach: leftover rematerialize did not leftover-spawn leftover `ceres_refinery_hauler`. Leftover `_markRouteDisrupted` writes leftover `traffic.disruptedStationIds`; leftover hard Ceres enter keeps that leftover approach thin.

## Next

Integrator updates the queue. Do not mark the queue from this receipt.
