<!-- LIFETIME: RECEIPT -->
# PQ-138.05 — The evidence ledger at one site

```text
STATUS PARTIAL

               Leftover rumor and leftover patch are now
               player-visible. leftover routeDisrupted is
               painted on the Orbital berth from leftover
               state. leftover traffic.js still has no
               spawn/routing reader for that flag.

WHAT IS TRUE   One player-caused kill at Ceres still writes
               five leftover traces through production
               handlers. Two were already reachable (wreck,
               iron price). The leftover wiring this pass
               adds readers, not new traces:

               • leftover pirateRumor:headline now reaches
                 leftover marketNews. The leftover headline
                 is on the ticker log and on the Orbital
                 berth news line / PIRACY card.
               • leftover station.data.structurePatch.text
                 is painted on the leftover Orbital berth
                 as one sentence.
               • leftover routeDisrupted + leftover live
                 hull count are read into dockArrival and
                 painted on the leftover berth route line.
                 leftover traffic.js was not edited.

WHAT IS NOT    leftover traffic.js still does not change
               spawn, mix, or routing because a route was
               marked disrupted. The player can READ that
               the approach is disrupted; leftover traffic
               still tops up on the next sector enter.

               The kill is still injected. The witness
               hauler is still planted so leftover
               _markRouteDisrupted has someone to mark.
               AFTER_S is still decorative.

WHAT I CHANGED Leftover pirateRumor payload now carries
               leftover eventId. leftover marketNews
               subscribes to leftover pirateRumor:headline
               (not leftover news:headline — that would
               loop). leftover dockArrival reads leftover
               lastCard / lastHeadline, leftover
               structurePatch, and leftover routeDisrupted.
               leftover stationFrames + leftover orbital.css
               give the berth two leftover lines. leftover
               writeBerthArrival paints them from the
               header (stationApp's existing
               { newsEl, cardEl: eventEl } call is
               unchanged). leftover test now pins paint,
               not only world state.

WHAT YOU WILL FEEL   Kill a loaded hull on the Ceres
               approach. The wreck stays. Iron moves at the
               Refinery. Dock at Ceres: the leftover ticker
               and leftover berth news read the leftover
               rumor; the leftover berth card is PIRACY /
               Pirate rumor; one leftover sentence names
               the yard patch; one leftover sentence names
               the disrupted approach.

THE NUMBERS    bar | world state | reachable by player | leaf target
               wreck persists | marker + saved + live body | rendered wreck, salvageable | a wreck — MET
               price moves | buy/mid move on freight_loss | market screen + marketNews | a price moves — MET
               traffic thins | 1 of 2 planted hulls gone | leftover berth route line | thins on a DISRUPTED route — PARTIAL
               route disrupted | routeDisrupted = true | leftover berth paints it | a disrupted route — MET (visible); traffic.js unread
               rumor appears | lastHeadline + ui card | leftover ticker + berth card | a rumor — MET
               structure patch | aft_patch on station | leftover berth sentence | a patch shows — MET
               checklist | 5/5 world state | 4/5 visible + 1 partial | five traces the player can see
               scenario | injected kill harness | headless paint | headless
```

## Files

- `src/systems/pirateRumor.js` — leftover eventId on leftover headline/card
- `src/ui/marketNews.js` — leftover pirateRumor:headline → leftover ticker
- `src/ui/dockArrival.js` — leftover rumor / patch / route readers + paint
- `src/ui/views/stationFrames.js` — leftover `.sxb-berth__patch` / `.sxb-berth__route`
- `styles/orbital.css` — leftover Orbital berth line styles
- `test/pq-138-05-evidence-ledger.test.mjs` — pins leftover ticker + berth paint
- `design/program/roadmap/receipts/PQ-138.05-REPORT.md` — this receipt

Not edited: `traffic.js`, `missions.js`, `world.js`, `activityClassification.js`,
`lawSecurity.js`, `ships.js`, `weapons.js`, `modules.js`, `input.js`, `src/render/`,
`styles/ui.css`, `NOW.md`, `program-queue.json`, `package.json`, `stationApp.js`,
`styles/kit.css`.

## Honesty gaps

- **The kill is injected.** Same leftover `entity:killed` shape as `combat.js:585`.
  No gun, physics, AI, or law system runs.
- **The witness is planted.** leftover `_markRouteDisrupted` still needs a second
  leftover hauler bound for `station_ceres`. The live 3-hull mix does not guarantee that.
- **The 8 s is decorative.** All leftover writes land inside the two emits.
- **leftover traffic.js still has no behavior reader.** The leftover berth line is a
  leftover state reader, not a leftover spawn change.
- **leftover rumor is in-session.** leftover `pirateRumor` does not serialize
  `lastHeadline` / leftover `ui.pirateRumor`. A save/load can drop the leftover card
  until another leftover surface event.
- **leftover route copy is presentation.** leftover `routeDisrupted` is a boolean;
  the leftover sentence is leftover dockArrival copy over leftover hull count.
- No shipping-camera strip. The campaign forbade headed capture.

## Proof

```text
PQ-138.05 Ceres evidence ledger
| trace | ok | evidence id |
| wreck persists | true | aft_13waj40 |
| traffic thins | true | route:station_ceres:21 |
| price moves | true | station_ceres:cmdty_ore_iron:fl_be57371d |
| rumor appears | true | sector_ceres_belt:zone_ceres_refinery |
| structure patch | true | aft_patch:aft_13waj40 |
| rumor paints | true | pirateRumor:sector_ceres_belt:zone_ceres_refinery |
| patch paints | true | aft_patch:aft_13waj40 |
| route paints | true | route:station_ceres:21 |
```

`node --test test/pq-138-05-evidence-ledger.test.mjs` — 1 pass, 0 fail.

Leftover painted copy from that run:

- leftover ticker / berth news: `Route notice: avoid Ceres Refinery Approach; raider reports are piling up.`
- leftover berth card: `PIRACY` / `Pirate rumor` / same leftover headline
- leftover berth patch: `Yard crews patch Ceres Refinery after the Ceres Refinery Approach loss.`
- leftover berth route: `One hull remains on this disrupted approach.`

## What would close this leaf

1. A leftover `traffic.js` reader so a disrupted route changes leftover spawn/mix,
   not only leftover berth copy. This pass was forbidden from editing `traffic.js`.
2. leftover rumor persistence across save/load, if the player is meant to see the
   leftover headline after a reload.

## Next

Integrator updates the queue. Do not mark the queue from this receipt.
