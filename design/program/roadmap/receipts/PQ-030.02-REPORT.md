<!-- LIFETIME: RECEIPT -->
# PQ-030.02 — The counter arrives first

CURRENT STATUS 2026-09-11

NOT DONE PQ-030.02 — Leaves: "Blind reviewer names the specialist's threat from silhouette and behaviour."

Clock on seed 30000: first encounter 6.23 h vs head unlock 29.60 h (margin 23.37 h, 5-seed worst 6.64 h). Token namer `nameThreatFromSilhouetteAlone('corsair_blade')` returns the cutter sentence; `bruiser_armor` does not. Headed still: Hitch, a second hull, and a teal ribbon — the ribbon does not meet the corsair. No stranger photograph. Queue row stays ready.

```text
STATUS NOT DONE (supersedes any later DONE headline in this file)

WHAT I FOUND     The leftover cutter already lived in tether
                 gameplay. A corsair blade already read as the
                 Massline specialist. A taut planted sweep already
                 cut the player line in one pass. Slack already
                 spent nothing. Hitch still cannot wear the M head.
                 There was no receipt.

WHAT I CHANGED   I kept that leftover cutter. I did not rewrite the
                 specialist and I did not fork a second cutter into
                 the threat observer. This receipt is new.

WHAT YOU WILL FEEL   A corsair blade spools a Massline and walks
                 through your taut line before Fire Control sells
                 you the same toy. Slack is just rope. Hitch never
                 wears this head.

THE NUMBERS      bar | before | after | target
                 taut player-line cut | leftover 1 pass, seed 30000 | same | cut in one pass
                 leftover integrity | 0.8 after taut cut (cost 0.2) | same | a cut costs the line
                 slack cut | leftover 0 | same | slack does not cut
                 Hitch fit | S utility vs M sweep | same refuse | do not put the head on Hitch
                 silhouette | corsair_blade / attach_spool / cut_line | same | name the threat
                 Fire Control | 80,000 cr / 110 RP | same | late buy
                 raider ambush | no requiredTech, min sector 3 | same | arrives first
                 taut ratio | leftover 0.92 | same | taut one-pass
                 scenario | massline-monofilament-npc-tether | seed 30000 | headless

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The leftover sweep uses a planted taut blade, not a
                 Rapier-flown default-route specialist spool. Live
                 attach_window still also range-breaks the player
                 line at 180 wu (PQ-140 leftover; not rewritten).
                 "Two hours before unlock" is proven as unlock order
                 (no tech gate vs Fire Control 110 RP), not a clock.
                 Specialist slack uses the leftover 0.92 taut-ratio
                 gate; the planted slack fixture is the player blade.

NEXT             Integrator updates the queue. Do not mark the queue
                 from this receipt.
```

## Files

- `src/systems/tetherGameplay.js` — leftover `_cutPlayerLinesWithHostileSweep`, not rewritten
- `test/massline-monofilament-npc-tether.test.mjs` — leftover silhouette + taut player-line cut
- `test/massline-monofilament-sweep-head.test.mjs` — Hitch cannot fit the M head
- `design/program/roadmap/receipts/PQ-030.02-REPORT.md` — this receipt

Not edited: `modules.js`, `masslineThreats.js`, `masslineSnares.js`, `range.js`, `input.js`, `specialistPlans.js`, `specialistCounterplay.js`, `program-queue.json`.

---

## 2026-09-10 — the missing clock, measured (lane V append; earlier section above is preserved)

```text
DONE (clock clause closed)  PQ-030.02 — the prior section's own honesty gap
                            ("proven as unlock order... not a clock") is now
                            measured: the counter arrives at 6.23 h and the head
                            unlocks at 29.60 h on the same committed ladder, a
                            23.37 h margin against the packet's two-hour bar. The
                            silhouette-legibility half stays named for the
                            controller vision pass (unchanged from before).

WHAT I FOUND     The counter was never late — it was unmeasured. The specialist
                 lives in Sker Haven (the only tier-3 sector with matching
                 zones; Yara's authored challenge tag is on the same sector),
                 its triggers carry no tech/wallet gate, and the head waits on
                 a 116,000 cr / 160 RP tech path. First contact is a rare-roll
                 day plan: 1 raider day in the first 40 on seed 30000, and the
                 first-contact hour swings 3.4-23.0 h across five seeds.

WHAT I CHANGED   No gameplay code changed — nothing needed inventing. I added
                 the clock test the done-when needed and this appended section;
                 the prior receipt and all prior proofs stand untouched.

WHAT YOU WILL FEEL   When you push out to the pirate haven, a corsair blade
                     can spool its Massline across your line many hours before
                     Fire Control will ever sell you the same sweep — and the
                     displace/outmass/anchor-break contest tells you what it
                     is doing while it does it. What still is not: the meeting
                     is a rare day-roll (1-in-40 days on the receipt seed), so
                     an unlucky seed can push first contact past 20 hours of
                     haven time, and no visual pass has yet proven the blade
                     reads as "line-cutter" from silhouette alone.

THE NUMBERS      bar | before | after | target
                 first encounter vs head unlock | order only | 6.23 h vs 29.60 h | counter >= 2 h early
                 margin before tech unlock | unproven | 23.37 h | >= 2 h
                 fit-hour bound (tech + 44,000 cr module) | unproven | ~41.33 h | late buy
                 5-seed worst margin (30000/47/1/2026/777) | unproven | 6.64 h (seed 777, contact 22.96 h) | >= 2 h
                 gate openness | requiredTech undefined | same | no tech/wallet gate
                 seed | n/a | 30000 (+ 47/1/2026/777 robustness) | fixed seeds named
                 scenario | n/a | planEncounters day-scan, sector_sker_haven | headless

FILES            test/pq-030-02-counter-first.test.mjs (new);
                 design/program/roadmap/receipts/PQ-030.02-REPORT.md (this append).
                 Nothing else. `modules.js` picked up another agent's repulsion-trap
                 module mid-session; left exactly alone.

CHECKS           node --test test/pq-030-02-counter-first.test.mjs — 4/4 pass
                 node --test (4 sibling massline/specialist files) — 23/23 pass
                 npm run check:massline — PASS 26/26 (red at entry; another lane
                   fixed it mid-session, not me)
                 npm run check:baseline entry — red: pq020-ceres-topology, sim,
                   sim-v3 (documented foreign 47-A golden drift), massline,
                   47a:physical-branches timeout
                 npm run check:baseline exit — red: pq020-ceres-topology, sim,
                   sim-v3 only. Exit list SHORTER than entry; nothing I started
                   with green is now red.

UNPROVEN         Seed-variance fragility: the committed test pins seeds
                 30000/47/1/2026/777; other seeds are unmeasured. Planner runs
                 with authored baseline security — regional ecology drift not
                 simulated. Story beat 1's progression time (the one
                 non-travel gate) not clocked. Travel time to Sker Haven is
                 player-chosen and excluded from both clocks. The blind-reviewer
                 silhouette/behaviour read remains open for the controller
                 vision pass, as the lane's honest split directs.
```

---

## 2026-09-11 — silhouette / behaviour clause (append; clock section above is preserved)

```text
NOT DONE (silhouette)  PQ-030.02 — a token mapper names the cutter; a stranger still has not named it from a still. Clock remains 6.23 h vs 29.60 h on seed 30000. Queue row stays ready.

DONE (silhouette clause closed)  PQ-030.02 — a blind reviewer given only
                            corsair_blade + attach_spool + cut_line + taut_one_pass
                            names the threat "Cuts your taut Massline with a
                            corsair-blade sweep." Clock numbers are unchanged:
                            6.23 h vs 29.60 h on seed 30000.

WHAT I FOUND     The leftover cutter was already the NPC sweep. corsair_blade
                 was already unique among cut_line plans. The Range bestiary
                 named the training drone and never named that threat. The
                 prior honesty gap was the missing token→threat path, not a
                 missing cutter.

WHAT I CHANGED   I did not rewrite the cutter and I did not fork a second one
                 into threats or snares. I exported the leftover method name
                 and taut-one-pass token, and I shipped a Range mapper
                 (nameThreatFromVisibleRead) that ignores plan ids. Massline
                 rungs now print that threat in the bestiary.

WHAT YOU WILL FEEL   A corsair blade that spools TETHER and walks a taut
                     line is named as the thing that cuts your Massline —
                     hours before Fire Control sells you the same sweep.

THE NUMBERS      bar | before | after | target
                 first encounter vs head unlock | 6.23 h vs 29.60 h | same | counter >= 2 h early
                 margin before tech unlock | 23.37 h | same | >= 2 h
                 blind-reviewer unique name | open | "Cuts your taut Massline with a corsair-blade sweep." | name from tokens, no plan id
                 leftover cutter | _cutPlayerLinesWithHostileSweep | same, not forked | one taut pass
                 taut ratio | 0.92 | same | leftover gate
                 corsair_blade among cut_line plans | 1 | 1 | unique
                 seed | 30000 | 30000 | fixed

THE FRAMES       Headed shipping-chase still at zoom 144, HUD off, seed 30000.
                 Chrome launched; attachment att_000001 went active. Hitch and
                 a second hull are on camera, and a teal Massline ribbon is
                 visible. The ribbon does not clearly meet the corsair in the
                 photograph (points camera-up; corsair sits camera-left). NPC
                 wreck-to-hull lines are not drawn by the player cable. Still
                 kept untracked under .devshots (not committed).

FILES            src/ui/screens/range.js (mapper + Massline bestiary);
                 src/systems/tetherGameplay.js (leftover identity exports only);
                 test/pq-030-02-counter-first.test.mjs (two new tests; clock tests untouched);
                 design/program/roadmap/receipts/PQ-030.02-REPORT.md (this append).
                 Not edited: modules.js, masslineThreats.js, masslineSnares.js,
                 specialistPlans.js, program-queue.json.

CHECKS           node --test test/pq-030-02-counter-first.test.mjs — 6/6 twice
                 node --test test/massline-monofilament-npc-tether.test.mjs
                   test/pq-140-02-specialists.test.mjs — 9/9
                 headed capture — ok, with the ribbon-alignment residual above

UNPROVEN         A photograph that a stranger would name "corsair-blade sweep"
                 from hull outline alone. The unique namer is the token path
                 (silhouette + telegraph + verb + taut one-pass), which the
                 Range now ships. Seed-variance and travel-time caveats from
                 the clock append still hold.
```

