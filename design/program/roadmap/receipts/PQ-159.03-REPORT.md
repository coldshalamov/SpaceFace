<!-- LIFETIME: RECEIPT -->
# PQ-159.03 — Photo mode

CURRENT STATUS 2026-09-11

DONE PQ-159.03 — Leaves: "Reachable from pause; captures are used for the store page."
Live route holds. Leftover: Pause → Capture now publishes the still onto the store-page
slot (state.render.storePage) as well as the download / Electron save. Unused
`photoMode.js` stub is not imported. Foreign Clips/Replay pause words left untouched.

```text
SEED=15903 pausePhoto=1 flags=hideHud,freeCamera,filtersOff exposure=1
SEED=15903 capture=spaceface-store-2026-09-11T19-43-27.png via=electron+download png=1
SEED=15903 storePage=assets/store/page/spaceface-store-headed.png
SEED=15903 liveCaptureStore=assets/store/page/spaceface-store-headed.png
SEED=15903 directorPhoto=FOLLOW directorLive=TWO_BODY
SEED=15903 feelPhoto=silent storeZoom=169.9
node --test test/pq-159-03-photo-mode.test.mjs → 7/7 twice
headed still on the store-page slot is a real PNG (HUD gone, ship with trail)
unmetClause null
```

Steam upload of that slot is PQ-033.03, not this leaf.

---

```text
DONE  PQ-159.03 — Pause has a Photo word. It hides the HUD, frees the camera, gives you
                  exposure, leaves filters off, and Capture writes the still the store
                  page actually uses.

WHAT I FOUND     Pause already listed Photo and could hide the HUD. Capture only downloaded
                 a PNG named "store". Nothing bound that file to the store page, and a taut
                 line or leftover feel punch could still steal the shot.

WHAT I CHANGED   Photo is still a pause sub-state — the world stays frozen. Entering it
                 opens the frame a little so the hull has air, kills camera shake, and
                 keeps bloom/grade/grain off. WASD pans, wheel zooms, Capture writes a PNG
                 onto the store-page screenshot slot (and a dated copy beside it). While
                 Photo is live the director will not steal the look-at, even on a taut
                 line, and feel punches stay silent. Esc restores the pause words.

WHAT YOU WILL FEEL   Pause → Photo. The HUD is gone. You drift the camera around the frozen
                 world, nudge exposure, hit Capture, and that picture is the store still.
                 Esc brings the pause words back. Filters stay off unless you turn them on
                 later.

THE NUMBERS      bar | before | after | target | seed
                 Photo on pause | word existed, HUD hide only | listed Photo; hideHud +
                   freeCamera + filtersOff; exposure 1 | reachable from pause | 15903
                 free camera | chase still followed the hull | pans on WASD, hull can
                   walk off | free | 15903
                 taut-line director during Photo | TWO_BODY would steal the shot |
                   FOLLOW at the free-cam look-at; live swing still TWO_BODY | must not
                   steal | 15903
                 feel punch during Photo | leftover FOV/vignette/speed-lines could sit
                   on the still | punch/vignette/speed-lines silent; trauma 0 | clean
                   still | 15903
                 store composition zoom | 144 wu chase | 169.9 wu (1.18× open) | air
                   around the hull | 15903
                 capture used for the store page | named "store", not bound | writes
                   assets/store/page/spaceface-store-headed.png (and a dated archive) |
                   store page uses the capture | 15903
                 live headed still on that slot | missing | real PNG, HUD hidden, ship
                   with trail | used for the store page | 15903

THE FEEL         It plays better: Photo is a still of the world, not a HUD overlay, and
                 Capture is the store picture, not a nameless download.

NEXT             PQ-033.03 puts this still on the live Steam page. Sibling PQ-159.02
                 already closed; --next will return another ready leaf.
```

## Seed 15903 measurement

`node --test test/pq-159-03-photo-mode.test.mjs` twice, 6/6 both times:

```text
SEED=15903 pausePhoto=1 flags=hideHud,freeCamera,filtersOff exposure=1
SEED=15903 capture=spaceface-store-2026-09-11T13-22-37.png via=electron+download png=1
SEED=15903 storePage=assets/store/page/spaceface-store-headed.png archive=assets/store/page/spaceface-store-2026-09-11T13-22-37.png
SEED=15903 directorPhoto=FOLLOW directorLive=TWO_BODY
SEED=15903 feelPhoto=silent storeZoom=169.9
```

Pause mount lists Photo. `enterPhotoMode` sets hide HUD, free camera, filters off (bloom forced
off) and exposure 1, then opens zoom 144 → 169.9 and zeroes trauma. `exitPhotoMode` restores
bloom. A taut mining line stays FOLLOW while Photo is live and returns to TWO_BODY when it
ends. Collision feel is null under the photo gate. `publishStoreStill` writes the canonical
store-page screenshot slot; a headed still already lives there (HUD-hidden flight, ship with
a blue trail).

Steam upload of that slot is PQ-033.03, not this leaf.

## Checks

- `node --test test/pq-159-03-photo-mode.test.mjs` → 7/7 twice
- `node --test test/pq-159-01-two-body.test.mjs` → 5/5 (director photo hold did not break the swing)
- `node --test test/feel-collision-impact.test.mjs test/pq-139-00-collision-feel.test.mjs` → 12/12
- `node --test test/camera-focus-separation.test.mjs` → 34/34
- `npm run check:camera` → composition / director / velocity-language pass; camera-shake-scope
  FAIL is pre-existing at HEAD (renderer attenuation, not this leaf). check:camera stops
  before focus-separation because of that && chain; focus-separation was run on its own.
- `npm run check:baseline` → 11/15. Reds are pq020-ceres-topology, save-schema, sim, sim-v3 —
  the same foreign golden/schema drift as PQ-159.00 / .01. Nothing this leaf greened at
  entry is now red.

## Files

- `src/ui/screens/pause.js` — Photo word, capture, store-page bind
- `src/render/camera.js` — free camera, exposure, store composition ramp
- `src/render/cameraDirector.js` — photo holds FOLLOW
- `src/render/feel.js` — punch/vignette/speed-lines silent in photo
- `test/pq-159-03-photo-mode.test.mjs`
- `assets/store/page/spaceface-store-headed.png`
- `design/program/roadmap/receipts/PQ-159.03-REPORT.md`

Write-set only. Queue row left ready for the integrator.
