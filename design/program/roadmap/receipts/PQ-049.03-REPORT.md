<!-- LIFETIME: RECEIPT -->
# PQ-049.03 — Massline express-liner render package

**Verdict:** DONE. The exact new release payload is bound into the pilot input, the shipping
`massline-express-liner-v1` render package (all three LODs) is rebuilt through the sanctioned
builder, `src/render/renderPackageManifest.js` regenerated, and the no-fallback binding is
proven by the focused runtime test.

## What ran

- Pilot input `assets/ships/render-packages/pilots.json`: the three existing
  `massline-express-liner-v1{,-lod1,-lod2}` rows were re-bound to the new release artifacts —
  `releaseSha256`/`releaseBytes` updated to the SG-04 payloads written under `.02`
  (`75bbdc56…/5,277,236 B`, `f9d7b5c0…/5,835,040 B`, `ea77fb49…/2,184,064 B`).
- `node scripts/build-render-package-pilots.mjs
  --only=massline-express-liner-v1,massline-express-liner-v1-lod1,massline-express-liner-v1-lod2`
  — rebuilt `render.glb` + `render-package.json` in each output dir and regenerated the runtime
  manifest row; no other package churned.

## Evidence

- `node scripts/generate-render-package-pilots.mjs` — coverage already complete (267 pilots);
  `--check` mode (`check:render-package-coverage`) green.
- `npm run build:render-package-pilots` — built, no drift.
- `npm run check:render-package-pilots` — green: every pilot's compiled package and runtime
  binding verified.
- `npm run check:render-package-plan` — green: 267/267 packages produce valid instance plans
  (8,028 nodes, 7,905 records).
- `node --test test/massline-express-liner-runtime.test.mjs` — 3/3 green, including the pin
  "liner release is packaged and does not use the source-route fallback".
- `npm run check:baseline` — the only in-tree reds are `check:sim`/`check:sim:v3` hash drift
  from uncommitted foreign route/economy work; the identical golden passes in a clean HEAD
  worktree, so committed state is green.

## Boundaries

- Per the leaf, no `express` selection changed here — the live presentation seam shipped under
  `.04`. Natural-route Browser/Electron acceptance is `.05`'s scope.
