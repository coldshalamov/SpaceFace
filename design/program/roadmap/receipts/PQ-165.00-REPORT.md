<!-- LIFETIME: RECEIPT -->
# PQ-165.00 — Presets and frame cap

DONE PQ-165.00 — Low/Medium/High presets select three distinct adaptive-quality tiers, frame cap 30/60/120/off resolves its four values against the live cap with VSync honoured, all measured headlessly at seed 16500.

DONE WHEN (verbatim): Preset switch captured; frame cap measured.

WHAT I FOUND — `src/render/adaptiveQuality.js` exposed only GPU detection (`detectGpu`) and the dynamic-resolution controller; Settings had per-toggle video rows but no preset and no frame cap, and nothing resolved a cap against VSync.

WHAT I CHANGED — Added a shared Low/Medium/High preset table that maps to three adaptive-quality tiers (adaptive floor, render scale, particle density, render graph) and writes presentation keys only, plus a VSync-aware frame-cap resolver/controller. Settings now has a Quality preset row and a Frame cap row that persist and live-apply; the Language row is untouched.

WHAT YOU WILL FEEL — A single pick switches Low/Medium/High and immediately changes render scale and particle density without removing ships, stations or effects. A frame cap of 30/60/120/off is saved and resolves to the real cap: on a 60 Hz VSync display 120 becomes 60, and Off follows the refresh.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Player presets mapped to adaptive-quality tiers | 0 | 3 (low/medium/high, three distinct tiers) | 3 | 16500 |
| Preset render scale / particle density | none | low 0.75/'low', medium 1/'medium', high 1/'high' | distinct tiers | 16500 |
| Frame-cap values resolved against live cap @60 Hz VSync | unmeasured | 30→30, 60→60, 120→60, off→60 | 4 values | 16500 |
| Frame-cap values live cap with VSync off @60 Hz | unmeasured | 30→30, 60→60, 120→120, off→0 (uncapped) | request passes | 16500 |
| VSync flag read | n/a | 120 request clamped to 60 on, 120 off; off follows display refresh | read | 16500 |
| Content cut by any preset | n/a | none — credits, actor/entity count, gameplay and accessibility untouched | none | 16500 |

FILES

- `src/render/adaptiveQuality.js` — added `ADAPTIVE_QUALITY_TIERS`, `QUALITY_PRESETS`, `applyQualityPreset`, `qualityTierForPreset`, `FRAME_CAP_OPTIONS`, `resolveFrameCap`, `createFrameCap`, `normalizeFrameCap`, `frameCapLabel`.
- `src/ui/screens/settings.js` — added the Quality preset and Frame cap rows to Video; `publishFrameCap` publishes the effective cap to `state.render.frameCap`. Language row preserved.
- `test/pq-165-00-presets.test.mjs` — new, 5 tests.
- `design/program/roadmap/receipts/PQ-165.00-REPORT.md` — this receipt.

CHECKS

| Command | Round 1 | Round 2 |
|---|---|---|
| `node --test test/pq-165-00-presets.test.mjs` | PASS, exit 0, 5/5 | PASS, exit 0, 5/5 |
| `npm run check:baseline` | FAIL, 11/15 (entry: 11/15) | FAIL, 10/15; massline timed out under PQ-194.01 headed-capture contention |
| `npm run check:player-facing-labels` | PASS | — |
| `npm run check:ui-a11y` | PASS | — |
| `npm run check:ui-effects` | PASS | — |
| `node --check src/render/adaptiveQuality.js src/ui/screens/settings.js` | PASS | — |

Entry baseline reds (unchanged, not mine): `pq020-ceres-topology`, `save-schema`, `sim`, `sim-v3`. No new assertion red was introduced. `massline` was green at entry; on the loaded second run `check:47a:physical-branches` timed out at 150 s (25/26 children green, no assertion output) — the same contention fingerprint recorded by PQ-193.00 — and its isolated re-runs also exceeded the wall under two `chrome-headless-shell` captures burning ~20k CPU-s each. This leaf touches only render/UI files and cannot affect that sim check.

UNPROVEN — The frame cap's effective value is computed and published to `state.render.frameCap`, but the render-frame scheduler (`src/core/presentationRunner.js` / `src/render/renderer.js`, both outside this leaf's write set) does not yet consume it, so no delivered-fps change is claimed. `check:47a:physical-branches` was not reproduced green under current GPU contention. No headed capture was attempted (PQ-194.01 owns the GPU), so the preset and cap visuals are not photographed; this leaf's done-when is headless by design.

Candidate: branch `master`, shared dirty checkout. No commit, push, stash, restore, checkout, reset or clean was run. `src/audio/audioSystem.js`, PQ-194.01 files and `src/localization/` were not touched. `three.quarks` in `package.json` is preserved. No acceptance status is self-assigned.

## Controller close (01a08d61)

`node --test test/pq-165-00-presets.test.mjs` **5/5 twice** (seed 16500). Language row still in Settings. Frame-cap numbers hold; the renderer does not yet consume `state.render.frameCap` (named unproven).

