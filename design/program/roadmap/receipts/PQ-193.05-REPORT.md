# PQ-193.05 — REPORT (Wave 1, replacement implementer)

Status: PARTIAL DELIVERY — 3 of 5 shapes publish packaged bodies; mine + massSeed
explicitly uncommissioned (no body exists to wire; commission spec below).
Date: 2026-09-17
Owner paths only. No commits, no queue flip, no NOW.md edits, no staging.

## Done-when (numbers line added to the .05 Leaves row in PQ-193.md)

Seed 1, chase 144 WU, default Helios Prime start:
before → after
- Gates publishing `place_gate_jump_ring.glb`: 71/71 gate spawn shapes across all
  sectors resolve the authored ring (Helios Prime: 3/3 neighbor gates); `buildGate`
  hoop unreachable (every gate spawns `archetypeGlb`; verified in `world.js`
  `_spawnGates` default + all `SECTORS` authored gates). Before: hoop was already
  fallback-only per census; after: pinned by test, 0 hoop arrivals.
- Drone entities publishing `place_mining_drone.glb` (V3 hardware): 0% → 100%
  of fixtures (packaged-body pointer committed by prior implementer; this unit
  adds the contract map + test + census close-out).
- Generic wrecks publishing an aftermath body: tube spine → 100% of fixtures
  (hazardous → engine section, military → corvette turret, generic → deterministic
  pick inside the six-body set).
- Visible procedural primitives among gate/drone/wreck arrivals: 0.
- Disc mine + mass seed: UNCOMMISSIONED — no mine/seed/charge GLB exists anywhere
  under `assets/` (searched `*mine*.glb`, `*seed*.glb`, `*charge*.glb`,
  `*snare*.glb`). Procedural bodies stay until commission. A Node-composed
  primitive GLB would be a stop-condition violation ("procedural fallback hull"),
  so no placeholder was authored.

## Files changed (owned paths only)

- `design/program/roadmap/active/PQ-193.md` — one done-when numbers line on the
  .05 Leaves row. No other packet edits.
- `design/program/WORLD_VISUAL_CENSUS.md` — 193.05 entries on the six touched
  section-A rows (gate, drone, vector mine, wreck, disc mine, mass seed).
- `src/render/partsLibrary.js` — additive-only hunk after
  `OPENING_DOCK_HULK_DEBRIS_PLACE_FILE_BY_ID` (foreign Hawser/193.09/193.12 hunks
  preserved; region re-read before patching): `PQ_193_05_DRONE_PACKAGED_FILE`,
  `PQ_193_05_GATE_PACKAGED_FILE`, `PQ_193_05_WRECK_PACKAGED_FILES` (6),
  `PQ_193_05_UNCOMMISSIONED_ENTITY_TYPES` (`mine`, `massSeed`),
  `resolve19305CensusAEntityPackagedFile()`, `is19305PackagedWreckFile()`.
  No existing table touched (`PLACE_FILES` membership unchanged).
- GLBs + manifests: VERIFY-ONLY, no edits needed. All 8 commissioned files exist
  in `assets/ships/release/parts/places/` and all 8 ids are in
  `release_manifest.json`; drone + gate ids are in `parts_manifest.json`
  (aftermath files are incubator-sourced direct to release per the PQ-045
  pattern). Staged foreign PQ-022 manifest hunks untouched.
- `test/pq-193-05-census-a-bodies.test.mjs` (NEW) — fixed-fixture contract test,
  PASS (see Checks).
- This receipt (NEW).

## Checks (each tried once per shell discipline)

- `node test/pq-193-05-census-a-bodies.test.mjs` — PASS:
  `gates=71, drone=1/1 packaged, wrecks=5/5 packaged, mine+seed uncommissioned`.
  (First run: 2 drone failures — missing DOM canvas stub in plain node; fixed by
  adopting the repo's `globalThis.document` stub pattern. No production code
  changed for the test.)
- `npm run check:baseline` — FAIL (exit 1, tried once): 13/15 green; FAIL
  `sim-v3` (47a seed-47 telemetry mismatch) and FAIL `massline`; wall 151s over
  the 90s budget. INHERITED, not this unit: this delivery's only code hunk is
  additive render-only exports (verified: all `+` lines, no existing line
  touched), no baseline gate consumes the new exports, and the one gate that
  imports `partsLibrary.js` (`render-package-plan`) PASSED. The tree carries
  extensive foreign dirty sim/data work (Hawser, traffic, tech) as the likely
  source. No stash/worktree verification possible (forbidden); controller to
  confirm at land time.
- `npm run check:playable` — PASS 16/16 (BOOT, LAUNCH, PILOT, HULL, WORLD 68
  entities, CONTROLS, SAVE, CONTINUE, +LOAD cohort, CLEAN, SHADER, ASSETS,
  AUTHORED HULL). Only warnings are pre-existing GPU-brick/first-flight notes.

## Remaining work (exact)

1. **Commission `place_disc_mine.glb`** (Blender, Tier A/B record + preflight):
   same-slot body for `type:'mine'` (radius ~6; armed/safe warning lens motif
   must survive — currently `MineArmingLens` swaps `MineWarningLensSafe/Armed`).
   Then: add release file + manifest ids, extend the 193.05 map
   (`resolve19305CensusAEntityPackagedFile` mine branch), point the runtime
   selector at it (visualFactory `buildMine` `attachPackagedBody` — a
   visualFactory owner's one-spot change; NOT in this unit's write set), flip
   test §6 mine pins from null/procedural to packaged.
2. **Commission `place_mass_seed.glb`** (same path): player-deployed anchor,
   `type:'massSeed'`; status beacon + travel chevrons are the identity motifs.
   Same wiring + test-flip steps as the mine.
3. **Delete the dead `buildGate` hoop** in `src/render/visualFactory.js`
   (visualFactory owner): unreachable on the default route once gates always
   carry `archetypeGlb`; removal is cleanup, not behavior change. Do not remove
   before confirming no headless test builds gates through raw visualFactory.
4. Controller: pathspec-commit the owned files above; no queue flip by implementer.

## Notes for controller

- `src/render/visualFactory.js` already held committed PQ-193.05 packaged-body
  pointers for drone + wreck (prior implementer, in HEAD, clean tree) — this
  delivery builds on them without touching that file.
- Sibling rows observed, not touched: PQ-018.cathedral-chase-read,
  PQ-195.00 (per delegation), Hawser + PQ-193.09 + PQ-193.12 partsLibrary hunks.

## FIX RECORD (controller close 2026-09-17)

- Test hardening landed (spec gap 3): to+pos pins on every authored gate in §5
  (closes the world.js mirror gap — the `!g.to||!g.pos` skip is now provably
  dead), plus new §5b: a gate built through `installVisualOverrides`
  (`releaseMode` + `directAuthoredMount`, the renderer.js live-play config) draws
  ZERO meshes before admission. Test green.
- Hoop deletion (spec gap 2) DEFERRED to the visualFactory owner as domain cleanup,
  not a behavior gap: no headless test builds gates through raw visualFactory;
  live play mounts a zero-draw substrate (`renderer.js:3760-3763`) and the §5b pin
  above proves the hoop never draws on the player route. The reviewer's cited live
  exception (`shipPreview.js:281`) does not exist at that path, and pre-publish
  substrate-timing semantics need owner confirmation before the ~150-line removal —
  controller will not guess on another owner's file while that lane has live
  foreign work in it (orphaned-admission hunk at `visualFactory.js:2940`).
- Residual (exact): commission `place_disc_mine.glb` + `place_mass_seed.glb`
  (Blender Tier A/B + preflight; spec in Remaining work §1–2 above) + the deferred
  hoop deletion above.
