# PQ-194.02 — acceptance review: S2, the bench register

Reviewer: devin-wave1 (dispatch unit PQ-194.02, kind=acceptance_review)
Date: 2026-09-11 (tree state at review)
Verdict: **REJECTED — nothing to accept. The S2 session was never run; no return exists.**

## What was reviewed

Queue leaf `PQ-194.02` in `design/program/roadmap/program-queue.json` (lines 18143–18167):
"S2 — the bench register, accepted", kind `acceptance_review`, paths
`design/frontend/direction/approved/`, `assets/ui/kit/`, `assets/ui/tiles/`. Acceptance criteria
from the queue brief and `design/frontend/direction/sessions/S2-bench-register/PACKET.md`
("Definition of done", lines 118–125; "Return contract", lines 105–116; "The way this session
gets faked", lines 132–136).

## The decisive fact

`.devshots/ui-packets/` contains the **outgoing** packet `S2-bench-register.zip` (4.8 MB, built
for hand-off) but **no `S2-return/` exists anywhere** — not under `.devshots/ui-packets/`,
`.devshots/ui-packets/_inspect/` (which holds only `S1-design-system` and `S1-return`), or
`.devshots/` generally. S2 is a ChatGPT 6 Pro browser session: the zip must be handed off and
its return dropped back before this leaf has anything to review. That step never happened.

Every done-when criterion therefore fails on absence of evidence, not on quality:

| # | Criterion (queue brief + S2 packet §done) | Verdict | Evidence |
|---|---|---|---|
| 1 | Six bench frames — docking arrival (POSTER), Market, THE SHIP, chart (galaxy scope), Settings, Load — passing the two tests, with plates/layers/crops and kit-notes appended | **FAIL** | `design/frontend/direction/approved/frames/` holds exactly the six S1 frames (`frame-title-v1..v3`, `frame-crucible-door`, `frame-hud-resting`, `frame-hud-wanted`). No `frame-dock*`, `frame-market*`, `frame-ship*`, `frame-chart*`, `frame-settings*`, `frame-load*`. `kit-notes.md` has no BENCH section (grep for `S2`/`BENCH` returns only an S1 inset-spec line 115). |
| 2 | Twelve prototypes on the S1 kit (`station-dock/market/ledger/contracts/factions/industry/bar/shipworks`, `ship`, `chart`, `settings`, `load`), `file://`-openable, twelve table rows at 1280, in `_compare.html` | **FAIL** | `assets/ui/kit/screens/` = `_compare.html`, `crucible-door.html`, `fixtures.js`, `hud.html`, `title.html` — the three S1 heroes only. Zero of twelve bench screens. |
| 3 | Keyart tiles (5 arena + 5 mode + 4 difficulty, opaque + masked) and backdrop plates (3 + cold variants), manifest entries | **FAIL** | `assets/ui/tiles/` **does not exist**. No `plates/backdrops/` under the kit. `kit-manifest.json` is the S1 manifest (`"packet": "S1 (P10 + P11 + P12)"`). |
| 4 | Draft GLBs (keycap, gauge bezel, socket well, gantry module, floor plate, work light, cable run, bay-door frame) loading in `preview/index.html` | **FAIL** | Zero `*.glb` under `assets/ui/`; no `preview/` page exists. |
| 5 | Blender scripts complete and documented (`build_hangar.py`, `build_berth.py`, `build_arena_foundry.py`, `render_review.py`, headless `blender -b -P`) for hand-off to L-B / PQ-194.03 | **FAIL** | No `build_*.py` / `render_review*` under `assets/ui/`. `tools/bl_common.py`, `bl_kit.py`, `bl_scenes.py`, `bl_world.py` are S1's kit-production scripts, not the set builders. |
| 6 | Kit extended, not forked: `/* S2 */` additions to `fh.css` and `tokens.css`, appended kit-notes | **FAIL** | No `S2` banner in `assets/ui/kit/kit/fh.css` or `assets/ui/kit/tokens/tokens.css`. |
| 7 | Accepted files committed under `approved/` and `assets/ui/kit/`; return docs (`NOTES.md` separating "finished here" from "for the local Blender lane", `QA.md`, `manifest.json`) | **FAIL** | `git log` on the leaf's paths shows only `ea33e1900` "Land the Field Hardware interface kit" (S1) and the program-admission commit. No S2 commit, no S2 return docs. |

## What does exist (the S1 substrate, verified at face value)

The review opened the actual rasters, not just the inventories:

- `approved/frames/frame-title-v2.png` (the pick, "Field at dusk"): a lit hull seated on rock
  under a low warm sun, mast and worklight practicals, cold sky above the warm band, 188 px-class
  wordmark and the machined menu rail — material and light truth, not words on black.
- `approved/frames/frame-crucible-door.png`: forge-lit arena plate with mode/starter/arena tiles
  as imaged, keyed instruments.
- `approved/frames/frame-hud-resting.png`: EDGE register — instruments pinned to frame edges over
  a real ship render; centre third clear.
- `assets/ui/kit/assets/plates/_contact-sheet.png`: real rendered plates (bench paper,
  primary/raised/sunk, edge, legend strips, poster rail, row-selected) — produced assets, not CSS.
- Kit inventory is coherent: `kit-manifest.json` parses, declares `"packet": "S1"`; ~157
  manifested assets at @1x/@2x, 86 icons × 24/32/48, marks, tokens, motion/sound specs, tools.

S1 (PQ-194.00, receipt `design/frontend/direction/receipts/S1-REPORT.md`) and the UI stage
(PQ-194.01, receipt `P20-REPORT.md`, controller accept-with-notes 2026-09-11) stand as accepted.
This review found no defect in them; it simply has no S2 work to judge.

## Checks

`npm run check:baseline` (the leaf's named check): **FAIL — `sim` red only in the observed tail**,
the 47-A authoritative-hash drift (`371585f2…` vs `ecd131b4…`) that `_COMMON.md` and both prior
receipts name as pre-existing. This unit mutated nothing; no red is attributable to the review.
Baseline is moot for acceptance regardless: there is no return to validate.

## Guard against the named fakes (S2 packet §"The way this session gets faked")

Not yet testable — all four fakes (full-width-table market, plate-with-pins chart,
one-silhouette prototypes, named-box GLBs) presuppose a return. Flagged for the next reviewer.

## Required next action (blocker)

S2 is an external session, not local work: `node scripts/build-ui-packet.mjs S2` has already
produced `.devshots/ui-packets/S2-bench-register.zip`; the owner/controller must hand it to
ChatGPT 6 Pro and drop the returned zip into `.devshots/ui-packets/returns/` (per
`FIELD_HARDWARE_PROGRAM.md` §9). Until an `S2-return` exists, PQ-194.02 cannot pass and
PQ-194.03 (L-B, which consumes the Blender scripts) remains correctly blocked on it.
