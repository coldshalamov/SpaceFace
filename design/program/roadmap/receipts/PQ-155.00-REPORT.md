<!-- LIFETIME: RECEIPT -->
# PQ-155.00 — The verb ladder

```text
STATUS DONE

WHAT I FOUND     Thirty-two tech nodes and no hour → verb → cost → gate
                 table. Start 5,000 cr and 0 RP. Cheapest node is Combat
                 Basics at 6,000 cr and 10 RP. The 15-minute first-upgrade
                 gate is already missed: the missing 1,000 cr is easy, the
                 10 RP from recon_scan is 60–90 minutes. Advanced Navigation
                 was already folded into Long-Range Survey.

WHAT I CHANGED   Committed the table next to tech.js. Hours come from the
                 live costs plus two named rates (8 RP/h from that recon
                 midpoint; 3,750 cr/h from the hunter floor). No cost was
                 moved. No node was folded. Three strict leftovers and six
                 broad extras each have a sentence.

WHAT YOU WILL FEEL   Nothing on the route yet. The tree still costs what it
                 cost this morning. The first toy is still more than an hour
                 away. This leaf only writes the honest ladder so .01 can
                 tune against it instead of by feel.

THE NUMBERS      bar | before | after | target
                 table | none | src/data/techVerbLadder.js | hour → verb → cost → gate
                 nodes | 32 | 32 | 32
                 strict verb / stat-only | 29 / 3 | 29 / 3 | stat-only ≤ 12
                 broad verb / stat-only | 23 / 9 | 23 / 9 | stat-only ≤ 12
                 first upgrade | Combat Basics 6,000 cr + 10 RP | same | —
                 first-upgrade hour | 60–90 min (1.25 h midpoint) | shown, not hidden | ≤ 15 min
                 first-upgrade bottleneck | RP (short 10) | RP | credits were the old story
                 start | 5,000 cr / 0 RP | same | —
                 folded this leaf | 0 | 0 | fold only if justified
                 prior fold | tech_advanced_navigation → survey | same | already done
                 scenario | test/pq-155-00-verb-ladder.test.mjs | 6/6 pass | headless data
                 check:baseline | sim + sim-v3 hash red on the dirty tree | same two reds | foreign; this leaf is not on the 47a path

THE FRAMES       Data table. No headed GPU strip. No camera claim.

NEXT             PQ-155.01 — the ten-hour sim. Three archetypes print net
                 worth, verbs, and sinks per hour. check:economy:curve gates
                 the ladder. Do not mark the queue from this receipt.
```

## Files

- `src/data/tech.js` — pointer only; costs and unlocks untouched
- `src/data/techVerbLadder.js` — committed table and leftover justifications
- `test/pq-155-00-verb-ladder.test.mjs`

Not edited: `economy.js`, `modules.js`, `ships.js`, `program-queue.json`, `NOW.md`.
