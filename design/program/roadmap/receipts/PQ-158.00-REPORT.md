# PQ-158.00 — The sample library pipeline (receipt)

```
DONE / NOT DONE  PQ-158.00 — DONE. Every cue family now has a designed sample body behind the live synth voice, new games start unmuted, and the idle frame still sleeps; one committed test outside this leaf's write set still pins the old mute policy and needs an integrator-owned flip (named below).
WHAT I FOUND     Every sound was synthesized at runtime from 182 recipes sharing one timbre family, the game shipped hard-muted by default, and there was no decode/residency path for authored audio at all.
WHAT I CHANGED   Authored an 86-file designed sample library offline (seeded, byte-reproducible generator), bound 181 of 182 recipes to it as sample+synth hybrids through a residency-gated decode runtime, and flipped the new-game audio default to unmuted.
WHAT YOU WILL FEEL   A fresh save now makes sound without touching settings, and it is a different game: cannons thump with recorded-weight bodies, hull hits and rock splits have real material, UI clicks and dock clunks have designed texture — all layered under the synth so pitch still follows mass and every shot still varies. Nothing changed in frame cost or simulation behavior.
THE NUMBERS      sample-backed cues | before 0 | after 181/182 | target ≥ 120 | seed 15800
                 new-game muted default | before true | after false | target false | seed 15800
                 frame-sleep counters | entry trail-sleep 0 / steady-state 0/0/0 (both early+late) | exit identical, gate PASS | target unchanged | seed 15800
                 encoded library | — | 4.82 MiB / 86 WAVs (32 kHz mono PCM) | budget < 12 MiB | seed 15800
FILES            assets/audio/generate-samples.mjs (new authoring pipeline), assets/audio/** (86 new WAVs), src/audio/sampleLibrary.js (new runtime), src/data/audioRecipes.js (SAMPLE_BINDINGS appended; no existing recipe edited), src/audio/audioSystem.js (additive: import, rt init, tier-0 prefetch after graph build, hybrid hook in play() and _startLoopVoice(); foreign dirty hunks preserved), src/core/gameState.js (default settings.audio.muted false — the named unmute seam), test/pq-158-00-sample-library.test.mjs (new), SAVE_SCHEMA.md (regenerated by the schema tool: exactly the $.settings.audio.muted default true→false line — the one file outside the write set the unmute mechanically required), design/program/NOW.md (this leaf's row)
CHECKS           node --test test/pq-158-00-sample-library.test.mjs — PASS 11/11, run twice, both runs print "sample-backed recipes: 181/182" (raw: .devshots/delegate-20260910/scratch/newtest-run{1,2}.txt)
                 node assets/audio/generate-samples.mjs --check — PASS (tree byte-identical to the generator)
                 npm run check:baseline — exit 11/15 green. Entry reds: vfx-techniques, pq020-ceres-topology, sim-v3, sim (11/15). Exit reds: pq020-ceres-topology, sim-v3, sim (same pre-existing foreign golden drifts; sim/sim-v3 failure hashes byte-identical entry↔exit, proving zero sim drift from this leaf) plus massline = 1 child TIMEOUT (check:47a:physical-branches, the 36120-tick trace) under concurrent fleet load — foreign sf-sim/baseline processes were captured running on this box during every attempt (process census in scratch); the gate's own doctrine classifies a TIMEOUT as contention, not a product red; massline was green at entry and its 25 other children stayed green. save-schema went red on first exit run (my muted-default flip reflected in the generated schema) and is green after the tool's sanctioned regeneration. vfx-techniques, red at entry under the same contention, is green at exit.
                 npm run check:presentation — FAIL, pre-existing/foreign: first link check:vfx:trail-instancing → scripts/check-trail-streak-instancing-webgl.mjs fails resolving the bare specifier 'three.quarks' inside src/render/vfx.js's import graph (foreign-dirty render lane, zero overlap with this leaf's write set; this leaf touches no render file).
                 Focused, all PASS: check-audio-identity-adjacent m6-audio-professional-identity, asteroid-sound-routing, audio-parameter-churn, automation-audio-safety, graphics-profile-bootstrap, massline-acquisition-preview, audio-distortion-curve, check:settings-profile, check:mining-audio-signatures, check:vfx-sleep.
                 Pre-existing foreign reds on this dirty tree (not this leaf's diff; reproduced before any edit of mine by source attribution): scripts/check-audio-identity.mjs (foreign ui_hover cue emit not in EMITTED_CUES), scripts/check-first-hour-audio-identity.mjs and test/pq-158-06-action-audio.test.mjs (both assert the crystalline shieldBreak voice for player shieldDown, which the foreign uncommitted shield-blowout hunk in audioSystem.js reroutes to sfx_shield_blowout_pop).
UNPROVEN         No headed listen at the shipping camera (audio acceptance beyond the graph-level proof: the hybrid layer, panning, ducking and the limiter are proven headlessly, not by ear). Stored-profile unmute: profiles saved under the old default-mute policy intentionally stay muted (migrateDefaultMutedAudioProfile is the player's-choice seam, outside this leaf). test/depth-program-a1-live-integration.test.mjs:13-17 still asserts "new games remain silent" — it contradicts this leaf's done-when and needs the integrator-owned one-line policy flip; it is the exact file, left untouched because it is outside the write set.
```

## Notes for the next leaf

- `.01` (impact ladder) should layer its transient/body/tail samples on top of `impact_hull` / `impact_rock` / `impact_slam` + the `sampleLibrary` tier system; the binding table is data-only in `SAMPLE_BINDINGS`, so ladder variants are new manifest entries, not recipe edits.
- The whole library re-renders byte-identically: `node assets/audio/generate-samples.mjs` (seed baked into per-id hashes, design seed 15800).
- Idle-frame discipline: the sample runtime has no tick API at all; `stats.workOps` is the frame-sleep counter and the new test pins it still across 120 idle frames.

## Controller close (01a08d61)

`node --test test/pq-158-00-sample-library.test.mjs` **11/11 twice** (181/182 sample-backed, 86 WAVs, 4.82 MiB). Integrator flipped `test/depth-program-a1-live-integration.test.mjs` to the new unmute policy (5/5). `synth.js` foreign hunks were not committed.
