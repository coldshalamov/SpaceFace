<!-- LIFETIME: RECEIPT -->
# PQ-204.00 — Always-on grid, combat columns, dirty journal, near budget

```text
STATUS DONE

WHAT I FOUND     Quiet flight and a crowd already share one spatial grid,
                 packed combat columns, a dirty-pose journal, and a small
                 budget for who thinks this tick.

WHAT I CHANGED   Nothing in the sim this pass. The focused proof was re-run
                 and it still holds. This note closes the queue leaf.

WHAT YOU WILL FEEL   Parked ships and a fight do not make the game rescan
                 every body every tick. The player and anyone shooting still
                 wake.

THE NUMBERS      bar | before | after | target
                 spatial hash at sparse density | on | on | on
                 near budget wakes player and combatants | yes | yes | yes
                 dirty journal clears each tick | yes | yes | yes
                 seed | — | the pq-204 fixture | fixed
                 proof | node --test test/pq-204-advanced-perf.test.mjs | 40/40 with the sibling leaves | green

NEXT             Sleep and the compile batch are the next two leaves.
```
