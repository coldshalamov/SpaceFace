<!-- LIFETIME: RECEIPT -->
# PQ-177.00 — Ticker and event cards

```text
STATUS Leftover line and leftover card both paint.
                 (Card half was NOT DONE after the 2026-09-09
                 honesty review. This pass draws the stored card.)

WHAT I FOUND     Leftover market news already lived in
                 marketNews.js and newsTemplates.js.
                 Shortage, boom, blockade, piracy, and
                 freight lines already had templates.
                 The ticker line was live. lastCard already
                 held badge/title/body/eventId. Zero
                 renderers. dockArrival used the card only
                 as a headline string. stationApp wrote
                 only .news.

WHAT I CHANGED   I kept that leftover writer and the ticker
                 line. buildDockArrival now carries the
                 leftover card. stationApp writes it onto
                 the Orbital berth article through
                 writeBerthArrival. Card CSS is in
                 orbital.css. No new kinds. Queue unmarked.

WHAT YOU WILL FEEL   The economy rolls its first event
                 about a minute and a half into a new game,
                 at one of the warmed markets, and the roll
                 is one of four kinds — shortage, boom,
                 blockade, piracy. In flight you get one
                 news line on the alert floor; on seed
                 17700 it is "Ceres Refinery locked down;
                 The Quiet quote canister rates." Dock at
                 that station while the event lasts and the
                 line still sits under the station name.
                 Under it you now see a card. For a
                 blockade that card reads BLOCKADE, "Iron
                 Ore Blockade", and the sentence that iron
                 ore is frozen there. Every kind has a
                 card; the berth is not blockade-only.

THE NUMBERS      bar | before | after | target
                 blockade line | leftover templates, no print | Helios Station locked down; The Quiet quote canister rates | ticker line from a sim event
                 event cite | leftover log had kind + station only | economy:eventStarted evt_1 | every line traces
                 spontaneous blockade | never proven | fires at 91 sim s, seed 17700, station_ceres, no injectEvent | the kind is live, not test-only
                 dock card | leftover lastCard, no renderer | BLOCKADE Iron Ore Blockade + evt_1 written on .sxb-event | card the player can SEE
                 card renderers | 0 | 1 (Orbital berth via writeBerthArrival) | at least 1
                 untraced line | leftover would keep it | refused | no invented news
                 station name | leftover stripped "helios" | Helios Station | catalog name
                 card sources proved | stored lastCard only | stored lastCard AND the cardsForStation fallback | both branches the berth takes
                 live-route pin | file mentions the writer | .sxb-event -> cardEl: eventEl inside renderStatus | a comment cannot pass it
                 scenario | leftover literal-publish test | pq-177-00 seed 17700, 4 tests | headless print

THE FRAMES       Headless leftover proof. No headed GPU strip.
                 The berth writer paints badge/title/body
                 and leftover eventId. That is the capture
                 this pass can give.

HONESTY GAPS     War tension and hunter sightings are still
                 leaf prose, not leftover kinds. Regime
                 templates stay unwired. The flight ticker
                 host is still absent; the line still ships
                 on the alert floor. Headed GPU capture was
                 not taken; the live paint is pinned from
                 source, not from a rendered berth. A
                 stored card outlives its event inside one
                 dock session. Do not mark the queue.

NEXT             Later leaves own charts, glyphs, twists,
                 and the black market. Do not mark the
                 queue from this receipt.
```

## Files

- `src/ui/dockArrival.js` — leftover `eventCard` on the arrival view; `writeBerthArrival` paints badge/title/body/eventId
- `src/ui/station/stationApp.js` — default Orbital berth calls that writer on arrival refresh
- `src/ui/views/stationFrames.js` — `.sxb-event` article on the berth title block
- `styles/orbital.css` — Orbital card look (not `styles/ui.css`, not kit.css)
- `src/ui/marketNews.js` — already live leftover card builder + ticker citation (not rewritten this pass)
- `test/pq-177-00-ticker.test.mjs` — prints the blockade line, the spontaneous roll, the painted berth card, and the `cardsForStation` fallback card
- `design/program/roadmap/receipts/PQ-177.00-REPORT.md` — this receipt

Already live, not rewritten: `src/data/newsTemplates.js` (blockade / shortage / boom / piracy / freight templates + dock cards), `test/market-news-literal-publish.test.mjs` (literal publish + freight provenance).

Not edited: `src/ui/hud.js`, `src/ui/uiRoot.js`, `src/systems/economy.js`, `src/systems/input.js`, `src/render/`, `assets/`, VFX docs, `index.html`, `build_map.md`, `program-queue.json`, `design/program/NOW.md`.

## Honesty review 2026-09-09 — what is live and what is not

This section replaces the original "the blockade is injectEvent" gap with the traced answer, and
adds two gaps the first pass did not name. Protocol: `.devshots/packets/HONESTY-REVIEW.md`
(trace the test's inputs; STATUS must match the live route).

### The blockade is live, not a constructed value

The first pass flagged `economy.injectEvent` as an honesty gap. Traced, it is not the PQ-140.01
pattern (a number the live route cannot produce). `injectEvent` is the production constructor:

- `economy.js:2128` — `rollSpontaneousEvent` picks from `['shortage','boom','blockade','piracy']`.
- `economy.js:2131` — that roll calls the same `this.injectEvent({type, stationId, commodityId, duration})`.
- `economy.js:2082` — `injectEvent` emits `economy:eventStarted { eventId, type, stationId, commodityId, duration }`, the exact payload `marketNews.js:323` subscribes to.
- `economy.js:832-834` — the roll fires inside `econTick` every `EVENT_INTERVAL_S = 90` sim seconds.
- `economy` is in the production `UPDATE_ORDER` (`docs/SYSTEM_REGISTRY.md:15`), so that clock runs on the default route.
- `economy.js:2256-2259` — `newGame()` warms every home-sector market, so the roll has targets from the first frame.
- The test's `duration: 120` sits inside the spontaneous 90–210 s band; the station and commodity ids are real catalog entries.

Test 2 in `test/pq-177-00-ticker.test.mjs` now proves it end to end with **no `injectEvent` call**:
it runs `newGame()`, drives `sim.step(1/60)`, and waits. On seed 17700 the very first scheduled
roll is a blockade at Ceres Refinery at **91 sim seconds**, and it lands on the ticker log citing
`economy:eventStarted`, with the cited id present on `state.economy.econEvents` as a live
`type: 'blockade'` event.

### The dock card had no renderer — this is why STATUS moved

Superseded by **Card renderer 2026-09-09** below. The review text is kept as the trace.

The done-when is "capture of a blockade line and its card." The card does not exist on screen:

- `marketNews.js:332` emits `news:dockCards`. It has **zero subscribers** — `docs/EVENT_ROUTING.md:845` records 1 emitter, no consumers, and a repo-wide search finds no listener.
- `state.ui.marketNews.lastCard` has exactly one live reader: `dockArrival.js:31-32`, and it uses the card only as a **fallback string** (`card.headline`), never `badge` / `title` / `body`.
- `dockArrival.js:29` prefers a station-matching log line first, so in practice even that fallback rarely fires.
- `stationApp.js:1000` renders `buildDockArrival(...).news` — one string under the station name.

So at the berth the player gets a headline, not a card. `BLOCKADE`, `Iron Ore Blockade` and "iron
ore is frozen at Helios Station" are computed, stored, asserted by the test, and drawn by nothing.
Test 3 records that surface without forbidding a future renderer.

### There is no ticker strip either — the line ships on the alert floor

`marketNews.js:358` mounts its DOM strip only into `document.getElementById('news-ticker')`. No
file in the repo creates that host. `scripts/check-ui-identity.mjs:74-83` pins that lookup and
**forbids** falling back to `#hud` or `document.body`, so the absence is deliberate policy, not an
oversight. `renderTicker` is therefore a no-op on the live route.

This is not a defect against the leaf, which asks for the line "at the arbiter's chatter tier":
`voiceArbiter.js:303-304` installs `ctx.helpers.voice`, `uiRoot.js:394` hands that same ctx to
`createMarketNews`, and `commitHeadline` says the line on the `news` channel with a `toast` floor
underneath. The line reaches the player. It is not a rotating ticker, and the original receipt's
"the ticker names the station and the freeze" overstated the surface. The original gap line said
the host "was not opened or captured," which reads as a missed capture; the truth is that no
renderer exists to capture.

### "Traces to a sim event" is enforced, with one vocabulary caveat

`commitHeadline` (`marketNews.js:230-231`) drops any record `tickerEventRef` cannot cite. That is
live production behaviour, not a test-only rule.

`tickerEventRef` accepts `eventId` / `id` / `intentId` / `sourceRef` / `receiptId`. Economy and
freight lines cite real runtime ids; authored `news:publish` lines cite an authored constant such
as `traffic:helios-civic-liner` — honest provenance, but a token, not a sim event id.

Every live emitter carries one, so the refusal rule drops nothing the player used to see. Checked
exhaustively: **8 emit sites in 4 files** — `traffic.js:2853` and `:8171` (`sourceRef`),
`titles.js:590` and `:701` (`receiptId`, on both the succession and the vacant branch),
`world.js:537` (`id` + `source`), `uniqueWrecks.js:371`, `:1490` and the `_surfaceCanonicalRumor`
relay reached at `:772` (`sourceRef` + `receiptId`). A repo-wide search finds no other emitter on
the live route — the remaining hits are the generated `build/web` bundle, the packaged `dist`
asar, and a CI log, all copies of the same `src` sites. In particular no scenario file emits
`news:publish`, so the dirty `329-curtain-convoy.js` is unaffected.

### Unchanged gaps from the first pass

- War tension and hunter sightings are named in the leaf prose. Leftover kinds are shortage, boom, blockade, piracy, freight, and authored `news:publish`. This leaf did not invent those missing kinds.
- Regime-flip templates exist in `newsTemplates.js`; `createMarketNews` subscribes to no regime bus event.
- The seeded variant for this event is the one blockade template with no commodity token, so the printed line names the station and the lockdown but not the frozen good. The commodity is named in the card copy, which the Orbital berth now draws.
- Headless only. The job peeled headed capture.
- Exit `npm run check:baseline` was red at the original landing on foreign seams (`pq020-ceres-topology` additive dressing, 47-A sim hash drift, massline mass-coupling), all live under other NOW rows. This review did not touch them.

## Card renderer 2026-09-09

The leftover card is no longer state-only. `buildDockArrival` exposes leftover `eventCard`
(badge / title / body / eventId) from stored `lastCard`, else leftover `cardsForStation`.
`stationApp.renderStatus` writes that view through `writeBerthArrival` onto `.sxb-event` in the
default Orbital berth frame. The ticker line stays on `.sxb-berth__news`. A quiet station still
invents no card. War tension and hunter kinds were not added.

## Honesty review 2026-09-09 (second pass) — the card half, traced to the live route

Protocol: `.devshots/packets/HONESTY-REVIEW.md`. The first pass moved STATUS because the card had
no renderer. This pass re-traced the renderer that was added and asks the only question that
matters: **can a player see BLOCKADE + title + body on the live berth?** Traced end to end, yes.
STATUS stands; it is not flipped.

### The chain, every link read in live code

1. `economy.js:832-834` — `econTick` rolls a spontaneous event every `EVENT_INTERVAL_S = 90` sim s.
   `economy.js:2116-2131` picks a warmed station, a commodity it really trades, and one of
   `['shortage','boom','blockade','piracy']`, then calls the same `injectEvent` the tests call.
2. `economy.js:2082` emits `economy:eventStarted`; `economy` is in the production `UPDATE_ORDER`.
3. `uiRoot.js:394` — `replaceMarketNewsOwner(this, ctx)` constructs `createMarketNews` on the
   default route. That is the only construction site; the log and the card both live on its model.
4. `uiRoot.js:916-918` — the live `dock:docked` handler writes `state.ui.dockedStationId`.
   `marketNews.js:328-332` — its own `dock:docked` handler writes `model.lastCard` for that same id.
5. `uiRoot.js:935` — the same handler pushes the `station` screen; `stationScreen.js:15` mounts
   `createStationApp`.
6. `stationApp.js:252` injects `stationFrameHtml()`, which carries the `.sxb-event` article;
   `:259` caches it as `eventEl`. Nothing rebuilds that header — `titleBlock` is only ever
   class-toggled (`:614`), so the cached node stays attached.
7. `stationApp.js:1003-1008` — `renderStatus()` builds `buildDockArrival(s, { id: stationId() })`
   and hands `{ newsEl, cardEl: eventEl }` to `writeBerthArrival`. `renderStatus` runs at mount
   (`:1120`), on `onShow` (`:1149`), and on the periodic UI refresh (`:1083`).
8. `dockArrival.js:70-96` unhides the article and writes badge / title / body / `data-event-id`.
9. `index.html:21` loads `styles/orbital.css` globally (the station app's own `ensureStylesheet`
   loads only `station.css`, so this mattered). `orbital.css:173-188` styles `.sxb-event` under
   `#screens`, and sets no `display`, so the `hidden` attribute still hides an empty card.

### The card is leftover, not constructed

`leftoverEventCard` (`dockArrival.js:50-59`) reads stored `lastCard` when it belongs to this berth,
else `cardsForStation` (`marketNews.js:179-194`), which derives read-only from
`state.economy.econEvents`. No field is authored at the berth. The tests feed that path only
through `economy.injectEvent` and the production `dock:docked` bus event — never a literal card.

### What this pass changed in the test, and why

- **The receipt claimed a path nothing exercised.** "stored `lastCard`, else leftover
  `cardsForStation`" — tests 1 and 3 only drove the stored branch. New test 4 drives the else: the
  stored card belongs to a different berth and this berth's own event is live in `econEvents` with
  no `dock:docked` of its own. That is the ordinary state whenever an event starts while the player
  is already sitting in the berth. It also pins that the other berth's card does not leak across.
- **The live-route assertion was a word match.** Test 3 asserted only that `stationApp.js` contains
  the strings `writeBerthArrival` and `eventCard` — a comment would have passed it. It now pins
  `const eventEl = app.querySelector('.sxb-event')`, the `buildDockArrival(s, { id: stationId()`
  call, `writeBerthArrival({ newsEl, cardEl: eventEl }, arrival,` and the fact that the call sits
  inside `renderStatus()`. Checked negatively: a comment-only decoy fails all four, and rebinding
  `cardEl` to anything but `eventEl` fails the third.

### Two gaps this pass adds

- **A stored card outlives its event inside one dock session.** `leftoverEventCard` prefers stored
  `lastCard`, and `marketNews` only refreshes it on `dock:docked` (`marketNews.js:328-332`). A
  120 s blockade that expires while the player stays docked leaves BLOCKADE on the berth until the
  next dock, when the handler recomputes and clears it. The ticker line behaves the same way. Not
  fixed here: `dockArrival.js` and `stationApp.js` were outside this review's allowed edits.
- **The live paint is pinned from source, not captured from a rendered berth.** The writer, the
  frame markup and the CSS were each read in live code, but no headless DOM or headed strip shows
  the article painted in a real document. The repo has no jsdom, and mounting `createStationApp`
  drags the berth's 3D preview in. A headed capture remains the honest closer.

### Corrected in the prose above

`WHAT YOU WILL FEEL` stated the seed-17700 outcome as the general one ("a blockade hits a station
about a minute and a half into a new game"). The mechanism is: the first roll lands at ~90 sim s,
at a random warmed market, and is one of four kinds — read off the `types` array at
`economy.js:2128`, not from a playtest. Every kind has a card template
(`newsTemplates.js:104-119`), so the berth card is not blockade-only.

## Proof

```text
PQ-177.00 blockade line: Helios Station locked down; The Quiet quote canister rates
PQ-177.00 event: economy:eventStarted evt_1
PQ-177.00 dock card: BLOCKADE Iron Ore Blockade — iron ore is frozen at Helios Station. Spreads are punishing until it lifts.
PQ-177.00 spontaneous blockade after 91 sim s (no injectEvent): Ceres Refinery locked down; The Quiet quote canister rates
PQ-177.00 spontaneous event: economy:eventStarted evt_1 @ station_ceres
PQ-177.00 berth line (live, rendered): Helios Station locked down; The Quiet quote canister rates
PQ-177.00 berth card (live, rendered): BLOCKADE | Iron Ore Blockade | iron ore is frozen at Helios Station. Spreads are punishing until it lifts.
PQ-177.00 berth card event: evt_1
PQ-177.00 fallback card (no stored card for this berth): BLOCKADE | Iron Ore Blockade | evt_2
```

`node --test test/pq-177-00-ticker.test.mjs` — 4 pass, 0 fail.
`node --test test/pq-177-00-ticker.test.mjs test/market-news-literal-publish.test.mjs test/dock-arrival-presenter.test.mjs test/orbital-presentation.test.mjs` — 38 pass, 0 fail.
