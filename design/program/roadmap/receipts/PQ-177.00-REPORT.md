<!-- LIFETIME: RECEIPT -->
# PQ-177.00 — Ticker and event cards

```text
STATUS DONE

WHAT I FOUND     Leftover market news already lived in
                 marketNews.js and newsTemplates.js.
                 Shortage, boom, blockade, piracy, and
                 freight lines already had templates.
                 Economy headlines did not keep the event
                 id. The leftover test never printed a
                 blockade line or its dock card.

WHAT I CHANGED   I kept that leftover writer. Every ticker
                 line now cites the sim event that made it.
                 A line with no citation does not stay.
                 Station names come from the catalog.
                 This receipt and the print test are new.

WHAT YOU WILL FEEL   A blockade hits Helios. The ticker
                 names the station and the freeze. Dock
                 and one card says BLOCKADE, iron ore is
                 frozen, spreads hurt until it lifts.

THE NUMBERS      bar | before | after | target
                 blockade line | leftover templates, no print | Helios Station locked down; The Quiet quote canister rates | ticker line from a sim event
                 event cite | leftover log had kind + station only | economy:eventStarted evt_1 | every line traces
                 dock card | leftover buildEventCard, no eventId | BLOCKADE Iron Ore Blockade + evt_1 | card cites the same event
                 untraced line | leftover would keep it | refused | no invented news
                 station name | leftover stripped "helios" | Helios Station | catalog name
                 scenario | leftover literal-publish test | pq-177-00 seed 17700 | headless print

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The blockade is injectEvent, not a waited
                 spontaneous roll on the default route.
                 War tension and hunter sightings are in
                 the leaf prose and are not leftover kinds.
                 Regime templates exist and are unwired.
                 hud.js and uiRoot.js stay dirty and off
                 limits. Do not mark the queue.

NEXT             Integrator updates the queue. Later leaves
                 own charts, glyphs, twists, and the black
                 market. Do not mark the queue from this
                 receipt.
```

## Files

- `src/ui/marketNews.js` — ticker event citation, catalog station names, card `eventId`
- `test/pq-177-00-ticker.test.mjs` — prints the blockade line and dock card
- `design/program/roadmap/receipts/PQ-177.00-REPORT.md` — this receipt

Already live, not rewritten: `src/data/newsTemplates.js` (blockade / shortage / boom / piracy / freight templates + dock cards), `test/market-news-literal-publish.test.mjs` (literal publish + freight provenance).

Not edited: `src/ui/hud.js`, `src/ui/uiRoot.js`, `src/systems/economy.js`, `src/systems/input.js`, `src/render/`, `assets/`, VFX docs, `index.html`, `build_map.md`, `program-queue.json`.

## Honesty gaps

- Leftover already had blockade templates and `buildEventCard`. It did not stamp `eventId` on ticker lines or print a blockade line plus card. That was the named gap.
- The printed blockade is `economy.injectEvent` on seed 17700, then `dock:docked`. It is not a waited spontaneous roll on the default flight route.
- `news:headline` for generic economy emissions stays compact. Provenance lives on the ticker log (`state.ui.marketNews.log`) and the dock card.
- War tension and hunter sightings are named in the leaf prose. Leftover kinds are shortage, boom, blockade, piracy, freight, and authored `news:publish`. This leaf did not invent those missing kinds.
- Regime-flip templates exist in leftover data. Leftover `createMarketNews` does not subscribe to a regime bus event.
- The dock card is `state.ui.marketNews.lastCard`. Dirty `hud.js` / `uiRoot.js` were off limits, so the on-screen ticker host was not opened or captured.
- Headless only. The job peeled headed capture. Headless proof is the landing bar.
- Exit `npm run check:baseline` was red on foreign seams: `pq020-ceres-topology` additive dressing, 47-A sim hash drift, and massline mass-coupling. Those files were live under other NOW rows. This leaf did not edit them and did not re-record goldens.
- `newsTemplates.js` was leftover-complete for blockade copy. This leaf did not rewrite it.
- Checkpoint finished. `NOW.md` was left for the live orchestrator / controller rows.

## Proof

```text
PQ-177.00 blockade line: Helios Station locked down; The Quiet quote canister rates
PQ-177.00 event: economy:eventStarted evt_1
PQ-177.00 dock card: BLOCKADE Iron Ore Blockade — iron ore is frozen at Helios Station. Spreads are punishing until it lifts.
```

`node --test test/pq-177-00-ticker.test.mjs test/market-news-literal-publish.test.mjs` — 4 pass, 0 fail.
