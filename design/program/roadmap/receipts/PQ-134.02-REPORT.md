<!-- LIFETIME: RECEIPT -->
# PQ-134.02 — Causal VFX/audio grammar (CRU-051)

**Verdict:** IMPLEMENTED (audio half). The causal cue grammar now resolves to authored audio
recipes instead of silently dropping.

## What shipped (commit 7dc63c287)

- `src/render/vfx.js`: structural-FX cue admission emits `audio:cue` + `presentation:audioCue`
  — on admission, not on spawn success, so dedup/ownership see the intent; five already-voiced
  families ride `playbackOwnedByRaw` so they cannot double-fire; same cue id emits at most once
  per tick; only the eight reachable `combat.causal.*` / `combat.impact.*` ids emit.
- `src/audio/audioSystem.js`: `AUDIO_CUE_TO_RECIPE` maps the eight reachable ids onto authored
  recipes (`impact_small_hit`, `impact_structure_break`, `explosion_medium`,
  `shield_absorb_tick`, `reaction_molten`, `reaction_cryo`, `reaction_ion`, `reaction_cascade`).
- `test/causal-audio-cues.test.mjs` — 6/6 green: resolved voice on admitted cue, reaction family
  voice, raw-owned families carry `playbackOwnedByRaw`, per-tick dedupe, inadmissible receipts
  silent, every declared cue id resolves.

## Bounds

- `npm run check:playable` (row check) not re-run for this packet — host-bound on a saturated
  machine; the causal path is headless-verified. VFX half landed earlier under the same row.
- Emission is bus-level verified; loudness/pan audition on the live route is the residual.
