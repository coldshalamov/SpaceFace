# PQ-210.05 — The demo HUD reads as hardware, not a web page — REPORT

RESULT: DONE

## What the player sees

Flight HUD, in-run Crucible HUD, and results are Field Hardware instruments over the world: one
fastened cluster with smoked-glass windows and a visible threat lamp, a machined power rail, a
radar binnacle, the swarm readout in the comms bezel (ROUND / CHAIN / SCORE — no teal cards), and
results as bezels + keycaps + a hero numeral. Not eight CSS boxes, not words-on-black.

## What landed

- `src/ui/views/hudStyles.js` — PQ-210.05 paint: `bezel.svg` chassis over a dark well (so the
  fasteners read), opaque `--dp-glass-solid` windows, threat lamp `display:flex`, power-rail thin
  bezel, no `--dp-glass-solid:var(--dp-glass-flight)` remap.
- `styles/crucible.css` — swarm only hides adventure instruments; it no longer restyles the HUD
  into teal cards or a `position:fixed` readout.
- `src/ui/survivalHud.js` — readout is legends and channels inside the comms bezel (`background:none`).
- `src/ui/deckplate/screens.js` + `src/ui/screens/crucible.js` — results `CRRES` skin; `data-outcome`
  for the hazard/victory stripe.
- `tools/ui-bench.js` + `scripts/ui-bench.mjs` — bench mounts the real HUD, ticks `createHud().frame`,
  seeds a hull, mounts `survivalHud` for swarm, `&chrome=0` on shots.
- `test/pq-210-05-demo-hud.test.mjs` — 5/5: bezel + visible lamp + no swarm teal + no crun card fill
  + results bezels/keycaps.

`src/ui/hud.js` DOM was already the cluster + lamp; the defect was paint (PASS 6 hid the lamp and
smoked every face) plus swarm overlay CSS. This leaf did not restyle the other forty screens.

## Evidence

Live `ui-bench --shot=` stills (in-session, not committed):

- `.devshots/ui-bench/pq210-05/flight.png`
- `.devshots/ui-bench/pq210-05/crucibleHud.png` — `crun live · run survival/active`
- `.devshots/ui-bench/pq210-05/crucibleResults.png`

Focused: `node --test test/pq-210-05-demo-hud.test.mjs` 5/5.

## Residuals

- Kit socket PNGs are relative `assets/ui/kit/...` URLs; from `/tools/ui-bench.html` they 404, so
  the bench rail is the deckplate well fallback (live route from `/` still resolves the kit).
- Alert/dock pills stay the existing kit floor; not this leaf's three surfaces.
- Queue close-out of `program-queue.json` left to the fleet lane. Do not start PQ-210.06–.08.

## Controller integration (2026-09-20)

Integrated to master as `31ababfca` (apply + pathspec). Independent review: Agy `gemini-3.8-flash-high`
verdict **accept** — no per-frame DOM writes bypass the cached helpers, accessibility contracts
(contrast, focus, forced colors) respected, no shared crucible.css casualties, telemetry channels
intact; two non-blocking notes (source-regex test pins, bench fixtures setting private fields
directly). The controller additionally generated BEFORE stills from the base commit
(`.devshots/ui-bench/pq210-05-before/`) and judged all three after-stills against the
Field-Hardware bar: consistent bone/amber identity, threat channel present, results plate
transformed from words-on-black to bezels + keycaps + hero numeral. Route acceptance on the live
game route remains the demo campaign's owner call.
