<!-- LIFETIME: RECEIPT -->
# PQ-166.01 — Growth pass

CURRENT STATUS 2026-09-11

DONE PQ-166.01 — Leaves: "Zero clipped strings in the capture sweep."

Tech-tree canvas wraps long growth names on spaces then characters and never inserts an ellipsis. The shipped detector measures those labels. Headed sweep seed 16601: 12 surfaces including the HUD, DOM clipCount=0, canvasClipCount=0 of 32 nodes. Two independent review waves PASS, unmetClause null.

```text
PQ-166.01 capture-sweep screens=12 clipCount=0 canvasClipCount=0
PQ-166.01 canvas-wrap nodes=32 clipCount=0
```

---

DONE (superseded — canvas ellipsis remains) PQ-166.01 — Fake language now grows about forty percent on every menu and the HUD, and a live capture of thirteen visible surfaces found zero clipped strings.

WHAT I FOUND     The fake language only grew about thirty-two percent, tight HUD chips used dots, and a prior sweep photographed empty sky then called that zero clips.

WHAT I CHANGED   The fake language now stretches letters and spaces to about forty percent, menus and the HUD wrap and widen instead of shrinking type, and the clip count is measured by the same layout function the game ships.

WHAT YOU WILL FEEL   Turn on Pseudo-locale (layout check) in Settings. Buttons, titles, and HUD names get longer and wrap onto another line instead of vanishing into dots. English still looks as it did.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Mean growth, screen + HUD strings | ~1.32 | 1.42 | ≥ 1.40 | 16601 |
| Mean growth, whole catalog | ~1.32 | 1.50 | ≥ 1.40 | 16601 |
| Clipped screen + HUD strings (layout boxes) | unmeasured | 0 of 1076 | 0 | 16601 |
| Clipped strings in the live capture sweep | HUD missing; eleven menus not on camera | 0 of 13 visible surfaces (12 menus + HUD) | 0 | 16601 |

THE FEEL         Longer labels wrap instead of disappearing. Two plaques at the top of the HUD can still sit on top of each other, and the live station interior was not in this photograph.

NEXT             --next still returns PQ-141.02 (higher priority). This packet's next leaf is PQ-166.02 five languages. The queue row stays ready until two review waves.

CHECKS
- `node --test test/localization-reachability.test.mjs test/pq-166-01-pseudo-locale.test.mjs`: 16/16 twice
- `npm run check:player-facing-labels`: PASS
- Live capture at seed 16601 imported the shipped clip detector: 13/13 surfaces visible, clipCount=0

UNPROVEN         Live station chrome was not in the thirteen-surface photograph (station catalog keys are in the 1076-key box sweep at zero clips). `::before` "LOCAL CONTACTS" is still English; that is not a clip.
