<!-- LIFETIME: RECEIPT -->
# PQ-166.01 — Growth pass

NOT DONE PQ-166.01 — Leaves: "Zero clipped strings in the capture sweep."

Seed 16601: mean growth 1.42 on screen+HUD keys (≥1.40); structural sweep clipCount=0 of 1076 painted keys against wrap-safe boxes. No headed Playwright capture sweep. Queue row stays ready.

DONE (layout-box stand-in only; capture sweep unmet — not a close) PQ-166.01 — Pseudo-locale now grows about forty percent on every screen and the HUD, and a wrap-safe layout pass leaves zero clipped strings.

DONE WHEN (verbatim): Zero clipped strings in the capture sweep.

WHAT I FOUND — The live pseudo-locale already painted funny letters, but it only grew about thirty-two percent, and tight HUD chips still clipped with nowrap. Growth was being judged in English if you were not careful.

WHAT I CHANGED — The fake language now grows about forty percent by stretching letters and spaces, never by shrinking type. Screens and the HUD wrap and flex instead of ellipsizing when you leave English, and a headless sweep measures every screen and HUD string against those boxes.

WHAT YOU WILL FEEL — Turn on Pseudo-locale (layout check) in Settings. Buttons, titles, and HUD lines get longer and wrap instead of vanishing into dots. English still looks as it did.

THE NUMBERS

| Bar | Before | After | Target | Seed |
|---|---|---|---|---|
| Mean growth, screen + HUD strings | ~1.32 (vowel-doubling only) | 1.42 | ≥ 1.40 | 16601 |
| Mean growth, whole catalog | ~1.32 | 1.50 | ≥ 1.40 | 16601 |
| Clipped screen + HUD strings in the structural sweep | unmeasured | 0 of 1076 painted keys | 0 | 16601 |
| Viewport the boxes assume | n/a | 1280×720 | shipping HUD width | 16601 |

FILES

- `src/localization/runtime.js` — +40 % per-character growth; placeholders stay a homomorphism.
- `src/localization/layout.js` — string-width vs wrap-safe boxes for every screen and HUD key.
- `src/localization/domBridge.js` — wrap/flex CSS for every non-English locale, including the HUD.
- `src/localization/layoutSweep.js` — alias onto the shipped sweep.
- `test/pq-166-01-pseudo-locale.test.mjs` — new, 5 tests.
- `design/program/roadmap/receipts/PQ-166.01-REPORT.md` — this receipt.

CHECKS

| Command | Round 1 | Round 2 |
|---|---|---|
| `node --test test/pq-166-01-pseudo-locale.test.mjs` | PASS, 5/5 | PASS, 5/5 |
| `node --test test/localization-runtime.test.mjs` | PASS, 8/8 | — |
| `node --test test/pq-166-00-language-setting.test.mjs` | PASS, 4/4 | — |
| `node --test test/localization-reachability.test.mjs` | PASS, 7/7 | — |

UNPROVEN — A headed Playwright capture of every screen was not run; GPU windows are contended. The zero-clip claim is the structural sweep (string width vs layout box) on the shipped pseudo-locale, not a photographed frame. `settings.js` was not touched: the picker already reads `LANGUAGE_OPTIONS`.

Candidate: shared dirty checkout. No commit, push, stash, restore, checkout, reset or clean. No acceptance status is self-assigned.
