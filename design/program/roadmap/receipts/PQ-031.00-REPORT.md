<!-- LIFETIME: RECEIPT -->
# PQ-031.00 — Bolas throw in one input

```text
DONE  PQ-031.00 — two lights at combat range are bridled 0.417 s after the first latch and both lose the helm for a B11 second

WHAT I FOUND     The leftover Twin Bridle was already a two-press throw with a 2 s window. Setup was 0.117 s on a 72 WU pair. The 10 s setup the packet named is gone.

WHAT I CHANGED   I kept that leftover throw. Each end now feels only the share the other mass can yank, so two lights still clothesline and a heavy does not. I did not add a third press or a hidden mode.

WHAT YOU WILL FEEL   Paint A, flick to B, press again. The rope exists before a second of combat time has passed and both lights lose the stick. You still press twice; it is a throw, not a setup ritual.

THE NUMBERS      bar | before | after | target
                 setup from first latch | leftover 0.117 s at 72 WU, seed leftover | 0.417 s at 220 WU, seed 31000 | ≤ 2 s throw / ≤ 3 s done-when
                 pair distance | leftover 72 WU | 220 WU | combat range
                 B11 k (coupled catch) | leftover test used full ΔV 160 → k 1.524 | catch ΔV 80 → k 0.762, seed 31000 | ≥ 0.30
                 B11 helm | leftover 3.500 s | 3.500 s | ≥ 1.00 s
                 presses | leftover two Massline presses | same | latch A, latch B, the bridle exists
                 velocity writes | leftover none | same | none
                 scenario | pq-031-00-bolas-throw | seed 31000 | headless

CHECKS           npm run check:baseline | entry 12/15, exit 12/15, same three reds (sim, sim-v3, pq020-ceres-topology)
                 npm run check:massline | PASS 26/26
                 node --test test/pq-031-00-bolas-throw.test.mjs | PASS
                 leftover massline-twin-bridle-head B11 | PASS elapsed 0.117 s

THE FRAMES       Headless leftover throw. No headed GPU strip.

UNPROVEN         I did not watch this at the shipping camera. The throw is still two public Massline presses inside the 2 s window, not a single auto-pair. tumbleStates still reads the process COMBAT_FLAGS map, so the B11 second in this fixture is the leftover flag flip plus production features, not a headed default-route capture.
```

## Files

- `src/systems/tetherGameplay.js` — leftover throw kept; catch ΔV is now the coupled share
- `test/pq-031-00-bolas-throw.test.mjs` — seed 31000 combat-range throw + B11
- leftover `test/massline-twin-bridle-head.test.mjs` still proves the same 3 s bar on a closer pair

Not edited: `modules.js`, `masslineSnares.js`, `masslineThreats.js`, `program-queue.json`, `NOW.md`.
