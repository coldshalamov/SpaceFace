<!-- LIFETIME: RECEIPT -->
# PQ-031.00 — Bolas throw in one input

```text
STATUS DONE

WHAT I FOUND     The leftover throw already lived in tether
                 gameplay. Setup was already 2 s. Two lights
                 already bridled in 0.117 s and both lost the
                 helm for a B11 second. Hitch already has an S
                 utility; the head is M. The leftover test
                 already named the throw. It did not pin Hitch.

WHAT I CHANGED   I kept that leftover throw. I did not rewrite
                 the pair solver or the NPC cut. The leftover
                 test now also refuses Hitch. This receipt is
                 new.

WHAT YOU WILL FEEL   Latch one light, latch the other. The
                 second press is the throw. They tumble. Wait
                 more than two seconds and the first catch
                 dies. Hitch never wears this toy.

THE NUMBERS      bar | before | after | target
                 setup window | leftover 2 s | same | ≤ 2 s
                 two-light latch | leftover 0.117 s after first latch | same | ≤ 3 s
                 B11 k | leftover 1.524 at rel 160 / cruise 105 | same | ≥ 0.30
                 B11 helm | leftover 3.500 s | same | ≥ 1.00 s
                 Hitch fit | S utility vs M bridle | same refuse | do not put the head on Hitch
                 scenario | massline-twin-bridle-head | headless | leftover test

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The throw uses the leftover fake attachment
                 create, not a Rapier-flown default-route pair.
                 tumbleStates still reads the process
                 COMBAT_FLAGS map, so the B11 test pins that map
                 ON; production already has it ON, 47-A does not.
                 The same leftover files already hold 031.01
                 pair admission and 031.02 specialist cut /
                 heavy ignore. This leaf did not rewrite those.
                 Combat-range setup is the 2 s expiry, not a
                 flown chase clock.

NEXT             Integrator updates the queue. PQ-031.01 is
                 coupled-pair physics. Do not mark the queue
                 from this receipt.
```

## Files

- `src/systems/tetherGameplay.js` — leftover `TWIN_BRIDLE_SETUP_S = 2` and catch tumble, not rewritten
- `test/massline-twin-bridle-head.test.mjs` — leftover 0.117 s / B11 proof plus Hitch refuse
- `design/program/roadmap/receipts/PQ-031.00-REPORT.md` — this receipt

Not edited: `modules.js`, `input.js`, `sg02DynamicBodyOwner.js`, `tumbleStates.js`, `program-queue.json`.
