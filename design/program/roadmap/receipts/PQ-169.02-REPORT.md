<!-- LIFETIME: RECEIPT -->
# PQ-169.02 — Weekly mutators

```text
STATUS DONE

WHAT I FOUND     Four weekly Crucible twists were already live:
                 gravity slalom, heavies only, weapons cold, reef.
                 UTC week picks one in a fixed rotation. The named
                 proof for this leaf was missing. Seed 16920 already
                 shows four different strategies.

WHAT I CHANGED   Nothing in the live Weekly path. This receipt records
                 the proof that was already on the tree. The named
                 proof is test/pq-169-02-weekly-mutators.test.mjs.

WHAT YOU WILL FEEL   Pick Weekly and this week's name is on the door.
                 Gravity slalom puts three wells in the room. Heavies
                 only throws out the wasp fodder. Weapons cold leaves
                 the guns in the rack. Reef is a tight rock recipe,
                 not Helios Core's empty floor. Next week is a
                 different one of those four. Ordinary Swarm is still
                 the default. Nothing here follows you into adventure.

THE NUMBERS      bar | before | after | target
                 same UTC week, two clocks | 2026-W36 gravity_slalom | same | identical id
                 four consecutive UTC weeks | gravity_slalom, heavies_only, weapons_cold, reef | same | all four once
                 gravity_slalom wellCount (seed 16920 wave 1) | 0 | 3 | 3
                 heavies_only heavyCount / fodder | 0 / 10 | 10 / 0 | heavies, no fodder
                 weapons_cold physicsOnly / skipDraft | false / false | true / true | guns skipped
                 weapons_cold launch loadout | concussion + marker | whip only | no wpn_
                 reef recipe | helios_core empty | crucible_reef | not default Helios
                 strategy signatures | 1 (plain swarm) | 4 distinct | each changes the top strategy
                 scenario | test/pq-169-02-weekly-mutators.test.mjs | seed 16920 | headless

THE FRAMES       Not a camera leaf. The claim is week keys, a four-game
                 rotation, and strategy telemetry. Headed Chromium skipped.

NEXT             Integrator updates the queue. Leftover: .03 hangar
                 feed is already closed as not needed. Do not mark the
                 queue from this receipt.
```

## Files

- `test/pq-169-02-weekly-mutators.test.mjs` — named proof

Already live: `src/data/survivalMutators.js` (`CRUCIBLE_WEEKLY_ROTATION`),
`src/systems/survivalRecords.js` (`weeklyMutatorForWeekKey`),
`src/systems/survivalMutators.js` (`compileChallenge`, `weeklyTelemetry`,
`applyWeaponsColdLoadout`), `src/ui/screens/crucible.js` (Weekly door).

Prior hyphen receipt: `design/program/roadmap/receipts/PQ-169-02-REPORT.md`.

Not edited: survival owners, `program-queue.json`, `NOW.md`.
