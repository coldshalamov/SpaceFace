<!-- LIFETIME: ACTIVE_RECEIPT -->
# PQ-147.00 — Five powers, five sentences, five drills

```text
DONE PQ-147.00 — Well, Repulsor, Clearing Cone, Skim Collector and Mass Seed each have a fitting-screen sentence, a Range drill, and a cone/ring/sheet volume. No field is a glowing sphere. Keys 4–8 already sit on the Power Rail; skim now feeds the hook the rail already reads.

WHAT I FOUND     Digit4–8 already fire five real verbs, but only Well/Repulsor/Cone were field data. Skim and Seed had no fitting sentence, no drill, and no authored volume. The rail already listed all five slots; skim stayed locked because collectorOn lived under planet.player, not on the path readRailModel already uses.

WHAT I CHANGED   1) fields.js: POWER_ROSTER + sentences + cone/ring/sheet volumes for all five; scoop-sheet and lock-ring defs; volume grammar that forbids a sphere.
                 2) fieldKernel.js: SHEET kind (lateral collect on a slab) and volume passthrough. Well/Repulsor stay ring volumes; Cone stays a wedge.
                 3) fields.js runtime: publish volume on every active record; skim sheet follows collectorOn; seed publishes a zero-strength lock-ring; feed planet.collectorOn / planetRuntime so the existing rail hook shows ready/armed.
                 4) Five drills in src/data/scenarios/pq147-*.scenario.json. No powerRail.js, no range.js, no vfx.js.

WHAT YOU WILL FEEL   Press 5 and a ring pulls junk into a pile. Press 6 and a ring bowls it out. Hold 7 and a wedge plows a lane. Open 8 on a planet band and a scoop sheet rakes debris onto your line. Throw 4 and a lock-ring becomes an anchor you can hitch. None of them is a glowing ball.

THE NUMBERS      bar | before | after | target
                 Powers with a fitting-screen sentence | 0 | 5 | 5
                 Range drills in src/data/scenarios | 0 | 5 | 5
                 Authored volumes that are cone/ring/sheet | 3 (cone only named) | 5 | 5
                 Authored volumes that are a sphere | 0 | 0 | 0
                 Rail slots 4–8 readable from live field/seed/skim state | 4 (skim locked) | 5 | 5
                 Focused tests | unmeasured | 39/39 field + PQ-147.00 | 100%

THE FRAMES       Not taken (headed GPU forbidden this lane). Headless drills prove pull/shove/wedge/sheet/lock-ring. #111 remastered swept impulses, not these fields — do not paper over that with a Fresnel ball.

NEXT             PQ-147.01 Fields participate in physics for everyone. Queue state left for the integrator.
```

## Rail clause

`src/ui/powerRail.js` already mounts slots 4–8 and reads:

- 4 seed — `state.player.massSeed.cooldownUntil`
- 5 well — `state.fields.cooldowns.well`
- 6 repulsor — `state.fields.cooldowns.repulsor`
- 7 cone — `state.fields.coneActive`
- 8 skim — `state.planetRuntime.record.collectorOn` or `state.planet.collectorOn`

This lane did not edit that file. Skim now copies `planet.player.collectorOn` onto those existing hooks when a planet is live. Away from a planet the slot stays locked, which is honest: the key does nothing in open space.

## Forbidden paths

Not edited: `src/render/vfx.js`, `src/render/combat/*`, `src/ui/powerRail.js`, `src/ui/screens/range.js`, `src/data/weapons.js`, `package.json`. No glowing sphere, no camera-facing field card.

## Checks

| Check | Result |
|---|---|
| `node --check` on fields data/system/kernel | pass |
| `node --test test/pq147-00-powers.test.mjs test/fields-kernel.test.mjs test/fields-integration.test.mjs` | 39 pass |

Queue unit state not set to `done`.
