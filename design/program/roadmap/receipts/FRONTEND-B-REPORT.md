# FRONTEND-B — shell screens and the flight HUD (PQ-181.00–.03, PQ-188.00)

Date: 2026-09-07. Task file: `design/frontend/direction/tasks/TASK_B_SHELL_AND_HUD.md`. Builds on
Task A (kit + title). Everything below is live on the default route.

## What the owner will see

- **New Game** — the hull in its rig fills the stage; pilot name and seed as underlined inputs;
  difficulty as four words with the live one underlined and its sentence below; loadout as four
  quiet words; Back then **Launch** in the foot. (`61025771f`, `886d2103a`)
- **Load** — saves as hairline rows down the hang; the focused save's hull on the stage with its
  credits as the hero number and the pilot/sector/date as a portrait; Load / Delete / Back as words.
  (`9f522f6f2`)
- **Settings** — section words down the hang; rows with hairlines; toggles as two words (the live
  one underlined); rebind keys as words that go live while listening; the stage scrolls. (`18c3653e3`)
- **Pause** — the world stays fully visible (0 % scrim), the HUD dims to 38 %; "Paused" with the
  flight brief under it; the actions as a column of words; **Photo** hides everything but the world
  (Esc returns). (`6d42d00b4`)
- **Game Over** — the frame cools to the wanted blue; the cause at title size; sortie and damage as
  the second line; dock / cost / cargo as three hero numbers; coverage as a sentence; Retry / Load /
  New / Menu as words. (`38befd0b2`, `baad45ea2`)
- **Credits** (new) — from the title's fine line "SpaceFace v0.1.0 · Credits": made by, type,
  libraries, fonts and licences, third-party notices (generated by `scripts/write-credits.mjs`,
  `npm run credits`). (`224e8e8e3`)
- **Flight HUD** — no plates anywhere; the kit's two faces and tokens. Speed is the one hero number
  with "speed" beneath it; weapons/tether/cargo/credits at data size; vitals as 2 px lines (energy,
  drive, heat, fuel — sentence case), the heat line and every signal on `--k-signal` so the frame
  goes red when wanted; the ship schematic kept as a drawn silhouette; the Power Rail's slots are
  words (key at fine size before the word, the armed word live with a 2 px signal rule, the sweep
  ring the one animation); the target card, contact roster, comms tape, mission line, receipts and
  the one voice are text on the world. The law card, comms log, band tuner, onboarding card and
  massline marks that other modules mount into the HUD keep their behaviour and lose their plates
  under `#hud`. On **undock** the four anchors settle in 200 ms apart with an `open` cue each
  (`hud.arrive()`, called only when the HUD was hidden by the dock; reduced motion: at once, one cue).
  (`3697dc3a1`)

## Files

Screens: `src/ui/screens/{newGame,saveLoad,settings,pause,gameOver}.js`, `src/ui/screens/credits.js`
(new), `src/ui/screens/stageHull.js` (new, the bounded 3D hull mount), `src/ui/screens/mainMenu.js`
(the Credits fine word), `src/ui/kit/temperature.js` (pause holds `flight`), `styles/ui.css`
(first-run splash rules moved from the deleted `newGame.js` style block), `src/data/credits.js` +
`scripts/write-credits.mjs`, `package.json` (`credits`).
HUD: `src/ui/uiRoot.js` (`injectHudCss`, `_hudHiddenByDock` + `arrive()` call), `src/ui/hud.js`
(death / travel-tape / caption / tell style blocks, labels, `arrive()`), `src/ui/targetPanel.js`
(inline styles), `test/ui-frame-references/budgets.json` (re-baselined).
Checks touched: `scripts/check-new-game-layout.mjs` (Launch last), `scripts/check-pause-brief.mjs`
(accepts the middle-dot separator already in source), `scripts/check-controls-discoverability.mjs`
(accepts `setText(elTetherKeys, …)` from `aa95391f5`), `scripts/ui-grammar-surfaces.mjs` (credits
entry; the stale "missing" credits stub removed), `test/instrument-hierarchy-six-screens.test.mjs`
(saveLoad no longer injects CSS).

## Recorded kit additions (`styles/kit.css`, under `/* Task B additions */`)

`.k-stage > .k-world--stage`, `.k-stage__foot`, `body.k-screen-top[data-k-screen="pause"] #hud`
(38 %), `.k-screen--cold::before`, `body.k-photo #hud/#toasts/#alerts`, `body.k-screen-top
#modal-backdrop { background:none }`, `.k-words--row + .k-sentence`, `.k-stage--scroll`.

## Choices where the sheet was silent

- Pause words at `emph` size (not `menu`) so all items fit at 1080p.
- Game Over's third hero word is "cargo consequence" (the public-route check reads that phrase).
- HUD `--hud-copy/--hud-line-strong/--hud-danger` kept as aliases (bone-62 / hair / red) because the
  encounter prompts still read them.
- Power Rail slot art hidden (the slot is a word); the sweep ring stays as a 10 px circle before it.
- `sfx_wanted_alert` already matched spec §8 from Task A's recipe commit (sine 196 Hz, 0.25 s
  attack, low-pass 800 Hz, 1.14 s total) — unchanged.

## Checks

Green: `check:baseline` (except one pre-existing red, below), `check:hud-j07`, `check:one-voice`,
`check:ui-identity`, `check:ui-a11y`, `check:wcag-contrast`, `check:player-facing-labels`,
`check:controls-discoverability`, `check-ui-frame-sleep`, the `check:ui:perf` chain up to budgets,
`check:new-game-layout`, `check:pause-brief`, `check:save-load-slot-trust`, `check:settings-profile`.

Not mine / left as found:
- `check:ui-screen-imports` → "station control prompt - near-dock mode" reads `src/systems/onboarding.js`,
  changed today by another agent (`f1058ca1d`); red on `HEAD` before this task's commits.
- `check:ui:budgets` after the re-baseline: `baseline:stale` because the digest covers `src/ui`,
  `styles`, `src/core`, `src/render` and other agents are editing `src/ui/AGENTS.md` and
  `src/render/AGENTS.md` in the shared tree; `baseline:incomplete` names `game-over` (the harness
  cannot die on the public route) and `asteroid-works` (its fixture never armed). Coverage is the
  same 16 surfaces as the previous baseline; `flight` itself has never produced samples in this
  harness (it is in both baselines' `missing` list — "no usable samples in the sampling window"),
  so the HUD's frame cost is measured through the `base`, `ship`, `footprint` and `save-load`
  surfaces, all green. The grammar-debt surfaces are station screens owed to PQ-184.
- `check:baseline`'s `massline` child timed out four 47-A checks under machine contention
  (each printed its OK line before the kill).

## menu.css

`styles/menu.css` still holds the `sf-saveload`, `sf-slot`, `sf-pause`, `sf-gameover` rules (19
selectors). Left for Task D: the station and reading screens other agents own still mount with
`panel sf-menu` and read `sf-slot*`; deleting the block now would strip their look before their
migration. Nothing in Task B's screens reads them.

## Captures

Per the owner's direction (2026-09-07) no capture set was produced for this receipt; the reviewer
runs the game. The undock and wanted moments are exercised on the default route (dock at any
station, undock; fire on a clean ship until the law notices).
