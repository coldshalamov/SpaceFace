<!-- LIFETIME: RECEIPT -->
# PQ-032.03 — NG+ carries a legacy

```text
STATUS NOT DONE on scars, titles, and ending world
                 facts. The live carry is the ending's name,
                 one keepsake, and unresolved hunter
                 grudges. The naming half of the done-when
                 IS live.

WHAT I FOUND     Leftover New Run+ already lived in
                 newGamePlus.js and the leftover head test.
                 A completed run names the ending title,
                 offers one keepsake, and carries only
                 unresolved hunter grudges. Story owns a
                 save-safe story.newGamePlus receipt. There
                 was no receipt.

                 The leaf names five carriers: ship scars,
                 titles, ace grudges, one head, and the
                 ending's world facts. Two of the five are
                 live. Three are not, so this leaf is not
                 done.

WHAT I CHANGED   I kept that leftover writer. I did not
                 invent a second New Game Plus owner. This
                 receipt is new. The leftover test gained
                 one case that rides the real save carrier.

WHAT YOU WILL FEEL   Finish a run. New Game offers New Run+.
                 It says THE NEXT RUN, keep one item, and
                 how many hunters still want you. You keep
                 one relic. Those hunters come back bigger.
                 The HUD names that leftover history.

THE NUMBERS      bar | before | after | target
                 ending title | leftover THE NEXT RUN | same leftover | mechanic names history
                 keepsake | leftover 1 selectable head | same 1 | one head
                 grudges | leftover 1 unresolved hunter | same 1 | ace grudges
                 save receipt | leftover spaceface.newGamePlus.v1 | same leftover | save-safe named history
                 save owner | leftover no player/world leak | same | completed source only
                 carriers named | 5 | 2 live | 5
                 scars | leftover none | still none | MISS
                 titles | leftover none | still none | MISS
                 world facts | leftover none | still none | MISS
                 migration | leftover none | current-version only | old save loads
                 scenario | leftover new-game-plus | headless | leftover test

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     Leftover carry is ending title + one
                 keepsake + unresolved grudges. Ship scars,
                 titles, and ending world facts do not
                 carry. That is three of the leaf's five
                 named carriers missing, so STATUS is NOT
                 DONE. ships.js is dirty and off limits.
                 titles.newGame wipes the ledger. Fresh
                 story clears postEnding. FRONTEND leftover
                 already names the history; this leaf did
                 not open those screens.

                 The done-when says "save migration test".
                 The named test builds CURRENT_VERSION
                 envelopes only, so nothing migrates. The
                 added case runs the real carrier
                 (missions.serialize's story payload, then
                 the story owner's load-time normalize) and
                 an absent-receipt case, which is the
                 migration concern that exists. It is still
                 not a version-migration test.

                 The keepsake GRANT is untested. main.js
                 calls ships.grantModule and throws
                 NEW_GAME_PLUS_UNAVAILABLE when it returns
                 false. grantModule resolves modules AND
                 weapons, so the relic case works, but no
                 test covers the grant or the fail-closed
                 throw. Harnessing it needs registry and
                 transition-guard plumbing beyond this
                 review, and ships.js is off limits.

NEXT             Scars, titles, and ending world facts stay
                 leftover gaps until a clean owner can carry
                 them. No second writer from this leaf.
                 Scars need a ships.js owner; titles need a
                 titles.js carry list; world facts need
                 postEnding projection into the overlay.
```

## Files

- `design/program/roadmap/receipts/PQ-032.03-REPORT.md` — this receipt
- `test/new-game-plus.test.mjs` — one added case: the legacy receipt rides the real save carrier, and a pre-New-Run+ save still loads

Already live, not rewritten: `src/core/newGamePlus.js` (candidate, overlay, `storyNewGamePlusRecord`, `normalizeStoryNewGamePlusRecord`), `src/systems/story.js` (save-safe `state.story.newGamePlus`), `src/save/saveSystem.js` (read-only source projection), `src/systems/aceMemory.js` (`applyNewGamePlusGrudges`), `src/main.js` (one keepsake grant + leftover grudges).

Not edited: `src/systems/ships.js`, `src/data/missions.js`, `src/systems/missions.js`, `src/combat/hullScars.js`, `src/systems/titles.js`, `src/render/`, `assets/`, `docs/visual-assets/`, `index.html`, `build_map.md`, `program-queue.json`, FRONTEND screens, `src/systems/input.js`, Program 1 clock files.

## Honesty gaps

- Leftover did NOT meet the done-when. Leaf `.03` names five carriers — ship scars, titles, ace grudges, one head, the ending's world facts. Ace grudges and one head are live; scars, titles, and world facts are not. STATUS is NOT DONE on those three.
- What IS live: the leftover candidate pins `sourceEndingTitle` `THE NEXT RUN` (ending `E`, `src/data/narrative.js:469`). The leftover story receipt is `spaceface.newGamePlus.v1` and is a fixed point of JSON clone + `normalizeStoryNewGamePlusRecord`. Story load uses that same normalize.
- Carry is ending title + one keepsake + unresolved hunter grudges. That is the leftover contract. This leaf did not add scars, titles, or ending world facts.
- The naming half of the done-when is live and traced. `src/ui/screens/newGame.js:258-270` prints `THE NEXT RUN · keep one item · 1 unresolved hunter grudge` and lists the keepsakes; `src/ui/hudMeta.js:93` prints `LEGACY E · THE NEXT RUN · Veil-Cutter · 1 GRUDGE`. That is why STATUS is a qualified NOT DONE, not a flat one.
- Hull scars have a live pure owner in `src/combat/hullScars.js`. The ship record that would carry them lives in dirty `src/systems/ships.js`. Leftover overlay has no scar list. Fresh `ships.newGame()` starts a clean hull.
- Titles have a live owner in `src/systems/titles.js`. `titles.newGame()` resets Thunderchild and `titlesSeen`. Leftover overlay has no title list.
- Ending world facts live on leftover `story.postEnding`. Fresh story reset clears that bag. Leftover overlay does not project world flags.
- New Game leftover copy already names the ending title and grudge count. HUD leftover already prints `LEGACY E · THE NEXT RUN · Veil-Cutter · 1 GRUDGE`. This leaf stayed off FRONTEND screens and did not re-verify those surfaces in a headed browser.
- Save owner leftover discovers a completed source and returns only the projection. It does not copy `player` or `world`.
- Defeated and unknown hunters do not carry. The leftover pin is `ace_yara_no_cut` only.
- "Save migration test" is not what the named test is. Every envelope is `CURRENT_VERSION`, so no migration runs. The added case covers the migration concern that actually exists — a pre-New-Run+ story loads to a clean `null` — but a real version-migration case is still absent.
- The keepsake grant is untested. `src/main.js:360-370` calls `ships.grantModule` and throws `NEW_GAME_PLUS_UNAVAILABLE` on false. `ships.js:68` `defById` resolves `MODULE_BY_ID` then `WEAPON_BY_ID`, so a weapon relic grants, but no test walks that path or the fail-closed throw.
- Headless only. No shipping-camera strip. The job forbade headed capture.

## Proof

Inputs traced against live producers, not just assertions:

- `data.missions.story` is the real save shape — `missions.serialize()` (`src/systems/missions.js:6420`) returns `story: this.state.story`, and `slotMetaFromEnvelope` (`src/save/saveSystem.js:4082`) reads `data.story || missions.story`.
- `player.moduleInventory` `{instanceId, defId}` and `ownedShips[].fittings` are the live shapes written by `ships.grantModule` (`src/systems/ships.js:1454`) and captured by the save owner's player capture.
- `data.aceMemory` is flat and ace-id keyed with `fled` / `defeated` / `returnTier` / `fleeCount` / `encounterCount` — the exact fields `normalizeRecord` (`src/systems/aceMemory.js:966-981`) writes and `aceMemory.serialize()` clones.
- `unique_veil_cutter` is a real `unique: true` weapon (`src/data/weapons.js:69`); ending `E` really is titled `THE NEXT RUN`.
- Slot discovery is not index-only: `_slotIndexWithFallback` always merges a live `_scanStoredSlots`, and both the index writer (`saveSystem.js:990`) and the envelope scanner (`saveSystem.js:4098`) set `endingChoice`. The test's index-less storage is not a shape the browser cannot produce.
- Grudge return timing is deterministic from the NEW run's seed — `returnPlanForAce(ace, seedOf(state), 0)` — not from `state.rng` draw order, so the determinism assertion survives the live boot's different rng consumption.

ending title=`THE NEXT RUN`. keepsake=1 (`unique_veil_cutter` first). grudges=1 (`ace_yara_no_cut`). save receipt=`spaceface.newGamePlus.v1` identity after the real carrier round trip + normalize. save owner exposes no `player` / `world`.

`node --test test/new-game-plus.test.mjs` — 5 pass, 0 fail.
