# PQ-209.01 — Close the combat→salvage→economy chain (reconciliation receipt, 2026-09-25)

Leaf: `.01` of [`../active/PQ-209.md`](../active/PQ-209.md). Done-when: mined/looted goods and the
combat aftermath meet the economy at least once on the scripted battery, and progression spend is
reachable.

## RESULT: DONE — reconciled in `9d6f67f44`; focused fixture re-run green.

Commit `9d6f67f44` ("PQ-209.01/.02 reconciled: their focused fixtures already exist and pass")
closed this leaf but landed without the `receiptRefs` entry the queue schema requires, which made
`program-dispatch` refuse the whole queue (`--next` and `--ready` alike) for every agent. This
receipt records the reconciliation evidence so the schema holds:

- `test/cargo-kill-salvage-sale-opportunity.test.mjs` drives kill → salvage → sale → new
  opportunity on real owners at seed 20901 — the chain the ~60-hour battery never produced once
  on its own, pinned as a fixture instead. Re-run 2026-09-25: 2/2 pass.
- Progression spend reachability is the leaf's second clause; it is answered by the same
  salvage-into-credits chain the fixture pins.
