DONE  PQ-170.00 — A kill on a contested front is now a war action the player can aim: contested-sector
kills bank momentum, blockade and siege reads are distinct verbs, one action can flip a sector's owner
with a toast, and every one of the eight station factions reacts on its own physical surface.

WHAT I FOUND     `factions.js` already ran wars between factions, but the player's kills leaned every
front the same small amount no matter where they happened — there was no contested-sector front, no
blockade/siege distinction, and no way for one action to tip an owner. `conflictReactions.js` carried
reaction copy for only two factions, so most of the station board never noticed a war at all.

WHAT I CHANGED   Committed as `271f75c7c`.
- `src/systems/factions.js` — contested-sector kills lean harder than remote ganks and bank momentum
  that resolves on the war tick (immediately while the front is hot); killing a lane's logistics hulls
  reads as blockade, breaking bastion-class hulls reads as siege, and a thrown mass or caused slam reads
  as the wrecking-ball verb; a resolved flip emits a factions-owned `conflict:flip` fact with a toast.
- `src/data/conflictReactions.js` — eight faction reaction surfaces resolved station→faction through
  SECTORS; flavor packs authored for mts/dmc boards and quiet/vael/free/choir graffiti.
- `src/systems/story.js`, `src/ui/station/adBoard.js` — graffiti walls and market boards consume the
  `conflict:flip` fact on every faction surface, not just the original two.
- `src/ui/galaxyMap.js`, `src/ui/screens/starmap.js` — contested fronts and resolved flips read on the
  map.
- `test/faction-war-fronts.test.mjs` (new, 5 tests), `test/story-ecology-reactions.test.mjs` extended.

WHAT YOU WILL FEEL   Pick a side in a contested sector and your kills actually move the front: strangle
the convoy lane to blockade, break the bastion hulls to siege, or wrecking-ball a thrown mass into the
fight. When the front tips, the sector's owner flips with a toast, the starmap shows the flip, and the
station walls and boards of all eight factions talk about it in their own register — and it survives
save/load.

THE NUMBERS      bar | evidence
player action flips one sector's owner | faction-war-fronts scenario: kill banks momentum, resolves to flip + toast (5/5)
reactions for 8 factions, not 2 | every station-holding faction resolves its own physical surface; saved flip surfaces on every wall (6/6 story-ecology tests)
save round-trip | front ledger survives inside the serialized conflict record; Continue preserves the exact flip fact

CHECKS
node --test test/faction-war-fronts.test.mjs test/story-ecology-reactions.test.mjs  PASS 11/11
node scripts/check-war-overlay.mjs                                             PASS
npm run check:faction-standings                                              PASS
npm run check:save-schema                                                    PASS (version 14, 288 paths)
npm run check:baseline                                                       12/15 — the three reds are foreign
  uncommitted hunks in src/systems/tetherGameplay.js and src/core/physics.js (sim hash drift +
  civilian-priority); each was verified green on an isolated HEAD tree at the time and none touch
  the PQ-170 write set.
