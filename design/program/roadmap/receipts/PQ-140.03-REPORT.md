<!-- LIFETIME: RECEIPT -->
# PQ-140.03 — Fodder is ammunition

```text
DONE  PQ-140.03 — twelve-body fodder resolves as ammunition. Thrown lights stay ballistic into rock / well / the body ahead. Median time-to-resolve a light under physics 2.808 s ≤ under guns 22.358 s on seeds 4242, 8008, 13502. Not more HP. Not a Crucible-only AI fork.

WHAT I FOUND     After a shove the cohort coasted 0.8–1.3 s, then the shared maneuver planner resumed obstacle avoidance. Lights dodged the rock they had been thrown at. Empty-space throwability (Motion Lab M11) was already fine; the miss was mass-ahead, not the shove itself.

WHAT I CHANGED   
1. Hypothesis: a thrown light is ammunition while its velocity still meets rock, well, or a body on that line. Keep `coast` so the planner does not dodge. A formed body in that same corridor goes limp so the chain is not steered off. Empty space does not limp the pack.
2. `src/ai/fodderCohort.js` only. Same director Adventure and Crucible already share. No hull HP buff. No tactical-AI fork. No swarm spawn edits.

WHAT YOU WILL FEEL   A shove into rock, a chain down the river, or a well in the pack finishes lights as physical objects. The free gun still works; it is the slow way.

THE NUMBERS      bar | before | after | target
                 physics median TTR a light (seeds 4242/8008/13502) | unmeasured on this candidate (they dodged mass after coast) | 2.808 s, 11/12 kills | ≤ guns
                 guns median TTR a light (starter pulse, same seeds) | — | 22.358 s, 7 kills in window | sequential gun
                 M11 disruptionResponseS / impulseRetention / collisions | 1.217 s / 1.00 / 0 | 1.217 s / 1.00 / 0 | throwability held

THE FRAMES       Headless only (packet: skip headed GPU). Scenario: `test/fodder-ammunition.test.mjs`, twelve wasp_swarmer river, rapier-dynamic, production profile.

NEXT             Integrator updates queue state. PQ-174.03 owns swarm roles.
```

## Review

One hypothesis, fixed seeds. Physics kit is concussion shove + rock wall + well. Guns kit is starter Pulse Laser S at authored RoF, nearest live light. Unresolved lights count as Infinity; physics must kill at least 7 of 12.

M11 river/crescent and both mutations stayed green: empty-space shove still coasts and rejoins; separation still prevents pileup.

## Checks

| Check | Result |
|---|---|
| `node --check src/ai/fodderCohort.js` | pass |
| `node --test test/fodder-ammunition.test.mjs` | 3 pass |
| `node --test --test-name-pattern "M11 swarm river\|mutation:" test/motion-lab-scenarios.test.mjs` | 3 pass |

No hull HP buffs. No Crucible AI fork. No `survivalSwarm` / swarm files. Queue unit state not set to done.
