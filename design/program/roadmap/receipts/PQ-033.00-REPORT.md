<!-- LIFETIME: RECEIPT -->
# PQ-033.00 — Legal and credits

```text
STATUS           credits reachable from title — leftover DONE
                 NOTICE — leftover-extracted
                 root LICENSE — missing, not invented
                 packaged build contains notices — NOT DONE
                 privacy / a11y statements — leftover settings;
                 missing store copy

WHAT I FOUND     Title already had a Credits word. The leftover
                 screen already named three.js MIT and Rapier
                 Apache-2.0. There was no root LICENSE. There
                 was no root NOTICE. Access settings already
                 lived. No player-facing privacy copy lived.

WHAT I CHANGED   I kept that leftover screen. I copied the
                 leftover third-party notice texts to NOTICE.
                 I did not write a game license. This receipt
                 and the print test are new.

WHAT YOU WILL FEEL   The title fine line says Credits. It opens
                 who made SpaceFace and the three.js / Rapier
                 notices. There is still no game license. A
                 packaged build still does not ship NOTICE.

THE NUMBERS      bar | before | after | target
                 title Credits | leftover button + _pick | Credits -> credits | reachable from title
                 three.js notice | leftover MIT in credits.js | three.js MIT | required notice named
                 Rapier notice | leftover Apache-2.0 | Rapier Apache-2.0 | required notice named
                 NOTICE | missing | leftover-extracted at root | third-party texts only
                 LICENSE | missing | missing, not invented | owner has not chosen
                 packaged notices | files list omits NOTICE | NOT DONE | needs dirty electron/package.json
                 privacy statement | leftover local-sink comment | missing store copy | do not invent
                 a11y statement | leftover Access settings | leftover settings, missing store copy | do not invent
                 scenario | leftover screen + credits.js | pq-033-00 headless | no headed capture

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     Packaged build files list does not include
                 NOTICE or LICENSE. three.quarks is a live
                 dependency and is not in leftover credits.
                 Access is settings, not a store statement.
                 Do not mark the queue.

NEXT             Integrator updates the queue. Later leaves
                 own crash reports, soak, and the store page.
                 Do not mark the queue from this receipt.
```

## Files

- `NOTICE` — leftover third-party notice texts from `src/data/credits.js` (three.js, Rapier, Floating UI, OFL faces). Not a game license.
- `test/pq-033-00-credits.test.mjs` — title pushes `credits`; leftover notices name three.js and Rapier; LICENSE stays missing
- `design/program/roadmap/receipts/PQ-033.00-REPORT.md` — this receipt

Already live, not rewritten: `src/ui/screens/mainMenu.js` (Credits word, `dataset.action = 'credits'`), `src/ui/screens/credits.js`, generated `src/data/credits.js`, `src/ui/screens/settings.js` Access tab.

Not edited: `LICENSE` (not invented), `package.json`, `package-lock.json`, `electron/`, `build/`, `program-queue.json`, `design/program/NOW.md`, `src/render/`, `assets/`, `src/systems/input.js`, `src/data/encounters/329-curtain-convoy.js`.

## Honesty gaps

- Credits are leftover-reachable from the title. This leaf did not rewrite the screen.
- NOTICE is leftover-extracted third-party text only. It is not a SpaceFace license and does not assign an SPDX to the game.
- Root LICENSE is missing. The owner has not chosen a game license. This leaf did not invent one.
- Packaged build contains notices is NOT DONE. Dirty `package.json` electron-builder `files` lists `build/web/**`, Electron shells, and `package.json`. It does not list `NOTICE` or `LICENSE`. Those files were off limits.
- Leftover `credits.js` names three.js, Rapier, Floating UI, and OFL faces. Live `package.json` also depends on `three.quarks`. That notice is not leftover and was not invented.
- Privacy: leftover telemetry comments call the sink local and PII-free. There is no player-facing privacy statement and no store copy. This leaf did not invent policy text.
- Accessibility: leftover Settings Access already has colorblind, contrast, flash, captions, motion, and readable-font toggles. That is not a store accessibility statement.
- Packet prose says asset licenses exist on disk and are not packaged. This leaf stayed off `assets/`.
- Headless only. The job peeled headed capture. Headless proof is the landing bar.
- Did not run `npm run credits`. That would rewrite a generated file this leaf does not own.
- Checkpoint finished. `NOW.md` and the queue were left for the live orchestrator / integrator.

## Proof

```text
PQ-033.00 title: Credits -> credits
PQ-033.00 notice: three.js MIT
PQ-033.00 notice: Rapier Apache-2.0
PQ-033.00 LICENSE: missing, not invented
PQ-033.00 NOTICE: leftover-extracted
```

`node --test test/pq-033-00-credits.test.mjs` — 3 pass, 0 fail.

## Completion addendum — 2026-09-11 (devin pq-033-00)

```text
STATUS           packaged build contains notices — DONE
                 three.quarks notice — closed
                 credits reachable from title — still DONE
                 root LICENSE — still missing, not invented (contract)
                 privacy / a11y statements — a11y in Settings Access;
                 privacy stays owner store/policy copy

WHAT I CHANGED   The packaged files list now carries NOTICE.
                 credits.js was regenerated by the repo's own
                 writer so the shipped three.quarks dependency has
                 a notice; NOTICE gained its MIT text.

THE NUMBERS      packaged notices | files list omitted NOTICE | NOTICE in files list + verified inside app.asar | packaged build contains every required notice
                 three.quarks notice | absent (live dep unlisted) | three.quarks 0.17.1 MIT in credits.js + NOTICE | shipped deps noticed
                 leaf test | 3/3 | 3/3 after regen | green
```

Proof: `npm run build:bundle` clean, then `electron-builder --dir`
produced `dist/win-unpacked/resources/app.asar`; `\NOTICE` is listed
at the archive root and byte-extracts to the updated file
(`@electron/asar extractFile`, 8790 bytes, names three.js,
three.quarks, Rapier, Floating UI, OFL). Entry `check:baseline`
was 11/15 green with pre-existing reds (pq020-ceres-topology,
save-schema, sim, sim-v3 — foreign dirty-tree drift, not touched).

Files this pass: `package.json` (files += NOTICE),
`src/data/credits.js` (regenerated), `NOTICE` (+three.quarks),
`design/program/roadmap/program-queue.json` (this leaf → done),
this receipt. Foreign hunks in `package.json` (three.quarks dep,
check:gamepad:screens script) preserved.

Remaining named gaps, unchanged by intent: no root LICENSE (the
contract forbids inventing one); player-facing privacy and store
accessibility copy stay with the owner / `.03` store readiness.
