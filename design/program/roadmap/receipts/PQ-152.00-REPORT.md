<!-- LIFETIME: RECEIPT -->
# PQ-152.00 — Three physical procedural types

```text
STATUS DONE

WHAT I FOUND     Boards rolled ten fly-there verbs. Tow, knock-down, and pull
                 were not types. Physical play lived in optional clauses.
                 no_slack still applied only to bulk_haul. Seed 15200 posted
                 none of tow_recovery, demolition, or rescue_under_fire.

WHAT I CHANGED   Those three types joined the catalog. Station weights ride as
                 named keys on the existing ten-column OFFER_MIX so Charon and
                 Tethys keep their hunter / junction pins. Each contract has
                 two physical finishes. Heist stays last with structural zero.

WHAT YOU WILL FEEL   Belt Outpost posts "Tow the slag core" and "Knock down the
                 tower". Forge posts "Pull the pods out". You finish a tow by
                 dragging the core into the yard or slinging it in. You finish
                 a demolition by putting mass through the tower or cutting it
                 down. You finish a rescue by towing a pod home or opening a
                 corridor and reeling from stand-off.

THE NUMBERS      bar | before | after | target
                 tow_recovery on seed 15200 boards | 0 | 8 (first Belt Outpost epoch 2) | appears
                 demolition on seed 15200 boards | 0 | 16 (first Belt Outpost epoch 1) | appears
                 rescue_under_fire on seed 15200 boards | 0 | 10 (first Forge epoch 3) | appears
                 tow methods | none | tow_in + sling_in | two solutions
                 demolition methods | none | wrecking_ball + cut_down | two solutions
                 rescue methods | none | stage_tow + corridor_pull | two solutions
                 OFFER_MIX positional length | 10 | 10 + named physical keys | hunter/junction pins hold
                 scenario | test/pq-152-00-physical-types.test.mjs | seed 15200 | headless

THE FRAMES       Headless board + completion proofs. No headed GPU strip.

NEXT             Integrator updates the queue. Do not mark the queue from this
                 receipt. PQ-152.01 is the ten authored set pieces.
```

## Offer status

| Type | Already offered? | After this leaf | Two methods |
|---|---|---|---|
| `tow_recovery` | no | yes — mining / fab / research / blackmarket | `tow_in` (latch + dock), `sling_in` (throw / clean release into the yard) |
| `demolition` | no | yes — same mix | `wrecking_ball` (whip or throw), `cut_down` (guns) |
| `rescue_under_fire` | no | yes — same mix | `stage_tow` (latch a pod + dock), `corridor_pull` (escorts down + reel) |

Civilian hunter hubs (`trade_hub`, `military`, `refinery`, `bounty_board`, `contracts_hub`) keep physical weight 0 so Charon / Tethys / Hunter cohort pins stay honest.

## Edited vs proved

Edited: `src/data/missions.js`, `src/data/missionConditions.js`, `src/systems/missions.js`, `test/pq-152-00-physical-types.test.mjs`, this receipt.

Proved: seed 15200 boards and both completion methods per type via `test/pq-152-00-physical-types.test.mjs` (5/5). `check-data` and `check-mission-receipts` green. `check:baseline` sim goldens are red on this shared dirty tree (Helios `trade_hub` physical weight is 0, so this leaf does not retune the 47-A envelope).

Not edited: `program-queue.json`, `NOW.md`, `329-curtain-convoy.js`, range/tether/sg02, aftermath/survivorPod, `tech.js`, `proofSixtySeconds.js`, `input.js`. No commit.
