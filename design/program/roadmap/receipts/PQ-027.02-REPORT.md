<!-- LIFETIME: RECEIPT -->
# PQ-027.02 — Weather that shapes fights

```text
STATUS DONE

DONE PQ-027.02 — A Veil storm sheet bends the shot and scoops a hauler onto
                 the rail. The belt dulls scan and stacks a well. Wait the
                 calm and the same line is still.

WHAT I FOUND     Veil and Vesta already had one storm sheet and one nebula
                 belt each. Surge moves mass. Calm does not. The belts are
                 named radiation belts and typed nebula.

WHAT I CHANGED   Nothing in the live weather. This leaf adopted that leftover
                 and re-ran the named seed-2702 Rapier proof. The receipt
                 now splits bend from toward-rail and names the leftover
                 sector radiation that was left alone.

WHAT YOU WILL FEEL   Fire through the Veil storm and the round leans onto
                 the current. Sit in the belt and the scan goes dull. A
                 hauler on the edge is shoved onto the rail. Wait, and the
                 same line is quiet. This is mass, not a hull-drain aura.

THE NUMBERS      bar | before | after | target
                 kernel shot bend | leftover 50.85 wu | 50.85 wu | > 8 wu, named from motion
                 kernel toward rail | leftover 179.4 | 179.4 | > 40
                 live Rapier shot bend | leftover 48.91 wu | 48.91 wu | > 8 wu
                 belt scan scale (surge) | leftover 0.4 | 0.4 | sensors shrink
                 calm / inner hole scan | leftover 1 | 1 | schedule + hole
                 stacked well | leftover 69.2 → 194.1 | 69.2 → 194.1 | fields amplify
                 hauler lateral | leftover 36.0 → 17.0 (Δ −19.0) | same | traffic shoved
                 calm hauler delta | leftover 0.0 | 0.0 | calm does not shove
                 traffic AI-steers | leftover false | false | mass, not theater
                 storm / belt types | leftover debris_current / nebula | same | never radiation
                 hull drain | none on weather volumes; world only chews hull on type radiation | same | never an HP aura
                 scenario | seed 2702 | Rapier | test/pq-027-02-weather.test.mjs

THE FRAMES       Headless Rapier is enough. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-027.03 hangar jam is the
                 leftover (aperture data already live). Do not mark the
                 queue from this receipt.
```

## Files

- `design/program/roadmap/receipts/PQ-027.02-REPORT.md` — this receipt, numbers from this session
- Already live, not rewritten: `src/data/environmentalMachinery.js`, `src/systems/environmentalMachinery.js`, `src/systems/world.js`, `src/data/hazardLanguage.js`, `test/pq-027-02-weather.test.mjs`

Not edited: `src/systems/input.js`, `src/data/modules.js`, `src/data/fields.js`, `src/data/sectors.js`, `program-queue.json`. Later leftover in the same files (027.03 aperture) was not rewritten.

## Honesty gaps

- The weather already bent shots, shrank scan, stacked a well, and scooped a hauler. This leaf did not retune sheets, wells, cycles, or scan scale.
- Belts are named `veil_radiation_belt` / `vesta_radiation_belt` and role `radiation_belt`. Their hazard type is `nebula`. No `WEATHER_VOLUMES` row is typed `radiation`.
- The named proof does not boot `world`, so it cannot watch the hull-drain path. That path only chews hull when a zone type is `radiation`. Weather zones enter the sector as `debris_current` and `nebula`.
- Scan shrink is the helper `weatherScanScale` (0.4 in the belt annulus during surge). `world.js` multiplies POI range by that helper. The named proof does not boot a live POI identify.
- Traffic is scooped onto the rail by the sheet (`trafficSteersAround=false`). Not AI theater.
- Veil authored `trafficPerMin` is 0. The pin spawns a mule. Vesta still has ordinary traffic (9/min) and the same leftover weather.
- Leftover authored `type: 'radiation'` zones still exist on Veil (200, −200) and Vesta (−800, 500) in `sectors.js`. Those still chew hull. This leaf did not invent a weather radiation belt and did not retune those leftover sector rows.
- The same leftover files already hold 027.03 aperture. This leaf did not rewrite that later leaf.
- Parent table asked for a shipping-camera capture. The leaf packet and this job forbade headed capture. Proof is headless Rapier seed 2702.
- Browser play has fields on. The named proof forces the same flag so Node can see the shove.
- An older `test/environmental-weather.test.mjs` still exists. The named proof is `test/pq-027-02-weather.test.mjs`.

## Proof

| Bar | Surge | Calm |
|---|---|---|
| kernel shot bend | 50.85 wu | — |
| kernel toward rail | 179.4 | — |
| live Rapier shot bend vs calm | 48.91 wu | shot lateral 36.0 |
| live shot travel / lateral | 115.8 / 12.9 | 105.1 / 36.0 |
| belt scan scale | 0.4 | 1 (also 1 in the inner hole) |
| stacked well | 69.2 → 194.1 | — |
| hauler lateral | 36.0 → 17.0 (Δ −19.0) | Δ 0.0 |
| storm live | true | false |
| types | storm `debris_current`, belt `nebula` | same |

`node --test test/pq-027-02-weather.test.mjs` — 2 pass, backend `rapier-dynamic`.

## 2026-09-09 — player-started fight (Lane I)

```text
DONE  PQ-027.02 — The player's own round misses a wasp on the rail in the calm, and the Veil storm bends that same round onto the wasp when the surge is up. The belt still dulls scan. Neither is a hull-drain aura.
WHAT I FOUND     The leftover storm already bent spectator shots and scooped haulers. No proof showed the player firing, and nothing showed the best shot changing with the window.
WHAT I CHANGED   The storm and belt still move mass the same way. The player now hears remaining seconds on the hazard language, and a named Rapier fight has the player fire the round.
WHAT YOU WILL FEEL   Stand in the Veil storm and fire down the offset lane: wait the calm and the round stays on your aim and misses the hull on the rail; fire in the surge and the sheet throws your round into them. Sit in the belt and scan still goes dull. What still is not: I did not look at a shipping-camera still, and leftover authored radiation zones on Veil/Vesta still chew hull.
THE NUMBERS      seed 2702, rapier-dynamic, profile production
                 bar | before | after | target
                 player-fired calm miss | leftover spectator, no player round | 36.04 wu | miss — offset shot is useless
                 player-fired surge miss | leftover spectator | 14.70 wu (shot lat 36.0 → 2.8) | storm finishes the player's shot
                 remaining window | leftover phase only | surge 5.58 s on diagnostics and hazardRead | readable
                 player in the volume | spectator pose | true, ownerId=1 | player started it
                 kernel shot bend | leftover 50.85 wu | 50.85 wu | > 8 wu
                 live Rapier shot bend | leftover 48.91 wu | 48.91 wu | > 8 wu
                 belt scan | leftover 0.4 | 0.4 | sensors shrink
                 stacked well | leftover 69.2 → 194.1 | same | fields amplify
                 hauler lateral | leftover 36.0 → 17.0 | same | traffic shoved
                 types | leftover debris_current / nebula | same | never radiation
CHECKS           npm run check:baseline — FAIL entry 12/15 and exit 12/15. Same three reds: pq020-ceres-topology, sim-v3, sim. Hashes unchanged. Nothing this unit added.
                 node --test test/pq-027-02-weather.test.mjs test/pq-027-03-timed-access.test.mjs — PASS 6/6
                 node scripts/check-hazard-language.mjs — PASS
UNPROVEN         I did not take a shipping-camera still. I did not fly Veil in a headed session. The wasp is a parked hull, not a live gunfight. Scan shrink is still the helper, not a live POI identify. Leftover sector `type: radiation` rows still chew hull.
```

FILES            `src/data/environmentalMachinery.js`, `src/data/hazardLanguage.js`, `src/systems/environmentalMachinery.js`, `test/pq-027-02-weather.test.mjs`, this receipt.
