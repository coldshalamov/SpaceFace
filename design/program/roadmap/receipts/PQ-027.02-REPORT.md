<!-- LIFETIME: RECEIPT -->
# PQ-027.02 — Weather that shapes fights

```text
STATUS DONE

WHAT I FOUND     Veil and Vesta already had one storm sheet and one nebula
                 belt each. Surge bends shots and scoops traffic onto the
                 rail. The belt shrinks scan and stacks a well. Calm is still.

WHAT I CHANGED   Nothing in the live weather. A named seed-2702 proof walks
                 the kernel and a live Rapier shot/hauler.

WHAT YOU WILL FEEL   Fire through the Veil storm and the round leans onto
                 the current. Sit in the belt and the scan goes dull. A
                 hauler on the edge is shoved onto the rail. Wait, and the
                 same line is quiet. This is mass, not a hull-drain aura.

THE NUMBERS      bar | before | after | target
                 kernel shot bend | 50.85 wu toward rail | same | blind reviewer names the current
                 live Rapier shot bend | 48.91 wu | same | same curve on physics
                 belt scan scale (surge) | 0.4 | same | sensors shrink
                 calm / inner hole scan | 1 | same | schedule + hole
                 stacked well | 69.2 → 194.1 | same | fields amplify
                 hauler lateral | 36.0 → 17.0 (Δ −19) | same | traffic moved
                 traffic AI-steers | false | same | mass, not theater
                 storm / belt types | debris_current / nebula | same | never radiation
                 scenario | test/pq-027-02-weather.test.mjs | seed 2702 | Rapier

THE FRAMES       Headless Rapier is enough. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-027.03 hangar jam is the
                 leftover (aperture data already live). Do not mark the
                 queue from this receipt.
```

## Files

- `test/pq-027-02-weather.test.mjs` — named proof

Not edited: `src/data/environmentalMachinery.js`, `src/systems/environmentalMachinery.js`, `src/data/hazardLanguage.js`, `program-queue.json`.
