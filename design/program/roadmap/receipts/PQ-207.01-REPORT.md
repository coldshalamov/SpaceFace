<!-- LIFETIME: EVIDENCE -->
# PQ-207.01 — One mechanic, one voice, room to read — REPORT

```yaml
unit: PQ-207.01
parent: PQ-207
state: implemented
acceptance: focused contract test green; merge decided at the presenter boundary
date: 2026-09-19
implementer: devin-cli
reviewer: independent read-only agent (adversarial trace of consumers, card shape, CSS routes, test quality) — clean; mechanism correction + kept nits recorded below
integratedCommit: b2b7a8442
```

## Status: DONE — one Mechanic header carries both channels

The berth bulletin mounted two sibling articles that both introduced themselves as `Mechanic`:
`leftoverLedgerCard` (the campaign-ledger recap: "I was the one who filed…") and
`leftoverMechanicCard` (the hull verdict: "Hard scar on the bow…").

The decision lands at the presenter boundary in `buildDockArrival` (`src/ui/dockArrival.js`): the
two readers stay independent, but when both have something to say the hull article carries the
ledger recap as a second sentence under one header, badge `HULL · LEDGER`, hull verdict first.
`view.ledger` is `null` in that case, so no second name plate mounts. When the ledger is the only
voice on the berth it still paints its own `Mechanic` article — nothing is hidden, nothing is
invented for a hull with no scars.

**Correction to the packet's mechanism (verified in review):** the "narrow-breakpoint header chip"
frame in the done-when describes retired CSS — the wrapping-chip tape is gated behind
`.sx-berth:not(.sx-observatory)`, a combination no code path produces; on the live route the
articles stack inside the collapsed `.so-bulletin` disclosure as grid cards at every breakpoint.
The real defect and fix are unchanged — two adjacent `Mechanic` name plates in the bulletin became
one — but the crowding claim should be read as "the duplicate speaker article is gone," not "a
chip stopped wrapping." `styles/orbital.css` needed no change for the same reason.

## Done-when

| Claim | Evidence |
|---|---|
| The double `Mechanic` article is resolved | `view.mechanic` carries hull text + ledger recap; `view.ledger` is `null`; the ledger DOM article stays `hidden`; badge reads exactly `HULL · LEDGER` — pinned in `test/pq-150-01-mechanic.test.mjs` |
| …and the narrow-breakpoint header no longer crowds the stage | the duplicate article is gone outright — on the live route the articles stack inside the `.so-bulletin` disclosure (the packet's chip-tape frame is retired CSS, `.sx-berth` is always `sx-observatory`); no CSS touched, no content lost |
| Ledger-only still works | `paintMechanic(leftoverLedgerState())` paints the ledger article with `Mechanic` title and the "I was…" body; `painted.mechanic` is `null` |
| Hull-only still works | unchanged — `mechanicCard` alone emits exactly as before |

## Files

- `src/ui/dockArrival.js` — merge in `buildDockArrival` (~14 lines); the only consumer of both
  cards. `src/ui/views/stationFrames.js`, `styles/orbital.css` needed no change: the berth frame
  mounts `view.ledger` conditionally and the hidden article already collapses.
- `test/pq-150-01-mechanic.test.mjs` — the old "sits beside" test rewritten to pin the one-speaker
  contract plus the ledger-only fallback.

## Checks

| Check | Result |
|---|---|
| `node --test test/pq-150-01-mechanic.test.mjs` | 6/6 |
| `node --test test/pq-207-00-berth-mechanic-cost.test.mjs` | 12/12 (PQ-207.00 cost pin unaffected) |
| Dock/station test family sweep | 3 foreign reds (`dock-source-contract` GLB hash/map drift, `first-hour-station-quiet` mission-kind naming) — neither imports `dockArrival.js`; fail identically without this change |

## For the next engineer

- The merge is a **presentation** decision: `story/mechanicVoice.js` and `story/storyLedger.js`
  still answer their own questions; only `dockArrival.js` decides how many name plates paint. If a
  future layout wants them separate again, the flag is one place, not two readers.
- The write set named `stationFrames.js`/`orbital.css` — they turned out clean because the frame
  already treats `view.ledger` as optional. Recorded here so the next reader doesn't hunt for a
  CSS fix that doesn't exist.
- Review nits kept, not fixed (out of scope, defensible): a clean-hull merge reads "Clean plate.
  Nothing on this hull to file. I was hitching…" — nothing on the *hull*, the recap is the
  mechanic's own story; the merged card keeps `kind:'mechanic-hull'` (no consumers key on it);
  `stationApp.js:999`'s comment describes the ledger article, now an edge case (ledger-only states
  still paint it).
- Pre-existing test gap found by review (not this leaf): the live `leftoverTraceHost` discovery
  path (parentElement querySelector) is never exercised against real markup — every test passes
  elements explicitly.
