<!-- LIFETIME: ACTIVE_RECEIPT -->
# PQ-137.05 — Weapons move ships; fresh Crucible starts with a shove

Integrated in `0fd8edf1` and `a621df37`, recovered from terminal Grok candidate `8f55489f`.

The starter Pulse now delivers 5% of a light hostile's cruise speed per hit, the mine 45%, and the concussion shove 54.526%. At two seconds the shove puts the victim 1.0083 screen depths off its former line. Eleven real held-Pulse shots all hit; their cumulative causal velocity change is 42.5035% of cruise. Damage and firing rate were preserved.

The deployed mine incorrectly acquired a solid Rapier body despite being a proximity sensor. At exact overlap this could eject the target millions of world units. It now remains a sensor and delivers its blast through the existing impulse authority.

Fresh Crucible starts with the existing physics toolkit (Hornet with concussion and rail). Explicit loadout choices, including the Pulse-only energy kit, remain available and are remembered exactly on return.

Validation: 65 focused candidate tests and impulse authority passed; 15 integrated force/mine tests and 28 Crucible launch/results tests passed. Seed 4242 repeated identically. The original baseline was already red in sim-v3 before these changes; no golden was changed.

## THE FRAMES

`design/program/roadmap/receipts/fun-loop/manifests/crucible/3f560ea5-dirty-8f437213/shove_light-physics_toolkit-s4242/`
— the `shove_light` tape, seed 4242, `physics_toolkit`, the real Crucible click-through (title →
Crucible door → hull/seed → "Hold the line"), shipping chase camera, default quality, HUD text off and
verified clean at both ends of the run, 124 retained frames spanning sim 32.93 s → 50.27 s, 5.71
delivered fps, zero page errors, drawn by a real GPU ("ANGLE (Intel, Intel(R) Graphics (0x00007D45)
Direct3D11 …)"). Source identity: gitHead `3f560ea5`, gitTree `25a2d44f`, production dirty, diff hash
`8f437213…`. **`normalSpeed: false` — 0.561 of real time against the 0.60 floor**, and that is the
machine, not the instrument: the manifest's `realtimeStages` show the game at **0.495** with the
debugger attached and nothing encoding, because three other lanes were running headed browsers on this
box at the time (the CPU sampled 76–100 % throughout). Three further attempts were made — two never
reached the settle gate at all and one never got the hostile hulls admitted to the frame in 241 s.

**What a viewer sees.** The Hornet sits nearly still (`playerSpeed` 0.00–0.45 WU/s for the whole
strip; this tape holds no throttle), sweeps its nose left, right, left, right, and holds the trigger
on the nearest live hostile through five bursts — **22 shots fired**, the times stamped in
`inputEvents` (fire held at sim 33.33, 37.55, 42.03, 44.62, 47.72). Six to ten hostiles are on the
glass at all times. Inside the photographed window the room answers with **31 moments**: 15
`combat:collisionConsequence`, 13 `physics:impact` and **3 kills** — sim 34.73 (surface `structure`),
38.07 and 44.03 (`craft`) — of which four consequences are ones the player was in (34.17, 36.33,
36.93, 37.53). Frames 26–36 (sim 36.2–37.5 s) are the loudest beat: a struck hull inside an orange
bloom with hulls scattering around it. B13's own reading of the same frames is clean —
`visibleJitter` measures 4 contact windows at ≥ 6.43 fps with **0 heading reversals** and 1 screen
reversal.

**What these frames do not carry.** They are evidence that the shove is fired and lands on the
default route and that the room moves when it does. They are **not** the screen-depth number: the
manifest records the *player's* screen position, never each victim's, so "1.0083 screen depths at two
seconds" cannot be read off it — and at 0.561 of real time no timing claim may be read off the
pictures at all (`CANONICAL_BUILD_MAP.md` §1.3 law 2). The 54.526 %-of-cruise shove and the
screen-depth figure remain the bench's numbers, measured headless. Closing that clause from pictures
needs a per-victim screen track in the manifest, which is a change to
`scripts/lib/bench/frameStripCapture.mjs` outside the `STRIP_SCENARIOS` block this lane owns.

**Not this lane's:** the other open clause on this unit — the 20-seed Crucible kit-score ordering —
is a balance question, not a capture. Nothing here touches it and it stays open.

State: implemented, broader route acceptance open. The 20-seed kit score ordering is not established; the earlier two-kit comparison did not clear a wave. The later nine-case swarm baseline is preserved separately for PQ-174. Normal-speed visual acceptance remains open. This receipt does not close the swarm balance packet.
