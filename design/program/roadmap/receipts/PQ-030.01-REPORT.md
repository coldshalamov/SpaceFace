<!-- LIFETIME: RECEIPT -->
# PQ-030.01 — Transverse snare clothesline

```text
STATUS DONE

WHAT I FOUND     The leftover clothesline already lived in
                 masslineSnares.js. A full-burn crossing already
                 rebound onto the kept peg and published the B11
                 hitstun law. Slack, along-line, friendly, and
                 missed contacts already passed through. Hitch
                 already has an S utility; the head is M. The
                 leftover test already named the B11 second. It
                 did not pin Hitch.

WHAT I CHANGED   I kept that leftover snare. I did not rewrite it
                 and I did not fork a second snare into
                 masslineThreats.js. tetherGameplay already routes
                 the press; I left it alone. The leftover test now
                 also refuses Hitch. This receipt is new.

WHAT YOU WILL FEEL   Stretch a line across a lane. A pursuer at
                 full burn hits it, loses the stick, and yanks
                 onto the peg you kept. A slack or friendly pass
                 is just empty air. Hitch never wears this toy.

THE NUMBERS      bar | before | after | target
                 B11 k | leftover 1.000 at 105/105 | same | ≥ 0.30
                 B11 helm | leftover 3.500 s | same | ≥ 1.00 s
                 cruise | wasp-class 105 wu/s | same | full burn
                 slack / along-line | leftover 0 rebinds | same | no yank
                 friendly / station | leftover 0 rebinds | same | no yank
                 Hitch fit | S utility vs M snare | same refuse | do not put the head on Hitch
                 scenario | massline-transverse-snare-head | headless | leftover test

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     The catch uses the leftover fake attachment
                 create/rebind, not a Rapier-flown default-route
                 crossing. tumbleStates still reads the process
                 COMBAT_FLAGS map, so the B11 test pins that map
                 ON; production already has it ON, 47-A does not.
                 Press routing stays in leftover tetherGameplay
                 and was not re-proven here.

NEXT             Integrator updates the queue. PQ-030.02 is the
                 tether-cutter specialist. Do not mark the queue
                 from this receipt.
```

## Files

- `src/systems/masslineSnares.js` — leftover clothesline, not rewritten
- `test/massline-transverse-snare-head.test.mjs` — leftover B11 proof plus Hitch refuse
- `design/program/roadmap/receipts/PQ-030.01-REPORT.md` — this receipt

Not edited: `modules.js`, `tetherGameplay.js`, `masslineThreats.js`, `range.js`, `input.js`, `npcJobsRuntime.js`, `program-queue.json`.

## 2026-09-10 late addendum (worker rewrite, merged — nothing above was replaced)

A late-waking worker rewrote this receipt instead of appending to it. Everything above is the
committed receipt, restored in full; everything the rewrite added is preserved verbatim below. Both
versions read DONE, so there is no status conflict on this leaf.

**What the rewrite added that is real and new.** It reports momentum work in
`src/systems/masslineSnares.js` that landed after this receipt was written — `git log -S` puts the
catch/yank coupling in `578244b22`, and this receipt was last committed in `8afa7c519`. The code is
committed and clean on disk: the catch now dumps the crossing into a shove against the lane plus a
yank toward the kept peg, without writing or freezing the ship's speed. New measured rows — 1 catch
at seed 30001 on the first crosser, catch ΔV 48.750 wu/s against the crossing, yank ΔV 26.250 wu/s
toward the kept peg, pursuer vx 105 wu/s kept and never written, hull 100 unchanged. It also names a
new test `test/pq-030-01-snare.test.mjs` (tracked and clean), adds CHECKS and UNPROVEN sections, and
adds one new disclosure: `masslineThreats.js` was read and left alone so a snare would not paint as
a swing warning.

**What the rewrite deleted, and is restored above.** The title word "clothesline"; the leftover-work
detail in WHAT I FOUND and WHAT I CHANGED — that the snare already lived in `masslineSnares.js`,
that it was not rewritten, that no second snare was forked into `masslineThreats.js`, that
`tetherGameplay` already routes the press, and that the leftover test already named the B11 second
but did not pin Hitch; the `cruise`, `slack / along-line`, `friendly / station`, `Hitch fit` and
`scenario` rows of THE NUMBERS; the `THE FRAMES` line; the `NEXT` line; the whole `## Files`
section, including the leftover proof `test/massline-transverse-snare-head.test.mjs` (still present
and unmodified on disk) and the seven-file "Not edited" list; and — most costly — the whole
`HONESTY GAPS` section. The rewrite's UNPROVEN restates only the first of those three gaps (the
fake attachment create/rebind rather than a Rapier-flown default-route crossing). The other two were
dropped outright: that `tumbleStates` still reads the process `COMBAT_FLAGS` map, so the B11 test
pins that map ON — production already has it ON, 47-A does not — and that press routing stays in
leftover `tetherGameplay` and was not re-proven here.

Worker's text, verbatim (its status line prefixed so it cannot be read as this receipt's status):

```text
[worker status line — agrees with the receipt above] DONE  PQ-030.01 — a full-burn pursuer hits the clothesline, gets yanked toward the peg, and loses the stick for a B11 second
WHAT I FOUND     The leftover snare already rebound a fast crosser onto the kept peg and published hitstun; it did not turn the crossing speed into a yank or a spin of its own.
WHAT I CHANGED   The catch still rebinds one line, and now dumps the crossing into a shove against the lane plus a yank toward the peg, without writing or freezing the ship's speed.
WHAT YOU WILL FEEL   Stretch a line across a lane. A pursuer at full burn hits it, keeps their speed, gets yanked toward the peg you kept, and loses the stick for more than a second. Slack, friendly, and along-the-line passes are still empty air; Hitch never wears this toy.
THE NUMBERS      bar | before | after | target
                 catch | leftover rebind | 1 catch, seed 30001 | first crosser
                 B11 k | leftover 1.000 at 105/105 | 1.000 | ≥ 0.30
                 B11 helm | leftover 3.500 s | 3.500 s | ≥ 1.00 s
                 catch ΔV | 0 queued | 48.750 wu/s against the crossing | convert linear momentum
                 yank ΔV | 0 queued | 26.250 wu/s toward the kept peg | yank into the anchor
                 pursuer vx | 105 wu/s | 105 wu/s kept (never written) | never clamp given momentum
                 hull | 100 | 100 | hit points never scale
                 seed | 30001 | 30001 | fixed
FILES            src/systems/masslineSnares.js
                 test/pq-030-01-snare.test.mjs
                 design/program/roadmap/receipts/PQ-030.01-REPORT.md
CHECKS           npm run check:baseline | FAIL (pre-existing: pq020-ceres-topology, sim-v3, sim; budget) — not this unit
                 node --test test/pq-030-00-sweep.test.mjs test/pq-030-01-snare.test.mjs | PASS
UNPROVEN         The catch harness uses a fake attachment rebind, not a Rapier-flown default-route crossing. NPC counterplay is leaf .02. masslineThreats.js was read and left alone so a snare would not paint as a swing warning.
```
