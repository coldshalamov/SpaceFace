<!-- LIFETIME: RECEIPT -->
# PQ-152.00 — Re-close 2026-09-22 (stale weight pins vs the heartbeat retune)

The original receipt (`PQ-152.00-REPORT.md`, commit ccf2894b9) implemented the leaf
and proved 5/5, but the queue was never flipped. The mid-game heartbeat (b65ad1f40)
then deliberately expanded physical-work weights onto civilian station types
(trade_hub tow 0→2, military demolition 0→2, refinery 0s→3/1/1) so boards stop
serving starter repeats, keeping the special boards (bounty, contracts hub) at
zero. That left one test red: `OFFER_MIX.trade_hub.tow_recovery` pinned 0, data
says 2.

Adjudication: the heartbeat values stand. The zeros were initial-implementation
caution, never the done-when (the done-when names no zeros). The heartbeat
commit itself recorded `check:baseline` 15/15 green with the nonzero weights;
the Tethys nexus pins pass 3/3 with them; the m3-hunter and charon failures on
this tree are reward-base/salvage-vs-refinery issues, not mix weights. The two
stale zero-pins were updated to the heartbeat values with the commit cited
in-line — test maintenance against a deliberate redesign, not a golden moved
to pass.

Proved this session: `test/pq-152-00-physical-types.test.mjs` 5/5 on seed
15200 (catalog + mix, all three types on route boards, tow_in + sling_in,
wrecking_ball + cut_down, stage_tow + corridor_pull);
`check:mission-receipts` OK; `check:playable` 16/16. `check:baseline` sim
goldens stay red from concurrent combat/economy dirt (47-A tape, telemetry
hash); the sim path imports none of this leaf's files.

Queue: PQ-152.00 ready → done.
