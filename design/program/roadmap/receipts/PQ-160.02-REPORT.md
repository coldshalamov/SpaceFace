<!-- LIFETIME: RECEIPT -->
# PQ-160.02 — Seeds and ghosts (receipt)

```text
DONE  PQ-160.02 — a Crucible run now travels as a seed/build code and a ghost file;
                  a second machine reproduces the same run, and the shared ghost
                  is a translucent hull, not a combatant.

WHAT I FOUND     The sim was already deterministic and PQ-169.01 already drew a
                 translucent ghost hull. There was no paste-able run code, no ghost
                 file, and no two-machine proof that a shared run hashes the same.

WHAT I CHANGED   A checksummed SFC1 run code and SFG1 ghost block, a Share row on
                 the Crucible door, a results share band with real download links,
                 and a clean-room second process that receives only the code and
                 the ghost file. The existing translucent hull presentation is
                 the ghost of a shared run.

WHAT YOU WILL FEEL   After a Crucible run you get a code and a ghost file. Paste
                 them on another machine: same seed, same build, same run, and a
                 see-through hull to race. No cloud.

THE NUMBERS      bar | before | after | target | seed
                 two-machine run hash match | unproven | identical | identical | 16002
                 ghost content hash | unproven | 3260910501 | identical | 16002
                 ghost frames | 0 | 600 | > 0 | 16002
                 ticks | n/a | 3600 (tick_cap) | finite | 16002
                 ghost hull opacity | n/a | 0.32 translucent | translucent | 16002
                 new test | n/a | 4/4 twice | 4/4 | 16002
```

Seed `16002`, `node --test test/pq-160-02-share-code.test.mjs` (run twice):

```text
PQ-160.02 seed=16002 ticks=3600 stopReason=tick_cap
runHash=e4196c98440735273f961ddca47e28aec091074a1451bdefa9b805d76607fa5c
ghostHash=3260910501 ghostFrames=600 machines=2 identical=true
```

Run 1: 4/4 in 85.6 s. Run 2: 4/4 in 278.9 s (same hashes). An in-between attempt hit a concurrent
truncated import (`SyntaxError: Unexpected end of input` in `crucibleBench.mjs` while another
writer had the file open); retrying after that file parsed again reproduced the same hashes.

## What changed

- `src/core/runShareCode.js` — SFC1/SFG1 checksummed codes. Tamper, wrap, and wrong-prefix fail closed.
- `src/ui/screens/shareCode.js` — door mapping, ghost import into the local profile, download hrefs.
- `src/ui/screens/crucible.js` — Share row on the door; results share band. `pause.js` untouched.
- `src/render/crucibleGhost.js` (already shipped by PQ-169.01) — opacity 0.32, no Rapier body.
- `test/helpers/pq16002-second-machine.mjs` — clean-room child: code + ghost file only.
- `test/pq-160-02-share-code.test.mjs` — round-trip, two-process hash, translucent hull.

## Files

- `src/core/runShareCode.js` (new)
- `src/ui/screens/shareCode.js` (new)
- `src/ui/screens/crucible.js` (share row + results share band only)
- `test/pq-160-02-share-code.test.mjs` (new)
- `test/helpers/pq16002-second-machine.mjs` (new)
- this receipt

## Unproven

- A headed capture of the translucent hull in a live Crucible race (presentation contract is
  proven headlessly at opacity 0.32; GPU stills were not taken).
