<!-- LIFETIME: RECEIPT -->
# PQ-153.02 — Landmarks fielded

```text
DONE      PQ-153.02 — six landmarks are reachable on the live route
                      and the still review ran at the shipping camera.

WHAT I FOUND     The six sectors already named hero landmarks. Reachability
                 was unmeasured; the still review (zcode-15302-stills) found
                 two composition failures at the shipping camera, and the
                 full six-sector vision round (2026-09-26) found four more —
                 the earlier pass judged only the two reseated sectors.

WHAT I CHANGED   A headless census on seed 15302 walks the live place table
                 and prints reachable=yes for each. The still review ran and
                 reseated the Skerris Throne onto the Bazaar approach and the
                 Wreck Cathedral beside its foundry (commit 8ba0a9786); the
                 follow-up census leg proves a live landmark entity stands in
                 each sector on full arrival (1cda39c5c).
                 The 2026-09-26 vision round then judged all six and fixed
                 what failed: the Quiessence's 17 carrier hulls render the
                 shared dead-hulk stand-in at half scale (they were scanner
                 identities only — the landmark was one buoy and bare space,
                 src/systems/world.js); the capture parks Ceres at the wreck's
                 verified-visible southern approach (0,-150), Vesta from the
                 north so the tall gantry fits, and the Tethys landmark anchor
                 at the skim band (r=1040) so the Anvil fills the frame
                 (scripts/lib/sectorAnchors.mjs). The capture tool now waits
                 for each landmark's authored asset admission and reports
                 landmarkAdmitted per sector, treats only collides:true bodies
                 as clearance obstacles, and never re-emits camera:zoom at the
                 boot-default level (the redundant emit resets zoom-transition
                 state and the authored-upgrade runway then denies the streamed
                 body for minutes — isolation probe 2026-09-26).

WHAT YOU WILL FEEL   The six named landmarks exist as places you can be
                     put at, and each reads placed on the approach framing,
                     not adrift in bare space — five of six judged PASS by a
                     blind vision reviewer at the shipping 144 WU zoom; the
                     Ceres cathedral renders and composes at the hull edge,
                     but its read at gameplay zoom is flat slabs: art/scale
                     quality beyond composition is the separate open D54
                     call.

THE NUMBERS      bar | before | after | target | seed
                 reachable | unmeasured | 6/6 | 6/6 | 15302
                 still review | unmeasured | 5/6 judged PASS, Ceres = D54 | pass | 4242
                 Ceres wreck rendered in capture | never | yes | yes | 4242

CHECKS           node --test test/pq-153-02-landmarks.test.mjs — 3/3
                 (census 6/6 reachable + gate-order live-entity leg),
                 re-verified 2026-09-26; with
                 test/depth-program-a1-physical-actors.test.mjs and
                 test/quiessence-census.test.mjs — 10/10 green.
                 npm run check:baseline — 16/16 green.
                 npm run check:atlas-integrity — PASS.
                 npm run check:playable — FAIL for an unrelated reason: the
                 shared tree's authored ship-asset library preload 404s
                 (another lane's in-flight write to assetLoader/assetResidency
                 landed after 2026-09-26 ~10:00; the game refuses flight with
                 the menu stuck). None of this unit's files touch the ship
                 preload path.

REVIEW           Subagent code review APPROVED (scope confined to the unit's
                 four files, entity contracts held, park rules stay mechanical);
                 its two cleanups applied.
```

## Files

- `src/data/sectors.js`
- `src/systems/world.js`
- `scripts/lib/sectorAnchors.mjs`
- `scripts/capture-sector-identity.mjs`
- `design/world-identity/sectors/sector_pallas_drift.md`
- `test/pq-153-02-landmarks.test.mjs`
- `design/program/roadmap/receipts/PQ-153.02-REPORT.md`
