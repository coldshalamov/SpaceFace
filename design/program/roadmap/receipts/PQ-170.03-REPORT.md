DONE  PQ-170.03 — Defeating an ace grants one legendary Massline head, one per promotion tier,
with lineage. Fit it and station NPCs bark who you took it from. A fourth ace grants nothing.

WHAT I FOUND     Ace promotion already capped at three tiers (`PIRATE_PROMOTION_MAX_TIER = 3`)
and named aces already existed, but a kill paid nothing unique and nobody recognized a trophy.
Unique wreck loot is a different channel; these heads are lineage grants, not wreck reservations.

WHAT I CHANGED
- `src/data/modules.js` — `unique_no_cut_filament`, `unique_toll_saint_bridle`,
  `unique_broken_ring_whip`: unique, not purchasable, salvageOnly, same `masslineHeadId` as the
  stock head they were taken from.
- `src/data/sectors.js` — `ACE_TROPHY_HEADS` plus lookups and `trophyFromFittings`.
- `src/systems/claims.js` — lazy `legendaryHeads` ledger; preferred `returnTier` then first unused
  1–3; once per ace; sealed-run skip; grant via `ships.grantModule`; news + toast.
- `src/data/conflictReactions.js` — `ACE_TROPHY_BARKS` for the eight station factions plus a
  fallback; `aceTrophyBarkFor` / `aceTrophyNewsLine`; `{ace|head}` tokens.
- `src/systems/barkDirector.js` — scan bark replaced with the trophy line when a head is fitted
  (reads `trophyFromFittings` from sectors.js, not claims — cycle risk).
- `test/pq-170-03-legendary-heads.test.mjs` — 3 tests.
- Depth-program loot audit treats the three ids as lineage uniques so they do not demand a wreck
  reservation (`scripts/lib/depthProgramLootAudit.mjs`). `unique-loot-reservations.json` is
  foreign dirty work and was not touched.

WHAT YOU WILL FEEL   Beat Yara No-Cut and you own the No-Cut Filament — the same monofilament
law, a unique id. Wear it and a Reach scanner names Yara and the head. Beat two more aces and you
hold all three. A fourth ace leaves your hold alone. Save, quit, Continue: the head and the
lineage are still there.

THE NUMBERS      bar | evidence (seed 17003)
one head per ace tier | 3 catalog rows; PIRATE_PROMOTION_MAX_TIER === ACE_TROPHY_TIER_MAX === 3
same physics law | each unique module shares `masslineHeadId` with its stock base
grant + lineage | Yara → unique_no_cut_filament tier 1; Toll-Saint with preferred 1 already taken → tier 2 bridle; Mako preferred 3 → whip
cap / once-per-ace | fourth ace null; repeat Yara returns the existing ledger row; sealed Crucible grants nothing
NPCs bark | Reach scan bark contains "No-Cut Filament" and "Yara No-Cut"; eight station factions authored; helix falls back
save round-trip | serialize/deserialize restores aceId + name; untraded shape omits legendaryHeads (schema 14 unchanged)

FILES
src/data/modules.js — three unique trophy modules
src/data/sectors.js — ACE_TROPHY_HEADS
src/systems/claims.js — legendaryHeads ledger + grant
src/data/conflictReactions.js — ACE_TROPHY_BARKS
src/systems/barkDirector.js — scan recognition
test/pq-170-03-legendary-heads.test.mjs — 3 tests
scripts/lib/depthProgramLootAudit.mjs — lineageUniqueIds channel

CHECKS
node --test test/pq-170-03-legendary-heads.test.mjs   PASS 3/3
node --test test/depth-program-gt1-loot-audit.test.mjs   PASS 9/9
npm run check:faction-standings   PASS
npm run check:save-schema         PASS (version 14, 288 paths)
node scripts/check-program-docs.mjs  PASS (2 warnings: NOW.md header, pre-existing)

LEFT OUT / UNPROVEN
- Headless only. Nobody heard the bark in a live sector.
- Trophy heads are not wreck loot and are not in `unique-loot-reservations.json` (foreign dirty).

RESULT: DONE
