<!-- LIFETIME: RECEIPT -->
# PQ-149.02 — Guaranteed ordinary life

```text
STATUS DONE

WHAT I FOUND     Quiet already blocks combat with reason `rhythm`
                 (PQ-149.00). Seed 14920 at the Ceres refinery pocket
                 already runs inspections, transfers, repair, waiting,
                 and a slow tug during that quiet window. No second
                 director was needed.

WHAT I CHANGED   Nothing in the directors. The leaf was already true
                 on the existing traffic and station-side events.
                 A 300 s headless sim on seed 14920 is the proof.

WHAT YOU WILL FEEL   A quiet dock still has a patrol holding, a hauler
                 unloading, a tender on a call, hulls waiting, and a
                 tug with a load on the line. Nobody starts a fight
                 to keep the field busy.

THE NUMBERS      bar | before | after | target
                 routine behaviours in quiet | 5 (already) | 5 | ≥ 4
                 names | inspection, transfer, repair, waiting, tug | same | those five
                 quiet window | 91 s of 300 s | 91 s | a quiet-phase window
                 first seen (s) | 0.02 / 2.0 / 0.02 / 0.02 / 11.0 | same | during quiet
                 combat fire in quiet | 0 | 0 | none
                 activity receipts in quiet | 10 | 10 | telemetry confirms
                 seed | 14920 | 14920 | 14920
                 backend | rapier-dynamic | rapier-dynamic | rapier-dynamic
                 scenario | test/pq-149-02-ordinary-life.test.mjs | headless 300 s
                 frames | none | none | skip headed Chromium

THE FRAMES       Headless 300 s sim with live activity census and
                 telemetry. No headed GPU strip.

NEXT             Integrator updates the queue. PQ-149.03 is story so
                 far. Do not mark the queue from this receipt.
```

## Files

Proved, not edited:

- `src/systems/encounterDirector.js` — quiet still defers combat with `'rhythm'`
- `src/ai/director.js` — quiet still holds tactical RESPITE
- `src/systems/stationSideEventDirector.js` — budgeted launches still defer; cosmetics may run
- `src/systems/shipLedger.js` — untouched
- `src/systems/telemetry.js` — existing `rhythm:phase` ring; no new director

Written:

- `test/pq-149-02-ordinary-life.test.mjs` — seed 14920, 300 s quiet-window census
- `design/program/roadmap/receipts/PQ-149.02-REPORT.md`

Not edited: `program-queue.json`, `NOW.md`, `input.js`, `proofSixtySeconds.js`, `range.js`, `tetherGameplay.js`, `aftermathWrecks.js`, `tech.js`, `missions.js`, `heat.js`, `lawSecurity.js`.
