<!-- LIFETIME: RECEIPT -->
# PQ-149.01 — Escalation seeds

```text
STATUS DONE

WHAT I FOUND     Rhythm was live and uncommitted. Witness, spill, and flee
                 already made the world flinch. Nothing queued a later beat
                 with a place, a delay, and a cause the ledger could read.
                 Seed 14910 before the wire: 0 cause-cited escalations.

WHAT I CHANGED   The encounter director now listens to those three tokens
                 and keeps a sixteen-deep seed queue. Witness becomes a
                 bounty, spill a shortage, flee a rumor (or an ace if a
                 named hull ran). Each seed waits, then arrives from a
                 zone — never on the player. The ship's ledger cites the
                 cause. Telemetry prints the seed and the arrival. The
                 tactical director will not stack a reinforce on top of a
                 hunt that is already coming.

WHAT YOU WILL FEEL   Kill in front of a patrol, dump a hold, scare a
                 hauler off. Later a bounty, a shortage, and a rumor show
                 up from somewhere else, and the book says why.

THE NUMBERS      bar | before | after | target
                 cause-cited escalations | 0 | 3 | ≥ 3
                 seed | 14910 | 14910 | 14910
                 delays | — | 8 / 12 / 15 s | delay > 0
                 place | — | not the player (≥ 400 wu) | from a place
                 ledger | no cause | each line names its cause | cite cause
                 scenario | test/pq-149-01-escalation-seeds.test.mjs | headless
                 check:baseline | 13/15 | 13/15 | 47a hash already off from preserved 149.00 rhythm; goldens not rewritten
                 check:playable | CLEAN red | same canvasFont map error | not this leaf

THE FRAMES       Headless session on seed 14910. No headed GPU strip.
                 This leaf is a ledger-and-delay contract, not a camera beat.

NEXT             Integrator updates the queue. PQ-149.02 is guaranteed
                 ordinary life. Do not mark the queue from this receipt.
```

## Files

Edited:

- `src/systems/encounterDirector.js` — bounded seed queue on the existing director (149.00 rhythm preserved)
- `src/ai/director.js` — read the seeds; no hard-counter reinforce
- `src/systems/shipLedger.js` — project cause-cited rows
- `src/systems/telemetry.js` — `escalation:seeded` / `escalation:arrived`
- `test/pq-149-01-escalation-seeds.test.mjs`
- `design/program/roadmap/receipts/PQ-149.01-REPORT.md`

Not edited: `program-queue.json`, `NOW.md`, `stationSideEventDirector.js`, range/tether/sg02, wreck/pod, `tech.js`, `proofSixtySeconds.js`, `input.js`.
