DONE  PQ-186.03 — The offer picker reads OFFER_MIX by named weight. Inserting a mission type
without a named column can no longer steal later types' weights or zero them with no test failing.

WHAT I FOUND     `_pickType` used `weights[i] || 0` against TYPE_ORDER. OFFER_MIX rows are 10 long
(cargo…recon) with physical types as named keys. Inserting a MISSION_TYPES row at the head assigned
cargo's column to the new type and shifted recon off the row to index 10 → weight 0. Found while
adding breakaway_recovery.

WHAT I CHANGED
- `src/data/missions.js` — `POSITIONAL_OFFER_TYPES` (the original ten), `withNamedColumns` /
  `withPhysicalMix`, `offerMixWeight(weights, typeId)` (named first, then the named positional
  column, never `weights[i]` against TYPE_ORDER), `validateOfferMix`.
- `src/systems/missions.js` — `_pickType` calls `offerMixWeight`.
- `scripts/check-data.mjs` — live `validateOfferMix()` on the missions catalog.
- `test/pq-186-03-offer-mix-named-weights.test.mjs` — live guard green; insert-without-named turns
  red; cargo and recon keep their columns.

WHAT YOU WILL FEEL   Nothing on the board today. The next mission type that lands cannot silently
delete recon, smuggling, or passenger work from every station mix.

THE NUMBERS      bar | evidence (seed 18603)
every procedural type named | validateOfferMix() ok on live OFFER_MIX
cargo/recon identity | trade_hub cargo 4, recon 1; both survive an inserted nameless type
insert-without-named is red | 9 profile errors including inserted_without_name; patched named 0 is green
authored-only stay off the roll | heist_intercept / breakaway_recovery weight 0

FILES
src/data/missions.js — named mix helpers + guard
src/systems/missions.js — _pickType
scripts/check-data.mjs — live guard
test/pq-186-03-offer-mix-named-weights.test.mjs — 2 tests

CHECKS
node --test test/pq-186-03-offer-mix-named-weights.test.mjs   PASS 2/2
node --test test/pq-152-00-physical-types.test.mjs test/charon-bounty-board.test.mjs
  test/tethys-contracts-nexus.test.mjs   related mix tests green (hunter 30/60/90 cohort is a
  pre-existing balance red, identical concern, not this leaf)
npm run check:mission-receipts    PASS
node scripts/check-data.mjs       17/17

LEFT OUT / UNPROVEN
- `test/m3-hunter-balance.test.mjs` cohort 30m floor is red on this tree for reasons unrelated
  to named weights (bounty column values unchanged).

RESULT: DONE
