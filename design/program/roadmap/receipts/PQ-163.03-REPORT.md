<!-- LIFETIME: RECEIPT -->
# PQ-163.03 — The store-page sentence, shown once

```text
STATUS DONE on show-once + three clauses before minute ten
STATUS NOT DONE on stranger restates the fantasy

WHAT I FOUND     Leftover rescue already swings a rock,
                 shoves the scout, and grabs the pod.
                 Leftover missing-three already boosts
                 after the grab. The store-page sentence
                 was never shown. That was the gap.

WHAT I CHANGED   The sentence lives in storeSentence.js.
                 Default New Game shows it once when
                 rescue starts, then silence. A once-only
                 firsthour:sentence event records the
                 show. Leftover swing, shove, and boost
                 stamp the three clauses. 47-A still
                 skips the rail.

WHAT YOU WILL FEEL   You start a new game. One line:
                 Light ships are ammunition. Swing a
                 rock. Keep the speed. Then quiet. You
                 swing the rock. You shove the scout.
                 You boost after the grab.

THE NUMBERS      bar | before | after | target
                 sentence on rescue:started | not shown | shown once, then silence | one line once
                 firsthour:sentence | none | { shown:true, atS } once | exactly once
                 swing a rock | leftover swing-release | stamped performed before 600s | clause done
                 light ships are ammunition | leftover scout shove | stamped performed before 600s | clause done
                 keep the speed | leftover boost after grab | stamped performed before 600s | clause done
                 47-A scenario payload | leftover skip | still skips; no sentence | golden-safe
                 drill table | leftover B0 thrust | untouched | same order
                 stranger restates fantasy | unmeasured | unmeasured | NOT DONE — no unaided player

THE FRAMES       Headless leftover proof. No headed GPU strip.

HONESTY GAPS     No stranger sat down and restated the
                 sentence. That clause stays NOT DONE.
                 No percentage was invented. Queue not
                 marked. No commit.

NEXT             Integrator updates the queue. A real
                 unaided player is required before the
                 stranger-fantasy clause can close.
                 Do not mark the queue from this receipt.
```

## Files

- `src/onboarding/storeSentence.js` — one-line copy, once-only funnel, clause stamps
- `src/systems/onboarding.js` — show on `rescue:started`; stamp leftover swing / shove / boost
- `test/pq-163-03-sentence.test.mjs` — show-once, clauses before 600s, 47-A skip, drill untouched, live-arbiter floor
- `design/program/roadmap/receipts/PQ-163.03-REPORT.md` — this receipt

Already live, not rewritten: `src/onboarding/rescueOpening.js`, `src/onboarding/missingThree.js`, `test/rescue-opening.test.mjs`.

Not edited: `src/ui/hudAttention.js`, `src/ui/screens/range.js`, `src/render/`, `assets/`, `src/systems/input.js`, `program-queue.json`, `design/program/NOW.md`, `package.json`, `src/systems/telemetry.js`.

## Honesty gaps

- The named gap was the missing sentence. Leftover already performed the three clauses. This leaf shows the line once and stamps those leftover completions.
- A stranger restating the fantasy after play is unproven. No unaided playtest. No invented percentage.
- "Before minute ten" is a RECORDING window, not a measured pacing result. The driven proof stamps
  swing at 35 s, ammunition at 45 s, speed at 55 s — a scripted drive that places bodies and jumps
  the clock in 5 s steps, so the `< 600 s` assertion cannot fail for any script that finishes. It
  proves the stamps carry a timestamp; it does not prove an unaided player reaches all three clauses
  inside ten minutes. That half of the leaf outcome needs the same real player the stranger clause
  needs.
- The default route hands onboarding `helpers.voice` (voiceArbiter), so it takes the arbiter branch,
  not the raw-toast fallback the first four tests cover. `say()` returns true on ACCEPT, not on
  display. A fifth test now wires the real arbiter in registry init + update order and asserts one
  `voice:surface` (id `firsthour:sentence`, channel `tutorial`), one mirrored toast, a later
  `voice:clear`, and no second floor on a repeat `rescue:started`. Mutation-checked: flipping the
  channel to `chatter` reds only that test — the other four stay green.
- Headless only. The job peeled headed capture.
- Default New Game (`game:started` with no scenario payload) shows the line once through the leftover tutorial voice and a one-shot toast id `firsthour:sentence`. B0 and leftover verb lines still speak later; the store sentence is not shown a second time.
- 47-A `game:started` with a scenario payload still skips the rail and never shows the line.
- Checkpoint adopted from a stale sibling hold. `NOW.md` was left for the live orchestrator.

## Proof

```text
PQ-163.03 sentence: Light ships are ammunition. Swing a rock. Keep the speed.
PQ-163.03 firsthour:sentence: shown once at rescue:started
PQ-163.03 clauses: swing + ammunition + speed performed before 600s on seed 47
PQ-163.03 47-A: rail skipped, sentence not shown
```

`node --test test/pq-163-03-sentence.test.mjs test/rescue-opening.test.mjs` — 14 pass, 0 fail.
