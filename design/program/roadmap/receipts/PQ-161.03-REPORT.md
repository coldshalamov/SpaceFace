<!-- LIFETIME: RECEIPT -->
# PQ-161.03 — Teaching overlay: receipt

```text
DONE  PQ-161.03 — trajectory and force ribbons now draw in the Range and the draw-to-fly stroke preview, and they stay off the default flight route.

WHAT I FOUND     The Range already had the stroke rung and massline toys, but no teaching overlay. Ordinary flight had no trajectory/force ghost layer either — the gap was the missing Range/preview ink, not a HUD that needed stripping.

WHAT I CHANGED   Added a small overlay helper that only plans ribbons when the surface is the Range or a live draw-to-fly stroke preview. Range paint now inks a trajectory ghost plus rope / well / punch ribbons in the 161.02 force hues. Default production / assisted flight cannot opt in, even with G armed.

WHAT YOU WILL FEEL   In the Range, the stroke you draw is a pale ribbon, a hitch reads magenta, a well pulls teal, a boost punch flashes gold. Flying the ship on the default route stays clean.

THE NUMBERS      bar | before | after | target
                 Range overlay draws | no | yes (trajectory + force) | yes
                 draw-to-fly preview draws | no | yes (1 trajectory ribbon) | yes
                 default flight ribbons | 0 | 0 | 0
                 flight owners importing overlay | — | 0 / 5 | 0
                 headless suite | missing | 4 / 4 | green
                 seed | — | 16103 | 16103

THE FRAMES       Headless canvas strokes. No headed Chromium. No stills.

NEXT             Integrator can mark the leaf. Queue not marked from this receipt. No commit.
```

## Proof

| Surface | Result |
|---|---|
| Range `draw_the_stroke` | trajectory ribbon `#EDE7DC` inked by `rangeScreen._render` |
| Range swing / well / boost | rope `#ED4EA3`, wells `#036F59`, impulses `#FFE956` |
| Draw-to-fly preview (`drawing: true`) | 1 trajectory ribbon |
| Default flight `production` / `assisted` | `allowed=false`, 0 ribbons |
| Default flight with G armed, not drawing | 0 ribbons |
| Default flight surface while a stroke is being drawn | still 0 — preview must opt in as its own surface |

Command: `node --test test/pq-161-03-teaching-overlay.test.mjs` — 4 pass.

## Stay-off

Not edited: `src/render/renderer.js`, `src/render/vfx.js`, `src/data/palettes.js`, `src/systems/input.js`, `src/ui/uiRoot.js`, `src/ui/hud.js`, `program-queue.json`, `design/program/NOW.md`. No commit. No push.

## Honesty gaps

The live HUD SVG stroke line in dirty `uiRoot.js` does not call this helper — that file was stay-off. The preview surface draws when asked; Range asks today. World VFX still paint the old 161.02 stand-in hues. No headed stills, by packet.
