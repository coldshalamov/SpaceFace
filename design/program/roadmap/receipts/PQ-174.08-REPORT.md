STATUS: DONE

<!-- LIFETIME: ACTIVE_RECEIPT -->

# PQ-174.08 — Earned breathing room: the pressure reservoir

```text
DONE  PQ-174.08 — after a three-kill clear the Crucible room goes thinner for four seconds, then a telegraphed group spends the stored pressure.
WHAT I FOUND     The stream patched every hole as fast as you made it. A good clear never bought a beat to collect, reposition, or set up a stunt — the room slammed shut in a fifth of a second.
WHAT I CHANGED   Small holes still refill as a group of three. A three-kill hole now stores that pressure for four seconds, warns "inbound — a group is on a bearing", then spends the stored count as one arrival. Same roster, same room size, same hulls. An empty board still comes back on the next tick.
WHAT YOU WILL FEEL   Wipe three at once and the room actually opens. You have four seconds. Then the next group announces itself and comes in on a bearing.
THE NUMBERS      seeds 4242 / 8008 / 13502 · 3-kill clear · emptier 4.0 s · telegraph 1 · spend returns the room to 10 · wave-1 quota still completes with survivors
THE FRAMES       none — FRONTEND-B owns headed Chromium. This leaf is headless. A camera strip is not required to land the implementation.
NEXT             .01 still owns the 45–90 s wave band. This leaf did not retune Pulse, kits, or hull values.
```

Helios Core. Headless. Seeds 4242, 8008, 13502. The live stream (survivalWave calling the new reservoir) is the proof. Quiet-after-wave-1 cannot say "thinner during a wave", so a `clearBreath` measure was added; historical traces without occupancy stay n/a with a reason, never a fake zero.

## The breath (forced 3-kill on the live stream)

Opening room is ten. Three kills leave seven. The stream holds replacement for 240 ticks, then spends a group of three. The board is never left at zero.

| Seed | Before | After the clear | Emptier | Telegraph | After the spend |
|---|---|---|---|---|---|
| 4242 | 10 | 7 | 4.0 s | 1 | 10 |
| 8008 | 10 | 7 | 4.0 s | 1 | 10 |
| 13502 | 10 | 7 | 4.0 s | 1 | 10 |

Wave 1 still meets its 15-kill quota with survivors rolling into wave 2. Difficulty did not move by hit points and did not move by "add more ships."

## Swarm bars vs the PQ-174.00 BEFORE dump

The nine-cell Rapier printer could not boot on this worktree (`stuntGrammar` is in the production manifest and the browser registry, and missing from the Node factory table — outside this leaf's write set). The BEFORE dump from PQ-174.00 is the comparison baseline. This leaf did not touch opening packages, quota 15, concurrency, Pulse, kits, or hull values, so:

- first hostile stays the opening burst (0.05 s on every BEFORE cell)
- Pulse 8008 quiet-after-wave-1 stays n/a (wave 1 never finished) — not an invented zero
- menu `0` on armed capture stays a real count
- `formatSwarmBars` required keys are unchanged; `clearBreath` is extra and n/a on historical traces

A headed GPU strip is FRONTEND-B's. Not claimed here.

## Checks

- `node --check` on edited JS
- `node --test test/swarm-pressure-reservoir.test.mjs test/crucible-swarm.test.mjs test/crucible-swarm-arena.test.mjs test/swarm-metrics.test.mjs` — pass (9-cell live sweep still behind `SWARM_BARS_FULL=1`)
- scenario `swarm.pressure_reservoir` on 4242 / 8008 / 13502 — 4.0 s, telegraph, spend

Queue unit state is not marked done — controller reviews.

## Controller review — 2026-09-07

The live stream holds a three-kill hole for 240 sim ticks, then spends a group. Duplicate `voice:say` next to `alert` was struck so the one-voice floor speaks once. No camera strip (FRONTEND-B). Nine-cell Rapier reprint was not possible in this worktree; quota, Pulse, and kits were not retuned.
