DONE  PQ-166.00 — A language picker in Settings now switches the live locale and re-renders every mounted screen without a reload; English remains the default unless chosen.

WHAT I FOUND     The runtime and pseudo-locale already existed, but the document bridge only installed once at page load and only for the `?locale=` route, so nothing could change language in-game and the English default had no path back after a pseudo switch.

WHAT I CHANGED   The document bridge is now re-runnable and keeps each node's authored English source so any locale can be applied and then reversed live; Settings gained a Language row that calls the shared locale setter; and the startup resolver now accepts any well-formed locale while still defaulting the route to English.

WHAT YOU WILL FEEL   Open Settings, Access, and the Language row is the first control. Pick a language and the whole UI redraws in place, including cached screens, without a reload; pick English again and everything returns to normal.

THE NUMBERS      bar | before | after | target | seed
mounted screens re-rendered on a live language switch | 0 of 4 (no setting existed; load-time install only) | 4 of 4 screens + translated attributes re-rendered, and 4 of 4 restore to English on switch-back | every mounted screen re-renders with no reload | 16600
default new-game locale | en-US | en-US | en-US unless chosen | 16600
bridge installs for | qps-ploc only (load route) | any well-formed locale | any locale | 16600

FILES
- `src/localization/domBridge.js`
- `src/localization/gameLocalization.js`
- `src/ui/screens/settings.js`
- `test/localization-reachability.test.mjs`
- `test/pq-166-00-language-setting.test.mjs`
- `design/program/NOW.md` (one mutation row)
- `design/program/roadmap/receipts/PQ-166.00-REPORT.md`

CHECKS
- `npm run check:baseline` (entry, before edits): FAIL — 4 red links, all pre-existing/foreign: `vfx-techniques` (untracked `src/render/uiStage.js`), `pq020-ceres-topology`, `sim-v3`, `sim` (47-A goldens). No new reds at entry.
- `npm run check:baseline` (exit, after edits): FAIL — same 4 red links, same causes. No green link at entry turned red; no new red caused by this unit.
- `npm run check:player-facing-labels`: PASS.
- `node --test test/localization-reachability.test.mjs`: PASS 7/7 (run once).
- `node --test test/localization-runtime.test.mjs`: PASS 8/8.
- `node --test test/pq-166-00-language-setting.test.mjs`: PASS 4/4 (run twice, identical result).
- `node test/settings-controller-label-truth.test.mjs`: PASS, all assertions green.
- `node scripts/check-ui-screen-imports.mjs`: PASS, 52 ok / 0 fail.

UNPROVEN     The browser route-level capture (`scripts/check-localization-reachability.mjs`, GPU/Playwright) was not run — the leaf forbids `check:playable` while PQ-194.01 owns the GPU. The live re-render is proven headlessly against a deterministic DOM fixture, not in a real browser. The language choice is session-scoped: it is not written into the profile settings snapshot (that file is outside this leaf's write set), so it does not survive a reload yet.
