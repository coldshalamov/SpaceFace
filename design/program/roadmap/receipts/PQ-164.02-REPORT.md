<!-- LIFETIME: RECEIPT -->
# PQ-164.02 — Deck and trackpad (receipt)

```text
NOT DONE  PQ-164.02 — Leaves: "Deck capture at 1280×800; trackpad route completes the first ten minutes." pending two reviews.

Headed still is 1280×800 live flight. Trackpad first-ten is a 600 s sim session (600 steps) covering latch/reel/throw/stroke/boost, not a five-item loop. Queue stays ready until reviews set unmetClause null.

WHAT I FOUND     Touch overlay existed for phones. A laptop trackpad still used
                 raw mouse/wheel (zoom-fight, no latch/reel/throw). Settings had
                 no Deck row. No 1280×800 capture script.

WHAT I CHANGED   Trackpad tap/scroll/flick/stroke/pinch write Space, W/S, Y, Shift
                 and draw-to-fly through touch.tick(inputHost). UI wheel consumes
                 a latched two-finger scroll as reel instead of zoom. Settings
                 names the Deck at 1280×800. A capture script was run and failed
                 at the live-game ready wait.

WHAT YOU WILL FEEL   On a trackpad: middle-tap latches the line, two-finger scroll
                 reels it, a flick throws, a stroke draws a route, pinch boosts.
                 Steam Deck resolution is named in Settings; a still at 1280×800
                 was not captured this pass.

THE NUMBERS      bar | before | after | target | seed
                 first-ten verbs via trackpad | 0 | latch,reel,throw,stroke,boost | all five | 16402
                 key-seam inject (Space/W/S/Y/Shift) | none | held on tick | held | 16402
                 new test | n/a | 3/3 twice | 3/3 | 16402
                 Deck capture 1280×800 | missing | Chromium timeout waiting for window.SF | still | 16402
```

Seed `16402`, `node --test test/pq-164-02-trackpad.test.mjs` (run twice, identical):

```text
PQ-164.02 seed=16402 verbs=latch,reel,throw,boost,stroke keys-ok=true
PQ-164.02 first-ten complete=true observed=latch,reel,throw,stroke,boost
```

Deck script:

```text
node scripts/check-pq-164-02-deck.mjs
DECK_CAPTURE=unavailable seed=16402 size=1280x800 reason=launch-or-walk
page.waitForFunction: Timeout 30000ms exceeded.
    at scripts/check-pq-164-02-deck.mjs:69
```

## What changed

- `src/systems/touch.js` — trackpad ingest + `touch.tick` injects the existing key codes.
- `src/ui/input.js` — flight wheel/pointer feed the trackpad layer; latched scroll is reel, not zoom.
- `src/ui/screens/settings.js` — one Steam Deck / trackpad note under Controls (no other rows).
- `test/pq-164-02-trackpad.test.mjs` (new)
- `scripts/check-pq-164-02-deck.mjs` (new)

## Files

- `src/systems/touch.js`
- `src/ui/input.js`
- `src/ui/screens/settings.js` (Deck note only)
- `test/pq-164-02-trackpad.test.mjs` (new)
- `scripts/check-pq-164-02-deck.mjs` (new)
- this receipt

## Unproven

- A 1280×800 still of the live game on Deck-shaped Chromium. Playwright launched; `window.SF`
  never became ready in 30 s (same class of residual as PQ-164.00).
