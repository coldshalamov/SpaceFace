# PQ-210.08 — The fifteen-minute demo, played end to end — REVIEW REPORT

**Date:** 2026-09-22 · **Host:** owner's machine, Intel D3D11 (headed), 14 logical cores
**Verdict:** REVIEWED, NOT SIGNED — the leaf stays open. The quiet-window session was fired
(2026-09-22 ~09:50–11:30): seven crucible smooth-flight attempts, the flagged production-route
witness, solid-world, two `--demo-opening` attempts and two boot attempts. The frame-timing letter
still has no ≤40 %-CPU sample (seven attempts, every one re-saturated mid-cook; best attempt read
**0 frames over 100 ms at 48 % CPU**), the witness's input-to-present proxy is **structurally**
unreachable on this host (its Crucible driver times out at 120 s against the `.00` roster cook's
~156–176 s launch-to-flight), and the m3 adventure leg is blocked by a **genuine Shipworks chooser
regression** introduced by today's station-rebuild work — reproduced in isolation. Four of five §5
bars keep their earlier green evidence; no bar was signed on today's numbers.

## The path, played

`node scripts/check-crucible-route.mjs` — **16/16 PASS** after the repairs below:
menu → Crucible door → mode/seed → launch → arsenal → wave 1 materialized → kill/clear → wallet →
draft offers → fitted upgrade → purchase → refit → results → same-seed restart (seed 4242 →
`swarm:w1:1092` reproduced exactly — determinism on the demo route confirmed live).

The Adventure half: `check-m3-player-facing-public-route.mjs` — public map → Set Waypoint →
autopilot cruise → corridor approach → **physical dock → station hub settled** all passed on the
second attempt (the first died at the 360 s wall-clock dock budget while correctly braking 121 m
out — a host-throughput artifact, not a route defect). The run then timed out inside the Outfitting
step; a focused reproduction of that exact interaction (dock → Shipworks → click hardpoint 0) shows
the chooser opens synchronously with **17 previewable module rows** — the step is functional and the
timeout was starvation. The job → pay → upgrade → travel legs remain **unplayed** today.

## The five bars, clause by clause

| §5 clause | Evidence | Verdict |
|---|---|---|
| Crucible is the first button | `mainMenu.js` top-level verb, direct entry (PQ-133 §12.2); route enters it straight from title | SIGNED |
| Cold boot → title < 10 s | two `check:playable` attempts (11:19, 11:27) both died at BOOT with `[startup]` syntax errors from a foreign audio lane's in-flight edits (`src/audio/audioSystem.js` "Unexpected token '/'", then `src/audio/sampleLibrary.js` "Illegal return statement"; parse state oscillated as the lane saved — `node --check` flip-flopped between attempts). Last clean boot before that: solid-world 11:16. No number. | BLOCKED — concurrent foreign work |
| Launch → flight < 10 s | ~170 s+ behind the loading shell — the deliberate post-.00 roster cook; today's seven attempts read 156.2–176.3 s. See "Findings" | LETTER vs DESIGN CONFLICT |
| First input moves ship < 2 s | `check:playable` PILOT: baseline 0 displacement → 247 u/s in 0.5 s powered window | SIGNED (sim-verified) |
| First physics kill ≤ 60 s | scripted-pilot cells, demo kit `ricochet_runner`: 12.4 s (8008), 78.5 s (4242), none in 90 s (13502); deliberate physics play (`physics_toolkit`): 38–57 s | AT RISK — seed-dependent; see below |
| …answers on four channels | `probe-hit-answer`: **65/65 hits** hit-stop+light+mass+sound ≤ 6 ticks, both default-kit weapons | SIGNED |
| Rounds 1–5 escalate problems not HP | `survivalWaves.js` helios w1–w5: threat 8→16 by composition (control/disruptor/elite enter), answer verbs rotate throw→shove→rope→shove→throw, room phases arm shutters/plate/furnace | SIGNED |
| Shop offers handling-changing build | route check OFFERS→FITTED→PURCHASE→REFIT all pass; handling delta asserted. The Outfitting **preview** step itself: see the chooser regression below | SIGNED (buy-fit leg) / BLOCKED (hover-preview leg) |
| No frame over 100 ms | seven quiet-window attempts (table below): structural gates green ×7 — `SHIP LOST ITS PLACE 0`, `shaderLinks +0 / bufferFullUploads +0`, wave arrival COVERED ×7, game speed 54–91 %. Sample-window CPU 48–78 % every time (re-saturated mid-cook; pre-fire samples were 5–37 %). Best: attempt 4, **0 frames over 100 ms, worst 83 ms at 48 % CPU** — matching the .00 acceptance worst-band, but outside the 32–38 % acceptance band. | ENVIRONMENT-BLOCKED (7 attempts recorded) |
| Results: seed, ghost, "run it again" | RESULTS step passes; same-seed restart reproduces wave plan; ui-bench `crucibleResults` reviewed | SIGNED |
| Nothing on the glass unloads | `probe-solid-world`: **onGlassDisposals = 0** crucible + 3-min belt (re-confirmed today: `onGlassDisposals=0 (diag 0)` both phases at 11:16) | SIGNED |
| Pending geometry ≤ 0.25 s | today 11:16 (headless): worst pending 0.991 s (crucible) / 0.773 s (belt) with maxRafGap **2013 / 1485 ms** and rafGapAtPendingMax 309 / 367 ms — the render loop itself starved (the .00-family starvation signature), host re-saturated mid-run | ENVIRONMENT-BLOCKED (starved attempt recorded) |
| Three screens read as hardware | ui-bench stills flight / crucibleHud / crucibleResults reviewed — machined-instrument grammar consistent | SIGNED |
| Hostile-in-frame ≥ 80 % | `ricochet_runner` 87.3 / 92.9 / 90.7 % across seeds 4242/8008/13502 — all MET | SIGNED |
| Input to photon ≤ 50 ms | witness fired **with** `--production-route=warm-dense-combat` (10:42–10:45): the production-matrix section printed, status **`unavailable`**, reason `"public route driver unavailable"`, n=0 — the driver's flight wait (120 s, `probe-runtime-witness.mjs` `launchProductionCrucible`) times out against the .00 cook's ~156–176 s launch-to-flight. `sustained-swarm` failed identically on Sep 11; all five non-Crucible routes measured (Sep 6–10). Verbatim: `- input-to-present CPU proxy (not physical photon latency): unknown`. **Instrumented-but-unmeasured**, structural cause named | INSTRUMENTED, NO OBSERVATION |

## Repairs landed by the review — commit `7b55d3563`

1. **`vfx.js` post-death render-loop crash.** After Crucible death the player entity is gone;
   `_actuatorsFor(undefined)` still resolves the player telemetry block and `player.pos` threw every
   frame on the results screen (the route's CLEAN gate). Guarded: RCS pool decay still runs, firing
   requires a live player. Verified by the 16/16 rerun.
2. **HUD below the 12 px instrument floor.** `.sf-crun__chaincause` was 11 px → 12 px. HUD gate green.
3. **Invalid `projectile:nearMiss` cue.** A stationary projectile's sweep segment is degenerate;
   `segmentDirection` returned a zero vector the cue schema rejects (bus errors spammed the massline
   cell). Falls back to projectile velocity, else `null` (schema-legal). Rerun cell: 0 bus errors.
4. **`probe-hit-answer` audited a retired kit.** Now audits the live default fit
   (`wpn_bank_stream_m` + `wpn_concussion_cannon_m`).

## Findings the review is handing forward

- **"Launch to flight in under ten seconds" vs. the roster cook.** The prewarm this packet family
  built (.00–.02) deliberately moved the roster's GPU/GLB cost into the launch shell — 171 s on this
  host, of which `cook.rockPools` alone burned **110 s** (the O(roots) admission .00's receipt named
  as the next engineer's first order — still the driver). §5's 10-second letter predates that
  design. Either the letter or the design moves; that is an owner product call, not a defect to
  silently fix.
- **Physics kill ≤ 60 s is seed-dependent on the scripted instrument.** Wave 1's authored answer is
  literally "throw them into each other," and deliberate physics play lands the kill at 38–57 s —
  but the demo kit flown as a pure shooter produced collision kills at 12.4 s / 78.5 s / none in the
  90 s cell. The scripted pilot never plays physics on `ricochet_runner`, so these are the emergent
  floor; the player-facing claim needs a physics-literate run or an owner judgment on the margin.
- **`check:rcs-jets` is red on HEAD** (`cb592dacd`): strafe/brake resolved nozzles no longer reach
  the production pool the way the wiring check counts them — `production pool must receive every
  resolved nozzle: 0 !== 2`. Reproduced on clean HEAD; predates this review; the juice-pass owner
  should adjudicate whether the check or the new jet routing is stale.
- **Shipworks bench stills flag layout collisions** (foreign in-flight work): "choose ordnance" copy
  prints over S1/S2, "Top speed" over "Industry", three controls off-frame at the bench viewport.
  The chooser was proven functional at review time (morning focused repro, 17 rows) — but see the
  quiet-window session's finding 4: on the tree as of 11:02 the chooser no longer opens at all.

## The quiet-window session, 2026-09-22 ~09:50–11:30 — what fired and what it read

### 1. Crucible smooth-flight — seven attempts, no ≤40 % sample window

`npm run probe:smooth-flight:crucible`, headed, seed 4242, default kit. Pre-fire CPU samples were
5–37 % every time (attempt 3 fired after 90 s of sustained 28–38 %); the host re-saturated inside
the ~160–175 s launch cook before every 30 s sample. Attempts fired back-to-back from attempt 4 on
(fire-time load does not predict sample-time load on a ~2–5 min oscillation). All times local −0400.

| # | fired | sample CPU | launch→flight | >33 / >50 / >100 (30 s) | worst | links/full uploads | presents | wave | game speed |
|---|---|---|---|---|---|---|---|---|---|
| 1 | ~09:50 | 68 % | 175.7 s | 361 (87.6 %) / 253 / 45 | 867 ms | **+0 / +0** | **0** | COVERED (edge) | 54.0 % |
| 2 | 10:17:48 | 50 % | 164.7 s | 230 (26.5 %) / 81 / 17 | 200 ms | **+0 / +0** | **0** | COVERED (real spawn) | 81.1 % |
| 3 | 10:25:35 | 78 % | 176.3 s | 349 (57.4 %) / 196 / 27 | 167 ms | **+0 / +0** | **0** | COVERED (edge) | 66.4 % |
| 4 | ~10:32 | 48 % | 164.0 s | 173 (17.4 %) / 47 / **0** | **83 ms** | **+0 / +0** | **0** | COVERED | 88.1 % |
| 5 | ~10:39 | 53 % | 156.2 s | 193 (19.7 %) / 52 / 1 | p99 133 ms | **+0 / +0** | **0** | COVERED | 91.4 % |
| 6 | ~10:45 | 58 % | 171.0 s | 370 (57.5 %) / 193 / 7 | — | **+0 / +0** | **0** | COVERED (edge) | 73.5 % |
| 7 | ~10:52 | 62 % | 172.7 s | 417 (69.8 %) / 218 / 8 | 117 ms | **+0 / +0** | **0** | COVERED (edge) | 77.0 % |

("—" = the capture filter dropped the line; the probe printed it.) Every structural gate the .00
acceptance defined — zero draw-time program links, zero first-draw full uploads, zero presents
redrawing an already-drawn moment, wave arrival covered — held in **all seven runs at every load
level**, including 78 %. The timing letter is the only thing without an accepted number: the
≤40 % band was never live at sample time, and attempt 4's 0-over-100 / worst-83 ms at 48 % is the
honest near-miss. The `.00` acceptance runs themselves read 0-over-100 at 34–38 % CPU with worst
67/83 ms — attempt 4 matches that shape on a host 10 points hotter.

### 2. Runtime witness, flagged — input-to-present proxy: no observation, structural cause

`node scripts/probe-runtime-witness.mjs --production-route=warm-dense-combat`, 10:42–10:45
(2026-09-22T14:45:23Z report stamp). With the flag the `Production route matrix (PQ-144.01)`
section prints; it read, verbatim:

```
- route: warm dense combat (warm-dense-combat)
- status: unavailable
- foreground intervals: n=0; p50=unknown ms; p95=unknown ms; p99=unknown ms; max=unknown ms; >33.3ms=0
- input-to-present CPU proxy (not physical photon latency): unknown
```

with `"reason": "public route driver unavailable"` and `hull: null` in
`.devshots/runtime-witness/production-matrix-warm-dense-combat.json` (gitignored; this quote is the
durable record). The cause is **structural, not load**: the Crucible driver allows 120 s to reach
flight (`launchProductionCrucible`'s `waitForFunction(..., { timeout: 120_000 })`), while Crucible
launch-to-flight measured 156.2–176.3 s in all seven probe runs today — the `.00` roster cook
(`cook.rockPools` alone 105–111 s, bounded by the 120 s cook budget) puts flight past the driver's
deadline on this host at any load. Corroboration: `production-matrix-sustained-swarm.json` failed
with the same status on Sep 11, while all five non-Crucible routes (cold-opening, dock-refit-
undock, busy-site-save-reload, asteroid-works-roundtrip, earned-speed-traversal) measured on
Sep 6–10. Until the driver's flight wait clears the cook (or the cook shrinks), the witness cannot
observe the Crucible-route input-to-present proxy. Host context: the witness's own "Host load
during the window" section read `unavailable`; the adjacent crucible attempt 6 (~10:45) sampled
58 % busy.

### 3. Solid-world — disposal gate green again, latency starved

`node scripts/probe-solid-world.mjs --headless`, 11:16. `onGlassDisposals=0 (diag 0)` in both the
Crucible and 3-minute belt phases — the leaf `.03` gate holds. Pending-latency read 0.991 s
(crucible) / 0.773 s (belt) against the 0.250 s bar with maxRafGap 2013 / 1485 ms and
rafGapAtPendingMax 309 / 367 ms — the render loop itself stalled (same starvation signature as the
review's 1.736 s / 2289 ms reading), so this is a starved sample, not a clean red. No quieter
window existed before the tree broke (finding 5).

### 4. m3 `--demo-opening` — two attempts, then reproduced in isolation: the Shipworks chooser is broken on the current tree

- Attempt 1 (~10:47): route green through `authored-flight-ready`, `helios-waypoint-armed`,
  `physical-dock-prompt`, `station-hub-settled`, `hardware-webgl`, `performance-captured` — then
  `proveEngineeringPreview` (check-m3-player-facing-public-route.mjs:288) timed out 20 s waiting
  for `.sx-modrow[data-preview-module]:not(.is-eq)`. Uncaught TimeoutError, exit nonzero.
- Attempt 2 (11:02:38): same failure at the same selector, after recovering through two
  `dock-prompt-recover` cycles and a `helios-waypoint-redock`.
- Isolated reproduction: `node scripts/probe-shipworks-chooser.mjs` (the focused force-dock →
  Shipworks → click first hardpoint → dump instrument) — Shipworks opens, the bench canvas is
  `previewReady: true` (`authored-plate`), the slot box is real (116×43 at 299,195),
  `slotCount: 7` — and after the same raw click at the slot's box center the chooser reads
  `chooserHidden: true, chooserOpen: false, rowCount: 0, eligible: 0`. **The chooser never opens.**
  Not starvation (the rest of each run raced; the dump is instantaneous), not real-drawer state
  (the probe force-docks its own fresh session), not m3-flow-specific (two instruments agree).

The morning review's focused repro saw the chooser open synchronously with 17 previewable rows
(before 05:00). Between then and the first failure the chooser code path was crossed by today's
station-rebuild wave — `dd7659e24` (09:59 berth veil), `cf928d98a` (10:08 dock rebuilt as light),
`e99c7d6d8` (10:34 station computed), `b87032a09` (10:39 mining-beam pod), `addf5a47e` (10:49 zero
raster nine-slices), `e291280a1` (11:05 motion-ask) — plus `8401dd79c` (04:44, G11 module-card
metrics, audited: its `liveDamageRate` is throw-safe and touches no attribute the check waits on).
`src/ui/station/screens/shipworks.js` itself is clean; the regression window is 05:00 → 11:02 with
the 09:59–11:05 rebuild wave as the suspect set. Not bisected here — that is the owning lane's
live work. This blocks the adventure leg's upgrade step: **the leaf cannot flip until the chooser
opens again**, independent of host load.

### 5. Boot wall-clock — blocked by a foreign lane's mid-write tree

`npm run check:playable` at 11:19 and 11:27 both died at BOOT (`sf:false`, 60 s readiness budget)
with a `[startup]` syntax error — first `Unexpected token '/'`, then `Illegal return statement` —
from a foreign audio lane's uncommitted in-flight edits: `src/audio/audioSystem.js` (182-line
dirty hunk) failed `node --check` during the first scan and parsed minutes later; a second file
(`src/audio/sampleLibrary.js`) turned dirty between attempts. The lane is actively rewriting the
audio boot graph; each instrument run samples whatever half-written state exists at that instant.
Every earlier instrument today booted the game fine (last clean boot: solid-world, 11:16). No BOOT
number this session; retry when the audio lane's mutation stops.

### State of the gate after the quiet-window session

- **Frame clause (no frame over 100 ms):** structural shape proven load-independent (×7); the
  ≤40 % timing number remains unmeasured — seven recorded attempts, best 0-over-100 @ 48 %.
- **Adventure leg:** blocked by the chooser regression (finding 4) — a code defect on the tree,
  not a measurement problem. Fix owner: the station-rebuild lane.
- **Input-to-photon:** instrumented-but-unmeasured with a named structural cause (finding 2).
- **Pending latency:** starved sample recorded; needs the same quiet window the timing gate needs.
- **Cold boot:** blocked by foreign mid-write tree (finding 5).

The five §1 bars keep the evidence they had; nothing was signed on today's numbers and the leaf
stays open. Handing forward, in priority order: (1) the chooser regression (blocks the adventure
leg outright), (2) one genuinely quiet window for the crucible sample and the solid-world latency
re-fire, (3) the witness driver's 120 s flight wait vs the `.00` cook (an instrument/design
decision for the PQ-144.01 owner), (4) the launch-cook LETTER-vs-DESIGN conflict and the
physics-kill seed-dependence, unchanged from the review above.
