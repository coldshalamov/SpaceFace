# PQ-210.08 — The fifteen-minute demo, played end to end — REVIEW REPORT

**Date:** 2026-09-22 · **Host:** owner's machine, Intel D3D11 (headed), 14 logical cores
**Verdict:** REVIEWED, NOT SIGNED — the leaf stays open. Four of five §5 bars carry green evidence;
the frame-timing clauses could not be honestly measured because the host sat at 46–87 % whole-machine
CPU for the entire session (other agents' runs), and every accepted number in this packet family was
taken at 32–38 % (see PQ-210.00/.01 receipts).

## The path, played

`node scripts/check-crucible-route.mjs` — **16/16 PASS** after the repairs below:
menu → Crucible door → mode/seed → launch → arsenal → wave 1 materialized → kill/clear → wallet →
draft offers → fitted upgrade → purchase → refit → results → same-seed restart (seed 4242 →
`swarm:w1:1092` reproduced exactly — determinism on the demo route confirmed live).

The Adventure half: `check-m3-player-facing-public-route.mjs` — public map → Set Waypoint →
autopilot cruise → corridor approach → **physical dock → station hub settled** all passed on the
second attempt (the first died at the 360 s wall-clock dock budget while correctly braking 121 m
out — a host-throughput artifact, not a route defect). The run then timed out inside the Outfitting
step; a focused reproduction of that exact interaction (dock → Shipworks → click hardpoint 0) shows
the chooser opens synchronously with **17 previewable module rows** — the step is functional and the
timeout was starvation. The job → pay → upgrade → travel legs remain **unplayed** today.

## The five bars, clause by clause

| §5 clause | Evidence | Verdict |
|---|---|---|
| Crucible is the first button | `mainMenu.js` top-level verb, direct entry (PQ-133 §12.2); route enters it straight from title | SIGNED |
| Cold boot → title < 10 s | measured only under 46–87 % CPU | ENVIRONMENT-BLOCKED |
| Launch → flight < 10 s | ~170 s+ behind the loading shell — the deliberate post-.00 roster cook; see "Findings" | LETTER vs DESIGN CONFLICT |
| First input moves ship < 2 s | `check:playable` PILOT: baseline 0 displacement → 247 u/s in 0.5 s powered window | SIGNED (sim-verified) |
| First physics kill ≤ 60 s | scripted-pilot cells, demo kit `ricochet_runner`: 12.4 s (8008), 78.5 s (4242), none in 90 s (13502); deliberate physics play (`physics_toolkit`): 38–57 s | AT RISK — seed-dependent; see below |
| …answers on four channels | `probe-hit-answer`: **65/65 hits** hit-stop+light+mass+sound ≤ 6 ticks, both default-kit weapons | SIGNED |
| Rounds 1–5 escalate problems not HP | `survivalWaves.js` helios w1–w5: threat 8→16 by composition (control/disruptor/elite enter), answer verbs rotate throw→shove→rope→shove→throw, room phases arm shutters/plate/furnace | SIGNED |
| Shop offers handling-changing build | route check OFFERS→FITTED→PURCHASE→REFIT all pass; handling delta asserted | SIGNED |
| No frame over 100 ms | today's samples: headed Intel D3D11 worst 5783 ms @86 % CPU; SwiftShader run discarded. .00 acceptance (34–38 % CPU): 0 over 100 ms, worst 67–83 ms | ENVIRONMENT-BLOCKED |
| Results: seed, ghost, "run it again" | RESULTS step passes; same-seed restart reproduces wave plan; ui-bench `crucibleResults` reviewed | SIGNED |
| Nothing on the glass unloads | `probe-solid-world`: **onGlassDisposals = 0** crucible + 3-min belt | SIGNED |
| Pending geometry ≤ 0.25 s | worst 1.736 s with maxRafGap 2289 ms — render loop itself starved | ENVIRONMENT-BLOCKED |
| Three screens read as hardware | ui-bench stills flight / crucibleHud / crucibleResults reviewed — machined-instrument grammar consistent | SIGNED |
| Hostile-in-frame ≥ 80 % | `ricochet_runner` 87.3 / 92.9 / 90.7 % across seeds 4242/8008/13502 — all MET | SIGNED |

## Repairs landed by the review — commit `7b55d3563`

1. **`vfx.js` post-death render-loop crash.** After Crucible death the player entity is gone;
   `_actuatorsFor(undefined)` still resolves the player telemetry block and `player.pos` threw every
   frame on the results screen (the route's CLEAN gate). Guarded: RCS pool decay still runs, firing
   requires a live player. Verified by the 16/16 rerun.
2. **HUD below the 12 px instrument floor.** `.sf-crun__chaincause` was 11 px → 12 px. HUD gate green.
3. **Invalid `projectile:nearMiss` cue.** A stationary projectile's sweep segment is degenerate;
   `segmentDirection` returned a zero vector the cue schema rejects (bus errors spammed the massline
   cell). Falls back to projectile velocity, else `null` (schema-legal). Rerun cell: 0 bus errors.
4. **`probe-hit-answer` audited a retired kit.** Now audits the live default fit
   (`wpn_bank_stream_m` + `wpn_concussion_cannon_m`).

## Findings the review is handing forward

- **"Launch to flight in under ten seconds" vs. the roster cook.** The prewarm this packet family
  built (.00–.02) deliberately moved the roster's GPU/GLB cost into the launch shell — ~170 s on
  this host even at 34–38 % load. §5's 10-second letter predates that design. Either the letter or
  the design moves; that is an owner product call, not a defect to silently fix.
- **Physics kill ≤ 60 s is seed-dependent on the scripted instrument.** Wave 1's authored answer is
  literally "throw them into each other," and deliberate physics play lands the kill at 38–57 s —
  but the demo kit flown as a pure shooter produced collision kills at 12.4 s / 78.5 s / none in the
  90 s cell. The scripted pilot never plays physics on `ricochet_runner`, so these are the emergent
  floor; the player-facing claim needs a physics-literate run or an owner judgment on the margin.
- **`check:rcs-jets` is red on HEAD** (`cb592dacd`): strafe/brake resolved nozzles no longer reach
  the production pool the way the wiring check counts them — `production pool must receive every
  resolved nozzle: 0 !== 2`. Reproduced on clean HEAD; predates this review; the juice-pass owner
  should adjudicate whether the check or the new jet routing is stale.
- **Shipworks bench stills flag layout collisions** (foreign in-flight work): "choose ordnance" copy
  prints over S1/S2, "Top speed" over "Industry", three controls off-frame at the bench viewport.
  The live chooser itself is proven functional; the stills overlap is for the CSS lane.

## What the quiet window still owes (the remaining gate)

On a host under ~40 % CPU (the band every .00/.01 acceptance number was taken in):

1. `npm run probe:smooth-flight:crucible` — expect the .00 result shape: 0 links, 0 full uploads,
   0 frames over 100 ms. Signs the frame clause.
2. `node scripts/check-m3-player-facing-public-route.mjs --demo-opening` — completes the adventure
   leg (job → pay → upgrade → travel/Continue) whose dock leg is already verified.
3. `probe-solid-world` pending-latency and boot/launch wall-clock numbers — signed or found red on
   clean timing.

Then sign the five bars and flip the leaf. Everything else in the brief is already evidenced.
