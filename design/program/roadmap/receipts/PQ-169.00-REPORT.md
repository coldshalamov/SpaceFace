<!-- LIFETIME: RECEIPT -->
# PQ-169.00 — Daily seed and board

```text
STATUS DONE

WHAT I FOUND     Two UTC clocks on 2026-09-06 already hashed to the
                 same run seed. Today's score already lived on the
                 local Crucible bag and came back after a fresh load.

WHAT I CHANGED   Nothing in the live Daily path. This receipt records
                 the proof that was already on the tree.

WHAT YOU WILL FEEL   Open Crucible, pick Daily, and the seed is today's
                 — the same one on every machine. Finish a run and
                 Today on the door keeps your score and wave after you
                 close the game. Steam is still later. Nothing you earn
                 here follows you into adventure.

THE NUMBERS      bar | before | after | target
                 same UTC day, two clocks, two storages | 1537801443 | same | identical uint32
                 next UTC day 2026-09-07 | 1521023824 | same | different seed
                 daily board after settle + reload + second JSON | score 16900, wave 12 | same | row persists
                 Today figures on the door | score 16900, wave 12 | same | Today
                 free run seed 16900 writes today's board | 0 rows | same | 0
                 scenario | test/pq-169-00-daily-seed.test.mjs | seed 16900 | injected UTC clock

THE FRAMES       Not a camera leaf. The claim is a date hash and a
                 save bag. Headed Chromium skipped.

NEXT             Integrator updates the queue. Leftovers: .01 ghosts
                 and .02 weekly mutators are already on the tree —
                 characterize before inventing. .03 hangar feed is
                 already closed as not needed. Do not mark the queue
                 from this receipt.
```

## Files

- `test/pq-169-00-daily-seed.test.mjs` — named proof

Already live: `src/systems/survivalRecords.js` (`dailySeedForDateKey`, local `daily.byDate` board),
`src/systems/survivalMutators.js` (queued `dailyDateKey`), `src/ui/screens/crucible.js` (Daily door + Today).

Prior hyphen receipt: `design/program/roadmap/receipts/PQ-169-00-REPORT.md`.

Not edited: survival owners, `program-queue.json`, `NOW.md`.
