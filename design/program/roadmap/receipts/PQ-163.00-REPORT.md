STATUS: PARTIAL
<!-- LIFETIME: ACTIVE_RECEIPT -->
# PQ-163.00 — The rescue

<!-- 2026-09-09 honesty review of commit 638cbd018 (the leftover rescue-shove iterate).
     Traced the leftover test's INPUTS, not its assertions, per .devshots/packets/HONESTY-REVIEW.md.
     Verdict: the leftover iterate's production change is real and the leftover wall the previous
     honesty review named is genuinely gone. STATUS stays PARTIAL — not because the shove is
     suspect, but because the second half of the done-when (≥80 % unaided testers) is uninvented
     and no live-route walk exists. Four claims in the iterate block below are restated with
     measured numbers. Every superseded block is kept so the correction stays legible. -->

```text
PARTIAL  PQ-163.00 — the leftover starter gun really can move the leftover staged scout now, and I traced every link of that chain in live code instead of taking the leftover test's word for it. Three landed shots clear the bar, not twenty. What nobody has done is watch a player do it, and the ≥80 % clause is still uninvented.
WHAT I FOUND     The leftover iterate is honest about the fix and wrong about two of its own numbers. Traced end to end: `weaponImpulseConsequences` is `true` in PRODUCTION_FEATURES (only the frozen 47-A profile pins it off), the live player is `team: 0` against a `team: 1` scout, `drone` is a damageable type with a real collision mask, `defaultDynamic` makes every drone a dynamic Rapier body, the exact-tier physics reach is ~glass-diagonal + 158 wu against a 302 wu stage distance, the `ship` contact material has NO linear damping so a push does not bleed, and the scout is staged ON the player→asteroid line (28 % in) so the gun's own impulse direction points at the wall. The last thing that could have eaten the push does not fire either: `aiPorts.js` `_listSquads` skips any actor with `ai.passive` unless it also carries `allowPassiveManeuver` + a faction behaviour profile, and `ai.js` skips passive too — so nothing thrusts against the shove. Then the corrections. (1) The leftover test hand-wrote its packet: `damage: 12`, `damageType: 'thermal'`, `impulse.magnitude: 84`. Production builds `impulsePerHit × (damage / authoredDmg)`, so 84 is only right for a full-strength damage-8 hit — at the leftover test's own 12 the live builder emits 126. The force was not inflated, but that packet is not one the game can send. (2) "~20 shots do not kill the lesson body" understates the hull raise by ~5x: a landed starter hit spends 5.23 of 500 on the default difficulty, so the scout takes 96 hits to destroy. (3) The shove bar is cleared at THREE landed hits (12.82 wu/s), not twenty. (4) Both in-play tests teleport the scout to the wall, so the ~118 wu it must actually cross is never simulated.
WHAT I CHANGED   Nothing in production — the leftover iterate's code change is correct as it stands. In the named test only: the LIVE GATE now feeds the packet production actually emits, built by `buildWeaponDamagePacket` from the authored `wpn_pulse_laser_s` def, so the 84 is DERIVED and the hull spend is the live one; the force claim is pinned as an identity (measured Δv == magnitude / authored mass) rather than as a balance float that would rot; and one new test covers the path that removing invuln made reachable for the first time — a player destroying the scout mid-beat.
WHAT YOU WILL ACTUALLY FEEL   You wake as a tow rig, swing the rock through the wreck, and after the shooting lesson the game says "Put the scout into the asteroid" and marks it. Three hits get it moving fast enough; it drifts the last stretch into the rock. That part now works by the verb the game just taught you. Two rough edges are real. The scout is a bullet sponge — if you read the marker as "kill it", you will empty roughly ninety-six shots into a drone that never shoots back before it dies. And when it does die, the game quietly restages a fresh scout somewhere else on a new bearing while the asteroid stays where it was, so your second attempt is a different and less readable problem than your first.
THE NUMBERS      leftover shove bar cleared at | 3 landed starter hits, 12.82 wu/s vs the 12 wu/s predicate (4 hits = 17.91 wu/s) · Δv per landed hit | 5.25 wu/s = 84 momentum / 16 mass, derived from the authored def, not written into the test · hull spent per landed hit | 5.23 of 500 (default difficulty; 4.55 on veteran) → 96 hits to destroy the lesson body, not the ~20 the code comment claims · staged geometry, seed 47 | player→scout 302.4 wu, scout→wall 117.6 wu, scout on the player→wall line · hits admitted | 20 of 20, zero `target_invulnerable` · focused tests | 10/10 green, `node --test test/rescue-opening.test.mjs` · unaided-tester percentage | none invented, ≥80 % clause NOT DONE
THE FRAMES       none — GPU/browser ban observed; the live-route walk stays peeled, so every claim above is a code-path trace plus a headless measurement, never a picture of the game.
NEXT             Two named leftovers before anyone marks this done: the restage re-rolls the tableau bearing while the wall stands (`_respawnRescueSlot`), and the 500-hull sponge invites the wrong verb. Then the ≥80 % unaided playtest, which is the only thing that can close the done-when.
```

## What this review verified, link by link

The previous honesty review found the wall (staged scout invuln → every hit rejected
`target_invulnerable` before the impulse code ran). The iterate removed it. Because a green test
can pass on inputs the live route cannot produce, each link between "the player pulls the trigger"
and "the scout moves" was traced in live code:

| Link | Verified where | Result |
|---|---|---|
| Impulse feature is on the default route | `runtimeProfiles.js` PRODUCTION_FEATURES | `weaponImpulseConsequences: true` (only `legacy47a` pins it off) |
| Player can legally damage the scout | `main.js:301` player `team: 0`, scout `team: 1` | no `friendly_fire` rejection |
| Scout is a damageable type | `damage.js` `verbAcceptsType('damage', 'drone')` | admitted |
| Projectiles can collide with it | `physics.js` `maskOf` → `Masks.DRONE`, `collides: true` | hits resolve |
| Impulse magnitude is real | `weapons.js` `impulsePerHit: 84`, `resolveWeaponImpulseForHit` | 84 at a full-strength hit |
| Impulse reaches a body | `physicsAuthority.js` `defaultDynamic` (drone → dynamic) | dynamic Rapier record |
| Body is in the physics layer | `activityRuntime.js` reach ≈ glass diagonal + 158 wu vs 302 wu | exact tier, inside reach |
| Push does not bleed | `sg02DynamicBodyOwner.js` `ship` material: angular damping only | no linear damping |
| Nothing thrusts against it | `aiPorts.js` `_listSquads` skips `ai.passive`; `ai.js:155` same | scout never gets a maneuver |
| Push points at the wall | `rescueOpening.js` scout staged on the player→asteroid line | gun direction is the shove direction |

## What is still NOT proven, and cannot be from here

- **The crossing.** Both in-play tests set `scout.pos = asteroid.pos + 40` by hand. What the file
  proves is packet admission plus the speed the gun raises; the ~118 wu the scout must travel, and
  the 5 wu/s random-bearing drift pulling it off the wall line during that travel, are unsimulated.
  Arithmetic says it fits (≈5.6 s at 4 hits, ≈28 wu of lateral drift against a 64 wu contact ring)
  — arithmetic is not a run.
- **`physics_rejected`.** `impulseApplied: true` in the LIVE GATE is guaranteed by the test's own
  port. Production routes to SG-02's `applyImpulse`, which returns `false` without a live dynamic
  record. No node test can observe that failure mode; only a live route can.
- **The ≥80 % unaided clause.** Uninvented. No tester has been sat in front of this rail.
- **Any picture.** GPU/browser ban observed. No headed capture, no live walk.

## Two leftovers this iterate created or exposed

1. **The scout is now killable, and the restage moves the target off the wall line.**
   `_onRescueKilled` has no scout branch, so a destroyed scout is caught one 0.2 s resolve tick
   later by `_resolveRescueDone` → `_rescueFail('shove', 'scout lost')` → `_respawnRescueSlot`.
   That is a retry, not a wall — newly pinned by a test. But `_respawnRescueSlot` re-rolls a whole
   tableau bearing from the *current* player position and spawns the scout slot only, so the
   standing asteroid keeps its old position while the fresh scout is placed relative to a bearing
   that no longer relates to it. The first attempt is a clean "push it along this line"; the retry
   is not. Before this iterate the scout could not be destroyed, so this path was reachable only by
   the 1500 wu escape.
2. **500 hull invites the wrong verb.** The marker says "Scout" one beat after the game teaches
   shooting. 96 landed starter hits to destroy an unarmed, non-firing drone is a long time to spend
   confirming that killing it is not the lesson. The hull raise is defensible (the body must survive
   the burst); the number chosen is ~5x what the code comment says it is for.

## Route and wiring

- Default route only: `game:started` without a scenario payload stages the tableau and arms the
  rail. Harness boots that name a scenario slice (47a sim, lab scenarios) keep their own cast.
- The 10-beat drill table is untouched: same keys, same order, same lines. B0 is still thrust.
- 47-A is untouched: no edits outside this leftover write set.

## The three verbs

1. **Swing** (after tether): latch the loose rock, winch to <=60 wu, release, and put it through
   the derelict at >=15 wu/s. Still real. Fail/escape restage still real.
2. **Shove** (after burst): put the leftover scout (mass 16, hull 500, inert, unarmed, **hittable**)
   into the asteroid at >=12 wu/s. The authored Pulse Laser S hit carries 84 momentum through
   `buildWeaponDamagePacket` → `routeDamage` → `applyImpulse` (Δv = J/m = 5.25 wu/s per hit, so the
   bar falls at three landed hits). Derelict and beacon stay leftover invuln. The gunnery trainer
   around the `onboarding.js` trainer spawn stays leftover invuln. Whip-into-scout remains a
   leftover untaught escape; it is no longer the only path.
3. **Grab-and-run** (after disengage): latch the escape pod and carry it inside the beacon at
   running speed (>=10 wu/s). Mechanically real; previously unreachable while shove was a wall.

## Files

Production, from the leftover iterate (commit 638cbd018) — unchanged by this review:

- `src/onboarding/rescueOpening.js` — leftover scout invuln removed; leftover hull 500.
- `src/systems/onboarding.js` — leftover spawn restamp is derelict + beacon only; leftover
  scout respawn no longer stamps `_invulnUntil = Infinity`. Trainer invuln untouched.

Changed by this honesty review (named test + named receipt only):

- `test/rescue-opening.test.mjs` — LIVE GATE fed by production `buildWeaponDamagePacket` from the
  authored `wpn_pulse_laser_s` def instead of a hand-written `damage: 12` / `thermal` / `84` packet;
  the force claim pinned as the identity `Δv == magnitude / mass` (measured on the entity, not a
  balance float); one new test for a destroyed scout mid-shove (fail + restage, no wall, and the
  wall stands); header states the two things the file cannot see.
- `design/program/roadmap/receipts/PQ-163.00-REPORT.md` — this review.

## No fake playtest

No unaided-tester percentage is claimed. The ≥80 % clause stays NOT DONE / not invented.
Headed capture stays peeled. Queue not marked. Nothing committed by this review.

## Honesty that still holds

- `combat:shove` is still published by `onboarding.js` when the leftover rail decides the
  leftover beat is done. That receipt is not an independent leftover physics event.
- The leftover test harness has no Rapier port. Impulse in the LIVE GATE is leftover
  `routeDamage` → leftover `helpers.combatPhysics.applyImpulse` using Δv = J/m. The magnitude and
  the mass are now read out of production data rather than asserted, but the port is still this
  file's own, so it can never reject the way SG-02 can.
- Swing and grab inputs were already leftover production events. That finding is unchanged.

<details><summary>Iterate claim block (2026-09-09, commit 638cbd018), superseded by the honesty review above</summary>

```text
PARTIAL  PQ-163.00 — the leftover starter gun can now move the leftover staged scout. Swing and grab were already real; shove is reachable by the verb the beat teaches. The ≥80 % unaided playtest is still uninvented.
WHAT I FOUND     Honesty review 2026-09-09 was right: makeRescueCastSpecs staged the scout flags.invuln + _invulnUntil Infinity, and _spawnRescueCast / _respawnRescueSlot restamped scout invuln, so routeDamage rejected every Pulse Laser S hit as target_invulnerable before applyImpulse.
WHAT I CHANGED   Removed leftover scout invuln from rescueOpening.js. Raised leftover scout hull to 500 so ~20 leftover starter shots do not kill the lesson body. Spawn restamps leftover derelict and leftover beacon only; leftover scout is not restamped on spawn or respawn. Gunnery trainer invuln is untouched. Flipped test/rescue-opening.test.mjs LIVE GATE to leftover production routeDamage: hits land, leftover Δv = J/m (84/16) raises leftover speed, leftover shove completes from leftover gun contact.
WHAT YOU WILL ACTUALLY FEEL   You still wake as a tow rig, swing the rock through the wreck, then the rail says "Put the scout into the asteroid" after the burst lesson. Shots now shove the scout. The derelict, the beacon, and the gunnery trainer stay invulnerable. Nobody has sat an unaided player in front of this rail and counted finishes.
THE NUMBERS      leftover shove: starter-gun hits on the leftover staged scout | 20 of 20 admitted, none target_invulnerable · leftover scout still alive after those 20 · leftover speed after leftover gun impulse | at or above the 12 wu/s leftover completion predicate · focused tests | node --test test/rescue-opening.test.mjs (this iterate) · no invented tester percentages: the ≥80 % done-when is still peeled, unmeasured
THE FRAMES       none — GPU/browser ban observed; headed capture stays peeled.
NEXT             Invent the 80 % unaided playtest, or move to the next leftover leaf. Do not mark the queue done on shove reachability alone.
```

Restated by the review above: the "~20 shots" hull claim (real figure 96), the implied
twenty-shot shove (real bar 3 landed hits), the hand-written packet described as the production
force identity (it was not; it is now), and "shove completes from gun contact" (the contact is
teleported in every test).

</details>

<details><summary>Honesty-review NOT DONE block, correctly diagnosed and now fixed in production</summary>

```text
NOT DONE  PQ-163.00 — the default New Game route does open on a tow-rig rescue, and the swing and the grab-and-run are real. The shove is not: the staged scout is invulnerable, so the starter gun the beat teaches cannot move it, and the beat it gates holds the rest of the rail.
WHAT I FOUND     The opening rail taught thrust, brake, latch, winch, cut and dock but never handed the player the store-page fantasy: swing a rock, keep the speed. Boost, draw-to-fly and wells stay for later leaves; this leaf stages the rescue the rest of the packet builds on.
WHAT I CHANGED   Staged a six-body rescue tableau on every fresh run and slotted its three verbs into the drill's silence gaps: swing after the tether lesson, shove after the burst lesson, grab-and-run after disengage. Fails restage the body and retry.
WHAT YOU WILL ACTUALLY FEEL   You wake as a tow rig next to a loose rock. You latch it, winch tight, and cut it through the wreck — scrap shakes loose, and that part is real. Then the game says "Put the scout into the asteroid", puts a marker on the scout right after teaching you to shoot, and your shots do nothing at all — not a nudge. The rail stops there: for a player doing what the game teaches, nothing else in the tutorial advances, the pod run never opens, and Contract 47-A never starts. There is one way out, by re-using the rope trick from the first beat, but nothing tells you that.
THE NUMBERS      beats reachable by the verb the game teaches | 2 of 3 · shove: starter-gun hits landed on the staged scout | 0 of 20, every one rejected `target_invulnerable` before the impulse code runs · scout speed after 20 hits | unchanged, vs the 12 wu/s the completion predicate demands · focused tests | 9/9 green, including the new live-gate trace · drill regression | 5/5 green · no invented tester percentages: the ≥80 % done-when is peeled, unmeasured
THE FRAMES       none — GPU/browser ban observed.
NEXT             Make the shove reachable by the verb it teaches (see "The fix this needs"), then re-review.
```

</details>

<details><summary>Original DONE claim block, superseded 2026-09-09</summary>

```text
DONE  PQ-163.00 — the default New Game route opens on a tow-rig rescue: one swing-release, one shove, one grab-and-run, in play with no walls, then the 47-A scene plays as the climax it already is.
WHAT I FOUND     The opening rail taught thrust, brake, latch, winch, cut and dock but never handed the player the store-page fantasy: swing a rock, keep the speed. Boost, draw-to-fly and wells stay for later leaves; this leaf stages the rescue the rest of the packet builds on.
WHAT I CHANGED   Staged a six-body rescue tableau on every fresh run and slotted its three verbs into the drill's silence gaps: swing after the tether lesson, shove after the burst lesson, grab-and-run after disengage. Each verb is taught by doing through production events, then silence. Fails restage the body and retry; nothing walls.
WHAT YOU WILL FEEL   You wake as a tow rig next to a loose rock. You latch it, winch tight, and cut it through the wreck — scrap shakes loose. A scout drifts nearby; your starter gun walks it into the asteroid. You grab the escape pod and run it to the beacon. Then Contract 47-A is still waiting, bigger than everything you just did.
THE NUMBERS      rescue beats completing headless on seed 47 | 3 of 3 · verb lines | 3, one per beat, then silence · funnel events | rescue:started, rescue:beat complete/fail per beat, rescue:complete, honest combat:shove · focused tests | 8/8 green · drill regression | 5/5 green · 47a sim golden | hash-equal, deterministic · no invented tester percentages: the funnel is the deliverable
THE FRAMES       none — GPU/browser ban observed; the browser probe cannot boot in this worktree (missing authored-asset tree, same root cause as the baseline entry red).
NEXT             PQ-163.01 (the Range is the door) or campaign dispatch
```

</details>
