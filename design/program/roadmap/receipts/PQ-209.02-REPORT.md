# PQ-209.02 — Income curve past hour 0 (reconciliation receipt, 2026-09-25)

Leaf: `.02` of [`../active/PQ-209.md`](../active/PQ-209.md). Done-when: post-hour-1 income is
non-zero on the derived ten-hour model, and the ~92-shots-per-rock mining click-farm clause is
answered.

## RESULT: DONE — reconciled in `9d6f67f44`; focused fixture re-run green.

Commit `9d6f67f44` ("PQ-209.01/.02 reconciled: their focused fixtures already exist and pass")
closed this leaf but landed without the `receiptRefs` entry the queue schema requires, which made
`program-dispatch` refuse the whole queue (`--next` and `--ready` alike) for every agent. This
receipt records the reconciliation evidence so the schema holds:

- `test/b1-hours-income-encounter-supply.test.mjs` asserts post-hour-1 income > 0 on the derived
  ten-hour model (seeds 4242/8008). Re-run 2026-09-25: 2/2 pass.
- The click-farm clause is answered by aim-weighted seam yield in `src/systems/mining.js`
  (PQ-155.04), per the leaf's own brief.
