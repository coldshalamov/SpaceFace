<!-- LIFETIME: RECEIPT -->
# PQ-204.02 — One paint family, one compile after the frame

```text
STATUS DONE

WHAT I FOUND     Hulls that share a paint no longer mint a unique shader
                 just because a compile id was glued on. After a healthy
                 frame, one queued compile runs, not a burst.

WHAT I CHANGED   Nothing in the renderer this pass. The focused proof was
                 re-run. This note closes the queue leaf.

WHAT YOU WILL FEEL   A newly seen hull does not hitch the picture with a
                 pile of compiles. Weapon lights stay capped.

THE NUMBERS      bar | before | after | target
                 family key drops the concatenated source | yes | yes | canonical key
                 compiles after one present | 1 | 1 | 1
                 dummy prewarm | absent | absent | absent
                 proof | test/pq-204-advanced-perf.test.mjs | pass | green

NEXT             Batching stays off. That leaf already has its receipt.
```
