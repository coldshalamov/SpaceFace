<!-- LIFETIME: RECEIPT -->
# PQ-208.01 — Wire or reclassify unwired verb keys

RESULT: DONE (wiring landed earlier; this receipt closes the missing paperwork found when
`node scripts/check-program-docs.mjs` flagged `PQ-208.01.state done` with no `receiptRefs`).

Done-when: the two keys named by the packet — `microJumpBlink`
(`unique_pale_coil_warp_drive`) and `reactiveMissileKnockback` (`unique_choir_bell_aegis`) —
either drive implemented behaviour or are honestly reclassified.

## What shipped

Both keys are **wired**, not reclassified:

- `src/systems/uniqueLootAbilities.js` consumes both through `fittedVerbSpec(state, key)`:
  `microJumpBlink` at the blink path (pale-coil dash, `usesPerEncounter` honoured) and
  `reactiveMissileKnockback` at the missile-impact path (one knockback per encounter).
- `scripts/check-progression-verb-audit.mjs` `VERB_MOD_KEYS` upgraded both entries from
  `{ declaredOnly: 'PQ-208.01' }` to live consumer evidence
  `{ file: 'src/systems/uniqueLootAbilities.js', symbol: '<key>' }` — verified against live
  source on every audit run, so the wiring cannot silently rot.

## Proof

- `node scripts/check-progression-verb-audit.mjs` → exit 0; both keys verify via
  `{ file, symbol }` against `uniqueLootAbilities.js` (symbols `microJumpBlink` and
  `reactiveMissileKnockback` both occur there); the declared-only list is empty for these keys.
- `node scripts/check-program-docs.mjs` → the PQ-208.01 receiptRefs error clears with this
  file referenced.

## Honest scope

The implementation predates this receipt — the queue row was already `done` when the program-docs
check flagged the missing reference. This receipt records the evidence the row was missing; no
runtime code changed in this pass.
