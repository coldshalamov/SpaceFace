<!-- LIFETIME: RECEIPT -->
# PQ-147.02 — Counterplay, and the Well/Repulsor pin that would not let go

Cover both parts. Seed 26029 is the pin-release route. Seed 14702 is the five-escape bench.

## Part 1 — `check:massline:mass-coupling` was RED

```text
DONE  mass-coupling polarity — A Well that pins a hull is released when a Repulsor of opposite polarity lands on it; the body goes light (mass 12) and is not stuck at 6×.

WHAT I FOUND     Dropping a Well, then a Repulsor to break the pin, did nothing: the Well's emitter reused the player and target ship ids, so the Repulsor spawned on top of the Well and never touched the pinned hull. Even with honest ids, a stronger overlapping Well would keep refreshing Pinned and swallow the Repulsor.

WHAT I CHANGED   Field emitters no longer take an id that already belongs to a living body, and opposite-polarity field statuses cancel by the catalog rule — the newer field is the verb that wins, not whichever pull is stronger.

WHAT YOU WILL FEEL   If a Well has you (or a mark) locked heavy, firing a Repulsor actually lets go — the pin drops and the hull goes light. You will not see a Power Rail line that names that counter; that slot is owned by another agent.

THE NUMBERS      bar | before | after | target
                 Seed 26029, production status/physics route (Well then Repulsor, two ticks each)
                 Target Pinned after Well | present (appliedTick 2, expires 92) | gone | gone
                 Target Unmoored after Repulsor | absent | present | present
                 Target solver mass | 240 (6× of 40) | 12 (0.3× of 40) | 12
                 Deploying owner Unmoored | absent | absent | absent
                 Player/target map type after both deploys | fieldEmitter (ids stolen) | ship | ship

FILES            src/systems/fields.js
CHECKS           node --test test/mass-coupling-states.test.mjs — PASS (3/3)
                 npm run check:baseline — massline PASS; pq020-ceres-topology / sim / sim-v3 still red (same fingerprints as pre-edit)
UNPROVEN         I did not fly this in a headed window. Combat's consumeWith already existed; it never ran on this route until the emitter stopped replacing the ships. The core spawn helper still does not bump nextEntityId for reserved ids — fields only refuse to land on an occupied slot.
```

## Part 2 — PQ-147.02 Counterplay

```text
DONE  PQ-147.02 — Each of the five powers has one named escape, and a trapped body becomes free after that verb on seed 14702.

WHAT I FOUND     The five verbs were already in the field data and kernel (Boost out, Cut the emitter, Sidestep, Out-mass, Cut the hitch). The missing player-facing proof was a named scenario file, and a Well could still trap you after a Repulsor if the Well's pull was stronger.

WHAT I CHANGED   Opposite-polarity fields now treat the newer tool as the answer, and the five escapes are proven on a fixed seed with times in seconds. I did not touch the Power Rail.

WHAT YOU WILL FEEL   Hold boost and a Well cannot keep you; shoot a plow emitter and the shove dies the same tick; strafe out of a cone; hitch heavy mass to shrug a scoop; cut a lock-ring hitch and the lock lets go. The rail still does not print those sentences — Digit 4–8 would need one extra line each, `FIELD_ESCAPES[id].sentence`, and that file is owned by another agent.

THE NUMBERS      bar | before | after | target
                 Seed 14702 (kernel unless noted). Hitch mass 28 unless named.
                 well boost_out time to leave ring | trapped at 4.00s without boost | 1.28s free with boost (live planted 1.30s) | free < 6s
                 repulsor cut_emitter | field still live | 0.02s (1 tick) field gone | same tick
                 cone sidestep | trapped at 2.00s with no strafe | 1.20s free (live 1.22s) | free < 4s
                 skim out_mass | light 16 mass trapped at 3.00s | 2.37s free at mass 216 (live 2.35s) | free < 4s
                 seed cut_hitch | boost still held at 3.00s | 0.52s free after cut (live 0.43s) | free after cut
                 well+repulsor overlap pin (reserved ids 1/2, still inside Well) | Pinned, mass 240 | Unmoored, mass 12 | pin gone

FILES            src/systems/fields.js
                 test/pq-147-02-counterplay.test.mjs
                 design/program/roadmap/receipts/PQ-147.02-REPORT.md
CHECKS           node --test test/pq-147-02-counterplay.test.mjs — PASS (7/7)
                 node --test test/pq-147-02-field-counterplay.test.mjs — PASS (already in tree)
                 npm run check:baseline — massline PASS (this leaf); foreign reds unchanged
UNPROVEN         No shipping-camera capture (headed GPU not in this lane). Digit 4–8 on the Power Rail do not show the escape sentence. I did not edit the Power Rail file: the exact change would be to put each power's escape sentence on the chip under the fitting line so the counter is visible without opening a drill.
```

## Forbidden / shared

Not edited: `src/ui/powerRail.js` (live other-agent owner), `test/mass-coupling-states.test.mjs` (read-only), `design/program/NOW.md`, any render/UI file on the lane forbid list.

`src/combat/statusEffects.js` and `src/data/statusEffects.js` do not exist. Polarity data already lives on the status catalog (`consumeWith`); this leaf honors it on the field apply route instead of special-casing Pinned/Repulsor.

## Entry baseline (pre-edit, not this leaf)

`npm run check:baseline` before the first edit was already red on `pq020-ceres-topology`, `sim-v3` (47-A hash), `sim` (47-A hash), and `massline` (only `check:massline:mass-coupling`). The first three are foreign. Mass-coupling is this leaf.

Post-edit `npm run check:baseline`: `massline` PASS (12/15). The same three foreign reds remain; 47-A hashes are unchanged from the pre-edit run. Budget overage is the same parallel wall-clock complaint, not a new fail.

## Power Rail (named, not made)

`src/ui/powerRail.js`: for each of the five `POWER_ROSTER` slots, set the chip's secondary/hint line to `fieldEscapeOf(row.id).sentence` so the player can read the counter on the rail (Boost out / Cut the emitter / Sidestep / Out-mass / Cut the hitch) without a drill. No restyle.
