<!-- LIFETIME: RECEIPT -->
# PQ-147.02 — Field counterplay

```text
STATUS DONE

DONE PQ-147.02 — Each of the five live powers has one named escape, and a Hitch that starts trapped can take that verb and be free. A well cannot keep a boosting hull. Shoot the plow and the shove dies. Strafe out of a wedge. Hitch heavy mass and a scoop shrugs. Cut a lock-ring hitch and the line is gone.

WHAT I FOUND     Leftover already named one verb per power and proved four of them in the kernel helper. Repulsor was the only one that killed a live emitter. Well, cone and skim never moved a live body; seed cut a live hitch and then asked the kernel to walk out. A reviewer would have called those kernel-only.

WHAT I CHANGED   Adopted the leftover verbs and the kernel helper. Added headless live-body proofs: plant the field, step Rapier, apply the verb on the Hitch. Boost shrugs the well and flies out. Sidestep leaves a planted wedge. Out-mass leaves a planted scoop. Cut the hitch and the live hull leaves the lock-ring. No field is an HP aura. Nothing is typed radiation.

WHAT YOU WILL FEEL   If a ring swallows you, hold boost and you come out. If a plow pins you, shoot the emitter. If a wedge owns the lane, step aside. If a scoop glues a light hull, hitch something heavy. If a lock-ring has you, cut the hitch. You are never in a field with no verb.

THE NUMBERS      bar | before | after | target
                 Powers with a named escape verb | 5 leftover names | 5 | 5
                 Live kinds with a deterministic escape | 1 (repulsor cut) | 5 | 5
                 well boost_out (live body, seed 14713) | kernel-only 1.28 s | 1.30 s | leave the ring
                 repulsor cut_emitter (live emitter) | 0.02 s | 0.02 s | same tick
                 cone sidestep (live body, seed 14715) | kernel-only 1.20 s | 1.22 s | leave the wedge
                 skim out_mass (live body, seed 14717) | kernel-only 2.37 s | 2.35 s | leave the sheet
                 seed cut_hitch (live body after cut) | kernel walk 0.52 s | 0.43 s | leave the lock-ring
                 Authored volumes that are a sphere | 0 | 0 | 0
                 Fields that deal HP / type radiation | 0 | 0 | 0

THE FRAMES       Not taken (headed GPU forbidden this lane). Headless seed 14702 family is the proof.

NEXT             PQ-147.03 Cluster and detonate (after PQ-137.09).
```

## Escape table (live body)

| id | verb | timeS |
|---|---|---|
| well | boost_out | 1.30 |
| repulsor | cut_emitter | 0.02 |
| cone | sidestep | 1.22 |
| skim | out_mass | 2.35 |
| seed | cut_hitch | 0.43 |

Kernel helper times (leftover, kept): well 1.28 s, cone 1.20 s, skim 2.37 s, seed 0.52 s. Live times are the ones that count.

## Hypothesis (one)

A reviewer would reject well / cone / skim as kernel-only, and seed as a live cut plus a kernel walk. Planting the same leftover field through `plantField`, stepping Rapier, and applying the named verb on the Hitch should free a live body in one deterministic time.

## Proof

| Claim | Scenario | Seed | Result |
|---|---|---|---|
| Five named verbs | `fieldEscapeOf` vs `POWER_ROSTER` | — | well Boost out, repulsor Cut the emitter, cone Sidestep, skim Out-mass, seed Cut the hitch |
| Well: Boost out | planted npc well + live Hitch, `FIELD_FLAGS.enabled = true` | 14713 | 1.30 s leave; twin without boost stays 4 s |
| Repulsor: Cut the emitter | planted plow, emitter `alive = false`, one tick | 14703 | field gone same tick (0.02 s) |
| Cone: Sidestep | planted wedge + live Hitch | 14715 | 1.22 s leave; twin without strafe stays 2 s |
| Skim: Out-mass | planted sheet, light hull vs hitchMass 200 | 14716 / 14717 | light stays 3 s; heavy leaves 2.35 s |
| Seed: Cut the hitch | planted lock-ring, latch, cut, live leave | 14718 | boost cannot beat the hitch; after cut, free in 0.43 s |

## Honesty gaps

Closed this leaf: well, cone and skim now move a live Rapier body under a planted field, not only `integrateFieldEscape`. Seed now leaves on the live hull after `cutFieldHitch`.

Left on purpose: boost and sidestep still apply the leftover Hitch floor (`FIELD_ESCAPE_BOOST_ACCEL` / 90 wu/s²) as a queued membrane impulse. This lane stays off `input.js` and flight. Coupling is live — `flags.boosting` and `data.hitchMass` go through `fieldBodyProfile`. Live flight may push harder; the floor is enough.

## Forbidden paths

Not edited: `src/ui/screens/range.js`, `src/render/renderer.js`, `src/render/vfx.js`, `src/systems/input.js`, `design/program/roadmap/program-queue.json`, `design/program/NOW.md`, `design/FRONTEND_DIRECTION.md`. No headed capture. No commit.

## Checks

| Check | Result |
|---|---|
| `node --test test/pq-147-02-field-counterplay.test.mjs` | 13 pass (9 leftover + 4 live-body) |

STATUS DONE
