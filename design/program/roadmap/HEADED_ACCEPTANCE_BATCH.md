<!-- LIFETIME: VOLATILE -->
# Headed-acceptance recovery — retained sequencing context

Reconciled 2026-08-02 after the H1/H2/H3 recovery wave. All functional H1 rows are accepted. The
integrator completed the evidence-bound H2 reviews without inventing external reviewer blockers;
their `REVISE` outcomes now route through exact asset-production leaves. PQ-019, PQ-020, PQ-023,
and PQ-024 matched H3 cells are accepted on candidate-bound receipts. PQ-034 and PQ-025 owner-fact
leases are released. `program-queue.json` plus `program-dispatch.mjs --ready` are current authority;
this page retains ordering rationale and must not override their exact units.

## Ordering rationale

1. Functional headed runs first (contention-tolerant: broker cells, captures, parity).
2. One consolidated evidence-bound verdict session (all art/legibility/motion judgments batched).
3. Matched-performance captures LAST, each in a quiet-machine window — per the NOW contention
   facts, a perf sample taken while anything else runs is not a measurement.

## Phase H1 — completed results

| Row | What runs | Upgrades |
|---|---|---|
| `check:assets:live` | **PASS** on real D3D11 GPU | functional authored admission closed; no timing claim |
| PQ-021 route | **PASS** Browser broker + Electron parity | H2 legibility/reachability accepted; physical-device feel remains an unclaimed hardware-specific extension |
| PQ-019A presentation | **PASS**; facilities/counts retained plus brokered capsule views at runtime zooms 45/66/108 | H2 keeps the capsule and revises facility identity through exact asset leaves |
| PQ-019C route | **PASS**; retained DOM/lawful evidence plus fence, confiscation, destruction, recovery continuation | H2 keeps all five route outcomes |
| PQ-020 route | **PASS**; the request-lifecycle defect has a seconds-scale regression and the fresh Browser/Electron pair passed 21/21 frames per runtime with exact semantic parity | accepted receipt `PQ-020-ceres-h1-capture-REPORT.md` |
| PQ-023 cues | **PASS**; retained combat/reduced/dense evidence plus Cathedral Browser/Electron continuation | H2 repair/re-review and H3 are accepted |
| PQ-022 asset leaves | **PASS** for 11 exact identities and 13 stills | H2 is complete at `REVISE`; exact asset-production chains remain |
| Electron end-to-end | **PASS** source-Electron menu → New Game → physical dock → Ledger on Intel D3D11 | exact-package, physical controller/audio/display, and matched-performance gates remain in PQ-041 |

PQ-024's fixed-seed Browser route, distinct Electron parity cell, and matched H3 performance are
accepted. The relay visual review is also terminal at `REVISE`; only its deliberately separate
asset re-author/recapture/re-review and receipt-promotion chain remains.

## Phase H2 — evidence-bound reviews complete

The earlier snapshot incorrectly encoded absent human roles as blockers. The execution protocol now
states the actual solo-recovery rule: the primary integrator may perform the same evidence-bound
review and record it as a self-review. Those exact review units are complete:

- PQ-021 is accepted for legibility/reachability, while absent physical hardware limits only a
  device-specific tactile claim;
- PQ-019 keeps the route and capsule but revises facility identity;
- PQ-020 keeps Cathedral/route semantics but revises selected arrival presentation;
- PQ-022 revises the relay and named corridor asset families;
- PQ-023 keeps the accessible Cathedral/cue family after its completed repair/recapture/re-review
  chain.

PQ-024 inherits the completed relay `REVISE` verdict; it is dependency-gated only by the exact
relay re-author/recapture/re-review and receipt-promotion units, not by an unavailable person.

## Phase H3 — matched performance (quiet machine, one lane at a time)

| Row | Contract |
|---|---|
| PQ-020 Cathedral-keyed matched route | **PASS** — receipt `PQ-020-h3-performance-REPORT.md` |
| PQ-019 representative performance | **PASS matched cell** — receipt `PQ-019-h3-performance-REPORT.md`; its separate absolute target remains open and unwaived |
| PQ-023 dense-scene target/floor | **PASS** — receipt `PQ-023-h3-performance-REPORT.md` |
| PQ-024 committed-floor/producing-relay | **PASS** — receipt `PQ-024-h3-performance-REPORT.md` |

The five PQ-025 performance owner facts are complete at `4df37347`: full-window bounded p50/p99/raw
intervals, calibrated missed-vsync, residency, and completed-frame draw/triangle counts. This closes
only the owner seam. No native PQ-025 cell may start until its six exact promotion dependencies are
terminal; H3 rows above still use their packet-local matched harnesses.

## Phase H4 — promotions and bindings (integrator, after H1–H3)

1. Upgrade receipts: PQ-019A/PQ-019C (→ parent PQ-019 promotion incl. W03/W04/W05 alias
   replacement), PQ-020 (→ `integrated` + PQ-018 Phase-4 closure and PQ-018 checkoff decision),
   PQ-021 (→ `route_accepted`/`integrated`), relay-collar (→ `route_accepted`).
2. Bind blobs: relay-collar receipt → the already functional PQ-024 survey claim, then promote
   PQ-024 after the relay repair/recapture/re-review chain upgrades that receipt; H3 is already
   accepted. Cues milestone receipt (→ `milestone_accepted`) → PQ-025 row; corridor-assets
   milestone receipt likewise once its three verdict-pending leaves pass H1/H2.
3. PQ-025: freeze matrix/rubric/profiles/retention, then Phase 2 calibration.

Retire this file when H4 completes; its content collapses into receipts and the queue.
