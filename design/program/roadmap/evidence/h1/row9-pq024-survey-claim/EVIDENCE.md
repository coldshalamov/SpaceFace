# H1 row 9 — PQ-024 survey-to-relay route pair

**Result: PASS — functional Browser/Electron H1 accepted.**

Candidate `c10f8921` consumed Browser claim `31656-aad66eda6f14d0177d92cd8d` at fixed seed
`24024`. The broker bound candidate digest
`61fa7c4ddaa125620b96af9fc2fae3027f8c7439935631e01a1a920215ee604b` to one Browser launch.
The route passed on real Intel ANGLE/D3D11 with zero page issues. After that prerequisite passed, the
distinct Electron wrapper ran the same public actor once, produced an identical normalized semantic
projection, used the same real GPU path, emitted zero page issues, and closed its owned runtime.

## Proven corridor

Both hosts used visible New Game, map, dock, Market, Undock, unified-map, Massline, Asteroid Ops,
machine-palette, F5, Continue, and re-entry controls. The retained receipts prove:

- all three required material purchases;
- deterministic Survey reveal before Core placement;
- an anchored Core and adjacent extractor at the same site;
- a real positive `cmdty_silicate` production mutation and authoritative receipt;
- exactly one `place_claim_outpost_relay` before and after Continue;
- matching restored receipt, producing lifecycle, relay count, and same-site public re-entry.

The final Browser and Electron frames visibly show the restored `PRODUCING` claim with `ASSAY 3 CELLS`.
The raw runtime entity ids and tick timing differ by host as expected; the normalized route semantics
are byte-for-byte equal.

## Review artifacts

Browser frames:

- [Market materials](browser-01-market-materials.png)
- [Survey reveal](browser-02-survey-reveal.png)
- [Core committed](browser-03-core-committed.png)
- [Producing relay exterior](browser-04-producing-relay.png)
- [Continue and same-site re-entry](browser-05-continue-reentered.png)

Electron frames use the corresponding `electron-01-...` through `electron-05-...` names. Machine
authority is retained in `browser-route-receipt.json`, `electron-route-receipt.json`,
`browser-claim.json`, `browser-fast-gate.json`, `browser-launch-counts.json`,
`browser-run-result.json`, and `route-pair-summary.json`.

## Scope boundary

This is functional H1 evidence only. It is explicitly marked as non-performance evidence by both
route receipts. It does not supply a human gameplay/visual verdict, physical-controller or
screen-reader acceptance, matched quiet-window performance, or the relay's independent
`route_accepted` promotion.
