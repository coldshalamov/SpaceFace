<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-208 — Build-identity drift guard and unwired verb keys

```yaml
queueId: PQ-208
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: master at or after 2026-09-18; progression vertical landed
stableRoute: BUILD-IDENTITY-DRIFT-GUARD
```

## Outcome

The verb ladder is a contract, not a label. A module can no longer declare a verb key that nothing
implements without failing a guard, and the two keys currently declared as verbs with no shipped
capability — `microJumpBlink` and `reactiveMissileKnockback` — are either wired or honestly
reclassified.

## Source

[`../../PROGRESSION_VERTICAL_2026-09-18.md`](../../PROGRESSION_VERTICAL_2026-09-18.md);
[`../../PROGRESSION_VERTICAL_AUDIT.md`](../../PROGRESSION_VERTICAL_AUDIT.md);
`scripts/check-progression-verb-audit.mjs`.

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | A promised capability is not a blank | The audit fails when a declared verb key names no implemented behaviour |
| `.01` | No item lies about what it grants | `microJumpBlink` and `reactiveMissileKnockback` are wired to a real capability or reclassified |

## How agents get this wrong

Weakening the generated audit or hand-editing `PROGRESSION_VERTICAL_AUDIT.md` (it is generated).
Adding a new module class instead of classifying honestly. Re-recording an expected file to pass.

## Write set

`scripts/check-progression-verb-audit.mjs`, `src/data/modules.js`, `src/data/techVerbLadder.js`,
`src/combat/`.

## Non-goals

Adding a verb key per item. Enforcing taste through module counts.

## Closing

Run `npm run check:progression:verbs` and `npm run check:build-identities`. If the guard fires on a
planted unwired key and the two keys are resolved, stop.
