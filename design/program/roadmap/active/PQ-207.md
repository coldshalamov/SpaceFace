<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-207 — People-who-remember residuals

```yaml
queueId: PQ-207
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: master at or after 2026-09-19; PQ-150 leaves .00–.03 landed
stableRoute: PEOPLE-WHO-REMEMBER-RESIDUALS
```

## Outcome

The world's memory layer is not only authored, it is affordable and reachable. The berth mechanic
does not name the same speaker twice, does not add an unmeasured ledger build to the 18-frame status
loop, and does not crowd its stage; the radio layer actually fires on a live open-world session
where the compass measured `bark:shown` at zero.

## Source

[`../receipts/PQ-150.01-REPORT.md`](../receipts/PQ-150.01-REPORT.md) (gaps 2–4);
[`../receipts/PQ-150.02-REPORT.md`](../receipts/PQ-150.02-REPORT.md);
[`../../../../docs/program-compass/2026-09-ACTUAL-GAME.md`](../../../../docs/program-compass/2026-09-ACTUAL-GAME.md) defect 8.

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | The dock stays smooth while the mechanic talks | The per-refresh `buildShipLedger` cost is measured and removed from the common clean-hull path |
| `.01` | One mechanic, one voice, room to read | The double `Mechanic` article is resolved and the narrow-breakpoint header no longer crowds the stage |
| `.02` | The radio actually speaks in a real session | `bark:shown` fires on the live route, or the compass finding is corrected on the audio bus |
| `.03` | We know the readback lands | Packet test re-run in a live environment; blind percentage produced, not invented |

## How agents get this wrong

Removing the campaign ledger to fix the double speaker. Hiding the mechanic article instead of
deciding. "Graceful silence" as a completed audio layer. Inventing a playtest percentage.

## Write set

`src/story/mechanicVoice.js`, `src/story/storyLedger.js`, `src/ui/dockArrival.js`,
`src/ui/views/stationFrames.js`, `styles/orbital.css`, `src/systems/barkDirector.js`,
`src/data/bandRadio.js`, `test/pq-150-01-mechanic.test.mjs`, `test/pq-150-02-radio.test.mjs`.

## Non-goals

Replacing the PQ-178.03 campaign ledger, a dialogue tree, or a second radio surface.

## Closing

Dock a clean hull and a scarred hull; fly a session and read the bus. If the mechanic is one voice
and the radio fires live, stop.
