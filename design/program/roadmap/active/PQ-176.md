<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-176 — Customization with consequences: a fit you can feel and predict

```yaml
queueId: PQ-176
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

Building a ship is a mental game with a physical answer. Nested capacity budgets give each hull a shape (the scout's engine pool is fat, the hauler's gun pool is thin); every module's mass feeds the flight model so a gun boat flies like one; drive and manoeuvring thrusters are separate parts so fast-but-clumsy and nimble-but-slow are real builds; mounts gate weapons by size and type; energy and heat are flows the fit screen shows as a sustained-fire margin; any module worth fifteen percent of the budget is visible on the hull; presets switch play styles; and the fit screen shows the handling deltas (turn radius, reversal time, top speed, tow class) before the player pays.

## Source

`CANONICAL_BUILD_MAP.md` §17 (admitted 2026-09-03) → **A · Adventure depth**, wave **BETA**. Gap it closes: Six-type slot grid; module mass barely matters; nothing makes a max-gun build feel different in the hands; the ship screen shows six of thirty-five derived fields; the signature mechanic's ceiling sits behind a 2.5 M tech. Reference: Endless Sky outfit space, Starsector OP and mounts, X4 engine/thruster split, FTL power routing. Routes through existing sources: S3-F5-OUTFITTING, RV-OUTFITTING, PQ-142, PQ-155, PQ-156, §11.4 Power Bar.

## What exists (audited 2026-09-03)

SPEC3-23/24 (designs written), `src/data/ships.js` (outfitSpace/weaponCapacity/engineCapacity fields), `modules.js` (72), `weapons.js` (25), `src/systems/ships.js` getDerivedStats (~35 fields), `src/ui/ship/shipScreen.js` + loadout presets (J13 done), `src/ui/screens/outfitting.js`, `livingHullPresentation.js`, parts hot-swap in `partsLibrary.js`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` (after PQ-137.03) | **Nested budgets and mass as the law.** Master pool plus weapon and engine sub-pools per hull; ship mass = hull + modules + current cargo, fed to the propulsion profile; a migration check proves every current default fit still fits. | A full-gun fit and a full-cargo fit of the same hull differ in reversal time by ≥ 25 % on the flight bench; migration check green. |
| `.01` | **Drive and thruster split.** Drive owns forward thrust and top speed; manoeuvring thrusters own turn torque, strafe and brake; each hull gets a default thruster part. | Two builds of one hull: fast-clumsy and nimble-slow, both viable on the Crucible bench; numbers printed. |
| `.02` | **Mounts gate by size and type; fixed vs turret.** S/M/L mounts with type classes; fixed hardpoints get more output than turrets so aim skill is paid. | Fit screen refuses illegal mounts with a sentence; a fixed-mount build out-damages its turret twin by the authored margin. |
| `.03` | **Flows on the fit screen.** Energy draw, heat and cooling as sustained-fire margin; the screen predicts turn radius, reversal time, top speed and tow class before commit; every module has a one-line sentence. | Blind test: a player predicts handling from the screen and is right within 20 % on the bench. |
| `.04` | **Visible builds.** Any module ≥ 15 % of the budget is visible on the hull (parts hot-swap); drives change nacelle glow; big guns bristle. | Stills at the shipping camera show the build; the asset reachability check stays green. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Adding budgets as UI-only numbers that the flight model never reads: leaf .00's bar is a flight-bench number, not a screen.
- Letting mass changes bypass the propulsion profile (writing to velocity or drag): mass enters only through the kernel's body mass.
- Hiding the prediction behind a tooltip: the handling deltas are the screen's primary content.

## Entry conditions

- [ ] Dependencies integrated at the candidate base: `PQ-137`, `PQ-142`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario on a fixed seed before tuning (the bar this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, the fun-loop bench, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Nested budgets and mass as the law.
- [ ] `.01` Drive and thruster split.

### Phase 2 — owner wiring

- [ ] `.02` Mounts gate by size and type; fixed vs turret.
- [ ] `.03` Flows on the fit screen.

### Phase 3 — presentation and route

- [ ] `.04` Visible builds.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `src/data/ships.js`
- `src/data/modules.js`
- `src/data/weapons.js`
- `src/systems/ships.js`
- `src/ui/screens/outfitting.js`
- `src/ui/ship/`
- `src/render/partsLibrary.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No rarity tiers; no set bonuses; no crafting grind; nothing that makes the fit screen a spreadsheet without a prediction.

## Performance and quality budget

Mass changes enter only through the body mass the kernel reads; derived stats are cached per fit change, never per tick. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:outfitting-buy-fit`, `npm run check:save-schema`, `npm run check:sim:v3`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

- Which gate row (§15.1, §16.1, §17.1, §18.1) did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden, on the default route?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, give an NPC a hidden gyro, scale hit points, or add a dialogue tree?
- Does the report read in the owner's words (§1.4)?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the gate row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-176-<leaf>-REPORT.md`; queue unit state updated by the integrator.
