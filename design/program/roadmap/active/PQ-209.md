<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-209 — Actual-game defects from the program compass

```yaml
queueId: PQ-209
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: master at or after 2026-09-18
stableRoute: ACTUAL-GAME-DEFECTS
```

## Outcome

The measured shape of hours 3–10 changes. Encounter supply returns past hour 4, the signature
combat→salvage→economy chain completes at least once in ordinary play, and income is a curve
past the first-hour cliff instead of a step function.

## Proof policy (packet revision 2 — 2026-09-19 owner ruling)

Each leaf names a FOCUSED, seconds-scale proof in its owning lane's suite. The multi-session
playthrough battery is an L4 diagnostic instrument (`docs/VALIDATION_WORKFLOW.md`), run only if
some leaf's claim cannot be expressed as a focused fixture — one archetype, one seed, hours
capped to the question. It is not this packet's acceptance, and "rerun the battery" is not a
closing step.

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | The galaxy keeps offering fights in hour 5 | A focused fixture asserts non-zero encounter supply from the director/planner past the hour-4 window on held-out seeds, and the runtime gate that starved it is covered by its own test |
| `.01` | Kill, salvage, sell, consequence — once, at least | A focused fixture drives the real chain owners (kill → aftermath → salvage → sale → consequence) end-to-end on a fixed seed in seconds |
| `.02` | Money after the first hour | A focused fixture asserts income past the first-hour window on the derived model (field regrowth seam + resource-work publisher already own their tests) |

## Source

[`../../../../docs/program-compass/2026-09-ACTUAL-GAME.md`](../../../../docs/program-compass/2026-09-ACTUAL-GAME.md).

## How agents get this wrong

Tuning the pilot instead of the world. Re-running hours-long sessions as a substitute for
writing the focused fixture. Claiming a fix from a log nobody read. Re-litigating what is not
broken (the first hour, world reachability, determinism).

## Write set

`src/systems/world.js`, `src/data/encounters.js`, `src/systems/economy.js`,
`src/systems/fieldDepletion.js`, `src/systems/missions.js`, plus the focused fixtures each leaf
adds. `scripts/run-actual-game-playthrough.mjs` (the instrument, only if its findings are wrong).

## Non-goals

Sector-boundary hysteresis — already closed by `3e84b438e`. A stronger pilot. New acceptance
infrastructure beyond focused fixtures. Routine battery reruns.

## Closing

Each leaf's focused fixture is green on held-out seeds and committed in its owning lane. If some
leaf genuinely cannot be expressed as a focused fixture, run ONE narrowed diagnostic session,
cite the ledger lines, then still write the fixture that captures the finding. Stop there.
