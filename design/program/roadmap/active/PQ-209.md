<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-209 — Actual-game defects from the program compass

```yaml
queueId: PQ-209
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: master at or after 2026-09-18; compass battery complete
stableRoute: ACTUAL-GAME-DEFECTS
```

## Outcome

The measured shape of hours 3–10 changes. Encounter supply returns past hour 4, the signature
combat→salvage→economy chain completes at least once on the scripted battery, and income is a curve
past the first-hour cliff instead of a step function. The proof is the rerun six-session battery's
hourly tables, not a capture.

## Source

[`../../../../docs/program-compass/2026-09-ACTUAL-GAME.md`](../../../../docs/program-compass/2026-09-ACTUAL-GAME.md).

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | The galaxy keeps offering fights in hour 5 | `encounter:spawned` is non-zero in hours 5–9 on both hunter seeds |
| `.01` | Kill, salvage, sell, consequence — once, at least | The complete `combat_salvage_economy` chain fires on the battery and progression spend is reachable |
| `.02` | Money after the first hour | Income continues past hour 0; the ~92-shots-per-rock mining loop is addressed |

## How agents get this wrong

Tuning the pilot instead of the world. Reading a partial hour-10 bucket. Claiming a fix from a
single session instead of the fixed-seed battery. Re-litigating what is not broken (the first hour,
world reachability, determinism).

## Write set

`src/systems/world.js`, `src/data/encounters.js`, `src/systems/economy.js`,
`src/systems/fieldDepletion.js`, `src/systems/missions.js`, `scripts/run-actual-game-playthrough.mjs`
(the instrument, only if its findings are wrong).

## Non-goals

Sector-boundary hysteresis — already closed by `3e84b438e`. A stronger pilot. New acceptance
infrastructure beyond the existing battery.

## Closing

Rerun the six sessions and diff the hourly tables. If `encounter:spawned` moved and the chain fired,
stop.
