<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-131 — Asteroid Works art: authored assets to the ship bar

```yaml
queueId: PQ-131
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: read ASTEROID_WORKS_ART_CAMPAIGN.md (inventory, works camera, build steps), the technique contract, the adversarial review workflow, design law §2.7, the live renderer owners, and exact dirty paths at claim time
stableRoute: ASTEROID-WORKS-ART
```

## Outcome

Every object in the mine — rover, Massline Core, extractor, refinery, surface derrick, conduit kit, gas
tap, fabricator, cargo port + crates + courier pod, and the ore/gas/exotic/ice inclusions — is an
**authored, reviewed, wired PBR asset** at the flight ships' bar, loaded through the game's release
pipeline, seen through the works camera. The procedural builders for those objects are deleted. The owner,
looking at the live mine beside the flight game, does not see a different game.

Owner verdict that created this packet (2026-08-21, live build): "the rover is like this 8-bit NES model
inside this 3d world … you're intentionally cutting corners … I'd rather you at least made plans for the
future development to actually get the game that we want." `PQ-130`'s presentation work stands; its
acceptance is blocked on this packet.

## Entry conditions

- [ ] Read [`ASTEROID_WORKS_ART_CAMPAIGN.md`](../../ASTEROID_WORKS_ART_CAMPAIGN.md) §1–§6 and the
      claimed unit's inventory row; [`ADVANCED_MODEL_TECHNIQUE_CONTRACT.md`](../../../../docs/visual-assets/ADVANCED_MODEL_TECHNIQUE_CONTRACT.md);
      [`MODEL_ADVERSARIAL_REVIEW_WORKFLOW.md`](../../../../docs/visual-assets/MODEL_ADVERSARIAL_REVIEW_WORKFLOW.md);
      design law §2.7.
- [ ] `node scripts/program-dispatch.mjs --id PQ-131`; take the first claimable unit. `.00` is the
      prerequisite for every asset unit; `.01` (rover) is first after it.
- [ ] Blender reachable (MCP bridge or headless `blender -b`), `tools/blender/` conventions read.
- [ ] `npm run check:baseline` at the candidate before the first edit; inherited reds noted (the 47a
      sim links are a stale golden on master as of 2026-08-21).

## Live seams

| Domain | Current owner | Existing seam to reuse | New seam |
|---|---|---|---|
| Works renderer | `src/ui/asteroid/asteroidRenderer3d.js` | rover/machine/derrick builders called from `src/render/asteroidInteriorPreview.js`; named runtime state (hopper stages, bit heat, lamps, gantry progress, flow, lid latch, scar) | `loadWorksPart(id)` returning a LOD-aware group with the named hooks; procedural builder per object deleted when its asset lands |
| Release loading | `src/render/assetLoader.js` (`createSharedKtx2LoaderOwner` :215 re-runs `detectSupport` per renderer; `createAuthoredAssetLease` :412; `loadAuthoredPart` :451; `disposeAuthoredAssetRuntime` :727), `assets/ships/release/release_manifest.json`, `assets/ships/render-packages/pilots.json` | the existing lease path — precedent on a second WebGL context is `src/ui/shipPreviewMount.js` :668/:1013. **A second KTX2Loader / transcoder / meshopt stack is an automatic reject.** | `loadWorksPart(id)` binding that lease to the works renderer (landed as `src/ui/asteroid/worksPartLoader.js` under `.00`) |
| Release build | `scripts/build-hull-release-assets.mjs` | the canonical KTX2/LOD builder | works-part category if the builder needs one |
| Review camera | `tools/blender/spaceface_chase_camera.py`, `chase_visible_faces.py` | chase framing + hidden-face culling | `spaceface_works_camera.py` (`works_top` / `works_edge` / `works_site`), hidden-face culling for the works camera |
| Evidence | `scripts/capture-asteroid-works.mjs` | works stills | a still per asset beside a flight still |

## Release pipeline for a works asset (verified 2026-08-21 under `.00`; every unit `.01`–`.10` must do all four)

1. **Release build + manifest row** — through a canonical builder. `scripts/build-hull-release-assets.mjs` is hardcoded to the ten hulls (`HULLS`, `kind: 'part:hulls'`); the closest generic analog for a non-hull category is `scripts/build-place-release-assets.mjs`. Add a works category there; never hand-edit generated metadata.
2. **`node scripts/generate-render-package-pilots.mjs`** — derives `assets/ships/render-packages/pilots.json` from the release manifest (existing entries preserved; `--check` fails on any unpackaged asset). **Without this step the asset throws at load**: `assetLoader.js:1184` `assertSourceRouteAdmitted` raises `AssetContractError` for any `assets/ships/release/parts/` URL without a render package.
3. **`node scripts/build-render-package-pilots.mjs`** — builds `assets/ships/release/render-packages/<key>/` (`render-package.json` + `render.glb`).
4. **Gates** already wired: `check:render-package-pilots`, `check:render-package-plan`, `check:render-package-coverage`.

**LOD is selected by node NAME.** `assetLoader.js:1858` tags `lod<N>` from a `LOD0_*` / `LOD1_*` / `LOD2_*` node-name prefix (or a `spaceface.lod` userData tag) and hard-errors if no primitive exposes lod0. The Blender export must name its LOD roots that way; there is no manifest field for it.

**Works camera `works_edge` offsets the OBJECT, not the camera** (`works_pose('works_edge')` computes `object_offset`); a renderer that ignores the offset silently produces a duplicate of `works_top` labelled "edge". **Hidden-face culling must evaluate one LOD at a time** — evaluating coincident LOD0/1/2 together marks every lower-LOD face unseen and `--delete` would erase LOD1/LOD2 (the Hornet 77 %-deletion failure class).

**Scale.** A works cell is `S = 2.2` wu. Author every works asset at works scale from the start (the rover brief: 1.87 × 1.76 × 0.99 wu, tracks' underside at z = 0); a flight-scale part mounted centre-at-face reads as a black slab half-buried in the rock.

## Work breakdown (one unit per claim; campaign §4 order)

- [ ] `PQ-131.00` Loader + works camera — the runtime prerequisite and the review tool.
- [ ] `PQ-131.01` Rover (hero).
- [ ] `PQ-131.02` Massline Core. `PQ-131.03` Extractor. `PQ-131.04` Refinery.
- [ ] `PQ-131.05` Surface derrick / head-frame.
- [ ] `PQ-131.06` Conduit kit (cable + lane, modular).
- [ ] `PQ-131.07` Gas tap. `PQ-131.08` Fabricator. `PQ-131.09` Cargo port + crates + courier pod.
- [ ] `PQ-131.10` Inclusion kit.

## Write set

`src/ui/asteroid/asteroidRenderer3d.js` (swap-in only), `src/render/asteroidInteriorPreview.js`
(delete the replaced builder), a new loader module under `src/ui/asteroid/` or `src/render/`,
`tools/blender/spaceface_works_camera.py` (+ culling adaptation), Blender sources under
`assets/incubator/asteroid_works_kit/` → release under `assets/ships/release/parts/works/` (or the
category the builder establishes), `release_manifest.json`, `scripts/build-hull-release-assets.mjs`
(only if a works category is needed), `scripts/capture-asteroid-works.mjs` (additive stills), this
packet and the campaign docs.

Do not edit hitch renderer owners, Hitch/Kestrel, fleet remaster sources, sim systems, the screen shell,
or the stylesheet.

## Non-goals

Scaling or re-skinning the procedural meshes; any new procedural "hardware"; billboards; emissive
outlines; studio renders as proof; `PQ-050` ships; `PQ-129`; new machines or sim laws.

## Performance and quality budget

Authored assets replace procedural meshes of similar or higher triangle counts; budgets per asset are in
the campaign inventory (LOD0 / LOD1, KTX2 textures). LOD1 at the site register keeps the whole-body view
cheaper than today's instanced procedural clusters. Bloom, shadows and the shared grade stay on. Never buy
a pass by removing an asset, a lamp, or a shadow; never ship a studio-quality LOD0 without its LOD1.

## Verification budget

- Per asset: works-camera stills (`works_top` 120 px/cell, `works_site` 19 px/cell) beside a flight
  still; three independent reviews listing every defect at play size; revise until KEEP; `npm run
  check:asteroid-theater` and `npm run check:playable` green; no quality cuts; release build hashes in the
  manifest; the replaced procedural builder deleted.
- `.00`: a release part loads and renders in the works context with correct colour space and LOD by
  register; disposal on exit leaks nothing (`renderer.info` before/after).

## Review questions

- Beside a flight still of a ship near an asteroid, does the mine's object read as the same game?
- Is the still a works-camera frame at play size (120 px / 19 px per cell), not a studio crop?
- Did three independent reviews list defects at play size, and were they revised, not argued with?
- Is the procedural builder for this object deleted, and does the runtime state (hopper stages, bit
  heat, lamps, progress, flow, lid, scar) still drive the authored hooks?
- Does the asset load through the release pipeline (KTX2 + manifest hash), with LOD by register and
  clean disposal on exit?
- Can the diff be honestly described by campaign §6 (vanilla collapse)? If yes, fail.

## Stop conditions

- A unit's diff can be described by campaign §6 (vanilla collapse) — not done.
- A still that is a studio crop, or no flight still beside it — not proof.
- Blender unavailable — do `.00`'s loader half, record the blocker, stop.

## Checkoff and receipt

- [ ] Entry conditions recorded for the claimed unit.
- [ ] Reference sheet, blockout and final stills kept under the asset's evidence folder.
- [ ] Works-camera stills beside a flight still; three reviews; KEEP recorded.
- [ ] Release build hashes in `release_manifest.json`; procedural builder deleted.
- [ ] `npm run check:asteroid-theater` and `npm run check:playable` green.
- [ ] Queue unit state/receipt updated; parent stays `ready` / `unproven` until every unit is done;
      `PQ-130` acceptance is re-evaluated by the owner after units `.00`–`.06`.

## References

- [`ASTEROID_WORKS_ART_CAMPAIGN.md`](../../ASTEROID_WORKS_ART_CAMPAIGN.md) · [`ASTEROID_WORKS_ART_GOAL.txt`](../../ASTEROID_WORKS_ART_GOAL.txt)
- [`ASTEROID_WORKS_DESIGN_LAW.md`](../../../ASTEROID_WORKS_DESIGN_LAW.md) §2.7 · [`PQ-130.md`](./PQ-130.md)
- [`GRAPHICS_3D_CAMPAIGN.md`](../../GRAPHICS_3D_CAMPAIGN.md) (the same bar for world objects)
