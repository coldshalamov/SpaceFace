<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-167 — Telemetry funnels and the weekly playtest loop

```yaml
queueId: PQ-167
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-03
```

## Outcome

Telemetry already captures onboarding milestones, kills, deaths by cause, trade and credits locally; nobody can read it. This packet exports a session report (funnel: first flight, first swing, first shove, first dock, first heat; session length; death causes; verbs used per hour), runs a weekly owner playtest with captures and a one-page findings sheet, and makes the ALPHA/BETA gates in §15.1 computable from it.

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **E · First hour, UX, platform**. Gap it closes: Telemetry is local-only, reachable via a window global; no export, no funnel, no playtest ritual. Reference: Any shipped indie's playtest cadence; Supergiant's weekly plays. Routes through existing sources: SPEC3-41; PROD-OBS-CAP; §1A observability.

## What exists (audited 2026-09-03)

`src/systems/telemetry.js`, `src/observability/sessionObserver.js` (unwired), `motionTelemetry.js`, receipts machinery.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | Session report. Export a JSON + one-page Markdown report per session from the existing aggregates; opt-in upload later. | Report renders for any saved session; funnel fields present. |
| `.01` | The weekly playtest. A protocol: owner plays 45 minutes on a clean build, capture + report + three findings routed to packets. | Four consecutive weeks recorded in receipts. |
| `.02` | Gates from data. ALPHA/BETA numeric gates computed from reports (completion %, verbs/hour, session-2 return). | `check:playtest:gates` prints the §15.1 rows. |

## Entry conditions

- [ ] `npm run check:baseline` green at the candidate base; dependencies integrated as listed in the queue row.
- [ ] Owner modules re-read; unknown owner contracts are blockers, not guesses.

## Non-goals

No PII, no network without opt-in.

## Checks and evidence

`npm run check:baseline`; one normal-speed capture at the shipping camera per player-felt leaf; receipts under design/program/roadmap/receipts/PQ-167-*.
