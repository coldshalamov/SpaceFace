<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-XXX — outcome title

```yaml
queueId: PQ-XXX
lifecycle: planned
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: re-read at claim time
```

## Outcome

One paragraph describing what the player sees, hears, or can do, on the ordinary route. Name what
makes it enjoyable or strategically meaningful, not only what data exists.

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | … | A clear player-facing bar. Not a green check. |

## How agents get this wrong

The lazy first tries for *this* packet. Be specific: the cheap shortcut, the missing feel, the
thing that looks done in a test and dead in the game. These are not process steps.

## Write set

Exact paths when known. Otherwise bounded owner directories. Shared/global files are an
integrator-owned change or an explicit lease.

## Non-goals

Adjacent systems, later breadth, and tempting redesigns this packet does not own.

## Closing

Play it. The first version that merely meets the bar is a draft. Look at it, find the cheap bits
and the bugs, and make it something you would put in front of the owner.

When you are about to call it done, have another agent look at the *actual work* for three things:
is it finished, what is broken, and what would make it better. Fix what is real. Then stop.

Do not store stills, review JSON, unlabeled archives, or receipt novels. A short note for the next
engineer only if you learned something they could not see from the code.

Run the checks that would catch *this* change. Skip the rest. Never edit a golden merely to pass.
Never reduce default quality to hit a number.
