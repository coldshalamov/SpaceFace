<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-140 — The roster is a set of physical problems

```yaml
queueId: PQ-140
lifecycle: planned
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: PQ-137 .03–.05 integrated
```

## Outcome

Enemies differ by the physical problem they pose, not by their arithmetic. A light interceptor is a
positioning problem. A heavy is moving terrain. Specialists threaten the player's plan: one cuts
tethers, one disrupts fields, one anchors its allies, one protects cargo. Light fodder resolves
fast and reads as ammunition. Verified in the Crucible, where the roster is already the whole game.

## Source

`design/VISION.md` Part II, "Combat should be physical before it is numerical." PQ-135.03–.05
delivered hull-relative envelopes, one choreographed wing and one flowing cohort; this packet gives
each roster slot a *physical* identity on top of that motion.

## Live seams

`src/systems/tacticalAI.js`, `src/ai/maneuver.js` (envelopes, desired-state control),
`src/ai/fodderCohort.js`, `src/ai/squad.js`, `src/data/ships.js` (masses 16–600),
`src/systems/survivalSwarm.js` / `swarmArena.js` (the Crucible), `src/systems/masslineThreats.js`
and `masslineSnares.js` (tether counterplay seams), `src/systems/fields.js`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Interceptor = positioning problem.** Crossing lanes, extend-and-return, never a stationary target; its threat is where it will be, not its DPS. | Scenario metric: time the player spends off its attack line; a capture where the lane geometry is readable. |
| `.01` | **Heavy = moving terrain.** Mass ≥ 150; gun-scale impulses do nothing (B11); the player can swing around it, hide behind it, and throw lights into it; its wake and turn radius matter. | A rope-swing around a heavy works in the Crucible; a light thrown into it dies (B6). |
| `.02` | **Specialists, one each.** Tether-cutter, field-disruptor, anchor, cargo-protector — each breaks one specific player plan and is readable by silhouette and behaviour before any label. | Four scenarios, each with the plan it breaks; silhouettes reviewed at the shipping camera. |
| `.03` | **Fodder is ammunition.** With the hitstun law and force table in, the twelve-body cohort resolves quickly under physics play (shove into rock, chain, well) and the free gun no longer out-performs the physics kit. | Crucible balance receipt re-run; median time-to-resolve a light under physics ≤ under guns. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Making an enemy 'hard' with hull points: heavies are terrain because of mass, not health.
- Giving a specialist an ability the player cannot see coming: it must read by silhouette and behaviour before any label.
- Forking the AI for the Crucible: one tactical AI, hull-relative envelopes, no campaign fork.

## Entry conditions

- [ ] Dependencies integrated at the candidate base as the queue row states (PQ-137 .03–.05).
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario before tuning.

## Non-goals

No new AI fork, no new hull models (silhouette work routes through `PQ-050` / asset programs); no
enemy that cheats physics.

## Checks and evidence

`npm run check:combat`, `check:crucible-run`, `check:playable`, scenario receipts, captures.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the leaf's scenario; record the before numbers for the `design/FEEL_CONTRACT.md` bar this packet moves.

### Phase 1 — pure contracts and data

- [ ] The data/rule leaves in the Leaves table, in order.

### Phase 2 — owner wiring

- [ ] The listener/seam leaves in the Leaves table, through the existing single writers.

### Phase 3 — presentation and route

- [ ] Captures at the shipping camera; replay the same scenario/seed after mutation and compare against Phase 0.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit; the packet checks named above; per leaf the deterministic scenario that prints its bar number and one normal-speed capture at the shipping camera when the claim is player-felt; `npm run check:all:smoke` for sim/save/render seams. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record.

## Review questions

- Which `FEEL_CONTRACT.md` bar did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, or give an NPC a hidden gyro?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism in the sim path, a golden moving without a causal record, or a passing route that reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after bar numbers recorded in the receipt with the scenario ID and seed; captures attached for player-felt leaves.
- [ ] Receipt written under `design/program/roadmap/receipts/PQ-140-<leaf>-REPORT.md`; queue unit state updated by the integrator.
