<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-032 — Campaign spine: one linear story that builds to an ending you earned with the toys

```yaml
queueId: PQ-032
lifecycle: ready
acceptance: unproven
packetRevision: 5
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
identityAction: this packet supersedes the deferred one-line brief; legacy shared identities (T/SF/A/R/W aliases on the queue row) resolve to this packet when its first leaf is claimed, and the integrator replaces any colliding alias before promotion
```

## Outcome

The 47-A opener is the best twelve minutes in the game; the next three beats are inventory errands and the ending fires at ~4 % of the money curve behind an empire-stake paywall. The owner ruled: no dialogue trees, one linear story that builds, and replay value is not a goal. This packet re-cuts the spine as ONE through-line: each beat is a set piece built from verbs (PQ-152), the ending gate sits where the player has heavy-class verbs, the empire stake accepts a combat stake, and NG+ carries the ship's history. The five endings and five post-ending chains that already exist stay as they are; no new branching work is admitted.

## Source

`build_map.md` §15 (admitted 2026-09-03) → **B · The world and its people**, wave **BETA**. Gap it closes: Authored content ~10–14 h; sharpest cliff at minute 12; ending at 100k cr vs 4.5M hulls. Reference: Freelancer campaign gating, Hades run narrative, Outer Wilds discovery. Routes through existing sources: WB-B0-B7, WB-WORLD-AFTER, RV-STORY, SPEC3-32; depth P2 story-beat embodiment; retired plan superseded by this packet: design/program/roadmap/retired/PQ-032.md. Candidate beat treatments are retained in [`EXPANSION_STORY_AND_PLACES.md`](../../../spec3/EXPANSION_STORY_AND_PLACES.md#1-campaign-spine-candidate-beats); they add no branches or acceptance criteria.

## What exists (audited 2026-09-03)

8 beats `src/data/missions.js:1001-1016`; `src/story/campaign47a/`; 5 endings `src/story/endings/endingDefs.js`; `postEndingReplayChains.js`; `newGamePlus.js`; eligibility gates `eligibility.js:152-168`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Beats 1–3 become set pieces.** honest_work → the wrecking-ball contract; first_blood → pod rescue under fire; bigger_boat → the long tow. | Each beat is a PQ-152 set piece with a physical headline verb. |
| `.01` | **The gate moves and widens.** Ending eligibility at the heavy-verb tier (tow class ≥ medium, one field, one ace beaten) with a combat stake alternative to the empire stake; the linear spine reaches it without a branch choice. | A combat-only run reaches an ending; a builder run reaches one; both in ≤ 25 h by telemetry. |
| `.02` | **The climax is a toy.** Each ending's final set piece requires the verbs of the branch (siege by wrecking ball, evidence tow, blockade run). | Owner plays each ending's climax; verdict recorded. |
| `.03` | **NG+ carries a legacy.** Ship scars, titles, ace grudges, one head, and the ending's world facts carry; everything else resets. | Save migration test; the mechanic names the carried history. |

## How agents get this wrong

These are the ways a lazy first try fails this packet. Read them, then do the work as if the owner will play it tonight.

- Adding a branch or a choice menu: one linear story, by ruling.
- Moving the ending gate without a combat stake: a combat-only run must reach an ending.

## Entry conditions

Glance at NOW.md so you don't collide. The Leaves table is the job. Dependencies are on the queue row.

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Look at the change on the default route. If a number is the done-when, print it from a named seed. If the player would see or hear it, look at it. A still is a way to look, not something to store.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Beats 1–3 become set pieces.
- [ ] `.01` The gate moves and widens.

### Phase 2 — owner wiring

- [ ] `.02` The climax is a toy.

### Phase 3 — presentation and route

- [ ] `.03` NG+ carries a legacy.
- Play it again after you change it. Fix what you would not ship.

## Write set

- `src/data/missions.js`
- `src/story/`
- `src/core/newGamePlus.js`
- `src/data/postEndingReplayChains.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No new endings, no new branches, no dialogue trees; existing endings stay untouched.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; save bytes bounded; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:sim`, `npm run check:save-schema`. Per leaf: look at the player-facing change; print the done-when number from a named seed when a number is the bar. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

Would you put this in front of the owner? What's unfinished, what's buggy, and what would make it better? If you're about to close, have another agent look at the actual work for those three things and fix what's real. Don't write a review file.

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

Would you ship this? Stay on the write set. Run the checks that would catch this change. A short note for the next engineer only if you learned something they couldn't see from the code.
