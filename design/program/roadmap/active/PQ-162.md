<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-162 — The station as a place: the redesign the flatten was waiting for

```yaml
queueId: PQ-162
lifecycle: ready
acceptance: unproven
packetRevision: 4
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

> **DIRECTION OVERRIDE, 2026-09-05.** Every "to the grammar" clause and "matrix green" done-when in
> this packet is void on aesthetics. This packet builds on the kit from `PQ-187.03` (the decided
> direction — [`DIRECTION_SHEET.md`](../../../frontend/direction/DIRECTION_SHEET.md), 2026-09-06) and is
> done when its captures match the sheet — checked against the sheet; the matrix is a
> prerequisite; the owner's veto is exercised in the game, never on a stylesheet.
> Implementation: [`design/frontend/direction/tasks/TASK_C_STATION_INSTRUMENTS_CHART.md`](../../../../design/frontend/direction/tasks/TASK_C_STATION_INSTRUMENTS_CHART.md); spec
> [`KIT_SPEC.md`](../../../../design/frontend/direction/KIT_SPEC.md).
> See [`design/FRONTEND_DIRECTION.md`](../../../FRONTEND_DIRECTION.md) §8 for the phase, §7 for the
> signature moments this packet owns, §9 for what is rejected.


## Outcome

The docked station is the second most used surface and the only one an owner-commissioned review called cheap screen by screen. Phase 6 flattened the stylesheet and borrowed tokens; the CREST/STAGE/APRON/DRAWER skeleton and the data-state and entity-link contracts have zero adoption across all seven screens. This packet is the redesign: one grammar, one hierarchy, the bar with a transcript, the industry tiers legible, orange doing one job, and docking that feels like arriving at a place with people (the mechanic of PQ-150 lives here).

## Source

`build_map.md` §15 (admitted 2026-09-03) → **D · Presentation**, wave **BETA**. Gap it closes: Flatten happened, redesign did not; zero skeleton adoption; review findings unverified (orange doing four jobs, bar transcript void, italic flavour outranking data). Reference: Hades hub, Hardspace: Shipbreaker hab, Starsector colony screens. Routes through existing sources: §11 Phase 6/7; RV-COMMAND-DECK (extract one interaction only); S2-06. Candidate place briefs for the existing arrival route are retained in [`EXPANSION_STORY_AND_PLACES.md`](../../../spec3/EXPANSION_STORY_AND_PLACES.md#3-four-candidate-place-briefs); they add no station types or asset approvals.

## What exists (audited 2026-09-03)

src/ui/station/ (7 screens via stationApp.js), styles/station-workbench.css (34 --sf-* refs, 12 px floor), design/frontend/INSTRUMENT_GRAMMAR.md, design/frontend/reviews/STATION-TASTE-KIMI-2026-08-23.md, src/ui/entityResolver.js, screenMemory.js, whyReveal.js (17 adopters).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Skeleton and data states adopted.** All seven station screens render through CREST/STAGE/APRON/DRAWER with the four data states and entity links; the legacy stationHub helpers are lifted out (Phase 7 as a refactor). | check:data-states and check:entity-links cover the station; zero legacy imports from stationHub.js. |
| `.01` | **Hierarchy and colour discipline.** One primary per screen, orange means one thing, italics never outrank data, the bar has a transcript, industry tiers read. | A cold reviewer verdict per screen; the Kimi findings closed one by one in the receipt. |
| `.02` | **Arriving somewhere.** Docking is a place: the berth with your hull, the mechanic, ambient work, one line of local news; screens open from the place, not from a tab strip. | Blind reviewer names the station from the docked view; capture. |

## How agents get this wrong

These are the ways a lazy first try fails this packet. Read them, then do the work as if the owner will play it tonight.

- Repainting the station again: the skeleton and the data-state and entity-link contracts must be adopted, measured by the checks.
- Redesigning the flight HUD on the way: out of scope; the attention pass stands.
- Deleting legacy modules before the live station stops importing them.

## Entry conditions

Glance at NOW.md so you don't collide. The Leaves table is the job. Dependencies are on the queue row.

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Look at the change on the default route. If a number is the done-when, print it from a named seed. If the player would see or hear it, look at it. A still is a way to look, not something to store.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Skeleton and data states adopted.

### Phase 2 — owner wiring

- [ ] `.01` Hierarchy and colour discipline.

### Phase 3 — presentation and route

- [ ] `.02` Arriving somewhere.
- Play it again after you change it. Fix what you would not ship.

## Write set

- `src/ui/station/`
- `styles/station-workbench.css`
- `src/ui/screens/stationHub.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No new station types; no redesign of the flight HUD.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; save bytes bounded; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:station-tabs`, `npm run check:command-deck-ui`, `npm run check:ui-effects`. Per leaf: look at the player-facing change; print the done-when number from a named seed when a number is the bar. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

Would you put this in front of the owner? What's unfinished, what's buggy, and what would make it better? If you're about to close, have another agent look at the actual work for those three things and fix what's real. Don't write a review file.

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

Would you ship this? Stay on the write set. Run the checks that would catch this change. A short note for the next engineer only if you learned something they couldn't see from the code.
