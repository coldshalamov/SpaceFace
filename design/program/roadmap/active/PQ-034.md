<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-034 — PERF-00 equivalence and attribution harness

```yaml
queueId: PQ-034
lifecycle: implemented
acceptance: focused_green
packetRevision: 31
owner: Codex primary integrator
baseRequirement: exact clean revision and uncontended evidence environment at claim time
receipt: ../receipts/PQ-034-performance-harness-REPORT.md
baseCommit: 9d8f3984d9bb846e9aa42dba8929ac78c99ac5eb
remainingPhases: fresh source-paired Browser/Electron evidence after current-target pipeline and owned-Chrome teardown repair, enabled overhead, and integrator causal review
```

## Recovery audit checkpoint — 2026-07-31

`PQ-034.candidate-audit` is integrated with a terminal report at
[`../receipts/PQ-034-candidate-audit-REPORT.md`](../receipts/PQ-034-candidate-audit-REPORT.md).
Of the protected ten-commit branch, only `e28082e6` was accepted: it derives p99.9 and strict
greater-than-two-times-median hitch counts from the complete raw closure window. Four commits require
narrow reimplementation and five were rejected; the protected branch remains unchanged. The packet
is still `focused_green`, not route accepted. `PQ-034.native-closure` owns the remaining immutable GPU
origin, current-driver, paired-manifest, Browser/Electron, and matched-performance authority.

## Native-closure claim — 2026-07-31

Exact unit `PQ-034.native-closure` is claimed by the Codex primary integrator at clean master
`9d8f3984d9bb846e9aa42dba8929ac78c99ac5eb`. It holds `browser-gpu`,
`performance-evidence`, and `validation-broker` plus only the packet write set. The protected
`sf-perf-admission-20260726` worktree and branch `claude/perf00-20260727` remain read-only at
`dce03987`; the protected lifecycle candidate remains untouched.

Entry evidence at this exact base is green: `npm run check:baseline` passed 10/10 in 45.644 s;
the existing PERF-00 focused suite passed 110/110; and `npm run check:sim:compare` reported
`hashEqual=true`. The worktree was clean, branch `master`, with no SpaceFace/Electron process and no
listener in the 4170-4185 broker range. The host reports Intel Graphics driver `32.0.101.8132` and
Balanced power policy. Browser-runtime renderer and measured display interval remain deliberately
unclaimed until a broker route records them.

The process census also found foreign Blender PID 32140 plus normal Chrome/WebView activity. Those
processes are protected and will not be stopped or altered. They make the current window ineligible
for matched L4 performance capture; implementation and focused proof may proceed, but promotion must
wait for the broker's clean-environment gate or retain an exact quiet-window blocker.

The first native-closure implementation slice is focused-green. Runtime display/render/simulation
origins, monotonic GPU query IDs, bounded terminal accounting, `didBegin`-scoped end calls, and
frame-bound post-target allocation events are integrated on the claimed branch. The new contracts
failed before implementation and now cover delayed completion, reset-drop, disjoint, context loss,
nested refusal, backpressure, and stale-origin exclusion. No headed route or performance evidence was
spent. Exact evidence and residuals are in
[`../receipts/PQ-034-performance-harness-REPORT.md`](../receipts/PQ-034-performance-harness-REPORT.md).

The second slice replaces the broker CLI's hard-coded loader table with one path-safe registry.
Only safe IDs resolving to Git-tracked regular files under `scripts/validation-manifests/` may execute;
the loader rejects unsafe/unknown IDs, non-stage-zero index entries, symlink/non-file modes, realpath
escape, missing/non-object default exports, ID disagreement, and manifests explicitly held behind
`registryEnabled: false`. The two deferred PQ-025 manifests use that explicit stop so discovery cannot
bypass their unmet entry conditions. Existing manifest tests now exercise real tracked lookup instead
of source-matching a loader table. No broker claim, headed runtime, or performance evidence was spent.

The third slice makes the existing attribution driver runtime-parameterized and adds the paired
`performance-closure-browser` / `performance-closure-electron` manifests. Both claims pin seed 47,
the same complete scenario/source surfaces, the same public route and restoration policy, one-use
acceptance authority, and exact owned cleanup; only runtime-bound launcher/tracker identity, broker
candidate identity, command argument, and artifact root differ. The library runner—not only its CLI—
consumes the broker claim before allocating an artifact directory or loading Playwright. A diagnostic
run is structurally non-promoting, and accepted evidence is published only after green validation and
owned cleanup. Focused proof spent no claim and launched neither runtime.

The fourth slice defines a runtime-neutral `sourceCandidateDigest` for those paired manifests. It
binds the exact Git worktree, build environment, complete production/regression/harness sources,
scenario-manifest sources, and explicit save/procedural-input/camera declarations while excluding
manifest ID and runtime kind. The runtime-bound broker candidate digest now folds that shared source
identity back in, so a dirty or changed source candidate invalidates a claim without collapsing the
Browser and Electron candidates into one identity. Accepted evidence v2 must expose the shared source
digest, runtime candidate digest, and content-verified raw-trace digest. Final acceptance v2 requires
three explicit Browser/Electron pairs and rejects source mismatch, candidate aliasing, raw-trace
aliasing, missing claim authority, wrong runtime, wrong commit, or an unverified raw artifact. No
broker claim, headed runtime, or performance evidence was spent.

The fifth slice completes the common runtime identity chain at the existing authored-upgrade queue.
`perfRuntime` now owns a default-off monotonic background-job ID and a fixed 128-record terminal
ring. Enabled records retain immutable start/end display-frame, render-frame, and simulation-tick
origins; reset never reuses an ID; disabling closes active records explicitly; overflow, active-record
overwrite, and refused starts remain visible invalidation counters. `partsLibrary` attaches that token
only at its existing serial upgrade start/finish boundary and makes no priority, decode, composition,
or publication change. The attribution driver and profiler enable the gate only inside measurement,
publish it with CPU evidence, and restore it off. Final acceptance rejects missing, disabled, active,
overflowed, refused, non-monotonic, or unterminated background-job evidence. No headed cell ran.

The sixth slice adds a recomputed `spaceface.performanceMeasurementValidity.v1` verdict. Closure
publication now fails closed on a dirty or drifting worktree, active/unknown authoring or foreign
browser/Electron census,
fallback or software renderer, disjoint/incomplete GPU query accounting, active or changing pipeline
residency, route/restoration drift, incomplete cleanup, or runtime errors. The raw GPU report now
retains `captureValid`, invalidation, query-count, and terminal evidence rather than reducing it to
capability status. Accepted Browser/Electron evidence carries the recomputed verdict and final
acceptance rejects an invalid runtime or matrix. No semantic-equivalence field is mutated by this
validity result, and no headed cell or performance claim was spent.

The seventh slice adds `spaceface.performanceImprovement.v1`. It recomputes three-run raw-frame
p95 values, requires direct route/environment comparability, and treats the larger of either arm's
observed range or a 1%/0.1 ms resolution floor as noise. Baseline compared with itself is `neutral`,
not improved; improvement must exceed that floor, and regression outside it is distinct. The combined
verdict preserves green equivalence, validity, and absolute-budget dimensions when the improvement
dimension is neutral. No live performance claim was made from the deterministic fixture.

The eighth slice upgrades final acceptance to v3 and makes all four answers explicit. It requires
three clean historical baseline matrices, three candidate matrices, the selected improvement
scenario, and one content-hashed semantic-equivalence document whose four raw simulation/presentation
artifacts are reread and recomputed by the CLI. The final report exposes equivalence, measurement
validity, noise-aware improvement, and absolute-budget dimensions independently. An injected semantic
difference leaves validity/improvement honest; contamination leaves semantic equivalence true; an
otherwise green within-noise candidate returns `neutral`, not pass. No live evidence was synthesized.

The ninth slice moves the bounded host-activity census ahead of acceptance artifact allocation,
runtime launch, and one-use claim consumption. Acceptance authority is first validated without
consuming it; an active or unavailable Blender/browser/Electron census then fails closed; only a
quiet census permits the atomic consume. The characterization also exposed and repaired a missing
`sourceCandidateDigest` projection in claim validation that had made valid source-bound paired claims
unusable. The regression proves a contaminated host spends neither the valid claim nor a run
directory. No production broker claim, headed runtime, or performance evidence was spent.

The tenth slice closes the remaining machine-only disabled-path and process-control proof. Direct
contracts show detailed system/render timing returns before record-key inspection, GPU timing
returns before origin/query construction, renderer origins reuse one caller-owned object, and the
authored-upgrade owner guards before creating a background-job record. The broker child owner also
retains its hard timeout, Windows descendant-tree termination, prompt exit handling, and recycled-PID
safety contracts. These are structural/seconds-scale guarantees, not a substitute for the still-open
enabled-under-1% matched-route measurement or live Browser/Electron matrix teardown evidence.

## Historical blocked release — 2026-07-31 (superseded)

At clean pushed candidate `cc3340c268769d69c985902f5a019f57a11b09c2`, the bounded census at
`2026-07-31T21:50:02-04:00` found protected Blender PID 32140, 22 Chrome processes, and 30
WebView2 processes. No SpaceFace or Electron process was present. Because acceptance now fails closed
before claim consumption on any active authoring/foreign-browser census, neither paired claim was
spent. Those foreign processes were not stopped or altered.

At that checkpoint, exact unit `PQ-034.native-closure` was recorded `BLOCKED`, not complete. The
named resume owners were the
**SpaceFace quiet-machine performance operator** for enabled-under-1% overhead plus three clean
baseline/candidate Browser/Electron pairs and bounded live cleanup, and an **independent performance
harness reviewer** for causal approval of the exact candidate/evidence. All three packet mutex leases
were released. The machine-only harness remained integrated and focused-green; the unchecked live
performance/termination gates remained open. The recovery resume below supersedes those role and
process-existence conclusions; it does not rewrite the historical observation.

## Recovery resume — 2026-08-01

`PQ-034.native-closure` is reclaimed at clean master `070064b4ff9aff9c4addb54d876c7ad877ba8e53`
with `browser-gpu`, `performance-evidence`, and `validation-broker`. The protected candidate worktree
and foreign processes remain read-only. The primary integrator owns both execution and the final
causal evidence review; no separate human role is required by the execution protocol.

Live characterization shows why the previous terminal blocker cannot remain authoritative: the
census labels process *existence* as activity. Days-old Blender, Chrome, and WebView processes with
near-zero bounded CPU deltas therefore block before the harness measures whether they are active.
The exact recovery repair will replace existence-only admission with a recorded, bounded,
fail-closed activity sample. It must still reject unavailable census, process churn, active
authoring/browser work, dirty/drifting source, or invalid GPU/runtime evidence before either paired
claim can promote.

The repair is now focused-green and pushed as `52cd5eb3`. The existence-only census was first
reproduced as a failing unit import/contract, then replaced by two bounded Windows CPU snapshots.
The classifier fails closed on unavailable or malformed snapshots, process start/exit/name churn,
regressing counters, aggregate sustained activity above `0.125` of one CPU core, or any one process
above `0.075` of a core. A five-second window avoids treating scheduler-quantized idle ticks as
foreground work; the route's slow-no-op control, immutable GPU-query accounting, renderer checks,
source fingerprint, and end census remain independently binding.

The live characterization at `2026-08-01T09:32:04Z` retained all 53 protected Blender, Chrome, and
WebView2 process identities and measured `0.234375` aggregate CPU-seconds over five seconds
(`0.046875` of one core), with no churn or active-process reason. It therefore returned
`active=false`; no process was stopped or changed. Focused PERF-00 contracts pass 183/183,
performance counters 35/35, performance packet hot-path checks 39/39, the paired manifest gates
9/9 and 27/27, `check:sim:compare` is deterministic/hash-equal, launch policy is green, and the
post-repair baseline is 10/10 in 44.419 s. No production broker claim or headed cell was spent by
this repair; the clean pushed candidate must still pass a fresh census inside each one-use claim.

## First Browser claim characterization and repair — 2026-08-01

The one-use Browser claim on candidate `2ab10f91483b38a4cfbd2197d99d7bfa84ae198f` was consumed
once and retained under
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T09-42-04-324Z-38724-955b49cc/`.
It proved the public menu → New Game → flight → map/travel → dock/station route, real Intel ANGLE
D3D11 hardware timing authority, context loss/restoration, quiet start/end activity censuses, and
owned cleanup. It did not prove a valid performance result: 46 false-positive dynamic-buffer upload
errors, unread timer queries at window close, an intentional docked 3D-idle window, and an intentional
jump asset-admission program-cache transition were all rejected by overly broad contracts.

Candidate `0fb2fc10bcceefbdb45fff768edd77ac71f76c16` repairs those characterized defects without
relaxing ordinary failure authority. Context restoration grants each tracked Three.js attribute one
identity/version/range-bound driver reupload and rejects the next unsolicited callback. Measurement
close now drains pending GPU queries for at most two seconds and records the drain receipt. Scenario
definitions explicitly distinguish the canonical docked UI-live/3D-idle route and the bounded jump
asset-admission transition from ordinary measured windows, where zero queries or pipeline drift still
fail closed.

The new regressions failed before the repair and pass after it. The targeted set is 40/40, the
expanded PERF-00/broker set is 201/201, performance counters are 35/35, packet hot paths are 39/39,
simulation comparison is deterministic/hash-equal, launch policy is green, and `check:baseline` is
10/10 in 50.109 s. No performance conclusion is accepted from the failed claim. Because the source
candidate materially changed, the repaired commit must receive one fresh Browser claim before any
source-paired Electron claim is spent.

## Second Browser claim characterization and terminal-order repair — 2026-08-01

The fresh claim on clean pushed candidate `dc8a4ae8148bf771188a2e982f0ba7c55da8bd4d` retained its
immutable artifacts under
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T10-08-31-743Z-26696-1ac246b9/`.
Every previously repaired gate passed: runtime errors were empty, all 25 GPU timer windows completed
with zero pending/dropped/rejected queries, docked 3D-idle and jump admission were valid, route state
was stable, context restoration passed, both activity censuses were quiet, and owned cleanup passed.
The sole remaining failure was `windows[24]-pipeline-cache-mismatch`.

That failure exposed an execution-order defect, not a reason to waive cache stability. The
cleanup-scoped jump cell intentionally preserves destination-sector progression and ended with five
authored admissions pending. Although the runner said jump progression was last, its variant-major
loop ran nine flight diagnostics afterward. One destination asset completed during the final depth
diagnostic: program count moved `88 → 92`, pending authored presentations `5 → 4`, and resident assets
`33 → 34`. The stable diagnostic correctly rejected that contamination.

Candidate `fc18ed4680711da7d88e97080ab8129ebfd523d7` makes the execution plan explicit and moves any
cleanup-scoped jump cell after every reversible baseline/diagnostic cell. A seconds-scale regression
pins the exact cell order and the runner refuses a non-baseline diagnostic on the cleanup-scoped
route. It does not weaken the stable-window rule or manufacture a cache exception. The selected
PERF-00/broker set passes 143/143, counters 35/35, packet hot paths 39/39, simulation comparison is
deterministic/hash-equal, launch policy is green, and `check:baseline` is 10/10 in 44.283 s. No
performance conclusion is accepted from the second failed claim; a fresh Browser claim is required
for this materially changed candidate.

## Third Browser claim characterization and context-order repair — 2026-08-01

The claim on clean pushed candidate `da98b9645e5096390990a0962c91fdd1c685d092` retained its
artifacts under
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T10-23-39-163Z-36568-6fba6fd4/`.
The execution artifact proves the prior repair: reversible baseline and diagnostic cells ran first
and `jump_asset_admission` was window 24, last. All route, timer, error, activity, context, jump,
cleanup, and other pipeline checks passed. The only invalidity was
`windows[16]-pipeline-cache-mismatch` in `bloom_off`.

No asset or background-admission fact changed in that window. Instead, context recovery at window 14
had reset the renderer cache from 89 programs to 79; window 15 paused simulation; and moving flight
resumed in window 16. One still-lazy ordinary shader compiled during the measured arm (`80 → 81`).
That is measurement contamination after a cache-destructive probe, and the stable-window gate was
right to reject it.

Candidate `243d3bce9f5865a81faafb7fb7c2e02ee1f42c42` makes both destructive operations terminal:
all reversible baseline/diagnostic cells run first, context recovery follows, and cleanup-scoped jump
remains absolutely last. The red-to-green execution-plan regression pins that exact order and refuses
non-baseline variants for either terminal cell. The selected PERF-00/broker set passes 143/143,
counters 35/35, packet hot paths 39/39, simulation comparison is deterministic/hash-equal, launch
policy is green, and `check:baseline` is 10/10 in 45.886 s. No performance result is accepted from
the third failed claim; a fresh Browser claim is required for the changed candidate.

## Fourth Browser claim characterization and pipeline-readiness repair — 2026-08-01

The claim on clean pushed candidate `cc88a5d8b1d0f68198b0e3f08badaebb46a8e84c` retained its
artifacts under
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T10-36-06-675Z-35024-c5b89875/`.
It proves the final execution order: all reversible cells ran first, context recovery was window 23,
and cleanup-scoped jump was window 24. Runtime/page/request/HTTP/GL errors were empty, all GPU queries
drained validly, both activity boundaries were quiet, route and restoration checks passed, and owned
cleanup passed. The only invalidities were `windows[15]-pipeline-cache-mismatch` in `bloom_off` and
`windows[16]-pipeline-cache-mismatch` in `background_hidden`.

Neither window had an authored-admission, queue, or residency transition. `bloom_off` changed only the
program count (`95 → 97`) while authored pending stayed `4`, resident assets `27`, and resident
resources `799`; `background_hidden` then changed only the program count (`97 → 98`) with the same
unchanged facts. This proves the fixed two-second delay was not a readiness contract: normal moving
flight could still compile lazy pipelines after measurement began even before the destructive cells.

Candidate `89470005843bca49a3b140f7e3f8931b4da671b3` replaces that delay-only guess with a bounded,
recorded quiescence gate. Before each measured arm, the harness observes program count, admission jobs,
mesh queue/dirty state, compile-pending count, authored pending count, and asset/resource residency at
100 ms intervals. It requires five unchanged seconds with the queue and admission owners settled,
caps the wait at 20 seconds, and publishes a versioned warmup receipt. Timeout or malformed receipt
invalidates the window as `pipeline-warmup-unsettled`; it does not call `gl.finish`, waive later
program drift, or change scenario quality. The reproduced validity test failed before implementation
and is now green. The selected PERF-00/broker set passes 144/144, counters 35/35, packet hot paths
39/39, simulation comparison is deterministic/hash-equal, launch policy is green, and
`check:baseline` is 10/10 in 47.246 s. No performance result is accepted from the fourth failed claim;
one fresh Browser claim is required for this materially changed candidate.

## Fifth Browser attempt characterization and churn-settling repair — 2026-08-01

The one-use broker attempt on clean pushed candidate `1ad68828dff71d599b2e14f8639677837af2dab1`
stopped before Browser or artifact allocation at `2026-08-01T10:58:36Z`. Release lock/build signals
were inactive and the five-second census measured only `0.171875` aggregate CPU-seconds
(`0.034375` of one core), below both declared thresholds. One Codex-host WebView process, PID 11512,
ended between snapshots, so its post-snapshot CPU was unknowable and the census correctly rejected
the sample as `process-churn`. The broker spent that candidate's one launch authorization; it did not
launch a headed runtime or produce performance evidence.

Candidate `c812ca50651c48265915de3df3a21b1a7bdaac55` makes transient preflight churn self-settling without
accepting it. Only before claim consumption, and only when `process-churn` is the sole reason, the
census may discard the indeterminate sample and require a wholly fresh five-second sample, with at
most three attempts. Every attempt is retained. Persistent churn, CPU activity, regression, malformed
or unavailable snapshots, locks/builds, and every end-of-run census still fail immediately; the end
census never retries. The reproduced churn-only regression failed before implementation and now
passes, while a persistent-churn counterexample remains fail-closed. The selected PERF-00/broker set
passes 146/146, counters 35/35, packet hot paths 39/39, simulation comparison is
deterministic/hash-equal, launch policy is green, and `check:baseline` is 10/10 in 45.404 s. A fresh
Browser claim is required for the changed candidate; Electron remains unspent.

## Sixth Browser claim accepted — 2026-08-01

The one-use Browser claim on clean pushed candidate `b847320e4aa0f864e2f6c4862de306fddd773a6b`
passed acceptance in 454.166 seconds. Immutable artifacts are under
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T11-08-23-266Z-9852-98f87623/`,
with the broker projection at `.devshots/perf/closure/browser/browser/evidence.json`. Claim
`4256-1baf8886d6425c5283c0dd78` binds source digest `b8193f95…`, Browser candidate digest
`2e47f480…`, and raw-window digest `d02eaa44…`.

The closure is clean and measurement-valid. All 25 pipeline warmups passed without timeout; the exact
formerly drifting arms now prove the new contract by observing a program transition and then waiting
for five stable seconds (`bloom_off` 5.854 s, `background_hidden` 9.730 s, and material-basic 5.271 s).
Every stable arm held identical start/end program counts, jump admission passed its explicit policy,
all GPU query queues drained with zero pending/dropped/rejected terminals, all runtime error arrays are
empty, the public route and context recovery passed, and owned cleanup passed. Start/end activity were
quiet on their first samples (`0.021875` and `0.025` aggregate core fractions) with no churn.

This is valid measurement authority, not an absolute performance pass. The raw default-quality
matrix contains literal 16.7/33.3 ms budget misses—including 50 ms p95 dense-fleet windows—and no
baseline/candidate improvement conclusion is promoted. The exact unit remains `IN_PROGRESS` until a
source-paired Electron matrix, disabled/enabled overhead proof, and integrator causal review close.
The source-paired Electron claim is next; Browser will not be rerun unchanged.

## First Electron claim characterization and runtime repair — 2026-08-01

The Electron claim on clean master `b1b15ee9a5f3a9cc3e6a77c41dabe36370d3fe0c` ran all 25
windows and cleaned up under claim `25476-83c733557dbe390afc61eedb`. Its immutable artifact is
`.devshots/perf/closure/electron/performance-closure-electron-2026-08-01T11-20-50-729Z-29580-c325bd84/`.
It is retained as a failed characterization, not accepted evidence. The renderer user agent proved
that Playwright silently launched Electron `31.7.7` even though package and lock authority declare
`43.2.0`. The route also emitted exactly 13 binding warnings: one insecure/missing-CSP warning and
twelve `GL_INVALID_VALUE: Program object expected` warnings after controlled context recovery.

Candidate `137e4d8f12a3f20df08d645a47b321c1afb90b1b` repairs the authority and runtime defects rather
than suppressing the verdict. The attribution launcher provisions the declared Electron package,
fails closed unless package/declaration/binary versions match, and publishes the exact executable
plus an Electron/43.2.0 user-agent check into closure validation. The Electron-owned HTTP response
now carries a shell-only CSP with no `unsafe-eval`; direct Playwright evaluation replaces its
string-evaluating poller. Capability-limited bridges replace the exact Rapier global getter and the
two pinned Emscripten Basis worker shims that strict CSP exposed. The Basis regression loads a real
release GLB's embedded KTX2 through both the stock and closure implementations and requires identical
texture facts.

The context warning trace identified a separate renderer race. `WEBGL_lose_context` makes the GL
object unusable before the asynchronous `webglcontextlost` listener sets application state, so one
frame could submit old Three.js programs inside that delivery gap. The draw boundary now checks the
live `gl.isContextLost()` result, and application-owned restore work remains paused until the restore
listener stack has returned. A bounded diagnostic repeat recorded full context recovery, retained
authored mesh identity, pixels and advancing rAF, replaced Three.js property caches, and
`invalidProgramCalls: []`. That diagnostic deliberately used a one-second sample and therefore
exited on the independent five-second measurement-window minimum; it is context characterization,
not performance evidence. Temporary program tracing was removed before the candidate commit.

Focused CSP/runtime/context contracts pass 48/48; physics authority and the 18-test
floating-origin/collision set pass; Electron platform and launch policy pass; and the exit baseline
passes 10/10 in 43.300 seconds. A final non-performance `check:electron:new-game` on the finished
source proves Electron 43.2.0, hardware Intel D3D11, flight mode, 16 authored ship presentations,
zero page/GPU-process errors, and owned cleanup. Because the repair changes production and harness
source, the accepted Browser source digest `b8193f95…` and failed Electron source digest `96cfc123…`
cannot be paired. The next action is one fresh Browser claim followed by one Electron claim on the
same clean pushed source; neither the failed Electron matrix nor the old Browser matrix is laundered
into a paired result.

## Seventh Browser claim characterization and scenario-isolation repair — 2026-08-01

The fresh Browser claim on clean pushed candidate `4240fb429a46af03c3f3ee5bea55ddc5a8920477`
completed all 25 windows and owned cleanup under claim `16368-9b4a75a2c2720889162335f7`. Its retained
artifact is
`.devshots/perf/closure/browser/performance-closure-browser-2026-08-01T13-13-29-855Z-34716-3b1a910b/`,
bound to source digest `286995aa07ab1f051178315571a2294893b6d2541adfc33ab80ee935720105d3`.
The route, hardware Intel D3D11 GPU timers, activity boundaries, runtime errors, restoration, and
cleanup all passed. The only invalidities were exact pipeline drift in
`fleet_full_render_10` (`84 → 86` programs) and `context_recover_steady` (`81 → 82`).

The retained boundary evidence identifies both causes rather than treating them as noise. Natural
courier `303` began the expected authored Kestrel admission after warmup as its distance closed from
`2488.25` to `1539.87` world units, changing residency `30/735 → 31/786`; the old warmup observed only
current state and could not predict entry into the 2400-unit runway during the upcoming sample.
Separately, the diagnostic `mining:start` arm remained active after its own scenario. Context loss
cleared its pooled GPU residency, then the leaked beam woke sprites/event lights during the later
recovery sample, adding five geometries, three textures, three visible unowned instanced roots, and
one program.

Candidate `c580a43e7d6c165949d6c58d64871aa0ad9fd87a` closes those harness defects without weakening the
stable-pipeline verdict. The renderer and evidence helper now share one pure authored-admission
runway predicate; capture supplies the exact upcoming sample horizon and will not start while a
pending inbound boundary can cross it. Promise presence is no longer mistaken for readiness: the
captured authored/precompile/exact gates settle under a bounded prerequisite barrier before the
five-second stability observation. Mining stress is armed inside the scenario journal and emits an
exact stop edge during failure-atomic restoration, so no later scenario inherits it.

Focused performance/render coverage passes 71/71, including seconds-scale regressions for the
2488-unit inbound courier and mining start/tick/stop journal. `npm run check:baseline` passes 10/10
in 42.194 seconds. No conclusion is promoted from the failed claim. Because source changed, one
fresh Browser claim is required before spending Electron on the same source.

## Eighth Browser pass and second Electron claim characterization — 2026-08-01

Candidate `191857fdc8aa44f1b77a20b4578e1b62d4118ead` passed Browser claim
`32112-9404c78daa9d2a9c5b1c8d1e` across all 25 windows with current route, Intel D3D11 GPU,
activity, context, restoration, issue, validity, and cleanup authority. Electron claim
`2612-798b762726a8c0634f5a3e40` used the same source digest
`c8a6cd7f2f232a74810c9a5e1c8cfb45a5914bd2d2964e6b6bd625aaddcfdb7f`, completed the same
matrix and cleanup, then failed only an Electron API deprecation warning and one first-use seam-marker
program link in `map_open` (`86 → 87`).

Candidate `7a5820a4` updates both Electron console listeners to the runtime's one-object event
contract, with executable lifecycle and source-platform regressions. Candidate `429f36f0` makes the
runtime and startup salvo share the exact seam-marker mesh/material program and proves through real
WebGL that waking the first live seam does not grow the compiled program cache. This revision adds
`electron/shipPreview.cjs`, `src/render/vfx.js`, `scripts/check-trail-streak-instancing-webgl.mjs`,
and `test/trail-streak-instancing.test.mjs` to the bounded repair write set. The old Browser pass
remains valid historical authority but cannot pair across the source change; a fresh Browser then
Electron pair is next.

## Ninth Browser claim characterization and exact VFX cache-key repair — 2026-08-01

Browser claim `7344-04c1b7704fa773e53b8f5ad4` on clean candidate `5473dab9` completed all route,
GPU, activity, issue, context, restoration, and cleanup authority, then failed only program drift in
`fleet_full_render_25` (`89 → 90`) and `bloom_off` (`100 → 101`). Both windows held authored
admission and residency constant. Electron was not spent.

The exact program-key reduction found three coupled startup defects: six temporary event lights were
counted twice when the compiled subtree was already attached to the target scene; the synthetic
ribbon geometry did not match the live `position/aTrailUv` attributes; and teardown disposed the
salvo owners needed for zero-count rare pools. Candidate `12b6b905` counts temporary lights once,
uses the live ribbon factory, and retains the bounded off-scene VFX owner root. The real production
precompile/teardown WebGL regression attaches the six-light live pool afterward and proves first
trail, seam, ribbon, and sprite wakes all keep program count at `14`. This revision adds
`src/render/precompile.js` and `src/render/engineTrailSurfaces.js` to the bounded repair write set.
A fresh Browser then Electron pair remains next.

## Broker retry binding repair — 2026-08-01

The next Browser request correctly stopped before launch with
`blocked_unresolved_failure: regression-required-after-acceptance-failure`. The previous failed
claim had a characterized and tested repair, but the manifest's regression/source identity omitted
the exact precompile, VFX, Electron-console, and real-WebGL files. Its regression digest therefore
could not distinguish the repaired candidate from the failed one. No runtime launched and no tenth
Browser claim was consumed.

Candidate `e343fe57` binds the three seconds-scale regressions, the four production owners, and the
two executable harness checks into both paired manifests. Manifest authority passes 12/12, the
focused repair regressions pass 11/11, real WebGL holds the full production-lifecycle program count
at `14`, and Electron 43.2.0 platform contracts pass. This is a broker-authority repair, not a
performance conclusion; one fresh Browser claim and, only after it passes, Electron on the exact
same clean pushed source remain next.

## Tenth Browser claim characterization and simulation-time runway repair — 2026-08-01

Browser claim `9824-4daa4c2591fd966a3d776dd4` on clean candidate `85f12646` completed the full
25-window route, including the formerly failing fleet-25, bloom-off, map, VFX, context-recovery,
admission, restoration, and cleanup checks. It failed only
`windows[0]-pipeline-warmup-unsettled` in `docked_market_ui`; Electron was not spent.

The retained trace proves the readiness failure was a false motion projection. The docked market
held `timeScale=0`, tick `3139 → 3139`, zero simulation steps, programs `87 → 87`, no active
admission, and identical `27`-asset / `799`-resource residency for 20 seconds. The runway predictor
nevertheless applied the wall-time sample duration to a frozen inbound Mule at 3324.57 WU.
Candidate `d521232a` converts the wall window to simulation time before admission prediction. Active
flight remains fully predictive, slow time remains conservatively scaled, and docked/diagnostic
pause uses a zero horizon. Focused performance contracts pass 50/50, deterministic sim comparison
is equal, and baseline passes 10/10 in 42.446 seconds. One fresh Browser then Electron pair remains
required because the harness source changed.

## Eleventh Browser claim characterization and semantic admission repair — 2026-08-01

Browser claim `18776-f52e09cf45f6f2f38ae80cec` on clean candidate `937fe965` proved the
simulation-time repair: docked window 0 settled normally, and all formerly failing fleet, bloom,
map, VFX, and paused windows remained stable. The full route and cleanup completed. Only
`jump_asset_admission` failed the validator because programs changed `85 → 84`; Electron was not
spent.

The retained boundary shows a valid transition, not missing readiness: warmup settled after three
cache transitions, station 179 then completed a new authored admission during measurement, every
queue ended empty, and residency stayed exactly 34 assets / 895 resources / 1,561,576,534 bytes.
The new sector released 23 old-sector geometries and one net program. The old admission rule required
global program count to grow, yet did not require any admission receipt, so it rejected valid scene
release and could accept a no-op jump. Candidate `20f78f49` instead requires a new terminal
`status=authored` sequence after the measurement-start snapshot; program growth, equality, or shrink
is then allowed at this transition-only boundary. Forty focused contracts pass, the retained claim
re-evaluates 25/25 validity rows green, and baseline passes 10/10 in 46.854 seconds. A new source-
paired Browser/Electron run remains required; the old failed artifact is not retroactively promoted.

## Twelfth Browser claim characterization and current-target repair — 2026-08-01

Browser claim `40996-2ebe5dcf069da08a18e4d580` on clean pushed candidate `b56d6e78`, source
digest `00154caf67a9c816ffa6c82f114b94bb3a5545597bdfbe3868a502f2d433d8a0`, completed the full
25-window route. The semantic admission repair passed. Every GPU, route, issue, restoration, context,
and cleanup predicate passed except two exact validity reasons: the post-cleanup activity census saw
the harness's own Chrome process exit, and `flight_steady @ bloom_off` grew programs `96 → 97`.
Electron was not spent.

Both failures were harness/runtime ownership defects, not external review or staffing blockers. The
Browser launcher proved only a disconnected Playwright client, so it could not await the exact OS
child before the end census. The repaired launcher now uses `chromium.launchServer()`, retains its
`ChildProcess`, closes the BrowserServer, and requires that exact process to exit. Boundary program
inventories then identified the bloom failure as material programs from already-installed Mule,
Atlas, recessed-canopy, and Helios-Lark roots compiling only after the render path switched away from
the HDR bloom target. Cached authored plans also had a wait-bypassing publication path.

Candidate `ddc9c1f5` removes that publication bypass, serializes the complete installed-scene compile
behind authored admissions, and makes each diagnostic variant compile the scene against its selected
target before measurement. The bounded Intel D3D11 reproduction now reaches `133` programs before
measurement and remains exactly `133`, with `measurementAdded: []`, for the 20-second bloom-off
window. Exact program inventory/delta evidence is retained at every measurement boundary.

- focused changed-lane regressions — PASS 54/54;
- live bloom-off exact-program diagnostic — PASS, `133 → 133`, zero measured additions;
- `npm run check:baseline` — PASS 10/10 in 42.564 seconds;
- code repair committed and pushed as `ddc9c1f5`.

The failed twelfth artifact remains historical. One fresh Browser claim is now justified on the
program-doc-synchronized candidate; Electron runs only if that Browser claim passes on the identical
source digest.

Before issuing that claim, a paired-authority audit found the new `partsLibrary.js`,
`pipelineReadiness.js`, and three seconds-scale regressions absent from the manifest digest despite
the broader renderer/harness files already changing it. Candidate `965c8dfb` binds all five exact
owners into both runtime manifests and adds their 31/31 green fast gate. Baseline remains 10/10 in
40.679 seconds. No claim was issued under the incomplete identity.

## Outcome

SpaceFace gains one versioned baseline/candidate harness that can independently answer four questions: did the game remain semantically equivalent, was the measurement valid, did the selected owner improve outside run noise, and did the candidate remain inside the absolute budget? The ordinary proof route alternates baseline and candidate across the same seeded Browser and Electron scenarios—boot to controllable flight, quiet and dense flight, combat, travel, dock/undock, save/Continue, authored cold/warm admission, lifecycle, and context recovery—while binding every sample to one frame/tick/query identity and exact process/environment evidence.

This packet changes evidence authority, not gameplay. A faster but different game fails equivalence; an equivalent run under a slow no-op control fails measurement validity; a valid equivalent run without a repeatable improvement closes only as neutral evidence.

## Entry conditions

- [x] Confirm the claimed worktree is clean at the selected base and record branch, HEAD, dirty-state fingerprint, renderer, display interval, power state, and process census.
- [x] Before the first packet edit, run and persist `npm run check:baseline` at that exact base; at exit rerun it and require the exit green set to be a superset of the entry green set. Repair any entry red or obtain an integrator-issued `INHERITED_RED` token—never self-classify a red check as out of scope.
- [x] Re-read the current performance closure contracts, scenario driver, profiler, runtime telemetry, post telemetry, validation broker, and existing performance tests named below.
- [x] Preserve the separate `sf-perf-modernization-20260726` foundation candidate as unaccepted input; review its exact diff rather than assuming its new harness files are authoritative.
- [x] Hold the `performance-evidence`, `validation-broker`, and `browser-gpu` mutexes before changing shared harness or launching an acceptance cell.
- [x] Do not run clean L4 captures while Blender/export or unrelated GPU-heavy work contaminates the machine; implementation and focused tests may proceed without pretending that environment is an acceptance baseline.
- [x] Characterize baseline-against-baseline variance and current invalidation gaps before changing the harness.

## Live seams

| Domain | Current owner symbol | Existing seam to reuse | Bounded addition |
|---|---|---|---|
| scenario setup | `scripts/lib/performanceScenarioDriver.mjs#performanceScenarioExecutionOrder`, `#preparePerformanceScenario`, `#waitForPerformanceScenarioReady`, `#restorePerformanceScenario` | deterministic scenario ordering, readiness, and restoration receipts | versioned seed/save/input/camera/entity-multiplier manifest |
| closure contracts | `scripts/lib/performanceClosureContracts.mjs#buildPerformanceClosureReport`, `#validatePerformanceClosureReport`, `#comparePerformanceWindows` | scenario/window schemas, matched budgets, environment and cleanup validation | separate equivalence, validity, and improvement verdicts |
| final ruling | `scripts/lib/performanceFinalAcceptance.mjs#evaluatePerformanceFinalAcceptance` | exact-run count, worktree identity, comparable matrices | require one cross-runtime `sourceCandidateDigest` while preserving distinct runtime broker and trace digests |
| profiler | `scripts/probe-performance-profile.mjs#runCrowdedFlightScenario`, `#sampleRuntime`, `#runDiagnosticVariants`, `#evaluateBudgets`, `#classifyBottleneck` | current route, controls, phase metrics, diagnostic isolation | alternating baseline/candidate orchestration and frame-linked identities |
| runtime matrix and launch | `scripts/lib/releaseSoakProbe.mjs#runPerformanceAttributionProbe`, `scripts/check-performance-attribution.mjs` | full `PERFORMANCE_SCENARIO_IDS` matrix plus existing isolated Browser/Electron launch and exact owned cleanup helpers | one runtime-parameterized matrix preserving identical order, restoration, artifacts, and teardown in both runtimes |
| runtime timing | `src/core/perfRuntime.js#ensurePerfRuntime` | frame, callback, sim, render, system, save, and render-work counters | common display-frame/sim-tick/render-frame/background-job IDs with disabled-by-default overhead |
| GPU query identity | `src/render/gpuTimers.js#createGpuTimers`; begin/end callers in `src/render/renderer.js#drawPreparedFrame` and `src/render/bloom.js#createBloom` | delayed non-blocking disjoint-aware queries and existing pass labels | immutable monotonic query ID plus origin display/render-frame IDs captured before `beginQuery`, retained through readback/drop accounting |
| presentation structure | `scripts/lib/performanceSceneMetrics.mjs#collectPerformanceSceneStructure`, `#collectPerformancePipelineReadiness` | visible roots/material/program/admission structure | stable transform/material/socket/LOD semantic projections |
| post allocation | `src/render/postTelemetry.js#recordPostRenderTargetAllocation`, `#getPostRenderTargetTelemetry` | stable-sample target-allocation evidence | attach post events to the common frame identity |
| manifest discovery | `scripts/validation-broker-cli.mjs#MANIFEST_LOADERS` | current tracked manifests are registered through a hard-coded table | path-safe `scripts/lib/validationManifestRegistry.mjs#loadValidationManifestById` that proves Git-tracked membership before import, rejects symlink escape, and verifies default-export/id agreement |
| expensive authority | `scripts/lib/validationBroker.mjs` and `scripts/validation-manifests/` | one-use candidate-bound acceptance claims with one runtime kind per manifest | paired `performance-closure-browser` and `performance-closure-electron` manifests with pinned authority fields, shared source/scenario identity, and distinct runtime claims |

Unknown or drifting owner contracts block the packet. The harness must extend these owners; it must not create a second simulator, renderer route, process launcher, scenario matrix, or acceptance ledger.

## Characterization and TDD contract

1. Preserve current green contracts with `test/performance-closure-contracts.test.mjs`, `test/performance-scenario-driver.test.mjs`, `test/performance-scene-metrics.test.mjs`, and `test/performance-attribution.test.mjs`.
2. Add a failing equivalence contract that mutates one authoritative field and reports the first divergent tick, field, and preceding ordered events.
3. Add a failing presentation contract that mutates one transform, material pipeline key, or socket and reports that semantic difference.
4. Add a failing validity contract for a slow no-op control, wrong renderer, disjoint GPU timer, route divergence, cache mismatch, or contaminating process.
5. Add GPU timer contracts for delayed readback, dropped/full pending queues, disjoint invalidation, context loss, and nested-begin refusal; every terminal record names its immutable query/display-frame/render-frame origin.
6. Add a baseline-against-itself contract that reports variance without manufacturing improvement.
7. Add manifest-discovery contracts that reject traversal/unsafe IDs, unknown modules, symlink escape, missing default exports, and manifest-ID mismatches. In a temporary Git fixture, create a safe-named untracked module with a top-level side effect and prove tracked-membership rejection occurs before import/execution.
8. Add paired-manifest contracts that pin each ID, `runtimeKind`, `mode: 'acceptance'`, `requireBrokerClaim: true`, `maxLaunchesPerCandidate: 1`, fixed seed, command/arguments, cleanup policy, artifact root, and shared source/scenario identity.
9. Add final-acceptance contracts proving Browser/Electron `sourceCandidateDigest` equality, runtime-bound `candidateDigest` inequality, and distinct raw-trace digests; a direct raw attribution invocation must remain diagnostic/non-promoting.
10. Observe each focused failure before implementing the corresponding owner behavior; never rewrite a golden to accommodate an optimization.

## Work breakdown

### Phase 0 — current evidence characterization

- [x] Inventory current scenario, profiler, closure, process-control, GPU-timer, and cleanup fields.
- [x] Record which values lack a shared frame/tick/query identity and where browser delay is conflated with game callback or GPU work.
- [x] Establish deterministic simulation and presentation-semantic projection schemas with explicit versioning.

### Phase 1 — pure manifests and equivalence

- [x] Add a versioned scenario manifest compiler/validator.
- [x] Add tick-by-tick authoritative projection comparison with first-difference reporting.
- [x] Add fixed-snapshot presentation-semantic comparison for roots, transforms, visibility, LOD, geometry/material identity, sockets, and HUD accessibility semantics.
- [x] Keep reports deterministic, bounded, path-safe, and free of gameplay mutation.

### Phase 2 — frame-linked attribution and validity

- [x] Add common identities for display frame, simulation tick, render frame, GPU query, and background job.
- [x] In `createGpuTimers`, assign a monotonic query ID and capture immutable display/render-frame origin before `beginQuery`; carry it through delayed readback and publish explicit completed, dropped, disjoint, nested-refused, reset, and context-loss accounting.
- [x] Update only the bounded renderer/bloom begin/end call sites needed to pass frame origin; no pass ordering or visual behavior changes.
- [x] Add explicit invalidation reasons and fail closed when the environment cannot support a causal comparison.
- [ ] Measure instrumentation overhead disabled and enabled; retain only bounded, reusable records.

### Phase 3 — broker-managed matched route

- [x] Replace the CLI-only hard-coded manifest table with a tested, path-safe tracked-manifest loader. Resolve only `scripts/validation-manifests/<safe-id>.mjs`, reject symlinks/escape, prove regular-file and `git ls-files --error-unmatch -- <repo-relative-path>` membership **before** dynamic import, then require a default export and `manifest.id === id`.
- [x] Refactor `runPerformanceAttributionProbe` and its CLI into one runtime-parameterized matrix that reuses existing Browser/Electron launch, canonical route, profile/listener isolation, and exact owned cleanup seams. Scenario order, restoration, timeout, and artifact policy must be identical across runtimes.
- [x] Add `performance-closure-browser` and `performance-closure-electron` manifests. Each explicitly pins its ID, scalar runtime kind, acceptance mode, broker-claim requirement, one-launch quota, seed 47, runtime-locked `check-performance-attribution.mjs` command/arguments, cleanup policy, timeout, and distinct artifact root.
- [x] Define `sourceCandidateDigest` as the stable hash of exact worktree/source/build identity plus production, harness, scenario-manifest, save/input-tape, and camera-manifest digests, excluding manifest ID and `runtimeKind`. Require equality across paired Browser/Electron evidence while preserving distinct broker `candidateDigest` values and distinct raw-trace digests.
- [x] Make direct `check-performance-attribution.mjs` or `runPerformanceAttributionProbe` acceptance invocation fail closed without `SF_BROKER_CLAIM`; diagnostic invocation may run but cannot publish primary/promoting evidence.
- [x] Produce separate verdict fields for equivalence, measurement validity, and improvement.

### Phase 4 — adversarial closure

- [x] Prove the three injected failures above are caught.
- [x] Prove a contaminated run cannot promote performance while still reporting semantic equivalence honestly.
- [x] Prove baseline-against-baseline and Browser/Electron matrices terminate with bounded artifacts and no leaked processes.

## Write set

Initial bounded paths:

- `scripts/lib/performanceScenarioDriver.mjs`;
- `scripts/lib/performanceClosureContracts.mjs`, `scripts/lib/performanceFinalAcceptance.mjs`, and `scripts/lib/performanceSceneMetrics.mjs`;
- `scripts/check-performance-final-acceptance.mjs` for bounded baseline/equivalence CLI inputs;
  L0 `node --test test/performance-final-acceptance.test.mjs`; L1 the packet focused suite below;
- `scripts/probe-performance-profile.mjs`;
- `scripts/lib/releaseSoakProbe.mjs#runPerformanceAttributionProbe` and `scripts/check-performance-attribution.mjs` for the single runtime-parameterized matrix/launcher route;
- `src/core/perfRuntime.js`;
- `src/render/partsLibrary.js` for authored-upgrade job identity and one frame-bounded exact-pipeline
  publication path for both newly decoded and already-resident plans; no authored substitute or
  quality reduction;
- `src/render/pipelineReadiness.js` and `src/render/renderer.js` for one serialized current-target
  compile of the complete installed scene after pending authored admissions;
- `src/render/gpuTimers.js#createGpuTimers`, plus only the query begin/end call sites in `src/render/renderer.js#drawPreparedFrame` and `src/render/bloom.js#createBloom` needed to pass immutable frame origin;
- `src/render/postTelemetry.js`;
- `electron/main.cjs` and `scripts/lib/gameServer.cjs` for the Electron-only response policy;
- `electron/shipPreview.cjs` for the same supported Electron console-event contract as the main shell;
- `src/core/rapierCompatRuntime.js`, `src/core/rapierCollisionWorld.js`, and
  `src/core/sg02DynamicBodyOwner.js` for the capability-limited strict-CSP Rapier bootstrap;
- `src/render/contextResourceLifecycle.js` and the context-loss boundary in
  `src/render/renderer.js` for event-gap and restore ordering;
- `src/render/vfx.js` for the exact seam-marker startup-precompile factory;
- `src/render/precompile.js` and `src/render/engineTrailSurfaces.js` for exact VFX light, lifetime,
  and lazy-ribbon program keys;
- `src/render/authoredAdmissionPolicy.js` for the shared live/capture runway predicate;
- `vendor/addons/loaders/KTX2Loader.js` for the two exact Basis/Emscripten strict-CSP shims;
- `scripts/lib/playwrightCspPolling.mjs` and `scripts/check-electron-new-game-launch.mjs` for
  direct-evaluation polling, exact runtime identity, and bounded failure snapshots;
- `scripts/lib/alphaLiveBaselineContracts.mjs` for exact BrowserServer and owned-child teardown proof;
- focused strict-CSP, runtime, Basis, context, Electron-security, and authored-route tests under
  `test/`;
- new `scripts/lib/performanceScenarioManifest.mjs` and `scripts/lib/performanceEquivalence.mjs`;
- `scripts/validation-broker-cli.mjs` plus new `scripts/lib/validationManifestRegistry.mjs` for pre-import path/Git-tracked manifest discovery;
- new runtime-specific `scripts/validation-manifests/performance-closure-browser.mjs` and `scripts/validation-manifests/performance-closure-electron.mjs`;
- new `test/performance-scenario-manifest.test.mjs`, `test/performance-equivalence-contract.test.mjs`, `test/performance-runtime-identities.test.mjs`, `test/gpu-timer-attribution.test.mjs`, `test/performance-attribution-runtime-matrix.test.mjs`, `test/validation-manifest-registry.test.mjs`, and `test/performance-closure-manifests.test.mjs`;
- bounded updates to `test/performance-closure-contracts.test.mjs`, `test/performance-final-acceptance.test.mjs`, `test/performance-closure-probe-contract.test.mjs`, `test/performance-scenario-driver.test.mjs`, `test/performance-scene-metrics.test.mjs`, and `test/performance-attribution.test.mjs`;
- bounded updates to `test/alpha-live-baseline-contracts.test.mjs`,
  `test/authored-entity-plan-budget.test.mjs`, and `test/render-target-pipeline-warmup.test.mjs`;
- bounded updates to `test/authored-critical-admission-order.test.mjs` for the existing queue seam;
- bounded seam-precompile updates to `test/trail-streak-instancing.test.mjs` and
  `scripts/check-trail-streak-instancing-webgl.mjs`;
- bounded registry-migration updates to `test/pq007-control-route-manifest.test.mjs`, `test/pq019a-capsule-presentation-h1-manifest.test.mjs`, `test/pq019-surface-heist-manifest.test.mjs`, `test/pq020-ceres-topology-manifest.test.mjs`, `test/pq021-ledger-route-manifest.test.mjs`, `test/pq022-corridor-asset-leaves-h1-manifest.test.mjs`, `test/pq023-corridor-cues-h1-manifest.test.mjs`, and `test/pq024-asteroid-claim-manifest.test.mjs`, replacing obsolete hard-coded-table source assertions with real tracked-registry resolution;
- ignored evidence only under the two manifest-owned roots `.devshots/perf/closure/browser/` and `.devshots/perf/closure/electron/`;
- `design/program/roadmap/receipts/PQ-034-performance-harness-REPORT.md` at terminal disposition.

Any additional runner, adapter, or test not named above must be added to this packet revision with an exact path and L0/L1 command before mutation. No `package.json`, runtime gameplay systems, authored assets, release manifests, settings defaults, or global program status pages are in the implementation write set without a separate integrator lease.

## Mutexes

- `performance-evidence` — one actor owns closure schema and acceptance artifacts.
- `validation-broker` — broker/manifest changes serialize with other expensive-route work.
- `browser-gpu` — one acceptance campaign at a time on the evidence machine.
- `git-index` only for the integrator's final atomic control-plane/receipt update; feature work must use pathspec-limited staging.

## Non-goals

- Optimizing a game subsystem inside the evidence packet.
- Replacing the deterministic lab, validation broker, current profiler, or final acceptance owner.
- Declaring the contaminated July 25 trace a baseline.
- Treating 16.7 ms as the physical display interval when the measured no-op floor differs.
- Using lower render scale, fewer entities, disabled bloom/effects, or different route content as improvement.
- Leaving always-on high-cardinality clocks, object-heavy traces, or process polling in normal play.
- Claiming pixel identity across drivers when the declared semantic oracle is the stable contract.

## Performance and quality budget

- Instrumentation disabled: no per-frame allocation and timing overhead below measurable noise.
- Instrumentation enabled: under 1% median owner/callback overhead on the matched route, with bounded ring/slab storage and explicit overflow diagnostics.
- Every GPU query terminal state preserves its immutable origin identity; a dropped, disjoint, context-lost, nested-refused, or undrained query invalidates the affected timing claim rather than being silently omitted.
- Artifact count and bytes are bounded per predeclared cell; raw traces are ignored evidence, not tracked source. Paired evidence shares `sourceCandidateDigest` but never aliases runtime broker or raw-trace digests.
- Process enumeration occurs at bounded route boundaries, not continuously.
- Scenario entity/population, visuals, settings, viewport, seed/save, and route remain identical between arms.
- Reports publish p50/p95/p99/max, long frames, missed-vsync/multi-step data, phase owners, GPU confidence, uploads, allocation/GC, admission, and process-contamination status where available.

## Verification budget

Run `npm run check:baseline` before and after edits. The harness's own contract tests are the
natural owners — they're already written to prove the three-way distinction (same game / valid
capture / faster) this packet is about. Don't over-build beyond what convinces you the harness is
trustworthy enough to gate later perf work.

Focused tests for this packet: `test/performance-scenario-manifest.test.mjs`,
`test/performance-equivalence-contract.test.mjs`, `test/performance-runtime-identities.test.mjs`,
`test/gpu-timer-attribution.test.mjs`, `test/performance-attribution-runtime-matrix.test.mjs`,
`test/performance-closure-contracts.test.mjs`, `test/performance-final-acceptance.test.mjs`,
`test/performance-scenario-driver.test.mjs`, `test/performance-scene-metrics.test.mjs`, plus
`test/authored-critical-admission-order.test.mjs`, plus
`npm run check:sim:compare` for deterministic coverage.

Rendered presentation, GPU timing, compositor floors, and the full production route can't be
certified by the deterministic lab. When you need that authority, use the paired broker manifests
(`performance-closure-browser` / `-electron`) on a clean machine; they share a source digest but
keep distinct runtime candidate digests. Don't invent a fake lab scenario to cover the gap, and
don't loop broker captures on a contended machine.

## Lab and broker gap

The current lab can't certify rendered presentation, Browser/Electron public route, GPU timing,
compositor floors, or process contamination. Use `npm run check:sim:compare` for deterministic
coverage, then close the expensive authority with `performance-closure-browser` and
`performance-closure-electron` broker manifests on a clean machine. The pair shares a source digest
but keeps distinct runtime candidate/raw-trace digests.

## Review questions

- Can the report distinguish semantic difference, invalid measurement, and neutral performance without conflating them?
- Does every metric name its frame/tick/query/background-job identity and exact candidate/environment, including delayed/dropped query origin?
- Do paired runtimes share only `sourceCandidateDigest` while retaining distinct broker candidate and raw-trace digests?
- Can one injected simulation or presentation defect be localized at the first causal difference?
- Are diagnostic switches restored and forbidden from becoming accepted quality settings?
- Is overhead bounded and absent from normal play when disabled?
- Does the broker stop unchanged retries and clean every browser/server/Electron process?

## Stop conditions

Return `BLOCKED` when the worktree or baseline is dirty, the evidence machine is contaminated, a required owner is concurrently leased, the current broker cannot bind the needed identity without a shared change, semantic projection ownership is unknown, or instrumentation materially perturbs the measured owner. Return `FAIL` when injected false-negative tests survive, baseline-against-baseline is unstable beyond declared noise, or cleanup/process identity cannot be trusted. Never proceed to later packet acceptance with an equivalence or validity defect.

## Checkoff and receipt

- [x] Exact scenario and projection schemas versioned.
- [x] Simulation and presentation mutations fail at the first causal difference.
- [x] Contaminated capture fails validity without falsifying equivalence.
- [x] Baseline-against-baseline reports natural variance and no invented gain.
- [ ] Browser/Electron broker cells are source-paired, runtime-candidate-bound, bounded, and cleaned up; direct raw invocation cannot promote.
- [x] GPU timer delayed-readback/drop/disjoint/context-loss/nested-begin contracts preserve or invalidate immutable origin identity correctly.
- [ ] Disabled/enabled overhead budget measured.
- [ ] Integrator discovery review and causal re-review terminal.
- [ ] Receipt written to `../receipts/PQ-034-performance-harness-REPORT.md`; no later packet is promoted from unbound evidence.

## References

- [`../../../PERFORMANCE_MODERNIZATION_EXECUTION_PLAN.md`](../../../PERFORMANCE_MODERNIZATION_EXECUTION_PLAN.md)
- [`../../../PERFORMANCE_OPTIMIZATION_CONSTELLATION.md`](../../../PERFORMANCE_OPTIMIZATION_CONSTELLATION.md)
- [`../../../PERF_BUDGET.md`](../../../PERF_BUDGET.md)
- [`../../../../docs/VALIDATION_WORKFLOW.md`](../../../../docs/VALIDATION_WORKFLOW.md)
- [`../00_EXECUTION_PROTOCOL.md`](../00_EXECUTION_PROTOCOL.md)
