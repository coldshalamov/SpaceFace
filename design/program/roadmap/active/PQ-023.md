<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-023 — unified physics-readable presentation language

```yaml
queueId: PQ-023
lifecycle: blocked
acceptance: focused_green
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
claimRule: select one cue-family leaf before implementation
identityAction: resolve shared G19 and legacy graphics-slice aliases when the first executable leaf is admitted
h2ReviewReceipt: ../receipts/PQ-023-h2-cue-motion-accessibility-REPORT.md
remainingPhases: targeted combat-readability causal review, H3 matched performance, then milestone promotion
```

## Recovery checkpoint — 2026-07-31

`PQ-023.cues-harness-repair` is focused-green. The retained H1 failure was a harness deadlock:
the player was `4936.901 WU` from a Cathedral whose authored approach range is `2400 WU`, while
both hosts waited for strict authored admission before invoking their existing framing/movement
seam. Browser and Electron now discover the production root, frame/move, and only then await
authored readiness. A seconds-scale regression pins both host orderings.

No headed route was launched and no retained evidence was replaced. The missing Cathedral H1
sequence, Browser completion, Electron parity, human motion/accessibility verdict, and matched
performance remain open as downstream exact units.

`PQ-023.cues-h1-capture` was claimed by the Codex primary integrator at candidate base
`51d5a045`. It held only `browser-gpu` and `validation-broker` plus the route/evidence paths
listed by that exact queue unit. Its accepted continuation used fresh materially changed Browser
candidates and one causal Electron re-review for the missing Cathedral continuation. Existing combat,
reduced-motion, and dense-scene artifacts remain retained and were not replaced.

The first Browser claim on candidate `eac04e78` failed with fingerprint `8ba3c191…` before any
Electron launch. World-site admission intentionally replaced the Cathedral root's recyclable
runtime entity id, while the framing seam retained the pre-admission id. The repaired route now
approaches by durable site/role identity before admission, reacquires the admitted root, and only
then requires its authored mesh for final framing. A seconds-scale regression covers that exact
replacement. The failed claim is retained; only a changed candidate may receive the next claim.

The next Browser claim on candidate `738d7b8d` reached the reacquired authored root, then failed
with fingerprint `a9fda4ec…` because the refactored page callback still passed one dangling
`targetId` argument at its outer call boundary. That argument is removed and the source-order
regression now rejects its return. Electron again did not launch.

Candidate `989be6b2` completed admission plus normal recovery/damage, then failed at the first
reduced frame with fingerprint `d35c6a3c…`. The harness had changed only the effective
`video.motionReduce` boolean; the live accessibility owner correctly reconciled it back from the
unchanged `accessibility.motionPreference: system`. Both hosts now change the preference owner
(`reduce|full`) and flash choice together, with an owner-to-effective reconciliation regression.
Electron did not launch and none of the failed partial artifacts replaces retained evidence.

Candidate `a6e73aaf` then completed all `87/87` Browser frames and all `15` motion segments on
Intel D3D11 with zero page issues. Its Cathedral projection contains the four expected
recovery/damage transitions, ring/bracket captions, correct reduced flags, and steady reduced
fixtures. The legacy aggregate nevertheless returned `ok:false` under fingerprint `a4a4ba4c…`
because its final cleanup conjunction discarded the individual predicate results. The same
predicates are now recorded as named `acceptanceChecks` plus `failedAcceptanceChecks`; this is
diagnostic observability, not a relaxed gate. Electron remained correctly gated.

Candidate `f61f20e0` reproduced the completed Browser projection and named the only terminal reds:
`particlesClean` (`6` live), `spritesClean` (`26` live), and `trailStreaksClean` (`2` live).
The Cathedral continuation had restored `timeScale: 1`, but final cleanup still assumed the
original frozen combat route; ambient simulation events repopulated VFX immediately after reset.
Cleanup now explicitly freezes simulation and clears its accumulator before resetting the pools.
A seconds-scale regression observes that order inside the reset callback. Electron did not launch.

Candidate `1d950ce7` then reached normal damage and failed with fingerprint `5f18b0b4…` because
the damage-stage root replacement moved the subject outside the framing inherited from the prior
recovery root. Framing is now performed after every admitted recovery/damage transition in both
hosts, with a regression requiring the Browser per-stage call and all five Electron calls
(initial plus four replacements). Electron again did not launch.

Candidate `f373e8a7`, Browser claim `40008-bf8ebe348500b65aa41197e6`, digest `3b31ee47…`,
passed the complete Browser cell on real Intel ANGLE/D3D11: `87/87` frames, `15/15` motion
segments, three contact sheets, all four Cathedral transitions and accessibility projections,
all named cleanup/framing predicates, and zero page issues. The first authorized Electron parity
attempt then completed and captured all four Cathedral states but reported an aborted
`47a.scenario.json` request plus `[boot] TypeError: Failed to fetch`. The harness had already
reached and awaited the canonical first window, then navigated to the identical root a second
time, cancelling that first-window boot. Electron now asserts the canonical URL and continues
the existing boot without `goto` or `reload`; the seconds-scale manifest regression rejects a
second navigation. Browser evidence remains valid because no production `src/` path changed.

The causal Electron re-review passed on commit `e2bb0165`: real Intel ANGLE/D3D11, exact Browser
semantic projection parity, all four normal/reduced Cathedral states, zero page issues, and clean
owned runtime/profile teardown. The continuation is committed under row 6 and exact receipt
`PQ-023-cues-h1-capture-REPORT.md`. The unit is done and releases `browser-gpu` plus
`validation-broker`. H2 review is complete: Cathedral accessibility, ordinary/capital destruction,
reduced mode, and dense composition are kept; flak/autocannon differentiation and small-destruction
salience are revised. Exact repair, targeted H1, and causal review now precede H3. No performance or
milestone-promotion claim is inherited from H1.

`PQ-023.combat-readability-repair` is focused-green. The missing `proximity-burst` renderer branch
now executes a compact full-volume flak release instead of falling through the autocannon default,
and small destruction now opens as a readable hot asymmetric breakup without borrowing the ordinary
ring grammar. The two behavior regressions reproduced the defects at 20/22 before the product edit
and pass at 22/22 after it; broader presentation and baseline gates are green. Receipt:
`PQ-023-combat-readability-repair-REPORT.md`. No headed or performance claim was made, so targeted H1
and its causal review remain the exact next cue units.

`PQ-023.combat-readability-h1` was claimed for the bounded continuation. Its candidate-bound manifest
selects exactly five changed cells—autocannon, flak, small destruction, reduced small destruction,
and one dense representative—then runs source Electron only after Browser PASS and compares a
normalized projection of the executed VFX pools. The pre-claim harness regression went from 13/14
to 16/16, including fail-closed mutants for flak release, small ring reuse, reduced settings, and the
dense connected beam. The accepted Cathedral, unrelated weapon, ordinary, and capital evidence is
retained. It held `browser-gpu` and `validation-broker` only until the one-use pair was recorded.

The targeted pair is now accepted at candidate `b626a8b3`. Browser claim
`20336-b461d7ba80289197646bd11d` passed 22 captures, five motion segments, and three sheets on real
Intel ANGLE/D3D11 with zero issues and clean pools. The gated source-Electron continuation matched
the exact normalized five-cell pool projection, emitted zero issues, and closed cleanly. Complete
artifacts are committed under `evidence/h1/row6-pq023-combat-readability/`; receipt:
`PQ-023-combat-readability-h1-REPORT.md`. H1 is done and releases `browser-gpu` plus
`validation-broker`; causal visual review remains separate and claim-ready.

Final `check:presentation` reproduced one preflight-only WebGL evidence race: Three's linked-program
wrapper retained the attributes it cached during binding, while a later raw-handle
`ACTIVE_ATTRIBUTES` query intermittently returned zero. The evidence helper now prefers the durable
Three cache and falls back to the raw handle; a seconds-scale late-empty-handle regression and three
consecutive real WebGL proofs pin the correction. No production renderer or visual was changed.

## Outcome

The player can read force, mass, velocity, attachment, charge, danger, damage, targeting, and recovery through a coherent hierarchy of motion, VFX, audio, camera, HUD, and accessible fallback cues. The game should look more spectacular—not more restrained—while every effect remains mechanically truthful, bounded, and usable in dense play.

PQ-023 is an umbrella. Implement separate leaves with distinct owners and routes:

1. **Physics cue vocabulary:** shared cause/state/intensity/ownership semantics for boost, RCS, Massline, fields, gravity, impacts, payload load, and recovery.
2. **Propulsion:** idle/cruise/boost/RCS/socket families and heat/load response.
3. **Weapons/impacts/destruction:** mechanically distinct projectile/beam/plasma/missile/flak/impulse and damage phases.
4. **Localized environment:** sector-specific depth, lighting, dust/debris/nebular structure without lifting all space into haze.
5. **HUD physics telemetry:** hierarchy for target, mass/load, Massline state, boost, fields, damage, warnings, and next action.
6. **Camera language:** acceleration, bank, scale, focus, impacts, tethers, planets, and reduced-motion alternatives.
7. **Dense-scene convergence:** cue arbitration, pooling, occlusion, accessibility, and target/floor performance across representative combat/travel/mining scenes.

A leaf may coordinate two cue channels when one mechanical event requires them, but it may not redesign every presentation surface at once.

## Entry conditions for a leaf

- [ ] Exact mechanical owner events/state and ordinary route traced.
- [ ] Existing VFX/audio/camera/HUD/profile owners and current cues characterized.
- [ ] Largest player readability/quality failure reproduced in motion, not inferred from source style.
- [ ] Renderer/VFX/HUD/styles/audio/camera/GPU mutexes free for the selected leaf.
- [ ] Baseline capture includes normal and dense representative scene, reduced-motion/flash, and target/floor cost.
- [ ] Cue can be expressed as a consumer of owner truth; no presentation-owned gameplay state.

## Cue contract

Every admitted cue family maps:

```js
{
  cueId,             // stable semantic ID
  sourceOwner,
  sourceReceiptId,
  state,             // armed/latched/taut/releasing/broken/etc.
  intensity,         // normalized from authoritative physical values
  origin,
  direction,
  targetId,
  priority,
  lifetimeTicks,
  accessibilityText,
  reducedMotionMode,
  reducedFlashMode
}
```

Exact fields follow current profile/event conventions. Identity and intensity derive from gameplay owners; rendering/audio/camera/HUD only project them. Event arrival order must not decide durable state. Transient duplicate suppression uses stable receipt/cue IDs and bounded windows.

For each mechanical state, specify:

| Question | Required answer |
|---|---|
| What caused it? | exact owner event/projection |
| What should the player infer? | force, ownership, risk, timing, direction, success/failure |
| Which channels carry it? | motion/VFX/audio/camera/HUD/text/haptic where applicable |
| What is critical vs flavor? | critical semantics survive channel reduction |
| How does it scale? | physical intensity and screen distance, bounded/capped |
| What is counterplay? | cue exposes a decision/window, not only spectacle |
| What happens in reduced motion/flash or no audio? | static/low-motion/text/non-color equivalent |
| How is cost bounded? | pool, cadence, culling, instancing, priority/arbitration |

## Work breakdown for a leaf

### Phase 0 — event and perception audit

- [ ] Trace exact owner event/state and current presentation consumers.
- [ ] Record state machine, physical ranges, duplicate/replay behavior, and current accessibility semantics.
- [ ] Capture current cue at normal route and dense stress route with motion/video where needed.
- [ ] Identify the smallest set of perceptual dimensions needed: silhouette, direction, phase, rhythm, color/value, scale, persistence, sound, camera, text.

### Phase 1 — declarative profile and focused contracts

- [ ] Extend one existing declarative cue/VFX/audio/camera/HUD profile rather than branching on feature names across render code.
- [ ] Normalize physical intensity once; cap and quantize where needed for deterministic/bounded presentation.
- [ ] Test owner-event mapping, phase ordering, duplicate suppression, cleanup, fallback semantics, and reduced modes.
- [ ] Preserve one gameplay/render route across Browser/Electron/probes.

### Phase 2 — implementation and composition

- [ ] Implement pooled/instanced/shared-resource presentation with no per-frame allocation.
- [ ] Bind to exact sockets/positions/directions and frame-origin/interpolation path.
- [ ] Add cue-priority/arbitration so critical state survives dense scenes while flavor degrades gracefully.
- [ ] Coordinate channels: camera cannot fight targeting; HUD cannot cover playfield; audio/flash cannot be the sole critical cue.
- [ ] Keep hidden/inactive UI/effects asleep and fully disposed.

### Phase 3 — independent motion review and tuning

- [ ] Review at normal camera, close diagnosis, far/readability, dense overlap, damage/failure, and reduced variants.
- [ ] Use a small predeclared parameter matrix tied to physical outcomes, not unrecorded taste iteration.
- [ ] Select values once from quantitative readability/performance plus independent player judgment; pin them in focused tests/profiles.
- [ ] One causal re-review verifies fixes; unrelated style ideas become future leaves.

### Phase 4 — route/performance closure

- [ ] Browser/Electron ordinary route with source fingerprint and exact cue IDs.
- [ ] Target/floor matched before/after: frame phases, particles/sprites/lights, draw calls/programs, DOM/compositor, resources, hitches, cleanup.
- [ ] Dense scene preserves critical cue accuracy and target selection while remaining within profile contract.
- [ ] No accepted visual quality removed or default settings lowered.

## Leaf write set

Name exact current profile, consumer, focused test/check, route harness, and receipt paths. Shared renderer/VFX/audio/HUD/camera changes need characterization and at least two representative consumers when they claim a general abstraction. Avoid `package.json`, input, gameplay owners, save schema, registry order, and unrelated CSS unless separately leased.

## Non-goals

- A universal aesthetic recipe, fixed palette, bloom ban, or one effect for all mechanics.
- Presentation-owned combat/physics/target/save truth.
- Full HUD rewrite during a propulsion leaf.
- Camera motion that steals control or obscures aiming.
- Full-screen fog/post effects used to hide weak assets.
- Reducing particle quality, render scale, shadows, bloom, content, or authored assets to pass performance.
- Exact effect-count tests that do not prove player perception.

## Performance and quality budget

Every leaf declares:

- maximum simultaneously active cue instances by priority/class;
- pool caps and exhaustion behavior preserving critical cues;
- emission/update cadence and distance/screen-size culling;
- instancing/batching/material/program sharing;
- dynamic light and post-processing budget;
- DOM/compositor/listener/observer behavior for HUD;
- no hot-path allocation/resource creation;
- baseline/peak/end resource high-water and cleanup;
- target/floor matched route and dense-scene thresholds.

Quality scales through hierarchy and priority, not wholesale deletion. When overloaded, retire low-priority flavor first while retaining mechanical state, direction, timing, and accessible fallback.

## Verification budget

```yaml
L0:
  - node --check for changed source/scripts
  - shader/profile/data validation
  - git diff --check
L1:
  - cue mapping, lifecycle, pool, duplicate, reduced-mode, cleanup tests
L2:
  - npm run check:presentation
  - npm run check:camera when relevant
  - npm run check:ui-a11y and check:wcag-contrast for UI leaves
  - relevant owner/determinism/visual-stability/perf-budget checks
L3BrokerManifest: pq023-<leaf-id>
acceptanceAttemptsPerCellPerCandidateDigest: 1
reviewClosure: discovery -> repair -> causal re-review when repairs affect the claim
```

A still image cannot accept motion timing, phase, camera, or dense cue arbitration. Record a short route video/trace where those are the claim.

## Review questions

- Can a player infer what force/state happened, from where, to whom, and what decision follows?
- Are boost, Massline, gravity/fields, impacts, and weapon families mechanically distinct rather than recolors?
- Does spectacle reinforce scale and physics without burying targets, projectiles, or interaction cues?
- Do reduced-motion/flash, no-audio, forced-colors, and increased text scale preserve critical meaning?
- Are cue lifetimes/pools/resources bounded and fully cleaned up?
- Is the accepted result stronger in motion at the real camera, not merely more effects in an isolated preview?

## Stop conditions

- Mechanical owner event/state is ambiguous or unavailable.
- Leaf requires gameplay mutation or another cue truth store.
- Shared presentation path is actively leased.
- Dense critical cues cannot survive the proposed effect stack.
- Accessibility requires removing the only mechanical signal rather than adding an equivalent channel.
- Browser/Electron or interpolation/frame-origin identity diverges.
- Performance can pass only by lowering default quality or deleting required spectacle.

## Parent checkoff

For each leaf:

- [x] Exact owner→cue contract focused-green.
- [ ] Motion/normal-camera/dense/reduced-mode independent review accepted. *(H2 keeps Cathedral,
      ordinary/capital, reduced, and dense presentation; the flak/small code repair is focused-green,
      while targeted H1 and causal review remain open in `PQ-023.combat-readability-*`)*
- [ ] Browser/Electron route and matched performance/cleanup accepted.
- [x] Leaf receipts linked. *(implementation, H1, and H2 review receipts are exact; final milestone
      upgrade remains downstream)*

The integrator tracks parent coverage. A leaf worker never marks PQ-023 complete.

## References

- [`../../../graphics-sprints/LONG_TERM_GRAPHICS_OVERHAUL.md`](../../../graphics-sprints/LONG_TERM_GRAPHICS_OVERHAUL.md)
- [`../../../spec2/06_UI_IDENTITY.md`](../../../spec2/06_UI_IDENTITY.md)
- [`../../../PERF_BUDGET.md`](../../../PERF_BUDGET.md)
