<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-191 — Senior review: the flash ten-unit batch (and its taste calls)

```yaml
queueId: PQ-191
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-06 (PQ-144.00/.186.00/139.04/143.02/184.00 shipped)
```

## Outcome

A senior agent has looked over the work the "flash ten-unit batch" session shipped, judged it
against the law and the owner's taste, and either ratified it or corrected it. The batch is
documented well enough that this review can start from receipts alone; nothing in it should need
archaeology.

## Source

Session goal 2026-09-05/06 (owner-directed): pick ten ready units rated ≤ 5 complexity, implement
out of order with subagent review loops, verify, mark done, and leave a review task in the build
map. Five units shipped before the session closed (see "The work to review"); the remainder
(PQ-184.01/.02/.03, the two PQ-022 captures) went back to the pool untouched — they are normal
ready units, not leftovers.

## The work to review (receipts are the entry point)

| Unit | Receipt | One line |
|---|---|---|
| PQ-144.00 | `design/program/roadmap/receipts/PQ-144-00-REPORT.md` | the three density layers as a budget table (PERF_BUDGET §8) |
| PQ-186.00 | `design/program/roadmap/receipts/PQ-186-00-REPORT.md` | every feel bar as a standing check that fails in the bar's own words |
| PQ-139.04 | `design/program/roadmap/receipts/PQ-139-04-REPORT.md` | tumbling ships corkscrew their trail (both plume families) |
| PQ-143.02 | `design/program/roadmap/receipts/PQ-143-02-REPORT.md` | six texture one-offs on the starter route |
| PQ-184.00 | `design/program/roadmap/receipts/PQ-184-00-REPORT.md` | per-surface UI frame/DOM budgets, measured, gated |

## The taste calls on the table (approve, adjust, or overturn — none is precedent)

These are the decisions the batch made where the law allowed more than one answer. Each names the
alternative it declined. The reviewer's judgment, not this list, is the record.

1. **Grammar debt is reported, not fatal** (PQ-184.00): 24 surfaces over the 2 ms budget print as
   named debt in every `check:ui:budgets` run; `--strict` makes it red. The packet's own order
   (instrument before fixes) motivated this; the alternative was gating on the grammar budget
   immediately and accepting a red chain until PQ-184.01/.02 land. Check whether the debt table is
   being read, and whether `--strict` has an owner.
2. **The Cinder Run Courier is a deterministic traffic fixture** (PQ-143.02): a new authored-slot
   seam in traffic.js (`_ensureCinderRunCourierFixture`), start-sector only, express role. The
   alternative was a pick-pool member (rejected in review: it broke Ceres's authored cast) or no
   courier at all. Is one far-too-fast fixture the right amount of whimsy, and is the seam the
   right shape to generalize if more fixtures ever want it?
3. **One-off set-piece art reuses packaged props** (PQ-143.02): the tug is `place_dead_hulk`, the
   shrine is `place_memorial_array` (in Ceres, so the art is unique per sector), the tanker is the
   freighter-bow hero. The alternative was authoring new GLBs or fielding the held-back
   tug/tanker hulls (an owner call, deliberately not taken). Do the set pieces read as specific
   enough at the shipping camera, or do they read as recycled dressing?
4. **The plume corkscrew's numbers** (PQ-139.04): 4.5 WU card amplitude / 2 rad/s saturation /
   0.35 rad-per-WU screw for enemies; 6 WU / 2 rad/s for the player contrail; 0.35 reduce-motion
   scale. All test-pinned but all chosen by eye against the shipping camera. Too much? Too little?
5. **The bars-as-checks polarity** (PQ-186.00): B11 pins "exactly one owed clause may be unmet"
   rather than pinning the owed clause red; B10's PQ-138.03 rider is asserted under its own claim,
   not B10's sentence. Both were review findings implemented as stated — confirm the shape is
   what the contract intended.
6. **PQ-144.00's budget table lives in PERF_BUDGET §8** with per-§13C-packet witness guards
   rather than as thresholds in code. Confirm prose-with-guards is the right home versus a checked
   artifact.

## How agents get this wrong

- Treating this packet as a rubber stamp: the point is a senior pass over provisional decisions,
  and "ratified" must mean looked-at, not seen-in-passing.
- Re-litigating what the law already settles (fixed seeds, golden law, default route): the taste
  calls above are the surface; the law is not up for review.
- Fixing findings silently: every change this packet makes gets its own receipt line, like any
  other unit.

## Entry conditions

- [ ] All five receipts above read end to end before the first judgment.
- [ ] `git log --grep "PQ-144.00\|PQ-186.00\|PQ-139.04\|PQ-143.02\|PQ-184.00"` walked; each
  unit's integratedCommit matches its receipt.
- [ ] `npm run check:baseline` green at the candidate base (the known pre-existing reds are
  recorded in the receipts; anything NEW is a finding).

## Non-goals

No re-implementation for its own sake; no new content; no re-shooting golden evidence that the
law protects (reference PNGs, provenance, floors) unless a finding demands it with a causal
record.

## Work breakdown

### Phase 0 — characterization

- [ ] Read the five receipts and the units' commits; list every taste call the batch made
  (the six above are the known ones — add any found in the diffs).

### Phase 1 — judgment

- [ ] For each taste call: ratified / adjusted / overturned, with one sentence of why in owner
  words.

### Phase 2 — correction

- [ ] Implement each adjustment/overturn as its own focused change with its own verification;
  bump the touched unit's receipt with a "senior review" line.

### Phase 3 — closure

- [ ] Write the review report; set this unit's state through the integrator path.

## Performance and quality budget

No new per-tick allocation in the sim; no golden re-shot without a causal record; no default
quality reduced. Corrections inherit each touched unit's own budget section.

## Verification budget

`npm run check:baseline` before and after; the touched units' own packet checks; the debt table
(`check:ui:budgets`) read, not just run. `node scripts/check-program-docs.mjs` after touching any
packet or the queue.

## Review questions

- Do the five units hold up as law-compliant work a senior would sign?
- Which taste call would the owner, playing the game, notice and disagree with?
- Is anything in the batch quietly load-bearing on a stale row or a dead writer's assumption?

## Stop conditions

- A required owner seam is missing → record the finding, finish the rest, `BLOCKED` with the
  exact shared-change request.
- Two failed repair cycles with the same causal model → record, narrow, escalate in plain words.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Every taste call has a ratified/adjusted/overturned verdict with a reason.
- [ ] Report in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-191-REPORT.md`;
  queue unit state updated by the integrator.
