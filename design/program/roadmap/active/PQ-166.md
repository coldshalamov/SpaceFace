<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-166 — Localization at launch: five languages and the growth test

```yaml
queueId: PQ-166
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-03
```

## Outcome

Localization is ahead of a typical indie already: a real runtime, a generated catalog and a pseudo-locale in CI. This packet ships it: five languages (EN, FR, DE, ES, PT-BR or per market data), a language setting, font fallback for the display face, and a +40 % growth pass that clips nothing.

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **E · First hour, UX, platform**. Gap it closes: Locale only via URL; default route English-only by assertion; no language setting; growth unverified on the station. Reference: Standard Steam launch matrix. Routes through existing sources: §11.11 #9.

## What exists (audited 2026-09-03)

`src/localization/gameLocalization.js`, pseudo-locale, placeholder-parity tests, `test/localization-reachability.test.mjs`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | Language setting and default-route bridge. Settings language picker; the DOM bridge installs for any locale; the reachability test flips to 'default stays English unless chosen'. | Switching language live re-renders every screen. |
| `.01` | Growth pass. Pseudo-locale at +40 % on every screen and the HUD; fix clipping by layout. | Zero clipped strings in the capture sweep. |
| `.02` | Five languages. Translation pipeline (machine first, human review for the bark corpus and store copy); fonts. | Five catalogs ship; store page in five languages. |

## Entry conditions

- [ ] `npm run check:baseline` green at the candidate base; dependencies integrated as listed in the queue row.
- [ ] Owner modules re-read; unknown owner contracts are blockers, not guesses.

## Non-goals

No VO localization at launch; captions only.

## Checks and evidence

`npm run check:baseline`, `npm run check:player-facing-labels`; one normal-speed capture at the shipping camera per player-felt leaf; receipts under design/program/roadmap/receipts/PQ-166-*.
