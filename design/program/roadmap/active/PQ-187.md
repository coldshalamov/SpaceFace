<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-187 — The direction lock: reference board, three rendered directions, the owner's pick, the kit, the proof

```yaml
queueId: PQ-187
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-05 (design/FRONTEND_DIRECTION.md admitted)
```

## Outcome

The frontend has a direction the owner chose from real, rendered options — not one an agent wrote
down. Three materially different directions are built as real HTML at real size on the same three
screens; the owner picks; the picked frames become the authority; a kit (type scale to hero size,
faces, colour-as-mood, transition system, UI sound palette, components) is built from them and
rendered on one page; and at the end of the program a blind side-by-side against the best games'
actual screens proves the result.

## Source

[`design/FRONTEND_DIRECTION.md`](../../../FRONTEND_DIRECTION.md) (owner ruling 2026-09-05) →
`CANONICAL_BUILD_MAP.md` §18 direction override. Gap it closes: every frontend authority in the
repo was written by agents and prescribes a neutral dashboard; the owner has only ever been asked to
reject, never to choose. Every surface packet (`PQ-162`, `PQ-168`, `PQ-181`, `PQ-182`, `PQ-185`,
`PQ-188`) depends on `.03` of this packet, so no surface work can be dispatched before the lock.

> **Delegated creative authority, 2026-09-06.** The user explicitly delegated complete control of
> creativity and taste, including approve/reject decisions, to the controller for this packet. This
> authorizes controller-curated boards and frame decisions; it does not supply owner quotes or
> preferences.

## What exists (audited 2026-09-05)

`_uilab.html` + `scripts/probe-frontend-snapshot.mjs` (component lab with headless capture);
`src/ui/shipPreviewMount.js` (the hull rendered in a hangar rig — the picture nobody uses);
`src/ui/station/icons.js` (24 px stroke icon set + 14 faction crests); vendored faces in
`styles/fonts/` including `bricolage-grotesque-600`, `instrument-sans-var`, `spline-sans-mono-500`
(unused), and the current Plex pair; `audio:cue` bus with a cue vocabulary and zero recorded
samples; `scripts/capture-ui-matrix.mjs` (three widths, four modes).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **The reference board.** `design/frontend/direction/REFERENCE_BOARD.md`: the ten games in the direction file §6 with one representative frame each (linked or described, never reproduced beyond fair use), controller-curated notes under the 2026-09-06 delegation, and any game the user adds. | The ten-game board and delegation record are present, no owner words are invented, and the two comps most consistent with the controller's curation are named. |
| `.01` | **Three directions, rendered.** Directions A, B, C from the direction file §5 (or the owner's replacements) built in `_uilab.html` as real HTML with real data for the title, the station market and THE SHIP, at 1920×1080 and 1280×720, each with one dense pane, one empty state, one focus state, reduced-motion noted, and a ten-second title→market transition capture with placeholder sound. Candidate faces vendored under OFL. | Nine frames per direction under `design/frontend/direction/comps/<A|B|C>/`; a contact sheet the owner can open; the three differ on face, scale, temperature, composition and imagery — the integrator rejects tints of one layout. |
| `.02` | **The direction lock.** The controller reviews the actual candidate frames under the recorded delegation and selects or rejects a direction; the picked frames copied to `design/frontend/direction/approved/`; `DIRECTION_SHEET.md` written *from the frames* (faces, scale, temperature, composition rules, imagery rules, motion and sound character, what it never does); grammar §3/§4 marked replaced. "None of these" restarts `.01` with new candidates. | `approved/` exists with ≥ 9 frames; the sheet cites frames by filename and records the controller decision without attributing owner-picked words; `PQ-187.03` becomes ready. |
| `.03` (after `.02`) | **The kit.** Tokens and the type scale to hero size; the display, text and numeral faces; colour-as-mood rules (docked, wanted, Crucible, works); the transition system (cut-like, ≤ 180 ms, state-bound, reduced-motion authored); the UI sound palette (authored or synthesised samples for open/close/confirm/deny/dock/undock/lock/alert, routed through `audio:cue`); every component rendered on one kit page in the lab and captured. Old stylesheets are not consolidated; screens migrate in later packets and delete their old CSS as they move. | The controller reviews the actual kit-page captures under the recorded delegation; the kit page is captured at three widths; `PQ-181`, `PQ-162`, `PQ-168`, `PQ-182`, `PQ-185`, `PQ-188` unblocked. |
| `.04` (after every surface packet) | **The proof.** Blind side-by-side per the direction file §11 with a memoryless vision reviewer; the owner's thirteen signature-moment sign-offs collected; the regression baseline reshot on the new look; a ninety-second reel cut to the UI sound. | SpaceFace chosen in ≥ 50 % of pairings and 100 % against the genre baselines; thirteen sign-offs on file; matrix and regression green; the reel committed under `design/program/roadmap/receipts/`. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line.

- Writing a direction sheet before the owner has picked, or from prose rather than from the frames.
- Building the comps outside the game's lab, or as images, or without real data.
- Three directions that share a layout and differ by tint.
- Reaching for glow, gradients, glass, tracked caps or a cockpit to make a comp "bold".
- Citing `INSTRUMENT_GRAMMAR.md` §3/§4, a per-screen spec or a review as an aesthetic constraint.
- Consolidating or "cleaning" the old stylesheets under this packet.
- Treating a green matrix as the owner's yes.

## Entry conditions

- [ ] Dependencies integrated at the candidate base: none for `.00`–`.02`; `.02` for `.03`; every surface packet for `.04`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] The owner's reference-board answer (direction file §12) recorded before `.01` starts; if absent, build `.01` against the direction file §5 as written and say so in the receipt.

## Work breakdown / phased implementation

1. `.00`: curate the ten-game board, attach representative sources, and record the delegated
   controller authority without inventing owner reactions.
2. `.01`: render materially different A/B/C candidates in the lab with the same real data, then
   assemble the contact sheet.
3. `.02`: review the actual frames, select or reject under the delegation, and write the sheet from
   the selected filenames. `.03`: build and review the kit page from those frames.
4. `.04`: after dependent surfaces land, run the blind comparison, signature moments, regression
   reshoot, and reel as one bounded closeout.

## Non-goals

- Superseded grammar prose, an agent-written direction, or a tint-only variation does not choose the
  look.
- This packet does not consolidate old stylesheets, migrate dependent screens, or add game mechanics.
- Do not fabricate owner quotes, owner preferences, human verdicts, or missing frame evidence.

## Performance

Reuse `_uilab.html` and the existing capture scripts. Bound candidate DOM, font, image, and audio
artifacts to the lab and kit pages; do not add per-frame production work or reduce authored quality
to meet a budget.

## Verification budget

- For this documentation change, run `node scripts/check-program-docs.mjs` once and `git diff --check`.
- For `.01`–`.03`, take one fresh declared-width capture set per candidate/phase and review the actual
  frames; use existing lab checks only where the phase claim needs them.
- For `.04`, use one blind comparison/reel/sign-off pass after the surfaces land. Keep unsupported
  route or human claims explicitly unproven instead of repeating unchanged evidence.

## Review questions

- Does `.00` contain ten representative sources and controller notes under the delegation, without
  invented owner words?
- Are A/B/C real HTML candidates with the same data and material differences in face, scale,
  temperature, composition, and imagery?
- Does `.02` cite actual approved frame filenames, and does `.03` preserve the selected direction with
  state-bound motion and reduced-motion authoring?
- Does `.04` compare actual frames and state exactly which claims remain unproven?

## Stop conditions

- Stop a candidate set when a direction is only a tint/layout variant, is not real HTML with real data,
  or has no actual frames to review.
- Stop the lock or kit when the decision would rely on prose, stale frames, or invented owner words;
  record the controller decision under the delegation and retain the unresolved claim.
- Stop proof when dependent surfaces or required floor checks are absent, or when performance is met
  by cutting authored quality; record the narrow follow-up.

## Checkoff

- [x] `.00` ten-game board, delegation record, and two curated comps are present.
- [ ] `.01` has nine frames per direction and a contact sheet showing three real directions.
- [ ] `.02` has reviewed actual approved frames and a frame-derived sheet with no owner-picked words
      attributed.
- [ ] `.03` has the three-width kit captures and an actual controller review.
- [ ] `.04` has the blind proof, signature moments, regression reshoot, and reel, or is honestly
      marked unproven with a concrete resume point.
- [ ] Lifecycle and acceptance labels are promoted only from the evidence that exists.
