<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-206 — Combat variety residuals

```yaml
queueId: PQ-206
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: master at or after 2026-09-18; combat-variety vertical landed
stableRoute: COMBAT-VARIETY-RESIDUALS
```

## Outcome

The six maneuver identities stay distinct, and the honest gaps the vertical named are closed: the
~12 s boss 2v2 resolution is explained and either authored or fixed, the swarm's one-pass lifespan
carries real combat value, the degenerate flee cells are bounded, B13 has a headed jitter number,
and the two pre-existing combat-ecology-roles failures are repaired at the source.

## Source

[`../receipts/combat-audit/2026-09-18-combat-variety-vertical.md`](../receipts/combat-audit/2026-09-18-combat-variety-vertical.md).

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | A capital wing fight is a fight, not a 12-second mystery | The non-DPS kill source is found, decided authored-vs-bug, and pinned on a fixed seed |
| `.01` **DONE** | The swarm threatens on its one pass | Five fixed seeds: 2–3 hits and 6.59–9.88 damage against the armed, moving starter; full 30-tick warning, one strike, unchanged hull/armor/shields |
| `.02` | A wing that cannot finish does not kite 10k WU | Degenerate flee/egress cells are bounded while authored flee survives |
| `.03` | Jitter and roles are measured, not asserted | B13 headed jitter measured; ecology-roles pin repaired without re-recording a golden |

## How agents get this wrong

Calling the 12 s capital death "pilot DPS" without tracing it. Adding HP until the swarm lands hits.
Deleting flee behaviour instead of bounding the degenerate case. Fixing an ecology pin by editing the
assertion rather than the source data.

## Write set

`src/ai/`, `src/ai/combatDoctrine.js`, `src/systems/tacticalAI.js`, `src/combat/runtime.js`,
`src/data/missions.js`, `src/data/enemies.js`, `test/combat-ecology-roles.test.mjs`.

## Non-goals

Making the two same-identity roster mates (wasp==zealot, lancer==ghost) differ — they share an
identity by design.

## Closing

Run the 2v2 cells and the swarm cell on the fixed seeds. If the different-fight bar holds and the
named gaps are closed, stop.

## PQ-206.01 completed 2026-09-20

The cue used to begin after the useful approach window and steer the fixed gun away from its
target. It now starts at 340 WU with target-facing intent for all 30 warning ticks. The existing
44-tick strike delivers a short 8-round/s pulse burst at 6 damage per round; health, shields,
armor, momentum and recovery are unchanged. The NPC firing adapter also used an obsolete
relative-frame lead direction with aim-true projectiles: it now uses the weapon owner's solver
on the default route. The optional momentum-inheritance path keeps its existing behavior.

Real Crucible 1v1, seeds 4242/8008/13502: hits **0/0/1 → 3/2/3**, damage
**0/0/1.65 → 9.88/6.59/9.88**, first fire **2.38 → 1.78 s**; kills remain
**4.40/6.53/4.40 s**. Held-out seeds 47/90210 each land 3 hits for 9.88 damage and die at
4.40 s. The real Swarm opener (4242, energy baseline, 30 seconds) survives with 3 kills,
52.704 damage taken and 99.85% hostile-in-frame; this is a bounded opening check, not a full-run
balance claim. StationServices/difficultyDirector were missing from the Node production factory
table; both real factories are registered so the existing bench boots without test-only injection.

Verification: 55 focused tests, independent code review, and the live New Game/save/Continue check
(16/16). Baseline 14/16: both old 47-A golden hashes still fail; loading all four pre-change source
modules through an in-memory loader reproduces the exact same hashes (V3 `5c84097c…`, legacy
`e2d355ca…`). Save/reload comparison checks pass. No golden was changed. Baseline wall time also
exceeded its budget under the concurrent workload; no performance improvement is claimed.
