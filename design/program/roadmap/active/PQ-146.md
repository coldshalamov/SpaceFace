<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-146 — Stunt grammar and the trick economy

```yaml
queueId: PQ-146
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

The game names what the player just did. A rope release already grades itself razor/clean/good/messy; the same receipt stream can recognise a bolas, a wrecking-ball kill, a clothesline, a collateral chain, a near-miss, a tow-kill and a rock discovery. A combo meter scores them in the Crucible, the ledger and titles record them in adventure, and a moment detector fires the slow-mo and the clip. This is the missing DEMAND for physics play: nothing in the build today asks the player to compose a chain. Per the Crucible-first law, the scoring lands in the Crucible bench before adventure inherits the ledger and titles.

## Source

`CANONICAL_BUILD_MAP.md` §15 (admitted 2026-09-03) → **A · The toys**, wave **ALPHA**. Gap it closes: Tony Hawk, Rocket League and Bulletstorm made a physics toy legible by naming the tricks; the audit found every link of the 60-second chain exists and nothing rewards composing them. Reference: Tony Hawk's Pro Skater, Rocket League, Bulletstorm skillshots, Just Cause. Routes through existing sources: GDX-A04 combat causality; §12 Crucible scoring.

## What exists (audited 2026-09-03)

`tether:releaseRated` (razor/clean/good/messy) in `src/systems/tetherGameplay.js`; `combat:collisionConsequence` receipts with exchangedMomentum/deltaV; `massline:throw`, `tether:whipImpact`, `combat:collisionDebris`; `src/systems/titles.js`, `shipLedger.js` (240 entries), `survivalRecords.js`, `bulletTime.js`; `src/combat/impulseKernel.js` provenance history per entity.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Trick taxonomy and detectors.** One pure module turns the receipt stream into named tricks with a cause chain (who threw, what hit, what it hit next): bolas, wrecking ball, clothesline, collateral ×N, near-miss, tow-kill, rock discovery, well golf, dead man's mass, razor release. | ≥ 12 tricks detected deterministically in scenarios; false-positive rate < 5 % on ordinary flight tapes. |
| `.01` | **Combo meter and scoring in the Crucible.** Chain window, multiplier by trick rarity and mass ratio, banked on quiet; scoring feeds survivalRecords and the results screen. | A physics run outscores a gun run of equal kills by ≥ 2×; the free Pulse cannot top the board. |
| `.02` | **Titles and ledger entries in adventure.** Tricks write ledger lines and earn titles NPCs use in barks; the rap sheet cites them. | A witnessed bolas kill produces a ledger line and a bark within one session; save round-trips. |
| `.03` (after PQ-139.00) | **The moment detector.** A rated 'holy shit' event (rarity × momentum × collateral) exposed on the bus for slow-mo (bulletTime), camera, audio stingers and the clip recorder. | Fires on ≥ 3 distinct moments in the 60-second proof scenario and never on ordinary traffic. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Detecting tricks from what the player pressed instead of what the physics produced: receipts only.
- Popping score text in adventure flight: the HUD attention pass stands; tricks go to the ledger and titles.
- Rewarding a trick nobody saw: a trick without a consequence chain is not a trick.

## Entry conditions

- [ ] Dependencies integrated at the candidate base: `PQ-137`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario on a fixed seed before tuning (the bar this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, the fun-loop bench, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Trick taxonomy and detectors.
- [ ] `.01` Combo meter and scoring in the Crucible.

### Phase 2 — owner wiring

- [ ] `.02` Titles and ledger entries in adventure.

### Phase 3 — presentation and route

- [ ] `.03` The moment detector.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `src/combat/`
- `src/systems/titles.js`
- `src/systems/shipLedger.js`
- `src/systems/survivalRun.js`
- `src/systems/survivalResults.js`
- `src/systems/bulletTime.js`
- `src/render/feel.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No score popups in adventure flight (the HUD attention pass stands); no trick that requires a new input.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; save bytes bounded; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:combat`, `npm run check:crucible:route`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

- Which gate row (§15.1, §16.1, §17.1, §18.1) did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden, on the default route?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, give an NPC a hidden gyro, scale hit points, or add a dialogue tree?
- Does the report read in the owner's words (§1.4)?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the gate row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-146-<leaf>-REPORT.md`; queue unit state updated by the integrator.
