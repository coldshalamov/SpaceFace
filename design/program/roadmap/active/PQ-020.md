<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-020 — Ceres activity-pocket topology

```yaml
queueId: PQ-020
lifecycle: integrated
acceptance: focused_green
packetRevision: 2
owner: unclaimed
integratedCommit: f98367c1
receipt: ../receipts/PQ-020-ceres-topology-REPORT.md
routeHarnessRepairReceipt: ../receipts/PQ-020-route-harness-repair-REPORT.md
remainingPhases: missing H1 route capture, H2 Ceres/Cathedral verdict, H3 matched performance, promotion with relocated PQ-018 Phase 4
```

## Outcome

Ceres reads as a navigable industrial place rather than a loose pile of points: a civic/refinery
pocket, a production/mining pocket with one bounded existing-owner physical condition, a visible
transit/weigh checkpoint between the Helios and Tethys approaches, and an off-lane Cathedral
graveyard. Existing natural jobs and offscreen embodiment respond to industrial metadata, while map,
route, and physical positions remain exactly consistent.

The implementation is intentionally a small data/topology slice. It does not rewrite routing, traffic, the galaxy map, or World Site ownership to force the desired composition.

The deterministic implementation and route-HARNESS repair are integrated. Current work is
acceptance-only and is machine-routed as `PQ-020.ceres-h1-capture`, `PQ-020.h2-verdict`,
`PQ-020.h3-performance`, and `PQ-020.promote`.

The first H1 continuation candidate (`5f086114`, candidate digest
`f9dd349c2de42aa0f05f2d6a2603a625926aeb24acc00f765d33b09f75a25f52`) consumed one Browser
claim and cleared the public Helios→Ceres jump plus refinery, outpost, and beacon before fingerprint
`2b94b84d87aeba2a5c550a91d47e2d3337b344d0b0a6a66cb79ec541e89b42d9`. The failure
snapshot proved the Cathedral was authored/admitted but its root projected at `y=3.19` when the old
`1100–1600 WU` far band expired. That band is geometrically outside the fixed-heading chase camera's
ground footprint, not a product travel failure. The repaired route completes natural autopilot
arrival first, then drives the shipped `+`/`-` camera controls to declared zooms `112/72/64`.
Seconds-scale static coverage rejects the impossible distance band and direct camera/state/event
mutation.

The next materially changed candidate (`928da89a`, candidate digest
`904cbb7349cbfebbc3800193d9a4273f9415e75dbb1dfff2a90415aa0daf9777`) cleared the public
jump, all four pockets, Cathedral admission, natural arrival, all three public-control framings, F5,
and the cold Continue click before fingerprint
`984e7825e53a16f3961b00fd08ec701bee87f99b6346a0d32ae9594be1b9f662`. Its
`16-continue-restored.png` is the production `LOADING CRITICAL FLIGHT ASSETS` blackout: the
entity-only restore predicate returned while `state.mode === 'loading'`, so the single `KeyN` press
was correctly ignored. Flight became ready during the subsequent 30-second map locator wait, which
explains why the later failure snapshot said `flight` without making the earlier input valid. The
repaired predicate retains seed/entity/pose checks and additionally requires the production-owned
flight mode, empty screen stack, released modal class, and hidden/non-busy loading overlay before
capture or input. Failure snapshots now expose the same readiness surfaces.

Candidate `47f2707d` then passed the complete Browser route under broker digest
`ac693a31e60104deff17a55cf6024d92cb8e1859a99b74a15a7bd7a29a4a6f90` on real Intel ANGLE
D3D11. The distinct Electron attempt completed all 21 declared frames, matched every normalized
Browser gameplay fact, and closed its owned runtime, but its receipt was already red before parity:
the intentional cold reload exposed one stale `uiRoot.registerScreens()` promise after screen-manager
teardown and seven module requests cancelled with `net::ERR_ABORTED`. This is a mixed product/harness
lifecycle defect, not a gameplay-route mismatch. Exact unit
`PQ-020.electron-cold-reload-lifecycle-repair` now owns `uiRoot` registration invalidation and a
bracketed expected-navigation window that may ignore only aborted requests while retaining console,
page, HTTP, and unrelated request failures. The capture unit and headed mutexes remain released until
that seconds-scale repair closes.

The exact shared repair is now focused-green. `uiRoot` owns a monotonic registration generation:
destroy/re-init invalidates every unresolved dynamic screen import before it can touch a stale or
replacement screen manager. The page-issue collector exposes an explicit expected-navigation token,
and the PQ-020 route holds it only around `page.reload()`. The first repaired-source Browser
recertification passed at digest
`066ba0a50b12ce3357f6bc010e7c14092c5d63085e6ad326d48f137cc38c045c`; Electron completed all
21 frames and matched every gameplay fact, but delivered eight old-document abort events just after
the awaited reload resolved. The collector had classified by failure-event time, making those late
events false hard failures. It now tags requests already in flight when expected navigation begins
and requests whose start is observed during the awaited reload, retaining that request-bound
attribution through delayed failure delivery. Post-reload, untagged, completed, non-abort, console,
page, and HTTP failures remain hard.
The first identity-bound candidate (`e5c4ecf4…`) proved one more ordering: Electron can observe a
request start during the awaited reload and deliver its abort only after the token closes. Browser
again passed 21/21 frames with zero issues; Electron again completed 21/21 frames, matched all
gameplay facts, and closed its runtime, with only eight differently ordered module aborts keeping the
receipt red. Attribution now includes pre-existing requests and request starts during the exact
awaited reload call, while starts after it remain hard. Because the harness changed again, the H1
capture requires one fresh Browser claim and one distinct Electron parity attempt. Candidate
`113c3563…` then proved the remaining gap was attachment: Browser again passed 21/21 with zero
issues; Electron again completed 21/21, matched gameplay facts, and closed its runtime, but its eight
aborts were entirely untagged because collection attached after `domcontentloaded`. Electron now
attaches immediately after `firstWindow()` and backfills Playwright request history. Response
headers do not retire a request; only `requestfinished` or an already-recorded failure does.
Combined lifecycle/issue/manifest coverage passes 19/19, UI imports pass 41/41, and PQ-020 proofs
pass 14/14. Candidate `97c8c822…` then falsified response-header retirement as the remaining cause:
Browser again passed 21/21 with zero issues; Electron again completed 21/21, matched facts, and
closed cleanly, with eight varying module aborts still untagged. The remaining boundary is request
wrapper identity. Attribution now falls back to method/resource/URL/start-time fingerprint, consumed
once; a same-URL request with a different start time stays hard. Focused coverage is 20/20.

`PQ-020.ceres-h1-capture` remains claimed. It holds only `browser-gpu` and
`validation-broker` plus the declared PQ-020 route/evidence/packet paths. The Browser cell must run
through the registered one-use manifest; Electron remains a distinct attempt gated on Browser PASS.

## Entry conditions

- [x] PQ-018 supplies the integrated Cathedral site ID, approved local/global placement, clearance envelope, map/place contract, and save identity.
- [x] Re-read current Ceres sector, anchor, zone, authored-place, Atlas index, galaxy/system map, traffic role mix, offscreen embodiment, and route owners.
- [x] Atlas/data/place lease was free for the bounded data lane; no renderer, asset, or GPU source was edited.
- [x] Confirm the candidate coordinates below against current sector geometry, direct-neighbor routes, traffic, asteroids, fields, and world radius. Perceptual label fit remains a headed route claim.
- [ ] Record a matched Ceres baseline: map layout, route legs, natural-job census, offscreen role mix/digest, sector-entry/map-open cost, and current entity/query/draw/residency metrics.
- [ ] Treat the Cathedral verification folded from PQ-018 as part of that same matched Ceres
      baseline: the route MUST include `world_site_wreck_cathedral` and capture Cathedral-specific
      route performance, close/default/far/motion/LOD evidence, and structural cost. This is
      relocated PQ-018 verification, not an optional or standalone phase.

## Candidate topology

All authored positions are sector-local. Current candidate values from the prior coordinate audit:

| Pocket/item | Local XZ | Purpose |
|---|---:|---|
| Civic | existing `station_ceres` at `(-1100, 620)` | refinery, services, law/civic identity |
| Production zone | `(500, -700)`, radius `850` | Belt Outpost, dense rocks, driller, miners/haulers |
| Transit zone | `(3155, -955)`, radius `500` | midpoint landmark between Helios/Tethys approaches; no new presence budget |
| Transit beacon | `(3040, -920)` | physical/map-selectable weigh beacon |
| Cathedral reservation | integrated `(300, 2700)`, radius `620` | immutable PQ-018 off-lane graveyard reservation |

The audit accepted the civic, production, transit, and beacon values, but rejected the stale proposed
Cathedral location `(-720, -2120)`: its conservative reservation violates the live
Helios Prime gate lane by `387.691 WU`. PQ-018's integrated `(300, 2700)` placement remains
authoritative and clears the same audit by `76.632 WU`. Exact data/map/physical equality and
geometric route legs are deterministic evidence; perceptual label fit and playable travel time remain
headed route claims. Preserve existing station/gate/field/POI IDs and positions unless a separately
evidenced defect requires change.

## Live seams

| Domain | Owner | Reuse/contract |
|---|---|---|
| Sector identity/industries | current `src/data/sectors.js` structure | add industrial metadata consumed by live traffic and offscreen embodiment |
| Canonical coordinates | `src/data/sectorAnchors.js` and sector-coordinate owner | author `poi.pos` once; derive compatibility aliases rather than duplicate coordinate literals |
| Zones | `src/data/sectorZones.js` + authored place append path | recenter/resize production; add no-presence transit checkpoint |
| Place identity | current `src/data/authoredPlaces.js`/place runtime | one physical beacon using an existing admitted lane-beacon identity |
| Atlas/maps | current Atlas index and map consumers | exact global/local/map/course/physical equality; no parallel map data |
| Traffic/jobs | current role-mix/job owners | metadata only; no new traffic controller, destination type, or job injection |
| Offscreen embodiment | current sector projection/digest owner | characterize deterministic role-bias and save/idempotence; no visible-ship claim |
| Cathedral | PQ-018 integrated World Site owner | consume exact placement; do not duplicate site state |
| Local mechanical condition | current zone/field/environment owner | reuse one bounded condition that changes how the production pocket is flown or read; no new system |

## Exact data lane

After live re-audit, the expected implementation remains five paths:

1. `src/data/sectors.js`
   - add Ceres industrial metadata such as `{ mining: true, refinery: true }` using current schema;
   - add one `poi_ceres_throughline` beacon declaration;
   - no unrelated Ceres edits.
2. `src/data/sectorAnchors.js`
   - add one local beacon anchor and existing lane-beacon asset identity;
   - keep `pos` canonical;
   - derive the map consumer's compatibility alias from the same object (`position === pos`) if the live consumer still requires it;
   - do not copy coordinates into another map owner.
3. `src/data/sectorZones.js`
   - recenter/tighten only the production belt if the route audit accepts the candidate;
   - encode one existing-owner local mechanical condition here only if the live schema already
     supports it; otherwise stop and create a narrow owner packet rather than accepting labels alone.
4. `src/data/authoredPlaces.js`
   - append one no-presence transit/checkpoint zone with exact local center/radius and DMC identity;
   - no RNG, global coordinate, discovery store, or new registry slot.
5. `test/pq020-ceres-topology.test.mjs`
   - exact IDs/math/round trips/consumer equality/non-drift;
   - live and offscreen metadata effects proven separately;
   - route destinations remain stations where required; beacon does not invent freight behavior.

If live code has moved, preserve these responsibilities but update paths in the claim receipt. Any need to edit galaxy-map, Atlas-index, traffic, embodiment, renderer, save-schema, or registry code is a stop/shared-owner request, not silent scope growth.

## Work breakdown

### Phase 0 — coordinate and consumer audit

- [x] Enumerate current Ceres stations/gates/fields/POIs/zones and local/global transforms.
- [x] Trace the Atlas, system-map, selected-course, and physical POI consumers and prove `pos` is canonical.
- [ ] Measure candidate pocket separation, world boundary, asteroid/field/site clearance, label auto-fit, and leg times.
- [ ] Record generic Helios↔Tethys routing and a deliberately selected through-Ceres itinerary. Do not claim the generic router uses Ceres if a direct edge bypasses it.
- [x] Confirm the exact Cathedral placement/clearance receipt.

### Phase 1 — five-path data/test change

- [x] Apply only the accepted metadata, beacon, zone, alias, and focused test.
- [x] Bind and prove one bounded local physical condition through an existing owner, or return a
  focused follow-up blocker before visual acceptance.
- [x] Keep transit zone free of presence/spawn budget unless a separate gameplay requirement is admitted.
- [x] Preserve existing station, gate, field, POI, and Cathedral identities and placements.

### Phase 2 — owner proof

- [x] Exact local↔global round trip, Atlas point, system-map point/draw position, selected course, physical beacon, and `place_lane_beacon` identity agree.
- [x] Natural live miner/hauler behavior is observed without job injection across held-out seeds.
- [x] The production pocket's local condition changes an observable player decision and remains
  deterministic, bounded, accessible, and owner-controlled.
- [x] Offscreen role-bias/digest change is deterministic and reported as projected intent, not visible traffic.
- [x] Save/Continue preserves projection audit/idempotence and materializes new static content once.
- [x] Generic and through-Ceres route behavior reported honestly.

### Phase 3 — route, visual, accessibility, performance

- [ ] Enter Ceres, read civic/production labels, select beacon, travel refinery→Belt Outpost→beacon→Cathedral, approach from both endpoint-gate directions, save/Continue, and repeat key selections.
- [ ] Review whether pockets are perceptually distinct at map and flight scales, not merely mathematically separated.
- [ ] Keyboard, pointer, and applicable controller map selection remain usable; labels/inspector carry non-color semantics.
- [ ] Matched baseline/after route on target and available floor profile; include map-open, sector-entry, frame p95/p99/hitches, entities/queries, draw/program/residency.

## Non-goals

- A new sector router or forced Helios→Tethys path through Ceres.
- A traffic attraction system for the beacon.
- New jobs, producers, embodiment schema, save store, registry slot, map implementation, renderer
  path, Cathedral state, or a new condition system.
- Moving existing stations/gates/fields/POIs to make the screenshot prettier.
- Multiple checkpoint assets or a broad Ceres art overhaul.
- Calling projected offscreen intent visible traffic.

## Performance and quality budget

- One static beacon/place and one no-presence zone; no added ambient spawn budget.
- Industrial metadata may alter role composition but not the existing live/offscreen caps.
- No new per-frame scans or destination search; use existing indexed owners.
- One admitted existing beacon asset, shared/cached materials, ordinary culling.
- No new save object; static content refresh/materialization is idempotent.
- Map labels must remain readable without suppressing Cathedral/beacon/civic/production identity.
- Matched route must not regress target/floor p95, p99, hitch count, map-open, or sector-entry beyond the declared packet threshold; investigate any regression causally rather than lowering quality.

## Verification budget

```yaml
L0:
  - node --check for changed data/tests
  - git diff --check
L1:
  - node --test test/pq020-ceres-topology.test.mjs
L2:
  - npm run check:sector-geography
  - npm run check:atlas-integrity
  - npm run check:atlas-spatial-truth
  - npm run check:atlas-place-path
  - npm run check:map-frames
  - npm run check:m2b:sector-graph
  - npm run check:m2:sector-embodiment
  - relevant NPC-job and save checks
CathedralInlineCheckOutput:
  - npm run check:asset-reachability
  - npm run check:assets:live
  - npm run check:visual-stability
  - pq020-ceres-topology route output MUST identify world_site_wreck_cathedral and inline matched route p95/p99/hitches/admission/residency, close/default/far/motion/LOD, and entity/collider/query/draw/program structural-cost evidence; generic bundle, starter-route, or ship-only PASS output does not satisfy this row
L3BrokerManifest: pq020-ceres-topology
acceptanceAttemptsPerCellPerCandidateDigest: 1
reviewClosure: discovery -> repair -> causal re-review when repairs affect the claim
```

The three named checks are supporting gates. Their bare current output does not establish Cathedral
route, visual-state, LOD, or structural-cost acceptance; the Cathedral-keyed inline
`pq020-ceres-topology` route row is mandatory.

Only run checks that still exist and own the traced seams; update the packet if names have moved. A map/route live failure must first become a focused topology/consumer regression.

## Review questions

- Can a new player distinguish civic, production, transit, and graveyard without reading hidden coordinates?
- Is the beacon a landmark, not a false promise of guaranteed traffic or a forced route?
- Are all coordinate consumers derived from one authored local value?
- Do live jobs and offscreen embodiment remain separate, truthful claims?
- Is Cathedral placement off-lane, reachable, and collision-free without collapsing labels or travel times?
- Does the change improve route texture without adding ambient cost or map clutter?

## Stop conditions

- PQ-018 placement/identity unavailable or clearance fails.
- Atlas/data lease occupied.
- Coordinate compatibility requires duplicated authored literals or a speculative map rewrite.
- Existing IDs/anchors drift.
- Natural-job proof depends on injected jobs or one magic seed.
- Generic route is falsely described as through Ceres.
- Static content duplicates after Continue.
- Labels/courses/physical positions disagree.
- Performance can pass only by hiding content or lowering quality.

## Checkoff and receipt

- [x] Entry audit and Cathedral receipt complete. *(coordinate/consumer audit done; integrated
      Cathedral placement receipt confirmed; matched-baseline headless rows recorded, headed rows
      remain in the row below)*
- [x] Diff stays in the five-path data lane plus the requested focused check/harness and baseline link; no map, route, traffic, embodiment, renderer, save, registry, or World Site owner code changed.
- [x] Coordinate/Atlas/map/physical equality green.
- [x] Natural jobs and offscreen projection green and reported separately. *(3/3 held-out seeds with
      sector:enter causation gating, no injection; offscreen digests deterministic and
      metadata-sensitive, reported as projected intent; receipt
      [`PQ-020-ceres-topology-REPORT.md`](../receipts/PQ-020-ceres-topology-REPORT.md))*
- [x] One local mechanical condition is perceptible and behaviorally proved through its existing
      owner. *(the authored `dense_asteroid` hazard in the production pocket: 14.2x rock density,
      hazard:enter/exit -> hazardRead with [avoid|time|tether] counterplay; honest negatives in the
      receipt)*
- [x] Save/materialization/idempotence green. *(save -> Continue identity, single materialization,
      projection-audit caveat withdrawn to the sectorSim owner — see receipt)*
- [ ] Browser/Electron route, visual/accessibility, and matched performance accepted.
      *(the packet-less 300-WU endpoint assertion is removed and the retained 429.564-WU
      source-direction arrival is pinned in
      [`PQ-020-route-harness-repair-REPORT.md`](../receipts/PQ-020-route-harness-repair-REPORT.md);
      missing headed continuation remains unproven)*
- [ ] Independent discovery and causal re-review terminal.
- [x] Receipt linked; integrator owns global promotion. *(`../receipts/PQ-020-ceres-topology-REPORT.md`)*

### Deterministic implementation receipt — 2026-07-27

- `npm run check:pq020:ceres-topology` identifies
  `world_site_wreck_cathedral` and passes with route legs `2074.223 WU`,
  `2667.218 WU`, and `4637.893 WU` (`9379.334 WU` total). The four pocket
  reservations have `504.223 WU` minimum peer clearance and `403.631 WU`
  minimum world-boundary clearance.
- The pinned structural-cost digest is
  `b2232d1d891f6d65b2e4420387a23223e0325a0e14971d046bd86ef61ddafc2d`
  for `sector_ceres_belt:seed47:one-fixed-tick`: `124` entities (`90`
  asteroid, `12` fx, `2` ship, `6` station, `14` wreck), `105` collidable
  entities, `105` colliders, `0` focused-scope spatial queries/candidates,
  and `15` Cathedral World Site entities (`7` interaction and `7` collision
  proxies). Headless World Site residency is `FULL`; presentation admission
  is recorded as `headless`, not GPU admission.
- Industrial metadata changes the live traffic role weights from hauler
  `30 → 45` and miner `16 → 40`; the deterministic offscreen
  projection emits `3` intents with digest `2416862514`. These are owner
  outputs, not claims of visible traffic.
- `npm run check:asset-reachability`, `npm run check:sector-geography`,
  `npm run check:atlas-integrity`, `npm run check:atlas-spatial-truth`,
  `npm run check:atlas-place-path`, `npm run check:map-frames`,
  `npm run check:m2b:sector-graph`, `npm run check:m2:sector-embodiment`, and
  `npm run check:npc-jobs` are green for this candidate.
- Real route p95/p99/hitches, render admission, GPU residency,
  close/default/far/motion/applied-LOD states, and draw/program counts are
  `null` with `requiresHeaded: true` in the inline output. Therefore
  `npm run check:assets:live`, `npm run check:visual-stability`, the matched
  headed route, save/Continue, accessibility, and the independent human-eye
  art verdict remain open. This is relocated PQ-018 Phase 4 verification,
  not a pass claim.

## References

- [`../program-queue.json`](../program-queue.json)
- [`../../../world-identity/sectors/sector_ceres_belt.md`](../../../world-identity/sectors/sector_ceres_belt.md)
- [`../../../../docs/handoffs/chatgpt-portfolio-20260723/PQ020_CERES_ACTIVITY_POCKET_TOPOLOGY.md`](../../../../docs/handoffs/chatgpt-portfolio-20260723/PQ020_CERES_ACTIVITY_POCKET_TOPOLOGY.md) (historical coordinate/consumer audit)
- [`../../../PERF_BUDGET.md`](../../../PERF_BUDGET.md)
