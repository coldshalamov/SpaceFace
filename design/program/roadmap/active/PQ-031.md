<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-031 — Twin Bridle: the bolas and tether-two

```yaml
queueId: PQ-031
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-03
```

## Outcome

Fire a bridle that couples two bodies. Two light pirates orbit each other and lawn-dart into a rock; two haulers jackknife; a pirate hitched to a passing freighter loses its engines. The pair shares helm loss and inertia (PQ-137.09). This is toy T1 and T4 of §15.5.

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **A · The toys**. Gap it closes: Twin Bridle exists behind a flag with setup time and no gameplay arc; the most-requested verb in the vision ('tether them together so they yank each other') is unreachable in practice. Reference: Just Cause 2 tether, Bolo physics. Routes through existing sources: SPEC3-17; PQ-137.09 chains.

## What exists (audited 2026-09-03)

`TWIN_BRIDLE_HEAD_ID`, `TWIN_BRIDLE_SETUP_S = 10` in `src/systems/tetherGameplay.js`; `attachments.listControlledBy`; `masslineHeadTwinBridle` flag ON.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | Bolas throw in one input. Setup time drops to a throw: latch A, latch B, the bridle exists; setup ≤ 2 s at combat range. | Two lights bridled within 3 s of the first latch in a scenario; the pair tumbles (B11). |
| `.01` | Coupled-pair physics. Shared inertia, shared helm loss, bounded line load, break by load rating; the pair reads as one system in the camera. | Pair orbits and impacts terrain in ≥ 4 of 5 seeds. |
| `.02` | NPC counterplay. Aces cut bridles; heavies ignore them; the specialist carries a line-cutter. | Scenario shows one cut and one ignore. |

## Entry conditions

- [ ] `npm run check:baseline` green at the candidate base; dependencies integrated as listed in the queue row.
- [ ] Owner modules re-read; unknown owner contracts are blockers, not guesses.

## Non-goals

No bridling of stations or planets as a cheese; large bodies stay anchors only.

## Checks and evidence

`npm run check:baseline`, `npm run check:massline`; one normal-speed capture at the shipping camera per player-felt leaf; receipts under design/program/roadmap/receipts/PQ-031-*.
