<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-163 — The first ten minutes: the power fantasy, honest

```yaml
queueId: PQ-163
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

The onboarding rail already teaches thrust, brake, latch, winch, cut and dock in-world with a verb-then-silence rhythm; it never teaches boost, draw-to-fly or wells, and the first shove is impossible with the starter gun. This packet re-cuts the opening around the store-page sentence — light ships are ammunition, swing a rock, keep the speed — as a scripted-but-honest rescue: you are a tow rig, a scout is chasing a tug, you swing a rock into a derelict for scrap, fling the scout into an asteroid, grab the pod and run. Then the 47-A scene plays as the climax it already is.

## Source

`build_map.md` §15 (admitted 2026-09-03) → **E · First hour, UX, platform**, wave **ALPHA**. Gap it closes: Boost/draw-to-fly/wells untaught; starter gun cannot shove; the quality cliff after 47-A. Reference: Portal, Half-Life, Doom (2016) intro, Subnautica. Routes through existing sources: S2-03 first hour; M3-CAREERS/M3-NAV-HUD; GDX-S03/A28; PQ-137.05 starter force.

## What exists (audited 2026-09-03)

`src/onboarding/flightDrill.js`, `src/systems/onboarding.js`, `src/ui/hudAttention.js` hints, `src/ui/screens/range.js`, `scripts/check-first-15-runtime.mjs`, 47-A scenario.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` (after PQ-137.05) | **The rescue.** A tow-rig opening at the reference site with one swing-release, one shove, one grab-and-run, in play, no walls. | ≥ 80 % of unaided testers complete it in ≤ 10 min; telemetry funnel. |
| `.01` | **The Range is the door.** Onboarding points at the Range (F4) after the first latch; the SWING, DO NOT PULL rung is the first drill; the empty Power Rail slots explain themselves. | Funnel shows ≥ 60 % of new players open the Range in hour one. |
| `.02` | **Teach the missing three.** Boost, draw-to-fly and the well enter the rail in the first hour, each as a verb-then-silence beat with a Range fallback. | Funnel shows each used unprompted within the hour by ≥ 70 %. |
| `.03` | **The sentence, proven.** The store-page sentence is shown once and each clause is performed by the player before minute ten. | A stranger states the fantasy back correctly after playing. |
| `.04` | **The cliff.** Beat 1 after 47-A is a set piece (PQ-152), never an errand. | Session-2 retention in playtests ≥ 60 %. |

## How agents get this wrong

These are the ways a lazy first try fails this packet. Read them, then do the work as if the owner will play it tonight.

- Teaching with text: every verb is taught by doing with silence after; the Range is the fallback.
- A mentor with dialogue: the rescue is fiction, the teaching is play.
- Leaving boost, the stroke and the fields untaught because the rail 'works': the funnel proves each is used unprompted.

## Entry conditions

Glance at NOW.md so you don't collide. The Leaves table is the job. Dependencies are on the queue row.

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Look at the change on the default route. If a number is the done-when, print it from a named seed. If the player would see or hear it, look at it. A still is a way to look, not something to store.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` The rescue.
- [ ] `.01` The Range is the door.

### Phase 2 — owner wiring

- [ ] `.02` Teach the missing three.
- [ ] `.03` The sentence, proven.

### Phase 3 — presentation and route

- [ ] `.04` The cliff.
- Play it again after you change it. Fix what you would not ship.

## Write set

- `src/onboarding/`
- `src/systems/onboarding.js`
- `src/ui/hudAttention.js`
- `src/ui/screens/range.js`
- `src/data/scenarios/`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No mentor dialogue; no tutorial text longer than one line.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; save bytes bounded; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:first-15-runtime`, `npm run check:new-game-first-run`. Per leaf: look at the player-facing change; print the done-when number from a named seed when a number is the bar. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

Would you put this in front of the owner? What's unfinished, what's buggy, and what would make it better? If you're about to close, have another agent look at the actual work for those three things and fix what's real. Don't write a review file.

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

Would you ship this? Stay on the write set. Run the checks that would catch this change. A short note for the next engineer only if you learned something they couldn't see from the code.

## Appendix — a candidate sequence (audit 2026-09-05 §1.5; a candidate, never a script)

Integrate with the current opening, never a second one. The player first controls the actual starter
Hitch (`ship_kestrel` stays the internal id) somewhere with enough visible geometry to understand
movement; one nearby recoverable object offers a safe, useful first attachment — the goal is to take
an object somewhere and feel how the relation changes the ship, not to read a control chart. The first
complication admits at least two genuinely usable responses shown in geometry and behaviour (avoid,
negotiate through the existing interaction, or physically displace; cut across a route at a handling
cost or follow it), not two menu options with one outcome. The undertaking pays off visibly: hand over
a payload, see a local operation resume, receive an intelligible payment, try one meaningful
modification (a loaner or reversible early fitting can introduce a different Massline behaviour before
a large hull; the starter's Small utility slot against Medium heads is a real access constraint to
evaluate). Then an optional industrial opportunity with a near-term extraction choice and a
longer-term productive face — the player may keep flying. The opening succeeds when the player can
say what they did, why it worked, and one thing they want to try next; completion flags are supporting
evidence.
