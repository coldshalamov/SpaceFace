<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-193 — The 3D picture never looks broken

```yaml
queueId: PQ-193
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: re-read live whole-ship maps, PACKAGED_LIVE allowlist, WORLD_VISUAL_CENSUS, and exact dirty paths at claim time
stableRoute: GFX-3D-PICTURE-NEVER-BROKEN
```

## Outcome

On the default Helios / Kessler flyby, every ship the player can lock is a complete manufactured
body from the same game as Hitch, reverse thrust reads as force leaving a nozzle, and no tube,
hoop, or floating accessory stands in for a designed object. Shelf hulls and place remasters
reach the camera only after that flyby is true.

## Why this packet exists

The 3D side had many doors and no single board. `PQ-050` remasters flyable ships one at a time.
`PQ-136` fielded unused packs (leaves `.00`–`.03` done). `VISUAL_WORLD_CLEANUP` and
`GRAPHICS_3D_CAMPAIGN` named live defects but were not queue units, so `--next` never handed them
out. `needed-assets.md` still calls Hitch / Pelican / Wasp blocked. The owner asked for the map of
what is missing, below bar, unused, or invisible.

Census 2026-09-09 (live maps, not a filename grep). Full tables:
[`MODEL_STOCKTAKE_MANIFEST.md`](../../MODEL_STOCKTAKE_MANIFEST.md). Order:
[`MODEL_STOCKTAKE_PLAN.md`](../../MODEL_STOCKTAKE_PLAN.md). How the picture
arrives (empty substrate, runway, LOD, quality cuts):
[`DYNAMIC_GRAPHICS_INVESTIGATION.md`](../../DYNAMIC_GRAPHICS_INVESTIGATION.md).

- All 13 roster ships have dedicated models and render packages. Hitch and Wasp are the only
  *required* whole-ships. The empty-admission allowlist still omits Pelican, Mule, Drifter, the
  nine factory hulls, and the Massline liner. Those files do **not** publish on the player path
  until they are required — “mapped” is not “loads.” Modular kit still substitutes.
- Only Wasp is `accepted`. Hornet’s map exists; Hitch-plus is unmet (`PQ-050.01` residual).
- Owner playtest 2026-09-08: Hitch reads as a ship; a nearby NPC reads as leftover blocks; reverse
  reads as two needles. Default New Game parks 47-A scavengers and the official tug far away.
  The opening kitbash is modular **smuggler / pirate** traffic. The **recovery tug** is modular
  Mule when its beat starts (`assetRef` is dead; Concord shipKit is skipped).
- 47-A cans, drone *entities*, mines, generic wrecks, and mass seeds are still code-built.
  **Gates are not.** They spawn as stations with `place_gate_jump_ring.glb`. The hoop is leftover
  fallback. Mining-drone *place* GLB exists; the flyer is still a diamond.
- Factory remasters of already-live Ashline / Helios / work boats sit on disk. An earlier remap
  onto them made traffic invisible. Do not remap until that exact body is packaged and beats live.
- Corsair still shares the pirate Rig. Arclight has no route. Tanker and inspection cutter are
  held. **Yard tug is live.** Faction kits exist and nothing reads them. Dock / hulk / debris
  remaster is blocked at live presentation. Station fallback is still a fat cylinder plus hoops.
- `check:asset-reachability` fails on two painted-planet PNGs (not retail-routable). Sky, not Wave A.

## The bar

1. **One game.** A stranger shown Hitch and the nearest NPC does not ask which title each is from.
2. **Complete hulls.** No floating engine, wing, socket, or glow blob. Empty targeting locks fail
   this packet.
3. **Jets are jets.** Brake / reverse is force leaving a nozzle, same family as the main drive.
4. **A valid file is not accepted art.** Packaged and wired is not Hitch-plus.

Hitch is the floor, not the photocopy. Do not dump Hitch. Do not paste Hitch panels onto NPCs.
Chase camera only (`play_chase`, `play_chase_abeam`, `play_chase_close`). No seats.

## Dispatch

`node scripts/program-dispatch.mjs --id PQ-193`. Take the first ready leaf (`.00` first; `.02`
can run in parallel). `--id` accepts the packet id only.

## Leaves

Wave A is the opening flyby. Waves B–E do not start until `.00` is true, except same-slot GLB
authoring that does not touch live selectors.

| Leaf | Player-facing production outcome | Lifecycle boundary |
|---|---|---|
| `PQ-193.00` | Every ship the player can lock on the default route publishes a packaged complete body. The empty-admission allowlist includes all 13 roster hulls and the liner, and those bodies are **required**. Accessory-only modular junk (smuggler / pirate traffic included) never substitutes — including as a stand-in while the real body decodes. | Production; Hitch freeze untouched. |
| `PQ-193.01` | Helios / Kessler opening NPCs belong in Hitch's world. No floating parts on that flyby. Includes modular smuggler / pirate and the official 47-A recovery tug when it arrives. | Production; taste-led; Hitch frozen. |
| `PQ-193.02` | Reverse / brake reads as thrust leaving a nozzle, not two needles. One honest jet path. | Production; existing VFX foundation only. |
| `PQ-193.03` | Lane buoy, lane beacon, and cargo pod look manufactured at chase size, not tube-plus-ring. | Production; same-slot replace. |
| `PQ-193.04` | The 47-A code-built family (spindle, rescue capsule, Kessler beacon, Bourse wreck) and the generic TOW can are designed objects, not cylinder stacks. | Production; model first if hitch still owns the 47-A wiring file. |
| `PQ-193.05` | Mining-drone entity, jump gate, disc mine, generic wreck, and mass seed stop being primitives. Gate: upgrade the live `place_gate_jump_ring.glb`, retire the hoop fallback. Drone entity: point at `place_mining_drone.glb` after that file looks like hardware. | Production. |
| `PQ-193.06` | Corsair raiders no longer share the pirate Rig. | Production; existing Corsair body if it beats live. |
| `PQ-193.07` | The Helios Arclight appears as a rare heavy hauler. | Production; do not steal the Span or Atlas slot. |
| `PQ-193.08` | Volatiles tanker and inspection cutter spawn only after they read as complete ships at chase size. Yard tug is already live — do not re-hold it. | Production; held until they read as complete ships at chase camera. |
| `PQ-193.09` | Live Span and Wasp carry faction kits; the three trade-hub overlays that exist on disk are readable. | Production; no new faction systems. |
| `PQ-193.10` | Opening dock, dead hulk, and debris chunk pass live presentation at the shipping camera. | Acceptance support for the existing remaster. |
| `PQ-193.11` | The four Helios lane marks wired 2026-09-09 hold at chase camera (no cube foot/deck). | Acceptance support. |
| `PQ-193.12` | The fat-cylinder station fallback never appears on the default route. | Production. |

Flyable remaster of buyable hulls remains `PQ-050` (Hornet residual, then Drifter, Ranger, …).
Massline liner G7 remains `PQ-049.05`. Do not duplicate those leaves here.

## Entry conditions

- [ ] `git status --short` and `design/program/NOW.md` read; stay off Hitch freeze and any exact
      path a live row names. `build_map.md` §13.1 is a different owner.
- [ ] Live whole-ship maps, `PACKAGED_LIVE` allowlist, and
      [`WORLD_VISUAL_CENSUS.md`](../../WORLD_VISUAL_CENSUS.md) re-read at claim time.
- [ ] Claimed leaf still `ready`; Wave C–E leaves wait for Wave A except same-slot GLB authoring.
- [ ] `npm run check:baseline` at the candidate base; inherited reds noted.

## Live seams

| Leaf | Current owner | Seam to reuse |
|---|---|---|
| `.00` | `src/render/partsLibrary.js`, `src/render/assetLoader.js` | `PACKAGED_LIVE_WHOLE_SHIP_FILES`, `SOURCE_ROUTE_ALLOWLIST`, `requiresProductionWholeShipForEntity` |
| `.01` | `partsLibrary.js` NPC maps; [`VISUAL_WORLD_CLEANUP.md`](../../VISUAL_WORLD_CLEANUP.md) | Same-slot body replace; Hitch frozen |
| `.02` | `src/render/thruster/`, existing volumetric retro | One path; delete leftover needles |
| `.03`–`.05` | [`GRAPHICS_3D_CAMPAIGN.md`](../../GRAPHICS_3D_CAMPAIGN.md), census A | Same-slot replace; no parallel prop |
| `.06`–`.09` | `partsLibrary.js`, traffic/jobs owners | Package then wire; existing foundry body if it beats live |
| `.10`–`.12` | places / lane furniture / station fallback | Live presentation, not Blender-only |

## Work breakdown

### Phase 0 — characterization

- [ ] Confirm the leaf's failure class (invisible / falls apart / wrong-game) on the default route.
- [ ] Record the live file, package, and allowlist state for that slot.

### Phase 1 — production

- [ ] `.00`: admit packaged complete bodies; no accessory-only substitute.
- [ ] `.01` / `.02`: taste-led hull and one honest reverse jet.
- [ ] Modeling leaves: chase-camera form, then package, then same-slot wire.

### Phase 2 — route

- [ ] Default Helios / Kessler flyby. Chase camera only.
- [ ] Receipt under `design/program/roadmap/receipts/PQ-193.<leaf>-REPORT.md`.

## Write set

Exact paths on the claimed leaf's queue row. Shared files (`build_map.md`, `program-queue.json`,
`NOW.md`) only for integrator state after the receipt exists. Hitch / Kestrel never.

## How agents get this wrong

- Wiring a factory remaster that is not packaged, then calling the blank lock a renderer bug.
- Unhiding a box, engine, glow, or modular kit “so the player sees something” while the
  real body decodes. Empty substrate until the same complete hull publishes. Process:
  [`DYNAMIC_GRAPHICS_INVESTIGATION.md`](../../DYNAMIC_GRAPHICS_INVESTIGATION.md).
- Starting a distant-LOD / impostor / cheaper-species campaign before Wave A. Zoom-out
  does not swap hulls today. Garnish hide at a speck is later, not this packet.
- Starting Corsair / Arclight / faction kits while the opening flyby still shows a kitbash NPC.
- Remastering Hornet interiors or studio three-quarters. The player sees 60° chase at 144 WU.
- Lowering texture size to dodge a Blender crash. That was already the wrong inference on Hornet.
- Treating `needed-assets.md` or the 2026-08-08 catalog as live status.
- Measuring unused models with a filename grep.
- Answering a missing piece with a glow blob or a second needle trail on the failed retro volume.
- Editing Hitch / Kestrel.
- Cutting default quality or deleting Hitch detail to pass a perf gate.
- Commissioning a new hull for a slot an unused authored body already fills.

## Non-goals

- Hitch remaster.
- Cabin kits, seats, studio beauty.
- New ship commissions while a shelf body can fill the slot.
- Generic runtime LOD1/2 selector (later; not “looks broken”). See the dynamic-graphics
  investigation: impostors and silhouette proxies are banned; `hlod.js` garnish hide is
  the ceiling.
- Frontend, hitch smoothness (`PQ-129`), Asteroid Works interiors. Leftover admission
  *hitch* (`PQ-129.09`) is already rejected. The leftover *picture* (blank lock / kitbash)
  stays `.00` / `.01` here.

## Performance

Equal visual quality. Prove with one matched before/after on the densest touched route (draw calls,
presentation p95). Placement and occupancy ride existing residency. Do not permanently resident a
new fleet.

## Verification budget

- `.00`: `test/live-ship-visual-package-coverage.test.mjs`, `npm run check:live-whole-ship-admission`,
  `npm run check:playable` AUTHORED HULL step.
- `.01` / `.02`: default-route chase stills vs Hitch; no new top frame-time bucket.
- Modeling leaves: chase stills, same-slot sockets/collision, `npm run check:baseline`,
  `npm run check:playable`.
- Fielding leaves: `check:asset-reachability` count actually rises; existing traffic/jobs owners only.

## Stop conditions

- Hitch freeze set is being edited.
- A live `NOW.md` row owns the exact path.
- The proposed “fix” is a procedural fallback hull, a glow card, or a quality cut.
- If you cannot look at the visual-acceptance leaf on the default route, leave it `unproven` and continue other leaves.

## Review questions

- On the Helios flyby, does every lockable ship publish a complete body from the same game as Hitch?
- Is reverse a jet, or two needles?
- Did this leaf start Corsair / Arclight / a new commission while the opening NPC was still a kitbash?
- Did texture size get cut to dodge a Blender crash?
- Is Hitch still frozen?

## Checkoff and receipt

Would you ship this on the Helios flyby? Hitch stays frozen. Stay on the claimed leaf's write set. Run the checks that would catch this change.
