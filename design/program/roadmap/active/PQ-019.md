<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-019 — physical cargo heist, pursuit, heat, and fence

```yaml
queueId: PQ-019
lifecycle: integrated
acceptance: focused_green
packetRevision: 3
owner: unclaimed
integratedCommit: 7a1c3ce3
receipt: ../receipts/PQ-019C-mission-REPORT.md
priorReceiptA: ../receipts/PQ-019A-facility-embodiment-REPORT.md
priorReceiptB: ../receipts/PQ-019B-seams-REPORT.md
capsuleCaptureRepairReceipt: ../receipts/PQ-019-capsule-capture-repair-REPORT.md
routeHarnessRepairReceipt: ../receipts/PQ-019-route-harness-repair-REPORT.md
remainingPhases: missing H1 capture, H2 verdict, H3 matched performance, then alias cleanup and promotion
identityAction: PQ-019A runtime IDs are collision-free; integrator must replace colliding W03/W04/W05 parent aliases before promotion
```

## Outcome

A visible surface launcher fires one physical cargo capsule toward a lawful catcher. The player can intercept it with existing flight/Massline/boost physics, trigger witness-validated theft and pursuit by existing job-origin patrols, escape without magically clearing heat, and physically deliver the same capsule to a fence for exactly one settlement. Lawful arrival, destruction, confiscation, expiry, absence, abandonment, and retry are deterministic, save-safe outcomes—not script gaps.

The fun is the geometry and mass: predict a launch, meet a moving body, alter its trajectory, manage attachment/load/boost, improvise escape, and choose when a risky physical handoff is worth committing.

## Entry conditions

- [x] PQ-019A uses collision-free runtime identities: `heist_launcher`, `cargo_capsule`,
      `lawful_catcher`, and `fence_receiver`.
- [ ] Integrator replaces the parent packet's colliding W03/W04/W05 aliases before parent promotion.
- [x] PQ-013 planetary/world vertical and PQ-014 natural jobs are integrated at the candidate base.
- [x] Current payload/receiver, physics, map/place, presentation, registry, and facility owners were
      re-read for PQ-019A. Mission, law/security, heat, NPC jobs, economy, faction, and save remain
      downstream PQ-019B/C owner seams.
- [x] PQ-019A reuses existing place/pod assets without editing the occupied asset-manifest surface;
      no browser-GPU or performance-evidence lease was taken.
- [x] The first route is same-sector Tethys Junction. Deterministic geometry/map/custody evidence is
      focused-green; live visual/performance acceptance remains open.

PQ-019A/B/C are integrated and focused-green. The legacy parent alias cleanup remains an integrator
promotion condition. Current work is acceptance-only and is machine-routed as
`PQ-019.capsule-h1-capture`, `PQ-019.surface-heist-h1-capture`, `PQ-019.h2-verdict`,
`PQ-019.h3-performance`, and `PQ-019.promote` in `program-queue.json`; both prerequisite HARNESS
repair units are already focused-green.

The capsule continuation uses the registered one-use Browser cell
`pq019a-capsule-presentation`; direct execution is broker-blocked and cannot promote its pixels.
Its first brokered attempt on `bbcf975e` was classified HARNESS with failure fingerprint
`1634a88ae3c48bb7bdfec83e1ee8a62fd21e5d6137c8c59b341a4e1fdd3d40ac`: the page-context return
referenced undeclared `player` and `separation` locals. A seconds-scale contract now pins both facts;
the causal two-local repair is the only source change that authorizes a fresh candidate attempt.
That fresh attempt on `53922e94` completed structurally, but original-resolution review rejected it
as incomplete H1 evidence: the six-WU capsule's close/default zooms both hit the same `45` floor,
producing `45,45,66` instead of three distinct framings (review fingerprint
`9d34483101ae63b655c8f311840590f72b1b310fbcc9910ac6e18b233aff8aa9`). The continuation now pins
`24,33,66` and invokes `--capsule-only`, so a final causal attempt cannot recapture the already-valid
facility or cue pixels.

## Split into three leaf packets

### PQ-019A — facility embodiment

Owns one launcher, one capsule identity, one lawful catcher/impound receiver, and one physical fence receiver. It proves schedule, geometry, sockets, route accessibility, collision/custody envelopes, assets, and performance. It does not implement theft law or settlement.

### PQ-019B — owner seams and pure arbiter

Owns deterministic outcome arbitration, witness-validated law report intake, heat-owner receipt consumption, NPC-job control leases, and physical receiver prepare/commit/abort. Each seam lands with focused idempotence tests before mission wiring.

### PQ-019C — authored mission and public route

Owns one authored-only mission offer using ordinary board/accept/completion/failure flow. It wires the accepted facilities and owner seams, save/recovery, route cues, balance, accessibility, and acceptance evidence. It does not become a generic heist framework unless reuse is proven by a second consumer later.

Do not assign all three leaves to one worker unless the integrator explicitly serializes every owner and review phase.

## Live seams to bind

| Domain | Existing owner/behavior to reuse | Required seam if absent |
|---|---|---|
| Mission | ordinary authored offer, active mission serialization, complete/fail/expire | pure outcome arbiter and durable heist subrecord inside the mission owner |
| Facility/capsule | authored place/world materialization and physical payload identity | scheduled launch receipt; stable capsule reconstruction/custody APIs |
| Possession | existing Massline/tether latch and physical state | normalized possession candidate only; no mission-owned physics |
| Law | jurisdiction, witnesses, lawful targets, responder selection | `law:reportIncident`-class owner intake returning idempotent incident receipt |
| Heat | existing private mutation/decay owner | listener consuming validated incident receipt exactly once; no mission heat write |
| NPC jobs | natural patrol jobs and one movement-intent writer | `claimControl`/`releaseControl` lease preserving/restoring job state |
| Receiver | existing physical payload/receiver substrate | prepare/commit/abort keyed by terminal receipt; no consume/payout during prepare |
| Economy/factions/cargo | existing owner events | mission requests ordinary reward/rep effects; capsule never becomes hidden player cargo |
| Save | mission/site/job/incident owner serializers | stable IDs/receipts/effect journal; transient entity IDs rebound on Continue |

Role names must be replaced with exact live symbols before PQ-019B becomes `ready`.

## State and arbitration contract

Separate nonterminal progress from immutable terminal outcome.

```text
scheduled -> launched -> possessed -> pursued -> escaped -> receiver_pending
     \          \           \          \             \
      \----------> resolution_pending -----------------> terminal
```

Terminal outcomes:

```text
lawful_arrival_observed
payload_destroyed
lawful_confiscation
fenced_success
expired
unresolved_absent
abandoned
```

Every report becomes plain deterministic candidate data:

```js
{
  candidateId, // hash(missionId, payloadStableId, kind, causalTick, sourceStableId)
  missionId,
  payloadStableId,
  kind,
  causalTick,
  sourceStableId,
  proof
}
```

The mission owner buffers same-step candidates and decides no earlier than `causalTick + 1`. Selection never uses callback/insertion order, wall time, live entity ID, or random choice.

Pin explicit tie rules in pure tests. Recommended terminal precedence at one tick:

```text
destroyed > confiscated > fenced > lawful_arrival > expired > unresolved_absent
```

Nonterminal possession applies only when no terminal candidate wins. Remaining ties use stable `candidateId`.

Exactly one prepared terminal receipt is compare-and-set, then owner effects commit idempotently:

```js
{
  receiptId,
  missionId,
  payloadStableId,
  outcome,
  causalTick,
  winnerCandidateId,
  status: 'prepared' | 'committed',
  effectKeys: {}
}
```

A crash after prepare resumes the same receipt. Receiver/economy/faction/mission effects never select a new winner during replay.

## Required owner invariants

For every route and reload:

```text
terminalReceiptCount == 1
capsuleProjectionCount <= 1
receiverCommitCount <= 1
missionSettlementCount == 1
economyRewardCount == (outcome == fenced_success ? 1 : 0)
factionOutcomeCount <= 1
heatApplicationCount == (validatedWitnessedTheft ? 1 : 0)
playerCargoMutationCountForCapsule == 0
sectorOwnershipMutationCount == 0
activeJobControlClaimsAfterTerminal == 0
```

Pursuit uses existing job-origin patrols. No available patrol is a valid, visible outcome recorded by law; it is not permission to spawn a fake job-origin responder.

## Work breakdown

### Phase A — physical facilities

- [x] Choose exact same-sector launcher/catcher/fence locations and stable identities.
- [ ] Build/approve visible assets, sockets, collision/custody envelopes, schedule cues, and map/place routes.
- [x] Materialize exactly one capsule per active schedule through the `heistFacilities` owner.
- [x] Prove real default-Rapier capsule contact at both catcher and fence, with schedule-bound,
      forged/stale-resistant candidate receipts. SG02 solver-level PAYLOAD-only collision groups are
      not available; PAYLOAD-only applies to custody eligibility, not exclusion of ship contact.
- [ ] Measure route/entity/collider/query/draw/residency cost with ordinary traffic.

Headless Phase A evidence is recorded below: authored socket geometry, Atlas courses, route clearance,
asset identity, exact schedule/capsule identity, real contact, and bounded entity/collider cost are
proven. Player-visible schedule cue, matched traffic, draw/program/residency, and human visual quality
remain open and keep the two broader Phase A checkboxes above unaccepted.

**Stop:** any facility is invisible, generic-only, debug-only, inaccessible, misaligned, fallback-loaded, or unmeasured.

### Phase B — pure arbiter

- [x] Add a pure transition validator, candidate normalizer, deterministic selector, terminal compare-and-set, and effect journal.
- [x] Test all callback-order permutations, duplicate candidates, stale IDs, save boundaries, and terminal replay.
- [x] No bus, renderer, economy, law, or mission side effects in pure tests.

### Phase C — owner seams

- [x] Law report intake validates jurisdiction/witnesses and returns one incident receipt.
- [x] Heat owner consumes validated incident once using its private mutation path.
- [x] NPC job owner grants/releases idempotent control leases and restores/fails safe.
- [x] Physical receiver supports prepare/commit/abort; prepare cannot consume or reward.
- [x] Each seam has focused duplicate/competing/save/reload/destruction tests before integration.

### Phase D — authored mission

- [x] Add one procedural-weight-zero offer through the ordinary board/accept route.
- [x] Wire schedule/launch/latch/law/pursuit/escape/catcher/fence/destruction/expiry/absence/abandonment to candidates.
- [x] Settle through existing mission/economy/faction owners using terminal effect keys.
- [x] Post at most one explicit reduced-stake recovery offer when authored policy allows.
- [x] Add readable timing, ownership, witness, WANTED, pursuit, catcher/fence, denial, and recovery cues.

### Phase E — save, adversarial, route, tuning

- [x] Save before launch; after launch; after possession; during pursuit; after escape; receiver prepared; terminal prepared; post-commit/pre-mission-event; missing projection.
- [x] Reconcile exactly one capsule or reach bounded `expired`/`unresolved_absent`; never fabricate payout.
- [ ] Route lawful observe, successful heist/fence, confiscation, destruction, and recovery through normal input.
- [x] Tune launch speed/mass/window, witnesses, responder count/leash, fence geometry, and payout from a predeclared fixed-seed matrix. Select once; pin the result.
- [ ] Browser/Electron, accessibility, visual/fun review, and matched performance.

## Bounded write set

Phase A exact paths are owned by the facility/asset/place implementation packet. Phase B–D are expected to touch:

- one pure mission arbiter module;
- current mission definition/runtime owner;
- current law/security owner;
- current heat owner listener;
- current NPC-job runtime/helper owner;
- current physical payload/receiver owner;
- focused tests for each owner and the integrated mission;
- one shared Browser/Electron route harness and validation-broker manifest;
- packet receipt.

No feature worker edits queue/global ledgers, registry order, package aliases, shared input mappings, general tactical AI, core Massline/tether authority, save schema, or common HUD/styles without a separate owner-approved request.

## Non-goals

- Generic procedural heists, a new crime simulation, or a pirate-economy rewrite.
- Mission-owned patrol spawning/AI steering, heat, cargo, credits, faction rep, or physics.
- Capsule conversion into inventory cargo.
- Cross-sector first implementation unless same-sector proof is impossible.
- New Massline controls or boost mechanics; use existing physics and expose tuning only where needed.
- Facility-free debug injections or cinematic-only launches.

## Performance and quality budget

- One active capsule per authored mission and one bounded facility set.
- Witness query only at theft transition and bounded by jurisdiction/spatial owner.
- Bounded responder leases from existing jobs; no all-pairs scans.
- Candidate journal capped and frozen at terminal.
- No per-frame mission scanning for catcher/fence when existing spatial/receiver owners can publish events.
- Facilities/capsule use appropriate LOD/culling/material sharing and explicit admission; no fallback identity.
- Save state remains bounded by one mission/payload/candidate/effect journal.
- Matched route records normal and traffic-loaded frame/query/entity/residency cost; no default quality reduction.

## Verification budget

```yaml
L0:
  - node --check for changed modules/scripts
  - git diff --check
L1:
  - npm run check:pq019a:facility-embodiment
  - pure arbiter transition/race/permutation suite
  - focused law, heat, NPC-job lease, receiver idempotence tests
L2:
  - mission/save/reload owner invariants
  - npm run check:sim:compare
  - relevant NPC jobs, Massline, payload/receiver, save, and facility checks
CathedralDamageRecovery:
  - npm run check:presentation
  - npm run check:visual-stability
  - if the accepted heist/combat/pursuit route enters or passes Ceres, candidate-bound evidence MUST include world_site_wreck_cathedral damage and recovery visual states
  - if the accepted same-sector route excludes Ceres, the receipt MUST record that exclusion and relocate this proof to PQ-023.gold-corridor-required-cues through its weapons/impacts/destruction cue family and independent motion/normal-camera review
L3BrokerManifest: pq019-surface-heist
acceptanceAttemptsPerCellPerCandidateDigest: 1
reviewClosure: discovery -> repair -> causal re-review when repairs affect the claim
```

PQ-019A selects a Tethys-only route: launcher, lawful catcher, and fence all resolve inside
`sector_tethys_junction`, with no Ceres leg. `PQ-023.gold-corridor-required-cues` therefore remains
the downstream owner of Cathedral damage/recovery presentation. If PQ-019C deliberately authors a
later accepted route through Ceres, PQ-019C takes that evidence row instead. The two named check
exits are supporting gates; without Cathedral-keyed route output they do not prove the visual states.

Run live routes only after the facility and all owner fast gates produce one candidate-bound receipt.

### PQ-019A deterministic implementation receipt — 2026-07-27

- **Commits:** authored capsule presentation `272e63b4`; Tethys facility/runtime owner `b66f2255`.
- **Route:** Tethys Junction only. Authored roots are launcher `(1515,-2012)`, catcher
  `(-400,-1270)`, and fence `(-1320,420)`. Center-corrected `SOCKET_Dock_Approach` heads are
  `(1509.2701024392563,-2009.4793777491086)`,
  `(-393.58684663122847,-1272.82828201166)`, and
  `(-1315.6454373542344,411.96793739926636)`.
- **Accessibility:** launcher → catcher is `2040.470388287225 WU`; catcher → fence is
  `1920.606764974925 WU`. The launch head clears The Anvil body by
  `56.416128042107744 WU` against `16 WU` required and the first leg points outward. Catcher and
  fence envelopes remain inside the Customs Checkpoint and Quiet Cache zones. All three POIs expose
  one finite ordinary Atlas/autopilot course.
- **Schedule/custody:** `heist:requestLaunchSchedule` returns one stable receipt, identical replay is
  idempotent, a competing schedule is denied, and one radius-6/mass-180 CCD `payload` launches.
  Default `rapier-dynamic` produces real catcher and fence `physics:impact` candidates. Forged,
  metadata-twin, stale, wrong-ship, and duplicate inputs cannot create a valid extra candidate.
  Sector exit preserves an unlaunched schedule or removes the launched transient capsule without
  settlement, reward, or fabricated terminal outcome.
- **Assets/presentation:** facilities reuse exact authored place assets and the capsule resolves
  `pod_cargo_container` from the release pod slot, SHA-256
  `fd5b538629d17a9191a147be1a3ef6222d6fb890565f5cf4d11722c7ece0d5fa`. Pending/unavailable
  capsule presentation is hidden; admitted presentation is centered and radius-matched; generic
  payloads are unchanged.
- **Deterministic structural cost:** idle `6` owned entities / `3` colliders; active
  `7` / `4`; update work is one O(1) schedule check; contact is event-driven; candidate history is
  capped at `32`. Real route frame p95/p99/hitches, draw/program counts, GPU admission/residency, and
  applied close/default/far/motion/LOD remain `null` with `requiresHeaded: true`.
- **Single writers:** `heistFacilities` writes only `state.heistFacilities` and entities it owns.
  It emits schedule, launch, and custody-candidate receipts. Player cargo, credits, reputation,
  WANTED heat, patrol steering, mission settlement, save schema, and sector ownership are untouched;
  `playerCargoMutationCountForCapsule == 0`.
- **Headless gates:** `npm run check:pq019a:facility-embodiment` is `12/12 PASS`;
  authoritative-manifest/runtime-profile tests are `23/23 PASS`; `check:atlas-integrity`,
  `check:save-schema`, `check:asset-reachability`, `check:cargo-conscience`,
  `check:faction-standings`, `check:physics-authority`, `check:sim:compare`, and the bounded
  `check:baseline` pass. PQ-019A adds no baseline link; the new focused check is explicit.
- **Unrelated open owner gates:** the current shared tree still has
  `check:economy:anti-exploit` red at `field_contract_dedupe` and the downstream headed
  `check:mission-cargo-loading` route red at its existing station-tab assertion. PQ-019A touches
  neither economy-contract dedupe nor station mission UI and does not use either failure as
  acceptance evidence; they remain open for their owners/PQ-019C.
- **Folded PQ-018 truth:** this implemented candidate does not enter Ceres, so it produces no Cathedral
  damage/recovery claim. That proof stays with `PQ-023.gold-corridor-required-cues` through
  `check:presentation` and `check:visual-stability`. Reduced-motion/non-color HUD evidence also
  remains PQ-023 via `check:presentation`, `check:ui-a11y`, and `check:wcag-contrast`. The independent
  Cathedral and facility/capsule art verdicts remain human-eye gates; no agent screenshot loop or
  synthetic pass was used.

## Review questions

- Is the heist won by understanding trajectory, mass, attachment, boost, and timing rather than hitting an invisible mission trigger?
- Can lawful arrival and theft both be observed without ambiguity?
- Are witnesses/jurisdiction/law/heat authoritative and replay-safe?
- Are responders genuine existing jobs with one steering writer and bounded release?
- Can any race, reload, duplicate callback, or stale retry pay twice, consume twice, or choose a different outcome?
- Does pursuit remain dangerous after local escape while heat stays owner-controlled?
- Is the route legible and exciting at normal camera without overwhelming effects or hidden text?

## Stop conditions

- Canonical identity collision unresolved.
- Facilities missing or unaccepted.
- Mission code would validate law, mutate heat/economy/cargo/rep, steer patrols, or consume receiver payload directly.
- Receiver must commit before terminal arbitration.
- Another active packet changes mission receipt, payload, law, job-control, save, facility, or Massline contracts.
- Deterministic candidate order or exactly-once effects cannot be proven.
- Same-sector route is physically infeasible without a deliberate scope decision.
- Performance requires deleting facilities/effects or lowering default quality.

## Checkoff and receipt

- [ ] Stable identity correction recorded.
- [ ] PQ-019A facility route accepted. *(deterministic geometry, assets, map, schedule, custody, and
      structural-cost receipt focused-green; live schedule cue, matched draw/residency/performance,
      solver-level collision-group exclusivity, and human visual verdict remain open; the seeded
      moving-capsule framing harness is repaired in
      [`PQ-019-capsule-capture-repair-REPORT.md`](../receipts/PQ-019-capsule-capture-repair-REPORT.md),
      but its new pixels remain unproven)*
- [x] PQ-019B pure arbiter and four owner seams focused-green. *(integrated; receipt
      [`PQ-019B-seams-REPORT.md`](../receipts/PQ-019B-seams-REPORT.md), 91/91, save schema untouched)*
- [x] PQ-019C ordinary mission/save routes focused-green. *(integrated; receipt
      [`PQ-019C-mission-REPORT.md`](../receipts/PQ-019C-mission-REPORT.md), 65/65, invariants proven
      in missions.js, nine save points reconciled; headless flows only — headed route evidence is the
      next row)*
- [ ] Lawful/heist/confiscation/destruction/recovery Browser/Electron evidence accepted.
      *(L3 manifest `pq019-surface-heist` authored+registered; H1 ran once and retained partial
      evidence; the simulation-time/terminal-race wait is repaired and focused-green in
      [`PQ-019-route-harness-repair-REPORT.md`](../receipts/PQ-019-route-harness-repair-REPORT.md),
      while missing headed routes remain unproven)*
- [x] Tuning matrix and selected values retained. *(matrix pre-committed before the runner existed;
      selections pinned in tests; one NaN-contaminated run voided and recorded; witness-radius 550
      selection recorded as a shared-change request against the lawSecurity constant)*
- [ ] Accessibility, visual/fun, and matched performance accepted.
- [ ] Independent discovery and causal re-review terminal.
- [ ] Receipt linked; integrator owns global promotion.

## References

- [`../program-queue.json`](../program-queue.json)
- [`../../../../docs/handoffs/chatgpt-portfolio-20260723/PQ019_IDENTITY_AND_HEIST_ARCHITECTURE.md`](../../../../docs/handoffs/chatgpt-portfolio-20260723/PQ019_IDENTITY_AND_HEIST_ARCHITECTURE.md) (historical seam audit)
- [`../../../revamp/BP-13_PIRATE_ECOLOGY.md`](../../../revamp/BP-13_PIRATE_ECOLOGY.md)
- [`../../../PERF_BUDGET.md`](../../../PERF_BUDGET.md)
