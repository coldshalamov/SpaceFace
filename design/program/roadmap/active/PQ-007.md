<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-007 — corrected auto-target and direct draw-to-fly acceptance

```yaml
queueId: PQ-007
lifecycle: implemented
acceptance: focused_green
packetRevision: 1
owner: unclaimed
integratedCommit: 4d00867e
remainingPhases: bounded Browser/Electron control-route acceptance only
```

## Outcome

The integrated user-directed correction receives one exact-revision player-route verdict: `G`
auto-targets through the live targeting owner, direct draw-to-fly remains clutchable and granular,
keyboard/pointer semantics remain reachable, and none of the rejected pursuit-slot behavior returns.

## Entry conditions

- [x] The correction is integrated at `4d00867e`.
- [x] Focused control, Massline, input-modality, UI/accessibility, flight, and sim-compare gates passed
  at the correction checkpoint.
- [ ] Re-read the current targeting, input, flight-V3, HUD, and acceptance-probe seams against HEAD.
- [ ] Confirm `browser-gpu` and `validation-broker` are free before launching either runtime.

## Work breakdown

1. Characterize the live `G` auto-target and draw-to-fly semantics without editing product code.
2. Rebase the existing control probes to the current root route only if a reproduced harness defect
   prevents observation.
3. Run one bounded Browser cell and one Electron parity cell with keyboard and pointer coverage.
4. Record target acquisition, clutch/release behavior, raw-axis preservation, HUD feedback, and
   accessibility semantics at the exact candidate revision.
5. If product behavior is wrong, stop with a seconds-scale regression and return a repair unit; do
   not fix gameplay inside the acceptance packet.

## Non-goals

- No pursuit slot, MMB pursuit selection, target-relative station keeping, pursuit impulses, pursuit
  HUD, or pursuit toasts.
- No flight-physics retuning, input-schema change, controller redesign, or compatibility-path edit.
- No broad browser loop or repeated capture after an unchanged failure fingerprint.

## Performance budget

This packet makes no performance-improvement claim. Acceptance must show no new continuous input,
targeting, HUD, or allocation work and must not change default graphics or controls to pass.

## Verification budget

- Fast gate: `npm run check:baseline`.
- Focused structure: `node scripts/check-auto-target-registry.mjs` and
  `node scripts/check-massline-auto-target.mjs`.
- Existing route probes: `scripts/probe-auto-target-steering.mjs` and
  `scripts/probe-dod-flight-acceptance.mjs`.
- Headed budget: one Browser claim and one Electron claim after preflight.

## Review questions

- Does `G` select a useful target through the ordinary route without inventing pursuit behavior?
- Can the player engage, steer with direct draw intent, clutch/release, and resume ordinary flight
  without sticky input or hidden automation?
- Are target and control-state changes visible and non-color-dependent?

## Stop conditions

- Stop on any product/harness/environment failure and classify it before another headed attempt.
- Stop if current live semantics no longer match the user-directed correction.
- Stop if another owner holds input, flight, targeting, HUD, Browser-GPU, or broker paths.

## Checkoff

- [x] User-directed focused implementation integrated.
- [ ] Browser route accepted.
- [ ] Electron parity accepted.
- [ ] Keyboard and pointer semantics accepted; applicable controller behavior recorded honestly.
- [ ] No rejected pursuit-slot behavior present.
- [ ] Exact-revision receipt linked and queue unit `PQ-007.route-acceptance` marked `done`.
