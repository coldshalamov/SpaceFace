<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-175 — Swarm mode content at craft: waves, arenas, drafts and mutators that create decisions

```yaml
queueId: PQ-175
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

Every wave asks a question in physics ('you are surrounded', 'the anchor is behind the elite', 'the current runs against you'), every arena's props are toys (crushers, shutters, relays, currents), every draft card changes a verb (a heavier line, a wider well, a faster reel, a ram plate) with at most one in three being a number, and each weekly mutator is a distinct game (gravity slalom, heavies only, weapons cold, reef).

## Source

`CANONICAL_BUILD_MAP.md` §16 (admitted 2026-09-03) → **S · Swarm mode**, wave **BETA**. Gap it closes: Waves are pinned recipes with roles; drafts include stat modifiers; arena props are mostly dressing; mutators exist as a list. Reference: Spelunky level grammar, Hades boons that change verbs, Risk of Rain stages. Routes through existing sources: §12 PQ-133.05–.10; PQ-027 machinery; PQ-171 content grammar.

## What exists (audited 2026-09-03)

`src/data/survivalWaves.js` (schema, roles, room phases), `survivalActs.js`, `arenaModuleLibrary.js`, `survivalMutators.js`, `survivalDraft.js`, the content factory validators (PQ-133.12), five arena controllers (lagrangeCrucible.js, cinderSluiceArena.js, cryoDriftArena.js, stormLatticeArena.js).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` (after PQ-174.01) | **Every wave asks a question.** Each of the thirty wave recipes declares its physical question and the verb that answers it; the planner never schedules two consecutive waves with the same question. | Validator asserts a question per recipe and no repeats in a row; a blind reviewer names the question from a strip for 8 of 10 waves. |
| `.01` (after PQ-027.00) | **Props are toys.** Each arena's props participate: shutters cut lines, plates bank shots, crushers kill, relays conduct, currents carry; at least three throwable or usable props per arena. | Per arena, three props used in a scenario to kill or escape; capture. |
| `.02` | **Drafts change verbs.** At most one card in three is a number; every other card changes a verb's shape (line load, well radius, reel speed, ram plate, whip snap); cards are legible in one line. | Draft catalog audit: ≥ 66 % verb cards; a playtester describes each card's effect after one use. |
| `.03` (after PQ-169.02) | **Mutators are games.** Four weekly mutators shipped as distinct games with their own best build (telemetry): gravity slalom, heavies only, weapons cold, reef. | Top strategy differs per mutator over 10 seeds each. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Writing waves as spawn lists: a wave without a stated physical question is rejected by the validator, not by taste.
- Filling drafts with +10 % cards because they are easy to author.
- Dressing arenas with props that cannot be touched; a prop that does not move mass is scenery, not content.

## Entry conditions

- [ ] Dependencies integrated at the candidate base: `PQ-174`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario on a fixed seed before tuning (the bar this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, the fun-loop bench, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Every wave asks a question.
- [ ] `.01` Props are toys.

### Phase 2 — owner wiring

- [ ] `.02` Drafts change verbs.

### Phase 3 — presentation and route

- [ ] `.03` Mutators are games.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `src/data/survivalWaves.js`
- `src/data/survivalActs.js`
- `src/data/arenaModuleLibrary.js`
- `src/systems/survivalMutators.js`
- `src/systems/survivalDraft.js`
- `src/systems/lagrangeCrucible.js`
- `src/systems/cinderSluiceArena.js`
- `src/systems/cryoDriftArena.js`
- `src/systems/stormLatticeArena.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No sixth arena; no draft rarity tiers; no wave that is a bullet-hell screen fill.

## Performance and quality budget

Props reuse the arena module library and pooled bodies; ≤ 12 dynamic props per arena. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:crucible:arc`, `npm run check:crucible:meta`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

- Which gate row (§15.1, §16.1, §17.1, §18.1) did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden, on the default route?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, give an NPC a hidden gyro, scale hit points, or add a dialogue tree?
- Does the report read in the owner's words (§1.4)?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the gate row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-175-<leaf>-REPORT.md`; queue unit state updated by the integrator.
