<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-177 — An economy you can read and play: ticker, charts, forecasts, contracts that twist, a black market

```yaml
queueId: PQ-177
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

The player learns the economy is real by watching it move and profits by understanding it: a rotating ticker of real events, a market chart with the last ten minutes and a forecast cone, supply-chain glyphs that teach what feeds what, an event card when docking under a shortage or blockade, a profit readout on sale, contracts whose clauses fire mid-mission, and a black market whose entrance is a smuggling run. Six interesting decisions an hour is the bar.

## Source

`build_map.md` §17 (admitted 2026-09-03) → **A · Adventure depth**, wave **BETA**. Gap it closes: Price cycles are computed but chartless; events fire silently; the production graph exists only as data; forecasts exist and are never drawn; contract twists exist only as static clauses. Reference: Endless Sky trade, Starsector markets, FTL event beats, X4 supply chains, Escape Velocity news. Routes through existing sources: S3-F1-ECONOMY, RV-ECONOMY, PQ-155 curve, PQ-148 cargo, PQ-151 wanted loop, PQ-168 chart.

## What exists (audited 2026-09-03)

SPEC3-10/11/12 (designs written), `src/systems/economy.js` (predictPriceCurve, regimeLabel, price pressure), `economyCycles.js`, `economyContracts.js`, `src/data/commodities.js` (33, producedBy/consumedBy), `newsTemplates.js`, `contractClauses.js`, `src/ui/screens/market.js`, `galaxyMap.js` pressure layer, `pirateDisguise.js`, `dockDeny.js`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **The ticker and event cards.** One-line rotating feed from real events (blockade, shortage, war tension, flips, hunter sightings) at the arbiter's chatter tier; a single card on docking under an event. | Every ticker line traces to a sim event; capture of a blockade line and its card. |
| `.01` | **Charts with a forecast cone.** Per-commodity sparkline of the last ten minutes plus the forecast band and regime label; profit readout on sale from cost basis. | Forecast quality is judged by calibration and decision tests, not an income uplift (audit 2026-09-05): the stated band contains the arrival-time outcome at its stated rate on held-out seeds; a player can tell a stale survey from a fresh quote and a modelled past from an observed one; the UI quotes the whole contemplated sale after slippage and net of travel and operating cost. |
| `.02` | **Supply-chain glyphs.** Each commodity shows producedBy → consumedBy station glyphs; hovering a station explains what it buys and sells and why. | Blind test: a player names where to sell ore and why after one screen. |
| `.03` (after PQ-138.04) | **Contracts that twist.** Clauses fire mid-mission (the escort turns, the buyer is the law, the cargo is volatile); every twist mutates rather than fails (PQ-138.04). | ≥ 5 twist clauses live on boards; each has a scenario. |
| `.04` (after PQ-148.02, PQ-151.02) | **The black market.** Reached by a smuggling run (PQ-148.02 physics), sells what stations refuse, launders heat for a cut (PQ-151.02); prices move with your own supply. | A seeded smuggling loop completes; laundering shows in the ledger. |
| `.05` (after PQ-173.01) | **Interesting decisions per hour.** Telemetry counts decisions with ≥ 2 viable options and a visible tradeoff; the bar is ≥ 6 per hour in adventure. | Metric printed by the fun-loop measurer; ≥ 6 on the reference route. |
| `.06` | **Cargo custody and one-commit transactions** (audit 2026-09-05, Order 8). The programmed-miner path stops adding ore to the player's hold: an operation owns its inventory or shipment record (quantity, owner, origin, destination, progress, risk, delivery state); custody moves once, with a receipt; a market purchase or shipment sale is one validated plan (identity, quote version, quantities, price, capacity effect) recommitted against preconditions and committed atomically through the canonical owners (economy→credits, cargo→cargo), with a durable receipt so a duplicate intent returns the prior receipt instead of paying twice. Reference semantics: `tools/reference/studio-recovery-audit-2026-09-05/transferPlan.mjs` (never imported). Station price quoted at the exact sale call. | Manual cargo can never be sold or discounted by a worker; retry and save/reload duplicate nothing and lose nothing (tests with a JSON round trip mid-transaction); the receipt names the station and the quote it used. |
| `.07` (after `.06`) | **Visible operational limits replace the passive haircut** (audit 2026-09-05, Order 9). One representative operation shows gross output, the limiting physical stage (no exposed face, no power, full storage, blocked lane, missing input, launch capacity, poor destination, demand saturation), realised sale and operating cost; a routine fuel shortage stops work visibly (stranded, waiting, stalled) and never deletes purchased equipment; operating cost ties to operating state and shows beside net throughput. The passive-income token bucket and overflow discount are retired as the PRIMARY constraint only after the replacement throughput/demand model is proven on representative loops and old saves migrate their machinery explicitly. | The player can explain why adding a machine helped or did not; returns stay bounded on the tested loops; an old save keeps every machine; no raids or maintenance punishment added as balancing. |

## How agents get this wrong

### Senior implementation direction for `.07` (2026-09-06)

Keep custody, exactly-once settlement and equipment persistence. Start with the existing programmed
miner operation: show output, stored shipment, current limiting stage, last realised sale and
operating cost together. A stopped operation must retain its machine and resume after the missing
resource returns, including after Continue. Prove the replacement limits against one machine,
several competing machines and a saturated destination before retiring the bucket/discount for
that route. Do not remove unrelated safeguards or invent recurring punishment to balance income.

These are the ways a lazy first try fails this packet. Read them, then do the work as if the owner will play it tonight.

- Inventing news that is not a sim event: every ticker line must cite the event it came from.
- Drawing a forecast that the sim does not compute: use predictPriceCurve; never fake a trend.
- Making the black market a menu: its door is a physical smuggling run.

## Entry conditions

Glance at NOW.md so you don't collide. The Leaves table is the job. Dependencies are on the queue row.

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Look at the change on the default route. If a number is the done-when, print it from a named seed. If the player would see or hear it, look at it. A still is a way to look, not something to store.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` The ticker and event cards.
- [ ] `.01` Charts with a forecast cone.

### Phase 2 — owner wiring

- [ ] `.02` Supply-chain glyphs.
- [ ] `.03` Contracts that twist.

### Phase 3 — presentation and route

- [ ] `.04` The black market.
- [ ] `.05` Interesting decisions per hour.
- Play it again after you change it. Fix what you would not ship.

## Write set

- `src/systems/economy.js`
- `src/systems/economyCycles.js`
- `src/data/newsTemplates.js`
- `src/ui/screens/market.js`
- `src/ui/hud.js`
- `src/data/contractClauses.js`
- `src/systems/missions.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No spreadsheet screens; no economy rewrite; every feature reads existing events and caches.

## Performance and quality budget

Sparklines from a 60-sample ring in marketIntel; the ticker is one DOM line; no per-frame allocation. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:market-first-loop`, `npm run check:economy:regional-supply`, `npm run check:price-memory`. Per leaf: look at the player-facing change; print the done-when number from a named seed when a number is the bar. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

Would you put this in front of the owner? What's unfinished, what's buggy, and what would make it better? If you're about to close, have another agent look at the actual work for those three things and fix what's real. Don't write a review file.

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

Would you ship this? Stay on the write set. Run the checks that would catch this change. A short note for the next engineer only if you learned something they couldn't see from the code.
