<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-141 — The 60-second proof (program acceptance gate)

```yaml
queueId: PQ-141
lifecycle: planned
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: PQ-137 (.03–.06), PQ-138 (.00–.02), PQ-139 (.00–.01) integrated
```

## Outcome

The sequence the owner wrote as the A-list version of SpaceFace happens **naturally and
repeatedly** at the reference site: a working op, a hauler leaving, pirates intercepting, a shove
that spins one, a rope-swing-release that makes a projectile, collateral, cargo spilling, the hauler
fleeing, a patrol arriving, the player grabbing a pod and running WANTED. Nobody scripts it; the
world and the verbs produce it. Bar **B12** in `design/FEEL_CONTRACT.md`. This is the acceptance
gate for the whole §13C program: the other packets exist so that this one can pass.

## Source

`design/VISION.md` Part I "The 60-second fantasy" and Part II "What an A-list version of SpaceFace
looks like." Reference site: the Ceres reference pocket (`design/program/roadmap/active/PQ-020.md`,
`PHYSICS_AS_SPECTACLE_PROGRAM.md` R5).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Deterministic scenario `proof.sixty_seconds`.** A seeded scenario at the reference site with the op running, a hauler on schedule, a pirate intercept on the route, a patrol in range, and a scripted *player input tape* of a few simple verbs. Receipts detect the 11 beats. | ≥ 9 of 11 beats detected on the default route across 5 seeds; the run never exceeds 90 s. |
| `.01` | **Headed capture at the shipping camera.** Normal speed, no debug overlays, default quality; owner review of the capture against the VISION paragraph. | Owner verdict recorded; missing beats routed back to their packet. |
| `.02` | **Every actor has a sentence.** With HUD text hidden, a viewer can say what the miner, hauler, tender, survey ship, patrol, pirate and scavenger are doing from motion and light alone. | A blind-review (no labels) capture; ≥ 6 of 7 roles named correctly by a reviewer who has not read the scenario. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Scripting a beat to make the proof pass: if a beat needs scripting, the packet that owns the beat is not done.
- Recording the capture at a flattering angle or speed: shipping camera, normal speed, default quality, no overlays.
- Counting a beat from receipts alone when the frames do not show it: both are required.

## Entry conditions

- [ ] Dependencies integrated at the candidate base as the queue row states.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario before tuning.

## Non-goals

No cutscene, no scripted NPC behaviour, no camera path. If a beat needs scripting to occur, the
packet that owns the beat is not done.

## Checks and evidence

The scenario in `tools/agentic/scenarios.json`, `npm run check:playable`, the capture, the owner's
verdict. Receipt: `design/program/roadmap/receipts/PQ-141-proof-REPORT.md`.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the leaf's scenario; record the before numbers for the `design/FEEL_CONTRACT.md` bar this packet moves.

### Phase 1 — pure contracts and data

- [ ] The data/rule leaves in the Leaves table, in order.

### Phase 2 — owner wiring

- [ ] The listener/seam leaves in the Leaves table, through the existing single writers.

### Phase 3 — presentation and route

- [ ] Captures at the shipping camera; replay the same scenario/seed after mutation and compare against Phase 0.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit; the packet checks named above; per leaf the deterministic scenario that prints its bar number and one normal-speed capture at the shipping camera when the claim is player-felt; `npm run check:all:smoke` for sim/save/render seams. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record.

## Review questions

- Which `FEEL_CONTRACT.md` bar did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, or give an NPC a hidden gyro?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism in the sim path, a golden moving without a causal record, or a passing route that reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after bar numbers recorded in the receipt with the scenario ID and seed; captures attached for player-felt leaves.
- [ ] Receipt written under `design/program/roadmap/receipts/PQ-141-<leaf>-REPORT.md`; queue unit state updated by the integrator.
