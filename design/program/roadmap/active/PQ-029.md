<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-029 — Practical Massline heads as toys: tractor, elastic whip, frame coupler

```yaml
queueId: PQ-029
lifecycle: ready
acceptance: unproven
packetRevision: 4
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (section 15 admitted)
identityAction: this packet supersedes the deferred one-line brief; legacy shared identities (T/SF/A/R/W aliases on the queue row) resolve to this packet when its first leaf is claimed, and the integrator replaces any colliding alias before promotion
```

## Outcome

Each head answers 'what can I do now?' in one sentence the fitting screen shows: the tractor picks things up and throws them; the elastic whip stores energy and snaps; the frame coupler locks two frames so a tow feels rigid. Each has a Range drill, a counterplay, and at least one NPC occupation that uses it in the world (a tug couples, a scavenger tractors).

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **A · The toys**, wave **BETA**. Gap it closes: Seven heads exist as ordinary purchasable modules with no arc; a player can finish the campaign using the rope as a tractor beam only. Reference: Just Cause tether upgrades, Bionic Commando, Hardspace: Shipbreaker tools. Routes through existing sources: SPEC3-17 tether & momentum verbs; GDX-A02.

## What exists (audited 2026-09-03)

`src/data/modules.js:115-141` seven heads; runtime flags `masslineHeadTractor`, `masslineHeadElasticWhip`, `masslineHeadFrameCoupler` (ON in production); `src/core/sg02DynamicBodyOwner.js` spring modes incl. `frame_coupler`; `src/ui/screens/range.js` three drills.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **Tractor head as a throw toy.** Pick up, spin, throw with the release rating; works on cargo, debris, drones, light hulls. | A Range drill teaches it in ≤ 60 s; throw speed ≥ 1.2× cruise on a light payload. |
| `.01` | **Elastic whip as stored energy.** Stretch stores energy, release snaps the payload or the player; readable strain glow; break by load. | A whip snap moves a light hostile ≥ 40 % of its cruise; the drill exists. |
| `.02` | **Frame coupler as the tow you can trust.** Rigid tow for the long haul; the coupled pair reads as one body in the physics and the camera. | A 200-mass tow holds through a 180° turn at 60 % cruise without oscillation. |
| `.03` | **NPCs use the heads.** Tugs couple, scavengers tractor, patrols net; the player sees the verbs in the world before buying them. | Each head is seen in ordinary traffic within 10 minutes at the reference site. |

## Entry conditions

- [ ] Dependencies integrated at the candidate base: `PQ-137`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario before tuning (the gate row in §15.1 this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario; record the before numbers for the §15.1 row this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` Tractor head as a throw toy.
- [ ] `.01` Elastic whip as stored energy.

### Phase 2 — owner wiring

- [ ] `.02` Frame coupler as the tow you can trust.

### Phase 3 — presentation and route

- [ ] `.03` NPCs use the heads.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `src/data/modules.js`
- `src/systems/tetherGameplay.js`
- `src/core/sg02DynamicBodyOwner.js`
- `src/ui/screens/range.js`
- `src/systems/npcJobsRuntime.js`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No new head models until PQ-050 fielding rules are met; no head that needs a second input scheme.

## Performance and quality budget

No new per-tick allocation in the sim; entity, collider, query, draw-call and residency growth bounded to the bodies the leaves name; save bytes bounded; the runtime witness is run before and after (`PQ-144` guard) and no new top frame-time bucket may appear. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:massline`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record.

## Review questions

- Which §15.1 gate row did this move, and by how much, in player units (screen depths, seconds, hull lengths, fraction kept/lost)?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, or give an NPC a hidden gyro?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the §15.1 row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Receipt written under `design/program/roadmap/receipts/PQ-029-<leaf>-REPORT.md`; queue unit state updated by the integrator.
