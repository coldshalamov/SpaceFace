<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-172 — Mods and data-driven content

```yaml
queueId: PQ-172
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-03
```

## Outcome

The community can add a ship, a weapon, an encounter or a sector by dropping JSON that the same validators check; determinism and save schema are protected by the loader. Workshop integration follows the Steam build.

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **F · Modes and replayability**. Gap it closes: All content is compiled ES modules; one JSON scenario. Reference: Starsector, RimWorld modding. Routes through existing sources: GDX-A23; S3-F9.

## What exists (audited 2026-09-03)

`src/data/` (135 modules), `47a.scenario.json`, data validators (`check-data.mjs`, `check-data-refs.mjs`).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | JSON content loader. Weapons, modules, encounters and places load from a user content directory through the existing validators; mods are listed in-app. | A sample mod adds a weapon and an encounter; determinism hash unaffected without the mod. |
| `.01` | Workshop. Steam Workshop publish/subscribe for the same directory. | One mod round-trips through Workshop. |

## Entry conditions

- [ ] `npm run check:baseline` green at the candidate base; dependencies integrated as listed in the queue row.
- [ ] Owner modules re-read; unknown owner contracts are blockers, not guesses.

## Non-goals

No script mods at launch.

## Checks and evidence

`npm run check:baseline`, `node scripts/check-data.mjs`; one normal-speed capture at the shipping camera per player-felt leaf; receipts under design/program/roadmap/receipts/PQ-172-*.
