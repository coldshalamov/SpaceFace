<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-180 — The frontend definition of done, applied to every surface: the grammar matrix

```yaml
queueId: PQ-180
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

There is one table, generated from the running game, that says for every surface — title, new game, load and save, settings, pause, game over, help, codex, mission log, the flight HUD, the Power Rail, the comms and wingman radials, THE SHIP, THE FOOTPRINT, THE RANGE, THE CHART, the seven station screens, the Crucible door, draft, refit, results and lab, Asteroid Works, base and claims, credits, statistics and photo mode — whether it meets each rule: type roles and the 12 px floor, tabular numerals on every figure, colour spent only on state, the motion contract and reduce-motion, the layout skeleton, three disclosure tiers, load-bearing names, the four data states, entity links, screen memory, responsive at three widths, pseudo-localised at +40 %, forced-colours, keyboard and gamepad reachability, and the UI frame budget. A check fails when any surface is below the floor, so 'polished' is a column of greens, not an opinion.

## Source

`CANONICAL_BUILD_MAP.md` §18 (admitted 2026-09-03) → **F · Frontend**, wave **ALPHA**. Gap it closes: The grammar exists and sixteen jobs landed for the four instruments and the HUD, but the station, Crucible screens, Asteroid Works, the meta shell and the legacy modules were never held to it; nothing measures adoption per surface. Reference: Design-system audits at studios that ship on consoles; the repo's own J16 visual-regression matrix. Routes through existing sources: §11 (J01–J16 done), §11.7, PQ-162 station, PQ-168 chart, PQ-130/131 Asteroid Works, PQ-165 accessibility, PQ-166 localization.

## What exists (audited 2026-09-03)

`design/frontend/INSTRUMENT_GRAMMAR.md` (§12 definition of done per screen), `A_LIST_GAPS.md` (15 standards), `scripts/capture-ui-matrix.mjs` + `check:visual-regression` (60 reference frames), `check:data-states`, `check:entity-links`, `check:ui-a11y`, `check:wcag-contrast`, `check:ui:perf`, `check:ui-frame-sleep`, `src/ui/screenMemory.js`, `entityResolver.js`, the localization pseudo-locale.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **The surface manifest.** A data file listing every surface with its entry route, owner file, archetype and the checks that can reach it; the matrix script boots the game, opens each surface and measures the rules it can measure (smallest font, numeral face, data states present, memory restored, safe-frame at three widths, pseudo-loc clipping, contrast, reduce-motion, DOM node count, frame cost). | `check:ui:grammar-matrix` prints the matrix for ≥ 30 surfaces and fails any below the floor; committed baseline. |
| `.01` | **The floor, written.** The exact thresholds per rule (12 px, +40 %, 1280/1920/2560, ≤ 1,500 DOM nodes per surface, ≤ 2 ms UI frame cost, four data states named) recorded once in the grammar and read by the check. | Thresholds live in one file; the check reads them; a deliberate violation goes red. |
| `.02` | **Ownership and order.** Every red cell is assigned to a packet (PQ-162 station, PQ-168 chart, PQ-181 meta shell, PQ-182 Crucible screens, PQ-130 works, PQ-183 links, PQ-184 UI perf) with the leaf that clears it; the matrix is the frontend's queue. | No red cell without an owner; the map §18 table mirrors the matrix. |
| `.03` | **Reference frames for every surface.** Extend the visual-regression matrix from 60 frames to every surface × default/reduced-motion/forced-colours/pseudo-loc × three widths; diff on change. | `check:visual-regression` covers every surface in the manifest. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Reviewing surfaces by eye and calling it a matrix: the matrix is generated from the running game or it is an opinion.
- Measuring in English only: pseudo-loc at +40 % is a column, not a footnote.
- Passing a surface because a check is green: three defects in this program were invisible to every check and visible on screen; the reference frames are the proof.

## Entry conditions

- [ ] Dependencies integrated at the candidate base: none.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario on a fixed seed before tuning (the bar this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, the fun-loop bench, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` The surface manifest.
- [ ] `.01` The floor, written.

### Phase 2 — owner wiring

- [ ] `.02` Ownership and order.

### Phase 3 — presentation and route

- [ ] `.03` Reference frames for every surface.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `scripts/`
- `test/`
- `design/frontend/`
- `src/ui/`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No redesign inside this packet; it measures and assigns. No new screens.

## Performance and quality budget

The matrix runs in the capture harness, never in the game loop; per-surface measurement ≤ 5 s. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:ui-effects`, `npm run check:ui:perf`, `npm run check:visual-regression`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

- Which gate row (§15.1, §16.1, §17.1, §18.1) did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden, on the default route?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, give an NPC a hidden gyro, scale hit points, or add a dialogue tree?
- Does the report read in the owner's words (§1.4)?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the gate row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-180-<leaf>-REPORT.md`; queue unit state updated by the integrator.
