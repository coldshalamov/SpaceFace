<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-204 — Deterministic smoothness algorithms

```yaml
queueId: PQ-204
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
dispatchPolicy: leaf_required
baseRequirement: re-read PERF_ADVANCED_CAMPAIGN.md; do not wait for a quiet-machine census
stableRoute: PERF-ADVANCED-SMOOTHNESS
```

## Outcome

The ordinary New Game / Ceres / swarm route stays the same picture and the same 60 Hz sim, but
NEAR work, spatial queries, Rapier islands, far NPC fights, shader families, and presentation lean
no longer scale with every body every tick. Hitches from first-use compile drain after present,
one program at a time.

## Leaves

| Leaf | What the player feels | Done when |
|---|---|---|
| `.00` | Quiet and crowded flight no longer fall back to scanning every collidable | Always-on grid, combat SoA, dirty journal, and a 16-token NEAR budget are live; focused tests prove them |
| `.01` | Distant traffic and parked hulls stop paying a full Rapier island | Non-player, non-projectile, unattached bodies may sleep; `physicsSleeping` survives save; off-glass hostiles resolve on a schedule |
| `.02` | A newly painted hull does not mint a unique program; leftover compiles do not hitch the photon | Packed-ORM uses the family-key canonicalizer; after-present compile batch is 1; lights stay occupancy-capped at 6+2 |
| `.03` | The picture does not change to chase a GPU number | Retained-slot batching, multi_draw, and cheaper bloom stay off |
| `.04` | Bank/pitch interpolate from the fence, not live entity objects | Snapshot lean columns packed; WASM/Worker/WebGPU not started |

## How agents get this wrong

Waiting for a quiet machine. Cutting bloom or emptying the sky. Dummy-prewarming shaders.
Enabling BatchedMesh because a catalog row exists. Sleeping Rapier without a save field.
Treating a technical receipt as a smoothness close.

## Write set

`src/core/`, `src/world/farEncounterOutcomes.js`, `src/world/farActorTable.js`,
`src/world/activityRuntime.js`, `src/systems/aiPorts.js`, `src/systems/aiFireIntent.js`,
`src/render/pipelineReadiness.js`, `src/render/snapshotFence.js`, `src/render/presentationSnapshot.js`,
`src/render/partsLibrary.js` (packed-ORM family key only), `src/core/presentationRunner.js`,
`test/pq-204-advanced-perf.test.mjs`.

## Non-goals

Quality cuts, ECS rewrite, Rapier sleep of the player or taut attachments, GPU batching,
WebGPU/Worker ports, emptying field rocks.

## Closing

Play New Game. If the photon hitch is gone and parked traffic no longer costs a full island,
stop. Do not store stills.
