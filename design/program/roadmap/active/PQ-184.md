<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-184 — UI performance and optimisation: frame budget, virtualisation, DOM and layout discipline, the legacy refactor

```yaml
queueId: PQ-184
lifecycle: ready
acceptance: unproven
packetRevision: 2
owner: unclaimed
baseRequirement: master at or after 2026-09-03 (sections 15–19 admitted)
```

## Outcome

The frontend is as optimised as the renderer: each surface stays inside a two-millisecond UI frame cost and a node budget at 1080p, long lists (market, contracts, ledger, codex) virtualise, no surface allocates per frame or thrashes layout, the legacy station hub's shared logic is lifted into modules and the dead renderers deleted, and the matrix reports the numbers so a regression is red the day it lands.

## Source

`CANONICAL_BUILD_MAP.md` §18 (admitted 2026-09-03) → **F · Frontend**, wave **BETA**. Gap it closes: One breakpoint until recently; no list virtualisation; no UI frame budget enforced per surface; a 4,057-line legacy hub still imported for helpers; the chart carries ~80 sub-12 px nodes. Reference: Any console-shipped UI with a frame budget. Routes through existing sources: §11.7 tier 3 #8, §11.10 Phase 7 refactor, PQ-162, PQ-168, PQ-180.

## What exists (audited 2026-09-03)

`check:ui:perf`, `check:ui-frame-sleep`, `src/ui/screens/stationHub.js` (legacy helpers), `station-workbench.css` (flattened), the matrix (PQ-180).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` (after PQ-180.00) | **The budgets, measured.** Per-surface UI frame cost and DOM node count in the matrix; thresholds in the grammar; regression fails red. | Matrix columns live; baseline committed. |
| `.01` | **Virtualise the long lists.** Market, contracts, ledger, codex and results lists render only visible rows. | A 2,000-row list stays under budget; keyboard navigation still reaches every row. |
| `.02` | **No per-frame allocation, no layout thrash.** Audit hot surfaces (HUD, chart, station) for per-frame DOM writes and forced reflows; fix by batching and state variables. | Frame cost under budget on all three; frame-sleep counters 0 when idle. |
| `.03` | **Lift the legacy hub.** Shared logic out of stationHub.js, market.js, bar.js, services.js, shipLedger.js, factions.js into modules; delete dead renderers; the import check confirms. | stationHub.js gone or < 300 lines of pure helpers; check:ui-screen-imports green. |

## How agents get this wrong

### Senior implementation direction (2026-09-06)

Keep visible-row rendering, idle sleep and shared owner logic. For `.01`, a 2,000-row fixture must
exercise the production list component, preserve selection by stable identity through sorting and
filtering, and reach the first/last row by keyboard with correct focus and accessible row counts.
For `.02`, measure UI work separately from whole-game frame intervals; an unchanged screen does
not rebuild DOM, while a changed price, target or selection still updates promptly. For `.03`,
remove dead presentation only after its consumers use explicit shared modules. The 300-line target
is a smell detector, not permission to split a monolith mechanically or change station behaviour.

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Optimising by removing content or density: the budget is met by structure, never by showing less.
- Deleting legacy modules before the live station stops importing them (the Phase 7 refutation).

## Entry conditions

- [ ] Dependencies integrated at the candidate base: `PQ-180`.
- [ ] `npm run check:baseline` green at the candidate base before the first edit; any entry red noted.
- [ ] Owner modules in the write set re-read and recorded; unknown owner contracts are blockers, not guesses.
- [ ] Existing behaviour characterised by a seconds-scale scenario on a fixed seed before tuning (the bar this packet moves, measured before).

## Live seams

See "What exists" above; the write set below is the bounded owner surface. Any file outside it is a shared-change request, not an edit.

## Player route and state machine

The default route (`production` profile, `assisted` flight, the starter hull unless a leaf names another). State lives with the existing single writers named in "What exists"; this packet adds intents, listeners and data, never a second writer. Save boundaries and exactly-once effects are called out per leaf in its done-when.

## Agent-observable scenario

Each leaf names a deterministic scenario (Motion Lab, `tools/agentic/scenarios.json`, the fun-loop bench, or a `node --test` fixture) that prints the number in its done-when, plus one normal-speed capture at the shipping camera for anything player-felt. Temporal claims need temporal evidence; a still proves appearance only.

## Work breakdown

### Phase 0 — characterization

- [ ] Reproduce the gap on the route with the scenario on a fixed seed; record the before numbers for the bar this packet moves.
- [ ] Owner/write map confirmed against the write set; one causal hypothesis per feel or performance defect.

### Phase 1 — pure contracts and data

- [ ] `.00` The budgets, measured.
- [ ] `.01` Virtualise the long lists.

### Phase 2 — owner wiring

- [ ] `.02` No per-frame allocation, no layout thrash.

### Phase 3 — presentation and route

- [ ] `.03` Lift the legacy hub.
- [ ] Replay the same scenario/seed after mutation; compare against Phase 0; capture at the shipping camera.

## Write set

- `src/ui/`
- `styles/`
- `scripts/`

Shared/global files (registry order, feature flags, save schema, the queue) require an integrator-owned change or an explicit lease.

## Non-goals

No framework migration; no rewrite of the HUD.

## Performance and quality budget

This packet IS the UI budget: ≤ 2 ms UI frame cost and ≤ 1,500 DOM nodes per surface at 1080p, virtualised lists, zero per-frame allocation on hot surfaces. Never reduce default content, population, effects, draw distance or visual quality as the mitigation.

## Verification budget

`npm run check:baseline` before and after every edit. Packet checks: `npm run check:baseline`, `npm run check:ui:perf`, `npm run check:ui-effects`, `node scripts/check-ui-screen-imports.mjs`. Per leaf: the deterministic scenario that prints its done-when number, and one normal-speed capture at the shipping camera when the claim is player-felt. Escalate to `npm run check:all:smoke` for sim/save/render seams; route-level headed evidence only when a claim cannot be proven headlessly. Never re-record a golden without the `docs/COMMON_BUGS.md` §8/§10d causal record. Run `node scripts/check-program-docs.mjs` after touching any packet or the queue.

## Review questions

- Which gate row (§15.1, §16.1, §17.1, §18.1) did this move, and by how much, in player units?
- Which existing system was connected before anything new was written?
- Is the outcome legible at the shipping camera with HUD text hidden, on the default route?
- Does any test now pin behaviour `design/VISION.md` forbids? Does any leaf add drag, clamp given momentum, give an NPC a hidden gyro, scale hit points, or add a dialogue tree?
- Does the report read in the owner's words (§1.4)?

## Stop conditions

- A required owner seam is missing or its contract is unknown → `BLOCKED` with a shared-change request, after every other leaf is finished.
- An exact-path collision with a live `NOW.md` row → work the disjoint leaf; never adopt the subsystem.
- Nondeterminism (`Math.random`, wall clock, host branching) in the sim path → stop and reduce.
- A golden moves without a causal record, or the only passing route reduces default quality → stop.
- Two failed repair cycles with the same causal model → record the falsified assumption and narrow the scenario.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Before/after numbers for the gate row recorded in the receipt with the scenario ID and seed.
- [ ] Capture(s) at the shipping camera attached for player-felt leaves.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-184-<leaf>-REPORT.md`; queue unit state updated by the integrator.
