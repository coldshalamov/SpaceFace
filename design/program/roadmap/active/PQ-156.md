<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-156 — Three starters, three verbs, and a save-file identity

```yaml
queueId: PQ-156
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-03
```

## Outcome

New Game offers three starters that are verbs: the brawler (ram plate, shove gun), the skater (light, fast, the best swinger), the tug (heavy line, tow class, slow). Each has a distinct first ten minutes. The load screen is a portrait of the save: hull with scars, titles, rap sheet, the ace who hates you.

## Source

`CANONICAL_BUILD_MAP.md` §15 (the finishing program, admitted 2026-09-03) → **C · Economy, progression, identity**. Gap it closes: One starter; the load screen is a list. Reference: Slay the Spire characters, Mount & Blade save cards. Routes through existing sources: PQ-142; GDX-A16.

## What exists (audited 2026-09-03)

`src/data/newGameDefaults.js`, `ships.js` (Hitch/Wasp/Pelican), `src/ui/screens/newGame.js`, `saveLoad.js`, `livingHull.js`, `titles.js`.

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | Three starters. Hitch (skater), a tug variant of Pelican, a brawler variant of Wasp; each with a kit and a sentence. | Three New Game routes complete the first ten minutes with distinct verbs; telemetry shows distinct funnels. |
| `.01` | The save portrait. Load screen renders hull + scars + titles + rap sheet + grudge. | Capture; save round-trip. |

## Entry conditions

- [ ] `npm run check:baseline` green at the candidate base; dependencies integrated as listed in the queue row.
- [ ] Owner modules re-read; unknown owner contracts are blockers, not guesses.

## Non-goals

No classes with locked abilities; any hull can buy any verb later.

## Checks and evidence

`npm run check:baseline`, `npm run check:new-game-first-run`, `npm run check:save-schema`; one normal-speed capture at the shipping camera per player-felt leaf; receipts under design/program/roadmap/receipts/PQ-156-*.
