<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-194 — Field Hardware: the UI production program

```yaml
queueId: PQ-194
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-10 (design/frontend/direction/FIELD_HARDWARE_PROGRAM.md admitted)
```

## Outcome

SpaceFace's interface is rebuilt to an A-list standard by production, not styling: rendered style
frames first, produced assets second (transparent-PNG kits, an SVG icon family and marks, 3D sets
and hull renders), code that assembles those assets to match the frames third, and a
picture-comparison review at every step. The direction is **Field Hardware** — manufactured,
backlit, hand-worn equipment laid over a lit living world, in three registers (POSTER, BENCH,
EDGE). The title goes live first as the owner's veto point; every surface follows on its own
packet; the sweep and a blind comparison close the program.

## Source

[`design/frontend/direction/FIELD_HARDWARE_PROGRAM.md`](../../../frontend/direction/FIELD_HARDWARE_PROGRAM.md)
(the program, method, routing, acceptance, floors), the art direction
[`packets/_COMMON/02_ART_DIRECTION.md`](../../../frontend/direction/packets/_COMMON/02_ART_DIRECTION.md),
the five sessions under [`sessions/`](../../../frontend/direction/sessions/README.md) with their 28 phase specs under [`packets/`](../../../frontend/direction/packets/README.md), and the
owner's directive of 2026-09-10 quoted in the program. Supersedes `PQ-187` (Cinematic Minimal) and
`build_map.md` §20.14 on aesthetics; the Cinematic Minimal units (`PQ-187.02/.03`, `PQ-181`,
`PQ-182`, `PQ-162`, `PQ-168`, `PQ-188`, `PQ-192`) are recorded as implemented under the old
direction and are not dispatched again.

## Why (the finding)

Ten passes since July converged on text in bordered boxes on a black ground because every pass
was CSS written in one session against a prose target, with no produced assets, judged against
docs the previous agent wrote. The code audit of 2026-09-10 added the engineering half: no screen
has a lit world behind it by design (any screen stack freezes rendering; the only 3D under a menu is
a second WebGL context refused on Intel GPUs), so "the world is the interface" never had machinery.

## Leaves

| Leaf | Outcome | Hand-off / lane | Done when |
|---|---|---|---|
| `.00` | The design system | **S1** (ChatGPT 6 Pro) | Six frames pass the two tests and the guard; the title shot picked and recorded in `approved/DECISIONS.md`; kits, icons, marks, motion, sound validated; the kit page and three hero prototypes open from `file://` and match their frames; accepted files committed under `approved/` and `assets/ui/kit/`. |
| `.01` | The UI stage — gate zero | **L-A** (local, P20) | Title, docking and Crucible door show a lit 3D world on the default route at 1280/1920/2560 via `--world` captures; one WebGL context; proven with the existing dock GLB before P16; authored plate fallback; flight frame cost unchanged. |
| `.02` | The bench register | **S2** (ChatGPT 6 Pro) | Six frames; twelve prototypes on the S1 kit with twelve-row tables at 1280; keyart and backdrop plates; draft GLBs in the preview; Blender scripts complete; accepted files committed. |
| `.03` | Blender sets and the hull render harness | **L-B** (local, P16) | Hangar, berth and arena sets render the approved plates from the frames' camera; a tile exists for every roster hull from one harness; material-truth evidence per the standard. |
| `.04` | The prototype app | **S3** (ChatGPT 6 Pro) | Every screen exists as a prototype and inside one navigable app by keyboard and gamepad; reduced motion and forced colours work; the audit passes or names each failure; `HANDOFF_TO_ENGINE.md` complete. |
| `.05` | The engine port, the kit runtime, the Title live — the veto point | **S4** (ChatGPT 6 Pro) then **L-C** (local, P21 + P22) | The port applies to the pinned commit with the static checks green; the kit runtime lands (asset-coverage grep passes, dead refit removed, lint lists updated); the 1920 `--world` capture of the title beside the approved frame passes the vision rubric; the owner has looked. |
| `.06` | The surfaces | **L-C** (local, P30–P38, parallel by mutex) | Each surface's capture passes the rubric beside its frame; named checks green; floors hold; every surface captured for S5. |
| `.07` | QA, motion, sound, sweep, proof | **S5** (ChatGPT 6 Pro) then **L-D** (local, P40–P42) | Punch list applied; assets v2 in; thirteen clips to the register numbers; cues wired and the mute default decided; dead skin deleted; baselines reshot; the blind tally meets its target; the reel exists. |
## Checks (all leaves)

`npm run check:baseline` · `npm run check:type-floor` · `npm run check:colour-tokens` ·
`npm run check:ui-effects` · `npm run check:command-deck-ui` · `node scripts/check-ui-screen-imports.mjs`
· `npm run check:ui:perf` · `npm run check:bundle` · `npm run check:ui-a11y` · `npm run check:wcag-contrast`
· `npm run check:responsive` · per-surface runtime checks named in each packet · `node
scripts/capture-ui-matrix.mjs --world --headed --only=<surfaces> --out=.devshots/frontend/<packet>`.

## Floors that no leaf may trade away

12 px type (incl. canvas), WCAG AA contrast, reduced-motion and flash-reduce, forced-colours,
keyboard/pad reachability with a visible focus ring, labelled icon-only controls, no idle rAF,
≤ 2 ms UI frame and ≤ 1,500 DOM nodes per surface, no `backdrop-filter` in flight, colour literals
as tokens, the simulation and determinism goldens untouched.

## Receipts

`design/frontend/direction/receipts/<PACKET>-REPORT.md` per packet; frame picks in
`design/frontend/direction/approved/DECISIONS.md`; the program receipt
`design/program/roadmap/receipts/PQ-194-REPORT.md` at close.
