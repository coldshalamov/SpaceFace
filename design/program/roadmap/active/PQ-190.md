<!-- LIFETIME: ACTIVE_PACKET -->
# PQ-190 — The style slice: stylized industrial energy, approved at the shipping camera

```yaml
queueId: PQ-190
lifecycle: ready
acceptance: unproven
packetRevision: 1
owner: unclaimed
baseRequirement: master at or after 2026-09-05 (the Studio Recovery Audit graded in CANONICAL_BUILD_MAP §15.9b); PQ-137.03 integrated (the camera the slice is judged at)
```

## Outcome

The game's WORLD look — hulls, rocks, machines, effects, the sky — is stated in one sentence and proven
on one small scene before any fleet-wide pass: **solid, readable industrial forms with expressive
luminous machinery and effects** — less polished metal, more authored light with a reason. The
starter, one contrasting enemy, one useful solid object, one landmark, one industrial machine and a
representative set of action effects are approved together at the shipping camera in quiet flight,
dense combat and the rover, within the measured budget; then material families, effect recipes, LOD
rules and regional composition profiles are derived from what was approved, so the next asset task
executes an observed style instead of re-interpreting "premium". Screens, HUD and type belong to the
frontend direction (`design/FRONTEND_DIRECTION.md`, `PQ-187`/`PQ-188`), not to this packet; the two
must read as one game, so this packet's approval sheet is reviewed beside the direction lock's.

## Source

The owner's current visual request (less shiny, more luminous, more expressive, more artistic) as
relayed by the 2026-09-05 Studio Recovery Audit Part 07, graded in `CANONICAL_BUILD_MAP.md` §15.9b;
`design/VISION.md`; the standing rulings that survive it: authored 3D form, no camera-facing
soft-square/disc stand-ins for designed objects (`docs/visual-assets/README.md`), the rover stays a
straight-on grid of real 3D objects (`design/ASTEROID_WORKS_DESIGN_LAW.md`), hash-bound visual review
(`VISUAL_ASSET_PRODUCTION_STANDARD.md`). The owner may veto any specific look; the packet's law is the
review context (the shipping camera, in motion, at density), not a palette.

## What exists (audited 2026-09-05)

`src/render/bloom.js` (HDR target, compact multi-scale bloom, strength 0.52 — already raised; not the
lever), `src/data/sectorVisualProfiles.js` (regional composition, lighting, post), the ACES-CDL grade,
`src/render/feel.js` (the emphasis coordinator: hitstop, trauma, FOV punch, speed-line ceilings),
`docs/visual-assets/VFX_TECHNIQUE_STANDARD.md` (one thin-plasma reference generalised into an
all-effects construction law — the thing `.01` revises), `VISUAL_ASSET_PRODUCTION_STANDARD.md`
(camera-first, independent review — keep), the `src/vfxnext` reference library, the deep-field
background (PR #109, 2026-09-05).

## Leaves

| Leaf | Outcome | Done when |
|---|---|---|
| `.00` | **The style slice.** Re-author material and light priorities for six things only — the starter hull, one contrasting enemy hull, one useful solid object (a cargo pod or rock), one landmark, one industrial machine, and the action effects they produce (engine state, a shove impact, a shield response, a loaded Massline) — toward matte or satin colour masses, controlled specular on edges/tools/glass, emission that explains a STATE (engines generating force, an energized tool, a loaded line, a blocked output) and never a permanent equally bright trim. Three attention levels (primary event / secondary bodies / atmosphere) allocated by state, not by permanently darkening the world. | Three normal-speed captures at the shipping camera with HUD text hidden — quiet flight, dense combat, the rover — read as one authored world; a critic that did not make the change names the player's action from frames alone; the runtime witness shows no new top frame-time bucket; the owner's yes on the captures (their veto is on the look, never on the review context). |
| `.01` (after `.00`) | **The effect-class matrix.** `docs/visual-assets/VFX_TECHNIQUE_STANDARD.md` is rewritten from one universal technique law into a per-effect-class table (engine jet · flight history · heavy impact · shield response · gas/smoke/dust · debris and cargo · Massline · background): preferred construction to prototype, what must survive at play scale, what to reject. Keep every rejection criterion the standard already has (hard clipping, blurry stand-ins, rigidly attached histories, no designed internal form); permit geometry, textures and hybrids in proportions the effect needs; the failed result is the placeholder appearance, not the technique. Revise a prohibition only where `.00`'s candidate demonstrated the better result at the camera. `check:vfx-techniques` and `SOFT_CARD_INVENTORY.json` stay the gate. | The standard's table has one row per effect class with the three columns; every existing effect family is assigned a class; `check:vfx-techniques` green; the derived recipes (material families, effect recipes, LOD rules, regional composition profiles) are written down once for the next asset task. |

## How agents get this wrong

Read this before touching code. The integrator rejects a unit that matches any line (§1.6, §19).

- Treating "glowy" as permission for transparent spheres or a radial-gradient blob as the whole effect: a stand-in is still a stand-in.
- Turning up a global bloom constant and calling it art direction: design the source radiance first (a thin bright core with structured falloff), reserve headroom for peaks.
- Flattening the rover or hiding detail to "stylize": stylization is authored form and light, not fewer polygons.
- Judging on an asset turntable: the proof is the mixed scene at the shipping camera, in motion, at density, with normal HUD.
- Restarting the whole catalog: six things, approved together, then derived rules. A whole-fleet remaster is what this packet prevents.
- Buying the look with performance: the witness runs before and after; no new top bucket.
- Touching screens or type: that is the frontend direction's.

## Entry conditions

- [ ] `npm run check:baseline`, `npm run check:vfx-techniques`, `npm run check:vfx-sleep` green at the base.
- [ ] `docs/visual-assets/README.md`, `VISUAL_ASSET_PRODUCTION_STANDARD.md`, `VFX_TECHNIQUE_STANDARD.md` read; the six things named with their current files before the first edit.

## Live seams

Materials and lights of the six named assets (their `src/render/` owners and `assets/` sources), `src/render/feel.js` (emphasis composition only), `src/data/sectorVisualProfiles.js` (one landmark composition), `docs/visual-assets/VFX_TECHNIQUE_STANDARD.md` (`.01`).

## Player route and state machine

The default route; the Crucible for dense combat, the adventure route for quiet flight, Asteroid Works for the rover.

## Agent-observable scenario

The three normal-speed strips (existing `captureFrameStrip` scenarios plus a rover capture) and the runtime witness before/after.

## Work breakdown

### Phase 0 — characterization
- [ ] Capture the six things as they are today, at the camera, in the three contexts.

### Phase 1 — pure contracts and data
- [ ] Material and light priorities written as data (families, not per-asset hacks).

### Phase 2 — owner wiring
- [ ] `.00` applied to the six things; emission keyed to state through existing owners.

### Phase 3 — presentation and route
- [ ] The three captures; the critic; the owner's yes; `.01` derived from what was approved.

## Write set

- the six assets' render owners and sources; `src/render/feel.js` (composition); `src/data/sectorVisualProfiles.js` (one composition)
- `docs/visual-assets/VFX_TECHNIQUE_STANDARD.md`, `docs/visual-assets/SOFT_CARD_INVENTORY.json` (`.01`)
- receipts and captures

## Non-goals

No fleet remaster; no new post pipeline; no palette law; no UI restyle (the frontend direction owns screens).

## Performance and quality budget

The witness before and after each leaf; no new top frame-time bucket; no default-quality reduction to buy the look.

## Verification budget

`npm run check:baseline`, `npm run check:vfx-techniques`, `npm run check:vfx-sleep`, `npm run probe:runtime-witness`, the three normal-speed captures.

## Review questions

- Can a stranger name the player's action from the frames alone?
- Does every luminous thing have a state and a reason?
- Did the frame-time witness move?

## Stop conditions

- The owner's veto on a look → record it, keep the review context, try the next candidate.
- The witness shows a new top bucket → stop and find the cost before continuing.

## Checkoff and receipt

- [ ] All entry conditions recorded.
- [ ] Diff stays inside the write set.
- [ ] Exit `npm run check:baseline` passes; nothing green at entry is now red.
- [ ] Captures at the shipping camera attached; the critic's verdict verbatim; the owner's yes recorded.
- [ ] Report written in the §1.4 format; receipt under `design/program/roadmap/receipts/PQ-190-<leaf>-REPORT.md`; queue unit state updated by the integrator.
