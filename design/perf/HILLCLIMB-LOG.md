<!-- LIFETIME: DURABLE --><!-- Append-only. One line per experiment. Numbers, not prose. -->
# SpaceFace performance hill-climb log

Scoreboard, measured with `npm run probe:runtime-witness` at `SPACEFACE_WITNESS_MS=60000
--no-sample-shots` and `npm run probe:shader-timeline --headed`:

| | metric | start | target |
|---|---|---|---|
| A | frames delivered per 60 s flight | ~3050 | 3400+ |
| B | DRAW-TIME-MISS shader links | 36 | 0 |
| C | long-task blocks >=2.5 s per 60 s | ~3 | 0 |
| D | JS heap floor after forced GC | ~486 MB | report only |
| E | excess ms per 1000 frames | ~559 | 300 |

Rules: one falsifiable hypothesis per experiment; smallest change that tests it; keep only if the
predicted number moved; a revert is a completed experiment, not a stop.

| # | hypothesis | change | A | B | C | D | E | verdict |
|---|---|---|---:|---:|---:|---:|---:|---|
| 0 | baseline, workers idle-to-busy (host CPU 33.8%) | none | 3124 | 36 | 3 | 485 | 629 | BASELINE |
| 1 | the +612MB world-population step is retained world data | none (diagnostic: --gc-timeline, forced GC at 3 points) | n/a | n/a | n/a | 134 -> 467 -> 484 | n/a | **REFUTED, and better than expected.** flight-entry 126->134 (nothing to reclaim), post-burst 1183->**467** (716 MB reclaimed), end 507->484. The burst is ~90% TRANSIENT GARBAGE; only ~333 MB is live world data. After one mid-flight collection the heap series reads 475 617 623 652 645 486 — it never climbs back to 1300. So the 1.3 GB plateau is V8 declining to collect, and the expensive majors are paid for ~716 MB of construction garbage made during world population. New target: that garbage. |
| 2 | the ~716MB of construction garbage is TypedArray/ArrayBuffer backing stores, invisible to V8's sampling heap profiler | none (diagnostic: --buffer-census wraps the typed-array constructors) | n/a | n/a | n/a | n/a | n/a | **CONFIRMED.** 10341 MB of typed-array/ArrayBuffer bytes over 10344 allocations in a 45 s run, against 32 MB total from the object profiler over a wider window. Backing stores ARE the bulk and the sampling profiler is blind to them. Caveat: views over a shared buffer are counted again, so the absolute figure is inflated; the ratio is not. Attribution: 10122 MB "vendor" (three/GLTFLoader frames only, no game frame on the stack), then **125.5 MB renderPackageLoader.js:861**, 49 MB canvasTextures.js:48, 12.5 MB canvasTextures.js:300, 12.3 MB canvasTextures.js:72. STOP RANKING ALLOCATORS WITH --alloc-probe; it measures ~0.3% of the bytes. |
| 3 | cutting per-tick economy/priceHistory allocation moves E or D | worker lane B (GLM 5.3): hoist the per-listing event scan to once per station per tick and share a frozen empty marker; replace the per-listing JSON.stringify pair in refreshListingDemand with structural comparison; stop re-cloning demandModel's frozen driver rows | 3108 | - | 3 | 485 | 697 | **REFUTED — no movement** (A 3124->3108, C 3->3, D 485->485, E 629->697, all noise; ext-scheduling hitches 9->17 explain E). Experiment 2 predicted this: the economy is ~0.3% of the bytes that drive the collections. KEPT anyway — behaviour-identical, economy suite unchanged at 112/103/9, all `demandDrivers` consumers read or clone so sharing the frozen rows is safe. Worker also corrected me: a point's `events` field is NOT dead, `scripts/check-market-chart.mjs` asserts live points carry active ids. |
| 4 | naming the vendor frames makes the 98% "vendor" bucket actionable | none (refined --buffer-census siteOf to name the vendor frame) | n/a | n/a | n/a | n/a | n/a | **YES, and it exposed an instrument bug.** 8256 MB of 8860 attributed to `meshopt_decoder.module.js:53` — which is `new Uint8Array(instance.exports.memory.buffer)`, a VIEW over the whole WASM heap taken once per decode call. A view reports the buffer's byteLength while allocating ~100 bytes. The census was counting views as allocations. |
| 5 | counting only buffers that OWN their bytes gives the real figure | census skips construction over an existing ArrayBuffer/SharedArrayBuffer | n/a | n/a | n/a | n/a | n/a | **CORRECTS EXPERIMENT 2.** Owned bytes are **251 MB / 6366 allocs**, not 10341 MB. Experiment 2's headline was inflated ~35x by views and is retracted; typed arrays are real but they are NOT the whole missing bulk (251 MB owned + 32 MB sampled objects vs ~716-854 MB reclaimed, so ~450-570 MB is still unattributed). New top real sites: 157 MB meshopt_decoder:188 (decode output, largely unavoidable), then **74 MB total in `src/render/canvasTextures.js` (:48 49 MB, :300 12.5 MB, :72 12.3 MB)** — procedural noise Float32Array scratch, the largest attributable GAME allocator. |
| 7 | warming the REAL authored materials (not stand-ins) closes the draw-time links | worker lane A2 (grok 4.6 xhigh): export a side-effect-free `peek` of the resolved part library, admit its 17 distinct live Kestrel materials as borrowed instanced+unique meshes in the global warmup | - | **36** | - | - | - | **REFUTED — reverted.** B unchanged at 36; contactAdded 32->29; and it ADDED 12 program links at load (173->185 total) because the warmup compiles what it admits. Cause: the peek can only see what is already decoded, which at post-opening is the player's own Kestrel — already covered by the opening plan. The 36 bricks come from OTHER ships and stations that decode later, and the peek deliberately never starts a load to chase them. The "warm the real subject" idea is right; the reachable set is wrong. Next attempt must widen WHAT is resident at warmup time, not how it is warmed. |
| 6 | pooling the procedural-noise scratch cuts real allocation and moves E | reuse module-scope Float32Array scratch for the noise field, accumulator and lattice in `canvasTextures.js` (one caller, result consumed before the next call, accumulator explicitly zeroed) | **3192** | - | 3 | 491 | **538** | **KEPT.** Owned typed-array bytes **251 -> 202 MB** (-20%), canvasTextures share 74 -> 29 MB — the predicted number moved, directly attributed by the census. Scoreboard: A 3124 -> 3192 (+2.2%), E 629 -> **538** (-14%), C 3, D 491. check:playable 16/16. |
