# Tombstone

The sequential-build-plan suite was retired in the 2026-09-08 owner-directed docs consolidation and
removed from the working tree; this file exists only because queue rows still name it as a source.
Retired plans are archaeology: retrieve what you need from git history, never from the working tree.
