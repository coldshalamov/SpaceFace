# Task Receipts

Each implementation prompt writes one machine-readable YAML receipt named `SF-XX.yaml`. The expected fields are described in every prompt and formalized in `../machine/receipt.schema.json`.

Receipts are evidence-bound resumability records, not marketing summaries. Retain failed commands, stale assumptions, unknowns, unproven visual claims, shared-change requests, and exact revision/worktree identity.
