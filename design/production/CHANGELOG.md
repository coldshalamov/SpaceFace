# Production-Spec Changelog

## 2026-07-10 — Initial durable suite

- Established `design/production/` as the persistent planning surface beneath `ALPHA_PROGRAM.md`.
- Added the external-completion production constitution and maturity ladder.
- Reconciled the prior professional build analysis into M0–M6 milestone refinements.
- Added persistent terminal-agent orchestration with continuation and role separation.
- Added asset profiles, meaningful review cycles, evidence integrity, and blind acceptance.
- Added the temporal Gameplay Observatory specification.
- Added a provisional cross-model capability matrix and required bake-off.
- Added multi-agent research/ideation and tutorial-to-technique-card workflow.
- Added an operational professional-quality standard, implementation backlog, goal/build/technique
  templates, and machine-readable candidate/reviewer/campaign schemas.
- Red-teamed and rejected the unsafe first draft; added SAFE-001 isolation, strict ACCEPTED/BLOCKED/
  continuation constraints, exact concurrency topology, stable coverage/classification/build-card
  schemas, and a phased capability bake-off.
- Restored the full 24-region M2 exit, fixed milestone clean-wave counts, and made observatory
  sampling, detector calibration, audio/full-session review, and sim-hash parity concrete.
- Added a provenance/color-space/downstream-ingestion pipeline for generated images and video.
- Recorded current reward-hacking evidence and open decisions.

## 2026-07-10 — Enforcement machinery and anti-laziness hardening

- Added `10_OBSERVATORY_HARD_GATES.md`: specifications for eventual mechanically enforced pass/fail
  thresholds across combat, lethality, mining, world/pacing, asset/presentation, and performance.
  An unimplemented applicable gate is `pending`, never `pass`, and blocks acceptance.
- Added `11_ENFORCEMENT_MACHINERY_SPEC.md`: implementation contracts for campaign-state manager,
  packet compiler, dispatch discipline tracker, observatory recorder, gate runner, blind review
  harness, asset validator repair, and truth registry generator.
- Added the first `schemas/dispatch-log.schema.json` and tracker candidate. Red-team subsequently
  proved self-reporting is gameable; PROD-004 now requires an automatic hash-chained action journal.
- Updated `WORK_PACKET.md` template: requires embedded reference captures (≥2 images) and
  observatory thresholds so the quality bar is re-grounded in-context after every compaction.
- Updated `02_ORCHESTRATOR_SPEC.md`: dispatch discipline has a persistent target contract; the
  current candidate is REVISE until automatic action capture/reconciliation is implemented.
- Updated `04_GAMEPLAY_OBSERVATORY.md`: explicit advisory→hard-gate promotion path with one-way
  escalation for safety gates.
- Updated `08_IMPLEMENTATION_BACKLOG.md`: added PROD-004 (dispatch tracker) and PROD-005 (truth
  registry) as first-wave packets.
- Motivation: the suite was sound design but unenforced documentation. These additions define the
  machinery; fresh hostile review prevents the definitions or passing toy fixtures from being
  mislabeled as already binding.

## 2026-07-10 — Candidate red-team and executable repair packets

- Rejected SAFE-001 despite 44/44 original fixtures; preserved two independent advisory rejections
  and created `packets/SAFE-001-REPAIR.md` with the union of hostile fixtures.
- Rejected the isolated PROD-001 candidate for fail-open metadata/lease parsing, weak placeholders
  and references, unbound acceptance hashes/identity/evidence, and acceptance before integration.
- Reframed PROD-004 as an automatic campaign supervisor/action journal rather than a manual counter.
- Added grounded `ASSET-001`, `ASSET-002`, `EVID-001`, and `OBS-001` implementation packets.
- Corrected observatory hooks to the live tactical/V3 event and registry seams, added three-run
  passivity/overhead comparison, and made media-pending OBS-001 records acceptance-invalid.
- Split admired/failure reference arrays, added artifact/cycle hashes, and made JSON Schema
  explicitly shape-only beneath a controller semantic trust layer.
- Added machine-resolved dependency receipts/snapshot hashing, post-integration terminal acceptance,
  controller-wrapped worker/reviewer payloads, and evidence-backed gate N/A waivers.
- Added `EVID-002` so tightening the artifact contract cannot strand or silently grandfather the
  eight current path-only Alpha records.
- Added separate player-facing and control-plane card modes: 2+1 real media for player work, or 2+1
  hash-bound good/hostile technical controls for machinery—never an ungrounded exemption.

## 2026-07-12 — Controller waiver and live-program reconciliation

- Froze SAFE-001 repair-2 under a controller waiver at 88/88 current fixtures; retained remaining
  findings as known P2 control-plane debt and ended further SAFE review cycles in this campaign.
- Reconciled the live roadmap so M2 remains complete while M1 and M3–M6 reflect substantial active
  implementation rather than remaining falsely queued behind M0.
- Removed the dangling `check:revamp:evidence` package alias instead of restoring its branch-only
  iteration-count scoring script; current asset classification/evidence gates supersede that
  reward-hack-prone contract.
