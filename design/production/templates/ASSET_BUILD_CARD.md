# ASSET BUILD CARD `<ASSET_ID>` — `<PLAYER-FACING ROLE>`

> Compile this card before authoring. Any unresolved placeholder is a dispatch failure. The compiler
> emits JSON validated by `design/production/schemas/asset-build-card.schema.json`; this Markdown is
> the human review surface.

## Identity and exposure

- Asset kind:
- Controller-derived exposure tier (`hero | cast | standard | background`):
- Profile-selection evidence:
- Family / variant classification:
- Canonical source / manifest / runtime map:
- Player distance, screen size, frequency, and critical views:
- Gameplay purpose and interaction anchors:
- SpaceFace identity constraints:

## Reference panel

- Admired reference and exact quality to extract:
- Second reference and exact quality to extract:
- Failure examples to avoid:
- Licensing/provenance record:

## Outcome hierarchy

- Primary silhouette / massing:
- Secondary construction / role forms:
- Tertiary detail and game-camera readability:
- Material zones and authored surface information:
- Faction, history, wear, repair, asymmetry:
- Motion, life, crew/function cues:
- Family distinction and reuse limits:

## Technique applicability

| Technique card | Applicability | Trigger/reason | Required proof |
|---|---|---|---|

Applicability is exactly one of `required`, `conditional`, `not_applicable`, or `forbidden`; every
row carries a controller decision hash approved before authoring. The author cannot select multiple
columns, self-approve N/A, or retroactively mark a difficult technique unnecessary.

## Runtime contract

- Units, transforms, origin, pivot, bounds:
- Semantic material names and map roles:
- LOD count, thresholds, reduction, silhouette requirement:
- Collision proxy:
- Sockets, mounts, docking/interaction anchors:
- Animation/rig/shape keys actually consumed:
- Triangle, material, texture, draw-call, memory budgets:
- Browser/Electron and fallback/reachability proof:

## Required evidence

- Reproducible clay / neutral-lit / close / profile-specific Blender views:
- Held-out reviewer angles:
- Actual game-camera frames:
- Runtime approach/action clip:
- Source/GLB/map hashes and delta report:
- Loader, LOD, collision, performance, and observatory evidence:

## Defect definitions and review budget

- Candidate-specific P0/P1 defects:
- Minimum meaningful macro-cycles (computed floor, not author-selected):
- Controller-owned cycle-ledger path/hash; each cycle binds measured defect, substantive source/
  candidate before/after hashes, repair technique, and new evidence:
- Blind comparison benchmark:
- Independent reviewer quorum:
