# GENERATED MEDIA PACKET `<PACKET_ID>` — `<PLAYER OUTCOME>`

> Compile before generation; unresolved placeholders fail dispatch. The worker submits candidates
> and cannot mark them production-final.

## Downstream target

- Target kind:
- Media/channel role:
- Consumer (`Blender | UI | VFX | environment | cinematic reference`):
- Color space / dimensions / alpha / duration / fps:
- Candidate staging path:

## Player-facing purpose and family constraints

## Shared prompt and negative constraints

- Positive prompt:
- Forbidden content, baked text/logos/light, artifacts, or imitation:
- Separation, orthographic, mask, tiling, or editability requirements:

## Reference and rights record

- Sources:
- Licenses/provider terms:
- Provenance/disclosure requirement:

## Generator set

- Provider/model/version:
- Seed/settings when exposed:
- Identical-input confirmation:

## Technical and downstream acceptance

- Role/color/channel checks:
- Family and artifact checks:
- Required Blender/UI/VFX/runtime application:
- Required evidence and blind review:

## Output

Validate every candidate against
`design/production/schemas/generated-media-manifest.schema.json`. Allowed worker lifecycle values
are `concept_only` and `production_input`; only the orchestrator may promote an integrated hash.
