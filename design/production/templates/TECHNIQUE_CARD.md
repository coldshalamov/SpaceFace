# TECHNIQUE CARD `<TECHNIQUE_ID>` — `<PROBLEM SOLVED>`

**Status:** research | proof-candidate | accepted | rejected | superseded

The compiler emits JSON validated by
`design/production/schemas/technique-card.schema.json`; this Markdown is the review surface.

## Why and when

- Observable problem solved:
- Eligible asset profiles:
- Trigger condition:
- Cases where this technique is unnecessary or harmful:

## Method

- Blender version/add-ons/prerequisites:
- Source-object/modifier/node/UV/map procedure:
- Order-of-operations constraints:
- Parameters that must remain art-directed rather than copied blindly:

## Export and runtime survival

- What must exist in the `.blend`:
- What survives GLB export:
- Three.js material/animation/runtime consumer:
- LOD/draw-call/texture/memory implications:
- Required fallback or bake where Blender-only behavior cannot survive:

## Evidence

- Source/tutorial/manual provenance:
- Transcript or notes path:
- Proof asset and source/GLB hashes:
- Blender before/after views:
- Actual in-game before/after clip:
- Technical and independent visual verdict:

## Failure modes

- Known artifacts:
- Common lazy/false-positive implementation:
- Validation that detects failure:
- Superseded/rejected reason, if applicable:
