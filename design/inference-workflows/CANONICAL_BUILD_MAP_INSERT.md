# Stable Canonical Build Map Route — Inference Workflows

Insert this paragraph in `CANONICAL_BUILD_MAP.md` near the existing professional reference-sector route if the workflow library is retained:

```md
**Inference-to-convergence production workflows:** when the explicit task is to expand, diversify,
improve, recover, or multiply a game domain through a reusable `1x` / `3x` / `5x` creative-production
loop, start at [`design/inference-workflows/`](./design/inference-workflows/README.md). `Nx` counts
independently reviewed **accepted production units**, not files, rows, recolors, attempts, commits, or
source-only candidates. The workflows supply SpaceFace-specific divergence, reference-mechanism
transfer, implementation through current owners, cold adversarial review, revision/rebuild/cut,
portfolio integration, and learning capture. They are supporting production doctrine, not a lease,
queue claim, or permission to bypass `program-dispatch`. Begin from current ordinary-player truth and
the selected active packet; never bulk-promote incubator packs, create a parallel gameplay/rendering
authority, or call green tests and isolated source candidates a better game.
```

This file is a stable insertion source, not proof that the route has been merged into the canonical map on the current base.
