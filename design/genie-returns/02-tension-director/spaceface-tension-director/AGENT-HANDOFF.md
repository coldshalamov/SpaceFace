# Handoff to the local SpaceFace integration agent

Implement the remaining HOST wiring for the supplied tension-director package, then run the
real repository checks. Do not replace this implementation with a timer that emits extra spawns.
The controller's central invariant is that requested drama is not evidence of delivered play.

Begin with `README.md`, `INTEGRATION-NOTES.md`, and `TEST-REPORT.md`. The `repo/` tree contains
complete modified/new source, but its registry and encounter consumer are based on the uploaded
packet. Current master may have unrelated changes, including work from other packets. Use the
context patches or `tools/install.mjs` dry run. Do not overwrite current full files with old
snapshots to make a merge easier. Preserve all existing system IDs and their relative order.

The package installer handles the browser lookup and actual production init/update/calendar
manifest arrays. Locate YOUR headless production system map and add a per-world factory instance.
Confirm the resolved Node/browser identities agree rather than claiming a focused system list
is the production manifest. Keep pure legacy-profile goldens unchanged unless explicitly opted in.

Locate the actual save serializer/deserializer and add the owner's JSON snapshot at the same
boundary as simulation time and the world state. Restore it using the documented port/event
route; test exact continuation and honest legacy-save reset. Add trade/mission observations only
where a real successful receipt exists. Do not infer sales from credits or fabricate a new bus
event that no producer emits.

Run package tests, then existing game regressions, then the actual production hunter/prospector/
improviser sessions with seeds 4242 and 8008. Carry all original program-compass acceptance bars
forward. Report requested-versus-observed pressure, missing delivery, choices with alternatives,
closed causal chains, deaths, harassment, and profile/manifest identity. Leave missing evidence
null with a reason. A phase transition is not a player choice; a telegraph is not a spawned squad;
a spawned squad is not necessarily a perceptible encounter.

Protect these invariants when resolving interface drift: one owner per state subtree; no global
RNG consumption; bounded telemetry; no stat scaling, reward grants, direct spawns, despawning to
fake relief, or bypasses around authored gates/spawn budgets. Keep the fractional-tick undock
regression: the grace lease must exist before the next one-second decision without minting time.

Deliver the host-wiring diff and actual test/run evidence. Distinguish package/component evidence
from game-wide evidence and measured enjoyment. If the catalog runs dry, identify that owner;
do not increase pressure gain or redefine the metric to turn absence into success.
