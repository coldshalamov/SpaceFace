Reading additional input from stdin...
OpenAI Codex v0.130.0-alpha.5
--------
workdir: C:\Users\93rob\Documents\GitHub\SpaceFace
model: gpt-5.5
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019f2ad2-7d85-7b42-8505-7e36d49fc757
--------
user
# TASK: Parallax depth stack (SpaceFace WS-E1) — background depth

You are Codex in the SpaceFace repo. Read `design/GDD_2_0.md` §9.1 and `design/BUILD_PLAN_2_0.md`
(wave-2 status — sector palettes just landed; `state.render.sectorPalette` exposes a `dust` color).
Study `src/render/starfield.js` (existing skydome), `src/render/renderer.js` (scene setup, palette
lerp), `src/render/vfx.js` _initPools for the pooling idiom (do NOT edit vfx.js — it changed today).

## Build exactly this — new file `src/render/parallaxLayers.js` + minimal renderer hook
1. Three camera-relative layers between skydome and play plane (XZ world, camera looks down at 60°):
   - FAR dust sheets: 2 large additive planes (~3000 wu) at y = -140, parallax factor 0.22 — soft
     nebula wisp textures generated on canvas (reuse the starfield's canvas-texture approach, 512px,
     2-3 giant soft blobs, tinted by `state.render.sectorPalette?.dust` or fallback #35406a).
   - MID debris: InstancedMesh of ~120 tiny tetrahedra (radius 0.6–2.2 wu) at y = -40, factor 0.55,
     slow individual tumble (rotation only, no per-frame allocation — store axis/speed in arrays).
   - NEAR speed-motes: ~200 additive point sprites at y = +26 (ABOVE the play plane, they read as
     foreground), factor 1.35. While the player is boosting (`state.entities.get(playerId)?.flags?.boosting`)
     or moving > 200 wu/s, stretch motes along the velocity direction (scale, not new geometry) —
     this is the layer that SELLS speed.
2. Parallax mechanics: each layer group's position = cameraTargetXZ * (1 - factor) with wrap-around
   tiling (modulo a tile size per layer) so the field never runs out. Read the camera target from
   the same source camera.js uses (find its follow target; do not modify camera.js).
3. Palette coupling: on `jump:arrive` (or when `state.render.sectorPalette` object identity changes),
   retint far-sheet materials + mote color toward the palette's `dust`/`nebulaTint` over ~1.5 s
   (preallocated Color lerp, mirror the renderer's palette lerp pattern).
4. `settings.video.motionReduce === true` → halve mote count and disable boost-stretch.
   `settings.video.particleQuality === 'low'` → halve all layer densities.
5. Renderer hook: init from renderer.js where starfield is created (≤4 lines) and call
   `parallaxLayers.update(dt)` from the render-phase update where starfield/vfx update runs.
6. New `scripts/check-parallax-layers.mjs`: node --check style import test + assert the module
   exports {init, update, dispose} and that constructing with a stub scene creates 3 groups with
   expected instance counts under both quality settings. Add `check:parallax` npm script.

## Constraints
- Files: NEW `src/render/parallaxLayers.js`, `scripts/check-parallax-layers.mjs`, package.json (one
  line), `src/render/renderer.js` (≤6 lines of hook). Do NOT touch: vfx.js, starfield.js internals
  (import its texture helper only if it's exported), camera.js, any sim file, any UI file.
- Zero per-frame allocations (preallocate arrays/colors/scratch). All additive materials
  depthWrite:false. Total new draw calls ≤ 5 (instancing + merged sheets).
- No new deps. Respect the existing render-order (skydome behind everything).

## Verify
```
node scripts/check-parallax-layers.mjs && npm run check:non-graphics 2>&1 | tail -5
```
(check:non-graphics currently has known-red 47a tape gates unrelated to you — report its tail, do
not chase those.) Write the files. 10-line summary max.
2026-07-04T01:51:06.260384Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.notion.com/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
2026-07-04T01:51:06.380773Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer resource_metadata=\"https://mcp.figma.com/.well-known/oauth-protected-resource\",scope=\"mcp:connect\",authorization_uri=\"https://api.figma.com/.well-known/oauth-authorization-server\"" })
2026-07-04T01:51:06.866566Z ERROR codex_memories_write::phase2::job: failed to claim job: error returned from database: (code: 1) no such table: jobs
codex
I’ll ground this in the SpaceFace notes first, then read the current design/render files before touching anything. Since this is Three.js render work, I’m also going to use the local threejs skill lightly for its workflow guardrails.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"SpaceFace|parallax|starfield|renderer|check:non-graphics|sectorPalette|BUILD_PLAN_2_0\" C:\\Users\\93rob\\.codex\\memories\\MEMORY.md" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 845ms:
2:scope: Use for `C:\Users\93rob\Documents\GitHub\Vamp` visual-rigor fixes where authored atlases look giant, reuse the same silhouette, or go dark under the current grade, and the user wants live renderer truth plus non-launching verification only.
3:applies_to: cwd=C:\Users\93rob\Documents\GitHub\Vamp; reuse_rule=reuse for visual pipeline audits, atlas/material routing fixes, silhouette/readability regression work, and bounded evidence refresh in this checkout; re-check the current live renderer, atlas contract, and evidence scripts before assuming the same file seams or metrics still hold.
9:- rollout_summaries/2026-07-03T08-44-59-mxlc-vamp_authored_atlas_readability_and_silhouette_fix.md (cwd=C:\Users\93rob\Documents\GitHub\Vamp, rollout_path=C:\Users\93rob\.codex\sessions\2026\07\03\rollout-2026-07-03T04-45-04-019f2727-127f-7171-acfd-1068bccc5356.jsonl, updated_at=2026-07-03T08:57:06+00:00, thread_id=019f2727-127f-7171-acfd-1068bccc5356, identified the live renderer/material seams and separated the dark-grade + same-silhouette regressions from stale visual-revamp docs)
43:- `src/present/CharacterAtlas2D.gd` is the live actor renderer; `tools/visual/CharacterAtlas2D.gd` can drift into a stale integration copy and should be synced if the live atlas/material logic changes. [Task 1][Task 2]
47:- Bulwark currently maps to `swat` for silhouette purposes, while choir maps to `thrall`, stalker to `hunter`, and mortar to `gunner`. `test/unit/test_visual_assets.gd` is the cheap regression gate for atlas size, renderer binding, shader routing, and silhouette-key selection. [Task 2]
54:- symptom: visual-revamp docs point one way while the current game assets/tests point another. cause: older visual-revamp docs no longer match the live renderer contract. fix: treat live code, current metrics, and current tests as truth before trusting older docs. [Task 1]
163:- The Desktop process can stay alive with renderer/GPU/utility/crashpad children while the localhost sidecar appears hung; quick `Invoke-WebRequest` probes to candidate ports are a useful first health check before broader repair steps. [Task 4]
661:# Task Group: SpaceFace mission-intro stabilization, landing pivots, and PR closeout
662:scope: Use for `C:\Users\93rob\Documents\GitHub\SpaceFace` wrap-up or stabilization runs where the user changes from open-ended autonomy to "take it in for a landing", wants the already-started fix finished without breaking the game, and needs branch/PR closeout with honest gate handling.
663:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for landing-phase stabilization, mission-intro debugging, and PR closeout in this checkout; re-check current branch state, mission scripts, and whether the graphics/art lane is intentionally locked before reusing the same gates or merge expectations.
669:- rollout_summaries/2026-06-27T08-33-40-k4Ad-spaceface_merged_pr87_story_intro_landing.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T04-33-42-019f0836-8c2b-7e23-9fcd-cfb2515025f5.jsonl, updated_at=2026-06-29T05:32:16+00:00, thread_id=019f0836-8c2b-7e23-9fcd-cfb2515025f5, merged PR #87 after tightening canonical Beat 4 intro gating and refreshing stale cached boards)
673:- SpaceFace, PR #87, Beat 4, story intro, mission standing ladder, missionPreflight, missions.js, check-mission-standing-ladder.mjs, npm run check:non-graphics, gh pr merge, advisor-artifacts
683:- `npm run check:non-graphics` is the right stabilization gate here when the graphics/art lane is intentionally locked. [Task 1]
765:- Boot.tscn, GameRenderer.gd, Sim.gd, CueBus.gd, UIManager.gd, SaveSystem.gd, gameplay_input_blocked, save/continue resync, renderer churn, cue modality collisions, RunGutSafe.ps1
779:- High-value first-suspect bug areas in this checkout are unbounded queues/history, menu/input blocking, save/continue scene resync, combat/status math, feeding cue payloads, NPC/heat logic, renderer churn, and cue modality clobbering. [Task 2]
790:# Task Group: SpaceFace dirty-tree review, perf-probe repair, gate hardening, and review-first boundaries
791:scope: Use for `SpaceFace` review requests where the repo has a dirty tree and the user wants the uncommitted change set inspected rigorously, with findings-first behavior, narrow follow-on fixes when requested, and the right repo gates treated as the confidence surface.
792:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for dirty-tree review, focused UI/perf repair, cross-cutting UI/render/gameplay validation, and review-vs-fix boundary decisions in this checkout; re-check the current dirty tree and gate scripts before assuming the same file set or pass surface.
798:- rollout_summaries/2026-06-27T04-45-37-wpld-spaceface_dirty_tree_review_then_perf_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T00-45-42-019f0765-c41c-7b50-9605-e1ec9d3b56a7.jsonl, updated_at=2026-06-27T04:49:01+00:00, thread_id=019f0765-c41c-7b50-9605-e1ec9d3b56a7, narrowed the dirty tree to four unstaged files and surfaced the single actionable perf-probe finding)
799:- rollout_summaries/2026-06-22T07-30-16-qdoa-spaceface_dirty_tree_review_and_gate_hardening.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T03-30-19-019eee3c-b5b6-7a63-9551-f235b6af6b2e.jsonl, updated_at=2026-06-22T08:47:10+00:00, thread_id=019eee3c-b5b6-7a63-9551-f235b6af6b2e, inventoried modified plus untracked files and treated the whole worktree as review scope)
809:- rollout_summaries/2026-06-27T04-45-37-wpld-spaceface_dirty_tree_review_then_perf_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T00-45-42-019f0765-c41c-7b50-9605-e1ec9d3b56a7.jsonl, updated_at=2026-06-27T04:49:01+00:00, thread_id=019f0765-c41c-7b50-9605-e1ec9d3b56a7, restored live Blender/export process hints as an active ownership signal in the perf probe)
819:- rollout_summaries/2026-06-22T07-30-16-qdoa-spaceface_dirty_tree_review_and_gate_hardening.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T03-30-19-019eee3c-b5b6-7a63-9551-f235b6af6b2e.jsonl, updated_at=2026-06-22T08:47:10+00:00, thread_id=019eee3c-b5b6-7a63-9551-f235b6af6b2e, mapped the touched UI/render files to the scripts and runtime contracts that actually exercise them)
829:- rollout_summaries/2026-06-27T04-45-37-wpld-spaceface_dirty_tree_review_then_perf_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T00-45-42-019f0765-c41c-7b50-9605-e1ec9d3b56a7.jsonl, updated_at=2026-06-27T04:49:01+00:00, thread_id=019f0765-c41c-7b50-9605-e1ec9d3b56a7, closed the narrow perf-probe repair with `node --check`, `node scripts/check-ui-frame-sleep.mjs`, and `npm run check:ui:perf`)
830:- rollout_summaries/2026-06-22T07-30-16-qdoa-spaceface_dirty_tree_review_and_gate_hardening.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T03-30-19-019eee3c-b5b6-7a63-9551-f235b6af6b2e.jsonl, updated_at=2026-06-22T08:47:10+00:00, thread_id=019eee3c-b5b6-7a63-9551-f235b6af6b2e, fixed real runtime/asset issues and closed with `npm run check` plus `git diff --check`)
849:- `npm run check` is the durable high-confidence gate for broader cross-cutting `SpaceFace` changes, and `git diff --check` is still worth running at the end even when it only reports CRLF warnings. [Task 4]
860:- symptom: supposedly minor asset/runtime edits still break behavior. cause: the touched files affected runtime reset, authored-part loading, or renderer shadow bookkeeping, which diff stats alone do not reveal. fix: trace the supporting validators and run the full gate suite when the patch crosses UI/render/system boundaries. [Task 3][Task 4]
975:# Task Group: SpaceFace performance hardening and visual probe verification
976:scope: Use for `C:\Users\93rob\Documents\GitHub\SpaceFace` perf passes where the user wants real optimization without reducing visible quality, parallel subsystem investigation when justified, and proof that browser visual verification still works after render-path changes.
977:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for performance optimization, flight visual-proof fixes, and repo-wide post-perf validation in this checkout; re-check current browser probe scripts and perf artifact schema before reusing exact thresholds or file names.
983:- rollout_summaries/2026-06-24T21-50-11-oe73-spaceface_performance_hardening_and_visual_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\24\rollout-2026-06-24T17-50-15-019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d.jsonl, updated_at=2026-06-25T06:56:19+00:00, thread_id=019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d, fixed the key perf/proof landmine by moving the visual probe off preserved backbuffer assumptions)
987:- SpaceFace, performance, preserveDrawingBuffer:false, probe-flight-visual.mjs, pngjs, screenshotCanvas, readPixels, GPU stall due to ReadPixels, check:flight:clean, #gl-canvas, no-value-loss
993:- rollout_summaries/2026-06-24T21-50-11-oe73-spaceface_performance_hardening_and_visual_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\24\rollout-2026-06-24T17-50-15-019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d.jsonl, updated_at=2026-06-25T06:56:19+00:00, thread_id=019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d, ended with repo-wide checks and careful reporting of required-budget pass versus stretch-target miss)
1015:- symptom: the flight visual proof fails immediately after a renderer perf change. cause: the probe assumed preserved WebGL backbuffer state after `preserveDrawingBuffer` was intentionally turned off. fix: verify from a clipped browser screenshot instead of re-enabling the expensive backbuffer-preservation mode. [Task 1]
1019:# Task Group: SpaceFace Figma-first UI design workflow and autonomy boundary
1020:scope: Use for `C:\Users\93rob\Documents\GitHub\SpaceFace` UI-design workflow questions where the user wants to know whether Figma helps materially, what can be generated there autonomously, and where the boundary sits between visual spec work and repo/browser verification.
1021:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for SpaceFace design-tooling clarification and UI-spec planning in this checkout; keep runtime/perf/launcher claims anchored to the repo/browser path rather than treating Figma as proof.
1023:## Task 1: explain how Figma helps SpaceFace design work, success
1027:- rollout_summaries/2026-06-27T04-50-14-W4Sv-spaceface_figma_ui_design_autonomy_clarification.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T00-50-20-019f0769-fd7d-7351-904e-5c588b19d743.jsonl, updated_at=2026-06-27T04:53:01+00:00, thread_id=019f0769-fd7d-7351-904e-5c588b19d743, grounded Figma's value in SpaceFace-specific screens and the repo's proof culture)
1031:- SpaceFace, Figma, design planning and comparison surface, HUD, market, shipyard, mission log, pause/settings, browser/Electron proof, npm run check
1037:- rollout_summaries/2026-06-27T04-50-14-W4Sv-spaceface_figma_ui_design_autonomy_clarification.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T00-50-20-019f0769-fd7d-7351-904e-5c588b19d743.jsonl, updated_at=2026-06-27T04:53:01+00:00, thread_id=019f0769-fd7d-7351-904e-5c588b19d743, made the design-generation boundary explicit with a capture -> Figma -> code -> verification loop)
1050:- For SpaceFace, Figma is best treated as a design planning/comparison surface or visual-spec workbench, not as a runtime/gameplay verification tool. [Task 1]
1052:- The plugin can autonomously create Figma-side artifacts such as HUD mockups, menu screens, modal layouts, button/input/table components, visual states, and a reusable SpaceFace UI kit. [Task 2]
1057:- symptom: a tooling answer drifts into generic design-tool praise. cause: the answer is not anchored to the repo's actual UI surfaces and verification culture. fix: frame the answer around SpaceFace screens and the existing browser/Electron proof path. [Task 1]
1203:scope: Use for `Vamp` backend-port requests where the user wants the Godot repo, not `SpaceFace`, treated as the active workspace; wants strict frontend/backend separation, deterministic sim rules, and enough verified backend coverage to hand off cleanly to a frontend/UI agent.
1206:## Task 1: correct the workspace and confirm `SpaceFace` was untouched, success
1210:- rollout_summaries/2026-06-22T04-39-02-kxcW-vamp_godot_deterministic_vertical_slice_handoff.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T00-39-07-019eed9f-f042-78e2-a813-93accadc8c66.jsonl, updated_at=2026-06-22T05:11:45+00:00, thread_id=019eed9f-f042-78e2-a813-93accadc8c66, corrected the initial repo mix-up and proved the SpaceFace tree was only pre-existing dirty work)
1214:- Vamp, SpaceFace, wrong repo, proper workspace, git status --branch --short, git diff --stat, project.godot, legacy/js, read-only verification
1220:- rollout_summaries/2026-06-22T04-39-02-kxcW-vamp_godot_deterministic_vertical_slice_handoff.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T00-39-07-019eed9f-f042-78e2-a813-93accadc8c66.jsonl, updated_at=2026-06-22T05:11:45+00:00, thread_id=019eed9f-f042-78e2-a813-93accadc8c66, rebuilt the deterministic sim slice and verified boot/move/feed/fight/heat/escape with GUT)
1240:- rollout_summaries/2026-06-22T04-39-02-kxcW-vamp_godot_deterministic_vertical_slice_handoff.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\22\rollout-2026-06-22T00-39-07-019eed9f-f042-78e2-a813-93accadc8c66.jsonl, updated_at=2026-06-22T05:11:45+00:00, thread_id=019eed9f-f042-78e2-a813-93accadc8c66, packaged the verified context into a pasteable handoff with the goal and proper repo path)
1248:- when the user corrects `this wasn't SpaceFace, it was supposed to be the Vamp repo`, treat repo/workspace corrections as hard stops and verify the active project before editing. [Task 1]
1257:- `SpaceFace` had unrelated pre-existing dirty UI/story changes during the first run, but that session did not write there; the substantive work belonged in `C:\Users\93rob\Documents\GitHub\Vamp`. [Task 1]
1273:# Task Group: SpaceFace goal prompt, SG-04 release asset pipeline, and live browser QA
1274:scope: Use for `SpaceFace` work where the user wants a persistent long-running goal framed from `MASTER_MAKEOVER_PLAN.md`, or wants the SG-04 release asset path implemented and proved with both repo gates and live browser evidence.
1275:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for spec-driven planning, release-asset pipeline work, and release-mode live QA in this checkout; re-check the current spec, branch state, and local browser/server path before reusing exact commit or asset details.
1281:- rollout_summaries/2026-06-21T03-24-16-qNDg-spaceface_goal_prompt_plus_release_asset_pipeline_live_brows.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\20\rollout-2026-06-20T23-24-21-019ee835-1fda-7713-86cb-d0f911fed7b6.jsonl, updated_at=2026-06-22T00:36:36+00:00, thread_id=019ee835-1fda-7713-86cb-d0f911fed7b6, produced the accepted persistent goal prompt and trimmed it below the hard length cap)
1285:- SpaceFace, MASTER_MAKEOVER_PLAN.md, 47-A, resident technical director, integrator, final quality gate, goal prompt, 2500 chars, 2473
1291:- rollout_summaries/2026-06-21T03-24-16-qNDg-spaceface_goal_prompt_plus_release_asset_pipeline_live_brows.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\20\rollout-2026-06-20T23-24-21-019ee835-1fda-7713-86cb-d0f911fed7b6.jsonl, updated_at=2026-06-22T00:36:36+00:00, thread_id=019ee835-1fda-7713-86cb-d0f911fed7b6, added the deterministic release asset builder/validator and proved the path with browser interaction plus asset fetch evidence)
1389:- skill dedupe, .codex\skills, .agents\skills, plugin-cache, hatch-pet, find-skills, React Native, TheRxSpot.com, Gloorbot, n8n, SpaceFace, Juiceage, exact duplicates
1412:- `SpaceFace` had a broad 19-skill game shelf that looked generic enough to promote or rewrite globally if the user wants a smaller shared pool, while plugin-cache duplicates were mostly provider/version cache pairs that should be managed by plugin selection rather than hand-deleting cache internals. [Task 1][Task 2]
1422:# Task Group: SpaceFace git/worktree cleanup and merge hygiene
1423:scope: Use for `SpaceFace` branch/worktree cleanup requests where the user suspects an agent-created worktree or merge target exists, wants the repo reconciled onto its real primary branch, and wants remote state plus repo verification checked before closeout.
1424:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for worktree audits, merge-state verification, and local-vs-remote push checks in this checkout; re-check current branch topology, `.git/worktrees`, and `npm run check` behavior before assuming the same no-op result still applies.
1430:- rollout_summaries/2026-06-21T00-04-48-kT7v-spaceface_master_noop_worktree_merge_and_push_check.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\20\rollout-2026-06-20T20-04-53-019ee77e-8289-7ef1-8f2d-8c2bfdf3090d.jsonl, updated_at=2026-06-21T00:07:55+00:00, thread_id=019ee77e-8289-7ef1-8f2d-8c2bfdf3090d, verified there was no extra registered worktree and the suspected branch was already merged into `master`)
1434:- SpaceFace, git worktree, master, origin/master, feat/offscreen-sector-sim, git worktree list --porcelain, git merge-base --is-ancestor, Already up to date, .git/worktrees
1440:- rollout_summaries/2026-06-21T00-04-48-kT7v-spaceface_master_noop_worktree_merge_and_push_check.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\20\rollout-2026-06-20T20-04-53-019ee77e-8289-7ef1-8f2d-8c2bfdf3090d.jsonl, updated_at=2026-06-21T00:07:55+00:00, thread_id=019ee77e-8289-7ef1-8f2d-8c2bfdf3090d, fetched, compared `HEAD` to `"@{u}"`, and confirmed `git push origin master` was a no-op)
1450:- rollout_summaries/2026-06-21T00-04-48-kT7v-spaceface_master_noop_worktree_merge_and_push_check.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\20\rollout-2026-06-20T20-04-53-019ee77e-8289-7ef1-8f2d-8c2bfdf3090d.jsonl, updated_at=2026-06-21T00:07:55+00:00, thread_id=019ee77e-8289-7ef1-8f2d-8c2bfdf3090d, used `npm run check` as the final gate after confirming the merge/push work was a no-op)
1468:- `npm run check` is still the meaningful verification gate after merge/worktree cleanup in `SpaceFace`, and repeated `THREE.WARNING: Multiple instances of Three.js being imported.` messages did not fail the run in this rollout. [Task 3]
1558:- GH001, large files detected, >100 MiB, Juiceage, git count-objects -vH, .gitignore, downloads/*.exe, node_modules, SpaceFace, nul, Windows reserved filename, a67948e, c05b0c3
1571:- The final verified root projects were `CheapSkater-`, `Chemitree`, `Flowtab.Pro`, `Gloorbot`, `Juiceage`, `Learning AI Landing Page`, `Leisure Lee Landing Page`, `lotus`, `n8n`, `Resume`, `SalesRepPortal-render`, `SpaceFace`, `SuperPaste`, `Telomere`, `TheRxSpot.com`, and `Vamp`. [Task 1]
1575:- `SpaceFace` had an untracked root `nul` path, which is invalid on Windows; ignoring `/nul` was the correct fix, and the cleanup landed in commit `a67948e`. [Task 3]
1661:# Task Group: SpaceFace launcher/menu lifecycle fixes and flight-controls follow-up
1662:scope: Use for `SpaceFace` launcher failures, unexpected returns to the main menu after gameplay starts, or follow-up work on flight-control/banking feel where the user wants conventional handling and less inertia.
1663:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for launcher/UI-lifecycle debugging and flight-controls follow-up in this checkout; re-check the current batch launcher, local Electron install state, `src/ui` lifecycle code, and flight-control pipeline before assuming the same exact symptoms.
1665:## Task 1: Fix `SpaceFace-Desktop.bat` crash and stop hidden menu/focus state from reappearing during gameplay, success
1669:- rollout_summaries/2026-06-18T03-46-13-4xo1-spaceface_launcher_menu_focus_fix_and_flight_physics_investi.md (cwd=\\?\C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\17\rollout-2026-06-17T23-46-18-019ed8d6-2575-7073-a225-aa366b83b8d5.jsonl, updated_at=2026-06-18T03:54:52+00:00, thread_id=019ed8d6-2575-7073-a225-aa366b83b8d5, verified launcher repair plus gameplay-state/focus regression fix with `npm run check` and a Playwright launch flow)
1673:- SpaceFace-Desktop.bat, cmd /c, electron, node_modules\.bin\electron.cmd, pushd "%~dp0", uiRoot, screenManager, cinematic splash, keydown dismiss, main menu, hidden focus, npm run check, Playwright
1679:- rollout_summaries/2026-06-18T03-46-13-4xo1-spaceface_launcher_menu_focus_fix_and_flight_physics_investi.md (cwd=\\?\C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\17\rollout-2026-06-17T23-46-18-019ed8d6-2575-7073-a225-aa366b83b8d5.jsonl, updated_at=2026-06-18T03:54:52+00:00, thread_id=019ed8d6-2575-7073-a225-aa366b83b8d5, user clarified the desired handling model but the rotation/physics fix was interrupted before code changes)
1683:- SpaceFace, flight physics, banking, turn inertia, overshoot, conventional aircraft-style bank, regular flight physics, angular damping, ship rotation, hard to pilot
1687:- when the user says the desktop launcher "only flashes open a terminal and crashes," reproduce the real launcher entrypoint first (`cmd /c SpaceFace-Desktop.bat`) and capture the actual crash text before editing. [Task 1]
1693:- `SpaceFace-Desktop.bat` needed to `pushd "%~dp0"` and check `node_modules\.bin\electron.cmd`, because the original launcher was sensitive to the caller's working directory and could miss a missing Electron install. [Task 1]
1706:# Task Group: SpaceFace, Vamp, and Chemitree multi-wave bug-fix orchestration
1708:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace plus sibling repos Vamp/Chemitree; reuse_rule=reuse for the three-game repair workflow and similar multi-repo game bugfix sweeps; re-check each repo's current dirty tree, probe scripts, and gate outputs before assuming commit handles or wave status still apply.
1710:## Task 1: SpaceFace multi-wave repair across gameplay, UI, save/load, crafting, combat, starmap, and HUD accessibility, partial
1714:- rollout_summaries/2026-06-17T19-50-35-xWKs-spaceface_vamp_chemitree_multiwave_bugfix_orchestration.md (cwd=\\?\C:\Users\93rob\Documents\Codex\2026-06-17\goal-i-have-3-game-folders, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\17\rollout-2026-06-17T15-50-35-019ed722-af56-7dc2-b3f9-63379b7dff20.jsonl, updated_at=2026-06-18T00:45:34+00:00, thread_id=019ed722-af56-7dc2-b3f9-63379b7dff20, repeated-wave SpaceFace repair lane with validated atomic fix commits and stronger regression checks)
1718:- SpaceFace, multi-agent, subagent waves, atomic commits, npm run check, check-gameplay-core, check-ui-screen-imports, check-ui-a11y, crafting queues, starmap, HUD accessibility, 24454c5, 36d8dcd
1759:- In SpaceFace, `npm run check` became the durable regression gate after it was extended with `scripts/check-gameplay-core.mjs`, `scripts/check-ui-screen-imports.mjs`, and `scripts/check-ui-a11y.mjs`; those scripts caught save/load, crafting, combat, starmap/menu, and HUD/modal regressions that broader smoke passes missed. [Task 1]
1760:- The validated SpaceFace commit handles are strong retrieval anchors for later regression work: `24454c5`, `2bce292`, `226a47f`, `6563ee8`, `20d34ad`, and `36d8dcd`. [Task 1]
1774:# Task Group: SpaceFace repo-local skill curation beyond Three.js
1775:scope: Use for SpaceFace or similar game-repo skill-shelf requests where the user wants a small curated set of helpful repo-local skills and will push back if the selection overfocuses on the renderer stack.
1776:applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for repo-local skill selection, source validation, and manifest/README updates in SpaceFace or similar game repos; re-check current gameplay scope and upstream skill sources before copying the same set wholesale.
1778:## Task 1: Add a first curated set of SpaceFace-relevant skills, partial
1782:- rollout_summaries/2026-06-17T23-35-13-qiTG-spaceface_skill_pack_broadened_beyond_threejs.md (cwd=\\?\C:\Users\93rob\Documents\Codex\2026-06-17\i-m-making-another-game-in, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\17\rollout-2026-06-17T19-35-13-019ed7f0-5a74-71e0-a6d2-72e8e7208954.jsonl, updated_at=2026-06-17T23:44:24+00:00, thread_id=019ed7f0-5a74-71e0-a6d2-72e8e7208954, initial SpaceFace skill install that overfit on Three.js before the user corrected the framing)
1786:- SpaceFace, skills folder, Three.js, threejs-game-skills, skills/README.md, threejs-game-director, threejs-debug-profiler, SKILL.md, package.json, README.md
1788:## Task 2: Broaden the SpaceFace skill shelf beyond renderer-specific tools, success
1801:- when the user corrected “why is it all threejs?” and “isn't there some other skills that could help with this game besides graphics?”, do not assume the renderer stack is the whole problem space for a game repo. [Task 1][Task 2]
1806:- SpaceFace is a Three.js/Electron game, but its repo scope is broader than rendering: economy, mining, combat, missions, factions, automation, progression, save/load, UI, audio, and packaging all show up in `README.md` and the repo structure. [Task 1][Task 2]
1807:- `threejs-game-skills` was a usable upstream source for renderer/runtime/game-asset skills; the source commit checked before install was `2215fd7c28ce55cee6a593ba1e422485db42498f`. [Task 1]
1809:- The final `SpaceFace/skills` shelf had 19 skill folders total, each with a `SKILL.md`, and `skills/README.md` documented why the repo is not “just Three.js” plus preserved upstream license/readme context. [Task 1][Task 2]
1814:- symptom: a helpful-skills pass comes back overly renderer-centric. cause: the repo uses Three.js for rendering, but the game’s leverage points extend far beyond graphics. fix: split rendering/asset skills from gameplay/design/production skills before choosing what to install. [Task 1][Task 2]
2213:- The canonical storefront path here is the Bluehost PHP proxy plus the Medusa renderer, not the old standalone Next.js shell. [Task 2]

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content C:\\Users\\93rob\\.agents\\skills\\threejs\\SKILL.md -TotalCount 220" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 896ms:
---
name: threejs
description: Build 3D web apps with Three.js (WebGL/WebGPU). Use for 3D scenes, animations, custom shaders, PBR materials, VR/XR experiences, games, data visualizations, product configurators.
license: MIT
version: 2.0.0
---

# Three.js Development

Build high-performance 3D web applications using Three.js - a cross-browser WebGL/WebGPU library.

## When to Use This Skill

Use when working with:
- 3D scenes, models, animations, or visualizations
- WebGL/WebGPU rendering and graphics programming
- Interactive 3D experiences (games, configurators, data viz)
- Camera controls, lighting, materials, or shaders
- Loading 3D assets (GLTF, FBX, OBJ) or textures
- Post-processing effects (bloom, depth of field, SSAO)
- Physics simulations, VR/XR experiences, or spatial audio
- Performance optimization (instancing, LOD, frustum culling)

## Progressive Learning Path

### Level 1: Getting Started
- Load `references/00-fundamentals.md` - Fundamentals
- Load `references/01-getting-started.md` - Scene setup, basic geometries, materials, lights, rendering loop

### Level 2: Common Tasks
- **Asset Loading**: `references/02-loaders.md` - GLTF, FBX, OBJ, texture loaders
- **Textures**: `references/03-textures.md` - Types, mapping, wrapping, filtering
- **Cameras**: `references/04-cameras.md` - Perspective, orthographic, controls
- **Lights**: `references/05-lights.md` - Types, shadows, helpers
- **Animations**: `references/06-animations.md` - Clips, mixer, keyframes
- **Math**: `references/07-math.md` - Vectors, matrices, quaternions, curves
- **Geometry**: `references/18-geometry.md` - Built-in shapes, BufferGeometry, custom geometry, instancing
- **Materials**: `references/11-materials.md` - PBR, basic, phong, lambert, physical, toon, normal, depth, raw, shader materials, material properties

### Level 3: Interactive & Effects
- **Interaction**: `references/08-interaction.md` - Raycasting, picking, transforms
- **Post-Processing**: `references/09-postprocessing.md` - Passes, bloom, SSAO, SSR
- **Controls (Addons)**: `references/10-controls.md` - Orbit, transform, first-person

### Level 4: Advanced Rendering
- **Materials Advanced**: `references/11-materials-advanced.md` - PBR, custom shaders
- **Performance**: `references/12-performance.md` - Instancing, LOD, batching, culling
- **Node Materials (TSL)**: `references/13-node-materials.md` - Shader graphs, compute

### Level 5: Specialized
- **Physics**: `references/14-physics-vr.md` - Ammo, Rapier, Jolt, VR/XR
- **Advanced Loaders**: `references/15-specialized-loaders.md` - SVG, VRML, domain-specific
- **WebGPU**: `references/16-webgpu.md` - Modern backend, compute shaders
- **Shaders**: `references/17-shader.md` - GLSL, ShaderMaterial, uniforms, custom effects

## Quick Start Pattern

```javascript
// 1. Scene, Camera, Renderer
const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth/window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer();
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

// 2. Add Objects
const geometry = new THREE.BoxGeometry();
const material = new THREE.MeshStandardMaterial({ color: 0x00ff00 });
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);

// 3. Add Lights
const light = new THREE.DirectionalLight(0xffffff, 1);
light.position.set(5, 5, 5);
scene.add(light);
scene.add(new THREE.AmbientLight(0x404040));

// 4. Animation Loop
function animate() {
  requestAnimationFrame(animate);
  cube.rotation.x += 0.01;
  cube.rotation.y += 0.01;
  renderer.render(scene, camera);
}
animate();
```

## External Resources

- Official Docs: https://threejs.org/docs/
- Examples: https://threejs.org/examples/
- Editor: https://threejs.org/editor/
- Discord: https://discord.gg/56GBJwAnUS

codex
I found the relevant SpaceFace memory guardrails: preserve visible quality, use the repo gates, and treat `check:non-graphics` as the right non-art verification surface when graphics lanes are locked. Now I’m reading the current design and render code so the hook matches today’s contracts.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git status --short' in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n "''^##? 9'"\\.1|9\\.1|sectorPalette|dust|nebula|parallax|WS-E1|wave-2|Wave 2|starfield|camera target|follow target|state\\.camera|camera\" design/GDD_2_0.md design/BUILD_PLAN_2_0.md" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 715ms:
 M AGENTS.md
 M ARCHITECTURE.md
 D assets/ships/release.__building/kestrel/kestrel_reference.glb
 D assets/ships/release.__building/parts/cockpits/cockpit_dome.glb
 D assets/ships/release.__building/parts/cockpits/cockpit_recessed.glb
 D assets/ships/release.__building/parts/cockpits/cockpit_slab.glb
 D assets/ships/release.__building/parts/engines/engine_industrial.glb
 D assets/ships/release.__building/parts/engines/engine_ion_small.glb
 D assets/ships/release.__building/parts/engines/engine_ion_twin.glb
 D assets/ships/release.__building/parts/engines/engine_resonator.glb
 D assets/ships/release.__building/parts/fins/fin_crystalline.glb
 D assets/ships/release.__building/parts/fins/fin_radiator_grid.glb
 D assets/ships/release.__building/parts/fins/fin_swept_smuggler.glb
 D assets/ships/release.__building/parts/fins/fin_wedge.glb
 D assets/ships/release.__building/parts/greebles/greeble_hatches.glb
 D assets/ships/release.__building/parts/greebles/greeble_vents.glb
 D assets/ships/release.__building/parts/hulls/hull_corvette.glb
 D assets/ships/release.__building/parts/hulls/hull_fighter.glb
 D assets/ships/release.__building/parts/hulls/hull_freighter.glb
 D assets/ships/release.__building/parts/hulls/hull_gunship.glb
 D assets/ships/release.__building/parts/hulls/hull_interceptor.glb
 D assets/ships/release.__building/parts/hulls/hull_miner.glb
 D assets/ships/release.__building/parts/hulls/hull_starter.glb
 D assets/ships/release.__building/parts/weapons/weapon_heavy_cannon.glb
 D assets/ships/release.__building/parts/weapons/weapon_lance.glb
 D assets/ships/release.__building/parts/weapons/weapon_pulse_cannon.glb
 D assets/ships/release.__building/parts/weapons/weapon_turret_dual.glb
 D assets/ships/release.__lock/owner.json
 M index.html
 M package.json
 M scripts/check-claim-base-input.mjs
 M scripts/check-input-modalities.mjs
 M scripts/check-mission-accept-handoff-runtime.mjs
 M src/combat/attachments.js
 M src/core/eventBus.js
 M src/core/gameState.js
 M src/core/registry.js
 M src/core/simSnapshot.js
 M src/data/combatDefs.js
 M src/data/commodities.js
 M src/data/mining.js
 M src/data/sectors.js
 M src/main.js
 M src/render/partsLibrary.js
 M src/render/renderer.js
 M src/render/starfield.js
 M src/render/vfx.js
 M src/save/saveSystem.js
 M src/systems/ai.js
 M src/systems/input.js
 M src/systems/mining.js
 M src/systems/onboarding.js
 M src/systems/weapons.js
 M src/systems/world.js
 M src/ui/bindings.js
 M src/ui/comms.js
 M src/ui/confirm.js
 M src/ui/controlPrompts.js
 M src/ui/hud.js
 M src/ui/input.js
 M src/ui/radar.js
 M src/ui/screens/bar.js
 M src/ui/screens/localmap.js
 M src/ui/screens/settings.js
 M src/ui/screens/starmap.js
 M src/ui/uiRoot.js
 M styles/ui.css
?? .tmp/
?? advisor-artifacts/
?? design/BUILD_PLAN_2_0.md
?? design/GDD_2_0.md
?? scripts/check-ai-telegraphs.mjs
?? scripts/check-hitch-budget.mjs
?? scripts/check-mining-2.mjs
?? scripts/check-sector-palettes.mjs
?? scripts/check-tether-gameplay.mjs
?? src/data/impulseCharges.js
?? src/render/precompile.js
?? src/systems/impulseCharges.js
?? src/systems/scanner.js
?? src/systems/tetherGameplay.js

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content src\\render\\starfield.js" in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content src\\render\\renderer.js" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1481ms:
design/GDD_2_0.md:31:| "No depth in background" | Single skydome nebula; zero parallax layers between camera and backdrop | §9 |
design/GDD_2_0.md:101:- **Industry uses:** haul ore chunks (§5), tow wrecks to salvage yards (new contract type), rescue
design/GDD_2_0.md:123:camera-shake trauma and hit-pause scale with momentum exchanged, not damage dealt. A freighter
design/GDD_2_0.md:174:- Kills: interior flash → breakup → shockwave ring + camera punch; kill-confirm reticle pip + comms bark.
design/GDD_2_0.md:256:### 9.1 Parallax depth stack (the "background has no depth" fix)
design/GDD_2_0.md:257:Between backdrop and play plane, add camera-relative parallax layers (all cheap, all pooled):
design/GDD_2_0.md:258:1. Skydome nebula (exists) — parallax ~0.
design/GDD_2_0.md:259:2. **Far dust sheets** (2–3 additive planes, factor 0.15–0.3) — huge soft nebula wisps.
design/GDD_2_0.md:261:4. **Near dust motes** (factor 1.2–1.5, subtle) — the layer that *sells speed* during boost/cruise.
design/GDD_2_0.md:267:(key/rim/fill light colors + nebula tint + fog color + dust hue). Core worlds: clean cyan/steel.
design/BUILD_PLAN_2_0.md:76:| D1 | **Tether gameplay integration** (flagship): fire/latch/reel/cut on input contract, slingshot physics, tension telegraphy events, mass-ratio yank rules, break thresholds | **CODEX-1** | **DONE** (sim-side; check-tether-gameplay + sg02 tether suites green. NOTE: tether has NO in-world visual yet — cable line + strain color is a D3/wave-2 VFX item) | `check:sg02:tether*` green + new scenario: latch asteroid at speed, cut at tangent, exit velocity ≥ 1.4× entry; events `tether:latched/strain/broke/released` emitted |
design/BUILD_PLAN_2_0.md:77:| D2 | Impulse charges: sticky lob (Q/helm, Y/classic), R-detonate radial impulse, self-plate trick, 6 s throw cd, friendly fire | **GROK-1** | **DONE** (system + data + registry wired; cmdty_impulse_charge added; sanity sim passes. No in-world visual yet — same wave-2 VFX item) | Standalone system + data file; sim scenario knocks a drone into an asteroid; lead wires registry |
design/BUILD_PLAN_2_0.md:85:| E1 | Parallax stack: far dust sheets, mid instanced debris, near speed-motes w/ boost streaks; motion-reduce halves | Claude design + CODEX plumbing (wave 2) | QUEUED | Boost visibly *reads* fast; no added hitches (check:hitch-budget) |
design/BUILD_PLAN_2_0.md:86:| E2 | Data-driven sector palettes in `sectors.js` (lights/fog/nebula/dust); 4 palette classes authored | CODEX (wave 2) | QUEUED | Jumping sectors visibly changes identity in 1 s |
design/BUILD_PLAN_2_0.md:96:| F1a | One-voice comms gate (minimal arbiter): chatter queues behind intro modal, drips 3.5 s apart, stale ambient drops | **Claude** | **DONE** (verified: 1 comms line during intro vs prior wall) | 10-min recording: zero overlapping text events (full F1 arbiter still wave-2) |
design/BUILD_PLAN_2_0.md:97:| E3a | HUD strip→chips: SPD+WPN live; CARGO/CR/CLASS contextual chips; THR/STOP retired into SPD tip | **Claude** | **DONE** | GDD §9.4 three-anchor layout (full pass incl. priority channel still wave-2) |
design/BUILD_PLAN_2_0.md:118:still staged (brief-codex-6-cruise.md). Remaining: E1 parallax depth, D3 juice stack, B3 overview
design/BUILD_PLAN_2_0.md:123:- **Wave 2:** C1, D3, D4, A3, E1, E2, F1, B3, B4 — after wave-1 review; reuse ownership map, updated per collision

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"function _initPools|const _initPools|_initPools|InstancedMesh|instanceMatrix|setMatrixAt|DynamicDrawUsage|Object3D|new THREE\\.Color|scratch|pool\" src\\render\\vfx.js" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 2124ms:
// Deep-space backdrop: a parallax starfield of soft ROUND stars (procedural sprite texture, not
// the default square points) layered over a colored nebula backdrop built from the generated
// nebula art. Together they kill the "dead black void" and give real depth + atmosphere.
import * as THREE from 'three';
import { createPlanetFactory } from './planetFactory.js';

// Soft round star sprite (radial gradient -> circular, glowing). Square PointsMaterial points are
// the #1 reason a starfield looks cheap; this fixes it.
function makeStarSprite() {
  const c = document.createElement('canvas');
  c.width = c.height = 64;
  const ctx = c.getContext('2d');
  const g = ctx.createRadialGradient(32, 32, 0, 32, 32, 32);
  g.addColorStop(0.0, 'rgba(255,255,255,1)');
  g.addColorStop(0.25, 'rgba(255,255,255,0.85)');
  g.addColorStop(0.5, 'rgba(255,255,255,0.32)');
  g.addColorStop(1.0, 'rgba(255,255,255,0)');
  ctx.fillStyle = g;
  ctx.fillRect(0, 0, 64, 64);
  const t = new THREE.CanvasTexture(c);
  t.colorSpace = THREE.SRGBColorSpace;
  return t;
}

// Procedural deep-space nebula: a moodier cyberpunk-noir backdrop — deep indigo base + denser
// layered clouds in magenta/cyan/violet (the signature neon palette) + dust lanes + painted distant
// planets for real depth. Sector-tintable via the optional tint so each region of the galaxy reads
// with its own atmosphere (clean-blue core vs rust-red frontier vs violet lawless edge). Wide canvas
// so it wraps the skydome without obvious repetition. Accepts an optional {tint, planets} for variety.
function makeNebulaCanvas(opts = {}) {
  const W = 2048, H = 1024;
  const c = document.createElement('canvas');
  c.width = W; c.height = H;
  const ctx = c.getContext('2d');
  const tint = opts.tint || null; // optional hex string to shift the whole palette toward a sector mood
  const rnd = (() => { let s = 0x9e3779b9; return () => { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; }; })();

  // base vertical gradient (slightly lighter toward the "galactic plane" middle). Deep indigo for the
  // noir mood; tintable so a sector can lean rust/violet/teal.
  const base = ctx.createLinearGradient(0, 0, 0, H);
  base.addColorStop(0.0, '#04050c');
  base.addColorStop(0.45, tint ? shade('#0a1028', tint) : '#080d24');
  base.addColorStop(0.6, tint ? shade('#141a40', tint) : '#10183c');
  base.addColorStop(1.0, '#05060e');
  ctx.fillStyle = base; ctx.fillRect(0, 0, W, H);

  // cloud palette — magenta, violet, cyan, teal: the cyberpunk-noir neon signature. Tinted per-sector.
  const rawPalette = ['#5a3ec8', '#7a3aa0', '#2f6fe0', '#1f8f9a', '#c83a8a', '#3a8fc8', '#8a2fa0'];
  const palette = tint ? rawPalette.map((p) => blend(p, tint, 0.35)) : rawPalette;

  // DEEP broad washes first (huge, low-alpha) to establish the moody color field
  ctx.globalCompositeOperation = 'lighter';
  for (let i = 0; i < 24; i++) {
    const x = rnd() * W, y = H * (0.05 + rnd() * 0.9), r = 600 + rnd() * 700;
    const col = palette[(rnd() * palette.length) | 0];
    const g = ctx.createRadialGradient(x, y, 0, x, y, r);
    g.addColorStop(0, col); g.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.globalAlpha = 0.05 + rnd() * 0.06;
    ctx.fillStyle = g; ctx.beginPath(); ctx.arc(x, y, r, 0, Math.PI * 2); ctx.fill();
  }
  // mid-size cloud blobs — the main readable structure
  for (let i = 0; i < 110; i++) {
    const x = rnd() * W, y = H * (0.05 + rnd() * 0.9), r = 140 + rnd() * 460;
    const col = palette[(rnd() * palette.length) | 0];
    const g = ctx.createRadialGradient(x, y, 0, x, y, r);
    g.addColorStop(0, col); g.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.globalAlpha = 0.085 + rnd() * 0.15;
    ctx.fillStyle = g; ctx.beginPath(); ctx.arc(x, y, r, 0, Math.PI * 2); ctx.fill();
  }
  // dust lanes — dark winding streaks (source-over) that break up the clouds and add structure
  ctx.globalCompositeOperation = 'source-over';
  ctx.strokeStyle = 'rgba(2,3,8,0.4)'; ctx.lineCap = 'round';
  for (let i = 0; i < 14; i++) {
    const x0 = rnd() * W, y0 = rnd() * H;
    ctx.lineWidth = 30 + rnd() * 120;
    ctx.globalAlpha = 0.18 + rnd() * 0.3;
    ctx.beginPath(); ctx.moveTo(x0, y0);
    ctx.bezierCurveTo(x0 + (rnd() - 0.5) * W * 0.4, y0 + rnd() * H * 0.3,
                      x0 + (rnd() - 0.5) * W * 0.4, y0 + rnd() * H * 0.3,
                      x0 + (rnd() - 0.5) * W * 0.5, y0 + (rnd() - 0.5) * H * 0.5);
    ctx.stroke();
  }
  ctx.globalAlpha = 1;
  // bright cores for punch (back to additive)
  ctx.globalCompositeOperation = 'lighter';
  for (let i = 0; i < 22; i++) {
    const x = rnd() * W, y = H * (0.15 + rnd() * 0.7), r = 40 + rnd() * 130;
    const g = ctx.createRadialGradient(x, y, 0, x, y, r);
    g.addColorStop(0, 'rgba(180,210,255,0.55)'); g.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.globalAlpha = 0.2 + rnd() * 0.25; ctx.fillStyle = g;
    ctx.beginPath(); ctx.arc(x, y, r, 0, Math.PI * 2); ctx.fill();
  }
  ctx.globalCompositeOperation = 'source-over';
  ctx.globalAlpha = 1;

  // PAINTED DISTANT PLANETS — 2-3 for real backdrop depth (the system's drawPlanet, previously dead
  // code, now wired in). They read as giant far-off worlds, reinforcing the sense of a lived-in galaxy.
  if (opts.planets !== false) {
    const planetSpecs = [
      { cx: W * 0.20, cy: H * 0.30, r: 90,  bands: [[60,40,80],[40,30,70],[80,50,110]], atm: [120,80,200] },
      { cx: W * 0.82, cy: H * 0.66, r: 130, bands: [[30,50,70],[20,40,60],[50,90,110]], atm: [80,160,200] },
      { cx: W * 0.55, cy: H * 0.16, r: 55,  bands: [[90,50,40],[120,70,40],[70,40,30]], atm: [220,140,80] },
    ];
    for (const p of planetSpecs) {
      // place each planet deterministically with a stable light direction (upper-left)
      drawPlanet(ctx, p.cx, p.cy, p.r, p.bands, p.atm, -0.6, -0.5);
    }
  }

  // vignette: darken the edges so gameplay (always centered) stays high-contrast over the nebula
  const vig = ctx.createRadialGradient(W / 2, H / 2, H * 0.18, W / 2, H / 2, H * 0.72);
  vig.addColorStop(0, 'rgba(0,0,0,0)');
  vig.addColorStop(1, 'rgba(2,3,9,0.7)');
  ctx.fillStyle = vig; ctx.fillRect(0, 0, W, H);
  ctx.globalAlpha = 1;
  return c;
}

// --- small color helpers for sector tinting (no deps) ---
function normalizeTint(tint) {
  if (tint == null) return null;
  if (typeof tint === 'number' && Number.isFinite(tint)) {
    return '#' + ((tint >>> 0) & 0xffffff).toString(16).padStart(6, '0');
  }
  if (typeof tint === 'string') return tint.startsWith('#') ? tint : '#' + tint.replace(/^0x/i, '');
  return null;
}
function hexToRgb(hex) {
  const h = hex.replace('#', ''); return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)];
}
function rgbToHex(r, g, b) {
  const c = (v) => Math.max(0, Math.min(255, Math.round(v))).toString(16).padStart(2, '0');
  return '#' + c(r) + c(g) + c(b);
}
// linear blend of two hex colors by t (0=a,1=b)
function blend(a, b, t) {
  const pa = hexToRgb(a), pb = hexToRgb(b);
  return rgbToHex(pa[0] + (pb[0] - pa[0]) * t, pa[1] + (pb[1] - pa[1]) * t, pa[2] + (pb[2] - pa[2]) * t);
}
// shift a base hex toward a tint hue (multiplies channels then normalizes) — used for sector mood
function shade(base, tint, amt = 0.3) { return blend(base, tint, amt); }


// Paint a shaded distant planet onto a 2D context: atmosphere rim, banded body, a lit crescent and
// a dark terminator. lx,ly = direction (in canvas space) toward the light source.
function drawPlanet(ctx, cx, cy, r, bands, atm, lx, ly) {
  ctx.save();
  // atmosphere rim glow (additive)
  ctx.globalCompositeOperation = 'lighter';
  const ag = ctx.createRadialGradient(cx, cy, r * 0.88, cx, cy, r * 1.32);
  ag.addColorStop(0, `rgba(${atm[0]},${atm[1]},${atm[2]},0)`);
  ag.addColorStop(0.55, `rgba(${atm[0]},${atm[1]},${atm[2]},0.16)`);
  ag.addColorStop(1, `rgba(${atm[0]},${atm[1]},${atm[2]},0)`);
  ctx.fillStyle = ag; ctx.beginPath(); ctx.arc(cx, cy, r * 1.32, 0, Math.PI * 2); ctx.fill();
  ctx.globalCompositeOperation = 'source-over';

  // body (clipped to the disc)
  ctx.beginPath(); ctx.arc(cx, cy, r, 0, Math.PI * 2); ctx.clip();
  ctx.fillStyle = `rgb(${bands[0][0]},${bands[0][1]},${bands[0][2]})`;
  ctx.fillRect(cx - r, cy - r, r * 2, r * 2);
  let y = cy - r;
  while (y < cy + r) {
    const bh = r * (0.04 + Math.random() * 0.16);
    const b = bands[(Math.random() * bands.length) | 0];
    ctx.globalAlpha = 0.5 + Math.random() * 0.4;
    ctx.fillStyle = `rgb(${b[0]},${b[1]},${b[2]})`;
    ctx.fillRect(cx - r, y, r * 2, bh + 1);
    y += bh;
  }
  ctx.globalAlpha = 1;
  // lit crescent: brighten toward the light, darken the far/terminator side
  const lit = ctx.createRadialGradient(cx + lx * r * 0.6, cy + ly * r * 0.6, r * 0.1, cx, cy, r * 1.25);
  lit.addColorStop(0, 'rgba(255,250,235,0.45)');
  lit.addColorStop(0.4, 'rgba(255,250,235,0.0)');
  lit.addColorStop(1, 'rgba(255,250,235,0)');
  ctx.globalCompositeOperation = 'lighter'; ctx.fillStyle = lit;
  ctx.fillRect(cx - r, cy - r, r * 2, r * 2);
  ctx.globalCompositeOperation = 'source-over';
  const dark = ctx.createRadialGradient(cx - lx * r * 0.9, cy - ly * r * 0.9, r * 0.1, cx - lx * r * 0.4, cy - ly * r * 0.4, r * 1.8);
  dark.addColorStop(0, 'rgba(2,3,8,0.92)');
  dark.addColorStop(0.55, 'rgba(2,3,8,0.5)');
  dark.addColorStop(1, 'rgba(2,3,8,0)');
  ctx.fillStyle = dark; ctx.fillRect(cx - r, cy - r, r * 2, r * 2);
  ctx.restore();
}

export function createStarfield(scene, opts = {}) {
  const layers = [];
  const sprite = makeStarSprite();
  let bgTexture = null;
  let currentTint = normalizeTint(opts.tint);

  // star tints (mostly cool white/blue with a few warm + teal accents)
  const TINTS = [0xffffff, 0xcfe0ff, 0x9fb6e0, 0xfff0d8, 0x8af0d6, 0xbfd2ff];
  const specs = [
    { count: 2200, spread: 5200, yLo: -1600, yHi: -120, size: 5,  par: 0.94, opacity: 0.85 },
    { count: 1300, spread: 4200, yLo: -1200, yHi: -80,  size: 9,  par: 0.86, opacity: 0.95 },
    { count: 520,  spread: 3200, yLo: -900,  yHi: -40,  size: 16, par: 0.74, opacity: 1.0 },
    { count: 90,   spread: 2400, yLo: -700,  yHi: 60,   size: 34, par: 0.60, opacity: 0.9 }, // sparse bright "hero" stars
  ];

  for (const s of specs) {
    const g = new THREE.BufferGeometry();
    const pos = new Float32Array(s.count * 3);
    const col = new Float32Array(s.count * 3);
    const tmp = new THREE.Color();
    for (let i = 0; i < s.count; i++) {
      pos[i * 3] = (Math.random() * 2 - 1) * s.spread;
      pos[i * 3 + 1] = s.yLo + Math.random() * (s.yHi - s.yLo);
      pos[i * 3 + 2] = (Math.random() * 2 - 1) * s.spread;
      tmp.setHex(TINTS[(Math.random() * TINTS.length) | 0]);
      // vary brightness so the field isn't uniform
      const b = 0.45 + Math.random() * 0.55;
      col[i * 3] = tmp.r * b; col[i * 3 + 1] = tmp.g * b; col[i * 3 + 2] = tmp.b * b;
    }
    g.setAttribute('position', new THREE.BufferAttribute(pos, 3));
    g.setAttribute('color', new THREE.BufferAttribute(col, 3));
    const m = new THREE.PointsMaterial({
      map: sprite, size: s.size, sizeAttenuation: true, vertexColors: true,
      transparent: true, opacity: s.opacity, depthWrite: false,
      blending: THREE.AdditiveBlending, fog: false,
    });
    const pts = new THREE.Points(g, m);
    pts.frustumCulled = false; pts.renderOrder = -10;
    scene.add(pts);
    // GR-7: tag the hero-star layer (last spec) so update() can shimmer it. The base opacity is kept
    // so the twinkle modulates around the authored brightness rather than replacing it.
    const rec = { pts, par: s.par, baseOpacity: s.opacity };
    if (s === specs[specs.length - 1]) rec.hero = true;
    layers.push(rec);
  }

  // ---- nebula backdrop (with painted distant planets) ------------------------------------------
  // A PROCEDURAL moody nebula (magenta/violet/cyan neon clouds + dust lanes + painted planets on
  // deep indigo) set as scene.background, so it reliably fills the whole frame with no seams. The
  // parallax star layers above sell the motion. Sector-tintable so each region of the galaxy has
  // its own atmosphere (clean-blue core vs rust-red frontier vs violet lawless edge).
  function applyBackground(tint) {
    try {
      const tex = new THREE.CanvasTexture(makeNebulaCanvas({ tint }));
      tex.colorSpace = THREE.SRGBColorSpace;
      if (bgTexture) bgTexture.dispose();
      bgTexture = tex;
      scene.background = tex;
    } catch (_) { /* nebula optional */ }
  }
  applyBackground(currentTint);

  // ---- GR-7: real 3D distant planets -----------------------------------------------------------
  // A few genuine 3D planet meshes (built by the planet factory) placed at huge distance, parallaxing
  // with the starfield. Unlike the 2D painted planets baked into the nebula canvas, these rotate, catch
  // real fresnel atmosphere shells, and shift against the stars as you fly — selling true galactic
  // depth. Kept to 2-3 small worlds so the cost is negligible; fog is off so they survive the distance.
  const distantPlanets = [];
  if (opts.distantPlanets !== false) {
    try {
      const pf = createPlanetFactory();
      const rnd = (() => { let s = 0x1234abcd; return () => { s = (s * 1664525 + 1013904223) >>> 0; return s / 4294967296; }; })();
      const types = ['gas_giant', 'terran', 'ice', 'arid', 'rocky'];
      const n = 2 + (rnd() < 0.5 ? 1 : 0);
      for (let i = 0; i < n; i++) {
        const type = types[(rnd() * types.length) | 0];
        const radius = 180 + rnd() * 260;
        const seed = (rnd() * 99999) | 0;
        const mesh = pf.buildPlanetMesh(type, radius, seed);
        const spread = 6000 + rnd() * 3000;
        const ang = rnd() * Math.PI * 2;
        mesh.position.set(
          Math.cos(ang) * spread,
          -(600 + rnd() * 1200),
          Math.sin(ang) * spread
        );
        mesh.frustumCulled = false;
        scene.add(mesh);
        distantPlanets.push({
          mesh,
          basePos: mesh.position.clone(),
          parallax: 0.96,   // nearly fixed -> appears extremely far, just creeping vs the stars
          spin: 0.01 + rnd() * 0.02,
        });
      }
    } catch (_) { /* distant 3D planets are optional polish */ }
  }

  return {
    recenter(camPos) {
      for (const L of layers) {
        L.pts.position.x = camPos.x * (1 - L.par);
        L.pts.position.z = camPos.z * (1 - L.par);
      }
      // GR-7: distant 3D planets parallax with the starfield (very low factor = appear extremely far).
      for (const p of distantPlanets) {
        p.mesh.position.x = p.basePos.x + camPos.x * (1 - p.parallax);
        p.mesh.position.z = p.basePos.z + camPos.z * (1 - p.parallax);
      }
    },
    // GR-7: per-frame twinkle for the hero-star layer. A slow sinusoidal shimmer on the brightest stars
    // so the backdrop feels alive (atmospheric scintillation) instead of a frozen field. Cheap: one
    // material.opacity write per frame.
    update(dt, time) {
      const hero = layers[layers.length - 1];
      if (hero && hero.hero) {
        hero.pts.material.opacity = hero.baseOpacity * (0.8 + 0.2 * Math.sin(time * 1.7));
      }
      // GR-7: slow-rotate the distant 3D planets so they read as real spinning worlds, not billboards.
      for (const p of distantPlanets) p.mesh.rotation.y += dt * p.spin;
    },
    // Swap the nebula mood on sector enter — each region of the galaxy reads with its own color
    // signature (core = clean blue, industrial = rust/amber, frontier = blood-red, alien = violet).
    setSectorTint(tint) {
      const nextTint = normalizeTint(tint);
      if (nextTint === currentTint) return;
      currentTint = nextTint;
      applyBackground(currentTint);
    },
    setWarp() { /* extended by vfx during jump */ },
  };
}

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"follow|target|cameraTarget|state\\.camera|camera\\.target|playerId|entities\\.get|boosting|sectorPalette|paletteLerp|jump:arrive|starfield\\.update|vfx\\.update|render-phase|update\\(dt\\)\" src\\camera.js src\\render\\renderer.js src -g \"*.js\"" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 2793ms:
// Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
// lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
// calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
import * as THREE from 'three';
import { createChaseCamera } from './camera.js';
import { createStarfield } from './starfield.js';
import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
import { installVisualOverrides } from './visualOverrides.js';
import { createBloom } from './bloom.js';
import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
import { invalidateAuthoredAsset } from './assetLoader.js';
import { getAuthoredInstancePoolDiagnostics, invalidatePartsLibraryCaches, preloadAuthoredPartLibrary, syncAuthoredInstancePools } from './partsLibrary.js';
import { projectedWidthPx } from './lod.js';
import { createCollisionDebug } from './collisionDebug.js';
import { installDiagnostics } from './diagnostics.js';
import { createPlanetFactory } from './planetFactory.js';
import { precompilePipelines } from './precompile.js';
import { SECTOR_PALETTE_CLASSES } from '../data/sectors.js';

const SECTOR_PALETTE_LERP_SECONDS = 1.5;
const SECTOR_LIGHT_INTENSITIES = { ambient: 0.85, key: 1.7, rim: 0.7, fill: 0.35 };

// ---- contact shadow disc (module-level cache so one texture serves all entities) ----------------
let _shadowTex = null;
let _shadowGeo = null;
let _shadowMat = null;
const CONTACT_SHADOW_INITIAL_CAPACITY = 256;
const CONTACT_SHADOW_POS = new THREE.Vector3();
const CONTACT_SHADOW_SCALE = new THREE.Vector3();
const CONTACT_SHADOW_MATRIX = new THREE.Matrix4();
const CONTACT_SHADOW_QUAT = new THREE.Quaternion().setFromEuler(new THREE.Euler(-Math.PI / 2, 0, 0));
const SOCKET_WORLD_POS = new THREE.Vector3();
const SOCKET_WORLD_QUAT = new THREE.Quaternion();
const SOCKET_WORLD_SCALE = new THREE.Vector3();
const SOCKET_FORWARD = new THREE.Vector3();
const RUNTIME_MESH_BUILD_BUDGET = 2;
function getContactShadowTex() {
  if (_shadowTex) return _shadowTex;
  const c = document.createElement('canvas'); c.width = c.height = 64;
  const ctx = c.getContext('2d');
  const g = ctx.createRadialGradient(32, 32, 0, 32, 32, 32);
  g.addColorStop(0.0, 'rgba(0,0,0,0.70)');
  g.addColorStop(0.6, 'rgba(0,0,0,0.35)');
  g.addColorStop(1.0, 'rgba(0,0,0,0)');
  ctx.fillStyle = g; ctx.fillRect(0, 0, 64, 64);
  _shadowTex = new THREE.CanvasTexture(c);
  return _shadowTex;
}
function getContactShadowGeo() {
  if (!_shadowGeo) _shadowGeo = new THREE.CircleGeometry(1, 20);
  return _shadowGeo;
}
function getContactShadowMat() {
  if (!_shadowMat) {
    _shadowMat = new THREE.MeshBasicMaterial({
      map: getContactShadowTex(),
      transparent: true,
      opacity: 0.55,
      depthWrite: false,
      blending: THREE.NormalBlending,
    });
  }
  return _shadowMat;
}

function attachContactShadow(mesh, entity) {
  if (!mesh || entity._noShadow) return;
  const r = Math.max(16, (entity.radius || 28) * 1.4);
  mesh.userData.contactShadowRadius = r;
  mesh.userData.hasContactShadow = true;
}

function createContactShadowPool(scene) {
  const pool = { scene, capacity: 0, mesh: null };
  ensureContactShadowCapacity(pool, CONTACT_SHADOW_INITIAL_CAPACITY);
  return pool;
}

function ensureContactShadowCapacity(pool, desired) {
  if (!pool || desired <= pool.capacity) return;
  const nextCapacity = Math.max(desired, pool.capacity ? pool.capacity * 2 : CONTACT_SHADOW_INITIAL_CAPACITY);
  const previous = pool.mesh;
  const mesh = new THREE.InstancedMesh(getContactShadowGeo(), getContactShadowMat(), nextCapacity);
  mesh.name = 'ContactShadow_Pool';
  mesh.count = 0;
  mesh.renderOrder = -2;
  mesh.frustumCulled = false;
  mesh.castShadow = false;
  mesh.receiveShadow = false;
  mesh.userData.sharedContactShadow = true;
  mesh.userData.contactShadowPool = true;
  mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
  pool.mesh = mesh;
  pool.capacity = nextCapacity;
  if (previous && pool.scene) {
    pool.scene.remove(previous);
    if (typeof previous.dispose === 'function') previous.dispose();
  }
  if (pool.scene) pool.scene.add(mesh);
}

function syncContactShadowPool(pool, entities, meshes) {
  if (!pool || !pool.mesh || !Array.isArray(entities)) return;
  let count = 0;
  for (const entity of entities) {
    if (!entity || entity.alive === false || entity._noShadow) continue;
    if (entity.type !== 'ship' && entity.type !== 'station') continue;
    const mesh = meshes && meshes.get(entity.id);
    if (!mesh || !(mesh.userData && mesh.userData.hasContactShadow)) continue;
    ensureContactShadowCapacity(pool, count + 1);
    const radius = Number(mesh.userData.contactShadowRadius) || Math.max(16, (entity.radius || 28) * 1.4);
    CONTACT_SHADOW_POS.set(entity.pos && Number.isFinite(entity.pos.x) ? entity.pos.x : mesh.position.x, -0.5,
      entity.pos && Number.isFinite(entity.pos.z) ? entity.pos.z : mesh.position.z);
    CONTACT_SHADOW_SCALE.set(radius, radius, radius);
    CONTACT_SHADOW_MATRIX.compose(CONTACT_SHADOW_POS, CONTACT_SHADOW_QUAT, CONTACT_SHADOW_SCALE);
    pool.mesh.setMatrixAt(count, CONTACT_SHADOW_MATRIX);
    count++;
  }
  pool.mesh.count = count;
  pool.mesh.visible = count > 0;
  pool.mesh.instanceMatrix.needsUpdate = true;
}

function requestAuthoredUpgrade(mesh, renderer, scene) {
  const request = mesh && mesh.userData && mesh.userData.requestAuthoredUpgrade;
  if (typeof request !== 'function') return;
  try { request(renderer, scene); }
  catch (error) { console.warn('[render] authored asset upgrade request failed', error); }
}

function configureShadowCasters(root) {
  root.traverse((o) => {
    if (!o.isMesh) return;
    if (!o.visible) { o.castShadow = false; o.receiveShadow = false; return; }
    if (o.userData && o.userData.spacefaceNoShadow) { o.castShadow = false; o.receiveShadow = false; return; }
    if (o.userData && o.userData.sharedContactShadow) { o.castShadow = false; return; }
    const mats = Array.isArray(o.material) ? o.material : [o.material];
    const casts = mats.some((m) => m && !m.transparent && m.depthWrite !== false && (m.opacity == null || m.opacity >= 1) && m.blending === THREE.NormalBlending);
    o.castShadow = casts;
    // GR-2: opaque hulls also RECEIVE shadows — a ship resting on a station pad should be shaded by
    // the station's superstructure, and ships in formation should shadow each other. The same opacity
    // test as casting: transparent shields/engine-plumes neither cast nor receive (they'd self-shadow
    // and flicker). This is what gives ships groundedness beyond the fake contact-shadow disc.
    o.receiveShadow = casts;
  });
}

const _plane = new THREE.Plane(new THREE.Vector3(0, 1, 0), 0);
const _ray = new THREE.Raycaster();
const _pt = new THREE.Vector3();
const _v2 = new THREE.Vector2();
const _drawSize = new THREE.Vector2();

export const render = {
  name: 'render',
  init(ctx) {
    this.state = ctx.state;
    this.bus = ctx.bus;
    const state = ctx.state, bus = ctx.bus;

    const canvas = document.getElementById('gl-canvas');
    // preserveDrawingBuffer is needed only by the explicit /__shot ship capture route. Keeping it off
    // during normal dev and perf probes avoids a readback-friendly WebGL path that players never use.
    const query = typeof location !== 'undefined' ? new URLSearchParams(location.search) : null;
    const devShot = !!(query && query.get('dev') === 'shipshot');
    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, powerPreference: 'high-performance', preserveDrawingBuffer: devShot });
    renderer.setClearColor(0x060912, 1);
    const drawSize = applyRendererSize(renderer, state);

    const scene = new THREE.Scene();
    const corePalette = SECTOR_PALETTE_CLASSES.core;
    // Thin fog for gentle depth cueing only — the old 0.00085 erased the entire backdrop, leaving a
    // black void. This keeps the nebula + far stars visible while still fading the deep distance.
    scene.fog = new THREE.FogExp2(corePalette.fog, corePalette.fogDensity);
    const ambient = new THREE.AmbientLight(corePalette.ambient, SECTOR_LIGHT_INTENSITIES.ambient); scene.add(ambient);
    const key = new THREE.DirectionalLight(corePalette.key, SECTOR_LIGHT_INTENSITIES.key); key.position.set(60, 140, 40); scene.add(key);
    const rim = new THREE.DirectionalLight(corePalette.rim, SECTOR_LIGHT_INTENSITIES.rim); rim.position.set(-70, 50, -60); scene.add(rim);
    const fill = new THREE.DirectionalLight(corePalette.fill, SECTOR_LIGHT_INTENSITIES.fill); fill.position.set(20, 30, 120); scene.add(fill);

    // Real shadow maps (graphics spec Workstream G). Gated behind settings.video.shadows (default
    // true). The key light becomes a shadow caster with a tight frustum that follows the player so
    // ships/stations cast real shadows on the play plane — a groundedness the contact-shadow disc
    // only faked. The bloom contract (bloom.js) is untouched: shadows write to the depth buffer
    // during the normal scene render, before bloom samples it.
    const shadowsOn = !(state.settings && state.settings.video && state.settings.video.shadows === false);
    if (shadowsOn) {
      renderer.shadowMap.enabled = false;
      renderer.shadowMap.type = THREE.PCFSoftShadowMap;
      key.castShadow = true;
      key.shadow.mapSize.set(2048, 2048);
      // Orthographic frustum sized to the local play area around the player (updated per frame in
      // renderFrame to follow the player). Tight bounds = crisp shadows at usable resolution.
      const SC = key.shadow.camera;
      SC.near = 10; SC.far = 600;
      SC.left = -700; SC.right = 700; SC.top = 700; SC.bottom = -700;
      SC.updateProjectionMatrix();
      key.shadow.bias = -0.0008;
      key.shadow.normalBias = 0.04;
      key.target = new THREE.Object3D(); scene.add(key.target);
    }

    const cam = createChaseCamera(state);
    const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
    const vf = createVisualFactory();
    // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
    // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
    // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
    installVisualOverrides(vf, {
      onAuthoredAssetSwap: () => { this._shadowReceiversDirty = true; },
    });

    // Bake a PMREM environment map from the nebula backdrop (scene.background) so chrome/authority
    // hulls can mirror the actual space around them — real reflections of the nebula + stars rather
    // than a canned gradient. Done once after the starfield sets scene.background; the resulting
    // envMap is exposed on state.render for the visual factory to attach to high-metalness hulls.
    // Factored into a method (_bakeEnv) so WebGL context-loss recovery can re-bake it: a lost GL
    // context invalidates the envMap GPU texture, and without re-baking chrome hulls go matte after
    // a driver/GPU hiccup.
    this._envMap = null;
    try {
      // wait one frame so scene.background (an async-decoded CanvasTexture) is present, then bake
      const bakeEnv = () => this._bakeEnv();
      setTimeout(bakeEnv, 120); // let the starfield's async background decode first
    } catch (_) { /* PMREM unavailable */ }

    // WebGL context-loss recovery. The browser fires webglcontextlost when the GPU driver resets
    // (driver crash, sleep/wake, VRAM exhaustion). THREE's WebGLRenderer only stops rendering on
    // loss — it does NOT restore the env map, re-upload procedural textures, or rebuild GPU state,
    // so without handling this the game silently freezes / goes black with no recovery path.
    // On lost: preventDefault (tells the browser we'll recover), set a flag so renderFrame skips
    // work while the context is gone. On restored: re-bake the PMREM env, force a full mesh
    // reconciliation (re-builds every entity mesh → re-uploads geometries/materials), re-apply
    // renderer config, and re-apply the video settings that drive tone mapping / shadow state.
    this._contextLost = false;
    if (canvas) {
      canvas.addEventListener('webglcontextlost', (ev) => {
        ev.preventDefault();        // allow restoration
        this._contextLost = true;
        if (typeof console !== 'undefined') console.warn('[render] WebGL context lost — awaiting restore');
        bus.emit('toast', { text: 'Graphics context lost — recovering…', kind: 'warn', ttl: 4 });
      }, false);
      canvas.addEventListener('webglcontextrestored', () => {
        if (typeof console !== 'undefined') console.warn('[render] WebGL context restored — rebuilding GPU resources');
        this._contextLost = false;
        try {
          // Re-apply renderer config that the new context defaults lose.
          this.renderer.setClearColor(0x060912, 1);
          if (this._shadowSettingOn && this._keyLight) this.renderer.shadowMap.enabled = false; // re-gated by _syncShadowMapEnabled on next frame
          // Re-bake the PMREM env (the old GPU texture is gone).
          this._bakeEnv();
          // Invalidate authored-asset and factory caches so the next rebuild reloads GLBs and
          // recreates materials against the restored context rather than reusing stale GPU handles.
          invalidateAuthoredAsset(renderer);
          invalidateVisualFactoryCaches();
          invalidatePartsLibraryCaches(renderer);
          // Rebuild the bloom post-process pipeline (its render targets are tied to the lost context).
          if (this.bloom && typeof this.bloom.rebuild === 'function') this.bloom.rebuild();
          // Force every entity mesh to rebuild so geometries/materials re-upload. The cleanest way
          // is to clear + reconcile: dispose the CPU mesh objects, then reconcileMeshes() rebuilds
          // them from the live entityList via the visual factory.
          this.clearAllMeshes(false);
          this._meshReconcileDirty = true;
          // Re-apply bloom + video settings (tone mapping / exposure live on settings:changed).
          bus.emit('settings:changed', { section: 'video' });
          bus.emit('toast', { text: 'Graphics recovered.', kind: 'good', ttl: 3 });
        } catch (err) {
          if (typeof console !== 'undefined') console.error('[render] context-restore rebuild failed', err);
        }
      }, false);
    }

    // Preload the menu background (the only generated .jpg we use — the rest are captioned
    // contact-sheet references and are replaced by procedural materials / inline SVG).
    { const i = new Image(); i.src = 'assets/cinematics/menu_background.jpg'; }

    this.renderer = renderer; this.scene = scene; this.cam = cam; this.starfield = starfield; this.vf = vf;
    this.authoredPartLibraryReady = preloadAuthoredPartLibrary(renderer).catch((error) => {
      console.warn('[render] authored part library preload failed', error);
      return null;
    });
    state.render.authoredPartLibraryReady = this.authoredPartLibraryReady;
    this._sectorPaletteRig = createSectorPaletteRig(scene, ambient, key, rim, fill);
    this._sectorPaletteTarget = corePalette;
    state.render.sectorPalette = corePalette;
    this._keyLight = shadowsOn ? key : null; // referenced by _updateShadowFollow() each frame
    this._shadowSettingOn = shadowsOn;
    this._shadowReceiversDirty = true;
    this._shadowReceiverCount = 0;
    this._contactShadowPool = createContactShadowPool(scene);
    this.planetFactory = createPlanetFactory();
    this._planetBodies = [];
    // LOD projector viewport (CSS px); onResize refreshes it. Initialize from drawSize so the first
    // frame before onResize has sane values.
    { const dpr = renderer.getPixelRatio() || 1; this.viewport = { width: drawSize.x / dpr, height: drawSize.y / dpr }; }
    try { this.bloom = createBloom(renderer, drawSize.x, drawSize.y); }
    catch (err) { console.warn('[render] bloom unavailable, falling back:', err); this.bloom = null; }
    // Collision/socket/landing-contact debug visualization (spec §12.5). OFF by default; toggled via
    // the render system handle (state.render.debug.toggle) — wired to F7 in ui/input.js.
    try { this.collisionDebug = createCollisionDebug(this); }
    catch (err) { console.warn('[render] collision debug unavailable:', err); this.collisionDebug = null; }
    this._meshes = new Map(); // entityId -> Object3D
    this._meshBuildQueue = [];
    this._meshBuildQueuedIds = new Set();
    this._hazardVisuals = []; // hazard zone visual meshes for the current sector
    this._meshReconcileDirty = true;
    this._initialMeshReconcileComplete = false;
    // Renderer diagnostics: window.__THREE_GAME_DIAGNOSTICS__ (draw calls/tris/memory + frame timing).
    try {
      this.diag = installDiagnostics(renderer, {
        entities: () => state.entityList.length,
        particles: () => {
          const sys = ctx.registry && ctx.registry.get('vfx');
          return sys ? sys._liveCount : 0;
        },
        sprites: () => {
          const sys = ctx.registry && ctx.registry.get('vfx');
          return sys ? (sys._liveSpriteCount || 0) : 0;
        },
        lights: () => {
          const sys = ctx.registry && ctx.registry.get('vfx');
          const pool = sys && sys._lights;
          if (!pool) return 0;
          let n = 0;
          for (const slot of pool) if (slot && slot.obj && slot.obj.visible) n++;
          return n;
        },
        perf: () => state.perfRuntime && state.perfRuntime.getReport ? state.perfRuntime.getReport() : {},
        settings: () => ({ video: { ...((state.settings && state.settings.video) || {}) } }),
        scenePools: () => getAuthoredInstancePoolDiagnostics(scene),
        post: () => ({
          activePath: this._lastRenderPath || null,
          bloomSelected: !!(this.bloom && state.settings && state.settings.video && state.settings.video.bloom !== false),
          bloom: this.bloom && typeof this.bloom.diagnostics === 'function' ? this.bloom.diagnostics() : null,
          renderGraph: !!this._renderGraph,
        }),
      });
      state.render.diagnostics = this.diag;
    }
    catch (err) { console.warn('[render] diagnostics unavailable:', err); this.diag = null; }

    state.render.scene = scene;
    state.render.renderer = renderer;
    state.render.camera = cam.obj;
    state.render.cameraCtrl = cam;   // controller (addTrauma/pushZoom) — exposed for feel.js / ui
    state.render.vf = vf;   // exposed for the dev-only ship turntable preview (shipPreview.js)
    state.render.warmPostProcess = () => (this.bloom && state.settings.video.bloom !== false ? this.bloom.render(scene, cam.obj) : renderer.render(scene, cam.obj));
    // Collision/socket/landing debug toggle (spec §12.5), bound to F7 in ui/input.js. Capture the
    // render-system `this` once so the handle closures resolve the live collisionDebug regardless of
    // how they're invoked (method `this` would otherwise bind to the debug handle object itself).
    const renderSys = this;
    state.render.debug = {
      get on() { return renderSys.collisionDebug ? renderSys.collisionDebug.on : false; },
      toggle: () => renderSys.collisionDebug ? renderSys.collisionDebug.toggle() : false,
      set: (v) => { if (renderSys.collisionDebug) renderSys.collisionDebug.setDebug(v); },
    };
    state.camera.obj = cam.obj;

    ctx.helpers.worldToScreen = (v) => this.worldToScreen(v);
    ctx.helpers.raycastToPlane = (ndc) => this.raycastToPlane(ndc);
    ctx.helpers.addTrauma = (a) => cam.addTrauma(a);
    ctx.helpers.socketWorldPose = (id, name) => this.socketWorldPose(id, name);
    ctx.helpers.socketWorldPos = (id, name) => this.socketWorldPos(id, name);

    bus.on('entity:spawned', () => { this._meshReconcileDirty = true; });
    bus.on('entity:destroyed', ({ id }) => {
      const m = this._meshes.get(id);
      if (m) { scene.remove(m); disposeObject(m); this._meshes.delete(id); }
    });
    // Ship hull swap or loadout change (fit/upgrade) — rebuild the mesh so visible hardpoints,
    // engines and tier reflect the current ship. Without this the mesh is frozen at spawn and a
    // shipyard hull switch or fitted weapon never shows. Mirrors the spawn path: dispose old,
    // build new, re-seat from the entity's live transform.
    bus.on('ship:appearanceChanged', ({ id }) => render.rebuildShipMesh(id));
    bus.on('camera:shake', ({ amount }) => cam.addTrauma(amount || 0.3));
    bus.on('camera:zoom', ({ delta, level }) => { if (level != null) cam.setZoom(level); else cam.setZoom(state.camera.zoom + (delta || 0)); });
    bus.on('game:started', () => cam.snapToPlayer && cam.snapToPlayer());
    bus.on('save:loaded', () => cam.snapToPlayer && cam.snapToPlayer());
    bus.on('player:respawn', () => cam.snapToPlayer && cam.snapToPlayer());
    // Live-apply video settings changes. Without this, dragging Bloom strength / FOV / particle
    // quality in the settings screen did nothing (only the initial value was used) — a "slider that
    // doesn't work" sore thumb. We forward the values to the systems that own them.
    bus.on('settings:changed', (p) => {
      if (!p || p.section !== 'video') return;
      const vd = state.settings.video;
      if (this.bloom) this.bloom.setOptions({ bloom: vd.bloom, strength: vd.bloomStrength, threshold: vd.bloomThreshold, exposure: vd.exposure, acesToneMapping: vd.acesToneMapping !== false });
      if (p.key === 'shadows' || p.key == null) {
        this._shadowSettingOn = vd.shadows !== false;
        this._shadowReceiversDirty = true;
      }
      if (p.key === 'renderScale' || p.key === 'pixelRatioCap' || p.key == null) this.onResize();
      // FOV: the feel system (feel.js) adds a transient punch on top of this base. We update the
      // camera's base fov here; feel.frame() re-derives its cached base from settings when no punch
      // is active, so the slider and the punch never fight.
      if (p.key === 'fov' || p.key == null) {
        const camObj = state.render.camera;
        if (camObj && camObj.isPerspectiveCamera && typeof vd.fov === 'number') {
          camObj.fov = vd.fov;
          camObj.updateProjectionMatrix();
        }
      }
    });
    // On sector change, reconcile rather than blindly clearing: the new sector's entities are
    // already spawned by the time this fires (enterSector spawns before its sector:enter resolves),
    // so a blind clearAllMeshes(keepPlayer) used to wipe the station/asteroids and leave the player
    // alone in empty space. reconcileMeshes() removes only meshes for entities that are gone.
    bus.on('sector:enter', ({ sector } = {}) => {
      this._meshReconcileDirty = true;
      if (cam.snapToPlayer) cam.snapToPlayer();
      this._updatePlanetBodies(sector);
      this._beginSectorPaletteTransition(sector);
      this._updateHazardVisuals(sector);
      precompilePipelines(renderer, scene, cam.obj, { sector }).catch((error) => console.warn('[render] sector pipeline precompile failed', error));
    });
    bus.on('jump:arrive', ({ sectorId } = {}) => {
      const sector = sectorId && state.world && state.world.sectors ? state.world.sectors[sectorId] : null;
      this._beginSectorPaletteTransition(sector);
    });
    bus.on('save:loaded', () => { this._meshReconcileDirty = true; });

    window.addEventListener('resize', () => this.onResize());
  },

  clearAllMeshes(keepPlayer) {
    for (const [id, m] of [...this._meshes]) {
      if (keepPlayer && id === this.state.playerId) continue;
      this.scene.remove(m); disposeObject(m); this._meshes.delete(id);
    }
    this._meshBuildQueue.length = 0;
    this._meshBuildQueuedIds.clear();
    // Also clear hazard zone visuals
    for (const obj of this._hazardVisuals) { this.scene.remove(obj); disposeObject(obj); }
    this._hazardVisuals = [];
  },

  // Bake (or re-bake) the PMREM environment map from the current nebula backdrop. Called once at
  // init after the starfield background decodes, AND on WebGL context restore (a lost GL context
  // invalidates the envMap GPU texture — without re-baking, chrome hulls go matte after recovery).
  _bakeEnv() {
    try {
      const renderer = this.renderer, scene = this.scene, state = this.state;
      const pmrem = new THREE.PMREMGenerator(renderer);
      const envMap = scene.background && scene.background.isTexture
        ? pmrem.fromEquirectangular(scene.background).texture
        : pmrem.fromScene(scene, 0, 0.1, 1000).texture;
      pmrem.dispose();
      // Dispose the previous env GPU texture if we're re-baking (context restore path).
      if (this._envMap && this._envMap !== envMap) {
        try { this._envMap.dispose(); } catch (_) {}
      }
      this._envMap = envMap;
      state.render.envMap = envMap;
      setEnvMapForShips(envMap);   // hand it to the visual factory for chrome/authority hulls
      if (scene.environment === null || scene.environment === this._envMap) scene.environment = envMap;
    } catch (_) { /* env-map optional — chrome falls back to high-metalness matte */ }
  },

  // Self-healing entity<->mesh reconciliation. Guarantees every alive, renderable entity has a
  // scene mesh and that meshes for gone entities are disposed — independent of event ordering.
  // This is the safety net that makes the world actually render (entity:spawned alone was being
  // undone by the old sector:enter clear). Cheap: only builds/destroys on a delta.
  reconcileMeshes() {
    const state = this.state;
    const buildBudget = this._initialMeshReconcileComplete ? RUNTIME_MESH_BUILD_BUDGET : Infinity;
    // remove meshes whose entity no longer exists or has died
    for (const [id, m] of this._meshes) {
      const e = state.entities.get(id);
      if (!e || e.alive === false) { this.scene.remove(m); disposeObject(m); this._meshes.delete(id); this._shadowReceiversDirty = true; }
    }
    // Queue alive entities that lack meshes (fx are particle-managed by vfx -> mark + skip).
    for (const e of state.entityList) {
      if (e._noMesh || this._meshes.has(e.id)) continue;
      if (!this._meshBuildQueuedIds.has(e.id)) {
        this._meshBuildQueue.push(e.id);
        this._meshBuildQueuedIds.add(e.id);
      }
    }
    const built = this._drainMeshBuildQueue(buildBudget);
    this._meshReconcileDirty = this._meshBuildQueue.length > 0;
    if (!this._meshReconcileDirty) this._initialMeshReconcileComplete = true;
    return built;
  },

  _drainMeshBuildQueue(buildBudget) {
    let built = 0;
    while (this._meshBuildQueue.length && built < buildBudget) {
      const id = this._meshBuildQueue.shift();
      this._meshBuildQueuedIds.delete(id);
      const e = this.state.entities.get(id);
      if (!e || e.alive === false || e._noMesh || this._meshes.has(id)) continue;
      const m = this.vf.build(e);
      if (!m) { e._noMesh = true; continue; }
      m.position.set(e.pos.x, 0, e.pos.z);
      m.rotation.y = -e.rot;
      if (e.type === 'ship' || e.type === 'station') { attachContactShadow(m, e); configureShadowCasters(m); }
      e.mesh = m; e.view = { root: m };
      this._meshes.set(e.id, m);
      this.scene.add(m);
      requestAuthoredUpgrade(m, this.renderer, this.scene);
      this._shadowReceiversDirty = true;
      built++;
    }
    return built;
  },

  // Rebuild one ship's mesh after a hull swap or loadout change. Disposes the old Object3D, builds a
  // fresh one from the (now-updated) entity, and re-seats it from the entity's live transform so it
  // doesn't snap. Player-only in practice, but safe for any ship id. Textures/geo/materials are
  // cached in the factory (never disposed), so only the per-entity Object3D graph is freed here —
  // exactly the same lifecycle the per-entity disposer in disposeObject() already assumes.
  rebuildShipMesh(id) {
    const e = this.state.entities.get(id);
    if (!e || e.alive === false) return;
    const old = this._meshes.get(id);
    if (old) { this.scene.remove(old); disposeObject(old); this._meshes.delete(id); this._shadowReceiversDirty = true; }
    const m = this.vf.build(e);
    if (!m) return;
    m.position.set(e.pos.x, 0, e.pos.z);
    m.rotation.y = -e.rot;
    // carry the bank pose so the rebuilt hull doesn't momentarily sit level mid-turn
    const hull = m.userData && m.userData.hull;
    if (hull && e.bank != null) hull.rotation.x = e.bank;
    if (e.type === 'ship' || e.type === 'station') { attachContactShadow(m, e); configureShadowCasters(m); }
    e.mesh = m; e.view = { root: m };
    this._meshes.set(id, m);
    this.scene.add(m);
    requestAuthoredUpgrade(m, this.renderer, this.scene);
    this._shadowReceiversDirty = true;
  },


  syncEntityViews(alpha) {
    const now = typeof performance !== 'undefined' ? performance.now() * 0.001 : 0;
    for (const e of this.state.entityList) {
      const m = e.mesh; if (!m) continue;
      if (this.collisionDebug && this.collisionDebug.on) m.userData.__lastEntity = e; // read-only debug overlay
      const hull = m.userData && m.userData.hull;   // bankable inner group (ships only)
      if (e.flags.noInterp) {
        m.position.set(e.pos.x, 0, e.pos.z); m.rotation.y = -e.rot;
        if (hull && e.bank != null) hull.rotation.x = e.bank; // roll around forward axis; +bank banks right
      } else {
        m.position.x = e.prevPos.x + (e.pos.x - e.prevPos.x) * alpha;
        m.position.z = e.prevPos.z + (e.pos.z - e.prevPos.z) * alpha;
        m.position.y = 0;
        let dr = e.rot - e.prevRot;
        dr = ((dr + Math.PI) % (Math.PI * 2)) - Math.PI; if (dr < -Math.PI) dr += Math.PI * 2;
        m.rotation.y = -(e.prevRot + dr * alpha);
        // interpolate bank for a smooth roll (prevBank snapshotted in core.preStep each step)
        if (hull && e.bank != null) {
          const pb = e.prevBank || 0;
          hull.rotation.x = pb + (e.bank - pb) * alpha;
        }
      }
      // Hero-asset damage states (spec §9.11): hero meshes carry an updateDamageState closure that
      // modulates light groups / armor / drive from the live hull fraction so damage reads without the
      // HUD bar. Cheap no-op for non-hero meshes (no closure). Called once per frame per entity.
      if (m.userData.updateDamageState) m.userData.updateDamageState(e, now);

      // GR-5: persistent 3D shield bubble visibility + impact flash. Shown while shields hold; the
      // flash decays each frame and is punched up whenever the entity's shield value drops (impact).
      const sb = m.userData.shieldBubble;
      if (sb) {
        const up = e.shield > 0;
        if (sb.visible !== up) sb.visible = up;
        if (up) {
          const u = sb.material.uniforms;
          // detect shield loss since last frame -> punch the fresnel flash
          const prev = sb.userData._prevShield != null ? sb.userData._prevShield : e.shield;
          if (e.shield < prev - 0.5) u.uFlash.value = Math.min(1, u.uFlash.value + 0.8);
          sb.userData._prevShield = e.shield;
          // frame-rate-independent exponential decay: uFlash *= 0.05^(dt) settles in ~0.4s at any fps.
          const dt = Math.min(0.1, now - (sb.userData._prevFlashT != null ? sb.userData._prevFlashT : now));
          sb.userData._prevFlashT = now;
          u.uFlash.value *= Math.pow(0.05, dt);
        }
      }
      // Projected-screen-size LOD (spec §12.4): resolve each entity's detail level from its projected
      // pixel width with hysteresis, so assets can drop detail at distance. The selector owns no
      // geometry; per-asset hooks read m.userData.lod.level and decide what to show. Cheap for entities
      // without a lod state (no closure attached).
      if (m.userData.lod && m.userData.updateLod) {
        const px = projectedWidthPx(e.pos, e.radius, this.cam.obj, this.viewport);
        const level = m.userData.lod.resolve(px);
        m.userData.updateLod(level);
      }
    }
  },

  _updatePlanetBodies(sector) {
    for (const b of this._planetBodies) { this.scene.remove(b.mesh); }
    this.planetFactory.disposeBodies(this._planetBodies);
    this._planetBodies = this.planetFactory.buildSectorBodies(sector);
    for (const b of this._planetBodies) {
      b.mesh.position.copy(b.basePos);
      this.scene.add(b.mesh);
    }
    this._shadowReceiversDirty = true;
  },

  _updatePlanetParallax() {
    const cam = this.cam.obj.position;
    // GR-4: advance the planet cloud-drift uniform from the background clock (sim-scaled, not wall
    // clock) so hit-stop/pause also stills the clouds. Sun bodies have no uTime uniform; planet
    // surface materials do — the lazy read avoids a per-body branch on suns.
    const t = this._bgTime || 0;
    for (const b of this._planetBodies) {
      b.mesh.position.x = b.basePos.x + cam.x * (1 - b.parallax);
      b.mesh.position.z = b.basePos.z + cam.z * (1 - b.parallax);
      const u = b.mesh.material && b.mesh.material.uniforms && b.mesh.material.uniforms.uTime;
      if (u) u.value = t;
    }
  },

  // --------------- hazard zone visuals ------------------------------------------------
  // Create a radial gradient CanvasTexture: bright center color fading to transparent edge.
  _makeHazardTexture(hexColor, centerAlpha, edgeAlpha) {
    const size = 256;
    const c = document.createElement('canvas'); c.width = c.height = size;
    const ctx = c.getContext('2d');
    const half = size / 2;
    const g = ctx.createRadialGradient(half, half, 0, half, half, half);
    // Parse hex to r,g,b
    const r = parseInt(hexColor.slice(1, 3), 16);
    const gr = parseInt(hexColor.slice(3, 5), 16);
    const b = parseInt(hexColor.slice(5, 7), 16);
    g.addColorStop(0.0, `rgba(${r},${gr},${b},${centerAlpha})`);
    g.addColorStop(0.5, `rgba(${r},${gr},${b},${centerAlpha * 0.6})`);
    g.addColorStop(0.85, `rgba(${r},${gr},${b},${edgeAlpha * 0.5})`);
    g.addColorStop(1.0, `rgba(${r},${gr},${b},${edgeAlpha})`);
    ctx.fillStyle = g; ctx.fillRect(0, 0, size, size);
    const tex = new THREE.CanvasTexture(c);
    tex.needsUpdate = true;
    return tex;
  },

  _updateHazardVisuals(sector) {
    // Dispose previous hazard visuals
    for (const obj of this._hazardVisuals) {
      this.scene.remove(obj);
      disposeObject(obj);
    }
    this._hazardVisuals = [];

    if (!sector || !sector.hazards || sector.hazards.length === 0) return;

    // Color/opacity config per hazard type
    const hazardStyles = {
      radiation:       { color: '#66ff44', centerAlpha: 0.18, edgeAlpha: 0.04, ring: true,  ringColor: 0x44ff22 },
      nebula:          { color: '#7744ff', centerAlpha: 0.15, edgeAlpha: 0.03, ring: false, ringColor: 0x7744ff },
      dense_asteroid:  { color: '#aa7744', centerAlpha: 0.10, edgeAlpha: 0.02, ring: false, ringColor: 0xaa7744 },
      debris:          { color: '#778899', centerAlpha: 0.12, edgeAlpha: 0.03, ring: false, ringColor: 0x778899 },
    };

    for (const hz of sector.hazards) {
      const style = hazardStyles[hz.type] || hazardStyles.debris;
      const intensityScale = hz.intensity != null ? hz.intensity : 0.5;

      // --- Main disc ---
      const discGeo = new THREE.CircleGeometry(hz.radius, 64);
      const tex = this._makeHazardTexture(style.color, style.centerAlpha * intensityScale, style.edgeAlpha * intensityScale);
      const discMat = new THREE.MeshBasicMaterial({
        map: tex,
        transparent: true,
        blending: THREE.AdditiveBlending,
        depthWrite: false,
        side: THREE.DoubleSide,
      });
      const disc = new THREE.Mesh(discGeo, discMat);
      disc.rotation.x = -Math.PI / 2;
      disc.position.set(hz.center.x, -0.5, hz.center.z);
      disc.renderOrder = -3; // below contact shadows
      disc.frustumCulled = false;
      this.scene.add(disc);
      this._hazardVisuals.push(disc);

      // --- Boundary ring (radiation zones get a visible edge ring) ---
      if (style.ring) {
        const ringInner = hz.radius - 4;
        const ringOuter = hz.radius + 4;
        const ringGeo = new THREE.RingGeometry(ringInner, ringOuter, 64);
        const ringMat = new THREE.MeshBasicMaterial({
          color: style.ringColor,
          transparent: true,
          opacity: 0.25 * intensityScale,
          blending: THREE.AdditiveBlending,
          depthWrite: false,
          side: THREE.DoubleSide,
        });
        const ring = new THREE.Mesh(ringGeo, ringMat);
        ring.rotation.x = -Math.PI / 2;
        ring.position.set(hz.center.x, -0.4, hz.center.z);
        ring.renderOrder = -2;
        ring.frustumCulled = false;
        this.scene.add(ring);
        this._hazardVisuals.push(ring);
      }
    }
  },

  _beginSectorPaletteTransition(sector) {
    const rig = this._sectorPaletteRig;
    if (!rig) return;
    const palette = sector && sector.palette ? sector.palette : SECTOR_PALETTE_CLASSES.core;
    this.state.render.sectorPalette = palette;
    if (palette === this._sectorPaletteTarget) return;

    this._sectorPaletteTarget = palette;
    writeRigToSectorPaletteFrame(rig.start, rig);
    writePaletteToSectorPaletteFrame(rig.target, palette);
    rig.elapsed = 0;
    rig.active = true;

    if (this.starfield && this.starfield.setSectorTint) {
      this.starfield.setSectorTint(palette.nebulaTint);
    }
  },

  _updateSectorPaletteTransition(frameDt) {
    const rig = this._sectorPaletteRig;
    if (!rig || !rig.active) return;
    const dt = Number.isFinite(frameDt) ? Math.max(0, frameDt) : 0;
    rig.elapsed = Math.min(SECTOR_PALETTE_LERP_SECONDS, rig.elapsed + dt);
    const rawT = SECTOR_PALETTE_LERP_SECONDS > 0 ? rig.elapsed / SECTOR_PALETTE_LERP_SECONDS : 1;
    const t = rawT * rawT * (3 - 2 * rawT);
    lerpSectorPaletteFrame(rig, rig.start, rig.target, t);
    if (rawT >= 1) {
      rig.active = false;
      applySectorPaletteFrame(rig, rig.target);
    }
  },

  prepareFrame(alpha, frameDt) {
    // While the GL context is lost, the renderer can't draw — skip all per-frame work until
    // webglcontextrestored rebuilds GPU resources. (cam.follow etc. would run against a dead
    // renderer; the context-restore handler re-applies everything that matters when it returns.)
    if (this._contextLost) return false;
    if (this._meshReconcileDirty) this.reconcileMeshes();
    this.syncEntityViews(alpha);
    this.cam.follow(frameDt);
    syncContactShadowPool(this._contactShadowPool, this.state.entityList, this._meshes);
    syncAuthoredInstancePools(this.scene, { camera: this.cam.obj });
    this.starfield.recenter(this.cam.obj.position);
    // Background-clock for distant animation (planet cloud drift, hero-star twinkle). Integrates real
    // frame dt scaled by state.timeScale so the cosmos respects hit-stop/pause — a death freeze
    // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
    this._updateSectorPaletteTransition(frameDt);
    const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
    this._bgTime = (this._bgTime || 0) + frameDt * ts;
    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
    this._updatePlanetParallax();
    this._syncShadowMapEnabled();
    // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
    // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is
    // an offset from its target; we move both together. No-op if shadows are disabled.
    this._updateShadowFollow();
    // Collision/socket/landing debug overlay (spec §12.5). Repositions pooled markers over the live
    // meshes once per frame; a cheap no-op when off (the group is hidden + nothing iterates).
    if (this.collisionDebug && this.collisionDebug.on) this.collisionDebug.update();
    return true;
  },

  drawPreparedFrame() {
    if (this._contextLost) return false;
    // Render path selection (INTEGRATION_MAP §8.1). The SpaceRenderGraph is a capability-aware HDR
    // pipeline (GTAO-lite ambient occlusion + multiscale bloom + ACES/grade composite) that
    // supersedes the monolithic bloom wrapper. It is opt-in behind settings.video.renderGraph so the
    // proven bloom path stays the default; the render graph module is no longer tree-shaken because
    // it is reachable from this live branch. The energy materials I wired write HDR radiance that the
    // render graph composites with contact-depth AO.
    if (this.state.settings.video.renderGraph && this._ensureRenderGraph()) {
      this._lastRenderPath = 'renderGraph';
      this._renderGraph.render(this.scene, this.cam.obj, { time: this._bgTime || 0 });
    } else if (this.bloom && this.state.settings.video.bloom !== false) {
      this._lastRenderPath = 'bloom';
      this.bloom.render(this.scene, this.cam.obj);
    } else {
      this._lastRenderPath = 'straight';
      this.renderer.render(this.scene, this.cam.obj);
    }
    return true;
  },

  renderFrame(alpha, frameDt) {
    if (!this.prepareFrame(alpha, frameDt)) return;
    this.drawPreparedFrame();
  },

  // Center the key light + its shadow camera on the player each frame. The light direction stays
  // fixed (60,140,40 offset); only the origin translates so shadows track the player across the
  // sector instead of being pinned to world (0,0,0) and clipping at the frustum edge.
  _updateShadowFollow() {
    if (!this._keyLight) return;
    if (!this.renderer.shadowMap || !this.renderer.shadowMap.enabled) return;
    const p = this.state.playerId ? (this.state.entities && this.state.entities.get(this.state.playerId)) : null;
    const px = p ? p.pos.x : 0, pz = p ? p.pos.z : 0;
    this._keyLight.position.set(px + 60, 140, pz + 40);
    this._keyLight.target.position.set(px, 0, pz);
    this._keyLight.target.updateMatrixWorld();
  },

  _syncShadowMapEnabled() {
    if (!this._keyLight || !this.renderer.shadowMap) return;
    if (!this._shadowSettingOn) {
      this.renderer.shadowMap.enabled = false;
      this._keyLight.castShadow = false;
      return;
    }
    if (this._shadowReceiversDirty) {
      let receivers = 0;
      this.scene.traverse((o) => { if (o && o.receiveShadow) receivers++; });
      this._shadowReceiverCount = receivers;
      this._shadowReceiversDirty = false;
    }
    const enabled = this._shadowReceiverCount > 0;
    this.renderer.shadowMap.enabled = enabled;
    this._keyLight.castShadow = enabled;
  },

  worldToScreen(v) {
    _pt.set(v.x, v.y || 0, v.z).project(this.cam.obj);
    return {
      x: (_pt.x * 0.5 + 0.5) * window.innerWidth,
      y: (-_pt.y * 0.5 + 0.5) * window.innerHeight,
      onScreen: _pt.z < 1 && Math.abs(_pt.x) <= 1 && Math.abs(_pt.y) <= 1,
    };
  },

  raycastToPlane(ndc) {
    _v2.set(ndc.x, ndc.y);
    _ray.setFromCamera(_v2, this.cam.obj);
    const hit = _ray.ray.intersectPlane(_plane, _pt);
    return hit ? { x: hit.x, z: hit.z } : { x: 0, z: 0 };
  },

  // World XZ of a named attachment socket on an entity's mesh, or null if the entity has no mesh or no
  // such socket. Used by VFX to originate weapon/mining/engine effects from authored hardware (spec
  // §9.9) instead of the entity center. Failure returns null so callers fall back to the payload origin.
  socketWorldPos(entityId, socketName) {
    const pose = this.socketWorldPose(entityId, socketName);
    return pose ? { x: pose.x, z: pose.z } : null;
  },

  socketWorldPose(entityId, socketName) {
    const m = this._meshes.get(entityId);
    if (!m) return null;
    let cache = m.userData.__socketCache;
    if (!cache) cache = m.userData.__socketCache = new Map();
    let socket = cache.get(socketName);
    if (socket === undefined) {
      socket = null;
      m.traverse((o) => { if (!socket && o.userData && o.userData.spacefaceSocket && o.name === socketName) socket = o; });
      cache.set(socketName, socket);
    }
    if (!socket) return null;
    socket.updateWorldMatrix(true, false);
    socket.matrixWorld.decompose(SOCKET_WORLD_POS, SOCKET_WORLD_QUAT, SOCKET_WORLD_SCALE);
    const authoredForward = socket.userData && socket.userData.forward || [1, 0, 0];
    const authoredForwardX = Array.isArray(authoredForward) ? authoredForward[0] : authoredForward.x;
    const authoredForwardY = Array.isArray(authoredForward) ? authoredForward[1] : authoredForward.y;
    const authoredForwardZ = Array.isArray(authoredForward) ? authoredForward[2] : authoredForward.z;
    SOCKET_FORWARD.set(
      Number.isFinite(authoredForwardX) ? authoredForwardX : 1,
      Number.isFinite(authoredForwardY) ? authoredForwardY : 0,
      Number.isFinite(authoredForwardZ) ? authoredForwardZ : 0,
    );
    if (SOCKET_FORWARD.lengthSq() < 1e-8) SOCKET_FORWARD.set(1, 0, 0);
    SOCKET_FORWARD.normalize().applyQuaternion(SOCKET_WORLD_QUAT).normalize();
    return {
      x: SOCKET_WORLD_POS.x,
      y: SOCKET_WORLD_POS.y,
      z: SOCKET_WORLD_POS.z,
      forwardX: SOCKET_FORWARD.x,
      forwardY: SOCKET_FORWARD.y,
      forwardZ: SOCKET_FORWARD.z,
    };
  },

  onResize() {
    const drawSize = applyRendererSize(this.renderer, this.state);
    if (this.bloom) this.bloom.setSize(drawSize.x, drawSize.y);
    if (this._renderGraph) this._renderGraph.setSize(drawSize.x, drawSize.y, this.renderer.getPixelRatio() || 1);
    this.cam.onResize();
    // Cache the CSS-pixel viewport for the LOD projector (projectedWidthPx expects CSS px, matching
    // the projected-width thresholds in spec §12.4). Drawing-buffer size carries devicePixelRatio.
    const dpr = this.renderer.getPixelRatio() || 1;
    this.viewport = { width: drawSize.x / dpr, height: drawSize.y / dpr };
  },

  // Lazily construct the SpaceRenderGraph only when its setting is on (it allocates GPU render
  // targets). Returns false if construction fails (e.g. a low-capability GPU) so the caller falls
  // back to bloom/straight-render. Options mirror the bloom/quality settings where they overlap.
  _ensureRenderGraph() {
    if (this._renderGraph) return true;
    if (this._renderGraphUnavailable) return false;
    try {
      const v = this.state.settings.video || {};
      const drawSize = this.viewport ? { x: this.viewport.width * (this.renderer.getPixelRatio() || 1), y: this.viewport.height * (this.renderer.getPixelRatio() || 1) } : { x: 1280, y: 720 };
      this._renderGraph = new SpaceRenderGraph(this.renderer, {
        enabled: true,
        ao: v.ao !== false,
        bloom: true,
        renderScale: Math.min(1, Math.max(0.5, v.renderScale || 0.7)),
        bloomStrength: v.bloomStrength != null ? v.bloomStrength : 0.9,
        bloomThreshold: v.bloomThreshold != null ? v.bloomThreshold : 0.65,
      });
      this._renderGraph.setSize(drawSize.x, drawSize.y, this.renderer.getPixelRatio() || 1);
      // Expose for diagnostics + the energy-materials depth binding path.
      this.state.render.renderGraph = this._renderGraph;
      return true;
    } catch (err) {
      console.warn('[render] SpaceRenderGraph unavailable, falling back to bloom:', err);
      this._renderGraphUnavailable = true;
      return false;
    }
  },
};

function applyRendererSize(renderer, state) {
  const vd = (state.settings && state.settings.video) || {};
  const cap = finiteInRange(vd.pixelRatioCap, 0.25, 4, 2);
  const scale = finiteInRange(vd.renderScale, 0.5, 2, 1);
  const base = Math.min(window.devicePixelRatio || 1, cap);
  renderer.setPixelRatio(Math.max(0.25, base * scale));
  renderer.setSize(window.innerWidth, window.innerHeight);
  return renderer.getDrawingBufferSize(_drawSize);
}

function finiteInRange(value, min, max, fallback) {
  const n = Number(value);
  if (!Number.isFinite(n)) return fallback;
  return Math.max(min, Math.min(max, n));
}

function createSectorPaletteRig(scene, ambientLight, keyLight, rimLight, fillLight) {
  const rig = {
    scene,
    lights: { ambient: ambientLight, key: keyLight, rim: rimLight, fill: fillLight },
    start: createSectorPaletteFrame(),
    target: createSectorPaletteFrame(),
    elapsed: 0,
    active: false,
  };
  writeRigToSectorPaletteFrame(rig.start, rig);
  writeRigToSectorPaletteFrame(rig.target, rig);
  return rig;
}

function createSectorPaletteFrame() {
  return {
    colors: {
      ambient: new THREE.Color(),
      key: new THREE.Color(),
      rim: new THREE.Color(),
      fill: new THREE.Color(),
      fog: new THREE.Color(),
    },
    intensities: { ambient: 0, key: 0, rim: 0, fill: 0 },
    fogDensity: 0,
  };
}

function writeRigToSectorPaletteFrame(frame, rig) {
  frame.colors.ambient.copy(rig.lights.ambient.color);
  frame.colors.key.copy(rig.lights.key.color);
  frame.colors.rim.copy(rig.lights.rim.color);
  frame.colors.fill.copy(rig.lights.fill.color);
  frame.colors.fog.copy(rig.scene.fog.color);
  frame.intensities.ambient = rig.lights.ambient.intensity;
  frame.intensities.key = rig.lights.key.intensity;
  frame.intensities.rim = rig.lights.rim.intensity;
  frame.intensities.fill = rig.lights.fill.intensity;
  frame.fogDensity = rig.scene.fog.density;
}

function writePaletteToSectorPaletteFrame(frame, palette) {
  frame.colors.ambient.setHex(palette.ambient);
  frame.colors.key.setHex(palette.key);
  frame.colors.rim.setHex(palette.rim);
  frame.colors.fill.setHex(palette.fill);
  frame.colors.fog.setHex(palette.fog);
  frame.intensities.ambient = SECTOR_LIGHT_INTENSITIES.ambient;
  frame.intensities.key = SECTOR_LIGHT_INTENSITIES.key;
  frame.intensities.rim = SECTOR_LIGHT_INTENSITIES.rim;
  frame.intensities.fill = SECTOR_LIGHT_INTENSITIES.fill;
  frame.fogDensity = palette.fogDensity;
}

function applySectorPaletteFrame(rig, frame) {
  rig.lights.ambient.color.copy(frame.colors.ambient);
  rig.lights.key.color.copy(frame.colors.key);
  rig.lights.rim.color.copy(frame.colors.rim);
  rig.lights.fill.color.copy(frame.colors.fill);
  rig.scene.fog.color.copy(frame.colors.fog);
  rig.lights.ambient.intensity = frame.intensities.ambient;
  rig.lights.key.intensity = frame.intensities.key;
  rig.lights.rim.intensity = frame.intensities.rim;
  rig.lights.fill.intensity = frame.intensities.fill;
  rig.scene.fog.density = frame.fogDensity;
}

function lerpSectorPaletteFrame(rig, start, target, t) {
  rig.lights.ambient.color.lerpColors(start.colors.ambient, target.colors.ambient, t);
  rig.lights.key.color.lerpColors(start.colors.key, target.colors.key, t);
  rig.lights.rim.color.lerpColors(start.colors.rim, target.colors.rim, t);
  rig.lights.fill.color.lerpColors(start.colors.fill, target.colors.fill, t);
  rig.scene.fog.color.lerpColors(start.colors.fog, target.colors.fog, t);
  rig.lights.ambient.intensity = lerp(start.intensities.ambient, target.intensities.ambient, t);
  rig.lights.key.intensity = lerp(start.intensities.key, target.intensities.key, t);
  rig.lights.rim.intensity = lerp(start.intensities.rim, target.intensities.rim, t);
  rig.lights.fill.intensity = lerp(start.intensities.fill, target.intensities.fill, t);
  rig.scene.fog.density = lerp(start.fogDensity, target.fogDensity, t);
}

function lerp(a, b, t) {
  return a + (b - a) * t;
}

function disposeObject(obj) {
  obj.traverse((c) => {
    if (c.isBatchedMesh && typeof c.dispose === 'function') c.dispose();
    else if (c.geometry && !(c.userData && (c.userData.sharedContactShadow || c.userData.sharedShieldGeo))) c.geometry.dispose();
    if (c.material && !(c.userData && c.userData.sharedContactShadow)) { const mm = Array.isArray(c.material) ? c.material : [c.material]; mm.forEach((m) => m.dispose()); }
  });
}

 succeeded in 2005ms:
2:// It owns a pooled GPU particle cloud (one THREE.Points) plus a pool of additive Sprites, and is
4:// animation frame inside renderFrame (after render.draw), so it integrates/ages pools and the new
29:// ---- pool caps by particle-quality setting (spec: low/med/high -> 1500/3000/4000) ----
33:// Sprite "kinds" — drive how a pooled sprite ages (scale/opacity curve).
104:    // colour scratch objects (reused; no per-event allocation)
105:    this._c0 = new THREE.Color();
106:    this._c1 = new THREE.Color();
107:    this._ctmp = new THREE.Color();
109:    this._initPools();
134:  _initPools() {
208:    // ---- discrete sprite pool (flash / ring / puff / fresnel) ----
454:    const scratch = this._socketScratch;
459:    scratch.x = Number.isFinite(x) ? x : 0;
460:    scratch.y = Number.isFinite(y) ? y : 0;
461:    scratch.z = Number.isFinite(z) ? z : 0;
462:    scratch.forwardX = fx / len;
463:    scratch.forwardY = fy / len;
464:    scratch.forwardZ = fz / len;
465:    scratch.angle = Math.atan2(scratch.forwardZ, scratch.forwardX);
466:    scratch.rotationY = Math.PI - scratch.angle;
467:    return scratch;
545:  // Event handlers (each pushes pooled visuals; no per-event allocation of GPU objects)
962:    posAttr.usage = THREE.DynamicDrawUsage;
968:      color: new THREE.Color('#60d0ff'),
981:      color: new THREE.Color('#60d0ff'),
1091:    posAttr.usage = THREE.DynamicDrawUsage;
1101:      color: new THREE.Color('#39d0ff'),
1113:      color: new THREE.Color('#39d0ff'),
1132:    this._tetherColorCool = new THREE.Color('#39d0ff');
1133:    this._tetherColorWarm = new THREE.Color('#ffb35c');
1134:    this._tetherColorHot = new THREE.Color('#ff5c5c');
1510:      // render may have come up after vfx.init (defensive) — try once to attach pools
1511:      if (this.state.render && this.state.render.scene) { this._initPools(); this._subscribeOnce(); }
1529:  // (defensive) only used if pools attached lazily; avoids double-subscription
1774:  // Event lights (V2 §11 Tier-A rendering finish). A small pool of dynamic PointLights grabbed on
1783:    // Respect the motion-reduce / quality gates: on low quality or motion-reduce, skip the pool
1784:    // entirely (lights are a vestibular/visual load). _flashLight becomes a no-op if pool is null.
1799:  // Grab a pool light, position it at {x,z} (y lifted slightly above the plane), set its color +
1804:    const pool = this._lights;
1805:    if (!pool || !pos) return false;
1811:    const slot = pool[this._lightCur];
1812:    this._lightCur = (this._lightCur + 1) % pool.length;
1831:    const pool = this._lights;
1832:    if (!pool) return;
1834:    for (const slot of pool) {
1862:  // per-frame engine-trail emission for every thrusting ship/drone (steady-state, pooled)
1890:  // c0/c1 are the color scratch pair; we reuse this._c0/_c1 like the other emitters.
1935:    // emit rate scales with severity; cap so a swarm of wounded ships can't drown the pool.
1974:      // is about to die" without needing a health bar. Sparse so it doesn't spam the pool.
2041:    const pool = this._spritePool, st = this._spr, active = this._activeSprites;
2048:      const spr = pool[i];
2162:  posAttr.usage = THREE.DynamicDrawUsage;
2171:    color: new THREE.Color(color || '#7fe0ff'),
2295:// shared radial-gradient glow sprite texture (one canvas for the whole pool)

 exited 1 in 1379ms:
rg: src\camera.js: The system cannot find the file specified. (os error 2)
src\render\renderer.js:181:    // true). The key light becomes a shadow caster with a tight frustum that follows the player so
src\render\renderer.js:192:      // renderFrame to follow the player). Tight bounds = crisp shadows at usable resolution.
src\render\renderer.js:199:      key.target = new THREE.Object3D(); scene.add(key.target);
src\render\renderer.js:256:          // Rebuild the bloom post-process pipeline (its render targets are tied to the lost context).
src\render\renderer.js:282:    this._sectorPaletteRig = createSectorPaletteRig(scene, ambient, key, rim, fill);
src\render\renderer.js:283:    this._sectorPaletteTarget = corePalette;
src\render\renderer.js:284:    state.render.sectorPalette = corePalette;
src\render\renderer.js:356:    state.camera.obj = cam.obj;
src\render\renderer.js:375:    bus.on('camera:zoom', ({ delta, level }) => { if (level != null) cam.setZoom(level); else cam.setZoom(state.camera.zoom + (delta || 0)); });
src\render\renderer.js:414:    bus.on('jump:arrive', ({ sectorId } = {}) => {
src\render\renderer.js:425:      if (keepPlayer && id === this.state.playerId) continue;
src\render\renderer.js:466:      const e = state.entities.get(id);
src\render\renderer.js:488:      const e = this.state.entities.get(id);
src\render\renderer.js:511:    const e = this.state.entities.get(id);
src\render\renderer.js:700:    const rig = this._sectorPaletteRig;
src\render\renderer.js:703:    this.state.render.sectorPalette = palette;
src\render\renderer.js:704:    if (palette === this._sectorPaletteTarget) return;
src\render\renderer.js:706:    this._sectorPaletteTarget = palette;
src\render\renderer.js:708:    writePaletteToSectorPaletteFrame(rig.target, palette);
src\render\renderer.js:718:    const rig = this._sectorPaletteRig;
src\render\renderer.js:724:    lerpSectorPaletteFrame(rig, rig.start, rig.target, t);
src\render\renderer.js:727:      applySectorPaletteFrame(rig, rig.target);
src\render\renderer.js:733:    // webglcontextrestored rebuilds GPU resources. (cam.follow etc. would run against a dead
src\render\renderer.js:738:    this.cam.follow(frameDt);
src\render\renderer.js:748:    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
src\render\renderer.js:751:    // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
src\render\renderer.js:753:    // an offset from its target; we move both together. No-op if shadows are disabled.
src\render\renderer.js:793:    const p = this.state.playerId ? (this.state.entities && this.state.entities.get(this.state.playerId)) : null;
src\render\renderer.js:796:    this._keyLight.target.position.set(px, 0, pz);
src\render\renderer.js:797:    this._keyLight.target.updateMatrixWorld();
src\render\renderer.js:889:  // targets). Returns false if construction fails (e.g. a low-capability GPU) so the caller falls
src\render\renderer.js:938:    target: createSectorPaletteFrame(),
src\render\renderer.js:943:  writeRigToSectorPaletteFrame(rig.target, rig);
src\render\renderer.js:1000:function lerpSectorPaletteFrame(rig, start, target, t) {
src\render\renderer.js:1001:  rig.lights.ambient.color.lerpColors(start.colors.ambient, target.colors.ambient, t);
src\render\renderer.js:1002:  rig.lights.key.color.lerpColors(start.colors.key, target.colors.key, t);
src\render\renderer.js:1003:  rig.lights.rim.color.lerpColors(start.colors.rim, target.colors.rim, t);
src\render\renderer.js:1004:  rig.lights.fill.color.lerpColors(start.colors.fill, target.colors.fill, t);
src\render\renderer.js:1005:  rig.scene.fog.color.lerpColors(start.colors.fog, target.colors.fog, t);
src\render\renderer.js:1006:  rig.lights.ambient.intensity = lerp(start.intensities.ambient, target.intensities.ambient, t);
src\render\renderer.js:1007:  rig.lights.key.intensity = lerp(start.intensities.key, target.intensities.key, t);
src\render\renderer.js:1008:  rig.lights.rim.intensity = lerp(start.intensities.rim, target.intensities.rim, t);
src\render\renderer.js:1009:  rig.lights.fill.intensity = lerp(start.intensities.fill, target.intensities.fill, t);
src\render\renderer.js:1010:  rig.scene.fog.density = lerp(start.fogDensity, target.fogDensity, t);
src\ai\contracts.js:130:    targetKinds: Object.freeze(Array.isArray(def.targetKinds) ? def.targetKinds.slice().sort() : []),
src\ai\contracts.js:157:    targetHeading: wrapAngle(finite(values.targetHeading, 0)),
src\ai\contracts.js:216:function requireMethod(target, name, label) {
src\ai\contracts.js:217:  if (!target || typeof target[name] !== 'function') throw new TypeError(`${label}.${name}() is required`);
src\ai\contracts.js:269:    targetId: value.targetId == null ? null : value.targetId,
src\ai\contracts.js:273:    targetSocketId: value.targetSocketId == null ? null : String(value.targetSocketId),
src\ai\contracts.js:289:    targetId: value.targetId == null ? null : value.targetId,
src\combat\validate.js:81:  const target = def.target || {};
src\combat\validate.js:82:  if (!TARGET_KINDS.has(target.kind)) ctx.errors.push(`${path}.target.kind is invalid`);
src\combat\validate.js:83:  if (target.maxRange != null && !finiteNonNegative(target.maxRange)) ctx.errors.push(`${path}.target.maxRange must be finite and >= 0`);
src\combat\validate.js:84:  if (target.required && target.kind === 'none') ctx.errors.push(`${path} cannot require a none target`);
src\combat\validate.js:178:  if (!Array.isArray(def.targetSocketTags) || !def.targetSocketTags.length) ctx.errors.push(`${path}.targetSocketTags must be non-empty`);
src\combat\validate.js:179:  if (!def.ownership || !['initiator', 'target', 'neutral'].includes(def.ownership.policy)) ctx.errors.push(`${path}.ownership.policy is invalid`);
src\combat\trace.js:38:  const targetId = options.targetId;
src\combat\trace.js:45:    if (targetId != null && event.targetId !== targetId) continue;
src\core\gameState.js:42:    targetId: null,
src\core\gameState.js:78:    playerId: 0,
src\core\gameState.js:115:    jump: { state: 'IDLE', targetSectorId: null, via: null, chargeT: 0, chargeNeeded: 0, cooldownT: 0 },
src\ai\shipDecision.js:32:      ? (directive.objective.targetId ?? directive.focusTargetId)
src\ai\shipDecision.js:34:    const target = resolveTarget(perception, requestedTarget);
src\ai\shipDecision.js:35:    const distance = target ? distance2(self.pos, target.pos) : Infinity;
src\ai\shipDecision.js:45:      const candidate = scoreAction(def, self, target, distance, directive, this.config, !!this.trace);
src\ai\shipDecision.js:58:      targetId: null,
src\ai\shipDecision.js:59:      targetContact: null,
src\ai\shipDecision.js:68:    if (selected && target && !selected.targetContact && (selected.targetId != null || selected.actionId == null)) {
src\ai\shipDecision.js:69:      selected.targetContact = compactTarget(target, freeze);
src\ai\shipDecision.js:71:    if (selected) selected.maneuver = maneuverFor(selectedActionDef, directive, target, freeze);
src\ai\shipDecision.js:93:          target: target ? { id: target.id, kind: target.kind, confidence: target.confidence, distance } : null,
src\ai\shipDecision.js:115:      state = { actionId: null, targetId: null, startedTick: -Infinity, utility: 0, status: 'idle', handle: null, maneuver: null, lastReason: 'init' };
src\ai\shipDecision.js:128:    const same = state.actionId === selected.actionId && state.targetId === selected.targetId;
src\ai\shipDecision.js:144:          tick, reason: interruptReason, source: 'ai', nextActionId: selected.actionId, nextTargetId: selected.targetId,
src\ai\shipDecision.js:166:        targetId: selected.targetId,
src\ai\shipDecision.js:167:        target: selected.targetContact || null,
src\ai\shipDecision.js:176:          state.targetId = selected.targetId;
src\ai\shipDecision.js:184:          state.targetId = selected.targetId;
src\ai\shipDecision.js:194:        state.targetId = selected.targetId;
src\ai\shipDecision.js:202:      state.targetId = null;
src\ai\shipDecision.js:224:      targetId: state.targetId,
src\ai\shipDecision.js:265:function scoreAction(def, self, target, distance, directive, config, collectReasons = true) {
src\ai\shipDecision.js:273:    if (!target) eligible = false;
src\ai\shipDecision.js:278:      note('hostile_target', 'range_fit');
src\ai\shipDecision.js:290:    if (!target) eligible = false;
src\ai\shipDecision.js:295:    eligible = eligible && !!target;
src\ai\shipDecision.js:300:    if (def.metadata && def.metadata.requiresEscapeAlignment && target) {
src\ai\shipDecision.js:301:      const desired = Math.atan2(self.pos.z - target.pos.z, self.pos.x - target.pos.x);
src\ai\shipDecision.js:333:  if (def.targetKinds.length && target && !def.targetKinds.includes(target.kind)) {
src\ai\shipDecision.js:336:    note('target_kind_mismatch');
src\ai\shipDecision.js:344:    targetId: target ? target.id : null,
src\ai\shipDecision.js:345:    targetContact: null,
src\ai\shipDecision.js:351:function maneuverFor(def, directive, target, freeze = Object.freeze) {
src\ai\shipDecision.js:355:    targetId: target ? target.id : null,
src\ai\shipDecision.js:387:function resolveTarget(perception, targetId) {
src\ai\shipDecision.js:388:  if (targetId == null || !perception) return null;
src\ai\shipDecision.js:389:  return perception.contacts.find((contact) => contact.id === targetId) || null;
src\ai\shipDecision.js:398:function compactTarget(target, freeze = Object.freeze) {
src\ai\shipDecision.js:400:    id: target.id, kind: target.kind, pos: target.pos, ownerId: target.ownerId,
src\ai\shipDecision.js:401:    attachmentId: target.attachmentId, sourceSocketId: target.sourceSocketId, targetSocketId: target.targetSocketId,
src\ai\shipDecision.js:402:    ownedBySelf: target.ownedBySelf, tags: target.tags,
src\ai\shipDecision.js:418:    targetId: candidate.targetId,
src\ai\shipDecision.js:428:    targetId: output.targetId,
src\ai\shipDecision.js:443:    output.targetId == null ? '' : output.targetId,
src\combat\subsystems.js:16:        targetId: entity.id,
src\combat\subsystems.js:60:        targetId: entity.id,
src\combat\subsystems.js:67:          targetId: entity.id,
src\combat\subsystems.js:116:    targetId: entity.id,
src\combat\subsystems.js:138:    targetId: entity.id,
src\core\flightDynamics.js:119:    boosting: !!(opts.boosting || (input && input.boosting)),
src\core\flightDynamics.js:140:      boosting: !!intent.boost,
src\core\flightDynamics.js:147:  const translation = stepTranslation(e, intent, dt, profile, { boosting: !!intent.boost, npc: true });
src\core\flightDynamics.js:241:  const targetBank = clamp(turnFraction * bankFactor * bankMax, -bankMax, bankMax);
src\core\flightDynamics.js:242:  integrateBank(e, targetBank, dt, {
src\core\flightDynamics.js:247:  return { targetBank, bank: e.bank || 0 };
src\core\flightDynamics.js:251:  if (!e.bank) return { targetBank: 0, bank: 0 };
src\core\flightDynamics.js:253:  return { targetBank: 0, bank: e.bank || 0 };
src\core\flightDynamics.js:266:  const targetYawRate = turnIntent * profile.maxYawRate;
src\core\flightDynamics.js:267:  const accel = Math.abs(targetYawRate) > Math.abs(e.angVel || 0) ? profile.angularAccel : profile.angularBrake;
src\core\flightDynamics.js:268:  e.angVel = approachValue(e.angVel || 0, targetYawRate, Math.max(0, accel) * dt);
src\core\flightDynamics.js:274:    targetYawRate,
src\core\flightDynamics.js:284:  const boosting = !!opts.boosting;
src\core\flightDynamics.js:285:  const thrustMult = boosting ? profile.boostMult : 1;
src\core\flightDynamics.js:304:  const max = profile.maxSpeed * (boosting ? profile.boostMaxSpeedMult : profile.normalMaxSpeedMult);
src\core\flightDynamics.js:313:    boosting,
src\core\flightDynamics.js:344:  const targetYawRate = turnIntent * profile.maxYawRate;
src\core\flightDynamics.js:345:  const accel = Math.abs(targetYawRate) > Math.abs(currentYawRate) ? profile.angularAccel : profile.angularBrake;
src\core\flightDynamics.js:346:  let requestedYawRate = approachValue(currentYawRate, targetYawRate, Math.max(0, accel) * dt);
src\core\flightDynamics.js:352:    targetYawRate,
src\core\flightDynamics.js:369:  const maxSpeed = profile.maxSpeed * (translation.boosting ? profile.boostMaxSpeedMult : profile.normalMaxSpeedMult);
src\core\flightDynamics.js:448:function integrateBank(e, targetBank, dt, tuning) {
src\core\flightDynamics.js:451:  targetBank = clamp(targetBank, -(tuning.bankMax ?? DEFAULT_FLIGHT_TUNING.bankMax), tuning.bankMax ?? DEFAULT_FLIGHT_TUNING.bankMax);
src\core\flightDynamics.js:453:  const response = Math.abs(targetBank) > 0.001 ? tuning.bankResponse : tuning.bankReturnResponse;
src\core\flightDynamics.js:454:  e.bank = dampValue(e.bank, targetBank, response, dt);
src\core\flightDynamics.js:456:  if (Math.abs(targetBank) < 0.001 && Math.abs(e.bank) < 0.001 && Math.abs(e.bankVel) < 0.02) {
src\data\impulseCharges.js:10:// We use 800 so the tier-1 mid reference (Pelican) hits the 25 wu/s feel target; Drifter gets ~17 wu/s.
src\combat\statuses.js:6:  function schedule(targetEntity, runtime, application, source = {}) {
src\combat\statuses.js:13:        targetId: targetEntity.id,
src\combat\statuses.js:35:      targetId: targetEntity.id,
src\combat\statuses.js:43:  function advance(targetEntity, runtime, routeDamage) {
src\combat\statuses.js:52:      appendCombatTrace(state.combat, tick, 'status.expired', { targetId: targetEntity.id, statusId });
src\combat\statuses.js:53:      if (bus) bus.emit('combat:statusExpired', { targetId: targetEntity.id, statusId });
src\combat\statuses.js:59:      if (applyActive(targetEntity, runtime, pending)) changed = true;
src\combat\statuses.js:73:            targetId: targetEntity.id,
src\combat\statuses.js:80:          targetId: targetEntity.id,
src\combat\statuses.js:90:  function clear(targetEntity, runtime, statusId, reason = 'cleared') {
src\combat\statuses.js:93:    appendCombatTrace(state.combat, state.tick, 'status.cleared', { targetId: targetEntity.id, statusId, reason });
src\combat\statuses.js:97:  function applyActive(targetEntity, runtime, pending) {
src\combat\statuses.js:136:      targetId: targetEntity.id,
src\combat\statuses.js:144:      targetId: targetEntity.id,
src\combat\statuses.js:157:          applyActive(targetEntity, runtime, {
src\ai\sg03ActionPort.js:8:    targetKinds: Object.freeze([]),
src\ai\sg03ActionPort.js:14:    targetKinds: Object.freeze([ContactKind.OBJECTIVE, ContactKind.SHIP]),
src\ai\sg03ActionPort.js:19:    targetKinds: Object.freeze([ContactKind.TETHER]),
src\ai\sg03ActionPort.js:24:    targetKinds: Object.freeze([ContactKind.TETHER]),
src\ai\sg03ActionPort.js:29:    targetKinds: Object.freeze([ContactKind.TETHER]),
src\ai\sg03ActionPort.js:35:    targetKinds: Object.freeze([ContactKind.SHIP]),
src\ai\sg03ActionPort.js:54:    range: Math.max(0, Number(def.target && def.target.maxRange) || 0),
src\ai\sg03ActionPort.js:57:      : Math.max(0, Number(def.target && def.target.maxRange) || 0) * 0.75,
src\ai\sg03ActionPort.js:58:    targetKinds: Object.freeze((profile.targetKinds || inferTargetKinds(def.target)).slice().sort()),
src\ai\sg03ActionPort.js:61:      targetKind: def.target && def.target.kind || 'none',
src\ai\sg03ActionPort.js:137:      if (def.target && def.target.required && request.targetId == null) return { ok: false, reason: 'target_required' };
src\ai\sg03ActionPort.js:138:      if (def.target && def.target.ownedByActor) {
src\ai\sg03ActionPort.js:139:        const tags = new Set(request.target && request.target.tags || []);
src\ai\sg03ActionPort.js:140:        if (!request.target || (!request.target.ownedBySelf && !tags.has('owned_by_self') && !tags.has('cuttable_by_self'))) {
src\ai\sg03ActionPort.js:160:      if (def.target && def.target.kind === 'entity') {
src\ai\sg03ActionPort.js:161:        payload.targetId = request.targetId;
src\ai\sg03ActionPort.js:162:        payload.sourceSocketId = request.target && request.target.sourceSocketId || null;
src\ai\sg03ActionPort.js:163:        payload.targetSocketId = request.target && request.target.targetSocketId || null;
src\ai\sg03ActionPort.js:164:      } else if (def.target && def.target.kind === 'attachment') {
src\ai\sg03ActionPort.js:165:        payload.attachmentId = request.target && request.target.attachmentId || request.targetId;
src\ai\sg03ActionPort.js:166:      } else if (def.target && def.target.kind === 'point') {
src\ai\sg03ActionPort.js:167:        payload.point = request.target && request.target.pos || null;
src\ai\sg03ActionPort.js:212:export function escapeAlignment(self, target) {
src\ai\sg03ActionPort.js:213:  if (!self || !target) return 0;
src\ai\sg03ActionPort.js:214:  const desired = Math.atan2(self.pos.z - target.pos.z, self.pos.x - target.pos.x);
src\ai\sg03ActionPort.js:218:function inferTargetKinds(target = {}) {
src\ai\sg03ActionPort.js:219:  if (target.kind === 'attachment') return [ContactKind.TETHER];
src\ai\sg03ActionPort.js:220:  if (target.kind === 'entity') return [ContactKind.SHIP, ContactKind.OBJECTIVE];
src\ai\sg03ActionPort.js:221:  if (target.kind === 'point') return [ContactKind.WAYPOINT];
src\ai\sg03ActionPort.js:247:  return state && state.entities && typeof state.entities.get === 'function'
src\ai\sg03ActionPort.js:248:    ? state.entities.get(entityId) || null
src\ai\stack.js:21: * The host deliberately has no authoritative world object. Its only knowledge of ships, targets,
src\ai\perception.js:91:        targetId: contact.targetId,
src\ai\perception.js:95:        targetSocketId: contact.targetSocketId,
src\core\simSnapshot.js:40:    playerId: state.playerId | 0,
src\combat\persistence.js:45:    if (entity.id === state.playerId) ref = { kind: 'player' };
src\combat\persistence.js:76:    const targetRef = refs.get(entityKey(attachment.targetId));
src\combat\persistence.js:77:    if (!ownerRef || !targetRef) continue;
src\combat\persistence.js:80:    delete saved.targetId;
src\combat\persistence.js:83:    saved.targetRef = clonePlain(targetRef);
src\combat\persistence.js:100:    const target = serializeTarget(request && request.target, refs, savedAttachmentIds);
src\combat\persistence.js:101:    if (!actorRef || !target) continue;
src\combat\persistence.js:105:    saved.target = target;
src\combat\persistence.js:114:    const target = serializeTarget(instance && instance.target, refs, savedAttachmentIds);
src\combat\persistence.js:115:    if (!actorRef || !target) continue;
src\combat\persistence.js:119:    saved.target = target;
src\combat\persistence.js:166:    const targetId = resolveEntityRef(saved.targetRef);
src\combat\persistence.js:167:    if (ownerId == null || targetId == null || ownerId === targetId) { summary.dropped++; continue; }
src\combat\persistence.js:170:    delete attachment.targetRef;
src\combat\persistence.js:173:    attachment.targetId = targetId;
src\combat\persistence.js:216:  const target = restoreTarget(saved && saved.target, resolveEntityRef, attachmentsById);
src\combat\persistence.js:217:  if (!target) return null;
src\combat\persistence.js:221:  record.target = target;
src\combat\persistence.js:225:function serializeTarget(target, refs, savedAttachmentIds) {
src\combat\persistence.js:226:  if (!target || target.kind === 'none') return { kind: 'none' };
src\combat\persistence.js:227:  if (target.kind === 'entity') {
src\combat\persistence.js:228:    const entityRef = refs.get(entityKey(target.entityId));
src\combat\persistence.js:233:      sourceSocketId: target.sourceSocketId == null ? null : String(target.sourceSocketId),
src\combat\persistence.js:234:      targetSocketId: target.targetSocketId == null ? null : String(target.targetSocketId),
src\combat\persistence.js:237:  if (target.kind === 'attachment') {
src\combat\persistence.js:238:    const attachmentId = target.attachmentId == null ? null : String(target.attachmentId);
src\combat\persistence.js:242:  if (target.kind === 'point') return { kind: 'point', x: Number(target.x) || 0, z: Number(target.z) || 0 };
src\combat\persistence.js:246:function restoreTarget(target, resolveEntityRef, attachmentsById) {
src\combat\persistence.js:247:  if (!target || target.kind === 'none') return { kind: 'none' };
src\combat\persistence.js:248:  if (target.kind === 'entity') {
src\combat\persistence.js:249:    const entityId = resolveEntityRef(target.entityRef);
src\combat\persistence.js:254:      sourceSocketId: target.sourceSocketId == null ? null : String(target.sourceSocketId),
src\combat\persistence.js:255:      targetSocketId: target.targetSocketId == null ? null : String(target.targetSocketId),
src\combat\persistence.js:258:  if (target.kind === 'attachment') {
src\combat\persistence.js:259:    const attachmentId = target.attachmentId == null ? null : String(target.attachmentId);
src\combat\persistence.js:263:  if (target.kind === 'point') return { kind: 'point', x: Number(target.x) || 0, z: Number(target.z) || 0 };
src\core\flight\propulsionKernel.js:114:      targetYawRate: yaw.targetYawRate,
src\core\flight\propulsionKernel.js:127:  let targetVelocity;
src\core\flight\propulsionKernel.js:131:  // target; this gives advanced pilots a decoupled option without pretending the
src\core\flight\propulsionKernel.js:134:    targetVelocity = { x: body.vel.x, z: body.vel.z };
src\core\flight\propulsionKernel.js:136:    targetVelocity = { x: 0, z: 0 };
src\core\flight\propulsionKernel.js:138:    targetVelocity = {
src\core\flight\propulsionKernel.js:144:  const error = sub2(targetVelocity, body.vel);
src\core\flight\propulsionKernel.js:170:      targetVelocity,
src\core\flight\propulsionKernel.js:172:      targetYawRate: yaw.targetYawRate,
src\core\flight\propulsionKernel.js:293:      targetYawRate: yaw.targetYawRate,
src\core\flight\propulsionKernel.js:354:      targetYawRate: yaw.targetYawRate,
src\core\flight\propulsionKernel.js:402:      targetYawRate: yaw.targetYawRate,
src\core\flight\propulsionKernel.js:462:    return { targetYawRate: body.angVel, angularAcceleration: 0 };
src\core\flight\propulsionKernel.js:464:  const targetYawRate = turn * positive(profile.maxYawRate, 2.5);
src\core\flight\propulsionKernel.js:465:  const error = targetYawRate - body.angVel;
src\core\flight\propulsionKernel.js:466:  const accelerating = Math.abs(targetYawRate) > Math.abs(body.angVel) && Math.sign(targetYawRate) === Math.sign(error);
src\core\flight\propulsionKernel.js:471:  return { targetYawRate, angularAcceleration };
src\core\flight\propulsionKernel.js:477:  const targetYawRate = clamp(error * 3.8, -maxRate, maxRate);
src\core\flight\propulsionKernel.js:478:  const rateError = targetYawRate - body.angVel;
src\core\flight\propulsionKernel.js:484:  return { targetYawRate, angularAcceleration };
src\core\flight\propulsionKernel.js:547:function resourceDemand(profile, acceleration, boosting, dt, idle = 0) {
src\core\flight\propulsionKernel.js:550:  const boostHeat = boosting ? positive(resources.boostHeatMult, 1) : 1;
src\core\flight\propulsionKernel.js:572:  if (!!input.boost !== !!previous.boosting) {
src\core\flight\propulsionKernel.js:575:  next.boosting = !!input.boost;
src\core\flight\propulsionKernel.js:649:    boosting: !!r.boosting,
src\core\flight\propulsionKernel.js:743:function approach(current, target, maxDelta) {
src\core\flight\propulsionKernel.js:744:  const delta = target - current;
src\core\flight\propulsionKernel.js:745:  if (Math.abs(delta) <= Math.abs(maxDelta)) return target;
src\ai\squad.js:205:    push('swarm_pincer', hostileShips ? 0.48 + (squad.doctrine === 'scavenger' ? 0.22 : 0) : 0, 'split attack vectors around a perceived focus target');
src\ai\squad.js:372:function freezeObjective(kind, targetId, reason, freeze = Object.freeze) {
src\ai\squad.js:373:  return freeze({ kind, targetId: targetId == null ? null : targetId, reason });
src\ai\squad.js:384:  if (perception && perception.events.some((event) => event.type === 'formation_break' && (event.targetId == null || event.targetId === memberId))) return 'authored_break';
src\ai\squad.js:412:function slewAngle(current, target, maxStep) {
src\ai\squad.js:413:  const delta = wrapAngle(target - current);
src\data\combatDefs.js:38:    target: { kind: 'none', required: false },
src\data\combatDefs.js:49:    target: {
src\data\combatDefs.js:51:      sourceSocketTag: 'massline', targetSocketTag: 'tether',
src\data\combatDefs.js:63:    target: { kind: 'attachment', required: true, ownedByActor: true },
src\data\combatDefs.js:74:    target: { kind: 'attachment', required: true, ownedByActor: true },
src\data\combatDefs.js:85:    target: { kind: 'attachment', required: true, ownedByActor: true },
src\data\combatDefs.js:96:    target: { kind: 'entity', required: true, maxRange: 220, hostile: true },
src\data\combatDefs.js:193:    sourceSocketTags: ['massline'], targetSocketTags: ['tether'],
src\data\combatDefs.js:205:    sourceSocketTags: ['massline'], targetSocketTags: ['tether'],
src\data\audioRecipes.js:148:  // Missile / homing lock acquired — a crisp two-note ascending blip (the classic targeting cue).
src\combat\damage.js:13:    const target = entity(input && input.targetId);
src\combat\damage.js:18:    if (!target || !target.alive) return rejected('target_missing', input, packet);
src\combat\damage.js:19:    if (!packet.flags.allowAnyTarget && !['ship', 'station', 'drone'].includes(target.type)) return rejected('target_not_damageable', input, packet);
src\combat\damage.js:20:    if (target.flags && target.flags.invuln && !packet.flags.ignoreInvulnerability) return rejected('target_invulnerable', input, packet);
src\combat\damage.js:21:    if (!packet.flags.ignoreFriendlyFire && attacker && attacker.id !== target.id && attacker.team != null && target.team != null && attacker.team === target.team) {
src\combat\damage.js:26:    const runtime = ensureCombatant(state, target, catalog);
src\combat\damage.js:27:    const before = snapshotVitals(target, runtime);
src\combat\damage.js:45:      const absorbedHp = Math.min(Math.max(0, target.shield || 0), potentialHp);
src\combat\damage.js:46:      target.shield = Math.max(0, (target.shield || 0) - absorbedHp);
src\combat\damage.js:53:    const shieldBroke = before.shield > 0 && target.shield <= 0;
src\combat\damage.js:54:    const postFlatRaw = applyArmorFlat(postShieldRaw, Math.max(0, Number(target.armorFlat) || 0), model.channelOrder);
src\combat\damage.js:61:      const absorbedHp = Math.min(Math.max(0, target.armorHp || 0), potentialHp);
src\combat\damage.js:62:      target.armorHp = Math.max(0, (target.armorHp || 0) - absorbedHp);
src\combat\damage.js:68:    const subsystemId = selectHitSubsystem(target, runtime, catalog, packet.hit || {});
src\combat\damage.js:77:      subsystemResult = damageSubsystem(context, target, runtime, subsystemId, subsystemInputTotal, subsystemInput, penetration);
src\combat\damage.js:83:    const damageReduction = clamp(Number(target.data && target.data.derived && target.data.derived.damageReductionMult) || 1, 0, 1);
src\combat\damage.js:91:    hullDamage = Math.min(Math.max(0, target.hull || 0), hullDamage);
src\combat\damage.js:92:    target.hull = Math.max(0, (target.hull || 0) - hullDamage);
src\combat\damage.js:93:    target.lastDamageT = Number.isFinite(state.simTime) ? state.simTime : (state.tick || 0) / 60;
src\combat\damage.js:99:      statusService.schedule(target, runtime, status, {
src\combat\damage.js:105:    const impulseResult = applyImpulse(target, attacker, packet.impulse, input);
src\combat\damage.js:106:    syncCombatantBounds(target, runtime);
src\combat\damage.js:107:    const after = snapshotVitals(target, runtime);
src\combat\damage.js:112:      targetId: target.id,
src\combat\damage.js:132:      targetId: target.id,
src\combat\damage.js:149:    if (shieldBroke && bus) bus.emit('shieldDown', { combatantId: target.id, pos: packet.hit && packet.hit.pos || target.pos });
src\combat\damage.js:151:      const factionLawful = !!(target.data && target.data.ai && target.data.ai.lawful);
src\combat\damage.js:153:        targetId: target.id,
src\combat\damage.js:162:        isPlayer: target.id === state.playerId,
src\combat\damage.js:163:        pos: packet.hit && packet.hit.pos || { x: target.pos.x, z: target.pos.z },
src\combat\damage.js:164:        factionId: target.factionId || null,
src\combat\damage.js:171:    if (before.hull > 0 && target.hull <= 0) {
src\combat\damage.js:172:      if (onKill) onKill(target, result.attackerId);
src\combat\damage.js:173:      else fallbackKill(target, result.attackerId);
src\combat\damage.js:178:  function applyImpulse(target, attacker, impulse, input) {
src\combat\damage.js:179:    const vector = resolveImpulseVector(target, attacker, impulse);
src\combat\damage.js:184:        targetId: target.id,
src\combat\damage.js:191:        entityId: target.id,
src\combat\damage.js:200:        targetId: target.id,
src\combat\damage.js:208:        targetId: target.id,
src\combat\damage.js:216:  function fallbackKill(target, killerId) {
src\combat\damage.js:217:    target.alive = false;
src\combat\damage.js:219:      id: target.id,
src\combat\damage.js:221:      type: target.type,
src\combat\damage.js:222:      pos: { x: target.pos.x, z: target.pos.z },
src\combat\damage.js:223:      factionId: target.factionId || null,
src\combat\damage.js:224:      victimClass: target.data && target.data.shipClass || target.type,
src\combat\damage.js:231:      targetId: input && input.targetId == null ? null : input.targetId,
src\combat\damage.js:239:    return state.entities && state.entities.get ? state.entities.get(id) || null : null;
src\combat\damage.js:289:    // Subsystem-targeting + shield coupling (EMP/disable verb, spec §9). subsystemShare 1.0 = all
src\combat\damage.js:307:function addProportional(target, weights, amount, order) {
src\combat\damage.js:309:  if (!(amount > 0)) return target;
src\combat\damage.js:311:    target.kinetic = (target.kinetic || 0) + amount;
src\combat\damage.js:312:    return target;
src\combat\damage.js:314:  for (const channel of order) target[channel] = (target[channel] || 0) + amount * ((weights[channel] || 0) / total);
src\combat\damage.js:315:  return target;
src\combat\damage.js:318:function resolveImpulseVector(target, attacker, impulse) {
src\combat\damage.js:328:    dx = (target.pos && target.pos.x || 0) - (attacker && attacker.pos && attacker.pos.x || 0);
src\combat\damage.js:329:    dz = (target.pos && target.pos.z || 0) - (attacker && attacker.pos && attacker.pos.z || 0);
src\core\physics.js:407:      for (const target of candidates) {
src\core\physics.js:408:        if (!target.alive || !target.collides || (target.type !== 'asteroid' && target.type !== 'station')) continue;
src\core\physics.js:409:        if (target === ship || (!canCollide(ship, target) && !canCollide(target, ship))) continue;
src\core\physics.js:410:        if (!segmentCircleHitInto(hit, start, end, target.pos, (ship.radius || 0) + (target.radius || 0))) continue;
src\core\physics.js:412:          bestTarget = target;
src\core\physics.js:511:    const player = state.entities.get(state.playerId);
src\core\physics.js:541:        this.bus.emit('dock:range', { stationId: this._dockStationId, shipId: state.playerId, inRange: false });
src\core\physics.js:545:        this.bus.emit('dock:range', { stationId: nextStationId, shipId: state.playerId, inRange: true });
src\core\physics.js:551:      if (this._gateEntityId) this.bus.emit('gate:range', { gateId: this._gateEntityId, shipId: state.playerId, inRange: false });
src\core\physics.js:557:          shipId: state.playerId,
src\core\physics.js:768:function projectileHitPayload(proj, targetId, pos) {
src\core\physics.js:771:    targetId,
src\data\tech.js:51:    unlocks: { modules: ['mod_targeting_computer_m'] },
src\core\sim.js:11:// step target used by browsers, tests, audits, replays, workers, and offscreen sector simulation.
src\ai\maneuver.js:53:      targetId: null,
src\ai\maneuver.js:60:    const target = intent.targetId == null ? null : perception.contacts.find((contact) => contact.id === intent.targetId);
src\ai\maneuver.js:68:      : desiredForIntent(intent, self, target, perception.contacts, this.seed, entityId, this.config);
src\ai\maneuver.js:111:      targetHeading: heading,
src\ai\maneuver.js:133:          targetId: target && target.id,
src\ai\maneuver.js:173:function desiredForIntent(intent, self, target, contacts, seed, entityId, config) {
src\ai\maneuver.js:176:      return target ? intercept(self, target, config.interceptHorizonTicks) : seekPoint(self, intent.formationSlot, 0.7);
src\ai\maneuver.js:179:      return target ? orbit(self, target, orbitRadius, seed, entityId) : seekPoint(self, intent.formationSlot, 0.7);
src\ai\maneuver.js:182:      return screen(self, target, intent.formationSlot);
src\ai\maneuver.js:185:      return target ? seekPoint(self, target.pos, 1) : seekPoint(self, intent.formationSlot, 0.8);
src\ai\maneuver.js:187:      return escapeTether(self, target || nearestTether(contacts, self), seed, entityId);
src\ai\maneuver.js:198:function intercept(self, target, horizonTicks) {
src\ai\maneuver.js:199:  const distance = distance2(self.pos, target.pos);
src\ai\maneuver.js:201:  const point = { x: target.pos.x + target.vel.x * horizon / 60, z: target.pos.z + target.vel.z * horizon / 60 };
src\ai\maneuver.js:205:function orbit(self, target, radius, seed, entityId) {
src\ai\maneuver.js:206:  const dx = target.pos.x - self.pos.x, dz = target.pos.z - self.pos.z;
src\ai\maneuver.js:215:function screen(self, target, formationSlot) {
src\ai\maneuver.js:216:  if (!target) return seekPoint(self, formationSlot, 0.8);
src\ai\maneuver.js:218:    x: formationSlot.x * 0.65 + target.pos.x * 0.35,
src\ai\maneuver.js:219:    z: formationSlot.z * 0.65 + target.pos.z * 0.35,
src\ai\maneuver.js:246:  const target = point || self.pos;
src\ai\maneuver.js:247:  const dx = target.x - self.pos.x, dz = target.z - self.pos.z;
src\combat\kernel.js:38:    onKill: (target, killerId) => {
src\combat\kernel.js:39:      if (hooks.onKill) hooks.onKill(target, killerId);
src\combat\kernel.js:41:        target.alive = false;
src\combat\kernel.js:42:        if (bus) bus.emit('entity:killed', { id: target.id, killerId, type: target.type, pos: { x: target.pos.x, z: target.pos.z } });
src\combat\kernel.js:140:      targetId: entity.id,
src\combat\kernel.js:153:    appendCombatTrace(state.combat, state.tick, 'combat.entityRemoved', { targetId: entityId });
src\combat\kernel.js:205:        targetId: request.targetId,
src\combat\kernel.js:220:    return state.entities && state.entities.get ? state.entities.get(id) || null : null;
src\data\narrative.js:85:  elroy:         { name: 'Elroy',       org: 'Pit Engineering, Maintenance Division', role: 'whistleblower (B2 target)' },
src\data\narrative.js:281:    hint: 'Complete the chain. One target vessel\u2019s last registered owner is VALE HOLDINGS LLC. The salvage manifest you deliver will read ADMINISTRATIVE RECORDS \u2014 3 YEARS / SEALED.',
src\data\scenarios\47aLiveScene.js:73:    spawned.targetDummy = spawnEntity(makeShipEntitySpec('ship_wasp', {
src\data\scenarios\47aLiveScene.js:78:      ai: { role: 'target_dummy' },
src\data\scenarios\47aLiveScene.js:80:    spawned.targetDummy.radius = Math.max(spawned.targetDummy.radius || 0, 44);
src\data\scenarios\47aLiveScene.js:81:    spawned.targetDummy.flags = Object.assign({}, spawned.targetDummy.flags, { persistent: true });
src\data\scenarios\47aLiveScene.js:134:  if (state && state.playerId) {
src\data\scenarios\47aLiveScene.js:135:    harasser.data.combat = Object.assign({}, harasser.data.combat, { targetId: state.playerId });
src\data\ships.js:8:// 'turret' tracks a target freely within turretArcDeg. Spreading facings across a hull makes
src\data\ships.js:37:    // boost: {max, drainRate (while boosting), regenRate (idle), dashImpulse (units), dashCooldown (s)}
src\data\missions.js:132:    completionEvent: 'enemy.killed (entityId==targetId)',
src\data\missions.js:133:    rewardFormula: 'round(200 * (1 + distance/2000) * RISK_MULT[riskTier] * targetStrength * f_faction * f_time)',
src\data\missions.js:135:    failureCondition: 'timer OR target despawns/flees sector',
src\data\missions.js:157:    rewardFormula: 'round(180 * (1 + distance/2000) * RISK_MULT[riskTier] * targetStrength * f_faction * f_time)',
src\data\missions.js:165:    rewardFormula: 'round(220 * (1 + distance/2000) * RISK_MULT[riskTier] * targetStrength * f_faction * f_time)',
src\data\missions.js:188:    completionEvent: 'scan.completed (targetId in objective set)',
src\data\missions.js:191:    failureCondition: 'timer OR scan-target despawns',
src\core\math.js:23:// Frame-rate-independent damping toward a target (lambda = rate/sec).
src\core\entity.js:142:    flags: { boosting: false, docked: false, invuln: false, noInterp: false },
src\combat\attachments.js:10:// mass-ratio-driven behavior (heavy target stalls the winch, sustained overload breaks the line)
src\combat\attachments.js:37:    const target = entity(spec && spec.targetId);
src\combat\attachments.js:41:    if (!target || !target.alive || target.id === owner.id) return fail('target_missing');
src\combat\attachments.js:45:    const targetRuntime = ensureCombatant(state, target, catalog);
src\combat\attachments.js:47:    const targetSocket = selectSocket(targetRuntime, def.targetSocketTags, spec.targetSocketId, target.id);
src\combat\attachments.js:49:    if (!targetSocket) return fail('target_socket_unavailable');
src\combat\attachments.js:57:    const targetWorld = socketWorldPosition(target, targetSocket);
src\combat\attachments.js:58:    const restLength = Math.hypot(targetWorld.x - sourceWorld.x, targetWorld.z - sourceWorld.z);
src\combat\attachments.js:63:      targetId: target.id,
src\combat\attachments.js:65:      targetSocketId: targetSocket.id,
src\combat\attachments.js:83:      targetId: target.id,
src\combat\attachments.js:87:      targetSocketId: targetSocket.id,
src\combat\attachments.js:93:      targetId: target.id,
src\combat\attachments.js:97:      targetSocketId: targetSocket.id,
src\combat\attachments.js:126:      attachment.masslineRuntime.targetLength = next;
src\combat\attachments.js:131:      targetId: attachment.targetId,
src\combat\attachments.js:138:      targetId: attachment.targetId,
src\combat\attachments.js:167:        // after the target despawned). An active record with an ungovernable joint is the
src\combat\attachments.js:191:      targetId: attachment.targetId,
src\combat\attachments.js:200:      targetId: attachment.targetId,
src\combat\attachments.js:220:  // Liveness sweep: an attachment whose owner or target no longer exists (despawned pickup,
src\combat\attachments.js:228:      const target = entity(attachment.targetId);
src\combat\attachments.js:233:      const targetLost = !target || target.alive === false;
src\combat\attachments.js:234:      if (!ownerLost && !targetLost) continue;
src\combat\attachments.js:235:      const result = breakAttachment(attachment, 'target_lost', attachment.ownerId);
src\combat\attachments.js:270:        targetId: attachment.targetId,
src\combat\attachments.js:288:      targetId: toOwnerId,
src\combat\attachments.js:334:          runtime.targetLength = seed;
src\combat\attachments.js:338:        const target = entity(attachment.targetId);
src\combat\attachments.js:354:          targetBody: target && { mass: finiteOrZero(target.physicsBody && target.physicsBody.mass) || finiteOrZero(target.mass) || 1 },
src\combat\attachments.js:401:          targetId: attachment.targetId,
src\combat\attachments.js:423:      .filter((attachment) => (!activeOnly || attachment.state === 'active') && (attachment.ownerId === entityId || attachment.targetId === entityId))
src\combat\attachments.js:437:    const target = entity(attachment.targetId);
src\combat\attachments.js:439:    if (!target || !target.alive || target.id === owner.id) return { ok: false, reason: 'target_missing' };
src\combat\attachments.js:441:    const targetRuntime = ensureCombatant(state, target, catalog);
src\combat\attachments.js:443:    const targetSocket = selectSocket(targetRuntime, def.targetSocketTags, attachment.targetSocketId, target.id, attachment.id);
src\combat\attachments.js:445:    if (!targetSocket) return { ok: false, reason: 'target_socket_unavailable' };
src\combat\attachments.js:447:    const targetWorld = socketWorldPosition(target, targetSocket);
src\combat\attachments.js:448:    const fallbackRestLength = Math.hypot(targetWorld.x - sourceWorld.x, targetWorld.z - sourceWorld.z);
src\combat\attachments.js:457:        targetId: target.id,
src\combat\attachments.js:459:        targetSocketId: targetSocket.id,
src\combat\attachments.js:461:        targetWorld,
src\combat\attachments.js:468:      attachment.targetSocketId = targetSocket.id;
src\combat\attachments.js:485:        (attachment.targetId === entityId && attachment.targetSocketId === socket.id))).length;
src\combat\attachments.js:492:    return state.entities && state.entities.get ? state.entities.get(id) || null : null;
src\combat\attachments.js:506:  if (attachment.ownerId === actorId) return attachment.targetId;
src\combat\attachments.js:507:  if (attachment.targetId === actorId) return attachment.ownerId;
src\core\flight\propulsionCatalog.js:13:  GRAVIMETRIC: 'gravimetric',        // target-velocity field drive, hard envelope
src\core\flight\propulsionCatalog.js:281:  if (labOverride && PROPULSION_PROFILES[labOverride] && entity && entity.id === state.playerId) {
src\core\sg02DynamicBodyOwner.js:196:    const target = this.records.get(input.targetId);
src\core\sg02DynamicBodyOwner.js:197:    if (!owner || !target || owner === target) return false;
src\core\sg02DynamicBodyOwner.js:199:    const targetWorld = worldPoint(input.targetWorld, target.body.translation());
src\core\sg02DynamicBodyOwner.js:200:    const restLength = positive(input.restLength, distance2d(sourceWorld, targetWorld));
src\core\sg02DynamicBodyOwner.js:205:      targetId: target.entity.id,
src\core\sg02DynamicBodyOwner.js:207:      targetSocketId: input.targetSocketId == null ? null : String(input.targetSocketId),
src\core\sg02DynamicBodyOwner.js:209:      target,
src\core\sg02DynamicBodyOwner.js:211:      anchorB: localAnchorFromWorld(target, targetWorld),
src\core\sg02DynamicBodyOwner.js:219:    return { id: attachment.id, attachmentId: attachment.id, ownerId: attachment.ownerId, targetId: attachment.targetId };
src\core\sg02DynamicBodyOwner.js:243:    const target = worldAnchor(attachment.target, attachment.anchorB);
src\core\sg02DynamicBodyOwner.js:244:    const dx = target.x - source.x;
src\core\sg02DynamicBodyOwner.js:245:    const dz = target.z - source.z;
src\core\sg02DynamicBodyOwner.js:250:    const targetVelocity = attachment.target.body.linvel();
src\core\sg02DynamicBodyOwner.js:251:    const relativeSpeed = (targetVelocity.x - ownerVelocity.x) * nx + (targetVelocity.z - ownerVelocity.z) * nz;
src\core\sg02DynamicBodyOwner.js:265:      targetWorld: Object.freeze(target),
src\core\sg02DynamicBodyOwner.js:350:      if (attachment.owner === rec || attachment.target === rec) this.cutAttachment({ attachmentId: attachment.id });
src\core\sg02DynamicBodyOwner.js:500:      attachment.target.body,
src\ui\uiRoot.js:323:      ov.addEventListener('click', (e) => { if (e.target === ov) ov.remove(); });
src\ui\uiRoot.js:372:      // hard-set on state.camera.zoom (which fought the dynamic-zoom damping and snapped). The
src\ui\uiRoot.js:467:          // waits for its primary New Game target so players never click a half-registered menu.
src\ui\uiRoot.js:522:  const player = state.entities.get(state.playerId);
src\ui\uiRoot.js:535:    state.player.targetId = null;
src\ui\uiRoot.js:540:  const idx = ids.indexOf(state.player.targetId);
src\ui\uiRoot.js:542:  const target = contacts[nextIdx].e;
src\ui\uiRoot.js:543:  state.player.targetId = target.id;
src\ui\uiRoot.js:544:  if (bus) bus.emit('toast', { text: 'Target: ' + targetLabel(target), kind: 'info', ttl: 2 });
src\ui\uiRoot.js:547:function targetLabel(e) {
src\ui\uiRoot.js:620:  /* hull/shield modifiers are now consumed only by the target panel — keep them distinct
src\ui\uiRoot.js:621:     (hull = red, shield = cyan) so a target's defensive state stays parseable. */
src\ui\uiRoot.js:632:  /* ===== top-center: nav / target-lock readout — chromeless floating text (§3E) ===== */
src\ui\uiRoot.js:704:  /* ===== bottom-right: tactical node map (radar) + target readout (§3D) ===== */
src\ui\uiRoot.js:725:  .sf-target { width:220px; display:flex; flex-direction:column; gap:5px; text-align:right; }
src\ui\uiRoot.js:726:  .sf-target__head { display:flex; align-items:baseline; justify-content:flex-end; gap:8px; }
src\ui\uiRoot.js:727:  .sf-target__name { font-family:var(--mono); font-size:12px; color:var(--text-primary); letter-spacing:.06em;
src\ui\uiRoot.js:730:  .sf-target__faction { font-family:var(--mono); font-size:10px; letter-spacing:.08em; text-shadow:var(--text-shadow-hard); }
src\ui\uiRoot.js:731:  .sf-target__meta { display:flex; justify-content:flex-end; gap:14px; font-family:var(--mono); font-size:11px;
src\ui\uiRoot.js:733:  /* The target panel's mini hull/shield bars become thin lines flush right (3px for legibility). */
src\ui\uiRoot.js:734:  .sf-target .sf-bar { width:100%; }
src\ui\uiRoot.js:735:  .sf-target .sf-bar--sm { height:3px; }
src\ui\uiRoot.js:789:  /* ===== combat HUD overlay (lock-on, weapon heat bars, target diamond) ===== */
src\ui\uiRoot.js:822:     Outer div is the invisible positioning anchor (translate -50% centers on target).
src\ui\uiRoot.js:881:    .sf-target { width:150px; }
src\ui\uiRoot.js:882:    .sf-target__name { font-size:11px; }
src\ui\uiRoot.js:883:    .sf-target__meta { font-size:10px; }
src\data\modules.js:93:    id: 'mod_targeting_computer_m', name: 'Targeting Computer M', slotType: 'utility', size: 'M', tier: 3, mass: 4, price: 40000, requiresTech: 'tech_fire_control',
src\core\flight\flightTelemetry.js:11:export function computeFlightTelemetry({ body, profile, control = null, target = null, horizonS = 12 } = {}) {
src\core\flight\flightTelemetry.js:37:    target: null,
src\core\flight\flightTelemetry.js:40:  if (target) result.target = computeRelativeTargetTelemetry(b, target, horizonS, p);
src\core\flight\flightTelemetry.js:133:export function computeRelativeTargetTelemetry(body, target, horizonS = 12, profile = {}) {
src\core\flight\flightTelemetry.js:135:  const t = normalizeTarget(target);
src\core\flight\flightTelemetry.js:153:  const timeToContact = solveCircleContact(relPos, relVel, positive(target.radius, 0) + positive(body.radius, 0));
src\core\flight\flightTelemetry.js:157:    id: target.id ?? null,
src\core\flight\flightTelemetry.js:168:      ? safeStop.directDistance < Math.max(0, distance - positive(target.radius, 0) - positive(body.radius, 0))
src\core\flight\flightTelemetry.js:174:export function solveIntercept(shooterPos, shooterVel, targetPos, targetVel, projectileSpeed, maxTimeS = 20) {
src\core\flight\flightTelemetry.js:176:    x: finite(targetPos && targetPos.x) - finite(shooterPos && shooterPos.x),
src\core\flight\flightTelemetry.js:177:    z: finite(targetPos && targetPos.z) - finite(shooterPos && shooterPos.z),
src\core\flight\flightTelemetry.js:180:    x: finite(targetVel && targetVel.x) - finite(shooterVel && shooterVel.x),
src\core\flight\flightTelemetry.js:181:    z: finite(targetVel && targetVel.z) - finite(shooterVel && shooterVel.z),
src\core\flight\flightTelemetry.js:204:    x: finite(targetPos && targetPos.x) + finite(targetVel && targetVel.x) * t,
src\core\flight\flightTelemetry.js:205:    z: finite(targetPos && targetPos.z) + finite(targetVel && targetVel.z) * t,
src\core\flight\flightTelemetry.js:291:function normalizeTarget(target = {}) {
src\core\flight\flightTelemetry.js:292:  return { id: target.id, pos: vec(target.pos), vel: vec(target.vel), radius: positive(target.radius, 0) };
src\ai\director.js:35:      targetPressure: this.config.initialPressure,
src\ai\director.js:65:    const target = clamp(0.22 + dominance * 0.62 - distress * 0.48 + authoredBias, envelope.min, envelope.max);
src\ai\director.js:66:    s.targetPressure = target;
src\ai\director.js:67:    const delta = target - s.pressure;
src\ai\director.js:76:      push('begin_build', s.phaseTick >= cfg.respiteMinTicks ? saturate((target - cfg.respiteThreshold) * 1.8) : 0, 'pressure target recovered');
src\ai\director.js:83:      push('begin_respite', s.phaseTick >= cfg.peakMinTicks ? saturate((cfg.peakThreshold - target) * 2 + distress * 0.8) : 0, 'peak delivered or squad distressed');
src\ai\director.js:109:        context: { telemetry, distress, dominance, envelope, targetPressure: target },
src\ai\director.js:117:      targetPressure: s.targetPressure,
src\core\constraints\masslineController.js:49:    targetLength: def.defaultLength,
src\core\constraints\masslineController.js:72: * @param {Object} args.targetBody optional mass/velocity data
src\core\constraints\masslineController.js:101:  const targetLength = clamp(prev.targetLength + reelVelocity * dt, def.minLength, def.maxLength);
src\core\constraints\masslineController.js:104:  // winch also loses authority; this is what makes a heavy target feel heavy without
src\core\constraints\masslineController.js:109:  const restLength = moveToward(prev.restLength, targetLength, lengthRate * dt);
src\core\constraints\masslineController.js:161:    targetLength,
src\core\constraints\masslineController.js:172:  return buildResult(next, def, t, command, events, shouldCut, energyCost, overloadRatio, args.ownerBody, args.targetBody);
src\core\constraints\masslineController.js:178: * owner will be moved far more than the target.
src\core\constraints\masslineController.js:180:export function estimateMasslineResponse(ownerMass, targetMass) {
src\core\constraints\masslineController.js:182:  const b = positive(targetMass, 1);
src\core\constraints\masslineController.js:186:    targetMotionShare: a / sum,
src\core\constraints\masslineController.js:192:function buildResult(runtime, def, telemetry, command, events, cut, energyCost = 0, overloadRatio = 0, ownerBody = null, targetBody = null) {
src\core\constraints\masslineController.js:195:  const relative = estimateMasslineResponse(ownerBody && ownerBody.mass, targetBody && targetBody.mass);
src\core\constraints\masslineController.js:213:      targetLength: runtime.targetLength,
src\core\constraints\masslineController.js:250:    targetLength: clamp(finite(r.targetLength, def.defaultLength), def.minLength, def.maxLength),
src\core\constraints\masslineController.js:284:function approach(cur, target, amount) { return moveToward(cur, target, Math.abs(amount)); }
src\core\constraints\masslineController.js:285:function moveToward(cur, target, amount) { const d = target - cur; return Math.abs(d) <= amount ? target : cur + Math.sign(d) * amount; }
src\core\coreSystem.js:32:    const getEntity = (id) => state.entities.get(id) || null;
src\core\coreSystem.js:33:    const removeEntity = (id) => { const e = state.entities.get(id); if (e) e.alive = false; };
src\core\coreSystem.js:55:    const player = () => state.entities.get(state.playerId) || null;
src\core\coreSystem.js:67:      const e = state.entities.get(id);
src\core\registry.js:2:// each step (§2.3), and the render-phase systems each frame. main.js builds it with the ctx.
src\core\registry.js:58:  // sim step order (AI submits commands, actions resolve before flight, weapons before physics) — render-phase systems excluded.
src\core\registry.js:129:        if (vfx.update) {
src\core\registry.js:131:          try { vfx.update(frameDt, state); }
src\combat\actions.js:21:      target: normalizeTarget(request, actionDef.target),
src\combat\actions.js:32:      target: queued.target,
src\combat\actions.js:83:    const targetCheck = validateTarget(actor, request.target, def.target);
src\combat\actions.js:84:    if (!targetCheck.ok) return reject(request, targetCheck.reason);
src\combat\actions.js:106:      target: request.target,
src\combat\actions.js:121:      target: request.target,
src\combat\actions.js:171:      const attachment = attachments.get(instance.target && instance.target.attachmentId);
src\combat\actions.js:210:        const targetId = instance.target && instance.target.entityId;
src\combat\actions.js:214:          targetId,
src\combat\actions.js:215:          sourceSocketId: instance.target && instance.target.sourceSocketId,
src\combat\actions.js:216:          targetSocketId: instance.target && instance.target.targetSocketId,
src\combat\actions.js:223:        result = attachments.reel(instance.target && instance.target.attachmentId, effect.restLengthDelta, effect.minRestLength);
src\combat\actions.js:226:        result = attachments.cut(instance.target && instance.target.attachmentId, actor.id, effect.reason || 'action_cut');
src\combat\actions.js:229:        const targetId = instance.target && instance.target.entityId;
src\combat\actions.js:230:        const target = entity(targetId);
src\combat\actions.js:231:        if (!target || !target.alive) result = { ok: false, reason: 'target_missing' };
src\combat\actions.js:234:          if (!packet.hit) packet.hit = { pos: { x: target.pos.x, z: target.pos.z } };
src\combat\actions.js:237:            targetId,
src\combat\actions.js:250:      targetId: actionTargetEntityId(instance),
src\combat\actions.js:265:      targetId: actionTargetEntityId(instance),
src\combat\actions.js:279:      targetId: actionTargetEntityId(instance),
src\combat\actions.js:295:      target: request.target || null,
src\combat\actions.js:309:      target: null,
src\combat\actions.js:340:  function validateTarget(actor, target, targetDef = {}) {
src\combat\actions.js:341:    if (!targetDef.required && (!target || target.kind === 'none')) return { ok: true };
src\combat\actions.js:342:    if (!target) return { ok: false, reason: 'target_required' };
src\combat\actions.js:343:    if (targetDef.kind === 'entity') {
src\combat\actions.js:344:      const targetEntity = entity(target.entityId);
src\combat\actions.js:345:      if (!targetEntity || !targetEntity.alive || targetEntity.id === actor.id) return { ok: false, reason: 'target_missing' };
src\combat\actions.js:346:      if (targetDef.hostile && actor.team != null && targetEntity.team != null && actor.team === targetEntity.team) return { ok: false, reason: 'target_not_hostile' };
src\combat\actions.js:347:      if (Number.isFinite(targetDef.maxRange)) {
src\combat\actions.js:348:        const distance = Math.hypot(targetEntity.pos.x - actor.pos.x, targetEntity.pos.z - actor.pos.z);
src\combat\actions.js:349:        if (distance > targetDef.maxRange) return { ok: false, reason: 'target_out_of_range' };
src\combat\actions.js:353:    if (targetDef.kind === 'attachment') {
src\combat\actions.js:354:      const attachment = attachments.get(target.attachmentId);
src\combat\actions.js:356:      if (targetDef.ownedByActor && attachment.ownerId !== actor.id) return { ok: false, reason: 'not_attachment_owner' };
src\combat\actions.js:359:    if (targetDef.kind === 'point') return Number.isFinite(target.x) && Number.isFinite(target.z) ? { ok: true } : { ok: false, reason: 'point_target_invalid' };
src\combat\actions.js:360:    if (targetDef.kind === 'none') return { ok: true };
src\combat\actions.js:361:    return { ok: false, reason: 'target_kind_invalid' };
src\combat\actions.js:378:    return state.entities && state.entities.get ? state.entities.get(id) || null : null;
src\combat\actions.js:413:function normalizeTarget(request, targetDef = {}) {
src\combat\actions.js:414:  if (!targetDef || targetDef.kind === 'none') return { kind: 'none' };
src\combat\actions.js:415:  if (targetDef.kind === 'entity') {
src\combat\actions.js:416:    const entityId = request.targetId != null ? request.targetId : request.target && request.target.entityId;
src\combat\actions.js:419:      sourceSocketId: request.sourceSocketId || request.target && request.target.sourceSocketId || null,
src\combat\actions.js:420:      targetSocketId: request.targetSocketId || request.target && request.target.targetSocketId || null,
src\combat\actions.js:423:  if (targetDef.kind === 'attachment') {
src\combat\actions.js:424:    return { kind: 'attachment', attachmentId: request.attachmentId || request.target && request.target.attachmentId || null };
src\combat\actions.js:426:  if (targetDef.kind === 'point') {
src\combat\actions.js:427:    const point = request.point || request.target || {};
src\combat\actions.js:453:    target: instance.target,
src\combat\actions.js:475:  return instance && instance.target && instance.target.kind === 'entity' ? instance.target.entityId : null;
src\core\rapierCollisionWorld.js:162:  return e.type === 'projectile' || !!(e.flags && e.flags.boosting);
src\ui\accessibility.js:92://     - target  : currently only a white ring (radar.js:105-108) — already shape-redundant, kept here.
src\ui\accessibility.js:103:// threat caret), 'square' (neutral), 'diamond' (friendly), 'chevron' (ally heading), 'ring' (target).
src\ui\accessibility.js:104:// `icon` is a unicode glyph for DOM contexts (target panel, legend, alert strips).
src\ui\accessibility.js:107:  // --- radar / target states (canvas — color was the sole channel; shape is the fix) ---
src\ui\accessibility.js:112:  target:   { color: '#ffffff', cssVar: null,            shape: 'ring',     icon: '◎', label: 'Target' },
src\ui\accessibility.js:192: * @param {Document|HTMLElement} [target=document.documentElement]  root to toggle classes/vars on
src\ui\accessibility.js:194:export function applyAccessibility(settings, target) {
src\ui\accessibility.js:210:  const root = target || (typeof document !== 'undefined' ? document.documentElement : null);
src\ui\floatingText.js:37:    nodes.push({ el, alive: false, age: 0, life: 1, x: 0, y: 0, vy: 0, vx: 0, targetId: null, wx: 0, wz: 0 });
src\ui\floatingText.js:49:  function spawn(text, cls, wx, wz, targetId, opts) {
src\ui\floatingText.js:57:    n.targetId = targetId != null ? targetId : null;
src\ui\floatingText.js:77:    const e = p.targetId != null ? state.entities.get(p.targetId) : null;
src\ui\floatingText.js:81:    spawn(Math.round(p.amount) + '', dmgColor(p) + (big ? ' sf-ft--big' : ''), wx, wz, p.targetId, { life: big ? 1.2 : 0.9, vy: big ? 62 : 46 });
src\ui\floatingText.js:83:  bus.on('combat:damage', (p) => { if (p && p.brokeShield) { const e = state.entities.get(p.targetId); if (e) spawn('SHIELD DOWN', 'sf-ft--shielddown', e.pos.x, e.pos.z, null, { life: 1.0, vy: 30 }); } });
src\ui\floatingText.js:89:  bus.on('ship:dash', (p) => { if (p && p.shipId === state.playerId) { const e = state.entities.get(p.shipId); if (e) spawn('DASH', 'sf-ft--dash', e.pos.x, e.pos.z, null, { life: 0.7, vy: 50 }); } });
src\ui\floatingText.js:93:    if (!p || !p.pos || !p.bountyCr || p.killerId !== state.playerId) return;
src\ui\floatingText.js:103:    if (!p || p.collectorId !== state.playerId) return;
src\ui\floatingText.js:162:  function update(dt) {
src\ui\floatingText.js:170:      // follow the entity if it still exists, else stay at the world point
src\ui\floatingText.js:172:      if (n.targetId != null) { const e = state.entities.get(n.targetId); if (e) { wx = e.pos.x; wz = e.pos.z; } }
src\ui\targetPanel.js:1:// Target panel (ARCHITECTURE §5, spec "Target panel") — the selected-target readout above
src\ui\targetPanel.js:2:// the radar. Populated from state.player.targetId → entity lookup. Shows name, faction tag,
src\ui\targetPanel.js:3:// hull%/shield% mini-bars, distance (wu) and closing speed. Hidden when targetId is null/dead.
src\ui\targetPanel.js:35:  el.className = 'sf-target sf-hudpanel';
src\ui\targetPanel.js:41:    <div class="sf-target__head">
src\ui\targetPanel.js:42:      <span class="sf-target__name">—</span>
src\ui\targetPanel.js:43:      <span class="sf-target__faction"></span>
src\ui\targetPanel.js:47:    <div class="sf-target__meta">
src\ui\targetPanel.js:48:      <span class="sf-target__dist mono">0 wu</span>
src\ui\targetPanel.js:49:      <span class="sf-target__closing mono"></span>
src\ui\targetPanel.js:52:  const elName = el.querySelector('.sf-target__name');
src\ui\targetPanel.js:53:  const elFac = el.querySelector('.sf-target__faction');
src\ui\targetPanel.js:56:  const elDist = el.querySelector('.sf-target__dist');
src\ui\targetPanel.js:57:  const elClose = el.querySelector('.sf-target__closing');
src\ui\targetPanel.js:74:    const tid = state.player.targetId;
src\ui\targetPanel.js:75:    const t = tid != null ? state.entities.get(tid) : null;
src\ui\targetPanel.js:84:    const targetChanged = tid !== lastTargetId || nextName !== lastName || t.factionId !== lastFactionId;
src\ui\targetPanel.js:85:    if (targetChanged) {
src\ui\targetPanel.js:107:    const p = state.entities.get(state.playerId);
src\ui\targetPanel.js:108:    if (p && (targetChanged || options.slow || (tickN % 6) === 0)) {
src\ui\comms.js:355:    const btn = ev.target.closest('.sf-endgame__accept');
src\core\eventTrace.js:57:  'jump:arrive',
src\contracts\scenarioSchemas.js:44:const METRIC_KEYS = new Set(['id', 'target', 'evidence', 'required', 'beatIds']);
src\contracts\scenarioSchemas.js:72:  'targetActorId',
src\contracts\scenarioSchemas.js:196:    requireString(metric.target, `${path}.target`, issues, file);
src\contracts\scenarioSchemas.js:373:      if (condition.targetActorId != null) requireId(condition.targetActorId, `${cpath}.targetActorId`, issues, file);
src\contracts\scenarioSchemas.js:377:      requireId(condition.targetActorId, `${cpath}.targetActorId`, issues, file);
src\contracts\scenarioSchemas.js:380:      requireId(condition.targetActorId, `${cpath}.targetActorId`, issues, file);
src\contracts\scenarioSchemas.js:389:      if (condition.targetActorId != null) requireId(condition.targetActorId, `${cpath}.targetActorId`, issues, file);
src\contracts\scenarioSchemas.js:447:      for (const key of ['actorId', 'ownerActorId', 'targetActorId']) {
src\ui\navigation\localSpaceMapModel.js:57:      targetable: contact.targetable !== false,
src\ui\hud.js:6://   bottom-right  : radar (radar.js) with target panel (targetPanel.js) above it
src\ui\hud.js:19:import { createTargetPanel } from './targetPanel.js';
src\ui\hud.js:94:  return MT_SECTOR_BY_ID.get(id) || id || 'target sector';
src\ui\hud.js:171:      return 'Eliminate target';
src\ui\hud.js:177:      return `Scan ${prog}/${tgt} targets`;
src\ui\hud.js:347:    if (!p || p.targetId !== state.playerId) return;
src\ui\hud.js:485:      const p = state.entities.get(state.playerId);
src\ui\hud.js:497:  // ---- bottom-right: target panel + radar ----
src\ui\hud.js:500:  const targetPanel = createTargetPanel(ctx);
src\ui\hud.js:502:  rightDock.append(targetPanel.el, radar.el);
src\ui\hud.js:510:    () => state.entities.get(state.playerId),
src\ui\hud.js:511:    state.playerId,
src\ui\hud.js:549:  // ---- combat HUD: lock-on ring, weapon heat bars, target lock diamond ----
src\ui\hud.js:599:  // Target lock diamond — follows the locked target's screen position.
src\ui\hud.js:825:    const btn = ev.target.closest('.sf-cargo-row__jet');
src\ui\hud.js:917:    if (!p || p.ownerId !== state.playerId) return;
src\ui\hud.js:943:  // event, we ease the displayed number from the previously-shown value toward the target over
src\ui\hud.js:945:  // digit jump — the classic "numbers feel alive" polish. Retargets smoothly if credits change
src\ui\hud.js:947:  let _credFrom = 0, _credTo = 0, _credT = 1;   // _credT in [0,1]; 1 = at rest at target
src\ui\hud.js:956:    const target = Math.round((state.player || {}).credits || 0);
src\ui\hud.js:957:    // Retarget from the value currently displayed (not the old target) so chained changes stay smooth
src\ui\hud.js:959:    _credTo = target;
src\ui\hud.js:994:        const need = o.target != null ? o.target : (o.required != null ? o.required : (o.count != null ? o.count : 0));
src\ui\hud.js:1026:  // Combat HUD update — lock ring + weapon heat bars + target lock diamond
src\ui\hud.js:1056:    // Locking a missile target was visually indicated but sonically silent — a clear cue closes that.
src\ui\hud.js:1090:    // ---- Target lock diamond (world-space overlay on locked/selected target) ----
src\ui\hud.js:1091:    const tid = (state.player || {}).targetId;
src\ui\hud.js:1092:    const tgt = tid != null ? state.entities.get(tid) : null;
src\ui\hud.js:1124:  const targetClock = createHudClock(20);
src\ui\hud.js:1145:    const targetDt = consumeHudClock(targetClock, frameDt);
src\ui\hud.js:1149:    const targetTick = targetDt > 0;
src\ui\hud.js:1153:    const p = state.entities.get(state.playerId);
src\ui\hud.js:1229:          const targetEnt = state.entities.get(tether.targetId);
src\ui\hud.js:1230:          const targetName = (targetEnt && (targetEnt.name || (targetEnt.data && targetEnt.data.name))) || (targetEnt ? targetEnt.type : '');
src\ui\hud.js:1231:          setText(elTether, `${strain > 0.85 ? 'CRITICAL' : strain > 0.6 ? 'STRAINED' : 'LOCKED'}${targetName ? ' · ' + String(targetName).toUpperCase() : ''} · [G] RELEASE`);
src\ui\hud.js:1243:      // Reticle reflects fire mode: amber ring when auto-fire is engaged (guns auto-target hostiles),
src\ui\hud.js:1309:    // --- target panel: DOM/compositor surface; update on a fixed HUD cadence ---
src\ui\hud.js:1310:    if (targetTick) targetPanel.update({ slow });
src\ui\hud.js:1312:    // --- combat HUD: lock ring, weapon heat bars, target diamond ---
src\ui\hud.js:1313:    if (targetTick) updateCombatHud(p, slow);
src\ui\hud.js:1334:    const p = state.entities.get(state.playerId);
src\ui\hud.js:1380:      wp = m.waypoint || m.targetPos || (m.objectives && m.objectives[0] && m.objectives[0].pos) || null;
src\ui\hud.js:1386:      // Route/cross-sector guidance — not a live target with a distance, so render plain text
src\ui\hud.js:1405:    // distance + ETA readout (always shown while a nav target is set)
src\ui\hud.js:1421:    // clamp to a screen-edge ellipse around center, pointing toward target
src\ui\hud.js:1443:    forceHudClock(targetClock);
src\ui\hud.js:1450:    if (targetPanel.forceRefresh) targetPanel.forceRefresh();
src\ui\damageIndicators.js:94:    const isPlayer = p.isPlayer || (p.targetId != null && p.targetId === this._playerId);
src\ui\damageIndicators.js:181:    _playerId: null,
src\ui\damageIndicators.js:182:    bind(getPlayer, playerId) { this._player = getPlayer; this._playerId = playerId; return this; },
src\ui\confirm.js:117:    if (root._sfConfirmToken !== token || dialog.contains(ev.target)) return;
src\ui\confirm.js:135:    if (ev.target === root) close(false);
src\ui\confirm.js:161:  // Esc cancels. Enter follows the focused button so danger dialogs can default safely to Cancel.
src\ui\input.js:4://          Tab (cycle target), P (pause), E (dock in range / undock station hub; Enter secondary dock),
src\ui\input.js:58:    const t = ev.target;
src\ui\input.js:176:        // Drill lens (V2 §7 / cut-list #27): open the ant-farm mining screen on the targeted
src\ui\input.js:214:  // Resolve a drillable asteroid: player's selected target (if it's an asteroid), else the mining
src\ui\input.js:218:    const tid = state.player.targetId;
src\ui\input.js:220:      const t = state.entities.get(tid);
src\ui\input.js:227:        const t = state.entities.get(mining._lockTargetId);
src\ui\input.js:232:      bus.emit('toast', { text: 'No asteroid targeted — target a rock and press ' + BINDINGS.drill.label + ' to drill', kind: 'warn', ttl: 3 });
src\ui\input.js:263:    const player = state.entities.get(state.playerId);
src\audio\audioSystem.js:33:// target stem weights per music state (A=calm drone, B=tense pad, C=combat, D=docked warm)
src\audio\audioSystem.js:150:    rt._wantMining = null;        // { minerId, targetId } desired mining loop
src\audio\audioSystem.js:174:      // Shield break: a sharp energy crackle at the target's position. Use the explosion-small recipe
src\audio\audioSystem.js:177:      const target = p && p.combatantId ? this.state.entities.get(p.combatantId) : null;
src\audio\audioSystem.js:178:      const position = pos || (target ? { x: target.pos.x, z: target.pos.z } : null);
src\audio\audioSystem.js:210:    // destination (the jump:arrive event from world.js after the 1.2s tunnel). Previously arrival
src\audio\audioSystem.js:213:    bus.on('jump:arrive', () => this.play('sfx_jump_arrive', { gain: 0.7 }));
src\audio\audioSystem.js:228:      if (p && p.shipId === this.state.playerId) this.play('sfx_boost_whoosh', { gain: 0.35 });
src\audio\audioSystem.js:234:      if (p && p.shipId === this.state.playerId) this.play('sfx_boost_whoosh', { gain: 0.6, rate: 1.4 });
src\audio\audioSystem.js:351:    const ramp = (param, target) => {
src\audio\audioSystem.js:355:        param.linearRampToValueAtTime(Math.max(0.0001, target), t + 0.05);
src\audio\audioSystem.js:356:      } catch (_) { try { param.value = target; } catch (__) {} }
src\audio\audioSystem.js:433:    const e = this.state.entities.get(this.state.playerId);
src\audio\audioSystem.js:532:    rt._wantMining = { minerId: p.minerId, targetId: p.targetId };
src\audio\audioSystem.js:537:    if (v) { v.trackId = p.targetId; rt.loops.mining = v; this._markLoopPositionDirty(); }
src\audio\audioSystem.js:668:    // triggered by the jump:arrive event (synced to the actual materialization after the 1.2s
src\audio\audioSystem.js:669:    // tunnel), not a fixed timer — see the jump:arrive subscriber in init().
src\audio\audioSystem.js:676:  // trickle, not machine-gun noise. Position follows the player's mining target when available.
src\audio\audioSystem.js:1149:      const target = Math.max(0.0001, w[key]);
src\audio\audioSystem.js:1151:        if (instant) { g.gain.cancelScheduledValues(t); g.gain.setValueAtTime(target, t); }
src\audio\audioSystem.js:1152:        else { g.gain.setTargetAtTime(target, t, xf / 3); }
src\audio\audioSystem.js:1153:      } catch (_) { try { g.gain.value = target; } catch (__) {} }
src\audio\audioSystem.js:1160:    const player = state.entities.get(state.playerId);
src\audio\audioSystem.js:1192:    const player = this.state.entities.get(this.state.playerId);
src\audio\audioSystem.js:1266:      if (rt._wantMining && !rt.loops.mining) this._onMiningStart({ minerId: rt._wantMining.minerId, targetId: rt._wantMining.targetId });
src\audio\audioSystem.js:1297:  // Track positional loop voices (beam/mining) toward their target's current position.
src\audio\audioSystem.js:1303:      const e = this.state.entities.get(v.trackId);
src\contracts\evidenceSchemas.js:11:const COMMAND_KEYS = new Set(['kind', 'actionId', 'actor', 'target', 'attachment', 'branchId', 'source']);
src\contracts\evidenceSchemas.js:172:      validateActorRef(command.target, `${itemPath}.target`, issues, file, { required: false });
src\contracts\evidenceSchemas.js:179:      if (command.target != null) addIssue(issues, file, `${itemPath}.target`, 'forbidden', 'scenarioBranch commands cannot include target');
src\ui\screens\factions.js:162:  if (!next) return 'Trusted work unlocked; Allied retainers are the next long-term faction target.';
src\ui\screens\manufacture.js:144:      detail: materials.action.title + ' Mine, salvage, buy cargo, or follow a trade route for the missing input.',
src\ui\screens\manufacture.js:205:    const btn = ev.target.closest('[data-act="build"]');
src\ui\missionPreflight.js:219:  const target = Math.floor(Number(raw) || 0);
src\ui\missionPreflight.js:220:  return target > 0 ? target : 0;
src\ui\missionPreflight.js:256:  const target = missionCargoTargetUnits(m);
src\ui\missionPreflight.js:257:  if (target <= 0) return null;
src\ui\missionPreflight.js:258:  const remaining = Math.max(0, target - missionCargoProgressUnits(m));
src\ui\missionPreflight.js:336:  return state && state.entities && state.entities.get && state.playerId != null
src\ui\missionPreflight.js:337:    ? state.entities.get(state.playerId)
src\ui\missionPreflight.js:365:  return sector ? sector.name : (id || '').replace(/^sector_/, '').replace(/_/g, ' ') || 'target sector';
src\ui\missionPreflight.js:369:  const targetSectorId = missionDestSectorId(m);
src\ui\missionPreflight.js:372:  if (targetSectorId && currentSectorId && targetSectorId === currentSectorId) {
src\ui\missionPreflight.js:375:  if (targetSectorId) {
src\ui\missionPreflight.js:378:      text: currentSectorId ? `Jump route: ${sectorName(targetSectorId)}` : `Route: ${sectorName(targetSectorId)}`,
src\ui\missionPreflight.js:388:  const targetSectorId = missionDestSectorId(m);
src\ui\missionPreflight.js:389:  if (!targetSectorId) return null;
src\ui\missionPreflight.js:391:  const local = !!(currentSectorId && currentSectorId === targetSectorId);
src\ui\missionPreflight.js:392:  const signal = sectorSignalFor(state, targetSectorId);
src\ui\missionPreflight.js:396:  const forecast = local ? null : forecastTransitFor(state, targetSectorId, {
src\ui\missionPreflight.js:414:    warning = label + ' route risk to ' + sectorName(targetSectorId) + '; refuel, repair, and consider escort before accepting.';
src\ui\missionPreflight.js:418:    warning = label + ' local-sector risk near ' + sectorName(targetSectorId) + '; launch ready for contact.';
src\ui\missionPreflight.js:422:    targetSectorId,
src\ui\missionPreflight.js:423:    targetName: sectorName(targetSectorId),
src\ui\missionPreflight.js:476:  const targetSectorId = missionDestSectorId(m);
src\ui\missionPreflight.js:478:  const offSector = !!(targetSectorId && currentSectorId && targetSectorId !== currentSectorId);
src\ui\screens\bar.js:332:    return 'Accept + Track puts the target in your Mission Log and sets nav when the lead is known.';
src\ui\screens\bar.js:761:              text: 'Got one - "' + missionOfferTitle(bounty) + '." Pays ' + reward + ' cr. Dangerous work, but Accept + Track will keep the target in your Mission Log.',
src\ui\screens\bar.js:967:    const logBtn = ev.target.closest('[data-open-mission-log]');
src\ui\screens\bar.js:974:    const surveyBtn = ev.target.closest('[data-buy-survey]');
src\ui\screens\bar.js:1012:    const acceptBtn = ev.target.closest('[data-accept-mission]');
src\ui\screens\bar.js:1052:    const btn = ev.target.closest('[data-choice]');
src\ui\controlPrompts.js:4:  flight: `W/Up thrust  •  A D steer  •  Mouse aim  •  LMB/Space fire  •  RMB mine  •  Shift boost  •  X countermeasure  •  Tab target  •  ${BINDINGS.localmap.label} local map  •  ${BINDINGS.starmap.label} star map  •  ${BINDINGS.missionLog.label} log  •  ${BINDINGS.codex.label} codex  •  ${BINDINGS.cargo.label} cargo  •  ${BINDINGS.comms.label} comms`,
src\ui\controlPrompts.js:6:  combat: 'LMB/Space fire  •  Mouse aim at target  •  Tab cycle targets  •  X countermeasure  •  F auto-fire  •  Shift boost to dodge',
src\ui\controlPrompts.js:13:  firstCombat: 'Hostile detected! LMB or Space to fire. Hold aim on a target to lock on. F toggles auto-fire.',
src\ui\controlPrompts.js:19:  flight: 'Left stick fly  •  Right stick aim  •  RT fire  •  LT mine  •  RB boost  •  LB brake  •  R3 countermeasure  •  A dock  •  X target  •  View star map  •  Y codex  •  Start pause/log',
src\ui\controlPrompts.js:21:  combat: 'RT fire  •  Right stick aim at target  •  X cycle targets  •  R3 countermeasure  •  RB boost to dodge  •  Start pause',
src\ui\controlPrompts.js:28:  firstCombat: 'Hostile detected! RT to fire. Hold right-stick aim on a target to lock on. X cycles targets.',
src\ui\controlPrompts.js:35:  mining: 'Mine button hold to mine  •  Release to cool  •  Fly through cargo drift  •  Touch target controls for signals',
src\ui\controlPrompts.js:36:  combat: 'Fire button  •  Right stick aim at target  •  Boost button to dodge',
src\ui\controlPrompts.js:43:  firstCombat: 'Hostile detected! Fire to shoot. Hold right-stick aim on a target to lock on.',
src\ui\controlPrompts.js:48:// HELM ASSIST (GDD §4.1, the 2.0 default scheme) — kbm copy for "the nose follows the mouse".
src\ui\controlPrompts.js:52:  flight: `Mouse steer+aim  •  W thrust  •  S reverse  •  A D strafe  •  Space brake  •  LMB fire  •  RMB mine  •  Shift boost  •  G tether  •  X countermeasure  •  Tab target  •  ${BINDINGS.localmap.label} local map  •  ${BINDINGS.starmap.label} nav chart  •  ${BINDINGS.missionLog.label} log  •  ${BINDINGS.cargo.label} cargo`,
src\ui\controlPrompts.js:53:  combat: 'Steer with the mouse  •  LMB fire  •  Tab cycle targets  •  X countermeasure  •  F auto-fire  •  Shift boost  •  Space brake to break pursuit',
src\ui\controlPrompts.js:54:  tutorialFlight: 'Follow the yellow nav arrow to the bad reading. Your nose follows the mouse — W thrusts toward it, Space brakes.',
src\ui\controlPrompts.js:55:  firstFlight: `Your nose follows the mouse. W thrusts, S reverses, A/D strafe, Space brakes to a full stop, Shift boosts. LMB fires the Pulse Laser S, RMB mines marked rocks. ${BINDINGS.localmap.label} opens the local map, ${BINDINGS.starmap.label} the nav chart, ${BINDINGS.dock.label} docks.`,
src\systems\cargo.js:98:      if (collectorId !== state.playerId) return; // NPC/drone collection is not the player hold
src\systems\cargo.js:114:      if (shipId !== state.playerId) return;
src\systems\cargo.js:162:    const player = state.entities.get(state.playerId);
src\ui\screens\missionLog.js:125:      return 'Eliminate target';
src\ui\screens\missionLog.js:131:      return `Scan ${prog}/${tgt} targets`;
src\ui\screens\missionLog.js:145:      return 'Next: follow tracked nav to ' + dest + ', dock, and complete the handoff.';
src\ui\screens\missionLog.js:149:      return 'Next: mine ' + cmdtyName(p.cmdtyId) + ' in an asteroid field, keep cargo space open, then follow the tracker.';
src\ui\screens\missionLog.js:153:      return 'Next: follow tracked nav, engage the target, then check this log for completion.';
src\ui\screens\missionLog.js:155:      return 'Next: follow tracked nav and clear hostiles until the progress bar fills.';
src\ui\screens\missionLog.js:159:      return 'Next: follow tracked nav and scan each marked site.';
src\ui\screens\missionLog.js:163:      return 'Next: track this mission, undock, and follow the nav/objective marker.';
src\ui\screens\missionLog.js:181:  const targetSectorId = (wp && wp.sectorId) || mission.destSectorId || null;
src\ui\screens\missionLog.js:182:  const sameSector = targetSectorId && currentSectorId && targetSectorId === currentSectorId;
src\ui\screens\missionLog.js:184:  if (hasLocalFix || sameSector || !targetSectorId) {
src\ui\screens\missionLog.js:241:  const targetSectorId = waypoint.sectorId || (info && info.sectorId) || null;
src\ui\screens\missionLog.js:243:  const sameSector = targetSectorId && currentSectorId && targetSectorId === currentSectorId;
src\ui\screens\missionLog.js:245:  if (hasLocalFix || sameSector || !targetSectorId) {
src\ui\screens\missionLog.js:325:  return state && state.entities && state.entities.get && state.playerId != null
src\ui\screens\missionLog.js:326:    ? state.entities.get(state.playerId)
src\ui\screens\missionLog.js:355:  const target = missionDestSectorId(m);
src\ui\screens\missionLog.js:357:  return !!(target && current && target !== current);
src\ui\screens\missionLog.js:361:  const target = missionDestSectorId(m);
src\ui\screens\missionLog.js:363:  if (target && current && target !== current) return 'off-sector route';
src\ui\screens\missionLog.js:364:  if (target && current && target === current) return 'local route';
src\ui\screens\missionLog.js:512:        body: 'Fit the Kestrel for a low-risk bounty, track the target, and cash the first kill.',
src\ui\screens\missionLog.js:596:      body: 'Set one active contract as your nav target before leaving the station lane.',
src\ui\screens\missionLog.js:807:      const btn = ev.target.closest('[data-rec-act]');
src\ui\screens\missionLog.js:822:      const btn = ev.target.closest('[data-mid]');
src\ui\screens\missionLog.js:914:      this._listEl.innerHTML = '<div class="sf-mlog-empty">No active missions. Dock at a station, open Missions or the Bar, accept a contract, then undock and follow the tracked nav marker.</div>';
src\systems\drill.js:117:    const target = d.field[nc][d.avatar.row];
src\systems\drill.js:118:    if (target.type !== 'empty') return; // solid wall — must drill, not push through
src\systems\drill.js:132:    const target = d.field[d.avatar.col][nr];
src\systems\drill.js:133:    if (target.type === 'empty') {
src\systems\drill.js:140:    target.hp -= DRILL_DPS * dt;
src\systems\drill.js:141:    if (target.hp <= 0) {
src\systems\drill.js:143:      const wasVein = target.type === 'vein';
src\systems\drill.js:144:      const wasGas = target.type === 'gas';
src\systems\drill.js:145:      const ore = target.ore;
src\systems\drill.js:146:      const yieldU = target.yieldU || 0;
src\systems\drill.js:164:        const player = this.state.entities.get(this.state.playerId);
src\ui\screens\codex.js:142:    body: 'Bounty broker. A clean tag is useful because it can be billed twice before anyone asks which target was real.',
src\ui\screens\services.js:328:    const btn = ev.target.closest('[data-svc]');
src\ui\screens\services.js:339:      const e = state.entities.get(state.playerId);
src\ui\screens\services.js:391:    const e = state.entities.get(state.playerId);
src\ui\screens\techTree.js:304:      const btn = e.target.closest('button[data-act]');
src\ui\screens\localmap.js:147:      const btn = ev.target.closest('[data-act="route-nav"]');
src\ui\screens\localmap.js:205:    const player = state.entities && state.entities.get(state.playerId);
src\ui\screens\localmap.js:209:      if (!e || !e.alive || e.id === state.playerId) continue;
src\ui\screens\localmap.js:252:    const player = state.entities.get(state.playerId);
src\ui\screens\localmap.js:346:    const player = state.entities && state.entities.get(state.playerId);
src\ui\screens\localmap.js:482:    item.id = wp.missionId || wp.targetEntityId || wp.stationId || 'nav-waypoint';
src\ui\screens\localmap.js:577:      const targetSectorId = wp.sectorId || null;
src\ui\screens\localmap.js:579:      const fixLabel = targetSectorId && currentSectorId && targetSectorId === currentSectorId
src\ui\screens\localmap.js:606:  const target = Math.max(1, m.objectiveTarget || 1);
src\ui\screens\localmap.js:607:  if (m.type === 'mining_quota') return `Mine ${progress}/${target} units`;
src\ui\screens\localmap.js:608:  if (m.type === 'bulk_trade') return `Sell ${progress}/${target} units`;
src\ui\screens\localmap.js:609:  if (m.type === 'patrol_clear') return `Clear ${progress}/${target} hostiles`;
src\ui\screens\localmap.js:610:  if (m.type === 'recon_scan') return `Scan ${progress}/${target} sites`;
src\ui\screens\localmap.js:611:  return progress > 0 ? `${progress}/${target}` : 'Proceed to the objective';
src\ui\screens\localmap.js:630:  const targetSectorId = (wp && wp.sectorId) || (route && route.destinationSectorId) || (last && last.to);
src\ui\screens\localmap.js:631:  if (!targetSectorId) return null;
src\ui\screens\localmap.js:632:  if (currentSectorId && currentSectorId === targetSectorId) return null;
src\ui\screens\localmap.js:633:  if (first && last && (!currentSectorId || first.from === currentSectorId) && last.to === targetSectorId) {
src\ui\screens\localmap.js:642:    next: 'Plot route to ' + sectorName(targetSectorId),
src\ui\screens\localmap.js:648:  return SECTOR_NAME.get(id) || id || 'target sector';
src\systems\countermeasures.js:6://           a fraction of in-flight missiles targeting the ship toward a decoy cloud (they fly
src\systems\countermeasures.js:17:// data.targetId to a decoy; jams by zeroing data.turnRate (read by weapons._steerHoming). Pure sim
src\systems\countermeasures.js:97:      const player = state.entities.get(state.playerId);
src\systems\countermeasures.js:104:      if (e.type !== 'ship' || !e.alive || e.id === state.playerId) continue;
src\systems\countermeasures.js:112:    // 4. Apply active effects to in-flight missiles. Chaff: divert missiles whose target is the
src\systems\countermeasures.js:131:          // Divert missiles targeting THIS ship to the decoy cloud (a static point behind the ship).
src\systems\countermeasures.js:136:          if (d.targetId === e.id && rng && rng() < cfg.divertPct) {
src\systems\countermeasures.js:137:            d.targetId = cm.effect.decoyId;
src\systems\countermeasures.js:190:    if (e.id === this.state.playerId) {
src\systems\countermeasures.js:196:  // Cheap threat check for AI auto-deploy: is any live missile targeting this ship, or is any ship
src\systems\countermeasures.js:205:      if (d && d.kind === 'missile' && d.targetId === e.id) {
src\ui\screens\automationPanel.js:3:// purchasable catalog). Buy / assign / order buttons emit ui:fleetOrder{shipId,order,targetRef}
src\ui\screens\automationPanel.js:225:      const btn = e.target.closest('button[data-tab]');
src\ui\screens\automationPanel.js:231:      const btn = e.target.closest('button[data-act]');
src\ui\screens\automationPanel.js:236:      const sel = e.target.closest('select[data-act="assignProgram"]');
src\ui\screens\automationPanel.js:327:      next && next.targetRef,
src\ui\screens\automationPanel.js:382:    const actionTarget = next.targetRef != null ? next.targetRef : next.tab;
src\ui\screens\automationPanel.js:667:    // single intent channel into automation: ui:fleetOrder {shipId, order, targetRef} (§4.4).
src\ui\screens\automationPanel.js:668:    // shipId carries the instance id for existing assets; targetRef carries the catalog defId or
src\ui\screens\automationPanel.js:685:    // For purchases/assigns the instance does not exist yet → shipId null, targetRef = defId/index.
src\ui\screens\automationPanel.js:688:    // assignProgram targets an EXISTING drone (shipId = ref) with the templateId as targetRef.
src\ui\screens\automationPanel.js:694:      targetRef: isProgram ? (extra || null) : ref,
src\ui\screens\automationPanel.js:823:        targetRef: droneMk1.id,
src\ui\screens\automationPanel.js:848:          targetRef: trader.id,
src\ui\screens\automationPanel.js:867:          targetRef: outpost.id,
src\ui\screens\automationPanel.js:881:        targetRef: spareShipIndex,
src\ui\screens\automationPanel.js:899:    targetRef: options.targetRef != null ? options.targetRef : tab,
src\ui\hudMeta.js:219:  /* phase readout — general ship-status line, just under the top-center target lock (§3E).
src\ui\hudMeta.js:220:     (Chromeless: stays clear of the now-borderless bottom-right radar/target dock.) */
src\ui\screens\market.js:222:  const target = Math.max(1, Number(mission.objectiveTarget || mission.params.qty || 1) || 1);
src\ui\screens\market.js:224:  const remaining = Math.max(0, target - progress);
src\ui\screens\market.js:231:    target,
src\ui\screens\market.js:249:  return 'Tracked contract cargo is aboard: undock and follow nav guidance to ' + info.destination + '.';
src\ui\screens\market.js:328:    const btn = ev.target.closest('[data-act="route-sell"]');
src\ui\screens\market.js:350:    const btn = ev.target.closest('[data-act]');
src\ui\screens\market.js:443:    const btn = ev.target.closest('[data-act]');
src\ui\screens\market.js:673:        escapeHtml(info.cmdtyName) + ' · hold ' + formatCargoUnits(info.owned) + 'u / target ' + formatCargoUnits(info.remaining) + 'u' +
src\ui\screens\saveLoad.js:224:  return !!(state && state.playerId && state.entities && state.entities.get(state.playerId));
src\systems\dangerModel.js:43:// substeps, so a 4-day catch-up follows the same trajectory as sixteen 0.25-day headless steps.
src\systems\world.js:108:    const e = this.state.entities.get(p.id);
src\systems\world.js:131:   * Despawn the previous sector's scoped entities, spawn the target sector from data, set
src\systems\world.js:206:      if (e.id === state.playerId) continue;          // keep the flyable ship
src\systems\world.js:412:      const player = this.state.entities.get(this.state.playerId);
src\systems\world.js:479:    const player = state.entities.get(state.playerId);
src\systems\world.js:585:      const player = state.entities.get(state.playerId);
src\systems\world.js:587:      this.bus.emit('jump:start', { from: state.world.currentSectorId, to: jump.targetSectorId, via: jump.via, fromPos });
src\systems\world.js:597:    const target = jump.targetSectorId;
src\systems\world.js:600:    const sector = state.world.sectors[target] || SECTOR_BY_ID.get(target);
src\systems\world.js:611:    this.enterSector(target, { fromJump: true, via, fromSectorId });
src\systems\world.js:619:    const player = state.entities.get(state.playerId);
src\systems\world.js:621:    this.bus.emit('jump:arrive', { sectorId: target, interdicted, ambushCount, toPos });
src\systems\world.js:625:    jump.targetSectorId = null;
src\systems\world.js:633:    const player = this.state.entities.get(this.state.playerId);
src\systems\world.js:713:  _onRequestJump({ targetSectorId, via }) {
src\systems\world.js:718:    const target = state.world.sectors[targetSectorId] || SECTOR_BY_ID.get(targetSectorId);
src\systems\world.js:722:    if (!target) return reject('unknown_target');
src\systems\world.js:727:    const isNeighbor = !!(sector && (sector.neighbors || []).includes(targetSectorId));
src\systems\world.js:728:    const isWormhole = !!(sector && sector.wormholeTo && sector.wormholeTo.sectorId === targetSectorId);
src\systems\world.js:739:    const edgeDist = this._edgeDist(sector, target);
src\systems\world.js:745:      const toll = this._gateToll(target);
src\systems\world.js:752:    jump.targetSectorId = targetSectorId;
src\systems\world.js:757:    this.bus.emit('jump:chargeStart', { targetSectorId, via, chargeNeeded });
src\systems\world.js:768:    jump.targetSectorId = null; jump.via = null;
src\systems\world.js:776:  _onRequestRoute({ targetSectorId, mode }) {
src\systems\world.js:777:    const route = this.computeRoute(targetSectorId, mode || 'fuel');
src\systems\world.js:789:  computeRoute(targetSectorId, mode = 'fuel') {
src\systems\world.js:792:    if (!start || !targetSectorId || start === targetSectorId) return null;
src\systems\world.js:815:      if (u === targetSectorId) break;
src\systems\world.js:821:        if (!isDiscovered(v) && v !== targetSectorId) continue; // route only through known space
src\systems\world.js:834:    if (!prev.has(targetSectorId)) return null;
src\systems\world.js:837:    let cur = targetSectorId;
src\systems\world.js:881:    this.bus.emit('scan:completed', { targetId: null });
src\systems\world.js:890:    const player = state.entities.get(state.playerId);
src\systems\world.js:895:      const ent = state.entities.get(p.id);
src\systems\world.js:917:    const player = state.entities.get(state.playerId);
src\systems\world.js:967:    const player = state.entities.get(state.playerId);
src\systems\world.js:988:    const player = this.state.entities.get(this.state.playerId);
src\systems\world.js:1051:        state: state.jump.state, targetSectorId: state.jump.targetSectorId, via: state.jump.via,
src\systems\world.js:1078:        state.jump.state = 'IDLE'; state.jump.targetSectorId = null; state.jump.via = null;
src\systems\world.js:1099:    state.jump.state = 'IDLE'; state.jump.targetSectorId = null; state.jump.via = null;
src\ui\screens\outfitting.js:461:    const cell = ev.target.closest('[data-slot]');
src\ui\screens\outfitting.js:464:    const unfitBtn = ev.target.closest('[data-act="unfit"]');
src\ui\screens\outfitting.js:489:    const cell = ev.target.closest('[data-slot]');
src\ui\screens\outfitting.js:497:    const item = ev.target.closest('[data-inst]');
src\ui\screens\outfitting.js:502:    // target slot: the selected slot if compatible, else the first compatible empty slot.
src\ui\screens\outfitting.js:507:    let target = (selectedSlot != null && fits(slots[selectedSlot], def)) ? selectedSlot : -1;
src\ui\screens\outfitting.js:508:    if (target < 0) target = slots.findIndex((s, i) => !owned.fittings[i] && fits(s, def));
src\ui\screens\outfitting.js:509:    if (target < 0) {
src\ui\screens\outfitting.js:514:    ctx.bus.emit('ui:fitModule', { slotIndex: target, instanceId });
src\ui\screens\outfitting.js:520:    const item = ev.target.closest('[data-inst]');
src\ui\screens\outfitting.js:527:    let target = (selectedSlot != null && fits(slots[selectedSlot], def)) ? selectedSlot : slots.findIndex((s, i) => !owned.fittings[i] && fits(s, def));
src\ui\screens\outfitting.js:528:    if (target >= 0) { previewFit = { slotIndex: target, defId: def.id }; renderPreview(); }
src\ui\screens\outfitting.js:534:    const btn = ev.target.closest('[data-act="buy"]');
src\ui\screens\pause.js:153:      return 'Next: resume, follow tracked nav to ' + dest + ', then dock to resolve the handoff.';
src\ui\screens\pause.js:157:      return 'Next: mine ' + missionCommodity(m) + ', keep cargo room open, then follow the tracker for payout.';
src\ui\screens\pause.js:160:      return 'Next: resume, follow tracked nav, and expect combat before the timer runs down.';
src\ui\screens\pause.js:162:      return 'Next: resume, follow tracked nav, and scan each marked site.';
src\ui\screens\pause.js:164:      return 'Next: resume and follow the tracked objective; Mission Log (' + BINDINGS.missionLog.label + ') has the details.';
src\ui\screens\pause.js:181:  const targetSectorId = wp && wp.sectorId || null;
src\ui\screens\pause.js:183:  const targetSectorName = targetSectorId ? sectorDisplayName(targetSectorId, wp && wp.sectorName) : '';
src\ui\screens\pause.js:184:  if (targetSectorId && !hasLocalFix && (!currentSectorId || targetSectorId !== currentSectorId)) {
src\ui\screens\pause.js:185:    return { kind: 'inter-system', objectiveLabel: 'INTER-SYSTEM ROUTE', targetSectorName };
src\ui\screens\pause.js:187:  if (hasLocalFix || (targetSectorId && currentSectorId && targetSectorId === currentSectorId)) {
src\ui\screens\pause.js:188:    return { kind: 'local', objectiveLabel: 'LOCAL ROUTE', targetSectorName };
src\ui\screens\pause.js:190:  return { kind: 'nav', objectiveLabel: 'NAV SET', targetSectorName: '' };
src\ui\screens\pause.js:198:    const place = commitment.targetSectorName ? ' in ' + commitment.targetSectorName : ' in this system';
src\ui\screens\pause.js:210:  const target = commitment.targetSectorName ? ' to ' + commitment.targetSectorName : '';
src\ui\screens\pause.js:214:    hint: 'Open Star Map (' + BINDINGS.starmap.label + ') to review the inter-system route' + target + ' before committing a jump.',
src\ui\screens\pause.js:221:  if (!mapAction) return 'resume and follow the current marker; open the map if the route gets muddy.';
src\ui\screens\pause.js:228:  return 'resume and follow the current marker; ' + mapAction.hint;
src\ui\screens\pause.js:293:export function pauseExitConfirmBody(state, target = 'menu') {
src\ui\screens\pause.js:295:  const opening = target === 'load'
src\ui\screens\pause.js:298:  const loss = target === 'load'
src\ui\screens\stationHub.js:261:  if (chip.targetScreen) {
src\ui\screens\stationHub.js:262:    const screenLabel = DEPARTURE_SCREEN_LABELS[chip.targetScreen] || 'screen';
src\ui\screens\stationHub.js:265:  if (!chip.targetTab) return '';
src\ui\screens\stationHub.js:266:  const tab = TABS.find((t) => t.id === chip.targetTab);
src\ui\screens\stationHub.js:276:  const targetAttr = chip.targetScreen
src\ui\screens\stationHub.js:277:    ? ' data-departure-screen="' + escapeHtml(chip.targetScreen) + '"'
src\ui\screens\stationHub.js:278:    : (chip.targetTab ? ' data-departure-tab="' + escapeHtml(chip.targetTab) + '"' : '');
src\ui\screens\stationHub.js:279:  if (!targetAttr) return '<span class="' + cls + '">' + body + '</span>';
src\ui\screens\stationHub.js:281:  return '<button type="button" class="' + cls + '"' + targetAttr +
src\ui\screens\stationHub.js:288:  return state && state.entities && state.entities.get && state.playerId != null
src\ui\screens\stationHub.js:289:    ? state.entities.get(state.playerId)
src\ui\screens\stationHub.js:312:      targetTab: 'market',
src\ui\screens\stationHub.js:321:      targetTab: 'market',
src\ui\screens\stationHub.js:329:    targetTab: 'market',
src\ui\screens\stationHub.js:344:      targetScreen: 'missionLog',
src\ui\screens\stationHub.js:354:      targetScreen: 'missionLog',
src\ui\screens\stationHub.js:367:      targetScreen: 'missionLog',
src\ui\screens\stationHub.js:375:    targetTab: 'missions',
src\ui\screens\stationHub.js:393:    targetTab: 'market',
src\ui\screens\stationHub.js:409:    targetTab: 'services',
src\ui\screens\stationHub.js:423:    targetTab: 'services',
src\ui\screens\stationHub.js:485:  if (issue && issue.targetTab) return issue.targetTab;
src\ui\screens\stationHub.js:507:      targetTab: 'market',
src\ui\screens\stationHub.js:518:      targetTab: 'missions',
src\ui\screens\stationHub.js:529:      targetTab: firstDockDepartureTarget(departureChips),
src\ui\screens\stationHub.js:541:  if (!step.targetTab) return '<span class="' + cls + '">' + body + '</span>';
src\ui\screens\stationHub.js:542:  return '<button type="button" class="' + cls + '" data-handoff-tab="' + escapeHtml(step.targetTab) + '"' +
src\ui\screens\stationHub.js:543:    ' title="' + escapeHtml('Open ' + tabLabel(step.targetTab) + ': ' + step.title) + '"' +
src\ui\screens\stationHub.js:544:    ' aria-label="' + escapeHtml('Open ' + tabLabel(step.targetTab) + ': ' + step.title + '. ' + step.text) + '">' +
src\ui\screens\stationHub.js:722:      const target = ev.target.closest('[data-handoff-tab]');
src\ui\screens\stationHub.js:723:      if (!target || !this._handoffEl || !this._handoffEl.contains(target)) return;
src\ui\screens\stationHub.js:724:      const tabId = target.getAttribute('data-handoff-tab');
src\ui\screens\stationHub.js:738:      const chip = ev.target.closest('[data-departure-tab],[data-departure-screen]');
src\ui\screens\stationHub.js:787:      const b = ev.target.closest('[data-tab]');
src\ui\screens\stationHub.js:854:      const btn = ev.target.closest('[data-mid]');
src\ui\screens\stationHub.js:1045:    const currentButton = ev.target && ev.target.closest && ev.target.closest('[role="tab"][data-tab]');
src\ui\screens\stationHub.js:1365:  return 'the target area';
src\ui\screens\stationHub.js:1451:      return 'Next: accept, undock to an asteroid field, mine the quota, then follow the tracked objective.';
src\ui\screens\stationHub.js:1456:      return 'Next: accept, undock, follow the tracked nav, and be ready to fight.';
src\ui\screens\stationHub.js:1458:      return 'Next: accept, undock, follow tracked nav, and scan the marked sites.';
src\ui\screens\stationHub.js:1460:      return 'Next: accept to auto-track it, undock, then follow nav guidance toward ' + dest + '.';
src\ui\screens\stationHub.js:1468:      return 'Undock to an asteroid field, mine the quota, then follow the tracker back for payout.';
src\ui\screens\stationHub.js:1473:      return 'Undock, follow tracked nav, and be ready to fight before the timer runs down.';
src\ui\screens\stationHub.js:1475:      return 'Undock, follow tracked nav, and scan each marked site before returning.';
src\ui\screens\stationHub.js:1481:      return 'Undock, follow nav guidance toward ' + dest + ', then dock to resolve the handoff.';
src\ui\screens\stationHub.js:1483:      return 'Undock, follow the tracked objective, and check Mission Log (' + BINDINGS.missionLog.label + ') for progress.';
src\systems\combat.js:215:    this.kernel = getCombatKernel(ctx, { onKill: (target, killerId) => this.kill(target, killerId) });
src\systems\combat.js:228:  onHit({ targetId, ownerId, damage, damageType, pos, penetration = 0, impulse = null, heat = 0, statuses = [], damagePacket = null, packet = null, weaponId = null, origin = null }) {
src\systems\combat.js:232:      targetId,
src\systems\combat.js:240:    if (result.ok && targetId === this.state.playerId) {
src\systems\combat.js:254:    }, { onKill: (target, killerId) => this.kill(target, killerId) });
src\systems\combat.js:260:    if (t.id === state.playerId) {
src\systems\combat.js:278:    const killedByPlayer = killerId === state.playerId;
src\systems\combat.js:361:      const entity = this.state.entities.get(station.id);
src\systems\combat.js:435:      const owner = state.entities.get(beam.ownerId);
src\systems\combat.js:452:          targetId: bestE.id,
src\ui\screenManager.js:285:  // dismissing it must use the undock route so docked state, HUD state, and autosave all follow the
src\ui\screens\help.js:118:    ['Deep-drill (ant-farm)', null, `${BINDINGS.drill.label} (target an asteroid)`],
src\ui\screens\help.js:120:    ['Cycle target', null, 'Tab'],
src\ui\screens\help.js:141:    ['Cycle target', null, 'X / □'],
src\systems\automation.js:72:const DRONE_SPEED = 130;              // wu/s cruise toward the targeted asteroid
src\systems\automation.js:317:    const getEnt = (this.helpers && this.helpers.getEntity) || ((id) => this.state.entities.get(id));
src\systems\automation.js:318:    const target = { x: beacon.x, z: beacon.z };
src\systems\automation.js:324:      this._driveDrone(e, target, dt, false);
src\systems\automation.js:327:    // arrival threshold scales with target type (rocks need a standoff; stations/depot closer)
src\systems\automation.js:331:    const dx = lead.pos.x - target.x, dz = lead.pos.z - target.z;
src\systems\automation.js:393:        data: { kind: 'mining_drone', groupId: g.id, targetAstId: null },
src\systems\automation.js:413:    const getEnt = (this.helpers && this.helpers.getEntity) || ((id) => this.state.entities.get(id));
src\systems\automation.js:422:      let ast = e.data && e.data.targetAstId != null ? getEnt(e.data.targetAstId) : null;
src\systems\automation.js:425:        e.data.targetAstId = ast ? ast.id : null;
src\systems\automation.js:452:  _driveDrone(e, target, dt, brake) {
src\systems\automation.js:453:    const dx = target.x - e.pos.x, dz = target.z - e.pos.z;
src\systems\automation.js:514:      || (this.state.entities && this.state.entities.get(this.state.playerId));
src\systems\automation.js:522:    const getEnt = (this.helpers && this.helpers.getEntity) || ((id) => this.state.entities.get(id));
src\systems\automation.js:530:    const getEnt = (this.helpers && this.helpers.getEntity) || ((id) => this.state.entities.get(id));
src\systems\automation.js:872:  // UI INTENT HANDLER — ui:fleetOrder {shipId, order, targetRef, kind}. The panel multiplexes
src\systems\automation.js:873:  // every action through `order`. Purchases carry targetRef=defId and shipId=null; asset orders
src\systems\automation.js:874:  // carry shipId=instanceId (or, for assignFleet, targetRef=owned-ship index).
src\systems\automation.js:879:      case 'buyDrone': return this.buyDrone(p.targetRef);
src\systems\automation.js:881:      case 'hireTrader': return this.hireTrader(p.targetRef);
src\systems\automation.js:884:      case 'buildOutpost': return this.buildOutpost(p.targetRef);
src\systems\automation.js:886:      case 'assignFleet': return this.assignFleet(p.targetRef);
src\systems\automation.js:887:      case 'orderEscort': return this.setFleetOrder(p.shipId, 'escort', p.targetRef);
src\systems\automation.js:888:      case 'orderMine': return this.setFleetOrder(p.shipId, 'mine', p.targetRef);
src\systems\automation.js:889:      case 'orderRecall': return this.setFleetOrder(p.shipId, 'idle', p.targetRef);
src\systems\automation.js:890:      // V2 §4 / cut-list #28: assign an alphabet template to a drone group (program it). targetRef
src\systems\automation.js:892:      case 'assignProgram': return this.assignProgram(p.shipId, p.targetRef);
src\systems\automation.js:1055:      name: ship.customName || null, order: 'escort', targetRef: null,
src\systems\automation.js:1063:  setFleetOrder(id, order, targetRef) {
src\systems\automation.js:1067:    fs.targetRef = targetRef != null ? { kind: 'ref', refId: targetRef } : null;
src\systems\automation.js:1082:  // The UI/system exposes fleet orders as 'escort' with a targetRef; treat that as guarding.
src\systems\automation.js:1087:      if ((fs.order === 'guard' || fs.order === 'escort') && fs.targetRef && fs.targetRef.refId == assetId) n++;
src\ui\screens\shipyard.js:404:    const btn = ev.target.closest('[data-act]');
src\ui\screens\shipyard.js:433:    const btn = ev.target.closest('[data-act="buy"]');
src\ui\screens\shipyard.js:452:    const row = ev.target.closest('[data-ship]');
src\ui\screens\starmap.js:173:function sectorName(id) { return SECTOR_NAME.get(id) || id || 'target sector'; }
src\ui\screens\starmap.js:200:  const target = Math.max(1, Number(mission.objectiveTarget) || 1);
src\ui\screens\starmap.js:201:  if (mission.type === 'mining_quota') return `Mine ${progress}/${target} units`;
src\ui\screens\starmap.js:202:  if (mission.type === 'bulk_trade') return `Sell ${progress}/${target} units`;
src\ui\screens\starmap.js:203:  if (mission.type === 'patrol_clear') return `Clear ${progress}/${target} hostiles`;
src\ui\screens\starmap.js:204:  if (mission.type === 'recon_scan') return `Scan ${progress}/${target} sites`;
src\ui\screens\starmap.js:205:  return mission.objectiveProgress ? `${progress}/${target}` : 'Proceed to the objective';
src\ui\screens\starmap.js:269:  const targetSectorId = objective.sectorId;
src\ui\screens\starmap.js:270:  if (!targetSectorId && objective.hasLocalFix) {
src\ui\screens\starmap.js:280:  if (!targetSectorId) {
src\ui\screens\starmap.js:294:  if (currentSectorId && currentSectorId === targetSectorId) {
src\ui\screens\starmap.js:297:      next: `In ${nameOf(targetSectorId)}`,
src\ui\screens\starmap.js:304:  if (first && last && (!currentSectorId || first.from === currentSectorId) && last.to === targetSectorId) {
src\ui\screens\starmap.js:317:    next: `Plot route to ${nameOf(targetSectorId)}`,
src\ui\screens\starmap.js:319:    canPlot: !!targetSectorId,
src\ui\screens\starmap.js:359:  let target = clamp(t, 0, 1) * total;
src\ui\screens\starmap.js:361:    if (target <= lengths[i] || i === lengths.length - 1) return pointOnLine(points[i], points[i + 1], lengths[i] ? target / lengths[i] : 0);
src\ui\screens\starmap.js:362:    target -= lengths[i];
src\ui\screens\starmap.js:451:      const button = e.target.closest('button[data-act]');
src\ui\screens\starmap.js:455:      const button = e.target.closest('button[data-act]');
src\ui\screens\starmap.js:1048:      const targetSectorId = objective && objective.sectorId;
src\ui\screens\starmap.js:1049:      if (!targetSectorId) {
src\ui\screens\starmap.js:1054:        sectorId: targetSectorId,
src\ui\screens\starmap.js:1061:      this._ctx.bus.emit('world:requestRoute', { targetSectorId, mode: 'fuel' });
src\ui\screens\starmap.js:1063:      if (this._isDiscovered(targetSectorId)) this._selectedId = targetSectorId;
src\ui\screens\starmap.js:1064:      this._ctx.bus.emit('toast', { text: `Plotting objective route to ${this._nameOf(targetSectorId)}`, kind: 'info', ttl: 3000 });
src\ui\screens\starmap.js:1070:    const target = this._selectedId;
src\ui\screens\starmap.js:1071:    if (!target) return;
src\ui\screens\starmap.js:1074:      bus.emit('world:requestJump', { targetSectorId: target, via: 'gate' });
src\ui\screens\starmap.js:1075:      bus.emit('ui:setCourse', { sectorId: target, path: null });
src\ui\screens\starmap.js:1076:      bus.emit('toast', { text: `Course set: ${this._nameOf(target)}`, kind: 'info', ttl: 3000 });
src\ui\screens\starmap.js:1078:      bus.emit('world:requestRoute', { targetSectorId: target, mode: 'fuel' });
src\ui\screens\starmap.js:1079:      bus.emit('ui:setCourse', { sectorId: target, path: null });
src\ui\screens\starmap.js:1080:      bus.emit('toast', { text: `Plotting route to ${this._nameOf(target)}…`, kind: 'info', ttl: 3000 });
src\ui\screens\starmap.js:1104:    const player = this._ctx.state.entities && this._ctx.state.entities.get && this._ctx.state.entities.get(this._ctx.state.playerId);
src\systems\actions.js:10:  update(dt) {
src\systems\wingmen.js:10:// targets automatically). The fleet order (escort/guard/attack) maps to an AI archetype + intent.
src\systems\wingmen.js:38:    // The UI emits ui:fleetOrder {shipId, order, kind, targetRef}; automation.handleOrder resolves
src\systems\wingmen.js:55:      const e = state.entities.get(fs._liveId);
src\systems\wingmen.js:75:    const player = state.entities.get(state.playerId);
src\systems\wingmen.js:98:        const e = state.entities.get(fs._liveId);
src\systems\wingmen.js:113:      team: 0,                  // player-aligned — the AI auto-targets team-1 hostiles
src\systems\wingmen.js:138:    const e = state.entities.get(fs._liveId);
src\ui\radar.js:4:// the current target gets a ring. Canvas is DPI-scaled so blips stay crisp on 4K/Retina.
src\ui\radar.js:132:  // Thin dashed tether from the player (center) to the current target (Tactical-Visor §3D).
src\ui\radar.js:248:    if (!contactsDirty && cachedEntityList === list && cachedLength === list.length && cachedPlayerId === state.playerId) {
src\ui\radar.js:259:    cachedEntityList = list; cachedLength = list.length; cachedPlayerId = state.playerId;
src\ui\radar.js:299:    const p = state.entities.get(state.playerId);
src\ui\radar.js:358:    const targetId   = state.player.targetId;
src\ui\radar.js:388:    let targetAsteroidBlip = false, targetAsteroidX = 0, targetAsteroidY = 0;
src\ui\radar.js:394:      if (!e.alive || e === p || e.type !== 'asteroid' || state.entities.get(e.id) !== e) continue;
src\ui\radar.js:401:      if (e.id === targetId) {
src\ui\radar.js:402:        targetAsteroidBlip = true;
src\ui\radar.js:403:        targetAsteroidX = bx;
src\ui\radar.js:404:        targetAsteroidY = by;
src\ui\radar.js:408:    if (targetAsteroidBlip) drawTargetRing(g, targetAsteroidX, targetAsteroidY, C);
src\ui\radar.js:486:      // target ring
src\ui\radar.js:487:      if (e.id === targetId) {
src\ui\radar.js:493:    // Shows where to aim to hit the locked target, accounting for both ships' velocity. This is the
src\ui\radar.js:494:    // target-centric combat cue that makes maneuvering matter more than pixel-perfect free aim.
src\ui\radar.js:495:    if (targetId) {
src\ui\radar.js:496:      const tgt = state.entities.get(targetId);
src\ui\radar.js:516:          // Small crosshair + tick line from the target ring to the lead point.
src\systems\claims.js:136:    this.bus.emit('claim:teleportRequest', { bodyId, targetStationId: body.linkedStationId });
src\ui\screens\settings.js:131:  { label: 'Drill / asteroid base', key: BINDINGS.drill.label, note: 'target asteroid first' },
src\ui\screens\settings.js:247:      // HDR radiance into the bloom target. On by default for the beautiful flight look.
src\ui\screens\settings.js:337:    pane.appendChild(el('p', 'sf-muted', 'Default layout: left stick fly, right stick aim, RT fire, RB boost, LB brake, R3 countermeasure, X target, View star map, Y codex, Start pause/log.'));
src\ui\screens\settings.js:392:    pane.appendChild(el('p', 'sf-muted', 'Flight keys above are rebindable here; these interface shortcuts follow the shared binding registry.'));
src\ui\screens\settings.js:480:    const onClickAway = (ev) => { if (ev.target !== btn) done(false); };
src\systems\aiEncounter.js:232:  const player = state && state.entities && state.entities.get ? state.entities.get(state.playerId) : null;
src\systems\presentationAdapters.js:125:      targetId: cue.targetId ?? null,
src\systems\presentationAdapters.js:147:      targetId: cue.targetId ?? null,
src\systems\weapons.js:66:    const player = this.helpers.getEntity(state.playerId);
src\systems\weapons.js:85:      if (e.type !== 'ship' || !e.alive || e.id === state.playerId) continue;
src\systems\weapons.js:140:      // lock decays when target leaves the cone / is gone
src\systems\weapons.js:160:      const tgt = d.targetId != null ? this.helpers.getEntity(d.targetId) : null;
src\systems\weapons.js:183:  // forceTarget: an explicit target entity (auto-fire / missile-lock); null = use ship's selected target.
src\systems\weapons.js:263:    // Targeting: missiles/turrets need a target (the forced auto-fire target, else the ship's selected).
src\systems\weapons.js:265:    // Player turret with no selected target: synthesize a point-target along the aim direction at
src\systems\weapons.js:267:    // there; a turret should too). Missiles still require a real lockable target.
src\systems\weapons.js:344:      data.targetId = tgt ? tgt.id : null;
src\systems\weapons.js:372:  // Current target for a ship: explicit combat.targetId, else player's selected target.
src\systems\weapons.js:375:    let id = combat && combat.targetId != null ? combat.targetId : null;
src\systems\weapons.js:376:    if (id == null && e.id === this.state.playerId) id = this.state.player.targetId;
src\systems\weapons.js:437:  // Auto-fire target: the nearest ship that is ACTIVELY hostile toward the player — either on a
src\systems\weapons.js:438:  // hostile team and in an attack FSM state, or currently targeting/attacking the player. This
src\systems\weapons.js:450:      if (e.team === player.team) continue;              // friendly — never auto-target allies
src\systems\weapons.js:462:  // selected/locked target, OR it recently damaged the player. Passive traders/patrols are skipped.
src\systems\weapons.js:466:      // Passive freighters (ambient traffic, V2 §28b) are NEVER auto-targeted — they're scenery +
src\systems\weapons.js:477:    if (combat && combat.targetId === player.id) return true;
src\systems\weapons.js:541:    // Subsystem-targeting / shield-bypass verbs (EMP disable, spec §9). Authored on the weapon def;
src\main.js:152:  state.playerId = player.id;
src\main.js:175:  state.entities.clear(); state.entityList.length = 0; state.freeIds.length = 0; state.nextEntityId = 1; state.playerId = 0;
src\main.js:325:  const cameraObj = state.camera && state.camera.obj;
src\main.js:326:  const cameraFocus = state.camera && state.camera.focus;
src\main.js:327:  const cameraShake = state.camera && state.camera.shakeOffset;
src\main.js:337:  Object.assign(state.camera, fresh.camera, {
src\main.js:342:  if (state.camera.focus && typeof state.camera.focus.set === 'function') state.camera.focus.set(0, 0, 0);
src\main.js:343:  if (state.camera.shakeOffset && typeof state.camera.shakeOffset.set === 'function') state.camera.shakeOffset.set(0, 0, 0);
src\systems\alphabet.js:6://   MOVE     — to a beacon / entity / sector (named targets, never raw coords)
src\systems\alphabet.js:8://   INTERACT — contextual verb from target: sell / buy / load / unload / dock / repair / build
src\systems\alphabet.js:9://   GUARD    — follow an entity, engage hostiles (how escorts work — allied NPC, no new engine)
src\systems\alphabet.js:34:// `target` is a beacon name (resolved at runtime to a world position/station); '*' = current/named.
src\systems\alphabet.js:40:      { op: P.MOVE, target: 'depot' },
src\systems\alphabet.js:42:      { op: P.MOVE, target: 'field' },
src\systems\alphabet.js:48:      { op: P.GUARD, target: 'player' },
src\systems\alphabet.js:54:      { op: P.MOVE, target: 'scout' },
src\systems\alphabet.js:56:      { op: P.MOVE, target: 'depot' },
src\systems\alphabet.js:70:    const e = state.entities.get(state.playerId);
src\systems\alphabet.js:75:    const p = state.entities.get(state.playerId);
src\systems\alphabet.js:90:    // nearest live asteroid — the MINE target
src\systems\alphabet.js:92:    const anchor = g.originPos || (state.entities.get(state.playerId) || {}).pos || { x: 0, z: 0 };
src\systems\alphabet.js:105:  const p = state.entities.get(state.playerId);
src\systems\alphabet.js:124:  const beacon = step.target ? resolveBeacon(step.target, ctx) : null;
src\systems\alphabet.js:157:      // follow the target (player); engagement is the existing fleet-escort path.
src\presentation\cueSchema.js:36:    targetId: { anyOf: [{ type: 'string' }, { type: 'integer' }, { type: 'null' }] },
src\presentation\cueSchema.js:57:  if (!state || id == null || !state.entities || typeof state.entities.get !== 'function') return null;
src\presentation\cueSchema.js:58:  const entity = state.entities.get(id);
src\presentation\cueSchema.js:67:  const id = raw && (raw.targetId ?? raw.sourceId ?? raw.attackerId ?? raw.ownerId ?? raw.id);
src\presentation\cueSchema.js:89:  const player = state && state.entities && typeof state.entities.get === 'function'
src\presentation\cueSchema.js:90:    ? state.entities.get(state.playerId)
src\presentation\cueSchema.js:111:  const playerId = state && state.playerId;
src\presentation\cueSchema.js:112:  if (playerId != null) {
src\presentation\cueSchema.js:113:    const targetId = raw && (raw.targetId ?? raw.combatantId ?? raw.id);
src\presentation\cueSchema.js:115:    if (targetId === playerId) return 1;
src\presentation\cueSchema.js:116:    if (sourceId === playerId) return 0.88;
src\presentation\cueSchema.js:134:    stablePart(event.targetId),
src\presentation\cueSchema.js:173:  const targetId = raw.targetId ?? raw.combatantId ?? (id === 'combat.kill' ? raw.id : null);
src\presentation\cueSchema.js:188:    targetId,
src\systems\ai.js:4:// Default hostility: team 1 (NPC) targets the player (team 0). Behaviour varies by
src\systems\ai.js:47:const REPATH_LOSE_S = 3.0;          // target out of sensor for this long -> drop to patrol
src\systems\ai.js:48:const RETARGET_INTERVAL = 0.4;      // seconds between (expensive) target re-selection
src\systems\ai.js:49:const FIRE_CONE = 0.30;             // rad half-angle: only fire when aim is within this of target
src\systems\ai.js:73:    this._targetScratch = [];
src\systems\ai.js:77:    // Threat tables live on state.combat (already allocated in gameState). Map<targetId, Map<attackerId, threat>>.
src\systems\ai.js:84:      if (!p || p.targetId == null || p.attackerId == null) return;
src\systems\ai.js:85:      if (p.attackerId === p.targetId) return;
src\systems\ai.js:86:      this._addThreat(p.targetId, p.attackerId, Math.max(1, p.amount || 1));
src\systems\ai.js:89:    // Clean up threat + target references when an entity leaves the world.
src\systems\ai.js:102:    const player = state.entities.get(state.playerId) || null;
src\systems\ai.js:136:      if (e.id === state.playerId) continue;
src\systems\ai.js:156:    if (ai._t == null) ai._t = 0;                 // time accumulator (for retarget cadence)
src\systems\ai.js:157:    if (ai._lostT == null) ai._lostT = 0;         // time since target last seen
src\systems\ai.js:158:    if (ai._retarget == null) ai._retarget = 0;
src\systems\ai.js:182:    // --- acquire / refresh target ---
src\systems\ai.js:183:    ai._retarget -= dt;
src\systems\ai.js:184:    let target = data.combat && data.combat.targetId != null
src\systems\ai.js:185:      ? state.entities.get(data.combat.targetId) : null;
src\systems\ai.js:186:    if (!target || !target.alive) target = null;
src\systems\ai.js:187:    if (ai._retarget <= 0 || !target) {
src\systems\ai.js:188:      ai._retarget = RETARGET_INTERVAL;
src\systems\ai.js:189:      target = this._selectTarget(e, data, state, player, arch);
src\systems\ai.js:190:      if (!data.combat) data.combat = { targetId: null, lockTarget: null, lockProgress: 0 };
src\systems\ai.js:191:      data.combat.targetId = target ? target.id : null;
src\systems\ai.js:194:    // Distance/relative bookkeeping to the current target.
src\systems\ai.js:196:    if (target) {
src\systems\ai.js:197:      dx = target.pos.x - e.pos.x; dz = target.pos.z - e.pos.z;
src\systems\ai.js:200:      predAng = this._leadAngle(e, target, this._projSpeed(data));
src\systems\ai.js:210:    const wantFlee = arch.alwaysFlee || (fleeFrac > 0 && hullFrac < fleeFrac && target != null);
src\systems\ai.js:215:    } else if (!target || ai._lostT > REPATH_LOSE_S) {
src\systems\ai.js:223:    next = this._resolveAttackTelegraph(e, data, ai, state, target, next);
src\systems\ai.js:240:    intent.aimAngle = target ? predAng : e.rot;
src\systems\ai.js:254:        this._steerHold(e, intent, target, arch, dist, dx, dz, predAng);
src\systems\ai.js:264:        if (target) intent.aimAngle = Math.atan2(-dz, -dx);
src\systems\ai.js:274:  _resolveAttackTelegraph(e, data, ai, state, target, next) {
src\systems\ai.js:275:    if (!isAttackState(next) || !target || !this._hasUsableWeapons(data)) {
src\systems\ai.js:285:    if (!pending || pending.targetId !== target.id || pending.state !== next) {
src\systems\ai.js:290:        targetId: target.id,
src\systems\ai.js:332:      if (!e || e.type !== 'ship' || !e.alive || e.id === state.playerId) continue;
src\systems\ai.js:344:      const leader = state.entities && state.entities.get ? state.entities.get(rec.leaderId) : null;
src\systems\ai.js:367:      const leader = state.entities && state.entities.get ? state.entities.get(rec.leaderId) : null;
src\systems\ai.js:486:  // ---- target selection (aggro/threat) -----------------------------------
src\systems\ai.js:502:      if (cand.id === state.playerId) score += 20;     // bias toward the player (§ AGGRO TARGET SELECT)
src\systems\ai.js:509:      const near = this.helpers.queryRadius(e.pos, arch.sensor, this._targetScratch || (this._targetScratch = []));
src\systems\ai.js:551:  _steerHold(e, intent, target, arch, dist, dx, dz, predAng) {
src\systems\ai.js:558:      // creep / strafe a little to stay a moving target
src\systems\ai.js:563:  // Circle-strafe: blend tangential motion (perpendicular to target) with a radial term that
src\systems\ai.js:621:  _addThreat(targetId, attackerId, amount) {
src\systems\ai.js:622:    let tbl = this._threat.get(targetId);
src\systems\ai.js:623:    if (!tbl) { tbl = new Map(); this._threat.set(targetId, tbl); }
src\systems\ai.js:675:  // of speed `p` fired now hits the moving target. Falls back to direct aim if uncatchable.
src\systems\ai.js:676:  _leadAngle(shooter, target, p) {
src\systems\ai.js:677:    const rx = target.pos.x - shooter.pos.x, rz = target.pos.z - shooter.pos.z;
src\systems\ai.js:678:    const rvx = target.vel.x - shooter.vel.x, rvz = target.vel.z - shooter.vel.z;
src\systems\heat.js:76:    if (!p || p.killerId !== this.state.playerId) return;
src\systems\heat.js:90:    if (!p || p.attackerId !== this.state.playerId) return; // only the player's own attacks
src\render\camera.js:1:// Tilted top-down chase camera (ARCHITECTURE §0.14): follows player POSITION only (never yaw,
src\render\camera.js:2:// anti-nausea), with damped follow, velocity look-ahead, aim bias, and trauma-based shake.
src\render\camera.js:4:// dynamic without violating the no-yaw-follow rule (we never rotate the camera's heading).
src\render\camera.js:82:  // Combat composes player + nearest threat instead of only following the player.
src\render\camera.js:128:  if (!attachments || !state.entities || typeof state.entities.get !== 'function') return null;
src\render\camera.js:136:    if (attachment.ownerId === player.id) otherId = attachment.targetId;
src\render\camera.js:137:    else if (attachment.targetId === player.id) otherId = attachment.ownerId;
src\render\camera.js:140:    const other = state.entities.get(otherId);
src\render\camera.js:156:  const c = state.camera;
src\render\camera.js:192:  // over the duration, multiplying targetZoom during its active window. 0 = inactive. This cooperates
src\render\camera.js:222:      const p = state.entities.get(state.playerId);
src\render\camera.js:235:    follow(dt) {
src\render\camera.js:237:      const p = state.entities.get(state.playerId);
src\render\camera.js:277:      const followLerp = finiteOr(c.lerp, 6);
src\render\camera.js:278:      c.focus.x = damp(c.focus.x, fx, followLerp, frameDt);
src\render\camera.js:279:      c.focus.z = damp(c.focus.z, fz, followLerp, frameDt);
src\render\camera.js:283:      let targetZoom = baseZoom;
src\render\camera.js:291:        // making fights feel detached; 47-A needs pressure, line tension, and target geometry.
src\render\camera.js:304:          if (p.flags && p.flags.boosting) zoomFactor = Math.max(zoomFactor, 1.06);
src\render\camera.js:313:        targetZoom = baseZoom * zoomFactor;
src\render\camera.js:316:      // decays. Applied to targetZoom so it eases through the same _dynamicZoom damping as everything
src\render\camera.js:319:        targetZoom *= (1 + _pushZoom);
src\render\camera.js:323:      _dynamicZoom = damp(_dynamicZoom, targetZoom, ZOOM_LERP, frameDt);
src\render\camera.js:354:      // we never clobber the heading (safe with the no-yaw-follow rule).
src\render\camera.js:355:      const targetRoll = Math.max(-ROLL_MAX, Math.min(ROLL_MAX, bankForLean));
src\render\camera.js:356:      camRoll = damp(camRoll, targetRoll, ROLL_LERP, frameDt);
src\systems\traffic.js:86:    // live freighter records: id -> {targetId, waitT, nextTradeT}
src\systems\traffic.js:133:        targetId: this._pickStation(stations).id,
src\systems\traffic.js:164:      for (const id of this._active) { const e = this.state.entities.get(id); if (e) e.alive = false; }
src\systems\traffic.js:182:      const e = state.entities.get(rec.id);
src\systems\traffic.js:192:      // resolve current target (it may have despawned)
src\systems\traffic.js:193:      let target = state.entities.get(rec.targetId);
src\systems\traffic.js:194:      if (!target || !target.alive) {
src\systems\traffic.js:195:        target = this._pickStation(stations);
src\systems\traffic.js:196:        rec.targetId = target ? target.id : null;
src\systems\traffic.js:197:        if (!target) continue;
src\systems\traffic.js:207:      // fly toward target
src\systems\traffic.js:208:      const dx = target.pos.x - e.pos.x;
src\systems\traffic.js:209:      const dz = target.pos.z - e.pos.z;
src\systems\traffic.js:216:          this._emitTrade(target);
src\systems\traffic.js:220:        rec.targetId = this._pickStation(stations).id;
src\systems\traffic.js:224:      // drive: face the target, thrust forward. moveZ=1 means forward along the nose.
src\systems\traffic.js:243:  // Pirates/raiders flee from the nearest hostile (the player) — they raid weak targets but bolt
src\systems\traffic.js:246:    const player = state.entities.get(state.playerId);
src\systems\traffic.js:265:  // haulers: their target is an asteroid, not a station, until they return to dock.
src\systems\traffic.js:269:      const tgt = state.entities.get(rec.targetId);
src\systems\traffic.js:272:        if (dist < DOCK_RANGE) { rec.carrying = false; rec.targetId = this._pickAsteroid(state) || this._pickStation(stations).id; rec.waitT = 2; setIntent(e, 0, 0, false, false, null, e.rot); return; }
src\systems\traffic.js:275:      rec.targetId = this._pickStation(stations).id; return;
src\systems\traffic.js:277:    let rock = state.entities.get(rec.targetId);
src\systems\traffic.js:278:    if (!rock || rock.type !== 'asteroid' || !rock.alive) { rec.targetId = this._pickAsteroid(state) || this._pickStation(stations).id; rock = state.entities.get(rec.targetId); }
src\systems\traffic.js:281:    if (dist < 40) { rec.carrying = true; rec.targetId = this._pickStation(stations).id; rec.waitT = 1.5; setIntent(e, 0, 0, false, false, null, e.rot); return; }
src\systems\traffic.js:315:      const w = state.entities.get(r.id);
src\render\assetLoader.js:303:      if (node.morphTargetInfluences && node.morphTargetInfluences.length) errors.push(`${label(node)} uses morph targets; bind game-state hooks instead`);
src\systems\onboarding.js:31:// follow a suspicious mass signal, verify the Kestrel's tools, dock, and choose who gets the answer.
src\systems\onboarding.js:34:  { key: 'claim', title: 'Reach the 47-A mass signal', target: 'asteroid', range: 420,
src\systems\onboarding.js:36:  { key: 'mine', title: 'Verify the signal and live tools', target: 'asteroid', qty: 3,
src\systems\onboarding.js:38:  { key: 'dock', title: 'Dock at Helios Station', target: 'station',
src\systems\onboarding.js:279:    if (p.collectorId != null && p.collectorId !== this.state.playerId) return;
src\systems\onboarding.js:392:  // relevant keys. Modes: 'mining' (beam active), 'combat' (hostile targeted or taking fire),
src\systems\onboarding.js:409:    // Check for hostile target or incoming fire (combat takes priority over mining).
src\systems\onboarding.js:410:    const tid = state.player.targetId;
src\systems\onboarding.js:412:      const target = state.entities.get(tid);
src\systems\onboarding.js:413:      if (target && target.alive && target.team != null) {
src\systems\onboarding.js:414:        const player = state.entities.get(state.playerId);
src\systems\onboarding.js:415:        if (player && target.team !== player.team) mode = 'combat';
src\systems\onboarding.js:443:    const p = this.state.entities.get(this.state.playerId);
src\systems\onboarding.js:458:    if (!curr || !curr.target) {
src\systems\onboarding.js:477:    const p = this.state.entities.get(this.state.playerId);
src\systems\onboarding.js:478:    if (!step || !step.target || !p) return null;
src\systems\onboarding.js:481:    const list = step.target === 'asteroid'
src\systems\onboarding.js:486:      if (step.target === 'asteroid') {
src\systems\onboarding.js:488:      } else if (step.target === 'station') {
src\systems\onboarding.js:495:      if (step.target === 'station' && e.data && e.data.stationId === 'station_helios') d -= 1000000;
src\systems\onboarding.js:499:    if (step.target === 'station') {
src\save\saveSystem.js:41:const TRANSIENT_ENTITY_FLAGS = new Set(['boosting', 'noInterp']);
src\save\saveSystem.js:223:      if (e.id !== state.playerId && !(e.flags && e.flags.persistent)) continue;
src\save\saveSystem.js:224:      out.push(plainEntity(e, e.id === state.playerId));
src\save\saveSystem.js:244:    return !!(state && state.playerId && state.entities && state.entities.get(state.playerId));
src\save\saveSystem.js:481:      // listeners must not fail or spawn targets for missions from the pre-load game.
src\save\saveSystem.js:525:      // 11. clear stale entity-id references (the saved targets belong to entities that no longer exist).
src\save\saveSystem.js:536:        try { missionsSys.spawnTargetsForSector(sectorId); } catch (err) { console.error('[save] spawn mission targets', err); }
src\save\saveSystem.js:669:      if (ref.kind === 'player') return state.playerId || null;
src\save\saveSystem.js:729:    state.playerId = 0;
src\save\saveSystem.js:747:    state.playerId = e.id;
src\save\saveSystem.js:772:    const e = this.helpers.getEntity(this.state.playerId);
src\save\saveSystem.js:782:    const e = this.helpers.getEntity(this.state.playerId);
src\save\saveSystem.js:791:  // Saved target ids point at NPCs that get fresh ids when the sector regenerates → null them.
src\save\saveSystem.js:794:    state.player.targetId = null;
src\save\saveSystem.js:795:    const e = this.helpers.getEntity(state.playerId);
src\save\saveSystem.js:797:      e.data.combat.targetId = null;
src\save\saveSystem.js:809:        m.targetEntityIds = [];
src\save\saveSystem.js:987:  if (!data.combat || typeof data.combat !== 'object') data.combat = { targetId: null, lockTarget: null, lockProgress: 0 };
src\render\feel.js:6://   - Hit-stop is implemented as a render-phase driver that pulses `state.timeScale` DOWN from 1
src\render\feel.js:21:// This is a render-phase system (no sim update). Driven from registry.renderUpdate -> feel.frame().
src\render\feel.js:150:    const pid = this.state.playerId;
src\render\feel.js:153:    let targetOpacity = 0;
src\render\feel.js:154:    let boosting = false;
src\render\feel.js:161:      boosting = !!(player.flags && player.flags.boosting);
src\render\feel.js:163:      if (boosting) {
src\render\feel.js:164:        targetOpacity = 0.62;
src\render\feel.js:169:        targetOpacity = intensity * 0.34;
src\render\feel.js:173:    // Smooth-damp toward target (rate 8 = responsive but not jarring)
src\render\feel.js:174:    this._slOpacity = damp(this._slOpacity, targetOpacity, 8, frameDt);
src\render\feel.js:198:    const want = Math.round((boosting ? 58 : 32) * (0.45 + 0.55 * intensity));
src\render\feel.js:201:    const flow = (boosting ? 1.85 : 1.05) * (0.55 + 0.7 * intensity);   // outward speed (maxR-fractions/s)
src\render\feel.js:203:    const widthMul = boosting ? 1.5 : 1.0;
src\render\feel.js:252:      const isPlayer = p.isPlayer || (p.targetId === state.playerId);
src\render\feel.js:262:      const playerInvolved = (p.killerId === state.playerId) || (p.id === state.playerId);
src\render\feel.js:279:      if (!p || p.ownerId !== state.playerId) return;
src\render\feel.js:312:    bus.on('jump:arrive', (p) => {
src\render\feel.js:326:      if (!p || p.minerId !== state.playerId) return;
src\render\feel.js:423:      const target = baseFov + this._fovPunch;
src\render\feel.js:424:      if (Math.abs(cam.fov - target) > 0.001) {
src\render\feel.js:425:        cam.fov = target;
src\systems\aiPorts.js:250:      if (!isLiveCraft(entity) || entity.id === state.playerId) continue;
src\systems\aiPorts.js:363:      addAttachment(byEntity, attachment.targetId, attachment);
src\systems\aiPorts.js:523:      targetId: other.data && other.data.combat ? other.data.combat.targetId : null,
src\systems\aiPorts.js:545:    const target = getEntity(state, attachment.targetId);
src\systems\aiPorts.js:546:    if (!owner || !target) continue;
src\systems\aiPorts.js:547:    const pos = midpoint(owner.pos, target.pos);
src\systems\aiPorts.js:549:    const endpointVisible = attachment.ownerId === self.id || attachment.targetId === self.id;
src\systems\aiPorts.js:551:    const hostile = isHostile(self, attachment.ownerId === self.id ? target : owner);
src\systems\aiPorts.js:559:      vel: freeze(relativeVelocity(owner, target)),
src\systems\aiPorts.js:563:      targetId: attachment.targetId,
src\systems\aiPorts.js:567:      targetSocketId: attachment.targetSocketId || null,
src\systems\aiPorts.js:569:      tethered: attachment.targetId === self.id || attachment.ownerId === self.id,
src\systems\aiPorts.js:586:    if (!event || event.targetId == null) continue;
src\systems\aiPorts.js:589:    let out = byTarget.get(event.targetId);
src\systems\aiPorts.js:592:      byTarget.set(event.targetId, out);
src\systems\aiPorts.js:599:        targetId: event.targetId,
src\systems\aiPorts.js:607:        targetId: event.targetId,
src\systems\aiPorts.js:632:    targetHeading: wrapAngle(finite(request.targetHeading)),
src\systems\aiPorts.js:813:  return state && state.entities && state.entities.get ? state.entities.get(id) || null : null;
src\systems\gamepad.js:11://   2  X / Square      -> cycle target
src\render\partsLibrary.js:158:// outside this map fall back to the seed-based pick across all seven hulls. Roles follow the genius's
src\render\partsLibrary.js:537:      position: [0, 0, 0], targetLength: 1.72, label: 'Hull',
src\render\partsLibrary.js:551:    position: [0.35, 0.12, 0], targetLength: 0.58, label: 'Cockpit',
src\render\partsLibrary.js:570:      targetLength: 0.58, label: `Engine_${index}`,
src\render\partsLibrary.js:572:    : defaultEnginePositions.map((position, index) => ({ position, targetLength: 0.58, label: `Engine_${index}` }));
src\render\partsLibrary.js:591:    position: [-0.06, 0.02, side * 0.50], targetLength: 0.62,
src\render\partsLibrary.js:763:        targetLength: weaponTargetLength(size, wdef),
src\render\partsLibrary.js:785:        targetLength: role.includes('miner') ? 0.30 : 0.38,
src\render\partsLibrary.js:798:        targetLength: role.includes('fighter') || role.includes('interceptor') ? 0.24 : 0.30,
src\render\partsLibrary.js:812:        targetLength: 0.25,
src\render\partsLibrary.js:829:      targetLength: heavy ? 0.42 : 0.34,
src\render\partsLibrary.js:847:    { position: [0.16, 0.30, 0.30], rotation: [0, 0, 0.02], targetLength: 0.16, label: 'Greeble_DorsalA' },
src\render\partsLibrary.js:848:    { position: [-0.24, 0.27, -0.30], rotation: [0, 0, -0.02], targetLength: 0.15, label: 'Greeble_DorsalB' },
src\render\partsLibrary.js:849:    { position: [-0.38, 0.14, 0.42], rotation: [0, 0, 0.04], targetLength: 0.14, label: 'Greeble_ServiceC' },
src\render\partsLibrary.js:1441:  const scale = placement.targetLength / sourceLength;
src\systems\flight.js:60:      targetYawRate: 0,
src\systems\flight.js:70:        const player = this.state && this.state.entities && this.state.entities.get(this.state.playerId);
src\systems\flight.js:74:        const player = this.state && this.state.entities && this.state.entities.get(this.state.playerId);
src\systems\flight.js:89:    const player = state.entities.get(state.playerId);
src\systems\flight.js:95:      // Emit boost start/stop on the TRUE transition (applyPlayerIntent already set flags.boosting
src\systems\flight.js:99:      if (player.flags.boosting && !wasBoosting) this.bus.emit('ship:boostStart', { shipId: player.id });
src\systems\flight.js:100:      else if (!player.flags.boosting && wasBoosting) this.bus.emit('ship:boostStop', { shipId: player.id });
src\systems\flight.js:101:      player._wasBoosting = player.flags.boosting;
src\systems\flight.js:108:      if (e.type !== 'ship' || !e.alive || e.id === state.playerId) continue;
src\systems\flight.js:155:    let boosting = false;
src\systems\flight.js:158:        boosting = true;
src\systems\flight.js:165:    if (!boosting) boost.energy = Math.min(boost.max, boost.energy + boost.regenRate * dt);
src\systems\flight.js:166:    e.flags.boosting = boosting;
src\systems\flight.js:169:    stepPlayerFlight(e, inp, dt, profile, { boosting, physicsAuthority: opts.physicsAuthority, source: 'player-flight' });
src\systems\flight.js:197:    const hadGesture = !!(this._prevBoost || (boost && (boost._dashCandidate || boost._boostHoldT > 0)) || (e && e.flags && e.flags.boosting));
src\systems\flight.js:205:    const wasBoosting = !!(e.flags.boosting || e._wasBoosting);
src\systems\flight.js:206:    e.flags.boosting = false;
src\systems\flight.js:216:    e.flags.boosting = !!intent.boost;
src\systems\flight.js:259:      targetYawRate: frame.targetYawRate || 0,
src\render\bloom.js:3:// progressive downsample chain (full→½→¼→⅛) followed by an additive upsample chain back to ½. Each
src\render\bloom.js:29://   three renders to render targets with NoToneMapping regardless, so rtScene would be un-tonemapped
src\render\bloom.js:218: * @param {THREE.WebGLRenderer} renderer - the live renderer (we drive its render targets).
src\render\bloom.js:236:  // ---- render targets ----
src\render\bloom.js:237:  // rtScene is full-res (needs a depth buffer for the scene render). The pyramid targets halve each
src\render\bloom.js:238:  // level (½→¼→⅛). bloomPing/bloomPong are scratch targets for the additive upsample chain, resized
src\render\bloom.js:249:  // scene target adds a costly resolve before the downsample/composite chain. Keep the offscreen target
src\render\bloom.js:251:  // Pyramid targets stay single-sampled too — they're downsampled, AA there is wasted.
src\render\bloom.js:326:  // draw the shared quad with a given material into a given target (null = screen)
src\render\bloom.js:327:  function blit(material, target) {
src\render\bloom.js:329:    renderer.setRenderTarget(target);
src\render\bloom.js:375:      const targetW = Math.max(1, W >> i);              // output = finer level (down[i-1]) resolution
src\render\bloom.js:376:      const targetH = Math.max(1, H >> i);
src\render\bloom.js:377:      outRT.setSize(targetW, targetH);
src\render\bloom.js:380:      upsampleMat.uniforms.uTexel.value.set(1 / targetW, 1 / targetH);
src\render\bloom.js:404:    // WebGL context restore: the old render-target GPU textures are invalid. Dispose them and
src\render\bloom.js:474:      targets: 1 + down.length + 2,
src\systems\touch.js:154:    // the knob follows the finger, clamped to the radius. The normalized delta (−1..1) becomes the
src\systems\economy.js:18://   reference AND the drift target collapses every market to basePrice (stock drifts to eq, price
src\systems\economy.js:21://   price reference is the FIXED baseEq and the role multiplies the stock target. We honor the
src\systems\economy.js:22://   schema field names (equilibrium = role-modified drift target, baseEq = fixed reference) and
src\systems\economy.js:178:/** Effective stock drift target = equilibrium (role*size*BASE_EQ) * event eq mods, clamped. */
src\systems\economy.js:358:      const equilibrium = economyStockTargetForRole(role, baseEqRef); // stock drift target
src\systems\economy.js:637:      const e = state.entities && state.entities.get(state.playerId);
src\systems\economy.js:773:    const e = this.state.entities && this.state.entities.get(id);
src\systems\economy.js:881:          const targets = ev.commodityId === '*' ? Object.keys(market) : [ev.commodityId];
src\systems\economy.js:882:          for (const cid of targets) {
src\systems\economy.js:885:            // shortage raises neighbour eq toward shortage (lowers stock target slightly),
src\systems\economy.js:887:            // (we don't persist propagation mods — recompute the blended target instead).
src\systems\missions.js:151:  const sectorName = sector && sector.name || 'target sector';
src\systems\missions.js:184:    this._spawnSeq = 0; // disambiguates re-spawns of the same mission target across visits
src\systems\missions.js:209:    // bulk_trade quota: sell qty of the target commodity (trade.sold alias → economy:tradeCompleted).
src\systems\missions.js:211:    // mining_quota: aggregate mined units of the target commodity.
src\systems\missions.js:217:    // recon_scan: a scan target (or sector scan) completed.
src\systems\missions.js:222:    // ── Lazy mission-target spawning when the player enters a target sector ───────────────────
src\systems\missions.js:232:  // PER-TICK: TTL decrement, expiry, story-gate checks, stale-target GC.
src\systems\missions.js:379:    // Per-type params (quota qty, target strength, scan count, commodity, …) + cargo value.
src\systems\missions.js:483:        const targetStrength = 1.2 + riskTier * 0.5 + rng() * 0.6; // ~1.2..3.8
src\systems\missions.js:484:        return { clearCount: 1, killCount: 0, targetStrength, fValue: targetStrength, taskTime: 60 };
src\systems\missions.js:487:        const targetStrength = 1.0 + riskTier * 0.4 + rng() * 0.5;
src\systems\missions.js:488:        return { targetStrength, fValue: targetStrength, taskTime: 90 };
src\systems\missions.js:492:        const targetStrength = (1.0 + riskTier * 0.4) * clearCount * 0.6;
src\systems\missions.js:493:        return { clearCount, killCount: 0, targetStrength, fValue: targetStrength, taskTime: clearCount * 45 };
src\systems\missions.js:513:      case 'bounty_hunt': return `Eliminate a wanted target near ${destName}`;
src\systems\missions.js:555:    // Spawn any immediate/deferred targets (if the player is already in the target sector).
src\systems\missions.js:565:    const _p = this.state.entities && this.state.playerId != null ? this.state.entities.get(this.state.playerId) : null;
src\systems\missions.js:570:      targetId: this.state.playerId,
src\systems\missions.js:633:      targetEntityIds: [],          // runtime entity ids (NOT serialized — re-spawned on load)
src\systems\missions.js:781:      const target = this._firstLiveMissionTarget(m);
src\systems\missions.js:782:      if (target) return { ...base, targetEntityId: target.id, pos: { x: target.pos.x, z: target.pos.z }, reason: 'Intercept the marked hostile' };
src\systems\missions.js:787:      const targetStation = this._liveStation(m.destStationId);
src\systems\missions.js:788:      if (targetStation) return { ...base, pos: { x: targetStation.pos.x, z: targetStation.pos.z }, reason: 'Escort the convoy to dock' };
src\systems\missions.js:800:    const targetStation = this._liveStation(m.destStationId);
src\systems\missions.js:801:    if (targetStation) return { ...base, pos: { x: targetStation.pos.x, z: targetStation.pos.z } };
src\systems\missions.js:806:    for (const id of m.targetEntityIds || []) {
src\systems\missions.js:807:      const e = this.state.entities.get(id);
src\systems\missions.js:827:    const player = this.state.entities.get(this.state.playerId);
src\systems\missions.js:841:    const player = this.state.entities.get(this.state.playerId);
src\systems\missions.js:954:    const byPlayer = p.killerId === this.state.playerId;
src\systems\missions.js:961:      if (!m.targetEntityIds.includes(p.id)) continue;
src\systems\missions.js:962:      m.targetEntityIds = m.targetEntityIds.filter((id) => id !== p.id);
src\systems\missions.js:982:    // recon_scan: a scan completed. We accept either a targeted scan (targetId matches a spawned
src\systems\missions.js:983:    // beacon) or a generic sector scan (targetId null) as one unit of progress.
src\systems\missions.js:987:      // only count if the player is in the mission's target sector
src\systems\missions.js:1040:    const e = this.state.entities.get(m._escorteeId);
src\systems\missions.js:1187:    const _cp = this.state.entities && this.state.playerId != null ? this.state.entities.get(this.state.playerId) : null;
src\systems\missions.js:1192:      targetId: this.state.playerId,
src\systems\missions.js:1281:  /** Spawn bounty/patrol hostiles or the escortee if the player is in the mission's target sector. */
src\systems\missions.js:1285:    if (m.targetEntityIds.length > 0) return;                        // already spawned
src\systems\missions.js:1293:    const player = helpers.player ? helpers.player() : this.state.entities.get(this.state.playerId);
src\systems\missions.js:1301:      // Spawn only the targets still owed (objectiveTarget - progress) so a mid-mission save/load or
src\systems\missions.js:1316:        if (ent) m.targetEntityIds.push(ent.id);
src\systems\missions.js:1332:      if (ent) { m._escorteeId = ent.id; m._escorteeArrived = false; m.targetEntityIds.push(ent.id); }
src\systems\missions.js:1334:    if (m.targetEntityIds.length) this.bus.emit('mission:updated', { missionId: m.id });
src\systems\missions.js:1341:    const e = state.entities.get(m._escorteeId);
src\systems\missions.js:1348:    let target = null;
src\systems\missions.js:1352:      target = byStationId && m.destStationId ? byStationId.get(m.destStationId) : null;
src\systems\missions.js:1353:      if (!target || !target.alive || target.type !== 'station') {
src\systems\missions.js:1355:        target = null;
src\systems\missions.js:1357:          if (cand.alive && cand.type === 'station' && cand.data && cand.data.stationId === m.destStationId) { target = cand; break; }
src\systems\missions.js:1361:    if (!target) {
src\systems\missions.js:1362:      const player = state.entities.get(state.playerId);
src\systems\missions.js:1363:      target = player && player.alive ? player : null;
src\systems\missions.js:1365:    if (!target) { intent.moveX = 0; intent.moveZ = 0; return; }
src\systems\missions.js:1367:    const dx = target.pos.x - e.pos.x, dz = target.pos.z - e.pos.z;
src\systems\missions.js:1369:    const arriveR = (target.type === 'station' ? (target.data && target.data.dockRadius) || 80 : 140) + 40;
src\systems\missions.js:1375:      if (inDestSector && target.type === 'station') m._escorteeArrived = true;
src\systems\missions.js:1385:  /** Mark mission target entities dead when the mission settles (avoid orphans). */
src\systems\missions.js:1387:    if (!m.targetEntityIds || !m.targetEntityIds.length) return;
src\systems\missions.js:1388:    for (const id of m.targetEntityIds) {
src\systems\missions.js:1389:      const e = this.state.entities.get(id);
src\systems\missions.js:1390:      if (e && e.alive && e.id !== this.state.playerId) e.alive = false; // swept end-of-step
src\systems\missions.js:1392:    m.targetEntityIds = [];
src\systems\missions.js:1406:    // Spawn (or re-spawn after load) deferred targets for any active mission keyed to this sector.
src\systems\missions.js:1410:      m.targetEntityIds = m.targetEntityIds.filter((id) => {
src\systems\missions.js:1411:        const e = this.state.entities.get(id); return e && e.alive;
src\systems\missions.js:1413:      if (m.targetEntityIds.length === 0 && (m.objectiveProgress < m.objectiveTarget)) {
src\systems\missions.js:1432:      if (m.needsTargets && m.destSectorId === sectorId) { m.targetEntityIds = []; m._escorteeId = null; }
src\systems\missions.js:1585:  // newGame / save-load (§4.5 — missions + story serialize; live target ids do NOT)
src\systems\missions.js:1615:      const { targetEntityIds, _escorteeId, _escorteeSectorId, _escorteeArrived, ...rest } = a;
src\systems\missions.js:1616:      return { ...rest, targetEntityIds: [], needsTargets: a.needsTargets };
src\systems\missions.js:1633:    // Stale-target GC: clear live entity ids; targets re-spawn when the player (re-)enters the sector.
src\systems\missions.js:1635:      ...a, targetEntityIds: [], _escorteeId: null, _escorteeArrived: false, status: a.status || 'active',
src\systems\missions.js:1776:  if ((a.targetEntityId || null) !== (b.targetEntityId || null)) return false;
src\render\visualFactory.js:653:  // selling the "tracks its target" read. (Static ships still get a gentle idle sweep.)
src\render\visualFactory.js:1609:      // turret heads sweep ±35° seeking a target — sells the "auto-tracking" read even when idle
src\render\diagnostics.js:121:  function update(dt) {
src\systems\flightV3.js:94:    const player = state.entities && state.entities.get(state.playerId);
src\systems\flightV3.js:100:      // Emit boost start/stop on the TRUE transition. _stepCraft set flags.boosting to the
src\systems\flightV3.js:104:      if (player.flags.boosting && !wasBoosting) this.bus.emit('ship:boostStart', { shipId: player.id });
src\systems\flightV3.js:105:      else if (!player.flags.boosting && wasBoosting) this.bus.emit('ship:boostStop', { shipId: player.id });
src\systems\flightV3.js:106:      player._wasBoosting = player.flags.boosting;
src\systems\flightV3.js:113:      if (!entity || entity.id === state.playerId || entity.alive === false) continue;
src\systems\flightV3.js:132:    let boosting = input.boost;
src\systems\flightV3.js:134:      boosting = this._stepPlayerBoost(entity, input.boost, dt, state);
src\systems\flightV3.js:135:      input.boost = boosting;
src\systems\flightV3.js:162:    entity.flags.boosting = isPlayer ? boosting : !!input.boost;
src\systems\flightV3.js:170:  // Player boost/dash state machine. Returns the resource-gated boosting flag to feed back into
src\systems\flightV3.js:196:    let boosting = false;
src\systems\flightV3.js:199:        boosting = true;
src\systems\flightV3.js:206:    if (!boosting) boost.energy = Math.min(boost.max, boost.energy + boost.regenRate * dt);
src\systems\flightV3.js:207:    return boosting;
src\systems\flightV3.js:232:      || (e.flags && e.flags.boosting));
src\systems\flightV3.js:238:    const wasBoosting = !!(e.flags.boosting || e._wasBoosting);
src\systems\flightV3.js:239:    e.flags.boosting = false;
src\systems\flightV3.js:248:    const player = state && state.entities && state.playerId
src\systems\flightV3.js:249:      ? state.entities.get(state.playerId) : null;
src\systems\flightV3.js:369:  const target = entity.data.propulsionRuntime || (entity.data.propulsionRuntime = {});
src\systems\flightV3.js:370:  Object.assign(target, runtime);
src\systems\flightV3.js:371:  target.previousBoost = !!boost;
src\systems\flightV3.js:372:  return target;
src\systems\flightV3.js:431:  const target = clamp(turn * factor * bankMax, -bankMax, bankMax);
src\systems\flightV3.js:432:  const lambda = Math.abs(target) > 0.001 ? BANK_RESPONSE : BANK_RETURN;
src\systems\flightV3.js:433:  entity.bank = damp(finite(entity.bank), target, lambda, dt);
src\systems\flightV3.js:434:  if (Math.abs(entity.bank) < 0.0005 && Math.abs(target) < 0.0005) entity.bank = 0;
src\systems\flightV3.js:447:function damp(cur, target, lambda, dt) { return cur + (target - cur) * (1 - Math.exp(-lambda * dt)); }
src\systems\factions.js:155:      if (p.killerId !== state.playerId) return; // NPC-on-NPC kills don't touch player rep
src\systems\factions.js:446:      const target = power[id];
src\systems\factions.js:447:      rec.power = rec.power + (target - rec.power) * 0.5;
src\systems\input.js:72:  yawRight: [],                // nose follows cursor — yaw keys retired in this scheme
src\systems\input.js:157:function isTextEntryTarget(target) {
src\systems\input.js:158:  if (!target || typeof target.closest !== 'function') return false;
src\systems\input.js:159:  return !!target.closest('input, textarea, select, [contenteditable="true"], [contenteditable=""], [data-text-input]');
src\systems\input.js:162:function isUiCommandTarget(target) {
src\systems\input.js:163:  if (!target || typeof target.closest !== 'function') return false;
src\systems\input.js:164:  return !!target.closest('button, [role="button"], a[href], input, textarea, select, [contenteditable="true"], [contenteditable=""], #ui-root, #screens');
src\systems\input.js:199:      if (modalInputActive() || isTextEntryTarget(e.target) || isUiCommandTarget(e.target)) {
src\systems\input.js:216:      if (this._canvas && e.target !== this._canvas) return;
src\systems\input.js:222:      if (this._canvas && e.target !== this._canvas) {
src\systems\input.js:226:      if (!this._canvas && isUiCommandTarget(e.target)) {
src\systems\input.js:352:    const p = state.entities.get(state.playerId);
src\render\lod.js:3:// "LOD should follow projected screen size, not only world distance." Starting thresholds:
src\systems\intervention.js:43:    const player = this.state.entities.get(this.state.playerId);
src\systems\intervention.js:117:      const e = rec.wreckEntityId != null ? state.entities.get(rec.wreckEntityId) : null;
src\systems\mining.js:60:      targetSpatialQueries: 0,
src\systems\mining.js:61:      targetCandidates: 0,
src\systems\mining.js:82:    const player = state.entities.get(state.playerId);
src\systems\mining.js:122:    const target = this._acquireTarget(player, beam.range, state);
src\systems\mining.js:123:    if (!target) { this._coolBeam(beam, dt); this._stopBeam(); return; }
src\systems\mining.js:126:    if (!this._beaming || this._lockTargetId !== target.id) {
src\systems\mining.js:127:      this._lockTargetId = target.id;
src\systems\mining.js:128:      this.bus.emit('mining:start', { minerId: player.id, targetId: target.id, position: { x: target.pos.x, z: target.pos.z } });
src\systems\mining.js:139:    this._activeBeamLine = beamLineFor(player, target);
src\systems\mining.js:142:    if (target.type === 'wreck') this._drainWreck(player, target, dps, dt);
src\systems\mining.js:143:    else this.applyMining(target.id, dps, dt, player.id);
src\systems\mining.js:162:    this.bus.emit('mining:stop', { minerId: this.state.playerId, targetId: this._lockTargetId, position: null });
src\systems\mining.js:165:  // Nearest mineable target (asteroid or salvageable wreck) within range, biased toward aim.
src\systems\mining.js:171:    this._diag.targetSpatialQueries = mineables === this._mineableScratch ? 1 : 0;
src\systems\mining.js:172:    this._diag.targetCandidates = mineables.length;
src\systems\mining.js:192:  applyMining(targetId, dps, dt, minerId = this.state.playerId) {
src\systems\mining.js:194:    const ast = state.entities.get(targetId);
src\systems\mining.js:210:    const miner = state.entities.get(minerId);
src\systems\mining.js:257:    const direct = !!(beam && beam.directToCargo) && miner && miner.id === this.state.playerId;
src\systems\mining.js:305:    const player = state.entities.get(state.playerId);
src\systems\mining.js:348:    if (p.collectorId !== this.state.playerId) return; // drones manage their own holds
src\systems\mining.js:513:    if (collectorId != null && collectorId !== this.state.playerId) return 0;
src\systems\mining.js:645:  diag.targetSpatialQueries = 0;
src\systems\mining.js:646:  diag.targetCandidates = 0;
src\systems\mining.js:653:function beamLineFor(player, target) {
src\systems\mining.js:657:    bx: target.pos.x,
src\systems\mining.js:658:    bz: target.pos.z,
src\render\renderer.js:181:    // true). The key light becomes a shadow caster with a tight frustum that follows the player so
src\render\renderer.js:192:      // renderFrame to follow the player). Tight bounds = crisp shadows at usable resolution.
src\render\renderer.js:199:      key.target = new THREE.Object3D(); scene.add(key.target);
src\render\renderer.js:256:          // Rebuild the bloom post-process pipeline (its render targets are tied to the lost context).
src\render\renderer.js:282:    this._sectorPaletteRig = createSectorPaletteRig(scene, ambient, key, rim, fill);
src\render\renderer.js:283:    this._sectorPaletteTarget = corePalette;
src\render\renderer.js:284:    state.render.sectorPalette = corePalette;
src\render\renderer.js:356:    state.camera.obj = cam.obj;
src\render\renderer.js:375:    bus.on('camera:zoom', ({ delta, level }) => { if (level != null) cam.setZoom(level); else cam.setZoom(state.camera.zoom + (delta || 0)); });
src\render\renderer.js:414:    bus.on('jump:arrive', ({ sectorId } = {}) => {
src\render\renderer.js:425:      if (keepPlayer && id === this.state.playerId) continue;
src\render\renderer.js:466:      const e = state.entities.get(id);
src\render\renderer.js:488:      const e = this.state.entities.get(id);
src\render\renderer.js:511:    const e = this.state.entities.get(id);
src\render\renderer.js:700:    const rig = this._sectorPaletteRig;
src\render\renderer.js:703:    this.state.render.sectorPalette = palette;
src\render\renderer.js:704:    if (palette === this._sectorPaletteTarget) return;
src\render\renderer.js:706:    this._sectorPaletteTarget = palette;
src\render\renderer.js:708:    writePaletteToSectorPaletteFrame(rig.target, palette);
src\render\renderer.js:718:    const rig = this._sectorPaletteRig;
src\render\renderer.js:724:    lerpSectorPaletteFrame(rig, rig.start, rig.target, t);
src\render\renderer.js:727:      applySectorPaletteFrame(rig, rig.target);
src\render\renderer.js:733:    // webglcontextrestored rebuilds GPU resources. (cam.follow etc. would run against a dead
src\render\renderer.js:738:    this.cam.follow(frameDt);
src\render\renderer.js:748:    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
src\render\renderer.js:751:    // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
src\render\renderer.js:753:    // an offset from its target; we move both together. No-op if shadows are disabled.
src\render\renderer.js:793:    const p = this.state.playerId ? (this.state.entities && this.state.entities.get(this.state.playerId)) : null;
src\render\renderer.js:796:    this._keyLight.target.position.set(px, 0, pz);
src\render\renderer.js:797:    this._keyLight.target.updateMatrixWorld();
src\render\renderer.js:889:  // targets). Returns false if construction fails (e.g. a low-capability GPU) so the caller falls
src\render\renderer.js:938:    target: createSectorPaletteFrame(),
src\render\renderer.js:943:  writeRigToSectorPaletteFrame(rig.target, rig);
src\render\renderer.js:1000:function lerpSectorPaletteFrame(rig, start, target, t) {
src\render\renderer.js:1001:  rig.lights.ambient.color.lerpColors(start.colors.ambient, target.colors.ambient, t);
src\render\renderer.js:1002:  rig.lights.key.color.lerpColors(start.colors.key, target.colors.key, t);
src\render\renderer.js:1003:  rig.lights.rim.color.lerpColors(start.colors.rim, target.colors.rim, t);
src\render\renderer.js:1004:  rig.lights.fill.color.lerpColors(start.colors.fill, target.colors.fill, t);
src\render\renderer.js:1005:  rig.scene.fog.color.lerpColors(start.colors.fog, target.colors.fog, t);
src\render\renderer.js:1006:  rig.lights.ambient.intensity = lerp(start.intensities.ambient, target.intensities.ambient, t);
src\render\renderer.js:1007:  rig.lights.key.intensity = lerp(start.intensities.key, target.intensities.key, t);
src\render\renderer.js:1008:  rig.lights.rim.intensity = lerp(start.intensities.rim, target.intensities.rim, t);
src\render\renderer.js:1009:  rig.lights.fill.intensity = lerp(start.intensities.fill, target.intensities.fill, t);
src\render\renderer.js:1010:  rig.scene.fog.density = lerp(start.fogDensity, target.fogDensity, t);
src\systems\tetherGameplay.js:3:// service into player flight. Rapier owns momentum exchange; this system only targets,
src\systems\tetherGameplay.js:11:// (despawned) tether target is how the invisible-anchor bug was born. Tether targets are things
src\systems\tetherGameplay.js:24:    this._targetScratch = [];
src\systems\tetherGameplay.js:32:    const player = state.entities && state.entities.get ? state.entities.get(state.playerId) : null;
src\systems\tetherGameplay.js:45:      const targetId = this._active.targetId;
src\systems\tetherGameplay.js:47:      // attachment_missing = the orphan sweep already broke it (target died) and reconcile
src\systems\tetherGameplay.js:49:      if (result && result.ok) this.bus.emit('tether:released', { targetId });
src\systems\tetherGameplay.js:57:      // Liveness belt-and-braces on the gameplay side: if the target vanished this tick and the
src\systems\tetherGameplay.js:59:      const target = state.entities.get(this._active.targetId);
src\systems\tetherGameplay.js:60:      if (!target || target.alive === false) {
src\systems\tetherGameplay.js:61:        attachments.cut(this._active.attachmentId, player.id, 'target_lost');
src\systems\tetherGameplay.js:62:        this.bus.emit('tether:broke', { targetId: this._active.targetId });
src\systems\tetherGameplay.js:71:      this._mirror(state, this._active.targetId, this._lastStrainRatio || 0, att ? att.restLength : 0);
src\systems\tetherGameplay.js:80:    const target = this._acquireTarget(player, def, state);
src\systems\tetherGameplay.js:81:    if (!target) return;
src\systems\tetherGameplay.js:86:      targetId: target.id,
src\systems\tetherGameplay.js:92:      targetId: target.id,
src\systems\tetherGameplay.js:96:    this.bus.emit('tether:latched', { targetId: target.id, type: TETHER_DEF_ID });
src\systems\tetherGameplay.js:106:      this._targetScratch,
src\systems\tetherGameplay.js:140:    const owned = attachments.listForEntity(state.playerId, true);
src\systems\tetherGameplay.js:142:      if (attachment.ownerId !== state.playerId) continue;
src\systems\tetherGameplay.js:145:        targetId: attachment.targetId,
src\systems\tetherGameplay.js:157:    const targetId = this._active.targetId;
src\systems\tetherGameplay.js:162:    if (reason === 'tether_cut') this.bus.emit('tether:released', { targetId });
src\systems\tetherGameplay.js:163:    else this.bus.emit('tether:broke', { targetId });
src\systems\tetherGameplay.js:203:  // they read, we write). null targetId = no tether. restLength lets the cable visual compute
src\systems\tetherGameplay.js:205:  _mirror(state, targetId, strain, restLength = 0) {
src\systems\tetherGameplay.js:207:    const t = player.tether || (player.tether = { active: false, targetId: null, strain: 0, attachmentId: null, restLength: 0 });
src\systems\tetherGameplay.js:208:    t.active = targetId != null;
src\systems\tetherGameplay.js:209:    t.targetId = targetId;
src\systems\tetherGameplay.js:237:function isAttachable(entity, playerId) {
src\systems\tetherGameplay.js:238:  if (!entity || !entity.alive || !entity.pos || entity.id === playerId) return false;
src\render\post\spaceRenderGraph.js:374:      const target = this.bloomTargets[i];
src\render\post\spaceRenderGraph.js:380:      this.quad.render(renderer, this.bloomMaterial, target);
src\render\post\spaceRenderGraph.js:381:      source = target.texture;
src\render\post\spaceRenderGraph.js:382:      sourceW = target.width;
src\render\post\spaceRenderGraph.js:383:      sourceH = target.height;
src\render\post\spaceRenderGraph.js:418:    for (const target of [this.sceneTarget,this.normalTarget,this.aoTarget,this.aoBlurTarget,...(this.bloomTargets||[])]) {
src\render\post\spaceRenderGraph.js:419:      if (target) target.dispose();
src\render\post\spaceRenderGraph.js:434:  render(renderer, material, target) {
src\render\post\spaceRenderGraph.js:436:    renderer.setRenderTarget(target || null);
src\render\post\spaceRenderGraph.js:453:  const target = new THREE.WebGLRenderTarget(width,height,{
src\render\post\spaceRenderGraph.js:458:  target.texture.name = 'SpaceRenderGraph:HDR';
src\render\post\spaceRenderGraph.js:460:    target.depthTexture = new THREE.DepthTexture(width,height,THREE.UnsignedIntType);
src\render\post\spaceRenderGraph.js:461:    target.depthTexture.format = THREE.DepthFormat;
src\render\post\spaceRenderGraph.js:462:    target.depthTexture.name = 'SpaceRenderGraph:Depth';
src\render\post\spaceRenderGraph.js:464:  return target;
src\systems\sectorSim.js:91:    this.bus.on('jump:arrive', (p) => this._onJumpArrive(p));
src\systems\sectorSim.js:426:    if (!p || p.killerId !== this.state.playerId) return;
src\systems\sectorSim.js:480:          targetId: player.id,
src\systems\sectorSim.js:769:  if (state.entities && typeof state.entities.get === 'function') return state.entities.get(state.playerId) || null;
src\systems\sectorSim.js:770:  if (Array.isArray(state.entityList)) return state.entityList.find((e) => e && e.id === state.playerId) || null;
src\systems\impulseCharges.js:89:    const player = state.entities.get(state.playerId);
src\systems\impulseCharges.js:107:        const host = state.entities.get(d.hostId);
src\systems\impulseCharges.js:262:          targetId: ent.id,
src\systems\impulseCharges.js:280:      targetId: null,
src\render\vfx.js:5:// state is drawn on the following frame. Determinism is irrelevant here: VFX may use Math.random()
src\render\vfx.js:268:    add('jump:arrive', (p) => this._onJumpArrive(p));
src\render\vfx.js:397:    return this.state.entities.get(id) || null;
src\render\vfx.js:552:    if (this.helpers.socketWorldPos && p.ownerId === this.state.playerId) {
src\render\vfx.js:594:    const pos = this._posFrom(p, p.targetId);
src\render\vfx.js:596:    const tgt = this._ent(p.targetId);
src\render\vfx.js:635:    const pos = this._posFrom(p, p.targetId);
src\render\vfx.js:637:    const tgt = this._ent(p.targetId);
src\render\vfx.js:772:    const pos = this._posFrom(p, p.targetId ?? p.sourceId);
src\render\vfx.js:774:    const player = this._ent(this.state && this.state.playerId);
src\render\vfx.js:779:    const e = this._ent(p && p.targetId);
src\render\vfx.js:998:    this._miningBeam.targetId = (p && p.targetId) || null;
src\render\vfx.js:999:    // Tint to the target asteroid's ore type if we can resolve it
src\render\vfx.js:1000:    const target = p && p.targetId ? this._ent(p.targetId) : null;
src\render\vfx.js:1001:    if (target && target.data) {
src\render\vfx.js:1002:      const def = target.data.typeId;
src\render\vfx.js:1023:    const player = this.helpers && this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1026:    const target = beam.targetId ? this._ent(beam.targetId) : null;
src\render\vfx.js:1027:    if (!target || !target.alive) { this._onMiningStop(); return; }
src\render\vfx.js:1035:    const dx = sx - target.pos.x, dz = sz - target.pos.z;
src\render\vfx.js:1037:    const r = target.radius || 6;
src\render\vfx.js:1038:    const tx = target.pos.x + (dx / dist) * r, tz = target.pos.z + (dz / dist) * r;
src\render\vfx.js:1079:  // between the ship's NOSE and the latched target: bows with slack, straightens and heats
src\render\vfx.js:1141:    const player = this.helpers && this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1143:    const target = active ? this._ent(tether.targetId) : null;
src\render\vfx.js:1144:    const live = active && target && target.alive;
src\render\vfx.js:1146:    if (live && !cable.wasActive) { cable.latchAge = 0; cable.fade = 1; cable.lastTargetId = tether.targetId; }
src\render\vfx.js:1154:    const anchorEnt = live ? target : this._ent(cable.lastTargetId);
src\render\vfx.js:1157:    // Endpoints: ship NOSE (matches socket_massline localPos [1,0]) → target surface point.
src\render\vfx.js:1234:    const player = this.helpers && this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1235:    const target = p && p.targetId != null ? this._ent(p.targetId) : null;
src\render\vfx.js:1237:    for (const ent of [player, target]) {
src\render\vfx.js:1256:    const player = this.helpers && this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1381:    if (e.id === this.state.playerId) this.helpers.camera && this.helpers.camera.addTrauma(0.28);  // punch
src\render\vfx.js:1386:    const player = this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1387:    const pos = this._posFrom(p, this.state.playerId) || (player ? player.pos : null);
src\render\vfx.js:1393:    const pos = this._posFrom(p, this.state.playerId);
src\render\vfx.js:1430:    const player = this.helpers && this.helpers.player ? this.helpers.player() : this._ent(this.state.playerId);
src\render\vfx.js:1453:    const boostBlend = e.flags && e.flags.boosting ? 1 : 0;
src\render\vfx.js:1492:    // AFTERBURNER: when boosting, add extra bright wide particles + a subtle sustained nozzle glow.
src\render\vfx.js:1535:  // Massline ribbon — that write HDR radiance into the half-float bloom target. These are
src\render\vfx.js:1563:    // Massline ribbon: a thin tube energy volume drawn between the player and a tethered target.
src\render\vfx.js:1584:    const player = this.state.entities && this.state.entities.get(this.state.playerId);
src\render\vfx.js:1592:    const targetBoost = driveInfo.boost;
src\render\vfx.js:1595:    const boostRate = targetBoost > energy.boostBlend ? 8.5 : 3.6;
src\render\vfx.js:1597:    energy.boostBlend += (targetBoost - energy.boostBlend) * (1 - Math.exp(-boostRate * Math.max(0, dt || 0)));
src\render\vfx.js:1681:    const player = this.state.entities && this.state.entities.get(this.state.playerId);
src\render\vfx.js:1683:    // Find an active attachment owned or targeted by the player to render the ribbon along.
src\render\vfx.js:1693:        if (a.state === 'active' && (a.ownerId === player.id || a.targetId === player.id)) { att = a; break; }
src\render\vfx.js:1697:    const other = this.state.entities.get(att.ownerId === player.id ? att.targetId : att.ownerId);
src\render\vfx.js:1749:    if (e.id === this.state.playerId) {
src\render\vfx.js:1758:    const boost = e.flags && e.flags.boosting ? 1 : 0;
src\render\vfx.js:1858:    const e = this.state.entities.get(this.state.playerId);
src\render\vfx.js:1926:      const e = state.entities.get(id);
src\systems\presentationOrchestrator.js:39:        targetId: payload && payload.targetId,
src\systems\presentationOrchestrator.js:45:        targetId: payload && payload.targetId,
src\systems\presentationOrchestrator.js:52:        targetId: payload && payload.targetId,
src\systems\presentationOrchestrator.js:59:        targetId: payload && payload.targetId,
src\systems\presentationOrchestrator.js:97:      const targetId = actorId ? resolveActorEntityId(this.state, actorId) : null;
src\systems\presentationOrchestrator.js:101:        targetId,
src\systems\presentationOrchestrator.js:114:      targetId: payload.targetId,
src\systems\presentationOrchestrator.js:124:      targetId: resolveActorEntityId(this.state, 'evidence_spindle_47a'),
src\systems\presentationOrchestrator.js:140:      targetId: options.targetId ?? payload.targetId ?? payload.combatantId ?? null,
src\systems\scenarioRuntime.js:36:    if (this.state.mode !== 'flight' || !this.state.playerId) return;
src\systems\scenarioRuntime.js:179:    const target = payload.target || {};
src\systems\scenarioRuntime.js:180:    if (target.kind) event.targetKind = stableString(target.kind);
src\systems\scenarioRuntime.js:181:    if (target.kind === 'entity') {
src\systems\scenarioRuntime.js:182:      event.targetActorId = scenarioActorIdForEntity(state, target.entityId);
src\systems\scenarioRuntime.js:183:    } else if (target.kind === 'attachment') {
src\systems\scenarioRuntime.js:184:      const attachmentId = stableString(target.attachmentId);
src\systems\scenarioRuntime.js:189:        event.targetActorId = scenarioActorIdForEntity(state, attachment.targetId);
src\systems\scenarioRuntime.js:199:    event.targetActorId = scenarioActorIdForEntity(state, payload.targetId);
src\systems\scenarioRuntime.js:281:      targetActorId: condition.targetActorId || null,
src\systems\scenarioRuntime.js:291:      targetActorId: condition.targetActorId || null,
src\systems\scenarioRuntime.js:298:    const target = entityForScenarioActor(state, condition.targetActorId);
src\systems\scenarioRuntime.js:299:    const distance = actor && target && actor.alive && target.alive
src\systems\scenarioRuntime.js:300:      ? distance2d(actor.pos, target.pos)
src\systems\scenarioRuntime.js:308:        targetActorId: condition.targetActorId || null,
src\systems\scenarioRuntime.js:322:      targetActorId: condition.targetActorId || null,
src\systems\scenarioRuntime.js:349:  if (condition.targetActorId && event.targetActorId !== condition.targetActorId) return false;
src\systems\scenarioRuntime.js:355:  const target = entityForScenarioActor(state, condition.targetActorId);
src\systems\scenarioRuntime.js:356:  if (!owner || !target) return [];
src\systems\scenarioRuntime.js:361:    && attachment.targetId === target.id);
src\systems\scenarioRuntime.js:525:  if (actor.id === 'player_kestrel' && state.playerId && state.entities) {
src\systems\scenarioRuntime.js:526:    const player = state.entities.get(state.playerId);
src\systems\scenarioRuntime.js:543:  const entity = state.entities && state.entities.get(id);
src\systems\scenarioRuntime.js:557:    const entity = state.entities.get(binding.entityId);
src\systems\scenarioRuntime.js:627:    'targetKind',
src\systems\scenarioRuntime.js:628:    'targetActorId',
src\render\shipPreview.js:137:  const prevPos = cam.position.clone(); const prevLook = (cam.userData && cam.userData.target ? cam.userData.target.clone() : null);
src\systems\story.js:289:    if (state.playerId && killerId !== state.playerId) return; // only the player's kills
src\systems\story.js:347:  _onJumpChargeStart({ targetSectorId, via }) {
src\systems\story.js:365:    this.bus.emit('endgame:promptChoiceC', { promptText: 'JUMP WITHOUT DESTINATION?', targetSectorId });
src\systems\story.js:492:  // The friend's message arrives at t=0. The registry and the dock follow without explanation.
src\render\shipShot.js:92:  // pipeline runs and records the structure (real per-frame p95 needs the live loop on target HW).
src\systems\telemetry.js:16://   - kills are filtered to `killerId === playerId` (most `entity:killed` are NPC-vs-NPC).
src\systems\telemetry.js:192:    if (state && state.playerId != null && killerId === state.playerId) {
src\systems\telemetry.js:195:    const killer = state && state.entities && typeof state.entities.get === 'function'
src\systems\telemetry.js:196:      ? state.entities.get(killerId) : null;
src\systems\telemetry.js:251:    if (state && state.playerId != null && p.killerId !== state.playerId) return; // NPC-vs-NPC: ignore
src\systems\telemetry.js:350:  // NAVIGATION — dock + jump milestones. input.js:29 (dock:docked) / world.js:500 (jump:arrive)
src\systems\telemetry.js:358:  sub('jump:arrive', (p) => {
src\systems\telemetry.js:363:    pushRing('jump:arrive', { sectorId: p.sectorId, interdicted: p.interdicted, ambushCount: p.ambushCount });
src\render\ships\shipKit.js:72:export function readDrivePoseScaleFactors(object, pose, target = new THREE.Vector3()) {
src\render\ships\shipKit.js:73:  if (!object || !pose) return target.set(1, 1, 1);
src\render\ships\shipKit.js:74:  return target.set(
src\systems\ships.js:327:  // homing weapons lock a target and steer in flight (no gimbal — they fire toward the target).
src\systems\ships.js:420:      combat: { targetId: null, lockTarget: null, lockProgress: 0 },
src\systems\ships.js:460:    const e = this.state.entities.get(this.state.playerId);
src\systems\ships.js:472:    const e = this.state.entities.get(shipId);
src\systems\ships.js:475:    const isPlayer = e.id === this.state.playerId;
src\systems\ships.js:673:    const target = getDerivedStats(owned.defId, owned.fittings || [], p);
src\systems\ships.js:675:    if ((cargo.usedVolume || 0) > target.cargoCap) {
src\systems\ships.js:681:    const e = this.state.entities.get(this.state.playerId);
src\systems\ships.js:786:    return isActive ? this.state.playerId : -1;
src\render\ships\kestrelHero.js:2:// Bespoke player-starter ship for SpaceFace. Authoring space follows the project contract:
src\systems\scanner.js:123:    const player = state.entities && state.entities.get && state.entities.get(state.playerId);
src\systems\scanner.js:158:    this.bus.emit('scan:completed', { targetId: null, sectorId, found });
src\systems\scanner.js:165:      const entity = state.entities && state.entities.get && state.entities.get(poi.id);

codex
The worktree is very dirty, including active asset lock/building deletions and modified render files from today. I’ll keep strictly to the requested files and avoid touching the active graphics lanes or `vfx.js`/`starfield.js`.
codex
The current renderer already has sector palette identity on `state.render.sectorPalette` and a 1.5s light/fog lerp, with `jump:arrive` as the sector-change trigger. Next I’m narrowing to the camera follow source, package scripts, and the exact renderer insertion points.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content src\\render\\camera.js" in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$i=0; Get-Content src'"\\render\\renderer.js | ForEach-Object { "'$i++; if (($i -ge 1 -and $i -le 25) -or ($i -ge 208 -and $i -le 380) -or ($i -ge 408 -and $i -le 430) -or ($i -ge 730 -and $i -le 755)) { '"'{0,4}: {1}' -f "'$i, $_ } }' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1278ms:
// Tilted top-down chase camera (ARCHITECTURE §0.14): follows player POSITION only (never yaw,
// anti-nausea), with damped follow, velocity look-ahead, aim bias, and trauma-based shake.
// Phase 1: adds a subtle camera roll that counter-leans into the ship's bank so high-G turns feel
// dynamic without violating the no-yaw-follow rule (we never rotate the camera's heading).
import * as THREE from 'three';
import { damp } from '../core/math.js';

const THREAT_COMPOSE_RANGE = 600;
const THREAT_COMPOSE_MAX_BIAS = 90;
const THREAT_COMPOSE_FRACTION = 0.18;
const TETHER_COMPOSE_MAX_BIAS = 130;
const TETHER_COMPOSE_FRACTION = 0.24;
const SAFE_VIEW_X = 0.52;
const SAFE_VIEW_Z = 0.46;
const CRUISE_LOOKAHEAD_MAX = 12;
const CRUISE_LOOKAHEAD_SPEED_SCALE = 0.16;
const AIM_BIAS = 0.02;
const AIM_BIAS_MAX = 18;
const SHAKE_POS_MAX = 1.55;
const MOTION_REDUCE_SHAKE_SCALE = 0.25;

function finiteOr(value, fallback) {
  return Number.isFinite(value) ? value : fallback;
}

function isMotionReduced(state) {
  return !!(state && state.settings && state.settings.video && state.settings.video.motionReduce);
}

function resolveAimLead(input, player) {
  if (!input || !input.aimWorld || !player || !player.pos) return { x: 0, z: 0 };
  const px = finiteOr(player.pos.x, 0);
  const pz = finiteOr(player.pos.z, 0);
  const dx = finiteOr(input.aimWorld.x, px) - px;
  const dz = finiteOr(input.aimWorld.z, pz) - pz;
  const d = Math.hypot(dx, dz);
  if (d <= 0.0001) return { x: 0, z: 0 };
  const lead = Math.min(AIM_BIAS_MAX, d * AIM_BIAS);
  return { x: (dx / d) * lead, z: (dz / d) * lead };
}

export function clampFocusToPlayerSafeRect(focus, player, options = {}) {
  const playerX = player && player.pos && Number.isFinite(player.pos.x) ? player.pos.x : 0;
  const playerZ = player && player.pos && Number.isFinite(player.pos.z) ? player.pos.z : 0;
  if (!player || !player.pos) {
    return {
      x: focus && Number.isFinite(focus.x) ? focus.x : 0,
      z: focus && Number.isFinite(focus.z) ? focus.z : 0,
      clamped: false,
    };
  }
  const zoom = Number.isFinite(options.zoom) ? options.zoom : 95;
  const fov = Number.isFinite(options.fov) ? options.fov : 50;
  const aspect = Math.max(0.45, Number.isFinite(options.aspect) ? options.aspect : 16 / 9);
  const halfV = Math.tan((fov * Math.PI / 180) * 0.5) * zoom * 0.72;
  const halfH = halfV * aspect;
  const safeX = Math.max(14, halfH * SAFE_VIEW_X);
  const safeZ = Math.max(22, halfV * SAFE_VIEW_Z);
  let x = focus && Number.isFinite(focus.x) ? focus.x : playerX;
  let z = focus && Number.isFinite(focus.z) ? focus.z : playerZ;
  let clamped = false;
  const dx = x - playerX;
  const dz = z - playerZ;
  if (dx > safeX) { x = playerX + safeX; clamped = true; }
  else if (dx < -safeX) { x = playerX - safeX; clamped = true; }
  if (dz > safeZ) { z = playerZ + safeZ; clamped = true; }
  else if (dz < -safeZ) { z = playerZ - safeZ; clamped = true; }
  return { x, z, clamped, safeX, safeZ };
}

export function resolveChaseComposition(state, player, focus) {
  let fx = focus && Number.isFinite(focus.x) ? focus.x : (player && player.pos ? player.pos.x : 0);
  let fz = focus && Number.isFinite(focus.z) ? focus.z : (player && player.pos ? player.pos.z : 0);
  let nearbyEnemies = 0;
  let nearestThreat = null;
  let nearestThreatD2 = Infinity;

  if (!state || !player || !player.pos || !state.entities || typeof state.entities.values !== 'function') {
    return { x: fx, z: fz, nearbyEnemies, hasThreatFocus: false, hasTetherFocus: false };
  }

  // Combat composes player + nearest threat instead of only following the player.
  for (const e of state.entities.values()) {
    if (e === player) continue;
    if (e.type !== 'ship' || e.alive === false || e.team === player.team || e.hull <= 0 || !e.pos) continue;
    const dx = e.pos.x - player.pos.x;
    const dz = e.pos.z - player.pos.z;
    const d2 = dx * dx + dz * dz;
    if (d2 < THREAT_COMPOSE_RANGE * THREAT_COMPOSE_RANGE) {
      nearbyEnemies++;
      if (d2 < nearestThreatD2) {
        nearestThreat = e;
        nearestThreatD2 = d2;
      }
    }
  }

  if (nearestThreat && nearestThreatD2 > 1) {
    const d = Math.sqrt(nearestThreatD2);
    const bias = Math.min(THREAT_COMPOSE_MAX_BIAS, d * THREAT_COMPOSE_FRACTION);
    fx += ((nearestThreat.pos.x - player.pos.x) / d) * bias;
    fz += ((nearestThreat.pos.z - player.pos.z) / d) * bias;
  }

  const tetherAnchor = resolveTetherCompositionAnchor(state, player);
  if (tetherAnchor) {
    const dx = tetherAnchor.x - player.pos.x;
    const dz = tetherAnchor.z - player.pos.z;
    const d = Math.hypot(dx, dz);
    if (d > 1) {
      const bias = Math.min(TETHER_COMPOSE_MAX_BIAS, d * TETHER_COMPOSE_FRACTION);
      fx += (dx / d) * bias;
      fz += (dz / d) * bias;
    }
  }

  return {
    x: fx,
    z: fz,
    nearbyEnemies,
    hasThreatFocus: !!nearestThreat,
    hasTetherFocus: !!tetherAnchor,
  };
}

function resolveTetherCompositionAnchor(state, player) {
  const attachments = state.combat && state.combat.attachments && state.combat.attachments.byId;
  if (!attachments || !state.entities || typeof state.entities.get !== 'function') return null;

  let x = 0;
  let z = 0;
  let weightTotal = 0;
  for (const attachment of Object.values(attachments)) {
    if (!attachment || attachment.state !== 'active') continue;
    let otherId = null;
    if (attachment.ownerId === player.id) otherId = attachment.targetId;
    else if (attachment.targetId === player.id) otherId = attachment.ownerId;
    if (otherId == null) continue;

    const other = state.entities.get(otherId);
    if (!other || !other.alive || !other.pos) continue;
    const isPayload = other.type === 'payload' || !!(other.data && other.data.tetherPayload);
    const weight = isPayload ? 1.35 : 1.0;
    x += other.pos.x * weight;
    z += other.pos.z * weight;
    weightTotal += weight;
  }

  if (weightTotal <= 0) return null;
  return { x: x / weightTotal, z: z / weightTotal };
}

export function createChaseCamera(state) {
  // Far plane is deep (14k) so distant planets + far star layers render; fog still fades mid-distance.
  const cam = new THREE.PerspectiveCamera(state.settings.video.fov || 50, window.innerWidth / window.innerHeight, 1, 14000);
  const c = state.camera;
  c.shakeOffset = new THREE.Vector3();
  c.focus = new THREE.Vector3();
  const tiltRad = (c.tilt || 60) * Math.PI / 180;
  const offset = new THREE.Vector3();
  const computeOffset = (D) => {
    const distance = finiteOr(D, 95);
    return offset.set(0, distance * Math.sin(tiltRad), -distance * Math.cos(tiltRad));
  };
  computeOffset(c.zoom);
  cam.position.copy(offset);
  cam.lookAt(0, 0, 0);

  // smoothed camera roll (visual counter-lean into the player's bank)
  let camRoll = 0;
  const ROLL_MAX = 0.035;  // rad (~2.0 deg): readable bank feel without swimming the horizon.
  const ROLL_LERP = 3.2;   // responsiveness
  // scratch: roll is applied about the camera's local forward axis (the view direction), so the
  // image spins in-plane without changing where the camera points.
  const _rollQ = new THREE.Quaternion();
  const _FORWARD = new THREE.Vector3(0, 0, -1);
  // GR-6: rotational shake. Translational shake alone reads as a float; adding a small angular jitter
  // (roll + pitch about the camera's local axes) gives trauma real impact. Scaled by trauma² so it's
  // imperceptible at low trauma and punchy near death. Pitch (about local X) is the most visceral.
  const SHAKE_ROT_ROLL = 0.024;  // rad (~1.4 deg) max roll from shake
  const SHAKE_ROT_PITCH = 0.012; // rad (~0.7 deg) max pitch from shake
  const _shakeRollQ = new THREE.Quaternion();
  const _shakePitchQ = new THREE.Quaternion();
  const _camRight = new THREE.Vector3(1, 0, 0);

  // dynamic zoom — smoothly adapts camera distance to gameplay context
  let _dynamicZoom = finiteOr(c.zoom, 95);
  const ZOOM_LERP = 1.9;   // slower transitions reduce zoom pumping in normal flight

  // Push-zoom: a transient multiplicative nudge to the camera distance for scripted moments (docking
  // fly-in, jump, cutscenes). set with pushZoom(factor, duration): the factor eases in then back out
  // over the duration, multiplying targetZoom during its active window. 0 = inactive. This cooperates
  // with the dynamic-zoom system (it biases the SAME _dynamicZoom the rest of the game uses) instead
  // of clobbering c.zoom the way the old uiRoot hard-set did.
  let _pushZoom = 0;          // current multiplicative offset added to the zoom factor (0 = none)
  let _pushZoomDecay = 0;     // per-second decay rate (derived from duration at push time)
  let _snappedPlayerId = null;

  function snapToEntity(p) {
    if (!p || !p.pos) return false;
    const px = finiteOr(p.pos.x, 0);
    const pz = finiteOr(p.pos.z, 0);
    c.focus.set(px, 0, pz);
    _dynamicZoom = finiteOr(c.zoom, 95);
    computeOffset(_dynamicZoom);
    cam.position.set(c.focus.x + offset.x, offset.y, c.focus.z + offset.z);
    cam.lookAt(c.focus.x, 0, c.focus.z);
    _snappedPlayerId = p.id;
    return true;
  }

  return {
    obj: cam,
    addTrauma(amount) {
      const a = Number.isFinite(amount) ? Math.max(0, amount) : 0;
      if (a <= 0) return;
      const scale = isMotionReduced(state) ? MOTION_REDUCE_SHAKE_SCALE : 1;
      c.trauma = Math.min(1, Math.max(0, c.trauma || 0) + a * scale);
    },
    setZoom(z) { c.zoom = Math.max(45, Math.min(220, finiteOr(z, c.zoom || 95))); },
    snapToPlayer() {
      const p = state.entities.get(state.playerId);
      return snapToEntity(p);
    },
    // pushZoom(factor, durationS): factor>0 pushes the camera OUT (wider) for `durationS`, easing in
    // and out. e.g. pushZoom(0.25, 0.8) widens the view 25% over 0.8s for a dock approach reveal.
    // The effect is additive on top of the dynamic zoom and decays smoothly.
    pushZoom(factor, durationS) {
      const f = Math.max(0, factor || 0);
      const d = Math.max(0.1, durationS || 0.5);
      _pushZoom = f;
      // ease in over ~half the duration, out over the other half → symmetric decay rate
      _pushZoomDecay = 4.0 / d;
    },
    follow(dt) {
      const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 1 / 15) : 0;
      const p = state.entities.get(state.playerId);
      let fx = finiteOr(c.focus.x, 0), fz = finiteOr(c.focus.z, 0);
      let bankForLean = 0;
      let playerSpeed = 0;
      let nearbyEnemies = 0;
      let hasTetherFocus = false;
      if (p && p.pos) {
        if (_snappedPlayerId !== p.id || !Number.isFinite(c.focus.x) || !Number.isFinite(c.focus.z)) {
          snapToEntity(p);
        }
        fx = finiteOr(p.pos.x, 0); fz = finiteOr(p.pos.z, 0);
        const vx = p.vel ? finiteOr(p.vel.x, 0) : 0;
        const vz = p.vel ? finiteOr(p.vel.z, 0) : 0;
        playerSpeed = Math.hypot(vx, vz);
        const focusGap = Math.hypot(c.focus.x - fx, c.focus.z - fz);
        if (focusGap > Math.max(320, _dynamicZoom * 2.6)) {
          snapToEntity(p);
        }
        if (playerSpeed > 1) {
          const la = Math.min(c.lookAhead, CRUISE_LOOKAHEAD_MAX, playerSpeed * CRUISE_LOOKAHEAD_SPEED_SCALE);
          fx += (vx / playerSpeed) * la; fz += (vz / playerSpeed) * la;
        }
        const aimLead = resolveAimLead(state.input, p);
        fx += aimLead.x;
        fz += aimLead.z;
        const composition = resolveChaseComposition(state, p, { x: fx, z: fz });
        fx = composition.x;
        fz = composition.z;
        nearbyEnemies = composition.nearbyEnemies;
        hasTetherFocus = composition.hasTetherFocus;
        const desiredSafe = clampFocusToPlayerSafeRect({ x: fx, z: fz }, p, {
          zoom: _dynamicZoom,
          fov: cam.fov,
          aspect: cam.aspect,
        });
        fx = desiredSafe.x;
        fz = desiredSafe.z;
        // counter-lean uses the ship's bank (already smoothed); a fraction keeps it tasteful
        bankForLean = (Number.isFinite(p.bank) ? p.bank : 0) * 0.045;
      }
      const followLerp = finiteOr(c.lerp, 6);
      c.focus.x = damp(c.focus.x, fx, followLerp, frameDt);
      c.focus.z = damp(c.focus.z, fz, followLerp, frameDt);

      // --- dynamic zoom ---
      const baseZoom = finiteOr(c.zoom, 95);
      let targetZoom = baseZoom;
      if (p && p.pos) {
        let zoomFactor = 1.0;
        const composeDx = fx - finiteOr(p.pos.x, fx);
        const composeDz = fz - finiteOr(p.pos.z, fz);
        const compositionLead = Math.hypot(composeDx, composeDz);

        // combat: push in slightly and preserve threat readability. The old behavior zoomed out,
        // making fights feel detached; 47-A needs pressure, line tension, and target geometry.
        if (nearbyEnemies > 0) {
          zoomFactor = 0.90;
          if (compositionLead > 55) {
            zoomFactor = Math.min(0.98, zoomFactor + Math.min(0.08, (compositionLead - 55) / 900));
          }
        } else if (hasTetherFocus) {
          zoomFactor = 0.94;
          if (compositionLead > 55) {
            zoomFactor = Math.min(1.0, zoomFactor + Math.min(0.06, (compositionLead - 55) / 900));
          }
        } else {
          // boost: modest zoom-out for speed feel without making ordinary flight breathe too much
          if (p.flags && p.flags.boosting) zoomFactor = Math.max(zoomFactor, 1.06);
          // dash: slightly wider at high speed (proxy: speed > 110% of maxSpeed)
          if (p.maxSpeed && playerSpeed > p.maxSpeed * 1.1) zoomFactor = Math.max(zoomFactor, 1.10);
          // damage without a visible threat gets a small emergency reveal
          if (c.trauma > 0.1) zoomFactor = Math.max(zoomFactor, 1.03);
          // idle/cruising: small zoom-in when slow and peaceful
          if (p.maxSpeed && playerSpeed < p.maxSpeed * 0.15 && c.trauma <= 0.1) zoomFactor = Math.min(zoomFactor, 0.96);
        }

        targetZoom = baseZoom * zoomFactor;
      }
      // scripted push-zoom (dock fly-in / jump): widens the view multiplicatively while active, then
      // decays. Applied to targetZoom so it eases through the same _dynamicZoom damping as everything
      // else — no jarring snap, no fight with the dynamic-zoom logic.
      if (_pushZoom > 0.0001) {
        targetZoom *= (1 + _pushZoom);
        _pushZoom += -_pushZoom * _pushZoomDecay * frameDt;
        if (_pushZoom < 0.0001) _pushZoom = 0;
      }
      _dynamicZoom = damp(_dynamicZoom, targetZoom, ZOOM_LERP, frameDt);
      if (p && p.pos) {
        const safeFocus = clampFocusToPlayerSafeRect(c.focus, p, { zoom: _dynamicZoom, fov: cam.fov, aspect: cam.aspect });
        if (safeFocus.clamped) {
          c.focus.x = safeFocus.x;
          c.focus.z = safeFocus.z;
        }
      }
      computeOffset(_dynamicZoom);
      let shakeRoll = 0;
      let shakePitch = 0;
      if (c.trauma > 0) {
        c.trauma = Math.max(0, c.trauma - 1.6 * frameDt);
        const t2 = c.trauma * c.trauma;
        const shakeScale = isMotionReduced(state) ? MOTION_REDUCE_SHAKE_SCALE : 1;
        c.shakeOffset.set(
          (Math.random() * 2 - 1) * SHAKE_POS_MAX * shakeScale * t2,
          0,
          (Math.random() * 2 - 1) * SHAKE_POS_MAX * shakeScale * t2,
        );
        // GR-6: angular shake — roll + pitch jitter, trauma²-scaled. Sampled once per frame from
        // trauma so it stays coherent with the translational shake rather than vibrating independently.
        shakeRoll = (Math.random() * 2 - 1) * SHAKE_ROT_ROLL * shakeScale * t2;
        shakePitch = (Math.random() * 2 - 1) * SHAKE_ROT_PITCH * shakeScale * t2;
      } else {
        c.shakeOffset.set(0, 0, 0);
      }
      cam.position.set(c.focus.x + offset.x + c.shakeOffset.x, offset.y, c.focus.z + offset.z + c.shakeOffset.z);
      cam.lookAt(c.focus.x, 0, c.focus.z);
      // apply a gentle, damped roll in the camera's local frame — counter to the ship's bank so the
      // view tips into the turn. lookAt() set the quaternion; we post-multiply a local-Z rotation so
      // we never clobber the heading (safe with the no-yaw-follow rule).
      const targetRoll = Math.max(-ROLL_MAX, Math.min(ROLL_MAX, bankForLean));
      camRoll = damp(camRoll, targetRoll, ROLL_LERP, frameDt);
      _rollQ.setFromAxisAngle(_FORWARD, camRoll);
      cam.quaternion.multiply(_rollQ);
      // GR-6: apply rotational shake after the bank roll. Post-multiplying local-axis quats keeps the
      // shake in the camera's frame (spins the image, never drags the heading).
      if (shakeRoll) { _shakeRollQ.setFromAxisAngle(_FORWARD, shakeRoll); cam.quaternion.multiply(_shakeRollQ); }
      if (shakePitch) { _shakePitchQ.setFromAxisAngle(_camRight, shakePitch); cam.quaternion.multiply(_shakePitchQ); }
    },
    onResize() {
      cam.aspect = window.innerWidth / window.innerHeight;
      cam.updateProjectionMatrix();
    },
  };
}

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content package.json' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1270ms:
   1: // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
   2: // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
   3: // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
   4: import * as THREE from 'three';
   5: import { createChaseCamera } from './camera.js';
   6: import { createStarfield } from './starfield.js';
   7: import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
   8: import { installVisualOverrides } from './visualOverrides.js';
   9: import { createBloom } from './bloom.js';
  10: import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
  11: import { invalidateAuthoredAsset } from './assetLoader.js';
  12: import { getAuthoredInstancePoolDiagnostics, invalidatePartsLibraryCaches, preloadAuthoredPartLibrary, syncAuthoredInstancePools } from './partsLibrary.js';
  13: import { projectedWidthPx } from './lod.js';
  14: import { createCollisionDebug } from './collisionDebug.js';
  15: import { installDiagnostics } from './diagnostics.js';
  16: import { createPlanetFactory } from './planetFactory.js';
  17: import { precompilePipelines } from './precompile.js';
  18: import { SECTOR_PALETTE_CLASSES } from '../data/sectors.js';
  19: 
  20: const SECTOR_PALETTE_LERP_SECONDS = 1.5;
  21: const SECTOR_LIGHT_INTENSITIES = { ambient: 0.85, key: 1.7, rim: 0.7, fill: 0.35 };
  22: 
  23: // ---- contact shadow disc (module-level cache so one texture serves all entities) ----------------
  24: let _shadowTex = null;
  25: let _shadowGeo = null;
 208:     installVisualOverrides(vf, {
 209:       onAuthoredAssetSwap: () => { this._shadowReceiversDirty = true; },
 210:     });
 211: 
 212:     // Bake a PMREM environment map from the nebula backdrop (scene.background) so chrome/authority
 213:     // hulls can mirror the actual space around them — real reflections of the nebula + stars rather
 214:     // than a canned gradient. Done once after the starfield sets scene.background; the resulting
 215:     // envMap is exposed on state.render for the visual factory to attach to high-metalness hulls.
 216:     // Factored into a method (_bakeEnv) so WebGL context-loss recovery can re-bake it: a lost GL
 217:     // context invalidates the envMap GPU texture, and without re-baking chrome hulls go matte after
 218:     // a driver/GPU hiccup.
 219:     this._envMap = null;
 220:     try {
 221:       // wait one frame so scene.background (an async-decoded CanvasTexture) is present, then bake
 222:       const bakeEnv = () => this._bakeEnv();
 223:       setTimeout(bakeEnv, 120); // let the starfield's async background decode first
 224:     } catch (_) { /* PMREM unavailable */ }
 225: 
 226:     // WebGL context-loss recovery. The browser fires webglcontextlost when the GPU driver resets
 227:     // (driver crash, sleep/wake, VRAM exhaustion). THREE's WebGLRenderer only stops rendering on
 228:     // loss — it does NOT restore the env map, re-upload procedural textures, or rebuild GPU state,
 229:     // so without handling this the game silently freezes / goes black with no recovery path.
 230:     // On lost: preventDefault (tells the browser we'll recover), set a flag so renderFrame skips
 231:     // work while the context is gone. On restored: re-bake the PMREM env, force a full mesh
 232:     // reconciliation (re-builds every entity mesh → re-uploads geometries/materials), re-apply
 233:     // renderer config, and re-apply the video settings that drive tone mapping / shadow state.
 234:     this._contextLost = false;
 235:     if (canvas) {
 236:       canvas.addEventListener('webglcontextlost', (ev) => {
 237:         ev.preventDefault();        // allow restoration
 238:         this._contextLost = true;
 239:         if (typeof console !== 'undefined') console.warn('[render] WebGL context lost — awaiting restore');
 240:         bus.emit('toast', { text: 'Graphics context lost — recovering…', kind: 'warn', ttl: 4 });
 241:       }, false);
 242:       canvas.addEventListener('webglcontextrestored', () => {
 243:         if (typeof console !== 'undefined') console.warn('[render] WebGL context restored — rebuilding GPU resources');
 244:         this._contextLost = false;
 245:         try {
 246:           // Re-apply renderer config that the new context defaults lose.
 247:           this.renderer.setClearColor(0x060912, 1);
 248:           if (this._shadowSettingOn && this._keyLight) this.renderer.shadowMap.enabled = false; // re-gated by _syncShadowMapEnabled on next frame
 249:           // Re-bake the PMREM env (the old GPU texture is gone).
 250:           this._bakeEnv();
 251:           // Invalidate authored-asset and factory caches so the next rebuild reloads GLBs and
 252:           // recreates materials against the restored context rather than reusing stale GPU handles.
 253:           invalidateAuthoredAsset(renderer);
 254:           invalidateVisualFactoryCaches();
 255:           invalidatePartsLibraryCaches(renderer);
 256:           // Rebuild the bloom post-process pipeline (its render targets are tied to the lost context).
 257:           if (this.bloom && typeof this.bloom.rebuild === 'function') this.bloom.rebuild();
 258:           // Force every entity mesh to rebuild so geometries/materials re-upload. The cleanest way
 259:           // is to clear + reconcile: dispose the CPU mesh objects, then reconcileMeshes() rebuilds
 260:           // them from the live entityList via the visual factory.
 261:           this.clearAllMeshes(false);
 262:           this._meshReconcileDirty = true;
 263:           // Re-apply bloom + video settings (tone mapping / exposure live on settings:changed).
 264:           bus.emit('settings:changed', { section: 'video' });
 265:           bus.emit('toast', { text: 'Graphics recovered.', kind: 'good', ttl: 3 });
 266:         } catch (err) {
 267:           if (typeof console !== 'undefined') console.error('[render] context-restore rebuild failed', err);
 268:         }
 269:       }, false);
 270:     }
 271: 
 272:     // Preload the menu background (the only generated .jpg we use — the rest are captioned
 273:     // contact-sheet references and are replaced by procedural materials / inline SVG).
 274:     { const i = new Image(); i.src = 'assets/cinematics/menu_background.jpg'; }
 275: 
 276:     this.renderer = renderer; this.scene = scene; this.cam = cam; this.starfield = starfield; this.vf = vf;
 277:     this.authoredPartLibraryReady = preloadAuthoredPartLibrary(renderer).catch((error) => {
 278:       console.warn('[render] authored part library preload failed', error);
 279:       return null;
 280:     });
 281:     state.render.authoredPartLibraryReady = this.authoredPartLibraryReady;
 282:     this._sectorPaletteRig = createSectorPaletteRig(scene, ambient, key, rim, fill);
 283:     this._sectorPaletteTarget = corePalette;
 284:     state.render.sectorPalette = corePalette;
 285:     this._keyLight = shadowsOn ? key : null; // referenced by _updateShadowFollow() each frame
 286:     this._shadowSettingOn = shadowsOn;
 287:     this._shadowReceiversDirty = true;
 288:     this._shadowReceiverCount = 0;
 289:     this._contactShadowPool = createContactShadowPool(scene);
 290:     this.planetFactory = createPlanetFactory();
 291:     this._planetBodies = [];
 292:     // LOD projector viewport (CSS px); onResize refreshes it. Initialize from drawSize so the first
 293:     // frame before onResize has sane values.
 294:     { const dpr = renderer.getPixelRatio() || 1; this.viewport = { width: drawSize.x / dpr, height: drawSize.y / dpr }; }
 295:     try { this.bloom = createBloom(renderer, drawSize.x, drawSize.y); }
 296:     catch (err) { console.warn('[render] bloom unavailable, falling back:', err); this.bloom = null; }
 297:     // Collision/socket/landing-contact debug visualization (spec §12.5). OFF by default; toggled via
 298:     // the render system handle (state.render.debug.toggle) — wired to F7 in ui/input.js.
 299:     try { this.collisionDebug = createCollisionDebug(this); }
 300:     catch (err) { console.warn('[render] collision debug unavailable:', err); this.collisionDebug = null; }
 301:     this._meshes = new Map(); // entityId -> Object3D
 302:     this._meshBuildQueue = [];
 303:     this._meshBuildQueuedIds = new Set();
 304:     this._hazardVisuals = []; // hazard zone visual meshes for the current sector
 305:     this._meshReconcileDirty = true;
 306:     this._initialMeshReconcileComplete = false;
 307:     // Renderer diagnostics: window.__THREE_GAME_DIAGNOSTICS__ (draw calls/tris/memory + frame timing).
 308:     try {
 309:       this.diag = installDiagnostics(renderer, {
 310:         entities: () => state.entityList.length,
 311:         particles: () => {
 312:           const sys = ctx.registry && ctx.registry.get('vfx');
 313:           return sys ? sys._liveCount : 0;
 314:         },
 315:         sprites: () => {
 316:           const sys = ctx.registry && ctx.registry.get('vfx');
 317:           return sys ? (sys._liveSpriteCount || 0) : 0;
 318:         },
 319:         lights: () => {
 320:           const sys = ctx.registry && ctx.registry.get('vfx');
 321:           const pool = sys && sys._lights;
 322:           if (!pool) return 0;
 323:           let n = 0;
 324:           for (const slot of pool) if (slot && slot.obj && slot.obj.visible) n++;
 325:           return n;
 326:         },
 327:         perf: () => state.perfRuntime && state.perfRuntime.getReport ? state.perfRuntime.getReport() : {},
 328:         settings: () => ({ video: { ...((state.settings && state.settings.video) || {}) } }),
 329:         scenePools: () => getAuthoredInstancePoolDiagnostics(scene),
 330:         post: () => ({
 331:           activePath: this._lastRenderPath || null,
 332:           bloomSelected: !!(this.bloom && state.settings && state.settings.video && state.settings.video.bloom !== false),
 333:           bloom: this.bloom && typeof this.bloom.diagnostics === 'function' ? this.bloom.diagnostics() : null,
 334:           renderGraph: !!this._renderGraph,
 335:         }),
 336:       });
 337:       state.render.diagnostics = this.diag;
 338:     }
 339:     catch (err) { console.warn('[render] diagnostics unavailable:', err); this.diag = null; }
 340: 
 341:     state.render.scene = scene;
 342:     state.render.renderer = renderer;
 343:     state.render.camera = cam.obj;
 344:     state.render.cameraCtrl = cam;   // controller (addTrauma/pushZoom) — exposed for feel.js / ui
 345:     state.render.vf = vf;   // exposed for the dev-only ship turntable preview (shipPreview.js)
 346:     state.render.warmPostProcess = () => (this.bloom && state.settings.video.bloom !== false ? this.bloom.render(scene, cam.obj) : renderer.render(scene, cam.obj));
 347:     // Collision/socket/landing debug toggle (spec §12.5), bound to F7 in ui/input.js. Capture the
 348:     // render-system `this` once so the handle closures resolve the live collisionDebug regardless of
 349:     // how they're invoked (method `this` would otherwise bind to the debug handle object itself).
 350:     const renderSys = this;
 351:     state.render.debug = {
 352:       get on() { return renderSys.collisionDebug ? renderSys.collisionDebug.on : false; },
 353:       toggle: () => renderSys.collisionDebug ? renderSys.collisionDebug.toggle() : false,
 354:       set: (v) => { if (renderSys.collisionDebug) renderSys.collisionDebug.setDebug(v); },
 355:     };
 356:     state.camera.obj = cam.obj;
 357: 
 358:     ctx.helpers.worldToScreen = (v) => this.worldToScreen(v);
 359:     ctx.helpers.raycastToPlane = (ndc) => this.raycastToPlane(ndc);
 360:     ctx.helpers.addTrauma = (a) => cam.addTrauma(a);
 361:     ctx.helpers.socketWorldPose = (id, name) => this.socketWorldPose(id, name);
 362:     ctx.helpers.socketWorldPos = (id, name) => this.socketWorldPos(id, name);
 363: 
 364:     bus.on('entity:spawned', () => { this._meshReconcileDirty = true; });
 365:     bus.on('entity:destroyed', ({ id }) => {
 366:       const m = this._meshes.get(id);
 367:       if (m) { scene.remove(m); disposeObject(m); this._meshes.delete(id); }
 368:     });
 369:     // Ship hull swap or loadout change (fit/upgrade) — rebuild the mesh so visible hardpoints,
 370:     // engines and tier reflect the current ship. Without this the mesh is frozen at spawn and a
 371:     // shipyard hull switch or fitted weapon never shows. Mirrors the spawn path: dispose old,
 372:     // build new, re-seat from the entity's live transform.
 373:     bus.on('ship:appearanceChanged', ({ id }) => render.rebuildShipMesh(id));
 374:     bus.on('camera:shake', ({ amount }) => cam.addTrauma(amount || 0.3));
 375:     bus.on('camera:zoom', ({ delta, level }) => { if (level != null) cam.setZoom(level); else cam.setZoom(state.camera.zoom + (delta || 0)); });
 376:     bus.on('game:started', () => cam.snapToPlayer && cam.snapToPlayer());
 377:     bus.on('save:loaded', () => cam.snapToPlayer && cam.snapToPlayer());
 378:     bus.on('player:respawn', () => cam.snapToPlayer && cam.snapToPlayer());
 379:     // Live-apply video settings changes. Without this, dragging Bloom strength / FOV / particle
 380:     // quality in the settings screen did nothing (only the initial value was used) — a "slider that
 408:       if (cam.snapToPlayer) cam.snapToPlayer();
 409:       this._updatePlanetBodies(sector);
 410:       this._beginSectorPaletteTransition(sector);
 411:       this._updateHazardVisuals(sector);
 412:       precompilePipelines(renderer, scene, cam.obj, { sector }).catch((error) => console.warn('[render] sector pipeline precompile failed', error));
 413:     });
 414:     bus.on('jump:arrive', ({ sectorId } = {}) => {
 415:       const sector = sectorId && state.world && state.world.sectors ? state.world.sectors[sectorId] : null;
 416:       this._beginSectorPaletteTransition(sector);
 417:     });
 418:     bus.on('save:loaded', () => { this._meshReconcileDirty = true; });
 419: 
 420:     window.addEventListener('resize', () => this.onResize());
 421:   },
 422: 
 423:   clearAllMeshes(keepPlayer) {
 424:     for (const [id, m] of [...this._meshes]) {
 425:       if (keepPlayer && id === this.state.playerId) continue;
 426:       this.scene.remove(m); disposeObject(m); this._meshes.delete(id);
 427:     }
 428:     this._meshBuildQueue.length = 0;
 429:     this._meshBuildQueuedIds.clear();
 430:     // Also clear hazard zone visuals
 730: 
 731:   prepareFrame(alpha, frameDt) {
 732:     // While the GL context is lost, the renderer can't draw — skip all per-frame work until
 733:     // webglcontextrestored rebuilds GPU resources. (cam.follow etc. would run against a dead
 734:     // renderer; the context-restore handler re-applies everything that matters when it returns.)
 735:     if (this._contextLost) return false;
 736:     if (this._meshReconcileDirty) this.reconcileMeshes();
 737:     this.syncEntityViews(alpha);
 738:     this.cam.follow(frameDt);
 739:     syncContactShadowPool(this._contactShadowPool, this.state.entityList, this._meshes);
 740:     syncAuthoredInstancePools(this.scene, { camera: this.cam.obj });
 741:     this.starfield.recenter(this.cam.obj.position);
 742:     // Background-clock for distant animation (planet cloud drift, hero-star twinkle). Integrates real
 743:     // frame dt scaled by state.timeScale so the cosmos respects hit-stop/pause — a death freeze
 744:     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
 745:     this._updateSectorPaletteTransition(frameDt);
 746:     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
 747:     this._bgTime = (this._bgTime || 0) + frameDt * ts;
 748:     if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
 749:     this._updatePlanetParallax();
 750:     this._syncShadowMapEnabled();
 751:     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
 752:     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is
 753:     // an offset from its target; we move both together. No-op if shadows are disabled.
 754:     this._updateShadowFollow();
 755:     // Collision/socket/landing debug overlay (spec §12.5). Repositions pooled markers over the live

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$i=0; Get-Content src'"\\render\\vfx.js | ForEach-Object { "'$i++; if ($i -ge 134 -and $i -le 230) { '"'{0,4}: {1}' -f "'$i, $_ } }' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1263ms:
{
  "name": "spaceface",
  "version": "0.1.0",
  "description": "SpaceFace — a semi-3D top-down space trading/combat/mining game (Three.js).",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "node server.js",
    "serve": "node server.js 8123",
    "electron": "electron .",
    "dist": "npm run build:bundle && electron-builder",
    "sf": "node scripts/sf.mjs",
    "check:contracts": "node scripts/sf.mjs validate test/47a.inputs.json test/47a.telemetry.expected.json src/data/scenarios/47a.scenario.json",
    "check:replay": "node scripts/sf.mjs replay verify test/47a.inputs.json --seed 47 --ticks 720 --expect test/47a.telemetry.expected.json --hash --repeat 20 --reload-at 600",
    "check:sim": "node scripts/sf-sim.mjs run 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.expected.json --hash --repeat 20 --reload-at 600",
    "check:sim:compare": "node scripts/sf-sim.mjs compare 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.expected.json --reload-at 600",
    "check:sim:dynamic": "node scripts/sf-sim.mjs compare 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.expected.json --reload-at 600",
    "check:sim:profile": "node scripts/sf-sim.mjs profile 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.expected.json",
    "check:sim:long": "node scripts/sf-sim.mjs run 47a --seed 47 --ticks 36120 --inputs test/47a.inputs.json --hash",
    "check:sim:long:compare": "node scripts/sf-sim.mjs compare 47a --seed 47 --ticks 36120 --inputs test/47a.inputs.json --reload-at 12000",
    "check:sim:v3": "node scripts/sf-sim.mjs run 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.v3.expected.json --hash --repeat 20 --reload-at 600 --flight-system v3",
    "check:sim:v3:compare": "node scripts/sf-sim.mjs compare 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --expect test/47a.telemetry.v3.expected.json --reload-at 600 --flight-system v3",
    "check:47a:tactics": "node scripts/check-47a-tactics.mjs",
    "check:47a:live-branch": "node scripts/check-47a-live-branch-predicate.mjs",
    "check:47a:live-cold-open": "node scripts/check-47a-live-cold-open.mjs",
    "check:47a:counterplay": "node scripts/check-47a-counter-tether-live.mjs",
    "check:47a:death-retry": "node scripts/check-47a-death-retry.mjs",
    "check:gameover-recovery-copy": "node scripts/check-gameover-recovery-copy.mjs",
    "check:47a:visuals": "node scripts/check-47a-visual-assets.mjs",
    "check:slice-scope": "node scripts/check-slice-scope.mjs",
    "check:balance": "node scripts/balance-sim.mjs",
    "check:mining:2": "node scripts/check-mining-2.mjs",
    "check:presentation": "node scripts/check-presentation-cues.mjs && node scripts/check-sg08-golden-trace.mjs && node scripts/check-sg08-mix-profile.mjs && node scripts/check-sg08-render-vfx.mjs",
    "check:camera": "node scripts/check-camera-composition.mjs",
    "check:sg05": "node scripts/check-sg05-scenario.mjs && node scripts/check-sg05-runtime.mjs && node scripts/check-sg05-branch-policies.mjs && node scripts/check-sg05-ui-lifecycle.mjs",
    "check:sg02:intake": "node scripts/check-sg02-intake.mjs",
    "check:physics-authority": "node scripts/check-physics-authority.mjs",
    "check:sg02:dynamic-lab": "node scripts/check-sg02-dynamic-body-owner.mjs",
    "check:sg02:authority": "node scripts/check-sg02-authority.mjs",
    "check:sg02:production-combat": "node scripts/check-sg02-production-combat-port.mjs",
    "check:sg02:tether": "node scripts/check-sg02-tether.mjs",
    "check:sg02:tether-break": "node scripts/check-sg02-tether-break.mjs",
    "check:sg02:dash-collision": "node scripts/check-sg02-dash-collision.mjs",
    "check:sg02:save-reload": "node scripts/check-sg02-save-reload.mjs",
    "check:sg02": "npm run check:sg02:intake && npm run check:physics-authority && npm run check:sg02:dynamic-lab && node scripts/check-sg02-authority.mjs && node scripts/check-sg02-production-combat-port.mjs && node scripts/check-sg02-tether.mjs && node scripts/check-sg02-tether-break.mjs && node scripts/check-sg02-dash-collision.mjs && node scripts/check-sg02-save-reload.mjs && npm run check:sim:dynamic",
    "check:sg06:intake": "node scripts/check-sg06-intake.mjs",
    "check:sg06:production-ports": "node scripts/check-sg06-production-ports.mjs",
    "check:sg06:encounter-sink": "node scripts/check-sg06-encounter-sink.mjs",
    "check:sg06:encounter-owner": "node scripts/check-sg06-encounter-owner.mjs",
    "check:sg06:live-shadow": "node scripts/check-sg06-live-shadow.mjs",
    "check:sg06:registry-init": "node scripts/check-sg06-registry-init.mjs",
    "check:sg06:live-registry": "node scripts/check-sg06-live-registry.mjs",
    "check:sg06:tether-break": "node scripts/check-sg06-live-tether-break.mjs",
    "check:sg06:formation": "node scripts/check-sg06-rapier-formation-convergence.mjs",
    "check:sg06:ai": "node scripts/check-sg06-ai.mjs --runs=100 --ticks=600",
    "check:ai:telegraphs": "node scripts/check-ai-telegraphs.mjs",
    "check:ai": "npm run check:sg06:ai",
    "check:sg06": "npm run check:sg06:intake && npm run check:sg06:production-ports && npm run check:sg06:encounter-sink && npm run check:sg06:encounter-owner && npm run check:sg06:live-shadow && npm run check:sg06:registry-init && npm run check:sg06:live-registry && npm run check:sg06:tether-break && npm run check:sg06:formation && npm run check:sg06:ai",
    "check:sg03:save-reload": "node scripts/check-sg03-save-reload.mjs",
    "check:save-resume-confidence": "node scripts/check-save-resume-confidence.mjs",
    "check:title-continue-runtime": "node scripts/check-title-continue-runtime.mjs",
    "check:save-load-slot-trust": "node scripts/check-save-load-slot-trust.mjs",
    "check:settings-profile": "node scripts/check-settings-profile-persistence.mjs",
    "check:controls-discoverability": "node scripts/check-controls-discoverability.mjs",
    "check:cargo-jettison-copy": "node scripts/check-cargo-jettison-copy.mjs",
    "check:player-facing-labels": "node scripts/check-player-facing-labels.mjs",
    "check:new-game-first-run": "node scripts/check-new-game-first-run-rail.mjs",
    "check:pause-brief": "node scripts/check-pause-brief.mjs",
    "check:confirm-dialog": "node scripts/check-confirm-dialog-safety.mjs",
    "check:departure-prompts": "node scripts/check-departure-check-prompts.mjs",
    "check:first-dock-handoff": "node scripts/check-first-dock-handoff.mjs",
    "check:first-15-runtime": "node scripts/check-first-15-runtime.mjs",
    "check:combat": "node scripts/check-combat-grammar.mjs && npm run check:sg03:save-reload",
    "bench:combat": "node scripts/bench-combat.mjs",
    "build:sg04:release-assets": "node scripts/build-sg04-release-assets.mjs",
    "build:bundle": "node scripts/build-bundle.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check:launch-policy": "node scripts/check-launch-policy.mjs",
    "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
    "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
    "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
    "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
    "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
    "check:hitch-budget": "node scripts/check-hitch-budget.mjs",
    "check:radar:perf": "node scripts/check-radar-perf.mjs",
    "check:ui:perf": "node scripts/check-ui-frame-sleep.mjs && npm run check:radar:perf",
    "check:bar:narrative": "node scripts/check-bar-canonical-contacts.mjs",
    "check:bar:mission-readiness": "node scripts/check-bar-mission-readiness-live.mjs",
    "check:station-departure": "node scripts/check-station-departure-readiness.mjs",
    "check:station-egress": "node scripts/check-station-egress-runtime.mjs",
    "check:station-tabs": "node scripts/check-station-tab-navigation-runtime.mjs",
    "check:station-interact-undock": "node scripts/check-station-interact-undock.mjs",
    "check:faction-standings": "node scripts/check-faction-standings-guidance.mjs",
    "check:mission-standing-ladder": "node scripts/check-mission-standing-ladder.mjs",
    "check:manufacture-guidance": "node scripts/check-manufacture-guidance.mjs",
    "check:crafting-guidance": "node scripts/check-crafting-guidance.mjs",
    "check:services-readiness": "node scripts/check-services-readiness.mjs",
    "check:outfitting-buy-fit": "node scripts/check-outfitting-buy-fit.mjs",
    "check:shipyard-guidance": "node scripts/check-shipyard-guidance.mjs",
    "check:ship-purchase-guidance": "node scripts/check-ship-purchase-guidance.mjs",
    "check:mission-board-recommendation": "node scripts/check-mission-board-recommendation.mjs",
    "check:mission-preflight": "node scripts/check-mission-preflight.mjs",
    "check:mission-handoff": "node scripts/check-mission-accept-handoff-runtime.mjs",
    "check:mission-cargo-loading": "node scripts/check-mission-cargo-loading-runtime.mjs",
    "check:mission-navigation": "node scripts/check-mission-navigation.mjs",
    "check:starmap-objective": "node scripts/check-starmap-objective-handoff.mjs",
    "check:mission-log-map": "node scripts/check-mission-log-map-handoff.mjs",
    "check:mission-log-map:runtime": "node scripts/check-mission-log-map-runtime.mjs",
    "check:mission-log-contract-terms": "node scripts/check-mission-log-contract-terms.mjs",
    "check:mission-receipts": "node scripts/check-mission-receipt-ledger.mjs",
    "check:automation-board": "node scripts/check-automation-ops-board.mjs",
    "check:automation-board:runtime": "node scripts/check-automation-board-runtime.mjs",
    "check:recommended-next": "node scripts/check-recommended-next-action.mjs",
    "check:tech-tree-guidance": "node scripts/check-tech-tree-guidance.mjs",
    "check:tech-display-names": "node scripts/check-tech-display-names.mjs",
    "check:market-nav": "node scripts/check-market-navigation.mjs",
    "check:market-first-loop": "node scripts/check-market-first-loop-runtime.mjs",
    "check:localmap-routes": "node scripts/check-localmap-route-metrics.mjs",
    "check:claim-base": "node scripts/check-claim-base-input.mjs",
    "check:base-build-guidance": "node scripts/check-base-build-guidance.mjs",
    "check:claims-guidance": "node scripts/check-claims-guidance.mjs",
    "check:flight:v3": "node test/flightV3.spec.mjs",
    "check:flight": "node test/flightV3.spec.mjs && node scripts/flight-lab-sim.mjs && node scripts/probe-flight-visual.mjs",
    "check:flight:clean": "node test/flightV3.spec.mjs && node scripts/flight-lab-sim.mjs && node scripts/probe-flight-visual.mjs --clean-runs 5 --strict-warnings --no-write",
    "check:art": "node scripts/check-kestrel-asset.mjs && node scripts/check-parts-manifest.mjs && node scripts/check-sg04-release-assets.mjs && node scripts/check-kestrel-hero.mjs && node scripts/check-kestrel-silhouette.mjs && node scripts/check-kestrel-damage.mjs && node scripts/check-kestrel-leak.mjs && node scripts/check-lod.mjs && node scripts/check-collision-debug.mjs && node scripts/check-concord-patrol.mjs && node scripts/check-reaver-pirate.mjs && node scripts/check-faction-ships.mjs && npm run check:47a:visuals",
    "check": "npm run check:launch-policy && node scripts/check-data.mjs && node scripts/check-data-refs.mjs && npm run check:balance && npm run check:sg02 && npm run check:sg05 && npm run check:sg06 && npm run check:combat && npm run check:save-resume-confidence && npm run check:title-continue-runtime && npm run check:save-load-slot-trust && npm run check:settings-profile && npm run check:controls-discoverability && npm run check:cargo-jettison-copy && npm run check:player-facing-labels && npm run check:new-game-first-run && npm run check:pause-brief && npm run check:confirm-dialog && node scripts/check-ui-screen-imports.mjs && node scripts/check-ui-a11y.mjs && npm run check:ui:perf && npm run check:bar:narrative && npm run check:bar:mission-readiness && npm run check:station-departure && npm run check:station-egress && npm run check:station-tabs && npm run check:station-interact-undock && npm run check:faction-standings && npm run check:mission-standing-ladder && npm run check:manufacture-guidance && npm run check:crafting-guidance && npm run check:services-readiness && npm run check:outfitting-buy-fit && npm run check:shipyard-guidance && npm run check:ship-purchase-guidance && node scripts/check-codex-narrative.mjs && node scripts/check-wcag-contrast.mjs && node scripts/check-input-modalities.mjs && node scripts/check-countermeasures.mjs && node scripts/check-onboarding.mjs && npm run check:first-dock-handoff && npm run check:departure-prompts && npm run check:mission-board-recommendation && npm run check:mission-preflight && npm run check:mission-handoff && npm run check:mission-cargo-loading && npm run check:mission-navigation && npm run check:starmap-objective && npm run check:mission-log-map && npm run check:mission-log-contract-terms && npm run check:mission-receipts && npm run check:automation-board && npm run check:recommended-next && npm run check:tech-tree-guidance && npm run check:tech-display-names && npm run check:first-15-runtime && npm run check:market-nav && npm run check:market-first-loop && npm run check:localmap-routes && npm run check:claim-base && npm run check:base-build-guidance && npm run check:claims-guidance && node scripts/check-wingmen.mjs && npm run check:contracts && npm run check:replay && npm run check:presentation && npm run check:camera && node scripts/check-gameplay-core.mjs && npm run check:sim && npm run check:sim:compare && npm run check:sim:profile && npm run check:sim:long && npm run check:sim:long:compare && npm run check:47a:tactics && npm run check:47a:live-branch && npm run check:47a:live-cold-open && npm run check:47a:counterplay && npm run check:47a:death-retry && npm run check:gameover-recovery-copy && npm run check:slice-scope && node scripts/check-phase0-slice-contract.mjs && npm run check:flight:v3 && npm run check:flight:clean && node scripts/check-sectorSim.mjs && npm run check:art && npm run check:bundle",
    "check:ci": "npm run check:launch-policy && node scripts/check-data.mjs && node scripts/check-data-refs.mjs && npm run check:balance && npm run check:sg02 && npm run check:sg05 && npm run check:sg06 && npm run check:combat && npm run check:save-resume-confidence && npm run check:title-continue-runtime && npm run check:save-load-slot-trust && npm run check:settings-profile && npm run check:controls-discoverability && npm run check:cargo-jettison-copy && npm run check:player-facing-labels && npm run check:new-game-first-run && npm run check:pause-brief && npm run check:confirm-dialog && node scripts/check-ui-screen-imports.mjs && node scripts/check-ui-a11y.mjs && npm run check:ui:perf && npm run check:bar:narrative && npm run check:bar:mission-readiness && npm run check:station-departure && npm run check:station-egress && npm run check:station-tabs && npm run check:station-interact-undock && npm run check:faction-standings && npm run check:mission-standing-ladder && npm run check:manufacture-guidance && npm run check:crafting-guidance && npm run check:services-readiness && npm run check:outfitting-buy-fit && npm run check:shipyard-guidance && npm run check:ship-purchase-guidance && node scripts/check-codex-narrative.mjs && node scripts/check-wcag-contrast.mjs && node scripts/check-input-modalities.mjs && node scripts/check-countermeasures.mjs && node scripts/check-onboarding.mjs && npm run check:first-dock-handoff && npm run check:departure-prompts && npm run check:mission-board-recommendation && npm run check:mission-preflight && npm run check:mission-handoff && npm run check:mission-cargo-loading && npm run check:mission-navigation && npm run check:starmap-objective && npm run check:mission-log-map && npm run check:mission-log-contract-terms && npm run check:mission-receipts && npm run check:automation-board && npm run check:recommended-next && npm run check:tech-tree-guidance && npm run check:tech-display-names && npm run check:first-15-runtime && npm run check:market-nav && npm run check:market-first-loop && npm run check:localmap-routes && npm run check:claim-base && npm run check:base-build-guidance && npm run check:claims-guidance && node scripts/check-wingmen.mjs && npm run check:contracts && npm run check:replay && npm run check:presentation && npm run check:camera && node scripts/check-gameplay-core.mjs && npm run check:sim && npm run check:sim:compare && npm run check:sim:profile && npm run check:sim:long && npm run check:sim:long:compare && npm run check:sim:v3 && npm run check:47a:tactics && npm run check:47a:live-branch && npm run check:47a:counterplay && npm run check:47a:death-retry && npm run check:gameover-recovery-copy && npm run check:slice-scope && node scripts/check-phase0-slice-contract.mjs && node scripts/check-sectorSim.mjs && npm run check:art && npm run check:bundle"
  },
  "private": true,
  "devDependencies": {
    "@gltf-transform/core": "^4.4.0",
    "@gltf-transform/extensions": "^4.4.0",
    "@gltf-transform/functions": "^4.4.0",
    "electron": "^31.3.0",
    "electron-builder": "^24.13.3",
    "esbuild": "^0.28.1",
    "ktx2-encoder": "^0.5.3",
    "meshoptimizer": "^1.1.1",
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0"
  },
  "build": {
    "appId": "com.spaceface.game",
    "productName": "SpaceFace",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/web/**",
      "electron/**",
      "package.json",
      "assets/cinematics/**",
      "assets/ui/**",
      "assets/ships/**"
    ],
    "win": {
      "target": "nsis",
      "artifactName": "SpaceFace-Setup-${version}.${ext}"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "0.19.3",
    "three": "0.184.0"
  }
}

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"settings\\.video|particleQuality|motionReduce|video:|settings:changed|render\\.\" src\\core\\gameState.js src\\ui\\screens\\settings.js src\\systems src\\render -g \"*.js\"" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1668ms:
 134:   _initPools() {
 135:     const state = this.state;
 136:     const scene = state.render && state.render.scene;
 137:     if (!scene) { this._scene = null; return; } // render not up yet (e.g. unit test) — degrade to no-op
 138:     this._scene = scene;
 139: 
 140:     const q = (state.settings.video && state.settings.video.particleQuality) || 'high';
 141:     const cap = PARTICLE_CAP[q] || PARTICLE_CAP.high;
 142:     this._cap = cap;
 143:     this._burst = QUALITY_BURST[q] || 1.0; // scales discrete-effect spawn counts
 144: 
 145:     this._initEventLights();
 146:     this._initRibbonTrails();
 147:     this._initMiningBeam();
 148:     this._initTetherCable();
 149:     // ---- GPU point cloud ----
 150:     const geo = new THREE.BufferGeometry();
 151:     const positions = new Float32Array(cap * 3);
 152:     const colors = new Float32Array(cap * 3);
 153:     const sizes = new Float32Array(cap);
 154:     const alphas = new Float32Array(cap);
 155:     geo.setAttribute('position', new THREE.BufferAttribute(positions, 3));
 156:     geo.setAttribute('aColor', new THREE.BufferAttribute(colors, 3));
 157:     geo.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
 158:     geo.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
 159:     geo.setDrawRange(0, 0);
 160: 
 161:     const mat = new THREE.ShaderMaterial({
 162:       uniforms: { uScale: { value: 520 } },
 163:       vertexShader: PARTICLE_VERT,
 164:       fragmentShader: PARTICLE_FRAG,
 165:       blending: THREE.AdditiveBlending,
 166:       depthWrite: false,
 167:       depthTest: true,
 168:       transparent: true,
 169:     });
 170: 
 171:     const points = new THREE.Points(geo, mat);
 172:     points.frustumCulled = false; // particles are world-scattered; never cull the whole cloud
 173:     points.renderOrder = 10;
 174:     scene.add(points);
 175: 
 176:     this._points = points;
 177:     this._pGeo = geo;
 178:     this._pPos = positions;
 179:     this._pCol = colors;
 180:     this._pSize = sizes;
 181:     this._pAlpha = alphas;
 182: 
 183:     // per-particle CPU state (Structure-of-Arrays; index == particle slot)
 184:     this._px = new Float32Array(cap);
 185:     this._py = new Float32Array(cap);
 186:     this._pz = new Float32Array(cap);
 187:     this._vx = new Float32Array(cap);
 188:     this._vy = new Float32Array(cap);
 189:     this._vz = new Float32Array(cap);
 190:     this._age = new Float32Array(cap);
 191:     this._life = new Float32Array(cap);
 192:     this._drag = new Float32Array(cap);
 193:     this._size0 = new Float32Array(cap);
 194:     this._size1 = new Float32Array(cap);
 195:     this._cr0 = new Float32Array(cap); this._cg0 = new Float32Array(cap); this._cb0 = new Float32Array(cap);
 196:     this._cr1 = new Float32Array(cap); this._cg1 = new Float32Array(cap); this._cb1 = new Float32Array(cap);
 197:     this._alive = new Uint8Array(cap);
 198:     this._head = 0;        // round-robin allocation cursor
 199:     this._liveCount = 0;
 200:     this._pDrawMax = 0;
 201:     this._activeParticles = new Int32Array(cap);
 202:     this._activeParticlePos = new Int32Array(cap);
 203:     this._activeParticlePos.fill(-1);
 204:     this._freeParticles = new Int32Array(cap);
 205:     for (let i = 0; i < cap; i++) this._freeParticles[i] = cap - 1 - i;
 206:     this._freeParticleCount = cap;
 207: 
 208:     // ---- discrete sprite pool (flash / ring / puff / fresnel) ----
 209:     // Two shared textures: a filled radial glow (flash/puff) and a hollow ring (shockwave/fresnel).
 210:     const tex = makeGlowTexture();
 211:     const ringTex = makeRingTexture();
 212:     this._glowTex = tex;
 213:     this._ringTex = ringTex;
 214: 
 215:     // NOTE: the generated assets/fx/*.jpg are LABELLED contact-sheet references, not usable sprite
 216:     // strips, so VFX is driven entirely by the clean procedural glow/ring textures above.
 217:     this._spritePool = [];
 218:     this._spr = []; // parallel CPU state
 219:     for (let i = 0; i < SPRITE_CAP; i++) {
 220:       const glowMaterial = new THREE.SpriteMaterial({ map: tex, color: 0xffffff, blending: THREE.AdditiveBlending, depthWrite: false, depthTest: true, transparent: true, opacity: 0 });
 221:       const ringMaterial = new THREE.SpriteMaterial({ map: ringTex, color: 0xffffff, blending: THREE.AdditiveBlending, depthWrite: false, depthTest: true, transparent: true, opacity: 0 });
 222:       const s = new THREE.Sprite(glowMaterial);
 223:       s.userData.glowMaterial = glowMaterial;
 224:       s.userData.ringMaterial = ringMaterial;
 225:       s.visible = false;
 226:       s.frustumCulled = false;
 227:       s.renderOrder = 11;
 228:       scene.add(s);
 229:       this._spritePool.push(s);
 230:       this._spr.push({ alive: false, kind: SPR_FLASH, age: 0, life: 1, size0: 1, size1: 1, op0: 1, op1: 0, x: 0, y: 0, z: 0, vx: 0, vz: 0, roll: 0 });

 succeeded in 2746ms:
src\core\gameState.js:15:    video: { renderScale: 0.85, bloom: true, bloomStrength: 0.9, bloomThreshold: 0.65, vsync: true, fov: 50, particleQuality: 'medium', pixelRatioCap: 2, motionReduce: false, shadows: false, energyMaterials: true, renderGraph: false },
src\core\gameState.js:22:    // Accessibility (V2 §9/§12). motionReduce lives under video (feel/vfx read it there); uiScale is the
src\ui\screens\settings.js:3:// emits settings:changed {section,key,value} (audio/render/save listen + live-apply/profile-persist).
src\ui\screens\settings.js:193:    ctx.bus.emit('settings:changed', { section, key, value });
src\ui\screens\settings.js:256:      rowSelect('Particle quality', () => vd.particleQuality, [['low', 'Low'], ['medium', 'Medium'], ['high', 'High']], (v) => this._set(ctx, 'video', 'particleQuality', v));
src\ui\screens\settings.js:260:      // feel module reads settings.video.motionReduce every trigger, so toggling takes effect now.
src\ui\screens\settings.js:261:      rowToggle('Reduce motion', () => !!vd.motionReduce, (v) => this._set(ctx, 'video', 'motionReduce', v));
src\ui\screens\settings.js:288:      rowToggle('Reduce motion', () => !!s.video.motionReduce, (v) => this._set(ctx, 'video', 'motionReduce', v));
src\ui\screens\settings.js:429:      ctx.bus.emit('settings:changed', { section: 'controls', key: 'bindings', value: null });
src\ui\screens\settings.js:503:    ctx.bus.emit('settings:changed', { section: 'controls', key: action, value: s.controls.bindings[action] });
src\render\feel.js:14://     the camera via state.render.camera (a PerspectiveCamera) and restore its projection matrix.
src\render\feel.js:17://   - Everything is gated on state.settings.video.motionReduce (new): vestibular-sensitive players
src\render\feel.js:19://     table stakes (V2 §9, §12). Default OFF (motionReduce=false) so the punch is felt by default.
src\render\feel.js:82:    // (FOV base is derived live from settings.video.fov each frame — no cached field, so the FOV
src\render\feel.js:281:      const mr = this.state.settings && this.state.settings.video && this.state.settings.video.motionReduce;
src\render\feel.js:288:      const ctrl = this.state.render && this.state.render.cameraCtrl;
src\render\feel.js:300:      && !(this.state.settings && this.state.settings.video && this.state.settings.video.motionReduce);
src\render\feel.js:301:    const _warpCtrl = () => this.state.render && this.state.render.cameraCtrl;
src\render\feel.js:343:    const mr = this.state.settings && this.state.settings.video && this.state.settings.video.motionReduce;
src\render\feel.js:416:    const cam = this.state.render && this.state.render.camera;
src\render\feel.js:419:      // punch cooperate: the slider sets settings.video.fov (renderer live-applies it), and we add
src\render\feel.js:422:      const baseFov = (this.state.settings && this.state.settings.video && this.state.settings.video.fov) || cam.fov || 50;
src\render\diagnostics.js:8://   render.renderFrame / vfx / feel / ui — all of which run after the draw and add no draw calls):
src\render\diagnostics.js:13://   renderer.info.render.{calls,triangles,points,lines} are PER-render() counters. By default
src\render\diagnostics.js:22://   info.render.* over-accumulate; calling it twice halves the reported counts.
src\render\diagnostics.js:133:    // Mirror renderer.info IN PLACE (no allocation). render.* has accumulated across every pass.
src\render\diagnostics.js:137:        info.calls = ri.render.calls | 0;
src\render\diagnostics.js:138:        info.triangles = ri.render.triangles | 0;
src\render\diagnostics.js:139:        info.points = ri.render.points | 0;
src\render\diagnostics.js:140:        info.lines = ri.render.lines | 0;
src\render\diagnostics.js:148:      // We own the reset (autoReset disabled): clear render.* now so NEXT frame starts at zero and
src\render\camera.js:27:  return !!(state && state.settings && state.settings.video && state.settings.video.motionReduce);
src\render\camera.js:155:  const cam = new THREE.PerspectiveCamera(state.settings.video.fov || 50, window.innerWidth / window.innerHeight, 1, 14000);
src\systems\presentationAdapters.js:117:    const motionReduced = !!(this.state && this.state.settings && this.state.settings.video && this.state.settings.video.motionReduce);
src\systems\presentationAdapters.js:118:    const amount = round4(base * (motionReduced ? 0.25 : 1));
src\systems\presentationAdapters.js:123:      reducedMotion: motionReduced,
src\systems\presentationAdapters.js:130:    return { event: 'camera:shake', amount, reducedMotion: motionReduced };
src\systems\presentationAdapters.js:196:      reducedMotion: !!(this.state && this.state.settings && this.state.settings.video && this.state.settings.video.motionReduce),
src\systems\touch.js:250:      if (bus && bus.emit) bus.emit('settings:changed', { section: 'controls' });
src\render\bloom.js:228:  // tunables (overridable via setOptions; defaults match settings.video.*)
src\render\bloom.js:442:  // Accept partial option updates (wired from settings:changed by the render layer).
src\render\bloom.js:446:    if (typeof o.bloom === 'boolean') enabled = o.bloom; // settings.video.bloom alias
src\render\bloom.js:453:    // cinematic post grade (cyberpunk-noir) — adjustable via settings.video.*
src\render\vfx.js:4:// animation frame inside renderFrame (after render.draw), so it integrates/ages pools and the new
src\render\vfx.js:136:    const scene = state.render && state.render.scene;
src\render\vfx.js:140:    const q = (state.settings.video && state.settings.video.particleQuality) || 'high';
src\render\vfx.js:1511:      if (this.state.render && this.state.render.scene) { this._initPools(); this._subscribeOnce(); }
src\render\vfx.js:1537:  // replacement for it. Gated on settings.video.energyMaterials (and bloom, since HDR radiance
src\render\vfx.js:1541:    const video = this.state.settings && this.state.settings.video;
src\render\vfx.js:1785:    const v = this.state.settings && this.state.settings.video;
src\render\vfx.js:1787:    if (v && (v.motionReduce || v.particleQuality === 'low')) { this._lights = null; return; }
src\render\shipPreview.js:8:// exposed on the render system (window.SF.state.render.*) and the public visual factory.
src\render\shipPreview.js:115:  const renderer = state.render && state.render.renderer;
src\render\shipPreview.js:116:  const scene = state.render && state.render.scene;
src\render\shipPreview.js:117:  const cam = state.render && state.render.camera;
src\render\shipPreview.js:118:  const factory = (state.render && state.render.vf) || null;
src\render\shipPreview.js:122:  for (let i = 0; i < 40 && !state.render.envMap; i++) await new Promise((r) => setTimeout(r, 100));
src\render\shipPreview.js:123:  console.log('[shipPreview] envMap', state.render.envMap ? 'ready' : 'not ready (chrome will be matte)');
src\render\renderer.js:180:    // Real shadow maps (graphics spec Workstream G). Gated behind settings.video.shadows (default
src\render\renderer.js:185:    const shadowsOn = !(state.settings && state.settings.video && state.settings.video.shadows === false);
src\render\renderer.js:263:          // Re-apply bloom + video settings (tone mapping / exposure live on settings:changed).
src\render\renderer.js:264:          bus.emit('settings:changed', { section: 'video' });
src\render\renderer.js:281:    state.render.authoredPartLibraryReady = this.authoredPartLibraryReady;
src\render\renderer.js:284:    state.render.sectorPalette = corePalette;
src\render\renderer.js:298:    // the render system handle (state.render.debug.toggle) — wired to F7 in ui/input.js.
src\render\renderer.js:328:        settings: () => ({ video: { ...((state.settings && state.settings.video) || {}) } }),
src\render\renderer.js:332:          bloomSelected: !!(this.bloom && state.settings && state.settings.video && state.settings.video.bloom !== false),
src\render\renderer.js:337:      state.render.diagnostics = this.diag;
src\render\renderer.js:341:    state.render.scene = scene;
src\render\renderer.js:342:    state.render.renderer = renderer;
src\render\renderer.js:343:    state.render.camera = cam.obj;
src\render\renderer.js:344:    state.render.cameraCtrl = cam;   // controller (addTrauma/pushZoom) — exposed for feel.js / ui
src\render\renderer.js:345:    state.render.vf = vf;   // exposed for the dev-only ship turntable preview (shipPreview.js)
src\render\renderer.js:346:    state.render.warmPostProcess = () => (this.bloom && state.settings.video.bloom !== false ? this.bloom.render(scene, cam.obj) : renderer.render(scene, cam.obj));
src\render\renderer.js:351:    state.render.debug = {
src\render\renderer.js:373:    bus.on('ship:appearanceChanged', ({ id }) => render.rebuildShipMesh(id));
src\render\renderer.js:382:    bus.on('settings:changed', (p) => {
src\render\renderer.js:384:      const vd = state.settings.video;
src\render\renderer.js:395:        const camObj = state.render.camera;
src\render\renderer.js:451:      state.render.envMap = envMap;
src\render\renderer.js:703:    this.state.render.sectorPalette = palette;
src\render\renderer.js:765:    // supersedes the monolithic bloom wrapper. It is opt-in behind settings.video.renderGraph so the
src\render\renderer.js:769:    if (this.state.settings.video.renderGraph && this._ensureRenderGraph()) {
src\render\renderer.js:772:    } else if (this.bloom && this.state.settings.video.bloom !== false) {
src\render\renderer.js:890:  // back to bloom/straight-render. Options mirror the bloom/quality settings where they overlap.
src\render\renderer.js:895:      const v = this.state.settings.video || {};
src\render\renderer.js:907:      this.state.render.renderGraph = this._renderGraph;
src\render\renderer.js:918:  const vd = (state.settings && state.settings.video) || {};
src\render\shipShot.js:12:  const renderer = state.render && state.render.renderer;
src\render\shipShot.js:13:  const scene = state.render && state.render.scene;
src\render\shipShot.js:14:  const cam = state.render && state.render.camera;
src\render\shipShot.js:15:  const vf = state.render && state.render.vf;
src\render\shipShot.js:19:  for (let i = 0; i < 40 && !state.render.envMap; i++) await new Promise((r) => setTimeout(r, 100));
src\render\shipShot.js:20:  console.log('[shipShot] envMap', state.render.envMap ? 'ready' : 'not ready');
src\render\shipShot.js:69:  const video = (state.settings && state.settings.video) || {};
src\render\shipShot.js:83:  video.bloom = true; if (state.render.bloom) state.render.bloom.setOptions && state.render.bloom.setOptions({ bloom: true });
src\render\shipShot.js:98:      console.log('[shipShot] diagnostics: calls=' + report.render.calls + ' tris=' + report.render.triangles + ' lastMs=' + (report.frameMs && report.frameMs.last));

codex
The camera follow target is `state.camera.focus`; `createChaseCamera(...).follow()` updates it after resolving player position, velocity look-ahead, aim bias, threat/tether composition, and safe-rect clamping. I’ll use that same focus vector rather than touching `camera.js`.
codex
I’ve got the shape now: three scene groups, four max draw calls, quality controls by draw ranges/instance counts, and palette retint driven from `state.render.sectorPalette` identity. I’m going to write the new module and the checker, then thread the renderer hook in with just an import, init, and update call.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content scripts\\check-sector-palettes.mjs" in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content scripts\\check-hitch-budget.mjs" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 957ms:
import { SECTORS, SECTOR_PALETTE_CLASSES } from '../src/data/sectors.js';

const REQUIRED_FIELDS = ['key', 'rim', 'fill', 'ambient', 'fog', 'fogDensity', 'nebulaTint', 'dust'];
const COLOR_FIELDS = ['key', 'rim', 'fill', 'ambient', 'fog', 'nebulaTint', 'dust'];
const CLASS_ENTRIES = Object.entries(SECTOR_PALETTE_CLASSES);
const CORE_LUMINANCE = combinedLightLuminance(SECTOR_PALETTE_CLASSES.core);
const MIN_LUMINANCE = CORE_LUMINANCE * 0.8;
const MAX_LUMINANCE = CORE_LUMINANCE * 1.2;

const issues = [];
const usedClasses = new Set();

for (const sector of SECTORS) {
  const palette = sector.palette;
  if (!palette || typeof palette !== 'object') {
    issues.push(`${sector.id}: missing palette block`);
    continue;
  }

  for (const field of REQUIRED_FIELDS) {
    if (!(field in palette)) issues.push(`${sector.id}: palette missing ${field}`);
  }
  for (const field of COLOR_FIELDS) {
    if (!isColorHex(palette[field])) issues.push(`${sector.id}: palette.${field} must be a 0x000000-0xffffff number`);
  }
  if (!Number.isFinite(palette.fogDensity) || palette.fogDensity <= 0) {
    issues.push(`${sector.id}: palette.fogDensity must be a positive finite number`);
  }

  const className = classNameForPalette(palette);
  if (!className) issues.push(`${sector.id}: palette does not match an authored class`);
  else usedClasses.add(className);

  const luminance = combinedLightLuminance(palette);
  if (luminance < MIN_LUMINANCE || luminance > MAX_LUMINANCE) {
    issues.push(`${sector.id}: ambient+fill luminance ${luminance.toFixed(4)} outside ${MIN_LUMINANCE.toFixed(4)}-${MAX_LUMINANCE.toFixed(4)}`);
  }
}

for (const [className] of CLASS_ENTRIES) {
  if (!usedClasses.has(className)) issues.push(`palette class unused: ${className}`);
}

if (issues.length) {
  console.error(`Sector palette check failed (${issues.length} issue${issues.length === 1 ? '' : 's'}):`);
  for (const issue of issues) console.error(`- ${issue}`);
  process.exit(1);
}

console.log(`Sector palettes OK: ${SECTORS.length} sectors, classes used: ${[...usedClasses].sort().join(', ')}`);

function classNameForPalette(palette) {
  for (const [className, classPalette] of CLASS_ENTRIES) {
    if (samePalette(palette, classPalette)) return className;
  }
  return null;
}

function samePalette(a, b) {
  return REQUIRED_FIELDS.every((field) => a[field] === b[field]);
}

function isColorHex(value) {
  return Number.isInteger(value) && value >= 0x000000 && value <= 0xffffff;
}

function combinedLightLuminance(palette) {
  return relativeLuminance(palette.ambient) + relativeLuminance(palette.fill);
}

function relativeLuminance(hex) {
  const r = linearRgb((hex >> 16) & 0xff);
  const g = linearRgb((hex >> 8) & 0xff);
  const b = linearRgb(hex & 0xff);
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}

function linearRgb(channel) {
  const c = channel / 255;
  return c <= 0.04045 ? c / 12.92 : ((c + 0.055) / 1.055) ** 2.4;
}

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"node --check|execFileSync\\(process\\.execPath|import\\(\" scripts -g \"check-*.mjs\"" in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1451ms:
#!/usr/bin/env node
import assert from 'node:assert/strict';
import { spawn } from 'node:child_process';
import { mkdirSync, writeFileSync } from 'node:fs';
import { createServer as createNetServer } from 'node:net';
import { dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

import { collectPageIssues } from './lib/browser-issues.mjs';
import { loadPlaywright } from './lib/load-playwright.mjs';

const ROOT = fileURLToPath(new URL('../', import.meta.url));
const argv = parseArgs(process.argv.slice(2));
const WIDTH = Number(argv.width || 1280);
const HEIGHT = Number(argv.height || 800);
const SEED = Number(argv.seed || 47) >>> 0;
const WARMUP_MS = Number(argv.warmup || 5000);
const DURATION_MS = Number(argv.duration || 60000);
const FRAME_BUDGET_MS = Number(argv.frameBudgetMs || argv['frame-budget-ms'] || 32);
const OUT = argv.out || '.devshots/perf/hitch-budget.json';
const HEADED = !!(argv.headed || argv.headful || argv.headless === 'false');
const { chromium } = await loadPlaywright();

let server = null;
let browser = null;

try {
  server = await startFreshServer();
  browser = await chromium.launch({
    headless: !HEADED,
    args: [
      '--disable-background-timer-throttling',
      '--disable-backgrounding-occluded-windows',
      '--disable-renderer-backgrounding',
      '--disable-frame-rate-limit',
      '--disable-gpu-vsync',
      '--disable-features=CalculateNativeWinOcclusion',
      '--ignore-gpu-blocklist',
      '--enable-webgl',
      `--window-size=${WIDTH},${HEIGHT}`,
    ],
  });
  const page = await browser.newPage({ viewport: { width: WIDTH, height: HEIGHT }, deviceScaleFactor: 1 });
  const issues = collectPageIssues(page);

  await page.addInitScript(() => {
    try { sessionStorage.setItem('sf.cinematicSeen', '1'); } catch (_) {}
  });
  await page.goto(withDebugFlight(server.baseUrl), { waitUntil: 'domcontentloaded' });
  await page.waitForFunction(() => window.SF && window.SF.state && window.SF.bus && window.SF.state.render && window.SF.state.render.renderer, null, { timeout: 90000 });
  await page.waitForFunction(() => {
    const overlay = document.getElementById('boot-overlay');
    return !overlay || overlay.classList.contains('hidden') || getComputedStyle(overlay).visibility === 'hidden';
  }, null, { timeout: 90000 });
  await page.evaluate((seed) => {
    window.SF.bus.emit('game:new', { name: 'Hitch Budget', seed });
    window.SF.bus.emit('ui:closeAll', {});
  }, SEED);
  await page.waitForFunction(() => {
    const sf = window.SF;
    const state = sf && sf.state;
    const player = state && state.entities && state.entities.get(state.playerId);
    return !!(state && state.mode === 'flight' && player && player.alive !== false && player.mesh);
  }, null, { timeout: 90000 });
  await dismissOnboarding(page);
  const stress = await installStressScenario(page);
  await waitForStressAssets(page);
  await warmStressPipelines(page);
  const sample = await sampleHitches(page, { warmupMs: WARMUP_MS, durationMs: DURATION_MS, frameBudgetMs: FRAME_BUDGET_MS });

  const report = {
    schema: 'spaceface.hitchBudget.v1',
    generatedAt: new Date().toISOString(),
    runner: { width: WIDTH, height: HEIGHT, seed: SEED, warmupMs: WARMUP_MS, durationMs: DURATION_MS, frameBudgetMs: FRAME_BUDGET_MS, headless: !HEADED },
    scenario: stress,
    frameMs: sample.frameMs,
    histogram: sample.histogram,
    spikes: sample.spikes.slice(0, 20),
    topSpikeSources: sample.topSpikeSources,
    diagnostics: sample.diagnostics,
    pageIssues: { errors: issues.errorIssues(), warnings: issues.warningIssues().slice(0, 12) },
    pass: sample.frameMs.overBudget === 0 && issues.errorIssues().length === 0,
  };

  mkdirSync(dirname(OUT), { recursive: true });
  writeFileSync(OUT, JSON.stringify(report, null, 2));

  printReport(report);
  assert.deepEqual(report.pageIssues.errors, [], 'browser run should not report page errors');
  assert.equal(report.frameMs.overBudget, 0, `expected zero post-warmup frames > ${FRAME_BUDGET_MS} ms`);
} finally {
  if (browser) await browser.close();
  if (server && server.kill) server.kill();
}

async function installStressScenario(page) {
  return page.evaluate(({ seed }) => {
    const sf = window.SF;
    const state = sf.state;
    const player = state.entities.get(state.playerId);
    if (!player) throw new Error('player not available for hitch stress setup');

    const byDistance = (a, b) => distSq(a, player) - distSq(b, player);
    const asteroidIds = state.entityList
      .filter((entity) => entity && entity.alive !== false && entity.type === 'asteroid')
      .sort(byDistance)
      .slice(0, 10)
      .map((entity) => entity.id);
    const enemyIds = state.entityList
      .filter((entity) => entity && entity.alive !== false && entity.type === 'ship' && entity.id !== player.id && entity.team !== player.team)
      .sort(byDistance)
      .slice(0, 6)
      .map((entity) => entity.id);
    state.player.targetId = enemyIds[0] || null;
    window.__SF_HITCH_STRESS__ = {
      asteroidIds,
      enemyIds,
      nextAsteroid: 0,
      nextEnemy: 0,
      nextWeapon: 0,
      nextBurstAt: 0,
      nextMiningStartAt: 0,
      weapons: ['wpn_pulse_laser_m', 'wpn_autocannon_m', 'wpn_plasma_cannon_m', 'wpn_railgun_m', 'wpn_missile_rack_m', 'wpn_heavy_beam_l'],
    };
    return {
      baseScenario: state.scenario && state.scenario.active && state.scenario.active.id || 'debug-flight',
      asteroids: asteroidIds.length,
      enemies: enemyIds.length,
      seed,
      video: { ...((state.settings && state.settings.video) || {}) },
      entityCount: state.entityList.length,
      injectedEntities: 0,
    };

    function distSq(entity, origin) {
      const dx = (entity.pos && entity.pos.x || 0) - (origin.pos && origin.pos.x || 0);
      const dz = (entity.pos && entity.pos.z || 0) - (origin.pos && origin.pos.z || 0);
      return dx * dx + dz * dz;
    }
  }, { seed: SEED });
}

async function waitForStressAssets(page) {
  await page.waitForFunction(() => {
    const sf = window.SF;
    const stress = window.__SF_HITCH_STRESS__;
    if (!sf || !stress) return false;
    const state = sf.state;
    const render = sf.registry && sf.registry.get && sf.registry.get('render');
    if (render && render._meshBuildQueue && render._meshBuildQueue.length > 0) return false;
    for (const id of stress.enemyIds || []) {
      const entity = state.entities.get(id);
      if (!entity || entity.alive === false) continue;
      const mesh = entity.mesh;
      if (!mesh) return false;
      const assetState = mesh.userData && mesh.userData.authoredAssetState;
      if (assetState && assetState !== 'authored') return false;
    }
    return true;
  }, null, { timeout: 90000 });
}

async function warmStressPipelines(page) {
  await page.evaluate(async () => {
    const sf = window.SF;
    const state = sf && sf.state;
    const render = state && state.render;
    const renderer = render && render.renderer;
    const scene = render && render.scene;
    const camera = render && render.camera;
    if (renderer && scene && camera && typeof renderer.compileAsync === 'function') {
      await renderer.compileAsync(scene, camera, scene).catch(() => null);
    }
    if (render && typeof render.warmPostProcess === 'function') {
      render.warmPostProcess();
    }
  });
  await page.waitForTimeout(250);
}

async function sampleHitches(page, opts) {
  return page.evaluate(({ warmupMs, durationMs, frameBudgetMs }) => new Promise((resolve) => {
    const started = performance.now();
    const frames = [];
    const spikes = [];
    let last = null;
    let sampleStart = null;
    let finished = false;
    let watchdog = null;

    function resetRuntimeProbes() {
      try { if (window.__THREE_GAME_DIAGNOSTICS__ && window.__THREE_GAME_DIAGNOSTICS__.reset) window.__THREE_GAME_DIAGNOSTICS__.reset(); } catch (_) {}
      try { if (window.__SPACEFACE_PERF__ && window.__SPACEFACE_PERF__.reset) window.__SPACEFACE_PERF__.reset(); } catch (_) {}
    }

    function pumpStress(now) {
      const sf = window.SF;
      const stress = window.__SF_HITCH_STRESS__;
      if (!sf || !stress || now < stress.nextBurstAt) return;
      stress.nextBurstAt = now + 120;
      const state = sf.state;
      const player = state.entities.get(state.playerId);
      if (!player) return;
      const asteroidId = stress.asteroidIds[stress.nextAsteroid++ % Math.max(1, stress.asteroidIds.length)];
      const enemyId = stress.enemyIds[stress.nextEnemy++ % Math.max(1, stress.enemyIds.length)];
      const asteroid = state.entities.get(asteroidId);
      const enemy = state.entities.get(enemyId);
      if (enemy) state.player.targetId = enemy.id;
      if (asteroid) {
        if (now >= stress.nextMiningStartAt) {
          stress.nextMiningStartAt = now + 1400;
          sf.bus.emit('mining:start', { targetId: asteroid.id });
        }
        sf.bus.emit('mining:tick', { targetId: asteroid.id, oreId: asteroid.data && asteroid.data.typeId || 'ast_metallic' });
        if ((stress.nextAsteroid % 5) === 0) {
          sf.bus.emit('mining:yield', { targetId: asteroid.id, oreId: asteroid.data && asteroid.data.typeId || 'ast_metallic', qty: 2 });
        }
      }
      const target = enemy || asteroid || player;
      const dx = (target.pos.x || 0) - player.pos.x;
      const dz = (target.pos.z || 0) - player.pos.z;
      const dir = Math.atan2(dz, dx);
      const weaponId = stress.weapons[stress.nextWeapon++ % stress.weapons.length];
      const origin = { x: player.pos.x + Math.cos(dir) * 18, z: player.pos.z + Math.sin(dir) * 18 };
      sf.bus.emit('combat:fire', { ownerId: player.id, weaponId, hardpointIdx: 0, origin, dir });
      sf.bus.emit('projectile:hit', { ownerId: player.id, targetId: target.id, weaponId, damageType: weaponId.includes('plasma') ? 'thermal' : 'energy', pos: { x: target.pos.x, z: target.pos.z } });
    }

    function finish() {
      if (finished) return;
      finished = true;
      if (watchdog != null) clearTimeout(watchdog);
      try { if (window.SF) window.SF.bus.emit('mining:stop', {}); } catch (_) {}
      const sorted = frames.slice().sort((a, b) => a - b);
      const frameMs = {
        samples: frames.length,
        avg: round(avg(frames)),
        min: round(frames.length ? sorted[0] : 0),
        max: round(frames.length ? sorted[sorted.length - 1] : 0),
        p50: round(percentile(sorted, 0.50)),
        p95: round(percentile(sorted, 0.95)),
        p99: round(percentile(sorted, 0.99)),
        over16_7: frames.filter((value) => value > 16.7).length,
        over24: frames.filter((value) => value > 24).length,
        overBudget: frames.filter((value) => value > frameBudgetMs).length,
        over40: frames.filter((value) => value > 40).length,
        over50: frames.filter((value) => value > 50).length,
      };
      const diagnostics = readDiagnostics();
      resolve({
        frameMs,
        histogram: histogram(frames),
        spikes: spikes.sort((a, b) => b.ms - a.ms),
        topSpikeSources: topSpikeSources(diagnostics.perf),
        diagnostics,
      });
    }

    function tick(now) {
      if (finished) return;
      const elapsed = now - started;
      if (sampleStart == null) {
        if (elapsed < warmupMs) {
          pumpStress(now);
        } else if (runtimeSettled()) {
          const stress = window.__SF_HITCH_STRESS__;
          if (stress) stress.nextBurstAt = now + 120;
          sampleStart = now;
          last = now;
          resetRuntimeProbes();
          watchdog = setTimeout(finish, durationMs + 1000);
        }
      } else {
        pumpStress(now);
        const dt = now - last;
        frames.push(dt);
        if (dt > frameBudgetMs) {
          spikes.push({
            atMs: round(now - sampleStart),
            ms: round(dt),
            entityCount: window.SF && window.SF.state && window.SF.state.entityList ? window.SF.state.entityList.length : 0,
          });
        }
        last = now;
      }
      if (sampleStart != null && now - sampleStart >= durationMs) finish();
      else requestAnimationFrame(tick);
    }

    requestAnimationFrame(tick);

    function readDiagnostics() {
      const diag = window.__THREE_GAME_DIAGNOSTICS__;
      const d = diag && typeof diag.getReport === 'function' ? diag.getReport() : {};
      const perf = window.__SPACEFACE_PERF__ && typeof window.__SPACEFACE_PERF__.getReport === 'function'
        ? window.__SPACEFACE_PERF__.getReport()
        : d.perf || {};
      return {
        frameMs: d.frameMs || {},
        render: d.render || {},
        memory: d.memory || {},
        counts: d.counts || {},
        perf,
      };
    }

    function runtimeSettled() {
      const sf = window.SF;
      const state = sf && sf.state;
      const render = sf && sf.registry && typeof sf.registry.get === 'function' ? sf.registry.get('render') : null;
      if (!state || !render) return false;
      if (render._meshBuildQueue && render._meshBuildQueue.length > 0) return false;
      if (render._meshReconcileDirty) return false;
      for (const entity of state.entityList || []) {
        if (!entity || entity.alive === false || entity._noMesh || entity.type === 'fx') continue;
        if (!entity.mesh) return false;
        if (entity.type === 'ship') {
          const assetState = entity.mesh.userData && entity.mesh.userData.authoredAssetState;
          if (assetState && assetState !== 'authored') return false;
        }
      }
      return true;
    }

    function topSpikeSources(perf) {
      const phases = perf && perf.phases || {};
      const out = [
        { name: 'render', ms: phases.render && phases.render.max },
        { name: 'vfx', ms: phases.vfx && phases.vfx.max },
        { name: 'sim-frame', ms: phases.simFrame && phases.simFrame.max },
        { name: 'ui', ms: phases.ui && phases.ui.max },
        { name: 'feel', ms: phases.feel && phases.feel.max },
        { name: 'untracked-callback', ms: perf && perf.frameUntracked && perf.frameUntracked.max },
      ].filter((entry) => Number.isFinite(entry.ms));
      return out.sort((a, b) => b.ms - a.ms).slice(0, 3).map((entry) => ({ name: entry.name, ms: round(entry.ms) }));
    }

    function histogram(values) {
      const bins = [
        { label: '0-8', max: 8, count: 0 },
        { label: '8-12', max: 12, count: 0 },
        { label: '12-16.7', max: 16.7, count: 0 },
        { label: '16.7-24', max: 24, count: 0 },
        { label: '24-32', max: 32, count: 0 },
        { label: '32-40', max: 40, count: 0 },
        { label: '40-50', max: 50, count: 0 },
        { label: '50+', max: Infinity, count: 0 },
      ];
      for (const value of values) {
        const bin = bins.find((entry) => value <= entry.max) || bins[bins.length - 1];
        bin.count++;
      }
      return bins;
    }

    function avg(values) {
      return values.length ? values.reduce((sum, value) => sum + value, 0) / values.length : 0;
    }

    function percentile(sorted, p) {
      if (!sorted.length) return 0;
      const idx = Math.min(sorted.length - 1, Math.max(0, Math.floor((sorted.length - 1) * p)));
      return sorted[idx];
    }

    function round(value) {
      return Number.isFinite(value) ? Math.round(value * 100) / 100 : value;
    }
  }), opts);
}

async function dismissOnboarding(page) {
  await page.evaluate(() => {
    try {
      window.SF.bus.emit('ui:closeAll', {});
      window.SF.bus.emit('onboarding:dismiss', {});
    } catch (_) {}
  });
}

function printReport(report) {
  console.log(`[hitch-budget] scenario=${report.scenario.baseScenario} enemies=${report.scenario.enemies} asteroids=${report.scenario.asteroids}`);
  console.log(`[hitch-budget] frames=${report.frameMs.samples} avg=${report.frameMs.avg}ms p95=${report.frameMs.p95}ms p99=${report.frameMs.p99}ms worst=${report.frameMs.max}ms`);
  console.log(`[hitch-budget] >${report.runner.frameBudgetMs}ms=${report.frameMs.overBudget} >40ms=${report.frameMs.over40} >50ms=${report.frameMs.over50}`);
  console.log('[hitch-budget] histogram:');
  for (const bin of report.histogram) console.log(`  ${bin.label.padEnd(8)} ${bin.count}`);
  if (!report.pass) {
    console.log('[hitch-budget] top spike sources:');
    for (const source of report.topSpikeSources) console.log(`  ${source.name.padEnd(18)} ${source.ms}ms`);
  }
  console.log(`[hitch-budget] report: ${OUT}`);
}

async function startFreshServer() {
  const port = await findFreePort(8721);
  const url = `http://127.0.0.1:${port}/`;
  const child = spawnProbeServer(port);
  await waitForReachable(url, child);
  return { baseUrl: url, kill: () => child.kill() };
}

function spawnProbeServer(port) {
  const child = spawn(process.execPath, ['server.js', String(port)], {
    cwd: ROOT,
    stdio: ['ignore', 'pipe', 'pipe'],
    windowsHide: true,
  });
  let output = '';
  const capture = (chunk) => { output = (output + String(chunk)).slice(-5000); };
  child.stdout.on('data', capture);
  child.stderr.on('data', capture);
  child.probeOutput = () => output.trim();
  return child;
}

async function waitForReachable(url, child) {
  for (let i = 0; i < 80; i++) {
    if (child.exitCode != null) {
      throw new Error(`Dev server exited before becoming reachable at ${url}\n${child.probeOutput ? child.probeOutput() : ''}`);
    }
    if (await reachable(url)) return;
    await sleep(250);
  }
  child.kill();
  throw new Error(`Dev server did not become reachable at ${url}`);
}

async function reachable(url) {
  try {
    const res = await fetch(url, { method: 'GET' });
    return !!res.ok;
  } catch (_) {
    return false;
  }
}

async function findFreePort(start) {
  for (let port = start; port < start + 200; port++) {
    if (await isPortFree(port)) return port;
  }
  throw new Error(`No free local port found starting at ${start}`);
}

function isPortFree(port) {
  return new Promise((resolve) => {
    const server = createNetServer();
    server.once('error', () => resolve(false));
    server.once('listening', () => server.close(() => resolve(true)));
    server.listen(port, '127.0.0.1');
  });
}

function withDebugFlight(url) {
  const u = new URL(url);
  u.searchParams.set('debug', 'flight');
  return String(u);
}

function parseArgs(args) {
  const out = {};
  for (const arg of args) {
    if (!arg.startsWith('--')) continue;
    const eq = arg.indexOf('=');
    if (eq >= 0) out[arg.slice(2, eq)] = arg.slice(eq + 1);
    else out[arg.slice(2)] = true;
  }
  return out;
}

function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

 succeeded in 1317ms:
scripts\check-47a-counter-tether-live.mjs:149:  const stdout = execFileSync(process.execPath, args, {
scripts\check-47a-visual-assets.mjs:26:const THREE = await import('three');
scripts\check-47a-visual-assets.mjs:28:const { build47aScenarioProp, SCENARIO_47A_PROP_ASSET_IDS } = await import('../src/render/scenarioProps47a.js');
scripts\check-47a-visual-assets.mjs:29:const { installVisualOverrides } = await import('../src/render/visualOverrides.js');
scripts\check-47a-visual-assets.mjs:30:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-47a-tactics.mjs:250:  const stdout = execFileSync(process.execPath, args, {
scripts\check-47a-live-branch-predicate.mjs:258:  const stdout = execFileSync(process.execPath, args, {
scripts\check-bundle.mjs:64:// 3. main.js is valid (esbuild produced parseable output — node --check is a cheap proxy).
scripts\check-bundle.mjs:65:// (Skipped: node --check on an ES module with import statements works, but the bundle uses relative
scripts\check-collision-debug.mjs:31:const THREE = await import('three');
scripts\check-collision-debug.mjs:33:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-collision-debug.mjs:34:const { createCollisionDebug } = await import('../src/render/collisionDebug.js');
scripts\check-concord-patrol.mjs:29:const THREE = await import('three');
scripts\check-concord-patrol.mjs:31:const { buildConcordPatrol } = await import('../src/render/ships/concordPatrol.js');
scripts\check-concord-patrol.mjs:32:const { installVisualOverrides } = await import('../src/render/visualOverrides.js');
scripts\check-concord-patrol.mjs:33:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-confirm-dialog-safety.mjs:95:    const mod = await import('/src/ui/confirm.js');
scripts\check-data.mjs:25:    const m = await import(path);
scripts\check-kestrel-damage.mjs:32:const THREE = await import('three');
scripts\check-kestrel-damage.mjs:35:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-kestrel-damage.mjs:36:const { damageStateFor } = await import('../src/render/ships/kestrelDamage.js');
scripts\check-kestrel-silhouette.mjs:34:const THREE = await import('three');
scripts\check-kestrel-silhouette.mjs:37:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-kestrel-leak.mjs:31:const THREE = await import('three');
scripts\check-kestrel-leak.mjs:33:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-lod.mjs:31:const THREE = await import('three');
scripts\check-lod.mjs:33:const { createLodState, projectedWidthPx, attachLodState } = await import('../src/render/lod.js');
scripts\check-lod.mjs:34:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-kestrel-hero.mjs:32:const THREE = await import('three');
scripts\check-kestrel-hero.mjs:35:const { buildKestrelHero } = await import('../src/render/ships/kestrelHero.js');
scripts\check-kestrel-hero.mjs:36:const { installVisualOverrides, isReleaseAssetMode } = await import('../src/render/visualOverrides.js');
scripts\check-kestrel-hero.mjs:37:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-phase0-slice-contract.mjs:189:const longBranchInspect = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:244:const inspect = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:296:const sfInspect = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:316:const trace = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:365:const sfTrace = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:400:const profile = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:430:const sfProfile = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:453:const sfReplayVerify = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:484:const sfDiffReplay = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:510:const sfValidateAsset = JSON.parse(execFileSync(process.execPath, [
scripts\check-phase0-slice-contract.mjs:530:const compare = JSON.parse(execFileSync(process.execPath, [
scripts\check-faction-ships.mjs:26:const THREE = await import('three');
scripts\check-faction-ships.mjs:28:const { buildMeridianTrader } = await import('../src/render/ships/meridianTrader.js');
scripts\check-faction-ships.mjs:29:const { buildDriftBarge } = await import('../src/render/ships/driftBarge.js');
scripts\check-faction-ships.mjs:30:const { buildQuietRaider } = await import('../src/render/ships/quietRaider.js');
scripts\check-faction-ships.mjs:31:const { buildVaelSniper } = await import('../src/render/ships/vaelSniper.js');
scripts\check-faction-ships.mjs:32:const { installVisualOverrides } = await import('../src/render/visualOverrides.js');
scripts\check-faction-ships.mjs:33:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-reaver-pirate.mjs:25:const THREE = await import('three');
scripts\check-reaver-pirate.mjs:27:const { buildReaverPirate } = await import('../src/render/ships/reaverPirate.js');
scripts\check-reaver-pirate.mjs:28:const { installVisualOverrides } = await import('../src/render/visualOverrides.js');
scripts\check-reaver-pirate.mjs:29:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-sg05-scenario.mjs:179:  const out = JSON.parse(execFileSync(process.execPath, [
scripts\check-sg06-intake.mjs:216:  const { TacticalAIStack } = await import('../src/ai/stack.js');
scripts\check-sg05-runtime.mjs:147:  return JSON.parse(execFileSync(process.execPath, args, {
scripts\check-sg05-branch-policies.mjs:167:  const stdout = execFileSync(process.execPath, args, {
scripts\check-sg08-golden-trace.mjs:25:const raw = execFileSync(process.execPath, args, {
scripts\check-ship-builders.mjs:2:// catching runtime errors (missing helpers, bad geometry args, undefined refs) that node --check
scripts\check-ship-builders.mjs:42:const THREE = await import('three');
scripts\check-ship-builders.mjs:45:const assert = (await import('node:assert/strict')).default;
scripts\check-ship-builders.mjs:46:const { createVisualFactory } = await import('../src/render/visualFactory.js');
scripts\check-ship-builders.mjs:47:const { ENEMY_TYPES } = await import('../src/data/enemies.js');
scripts\check-ship-builders.mjs:48:const { SHIPS } = await import('../src/data/ships.js');
scripts\check-ship-builders.mjs:49:const kit = await import('../src/render/ships/shipKit.js');
scripts\check-station-interact-undock.mjs:27:const { createUiInput } = await import('../src/ui/input.js');
scripts\check-ui-screen-imports.mjs:31:    const mod = await import(path);
scripts\check-ui-screen-imports.mjs:294:  const { respawnToastText } = await import('../src/ui/hud.js');
scripts\check-ui-screen-imports.mjs:391:  const { storyProgressLabel, deathCauseLabel, lastDeathSummary } = await import('../src/ui/screens/gameOver.js');

codex
I’m editing now. The module keeps all mutable render state in preallocated typed arrays, scratch vectors, quaternions, matrices, and colors; density changes only adjust visibility/counts, so the settings path does not rebuild GPU objects.
apply patch
patch: completed
C:\Users\93rob\Documents\GitHub\SpaceFace\src\render\parallaxLayers.js
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}

diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}

apply patch
patch: completed
C:\Users\93rob\Documents\GitHub\SpaceFace\src\render\renderer.js
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The renderer hook is in at three lines. I’m adding the checker now; it uses a tiny canvas stub for the procedural textures, imports the module in Node, and verifies the high/low/reduced-motion counts without needing a WebGL context.
apply patch
patch: completed
C:\Users\93rob\Documents\GitHub\SpaceFace\scripts\check-parallax-layers.mjs
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The checker is in. I’m adding the npm script next, then I’ll run the requested verification exactly and report the known-red `check:non-graphics` tail instead of chasing unrelated gates.
apply patch
patch: completed
C:\Users\93rob\Documents\GitHub\SpaceFace\package.json
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The requested files are now written. I’m starting with the new focused checker so any module/API/count issue gets caught before the broader known-red gate.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'node scripts/check-parallax-layers.mjs' in C:\Users\93rob\Documents\GitHub\SpaceFace
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 1604ms:
Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The focused checker passes. Now I’m running the requested non-graphics gate with a last-five-lines tail; if it hits the known 47a reds, I’ll stop there and report that tail.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'node scripts/check-parallax-layers.mjs; if ($LASTEXITCODE -eq 0) { npm run check:non-graphics 2>&1 | Select-Object -Last 5 }' in C:\Users\93rob\Documents\GitHub\SpaceFace
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 exited 1 in 34681ms:
Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap
  operator: '==',
  diff: 'simple'
}

Node.js v24.15.0

codex
The combined verification is still running inside `check:non-graphics`. The new parallax check already printed green; I’m waiting for the gate tail now.
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The non-graphics command exited red and the captured tail is just the final assertion footer plus `Node.js v24.15.0`, matching the “known-red, do not chase” instruction. I’m doing a quick diff read now to make sure the touched surface stayed inside the requested files and hook budget.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git diff -- src/render/parallaxLayers.js src/render/renderer.js scripts/check-parallax-layers.mjs package.json' in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git status --short -- src/render/parallaxLayers.js src/render/renderer.js scripts/check-parallax-layers.mjs package.json src/render/vfx.js src/render/starfield.js' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 2639ms:
warning: in the working copy of 'package.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/render/renderer.js', LF will be replaced by CRLF the next time Git touches it
diff --git a/package.json b/package.json
index 8c295e58..e7135e70 100644
--- a/package.json
+++ b/package.json
@@ -29,6 +29,7 @@
     "check:47a:visuals": "node scripts/check-47a-visual-assets.mjs",
     "check:slice-scope": "node scripts/check-slice-scope.mjs",
     "check:balance": "node scripts/balance-sim.mjs",
+    "check:mining:2": "node scripts/check-mining-2.mjs",
     "check:presentation": "node scripts/check-presentation-cues.mjs && node scripts/check-sg08-golden-trace.mjs && node scripts/check-sg08-mix-profile.mjs && node scripts/check-sg08-render-vfx.mjs",
     "check:camera": "node scripts/check-camera-composition.mjs",
     "check:sg05": "node scripts/check-sg05-scenario.mjs && node scripts/check-sg05-runtime.mjs && node scripts/check-sg05-branch-policies.mjs && node scripts/check-sg05-ui-lifecycle.mjs",
@@ -52,6 +53,7 @@
     "check:sg06:tether-break": "node scripts/check-sg06-live-tether-break.mjs",
     "check:sg06:formation": "node scripts/check-sg06-rapier-formation-convergence.mjs",
     "check:sg06:ai": "node scripts/check-sg06-ai.mjs --runs=100 --ticks=600",
+    "check:ai:telegraphs": "node scripts/check-ai-telegraphs.mjs",
     "check:ai": "npm run check:sg06:ai",
     "check:sg06": "npm run check:sg06:intake && npm run check:sg06:production-ports && npm run check:sg06:encounter-sink && npm run check:sg06:encounter-owner && npm run check:sg06:live-shadow && npm run check:sg06:registry-init && npm run check:sg06:live-registry && npm run check:sg06:tether-break && npm run check:sg06:formation && npm run check:sg06:ai",
     "check:sg03:save-reload": "node scripts/check-sg03-save-reload.mjs",
@@ -75,9 +77,12 @@
     "check:bundle": "node scripts/check-bundle.mjs",
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
+    "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
+    "check:hitch-budget": "node scripts/check-hitch-budget.mjs",
     "check:radar:perf": "node scripts/check-radar-perf.mjs",
     "check:ui:perf": "node scripts/check-ui-frame-sleep.mjs && npm run check:radar:perf",
     "check:bar:narrative": "node scripts/check-bar-canonical-contacts.mjs",
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 0fefd0e2..958a1f77 100644
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -4,6 +4,7 @@
 import * as THREE from 'three';
 import { createChaseCamera } from './camera.js';
 import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
 import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
@@ -14,21 +15,11 @@ import { projectedWidthPx } from './lod.js';
 import { createCollisionDebug } from './collisionDebug.js';
 import { installDiagnostics } from './diagnostics.js';
 import { createPlanetFactory } from './planetFactory.js';
+import { precompilePipelines } from './precompile.js';
+import { SECTOR_PALETTE_CLASSES } from '../data/sectors.js';
 
-// Map a sector's danger/tier to a nebula backdrop tint so each region of the galaxy has its own
-// color signature. Core (safe, low tier) = clean blue; industrial mid-ring = rust/amber; lawless
-// frontier = blood-red; alien/endgame tier 4+ = violet. Returns a hex string or null (default).
-function sectorNebulaTint(sector) {
-  if (!sector) return null;
-  const tier = sector.tier || 0;
-  const sec = sector.security != null ? sector.security : 1;
-  const danger = (1 - sec) + tier * 0.15; // blended danger metric
-  if (tier >= 4) return '#5a1e8a';        // violet — alien / lawless endgame (Veil, Ashfall)
-  if (danger > 0.7) return '#8a1e1e';     // blood-red — dangerous frontier (Io Reach, Sker)
-  if (danger > 0.45) return '#8a4a1e';    // rust/amber — industrial mid-ring (Vesta, Pallas)
-  if (danger > 0.2) return '#1e4a8a';     // deep blue — settled belt (Ceres, Tethys)
-  return '#1e3a6a';                        // clean blue — safe core (Helios Prime)
-}
+const SECTOR_PALETTE_LERP_SECONDS = 1.5;
+const SECTOR_LIGHT_INTENSITIES = { ambient: 0.85, key: 1.7, rim: 0.7, fill: 0.35 };
 
 // ---- contact shadow disc (module-level cache so one texture serves all entities) ----------------
 let _shadowTex = null;
@@ -43,7 +34,7 @@ const SOCKET_WORLD_POS = new THREE.Vector3();
 const SOCKET_WORLD_QUAT = new THREE.Quaternion();
 const SOCKET_WORLD_SCALE = new THREE.Vector3();
 const SOCKET_FORWARD = new THREE.Vector3();
-const RUNTIME_MESH_BUILD_BUDGET = 1;
+const RUNTIME_MESH_BUILD_BUDGET = 2;
 function getContactShadowTex() {
   if (_shadowTex) return _shadowTex;
   const c = document.createElement('canvas'); c.width = c.height = 64;
@@ -178,13 +169,14 @@ export const render = {
     const drawSize = applyRendererSize(renderer, state);
 
     const scene = new THREE.Scene();
+    const corePalette = SECTOR_PALETTE_CLASSES.core;
     // Thin fog for gentle depth cueing only — the old 0.00085 erased the entire backdrop, leaving a
     // black void. This keeps the nebula + far stars visible while still fading the deep distance.
-    scene.fog = new THREE.FogExp2(0x0a1430, 0.00026);
-    scene.add(new THREE.AmbientLight(0x42506f, 0.85));
-    const key = new THREE.DirectionalLight(0xcfe2ff, 1.7); key.position.set(60, 140, 40); scene.add(key);
-    const rim = new THREE.DirectionalLight(0x6a5cff, 0.7); rim.position.set(-70, 50, -60); scene.add(rim);
-    const fill = new THREE.DirectionalLight(0x39d0ff, 0.35); fill.position.set(20, 30, 120); scene.add(fill);
+    scene.fog = new THREE.FogExp2(corePalette.fog, corePalette.fogDensity);
+    const ambient = new THREE.AmbientLight(corePalette.ambient, SECTOR_LIGHT_INTENSITIES.ambient); scene.add(ambient);
+    const key = new THREE.DirectionalLight(corePalette.key, SECTOR_LIGHT_INTENSITIES.key); key.position.set(60, 140, 40); scene.add(key);
+    const rim = new THREE.DirectionalLight(corePalette.rim, SECTOR_LIGHT_INTENSITIES.rim); rim.position.set(-70, 50, -60); scene.add(rim);
+    const fill = new THREE.DirectionalLight(corePalette.fill, SECTOR_LIGHT_INTENSITIES.fill); fill.position.set(20, 30, 120); scene.add(fill);
 
     // Real shadow maps (graphics spec Workstream G). Gated behind settings.video.shadows (default
     // true). The key light becomes a shadow caster with a tight frustum that follows the player so
@@ -209,7 +201,8 @@ export const render = {
     }
 
     const cam = createChaseCamera(state);
-    const starfield = createStarfield(scene);
+    const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
+    parallaxLayers.init(scene, state, bus, corePalette);
     const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
@@ -288,6 +281,9 @@ export const render = {
       return null;
     });
     state.render.authoredPartLibraryReady = this.authoredPartLibraryReady;
+    this._sectorPaletteRig = createSectorPaletteRig(scene, ambient, key, rim, fill);
+    this._sectorPaletteTarget = corePalette;
+    state.render.sectorPalette = corePalette;
     this._keyLight = shadowsOn ? key : null; // referenced by _updateShadowFollow() each frame
     this._shadowSettingOn = shadowsOn;
     this._shadowReceiversDirty = true;
@@ -305,6 +301,8 @@ export const render = {
     try { this.collisionDebug = createCollisionDebug(this); }
     catch (err) { console.warn('[render] collision debug unavailable:', err); this.collisionDebug = null; }
     this._meshes = new Map(); // entityId -> Object3D
+    this._meshBuildQueue = [];
+    this._meshBuildQueuedIds = new Set();
     this._hazardVisuals = []; // hazard zone visual meshes for the current sector
     this._meshReconcileDirty = true;
     this._initialMeshReconcileComplete = false;
@@ -347,6 +345,7 @@ export const render = {
     state.render.camera = cam.obj;
     state.render.cameraCtrl = cam;   // controller (addTrauma/pushZoom) — exposed for feel.js / ui
     state.render.vf = vf;   // exposed for the dev-only ship turntable preview (shipPreview.js)
+    state.render.warmPostProcess = () => (this.bloom && state.settings.video.bloom !== false ? this.bloom.render(scene, cam.obj) : renderer.render(scene, cam.obj));
     // Collision/socket/landing debug toggle (spec §12.5), bound to F7 in ui/input.js. Capture the
     // render-system `this` once so the handle closures resolve the live collisionDebug regardless of
     // how they're invoked (method `this` would otherwise bind to the debug handle object itself).
@@ -410,13 +409,13 @@ export const render = {
       this._meshReconcileDirty = true;
       if (cam.snapToPlayer) cam.snapToPlayer();
       this._updatePlanetBodies(sector);
-      // Tint the nebula backdrop to the sector's mood so each region of the galaxy reads with its
-      // own color signature: clean-blue core → rust/amber industrial → blood-red frontier → violet
-      // alien/endgame. Drives the whole-frame atmosphere, reinforcing the core-to-frontier gradient.
-      if (this.starfield && this.starfield.setSectorTint) {
-        this.starfield.setSectorTint(sectorNebulaTint(sector));
-      }
+      this._beginSectorPaletteTransition(sector);
       this._updateHazardVisuals(sector);
+      precompilePipelines(renderer, scene, cam.obj, { sector }).catch((error) => console.warn('[render] sector pipeline precompile failed', error));
+    });
+    bus.on('jump:arrive', ({ sectorId } = {}) => {
+      const sector = sectorId && state.world && state.world.sectors ? state.world.sectors[sectorId] : null;
+      this._beginSectorPaletteTransition(sector);
     });
     bus.on('save:loaded', () => { this._meshReconcileDirty = true; });
 
@@ -428,6 +427,8 @@ export const render = {
       if (keepPlayer && id === this.state.playerId) continue;
       this.scene.remove(m); disposeObject(m); this._meshes.delete(id);
     }
+    this._meshBuildQueue.length = 0;
+    this._meshBuildQueuedIds.clear();
     // Also clear hazard zone visuals
     for (const obj of this._hazardVisuals) { this.scene.remove(obj); disposeObject(obj); }
     this._hazardVisuals = [];
@@ -462,20 +463,32 @@ export const render = {
   reconcileMeshes() {
     const state = this.state;
     const buildBudget = this._initialMeshReconcileComplete ? RUNTIME_MESH_BUILD_BUDGET : Infinity;
-    let built = 0;
-    let pendingBuilds = false;
     // remove meshes whose entity no longer exists or has died
     for (const [id, m] of this._meshes) {
       const e = state.entities.get(id);
       if (!e || e.alive === false) { this.scene.remove(m); disposeObject(m); this._meshes.delete(id); this._shadowReceiversDirty = true; }
     }
-    // build meshes for alive entities that lack one (fx are particle-managed by vfx -> mark + skip)
+    // Queue alive entities that lack meshes (fx are particle-managed by vfx -> mark + skip).
     for (const e of state.entityList) {
       if (e._noMesh || this._meshes.has(e.id)) continue;
-      if (built >= buildBudget) {
-        pendingBuilds = true;
-        continue;
+      if (!this._meshBuildQueuedIds.has(e.id)) {
+        this._meshBuildQueue.push(e.id);
+        this._meshBuildQueuedIds.add(e.id);
       }
+    }
+    const built = this._drainMeshBuildQueue(buildBudget);
+    this._meshReconcileDirty = this._meshBuildQueue.length > 0;
+    if (!this._meshReconcileDirty) this._initialMeshReconcileComplete = true;
+    return built;
+  },
+
+  _drainMeshBuildQueue(buildBudget) {
+    let built = 0;
+    while (this._meshBuildQueue.length && built < buildBudget) {
+      const id = this._meshBuildQueue.shift();
+      this._meshBuildQueuedIds.delete(id);
+      const e = this.state.entities.get(id);
+      if (!e || e.alive === false || e._noMesh || this._meshes.has(id)) continue;
       const m = this.vf.build(e);
       if (!m) { e._noMesh = true; continue; }
       m.position.set(e.pos.x, 0, e.pos.z);
@@ -488,8 +501,7 @@ export const render = {
       this._shadowReceiversDirty = true;
       built++;
     }
-    this._meshReconcileDirty = pendingBuilds;
-    if (!pendingBuilds) this._initialMeshReconcileComplete = true;
+    return built;
   },
 
   // Rebuild one ship's mesh after a hull swap or loadout change. Disposes the old Object3D, builds a
@@ -686,6 +698,38 @@ export const render = {
     }
   },
 
+  _beginSectorPaletteTransition(sector) {
+    const rig = this._sectorPaletteRig;
+    if (!rig) return;
+    const palette = sector && sector.palette ? sector.palette : SECTOR_PALETTE_CLASSES.core;
+    this.state.render.sectorPalette = palette;
+    if (palette === this._sectorPaletteTarget) return;
+
+    this._sectorPaletteTarget = palette;
+    writeRigToSectorPaletteFrame(rig.start, rig);
+    writePaletteToSectorPaletteFrame(rig.target, palette);
+    rig.elapsed = 0;
+    rig.active = true;
+
+    if (this.starfield && this.starfield.setSectorTint) {
+      this.starfield.setSectorTint(palette.nebulaTint);
+    }
+  },
+
+  _updateSectorPaletteTransition(frameDt) {
+    const rig = this._sectorPaletteRig;
+    if (!rig || !rig.active) return;
+    const dt = Number.isFinite(frameDt) ? Math.max(0, frameDt) : 0;
+    rig.elapsed = Math.min(SECTOR_PALETTE_LERP_SECONDS, rig.elapsed + dt);
+    const rawT = SECTOR_PALETTE_LERP_SECONDS > 0 ? rig.elapsed / SECTOR_PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    lerpSectorPaletteFrame(rig, rig.start, rig.target, t);
+    if (rawT >= 1) {
+      rig.active = false;
+      applySectorPaletteFrame(rig, rig.target);
+    }
+  },
+
   prepareFrame(alpha, frameDt) {
     // While the GL context is lost, the renderer can't draw — skip all per-frame work until
     // webglcontextrestored rebuilds GPU resources. (cam.follow etc. would run against a dead
@@ -700,9 +744,11 @@ export const render = {
     // Background-clock for distant animation (planet cloud drift, hero-star twinkle). Integrates real
     // frame dt scaled by state.timeScale so the cosmos respects hit-stop/pause — a death freeze
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
+    this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
     this._bgTime = (this._bgTime || 0) + frameDt * ts;
     if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
     this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
@@ -887,6 +933,90 @@ function finiteInRange(value, min, max, fallback) {
   return Math.max(min, Math.min(max, n));
 }
 
+function createSectorPaletteRig(scene, ambientLight, keyLight, rimLight, fillLight) {
+  const rig = {
+    scene,
+    lights: { ambient: ambientLight, key: keyLight, rim: rimLight, fill: fillLight },
+    start: createSectorPaletteFrame(),
+    target: createSectorPaletteFrame(),
+    elapsed: 0,
+    active: false,
+  };
+  writeRigToSectorPaletteFrame(rig.start, rig);
+  writeRigToSectorPaletteFrame(rig.target, rig);
+  return rig;
+}
+
+function createSectorPaletteFrame() {
+  return {
+    colors: {
+      ambient: new THREE.Color(),
+      key: new THREE.Color(),
+      rim: new THREE.Color(),
+      fill: new THREE.Color(),
+      fog: new THREE.Color(),
+    },
+    intensities: { ambient: 0, key: 0, rim: 0, fill: 0 },
+    fogDensity: 0,
+  };
+}
+
+function writeRigToSectorPaletteFrame(frame, rig) {
+  frame.colors.ambient.copy(rig.lights.ambient.color);
+  frame.colors.key.copy(rig.lights.key.color);
+  frame.colors.rim.copy(rig.lights.rim.color);
+  frame.colors.fill.copy(rig.lights.fill.color);
+  frame.colors.fog.copy(rig.scene.fog.color);
+  frame.intensities.ambient = rig.lights.ambient.intensity;
+  frame.intensities.key = rig.lights.key.intensity;
+  frame.intensities.rim = rig.lights.rim.intensity;
+  frame.intensities.fill = rig.lights.fill.intensity;
+  frame.fogDensity = rig.scene.fog.density;
+}
+
+function writePaletteToSectorPaletteFrame(frame, palette) {
+  frame.colors.ambient.setHex(palette.ambient);
+  frame.colors.key.setHex(palette.key);
+  frame.colors.rim.setHex(palette.rim);
+  frame.colors.fill.setHex(palette.fill);
+  frame.colors.fog.setHex(palette.fog);
+  frame.intensities.ambient = SECTOR_LIGHT_INTENSITIES.ambient;
+  frame.intensities.key = SECTOR_LIGHT_INTENSITIES.key;
+  frame.intensities.rim = SECTOR_LIGHT_INTENSITIES.rim;
+  frame.intensities.fill = SECTOR_LIGHT_INTENSITIES.fill;
+  frame.fogDensity = palette.fogDensity;
+}
+
+function applySectorPaletteFrame(rig, frame) {
+  rig.lights.ambient.color.copy(frame.colors.ambient);
+  rig.lights.key.color.copy(frame.colors.key);
+  rig.lights.rim.color.copy(frame.colors.rim);
+  rig.lights.fill.color.copy(frame.colors.fill);
+  rig.scene.fog.color.copy(frame.colors.fog);
+  rig.lights.ambient.intensity = frame.intensities.ambient;
+  rig.lights.key.intensity = frame.intensities.key;
+  rig.lights.rim.intensity = frame.intensities.rim;
+  rig.lights.fill.intensity = frame.intensities.fill;
+  rig.scene.fog.density = frame.fogDensity;
+}
+
+function lerpSectorPaletteFrame(rig, start, target, t) {
+  rig.lights.ambient.color.lerpColors(start.colors.ambient, target.colors.ambient, t);
+  rig.lights.key.color.lerpColors(start.colors.key, target.colors.key, t);
+  rig.lights.rim.color.lerpColors(start.colors.rim, target.colors.rim, t);
+  rig.lights.fill.color.lerpColors(start.colors.fill, target.colors.fill, t);
+  rig.scene.fog.color.lerpColors(start.colors.fog, target.colors.fog, t);
+  rig.lights.ambient.intensity = lerp(start.intensities.ambient, target.intensities.ambient, t);
+  rig.lights.key.intensity = lerp(start.intensities.key, target.intensities.key, t);
+  rig.lights.rim.intensity = lerp(start.intensities.rim, target.intensities.rim, t);
+  rig.lights.fill.intensity = lerp(start.intensities.fill, target.intensities.fill, t);
+  rig.scene.fog.density = lerp(start.fogDensity, target.fogDensity, t);
+}
+
+function lerp(a, b, t) {
+  return a + (b - a) * t;
+}
+
 function disposeObject(obj) {
   obj.traverse((c) => {
     if (c.isBatchedMesh && typeof c.dispose === 'function') c.dispose();

exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'npm run check:parallax' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 2768ms:
 M package.json
 M src/render/renderer.js
 M src/render/starfield.js
 M src/render/vfx.js
?? scripts/check-parallax-layers.mjs
?? src/render/parallaxLayers.js

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 4318ms:

> spaceface@0.1.0 check:parallax
> node scripts/check-parallax-layers.mjs

Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The focused npm script is green too. I noticed the diff view includes earlier uncommitted renderer/package work from the dirty tree, so I’m treating only the parallax import/init/update and `check:parallax` line as mine and leaving the rest alone.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'node --check src/render/renderer.js' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1504ms:
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'node --check scripts/check-parallax-layers.mjs' in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git diff --check -- src/render/parallaxLayers.js scripts/check-parallax-layers.mjs src/render/renderer.js package.json' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 1852ms:
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 1437ms:
warning: in the working copy of 'package.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/render/renderer.js', LF will be replaced by CRLF the next time Git touches it

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..846592a735658aa03a1c8d0f9845e9d1d8a4b110
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,468 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || {};
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || {};
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
Syntax checks passed, and diff whitespace check only produced the existing CRLF conversion warnings for `package.json` and `renderer.js`. I’m making one tiny cleanup to avoid fallback object literals in the per-frame path, then I’ll rerun the focused checker.
apply patch
patch: completed
C:\Users\93rob\Documents\GitHub\SpaceFace\src\render\parallaxLayers.js
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
That cleanup is in. Re-running the targeted checker now to make sure the small change stayed boring.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'npm run check:parallax' in C:\Users\93rob\Documents\GitHub\SpaceFace
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 5905ms:

> spaceface@0.1.0 check:parallax
> node scripts/check-parallax-layers.mjs

Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap

codex
The focused checker is green after cleanup. I’m running the requested combined verification one final time so the reported tail reflects the final files.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'node scripts/check-parallax-layers.mjs; if ($LASTEXITCODE -eq 0) { npm run check:non-graphics 2>&1 | Select-Object -Last 5 }' in C:\Users\93rob\Documents\GitHub\SpaceFace
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

codex
The final combined run has cleared the parallax check and is in `check:non-graphics` now. Same drill: I’m only capturing the last five lines.
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 exited 1 in 23518ms:
Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap
  operator: '==',
  diff: 'simple'
}

Node.js v24.15.0

codex
Final verification matches the earlier result: parallax check passes, then the non-graphics gate exits red with the requested five-line assertion footer. I’m grabbing final status for the handoff.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git status --short -- src/render/parallaxLayers.js scripts/check-parallax-layers.mjs src/render/renderer.js package.json' in C:\Users\93rob\Documents\GitHub\SpaceFace
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$i=0; Get-Content C:'"\\Users\\93rob\\.codex\\memories\\MEMORY.md | ForEach-Object { "'$i++; if (($i -ge 975 -and $i -le 983) -or ($i -ge 993 -and $i -le 995) -or ($i -ge 683 -and $i -le 683)) { '"'{0}: {1}' -f "'$i, $_ } }' in C:\Users\93rob\Documents\GitHub\SpaceFace
 succeeded in 919ms:
 M package.json
 M src/render/renderer.js
?? scripts/check-parallax-layers.mjs
?? src/render/parallaxLayers.js

diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 931ms:
683: - `npm run check:non-graphics` is the right stabilization gate here when the graphics/art lane is intentionally locked. [Task 1]
975: # Task Group: SpaceFace performance hardening and visual probe verification
976: scope: Use for `C:\Users\93rob\Documents\GitHub\SpaceFace` perf passes where the user wants real optimization without reducing visible quality, parallel subsystem investigation when justified, and proof that browser visual verification still works after render-path changes.
977: applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for performance optimization, flight visual-proof fixes, and repo-wide post-perf validation in this checkout; re-check current browser probe scripts and perf artifact schema before reusing exact thresholds or file names.
978: 
979: ## Task 1: optimize the game without downgrading visuals and repair the flight visual proof, success
980: 
981: ### rollout_summary_files
982: 
983: - rollout_summaries/2026-06-24T21-50-11-oe73-spaceface_performance_hardening_and_visual_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\24\rollout-2026-06-24T17-50-15-019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d.jsonl, updated_at=2026-06-25T06:56:19+00:00, thread_id=019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d, fixed the key perf/proof landmine by moving the visual probe off preserved backbuffer assumptions)
993: - rollout_summaries/2026-06-24T21-50-11-oe73-spaceface_performance_hardening_and_visual_probe_fix.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\24\rollout-2026-06-24T17-50-15-019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d.jsonl, updated_at=2026-06-25T06:56:19+00:00, thread_id=019efb9c-b3c8-7ba1-8bd7-ebcf1e4a0d4d, ended with repo-wide checks and careful reporting of required-budget pass versus stretch-target miss)
994: 
995: ### keywords

codex
Final status is clean for my requested surface: two new files plus the renderer/package hook lines. The broader tree still has pre-existing dirty render/assets/UI/etc. changes, and I did not touch `vfx.js` or `starfield.js`.
exec
"C:\\Users\\93rob\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$i=0; Get-Content C:'"\\Users\\93rob\\.codex\\memories\\MEMORY.md | ForEach-Object { "'$i++; if ($i -ge 661 -and $i -le 683) { '"'{0}: {1}' -f "'$i, $_ } }' in C:\Users\93rob\Documents\GitHub\SpaceFace
diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

 succeeded in 947ms:
661: # Task Group: SpaceFace mission-intro stabilization, landing pivots, and PR closeout
662: scope: Use for `C:\Users\93rob\Documents\GitHub\SpaceFace` wrap-up or stabilization runs where the user changes from open-ended autonomy to "take it in for a landing", wants the already-started fix finished without breaking the game, and needs branch/PR closeout with honest gate handling.
663: applies_to: cwd=C:\Users\93rob\Documents\GitHub\SpaceFace; reuse_rule=reuse for landing-phase stabilization, mission-intro debugging, and PR closeout in this checkout; re-check current branch state, mission scripts, and whether the graphics/art lane is intentionally locked before reusing the same gates or merge expectations.
664: 
665: ## Task 1: stabilize Beat 4 mission-intro identity and land PR #87, success
666: 
667: ### rollout_summary_files
668: 
669: - rollout_summaries/2026-06-27T08-33-40-k4Ad-spaceface_merged_pr87_story_intro_landing.md (cwd=C:\Users\93rob\Documents\GitHub\SpaceFace, rollout_path=C:\Users\93rob\.codex\sessions\2026\06\27\rollout-2026-06-27T04-33-42-019f0836-8c2b-7e23-9fcd-cfb2515025f5.jsonl, updated_at=2026-06-29T05:32:16+00:00, thread_id=019f0836-8c2b-7e23-9fcd-cfb2515025f5, merged PR #87 after tightening canonical Beat 4 intro gating and refreshing stale cached boards)
670: 
671: ### keywords
672: 
673: - SpaceFace, PR #87, Beat 4, story intro, mission standing ladder, missionPreflight, missions.js, check-mission-standing-ladder.mjs, npm run check:non-graphics, gh pr merge, advisor-artifacts
674: 
675: ## User preferences
676: 
677: - when the user says `Take it in for a landing, finish the optimizations you were working on, don't leave the game in a broken state ... eventually we have to wrap up and assess what's happening`, stop spawning new work and pivot immediately to stabilization, verification, and assessment. [Task 1]
678: - when the user starts with broad autonomous improvement but later changes the objective, treat the late objective change as a hard stop rather than a soft suggestion. [Task 1]
679: - when using ChatGPT Pro or another advisor in this repo, the user wants concrete PR-ready changes or fallback artifacts, not vague brainstorming. [Task 1]
680: 
681: ## Reusable knowledge
682: 
683: - `npm run check:non-graphics` is the right stabilization gate here when the graphics/art lane is intentionally locked. [Task 1]

codex
Done. Added `src/render/parallaxLayers.js` with 3 groups: far dust sheets, instanced mid tetra debris, and near shader point-motes with boost stretch.

Hooked `renderer.js` with import/init/update only; did not edit `vfx.js` or `starfield.js`. Added `scripts/check-parallax-layers.mjs` and `check:parallax`.

Verified `node scripts/check-parallax-layers.mjs` and `npm run check:parallax` pass. Requested combined run: parallax passes, then `check:non-graphics` exits 1 with tail: `operator: '=='`, `diff: 'simple'`, `}`, blank line, `Node.js v24.15.0`.


diff --git a/package.json b/package.json
index 73b894d42b9941d355beaa7cce8776b04e646ef4..9471df0016508be3227298ddf93ec787be3b93d9
--- a/package.json
+++ b/package.json
@@ -78,6 +78,7 @@
     "check:launch-policy": "node scripts/check-launch-policy.mjs",
     "check:non-graphics": "node scripts/check-non-graphics-ci.mjs",
     "check:sector-palettes": "node scripts/check-sector-palettes.mjs",
+    "check:parallax": "node scripts/check-parallax-layers.mjs",
     "check:assets:live": "node scripts/probe-authored-assets-live.mjs",
     "check:perf:control": "node scripts/probe-raf-control.mjs --headed --strict --width 1830 --height 973 --warmup 1000 --duration 5000",
     "check:perf": "node scripts/probe-performance-profile.mjs --headed --strict --width 1830 --height 973 --warmup 2500 --duration 7000 --diagnostic-variants",
diff --git a/scripts/check-parallax-layers.mjs b/scripts/check-parallax-layers.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..30df078bf2478b02eecf232240d334946726cd09
--- /dev/null
+++ b/scripts/check-parallax-layers.mjs
@@ -0,0 +1,100 @@
+#!/usr/bin/env node
+import assert from 'node:assert/strict';
+import { execFileSync } from 'node:child_process';
+import { fileURLToPath, pathToFileURL } from 'node:url';
+
+import * as THREE from 'three';
+
+const ROOT = fileURLToPath(new URL('../', import.meta.url));
+const MODULE_PATH = fileURLToPath(new URL('../src/render/parallaxLayers.js', import.meta.url));
+
+execFileSync(process.execPath, ['--check', MODULE_PATH], { cwd: ROOT, stdio: 'pipe' });
+installCanvasStub();
+
+const parallaxLayers = await import(pathToFileURL(MODULE_PATH).href);
+assert.equal(typeof parallaxLayers.init, 'function', 'parallaxLayers.init export missing');
+assert.equal(typeof parallaxLayers.update, 'function', 'parallaxLayers.update export missing');
+assert.equal(typeof parallaxLayers.dispose, 'function', 'parallaxLayers.dispose export missing');
+
+checkStack({ particleQuality: 'medium', motionReduce: false }, { far: 2, mid: 120, near: 200 });
+checkStack({ particleQuality: 'low', motionReduce: false }, { far: 1, mid: 60, near: 100 });
+checkStack({ particleQuality: 'low', motionReduce: true }, { far: 1, mid: 60, near: 50 });
+
+console.log('Parallax layers OK: exports, 3 groups, quality counts, reduced-motion mote cap');
+
+function checkStack(video, expected) {
+  const scene = new THREE.Scene();
+  const state = makeState(video);
+  const stack = parallaxLayers.init(scene, state, null, state.render.sectorPalette);
+
+  assert.equal(stack.groups.length, 3, `${video.particleQuality}: expected 3 parallax groups`);
+  assert.equal(scene.children.length, 3, `${video.particleQuality}: expected 3 scene children`);
+
+  const far = stack.groups.find((group) => group.userData.layer === 'farDust');
+  const mid = stack.groups.find((group) => group.userData.layer === 'midDebris');
+  const near = stack.groups.find((group) => group.userData.layer === 'nearSpeedMotes');
+  assert.ok(far, 'far dust group missing');
+  assert.ok(mid, 'mid debris group missing');
+  assert.ok(near, 'near speed-motes group missing');
+
+  assert.equal(far.userData.activeCount, expected.far, `${video.particleQuality}: far count`);
+  assert.equal(far.children.filter((child) => child.visible).length, expected.far, `${video.particleQuality}: far visible planes`);
+  assert.equal(mid.userData.activeCount, expected.mid, `${video.particleQuality}: mid count`);
+  assert.equal(mid.children[0].isInstancedMesh, true, `${video.particleQuality}: mid layer should be instanced`);
+  assert.equal(mid.children[0].count, expected.mid, `${video.particleQuality}: mid mesh count`);
+  assert.equal(near.userData.activeCount, expected.near, `${video.particleQuality}: near count`);
+  assert.equal(near.children[0].isPoints, true, `${video.particleQuality}: near layer should be points`);
+  assert.equal(near.children[0].geometry.drawRange.count, expected.near, `${video.particleQuality}: near draw range`);
+
+  parallaxLayers.update(1 / 60);
+  if (video.motionReduce) {
+    assert.equal(near.children[0].material.uniforms.uStretch.value, 1, 'reduced motion disables boost stretch');
+  } else {
+    assert.ok(near.children[0].material.uniforms.uStretch.value > 1, 'boost/high speed should stretch motes');
+  }
+
+  parallaxLayers.dispose();
+  assert.equal(scene.children.length, 0, `${video.particleQuality}: dispose should remove groups`);
+}
+
+function makeState(video) {
+  const player = {
+    id: 1,
+    pos: { x: 12, z: -18 },
+    vel: { x: 260, z: 30 },
+    flags: { boosting: true },
+  };
+  return {
+    settings: { video },
+    render: { sectorPalette: { dust: 0x425987, nebulaTint: 0x334466 } },
+    camera: { focus: { x: 320, y: 0, z: -180 } },
+    playerId: 1,
+    entities: new Map([[1, player]]),
+  };
+}
+
+function installCanvasStub() {
+  const gradient = { addColorStop() {} };
+  const ctx = {
+    fillStyle: null,
+    globalCompositeOperation: 'source-over',
+    clearRect() {},
+    createRadialGradient() { return gradient; },
+    beginPath() {},
+    arc() {},
+    fill() {},
+  };
+  globalThis.document = {
+    createElement(tag) {
+      assert.equal(tag, 'canvas', 'parallax texture helper should only create canvas elements');
+      return {
+        width: 0,
+        height: 0,
+        getContext(type) {
+          assert.equal(type, '2d', 'parallax texture helper should request a 2d canvas');
+          return ctx;
+        },
+      };
+    },
+  };
+}
diff --git a/src/render/parallaxLayers.js b/src/render/parallaxLayers.js
new file mode 100644
index 0000000000000000000000000000000000000000..47aacbc27bd2e408bf36946be50534e019a60926
--- /dev/null
+++ b/src/render/parallaxLayers.js
@@ -0,0 +1,469 @@
+import * as THREE from 'three';
+
+const PALETTE_LERP_SECONDS = 1.5;
+const FALLBACK_DUST = '#35406a';
+
+const FAR = { count: 2, factor: 0.22, tile: 3000, y: -140, size: 3000 };
+const MID = { count: 120, factor: 0.55, tile: 1700, y: -40 };
+const NEAR = { count: 200, factor: 1.35, tile: 900, y: 26 };
+const EMPTY_OBJECT = {};
+
+const DUST_VERTEX = `
+attribute float aSize;
+attribute float aAlpha;
+uniform float uSizeScale;
+uniform float uStretch;
+varying float vAlpha;
+void main() {
+  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
+  gl_PointSize = aSize * (uSizeScale / max(16.0, -mvPosition.z)) * max(1.0, uStretch);
+  gl_Position = projectionMatrix * mvPosition;
+  vAlpha = aAlpha;
+}`;
+
+const DUST_FRAGMENT = `
+precision mediump float;
+uniform vec3 uColor;
+uniform float uStretch;
+uniform vec2 uDir;
+varying float vAlpha;
+void main() {
+  vec2 dir = normalize(uDir);
+  vec2 side = vec2(-dir.y, dir.x);
+  vec2 p = gl_PointCoord * 2.0 - 1.0;
+  float along = dot(p, dir) / max(1.0, uStretch);
+  float across = dot(p, side);
+  float d = along * along + across * across;
+  float alpha = smoothstep(1.0, 0.05, d) * vAlpha;
+  if (alpha <= 0.01) discard;
+  gl_FragColor = vec4(uColor, alpha);
+}`;
+
+let active = null;
+
+export function init(scene, state, bus, initialPalette) {
+  if (active) active.dispose();
+  active = new ParallaxLayers(scene, state, bus, initialPalette);
+  return active;
+}
+
+export function update(dt) {
+  if (active) active.update(dt);
+}
+
+export function dispose() {
+  if (!active) return;
+  active.dispose();
+  active = null;
+}
+
+class ParallaxLayers {
+  constructor(scene, state, bus, initialPalette) {
+    this.scene = scene;
+    this.state = state || {};
+    this.bus = bus || null;
+    this.groups = [];
+    this._layers = [];
+    this._farMaterials = [];
+    this._farTextures = [];
+
+    this._colorStart = new THREE.Color();
+    this._colorTarget = new THREE.Color();
+    this._colorCurrent = new THREE.Color();
+    this._colorScratch = new THREE.Color();
+    this._paletteIdentity = initialPalette || readPalette(this.state);
+    resolvePaletteColor(this._colorCurrent, this._paletteIdentity);
+    this._colorStart.copy(this._colorCurrent);
+    this._colorTarget.copy(this._colorCurrent);
+    this._paletteElapsed = PALETTE_LERP_SECONDS;
+    this._paletteActive = false;
+
+    this._matrix = new THREE.Matrix4();
+    this._quat = new THREE.Quaternion();
+    this._axis = new THREE.Vector3();
+    this._pos = new THREE.Vector3();
+    this._scale = new THREE.Vector3();
+    this._screenDir = new THREE.Vector2(0, -1);
+    this._lastVelX = 0;
+    this._lastVelZ = 1;
+    this._stretch = 1;
+    this._qualityLow = null;
+    this._motionReduce = null;
+
+    this._createFarDust();
+    this._createMidDebris();
+    this._createNearMotes();
+    this._syncQuality();
+    this._applyPaletteColor(this._colorCurrent);
+  }
+
+  update(dt) {
+    const frameDt = Number.isFinite(dt) && dt > 0 ? Math.min(dt, 0.1) : 0;
+    this._detectPaletteChange();
+    this._syncQuality();
+
+    const camera = this.state.camera || EMPTY_OBJECT;
+    const focus = camera.focus || camera.obj && camera.obj.position || null;
+    const x = focus && Number.isFinite(focus.x) ? focus.x : 0;
+    const z = focus && Number.isFinite(focus.z) ? focus.z : 0;
+    for (let i = 0; i < this._layers.length; i++) {
+      const layer = this._layers[i];
+      layer.group.position.x = wrapOffset(x * (1 - layer.factor), layer.tile);
+      layer.group.position.z = wrapOffset(z * (1 - layer.factor), layer.tile);
+    }
+
+    this._updatePalette(frameDt);
+    this._updateDebris(frameDt);
+    this._updateMoteStretch(frameDt);
+  }
+
+  dispose() {
+    for (let i = 0; i < this.groups.length; i++) {
+      const group = this.groups[i];
+      if (this.scene) this.scene.remove(group);
+      disposeGroup(group);
+    }
+    this.groups.length = 0;
+    this._layers.length = 0;
+    this._farMaterials.length = 0;
+    this._farTextures.length = 0;
+  }
+
+  _createFarDust() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_FarDust';
+    group.renderOrder = -9;
+    group.userData.layer = 'farDust';
+    group.userData.factor = FAR.factor;
+    group.userData.tileSize = FAR.tile;
+    group.userData.baseCount = FAR.count;
+    group.userData.activeCount = FAR.count;
+
+    const geometry = new THREE.PlaneGeometry(FAR.size, FAR.size, 1, 1);
+    for (let i = 0; i < FAR.count; i++) {
+      const texture = makeDustSheetTexture(i + 17);
+      const material = new THREE.MeshBasicMaterial({
+        map: texture,
+        color: this._colorCurrent,
+        transparent: true,
+        opacity: i === 0 ? 0.26 : 0.2,
+        blending: THREE.AdditiveBlending,
+        depthWrite: false,
+        depthTest: true,
+        side: THREE.DoubleSide,
+        fog: false,
+      });
+      const plane = new THREE.Mesh(geometry, material);
+      plane.name = `Parallax_FarDust_${i}`;
+      plane.position.set(i === 0 ? -280 : 360, FAR.y, i === 0 ? 210 : -330);
+      plane.rotation.x = -Math.PI / 2;
+      plane.rotation.z = i === 0 ? 0.18 : -0.31;
+      plane.renderOrder = -9;
+      plane.frustumCulled = false;
+      group.add(plane);
+      this._farMaterials.push(material);
+      this._farTextures.push(texture);
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: FAR.factor, tile: FAR.tile });
+    this._farGroup = group;
+  }
+
+  _createMidDebris() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_MidDebris';
+    group.renderOrder = -6;
+    group.userData.layer = 'midDebris';
+    group.userData.factor = MID.factor;
+    group.userData.tileSize = MID.tile;
+    group.userData.baseCount = MID.count;
+    group.userData.activeCount = MID.count;
+
+    const geometry = new THREE.TetrahedronGeometry(1, 0);
+    const material = new THREE.MeshBasicMaterial({
+      color: 0x7d88a6,
+      transparent: true,
+      opacity: 0.36,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+    const mesh = new THREE.InstancedMesh(geometry, material, MID.count);
+    mesh.name = 'Parallax_MidDebris_Instances';
+    mesh.renderOrder = -6;
+    mesh.frustumCulled = false;
+    mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
+    group.add(mesh);
+
+    this._debrisMesh = mesh;
+    this._debrisX = new Float32Array(MID.count);
+    this._debrisZ = new Float32Array(MID.count);
+    this._debrisRadius = new Float32Array(MID.count);
+    this._debrisAxisX = new Float32Array(MID.count);
+    this._debrisAxisY = new Float32Array(MID.count);
+    this._debrisAxisZ = new Float32Array(MID.count);
+    this._debrisSpeed = new Float32Array(MID.count);
+    this._debrisAngle = new Float32Array(MID.count);
+
+    const rnd = makeRand(0x47a2e1);
+    for (let i = 0; i < MID.count; i++) {
+      this._debrisX[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisZ[i] = (rnd() - 0.5) * MID.tile;
+      this._debrisRadius[i] = 0.6 + rnd() * 1.6;
+      let ax = rnd() * 2 - 1;
+      let ay = rnd() * 2 - 1;
+      let az = rnd() * 2 - 1;
+      const len = Math.hypot(ax, ay, az) || 1;
+      ax /= len; ay /= len; az /= len;
+      this._debrisAxisX[i] = ax;
+      this._debrisAxisY[i] = ay;
+      this._debrisAxisZ[i] = az;
+      this._debrisSpeed[i] = 0.035 + rnd() * 0.09;
+      this._debrisAngle[i] = rnd() * Math.PI * 2;
+    }
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: MID.factor, tile: MID.tile });
+    this._midGroup = group;
+    this._updateDebris(0);
+  }
+
+  _createNearMotes() {
+    const group = new THREE.Group();
+    group.name = 'Parallax_NearSpeedMotes';
+    group.renderOrder = 4;
+    group.userData.layer = 'nearSpeedMotes';
+    group.userData.factor = NEAR.factor;
+    group.userData.tileSize = NEAR.tile;
+    group.userData.baseCount = NEAR.count;
+    group.userData.activeCount = NEAR.count;
+
+    const positions = new Float32Array(NEAR.count * 3);
+    const sizes = new Float32Array(NEAR.count);
+    const alphas = new Float32Array(NEAR.count);
+    const rnd = makeRand(0xe147ac);
+    for (let i = 0; i < NEAR.count; i++) {
+      positions[i * 3] = (rnd() - 0.5) * NEAR.tile;
+      positions[i * 3 + 1] = NEAR.y + (rnd() - 0.5) * 10;
+      positions[i * 3 + 2] = (rnd() - 0.5) * NEAR.tile;
+      sizes[i] = 2.2 + rnd() * 3.8;
+      alphas[i] = 0.25 + rnd() * 0.45;
+    }
+
+    const geometry = new THREE.BufferGeometry();
+    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
+    geometry.setAttribute('aSize', new THREE.BufferAttribute(sizes, 1));
+    geometry.setAttribute('aAlpha', new THREE.BufferAttribute(alphas, 1));
+    geometry.setDrawRange(0, NEAR.count);
+
+    const material = new THREE.ShaderMaterial({
+      uniforms: {
+        uColor: { value: this._colorCurrent.clone() },
+        uSizeScale: { value: 420 },
+        uStretch: { value: 1 },
+        uDir: { value: this._screenDir },
+      },
+      vertexShader: DUST_VERTEX,
+      fragmentShader: DUST_FRAGMENT,
+      transparent: true,
+      blending: THREE.AdditiveBlending,
+      depthWrite: false,
+      depthTest: true,
+      fog: false,
+    });
+
+    const points = new THREE.Points(geometry, material);
+    points.name = 'Parallax_NearSpeedMotes_Points';
+    points.renderOrder = 4;
+    points.frustumCulled = false;
+    group.add(points);
+
+    this._motePoints = points;
+    this._moteGeometry = geometry;
+    this._moteMaterial = material;
+
+    this.scene.add(group);
+    this.groups.push(group);
+    this._layers.push({ group, factor: NEAR.factor, tile: NEAR.tile });
+    this._nearGroup = group;
+  }
+
+  _syncQuality() {
+    const video = this.state.settings && this.state.settings.video || EMPTY_OBJECT;
+    const low = video.particleQuality === 'low';
+    const motionReduce = video.motionReduce === true;
+    if (low === this._qualityLow && motionReduce === this._motionReduce) return;
+    this._qualityLow = low;
+    this._motionReduce = motionReduce;
+
+    const farCount = low ? Math.max(1, Math.floor(FAR.count * 0.5)) : FAR.count;
+    const midCount = low ? Math.max(1, Math.floor(MID.count * 0.5)) : MID.count;
+    let nearCount = low ? Math.max(1, Math.floor(NEAR.count * 0.5)) : NEAR.count;
+    if (motionReduce) nearCount = Math.max(1, Math.floor(nearCount * 0.5));
+
+    if (this._farGroup) {
+      for (let i = 0; i < this._farGroup.children.length; i++) {
+        this._farGroup.children[i].visible = i < farCount;
+      }
+      this._farGroup.userData.activeCount = farCount;
+    }
+    if (this._debrisMesh) {
+      this._debrisMesh.count = midCount;
+      if (this._midGroup) this._midGroup.userData.activeCount = midCount;
+    }
+    if (this._moteGeometry) {
+      this._moteGeometry.setDrawRange(0, nearCount);
+      if (this._nearGroup) this._nearGroup.userData.activeCount = nearCount;
+    }
+  }
+
+  _detectPaletteChange() {
+    const palette = readPalette(this.state);
+    if (!palette || palette === this._paletteIdentity) return;
+    this._paletteIdentity = palette;
+    this._colorStart.copy(this._colorCurrent);
+    resolvePaletteColor(this._colorTarget, palette);
+    this._paletteElapsed = 0;
+    this._paletteActive = true;
+  }
+
+  _updatePalette(dt) {
+    if (!this._paletteActive) return;
+    this._paletteElapsed = Math.min(PALETTE_LERP_SECONDS, this._paletteElapsed + dt);
+    const rawT = PALETTE_LERP_SECONDS > 0 ? this._paletteElapsed / PALETTE_LERP_SECONDS : 1;
+    const t = rawT * rawT * (3 - 2 * rawT);
+    this._colorCurrent.lerpColors(this._colorStart, this._colorTarget, t);
+    this._applyPaletteColor(this._colorCurrent);
+    if (rawT >= 1) {
+      this._paletteActive = false;
+      this._colorCurrent.copy(this._colorTarget);
+      this._applyPaletteColor(this._colorCurrent);
+    }
+  }
+
+  _applyPaletteColor(color) {
+    for (let i = 0; i < this._farMaterials.length; i++) {
+      this._farMaterials[i].color.copy(color);
+    }
+    if (this._moteMaterial) this._moteMaterial.uniforms.uColor.value.copy(color);
+  }
+
+  _updateDebris(dt) {
+    const mesh = this._debrisMesh;
+    if (!mesh) return;
+    const count = mesh.count;
+    for (let i = 0; i < count; i++) {
+      this._debrisAngle[i] += this._debrisSpeed[i] * dt;
+      this._axis.set(this._debrisAxisX[i], this._debrisAxisY[i], this._debrisAxisZ[i]);
+      this._quat.setFromAxisAngle(this._axis, this._debrisAngle[i]);
+      this._pos.set(this._debrisX[i], MID.y, this._debrisZ[i]);
+      const s = this._debrisRadius[i];
+      this._scale.set(s, s, s);
+      this._matrix.compose(this._pos, this._quat, this._scale);
+      mesh.setMatrixAt(i, this._matrix);
+    }
+    mesh.instanceMatrix.needsUpdate = true;
+  }
+
+  _updateMoteStretch(dt) {
+    if (!this._moteMaterial) return;
+    const player = this.state.entities && this.state.playerId != null
+      ? this.state.entities.get(this.state.playerId)
+      : null;
+    const vx = player && player.vel && Number.isFinite(player.vel.x) ? player.vel.x : 0;
+    const vz = player && player.vel && Number.isFinite(player.vel.z) ? player.vel.z : 0;
+    const speed = Math.hypot(vx, vz);
+    if (speed > 1) {
+      this._lastVelX = vx / speed;
+      this._lastVelZ = vz / speed;
+    }
+
+    const boosting = !!(player && player.flags && player.flags.boosting);
+    let targetStretch = 1;
+    if (!this._motionReduce && (boosting || speed > 200)) {
+      const speedBoost = Math.max(0, Math.min(1.5, (speed - 200) / 260));
+      targetStretch = 2.2 + speedBoost + (boosting ? 0.7 : 0);
+    }
+    const t = dt > 0 ? 1 - Math.exp(-8 * dt) : 1;
+    this._stretch += (targetStretch - this._stretch) * t;
+    this._moteMaterial.uniforms.uStretch.value = this._stretch;
+
+    const sx = this._lastVelX;
+    const sy = -this._lastVelZ * 0.62;
+    const len = Math.hypot(sx, sy) || 1;
+    this._screenDir.set(sx / len, sy / len);
+  }
+}
+
+function readPalette(state) {
+  return state && state.render ? state.render.sectorPalette : null;
+}
+
+function resolvePaletteColor(target, palette) {
+  const value = palette && (palette.dust != null ? palette.dust : palette.nebulaTint);
+  try { target.set(value != null ? value : FALLBACK_DUST); }
+  catch (_) { target.set(FALLBACK_DUST); }
+  return target;
+}
+
+function wrapOffset(value, tile) {
+  return value - Math.floor(value / tile) * tile - tile * 0.5;
+}
+
+function makeRand(seed) {
+  let s = seed >>> 0;
+  return () => {
+    s = (s * 1664525 + 1013904223) >>> 0;
+    return s / 4294967296;
+  };
+}
+
+function makeDustSheetTexture(seed) {
+  const canvas = document.createElement('canvas');
+  canvas.width = 512;
+  canvas.height = 512;
+  const ctx = canvas.getContext && canvas.getContext('2d');
+  if (ctx) {
+    const rnd = makeRand(seed);
+    ctx.clearRect(0, 0, 512, 512);
+    ctx.globalCompositeOperation = 'lighter';
+    const blobs = 2 + (rnd() > 0.5 ? 1 : 0);
+    for (let i = 0; i < blobs; i++) {
+      const x = 110 + rnd() * 300;
+      const y = 120 + rnd() * 280;
+      const r = 190 + rnd() * 170;
+      const g = ctx.createRadialGradient(x, y, 0, x, y, r);
+      g.addColorStop(0, 'rgba(255,255,255,0.34)');
+      g.addColorStop(0.42, 'rgba(255,255,255,0.13)');
+      g.addColorStop(1, 'rgba(255,255,255,0)');
+      ctx.fillStyle = g;
+      ctx.beginPath();
+      ctx.arc(x, y, r, 0, Math.PI * 2);
+      ctx.fill();
+    }
+    ctx.globalCompositeOperation = 'source-over';
+  }
+  const texture = new THREE.CanvasTexture(canvas);
+  texture.colorSpace = THREE.SRGBColorSpace;
+  texture.needsUpdate = true;
+  return texture;
+}
+
+function disposeGroup(group) {
+  group.traverse((obj) => {
+    if (obj.geometry) obj.geometry.dispose();
+    if (obj.material) {
+      const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
+      for (let i = 0; i < materials.length; i++) {
+        const material = materials[i];
+        if (!material) continue;
+        if (material.map) material.map.dispose();
+        material.dispose();
+      }
+    }
+  });
+}
diff --git a/src/render/renderer.js b/src/render/renderer.js
index 7b83534145db2cec91608a3a2a42dfe1ec083a9d..dc117105fbd4c8ebb49d7c913a0c74c5f8b5761a
--- a/src/render/renderer.js
+++ b/src/render/renderer.js
@@ -1,10 +1,11 @@
 // Render system: owns the WebGLRenderer, scene, lights, camera, starfield, and the entity→mesh
 // lifecycle. Exposes worldToScreen / raycastToPlane via ctx.helpers and a renderFrame() the loop
 // calls each animation frame. Sim never touches this; it's all in renderFrame (ARCHITECTURE §1,§2.4).
-import * as THREE from 'three';
-import { createChaseCamera } from './camera.js';
-import { createStarfield } from './starfield.js';
-import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
+import * as THREE from 'three';
+import { createChaseCamera } from './camera.js';
+import { createStarfield } from './starfield.js';
+import * as parallaxLayers from './parallaxLayers.js';
+import { createVisualFactory, setEnvMapForShips, invalidateVisualFactoryCaches } from './visualFactory.js';
 import { installVisualOverrides } from './visualOverrides.js';
 import { createBloom } from './bloom.js';
 import { SpaceRenderGraph } from './post/spaceRenderGraph.js';
@@ -199,9 +200,10 @@
       key.target = new THREE.Object3D(); scene.add(key.target);
     }
 
-    const cam = createChaseCamera(state);
+    const cam = createChaseCamera(state);
     const starfield = createStarfield(scene, { tint: corePalette.nebulaTint });
-    const vf = createVisualFactory();
+    parallaxLayers.init(scene, state, bus, corePalette);
+    const vf = createVisualFactory();
     // Hero-asset registry (spec §17.3): wraps the factory's build() so the bespoke player Kestrel is
     // intercepted before the procedural visualFactory. Narrow + failure-isolated — any throw falls
     // back to the original procedural builder, so non-Kestrel entities are completely unaffected.
@@ -744,9 +746,10 @@
     // momentarily stills the clouds too, keeping the backdrop in the same time model as the action.
     this._updateSectorPaletteTransition(frameDt);
     const ts = (this.state.timeScale != null) ? this.state.timeScale : 1;
-    this._bgTime = (this._bgTime || 0) + frameDt * ts;
-    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
-    this._updatePlanetParallax();
+    this._bgTime = (this._bgTime || 0) + frameDt * ts;
+    if (this.starfield.update) this.starfield.update(frameDt, this._bgTime);
+    parallaxLayers.update(frameDt);
+    this._updatePlanetParallax();
     this._syncShadowMapEnabled();
     // Shadow follow (graphics spec G): keep the key light's shadow frustum centered on the player
     // so the tight 1400-unit ortho box always covers the local action. DirectionalLight position is

SUCCESS: The process with PID 10836 (child process of PID 22424) has been terminated.
SUCCESS: The process with PID 9844 (child process of PID 22424) has been terminated.
tokens used
154,938
Done. Added `src/render/parallaxLayers.js` with 3 groups: far dust sheets, instanced mid tetra debris, and near shader point-motes with boost stretch.

Hooked `renderer.js` with import/init/update only; did not edit `vfx.js` or `starfield.js`. Added `scripts/check-parallax-layers.mjs` and `check:parallax`.

Verified `node scripts/check-parallax-layers.mjs` and `npm run check:parallax` pass. Requested combined run: parallax passes, then `check:non-graphics` exits 1 with tail: `operator: '=='`, `diff: 'simple'`, `}`, blank line, `Node.js v24.15.0`.


