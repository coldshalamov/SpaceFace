// Shipped Brazilian Portuguese catalog. Built from the English inventory through the translation pipeline.
import { buildTranslatedCatalog } from '../pipeline.js';

export const locale = 'pt-BR';
export const messages = buildTranslatedCatalog('pt-BR');
export default messages;
