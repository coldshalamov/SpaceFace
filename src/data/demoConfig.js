// Demo build links (ZERO_TO_HERO Phase 5.5): where the end card sends the player when it
// closes. Blank hides the verb on the card — the shipped defaults carry neither.
export const DEMO_CONFIG = Object.freeze({
  feedbackUrl: '',
  storeUrl: '',
});
