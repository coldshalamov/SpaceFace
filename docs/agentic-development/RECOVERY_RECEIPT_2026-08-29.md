<!-- LIFETIME: EVIDENCE -->
# 2026-08-29 Pro-work recovery receipt

This receipt records the recovery/publication failure that led to the Central Brain follow-up work so later agents do not mistake the scaffold, the recovered architecture, and the routing migration for the same delivery.

## Sequence

1. A long Pro-model audit/research pass inspected the SpaceFace planning, queue, observability, deterministic lab, performance, flight, VFX, graphics and agent-dispatch systems, but failed to publish its implementation. The delivered bundle/PR claim was not backed by a real repository publication.
2. PR #107 later landed the first executable scaffold: Central Brain compact doctrine, quality-workstream/scenario registries, advisory manager, session reducer, graphics marginal-value stopping law, packet temporal-scenario requirement and self-test.
3. The deeper recovered architecture was reconstructed from the retained Pro analysis and live repository seams: nine durable plans under `docs/agentic-development/` plus restored plan inventory, run comparison, runtime-log analysis, next-work selection and control-plane validation utilities.
4. Those recovered plans/tools were merged to master after #107 as `ff56e9db...`; `f6584b08...` added a non-destructive Central Brain hook to the existing long canonical build map.
5. The final migration is a separate follow-up because #107 was already merged: make broad unnamed development quality-ranked rather than hard-coded to PQ-050, rewrite the generic agent prompts accordingly, remove the failed one-time migration workflow, and retain the full architecture under `docs/agentic-development/`.

## Truth boundary

- PR #107 is the initial scaffold, not the full recovered Pro work.
- The nine deep architecture documents and five restored tools are the recovered design/tooling layer.
- The final routing migration changes **which existing admitted work broad agents choose**; it does not create another queue, acceptance system, or product authority.
- `program-queue.json`, active PQ packets, current user direction, and normal receipts remain authoritative.

## Verification boundary

This receipt proves publication/accounting only. Gameplay acceptance still belongs to the normal deterministic/live evidence routes. The Central Brain's planned observability wiring and autonomous playtesting are staged implementation work, not claimed complete merely because the architecture documents exist.
