# Foundation Handoff Intake Ledger

Snapshot date: 2026-06-21.

This ledger records external SG handoff intake status for the active 47-A vertical slice. It is an integration aid, not a substitute for `docs/Spec/MASTER_MAKEOVER_PLAN.md`. A handoff is not accepted because a branch, zip, or report exists; it is accepted only when its stated acceptance evidence passes in this repository and the resulting surface improves a named slice metric.

## Current Intake Status

| Job | Artifact inspected | Status | Integration boundary |
| --- | --- | --- | --- |
| SG-04 authored asset pipeline | Current master GLTFLoader/asset pipeline + Kestrel reference asset + modular parts manifest | Partial validation + release fallback + prefab manifest gate accepted | The Kestrel hero GLB and manifest are validated by both the legacy art checks and the agent-facing `sf validate asset` JSON contract. `node scripts/check-kestrel-hero.mjs` now proves the dev seam catches hero-builder failures with a procedural fallback while release mode (`?prod=1`, explicit global flag, or release override) fails instead of hiding a missing/broken Kestrel hero. `node scripts/check-parts-manifest.mjs` validates the wider modular prefab manifest against all committed part GLBs, including transport, counts, budgets, coordinates, materials, hooks, sockets, bounds, embedded textures, and runtime slot coverage. KTX2/Draco/Meshopt release gating remains pending. |
| SG-08 presentation orchestration | `origin/commission/sg-08-presentation-orchestration` plus current master runtime bridge | Phase 0 cue runtime + adapter fanout + golden timing + mix profile accepted | The received branch contributed the presentation cue schema/validator surface; current master now adds deterministic cue recipes, a headless `presentationOrchestrator` that maps scenario, tether, shield-collapse, and subsystem-disable events to normalized `presentation:cue` records, and `presentationAdapters` that fan accepted cues into camera, VFX, audio, UI, and accessibility bus outputs with reduced-motion/reduced-flash transforms. `npm run check:presentation`, `npm run check:sg05`, `npm run check:sim`, and `node scripts/check-phase0-slice-contract.mjs` prove the 47-A cue contract, including two Phase 0 golden cues at 720 ticks, applied-output trace counts, ordered timing between source events and camera/VFX/audio/UI/accessibility outputs, four semantic cues through `scavenger_arrival`, and a worst-combat SG-08 mix profile with clipping, voice-budget, finite-envelope, ducking, and critical-cue masking checks. Bespoke renderer particle/light consumers and audiovisual capture beyond the headless golden trace remain pending. |
| SG-05 scenario/mission/dialogue DSL | Current master 47-A scenario contract + runtime bridge | Phase 0 live contract + branch lifecycle policies accepted with headless evidence | `src/data/scenarios/47a.scenario.json` now declares the canonical 47-A actors, world facts, presentation event ids, proof metrics, eight beat windows, and four outcome branches with authored offer/active/reminder/fail/abandon/complete/aftermath lifecycle text. `npm run check:sg05` validates required beats/branches, reference integrity, critical SG-08 cue reservation, immediate world-fact consequences for every branch, complete lifecycle text for every branch, the `sf-sim 47a` runtime bridge, and a four-policy branch replay suite. The bridge loads the validated contract, emits deterministic `scenario:*` trace events, snapshots/saves/reloads scenario state, binds all eight required Phase 0 actors, proves beat progression through `scavenger_arrival`, records hostile fire from a scenario-bound scavenger inside the 90s proof window, and proves escape/surrender/destroy/deliver policies mutate immediate world facts while preserving branch resolution and lifecycle/aftermath payloads through save/load. Full DSL runtime, localization, content hot reload, AI-driven live actor migration, authored dialogue execution, and UI/runtime rendering of the lifecycle text remain pending. |
| SG-03 combat/action/subsystem framework | `SpaceFace-SG-03-handoff.zip` plus current master integration | Landed | Shared ActionDef runtime, combat grammar, damage routing, subsystem/status/attachment semantics, deterministic combat trace, and v5 semantic combat save/reload are integrated. Live movement/constraint effects route through SG-02's `helpers.combatPhysics` port. Live projectile/beam hits now carry canonical `DamagePacket` snapshots while scalar hit fields remain compatibility-only; `npm run check:combat` includes the save/reload fixture, and `node scripts/check-gameplay-core.mjs` proves packet forwarding/routing. |
| SG-02 dynamic Rapier authority | `origin/sg-02-rapier-dynamic-authority`; recovered zip `6D3FE3C...E474AF` | Default authority accepted | The recovered package is integrated as the default `rapier-dynamic` backend: command/telemetry membrane, dynamic body owner, Massline tether constraints, SG-03 `helpers.combatPhysics`, save schema v4/v5 reload support, Massline break telemetry gate, SG-01 folded quantized body snapshots, dynamic pickup contact parity, live projectile contact parity, docking/gate range parity, and acceptance tests are gated by `npm run check:sg02` plus `npm run check:combat`. `npm run check:sg02` now includes the 47-A default dynamic replay gate, proving uninterrupted-vs-reload hash parity with folded SG-02 body snapshots. The old custom/observer path is no longer the gameplay default; keep it only for explicit compatibility probes until a later deletion pass proves all non-slice callers have migrated. |
| SG-06 layered tactical AI | `SpaceFace-SG-06.zip` (`F10CC6B0...20A358D5`) | Default tactical AI accepted | The five-layer director/squad/perception/utility/maneuver stack, SG-03 action adapter, inspection endpoint, trace ring, handoff doc, clean-room ledger, 100-seed acceptance harness, production-shaped `helpers.aiSensors`/`helpers.aiRoster`/`helpers.aiManeuver`/`helpers.aiEncounter` ports, the encounter-sink gate, the active encounter-owner gate, production-spawn tactical capability metadata, the live-shadow SG-03 ActionDef gate, the lazy registry-init gate, the production `sg06-tactical` registry gate, the default tactical Massline overload gate, the save/load transient reset gate, and the standalone Rapier formation-convergence gate are integrated and gated by `npm run check:sg06` plus `node scripts/check-gameplay-core.mjs`. New default gameplay uses SG-06 with SG-02 dynamic authority, while legacy FSM code is no longer the default production slot. Do not wire AI-only damage, motion, or hidden-state paths. |

## Accepted Evidence On Master

- SG-08 recipe/runtime cue + adapter fanout + golden timing + mix profile gate: `npm run check:presentation`; 47-A cue replay: `npm run check:sg05`, `npm run check:sim`, and `node scripts/check-phase0-slice-contract.mjs`.
- SG-05 Phase 0 scenario + runtime bridge + branch policy gate: `npm run check:sg05`.
- SG-03 grammar + semantic combat save/reload gate: `npm run check:combat`; live weapon packet bridge: `node scripts/check-gameplay-core.mjs`.
- SG-04 asset CLI gate: `node scripts/sf.mjs validate asset assets/ships/kestrel/kestrel_reference.glb`; hero startup fallback gate: `node scripts/check-kestrel-hero.mjs`; modular prefab manifest gate: `node scripts/check-parts-manifest.mjs`.
- SG-02 partial intake + command membrane gate: `npm run check:sg02`.
- SG-06 production-port + port-level AI gate: `npm run check:sg06`.
- 47-A replay gate: `npm run check:sim`.
- Save/reload parity gate: `npm run check:sim:compare`.
- Long replay gate: `npm run check:sim:long`.
- Agent-facing trace gate: `node scripts/sf-sim.mjs trace 47a --seed 47 --ticks 720 --inputs test/47a.inputs.json --events scenario.*,tether.*,combat.*,story.*,presentation.* --limit 220`.
- Full local evidence bundle: `npm run check`.

## Standing Rules For New Handoffs

- Read the handoff package first: interface spec, ownership table, schema/version changes, migration plan, tests, benchmark, inspection endpoint, limitations, reference ledger, fixture, and deletion list.
- Run the handoff acceptance test before integration. If it fails, record the exact failing contract instead of papering over it.
- Integrate on master only after the acceptance surface is clear. Migrate the 47-A actors that use the new contract and delete superseded paths in the same milestone.
- No temporary physics, mission, UI, or asset formats while waiting for the real SG owner. Missing SG-02 runtime authority stays pending, not half-implemented.
- Every accepted handoff must leave an agent-facing evidence surface: replay hash, event trace, combat trace, scenario trace, cue trace, profile, or validation report.
