<!-- LIFETIME: VOLATILE — owner-triage stamp after independent review of this ledger. -->
# Owner triage (2026-08-10)

Independent review of this `review/` ledger. **Use SUMMARY as a menu, not a mandate.**

## Executed (safe packet)

- `lossLedger` Concord/Drift headlines → canonical `faction_scn` / `faction_dmc` (legacy aliases kept).
- ARCHITECTURE truth-up: loop cap 4; §2.3/§4.2 → pointers to `authoritativeSystemManifest.js`;
  14 factions / 24 sectors; deleted nonexistent `shaders.js` row; magnet default note.
- Stale `customsRisk.js` header comment corrected (hidden hold IS wired).
- Outfitting / fit-tree no longer advertise inert `magnetRange` module stats.
- Module catalog comment notes magnetRange is unwired.

## Rejected / demoted (do not “fix” from SUMMARY)

| SUMMARY # | Decision |
|---|---|
| 6 intervention `_nextId` | **Rejected.** Interventions cleared on Continue; not a save-collision bug. |
| 4 `shaders.js` | **Demoted.** Doc cleanup only (done). Not a runtime bug. |
| 1 “one-line glob all tests” | **Rejected as a first move.** Orphan count is real; blind glob will red CI. Audit/wire in clusters later. |
| 7 map `\|\| true` | **Demoted.** Placeholder with a real assert below. Living-galaxy soft authored check is a separate soft hole — not in this packet. |
| 2 drillFadeStart | **Deferred.** Real contract issue; not a safe no-behavior change. |
| 3 wire magnetRange into mining | **Deferred.** Balance/feel change. Honesty UI hide done instead. |
| 8–13 | **Deferred.** CI wiring, goldens, event-table dump, magic strings — not safe-for-sure. |

## Ledger hygiene notes

- Seed section of `FINDINGS.md` still contains overturned claims (e.g. pitborn starts 0,
  sector count 10). Prefer the **VERIFIED** block and this triage file over seed rows.
- Exact “~51% tests dead” count is soft; direction (many unwired) is correct.
- `FIXES_PROPOSED.md` Category A docs items partially landed via this packet; do not re-run blindly.
