<!-- LIFETIME: STABLE -->
# `skills/` — vendored generic kit, not SpaceFace policy

This tree is a third-party Three.js/GameForge skill pack kept for license/provenance. It is **not**
SpaceFace law, not a workflow, and not a reason to start a greenfield Three.js game inside this repo.

- Do not load these `SKILL.md` files for SpaceFace work.
- Root and nested `AGENTS.md` files win. Blender/GLB work uses
  `.grok/skills/spaceface-blender-material-truth/SKILL.md`.
- Ordinary search already excludes this directory (`.ignore`, `docs/SEARCH_CONTEXT.md`).

Keep or delete the package as one unit (`docs/REPOSITORY_HYGIENE.md`). Do not mix it into live
instruction routing.
